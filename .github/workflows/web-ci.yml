# CI placeholder: Web (Next.js)
name: web-ci
on: [push, pull_request]
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: placeholder
        run: echo "Web CI — Next.js build, lint, test. Replace with actual steps."
