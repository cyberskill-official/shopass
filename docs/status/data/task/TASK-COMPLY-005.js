window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-COMPLY-005"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service COMPLY <strong>MUST</strong> cưỡng chế hai bất biến bảo mật của SănDeal bằng kiểm tra tự động: (a) no-cleartext - mật khẩu chỉ tồn tại dạng argon2id, secrets chỉ trong Vault; (b) token-not-on-server - token phiên sàn không bao giờ rời client và không có mặt ở backend. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> cung cấp bộ quét tĩnh <code>audit.Scan(root string) ([]Finding, error)</code> quét cây mã + migration tìm pattern cấm, trả vi phạm kèm <code>file</code> + <code>line</code> + <code>rule</code>.</li><li><strong>MUST</strong> định nghĩa tập quy tắc cấm (DEC-COMPLY-19) tối thiểu:</li></ol>\n<ul><li>Cột/biến tên gợi ý lưu cleartext mật khẩu (ví dụ <code>password TEXT</code> không phải <code>pwd_hash</code>, <code>plain_password</code>).</li><li>Cột/biến/log lưu token phiên sàn (ví dụ <code>shopee_token</code>, <code>session_cookie</code>, <code>platform_access_token</code> ở backend).</li><li>Secret hardcode (khóa API/DB password chuỗi literal) thay vì đọc từ Vault (TASK-INFRA-003).</li><li>Băm mật khẩu yếu (<code>md5</code>, <code>sha1</code>) cho credential.</li></ul>\n<ol><li><strong>MUST</strong> cung cấp CI gate <code>no_cleartext_gate.sh</code> gọi <code>Scan</code> và thoát mã khác 0 khi có vi phạm (DEC-COMPLY-22), chặn merge. Tích hợp vào <code>.github/workflows/ci.yml</code>.</li><li><strong>MUST</strong> cưỡng chế token-not-on-server (DEC-COMPLY-20): backend KHÔNG có cột, biến, hoặc dòng log nào chứa token phiên sàn. Quy tắc quét bắt mọi tên trường khớp danh sách token sàn.</li><li><strong>MUST</strong> cung cấp payload guard động <code>audit.GuardPayload(p map[string]any) error</code> (DEC-COMPLY-22): khẳng định payload nhận từ extension chỉ chứa trường cho phép (productId/price/qty...) và KHÔNG chứa <code>cookie</code>, <code>token</code>, <code>session</code>, <code>authorization</code> (tôn trọng TASK-EXT-003 tối thiểu hóa).</li><li><strong>MUST</strong> tái dùng argon2id PHC của TASK-AUTH-001 cho mật khẩu (DEC-COMPLY-21): quy tắc quét xác nhận không có đường ghi mật khẩu nào ngoài hàm hash chuẩn.</li><li><strong>MUST</strong> tái dùng Vault/Secrets Manager của TASK-INFRA-003 cho secrets (DEC-COMPLY-21): quy tắc quét cấm secret literal trong mã và file <code>.env</code> commit.</li><li><strong>MUST</strong> trả <code>Finding</code> xác định (ổn định thứ tự) để CI log rõ ràng và để test so khớp được.</li><li><strong>MUST</strong> phân biệt true-positive với mã hợp lệ (ví dụ biến tên <code>token</code> của JWT nội bộ KHÁC token phiên sàn): quy tắc nhắm danh sách token SÀN cụ thể, có cơ chế allowlist dòng (<code>// audit:allow &lt;lý do&gt;</code>) cho ngoại lệ có kiểm soát.</li><li><strong>SHOULD</strong> phát báo cáo tóm tắt số vi phạm theo rule khi chạy gate; log ở CI.</li><li><strong>MUST</strong> đảm bảo allowlist không thành lỗ hổng: mỗi <code>// audit:allow</code> phải kèm lý do; quét đếm và liệt kê allowlist để review định kỳ.</li><li><strong>MUST</strong> cung cấp test fixture cả hai chiều: mã vi phạm bị bắt (no false-negative trên tập đã biết), mã sạch không báo nhầm (kiểm soát false-positive).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao cưỡng chế tự động, không chỉ quy ước (DEC-COMPLY-22)?</strong> TASK-AUTH-001 nói \"dùng argon2id\" và TASK-INFRA-003 nói \"secrets trong Vault\", nhưng quy ước do con người giữ sẽ bị phá qua một commit vội. CI gate biến quy tắc thành ràng buộc máy kiểm: build đỏ nếu ai đó thêm cột <code>password TEXT</code> hay log token sàn. Đây là khác biệt giữa \"chính sách trên giấy\" và \"chính sách thực thi\".</p>\n<p><strong>Vì sao token-not-on-server là bất biến tuyệt đối (DEC-COMPLY-20)?</strong> Toàn bộ định vị niềm tin hậu-Honey của SănDeal đặt trên việc backend KHÔNG bao giờ thấy token phiên sàn của user. Extension đọc giỏ hàng bằng phiên của chính user (session piggyback) và chỉ gửi về dữ liệu tối thiểu. Nếu một token sàn lọt vào backend, lời hứa sụp đổ. Quét tĩnh bắt mọi tên trường token sàn ở backend là rào cứng.</p>\n<p><strong>Vì sao payload guard động bên cạnh quét tĩnh (§1 #5)?</strong> Quét tĩnh bắt mã; payload guard bắt dữ liệu chạy thực. Ngay cả khi mã sạch, một payload bất ngờ từ extension (do bug client) có thể mang theo cookie. Guard từ chối tại biên: backend chỉ nhận trường cho phép, drop mọi thứ khác. Hai lớp (tĩnh + động) phủ cả \"mã sai\" lẫn \"dữ liệu sai\".</p>\n<p><strong>Vì sao allowlist có lý do bắt buộc (§1 #9, #11)?</strong> Quét tĩnh sẽ có false-positive (biến <code>token</code> của JWT nội bộ hợp lệ). Cấm tuyệt đối thì gate vô dụng vì luôn đỏ; cho qua tùy tiện thì gate vô nghĩa. Giải pháp: allowlist từng dòng kèm lý do, đếm và liệt kê để review. Ngoại lệ tồn tại nhưng có dấu vết và bị soi.</p>\n<p><strong>Vì sao đây là nền cho TASK-TRUST-003 (§7)?</strong> Audit bảo mật độc lập (TASK-TRUST-003) cần chứng minh \"không gửi cookie/mật khẩu\". Bộ quét + payload guard ở đây cung cấp bằng chứng tự động, lặp lại được mà auditor có thể chạy. Cưỡng chế nội bộ làm audit độc lập đáng tin hơn.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Quy tắc + quét (Go)</h3>\n<pre><code class=\"language-go\">// services/comply/internal/audit/rules.go\ntype Rule struct {\n    Name    string\n    Pattern *regexp.Regexp\n    Hint    string\n}\n\n// Token phien SAN cu the (KHAC token JWT noi bo).\nvar bannedRules = []Rule{\n    {\"cleartext_password\",\n        regexp.MustCompile(`(?i)\\b(plain_?password|password)\\s+TEXT\\b`),\n        \"Mat khau phai la pwd_hash argon2id (TASK-AUTH-001)\"},\n    {\"platform_session_token\",\n        regexp.MustCompile(`(?i)(shopee|tiktok|lazada)_?(token|cookie|session)|platform_(access_)?token`),\n        \"Token phien san KHONG duoc co o backend (token-not-on-server)\"},\n    {\"hardcoded_secret\",\n        regexp.MustCompile(`(?i)(api_?key|db_?password)\\s*[:=]\\s*[\"'][A-Za-z0-9/+]{12,}[\"']`),\n        \"Secret phai doc tu Vault (TASK-INFRA-003)\"},\n    {\"weak_password_hash\",\n        regexp.MustCompile(`(?i)\\b(md5|sha1)\\s*\\(`),\n        \"Cam bam yeu cho credential; dung argon2id\"},\n}\n\n// services/comply/internal/audit/scan.go\ntype Finding struct {\n    File string\n    Line int\n    Rule string\n    Hint string\n}\n\n// Scan quet cay ma, bo qua dong co // audit:allow.\nfunc Scan(root string) ([]Finding, error) {\n    var out []Finding\n    // ... walk files, match rules, skip allowlisted lines, sort on dinh ...\n    return out, nil\n}</code></pre>\n<h3>Payload guard động + CI gate</h3>\n<pre><code class=\"language-go\">// services/comply/internal/audit/payload_guard.go\nvar forbiddenKeys = []string{\"cookie\", \"token\", \"session\", \"authorization\", \"set-cookie\"}\n\n// GuardPayload tu choi payload chua truong nhay cam tu extension.\nfunc GuardPayload(p map[string]any) error {\n    for k := range p {\n        lk := strings.ToLower(k)\n        for _, f := range forbiddenKeys {\n            if strings.Contains(lk, f) {\n                return fmt.Errorf(\"%w: %s\", ErrForbiddenField, k)\n            }\n        }\n    }\n    return nil\n}</code></pre>\n<pre><code class=\"language-sh\"># services/comply/scripts/no_cleartext_gate.sh\n#!/usr/bin/env bash\nset -euo pipefail\nn=$(go run ./services/comply/cmd/auditscan --root . --count)\nif [ \"$n\" -ne 0 ]; then\n  echo \"no-cleartext gate FAILED: $n vi pham (xem log tren)\"\n  exit 1\nfi\necho \"no-cleartext gate PASSED\"</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>Scan</code> trên fixture chứa <code>password TEXT</code> -&gt; trả Finding rule <code>cleartext_password</code> với đúng file/line.</li><li><code>Scan</code> trên fixture chứa <code>shopee_token</code> -&gt; trả Finding rule <code>platform_session_token</code>.</li><li><code>Scan</code> trên fixture chứa <code>api_key = \"AKIA...\"</code> -&gt; trả Finding rule <code>hardcoded_secret</code>.</li><li><code>Scan</code> trên fixture dùng <code>md5(</code> cho mật khẩu -&gt; trả Finding rule <code>weak_password_hash</code>.</li><li><code>Scan</code> trên mã sạch (chỉ <code>pwd_hash</code>, đọc secret từ Vault) -&gt; 0 Finding.</li><li>Dòng có <code>// audit:allow lý do</code> -&gt; bị bỏ qua, không tính vi phạm.</li><li><code>no_cleartext_gate.sh</code> thoát mã != 0 khi có &gt;= 1 vi phạm; = 0 khi sạch.</li><li><code>GuardPayload</code> với payload chứa <code>cookie</code> -&gt; lỗi <code>ErrForbiddenField</code>.</li><li><code>GuardPayload</code> với payload chứa <code>authorization</code> (hoa/thường) -&gt; lỗi.</li><li><code>GuardPayload</code> với payload tối thiểu (<code>productId</code>, <code>price</code>, <code>qty</code>) -&gt; nil (pass).</li><li>Thứ tự <code>Finding</code> ổn định giữa hai lần chạy cùng input (xác định).</li><li>Báo cáo gate liệt kê số vi phạm theo rule và danh sách allowlist hiện có.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/comply/internal/audit/scan_test.go\nfunc TestScan_CatchesPlatformToken(t *testing.T) {\n    dir := writeFixture(t, \"repo.go\", `var shopeeToken = readCookie()`)\n    f, _ := Scan(dir)\n    require.True(t, hasRule(f, \"platform_session_token\"))\n}\n\nfunc TestScan_CatchesCleartextPassword(t *testing.T) {\n    dir := writeFixture(t, \"schema.sql\", `CREATE TABLE u (password TEXT);`)\n    f, _ := Scan(dir)\n    require.True(t, hasRule(f, \"cleartext_password\"))\n}\n\nfunc TestScan_CleanCodePasses(t *testing.T) {\n    dir := writeFixture(t, \"ok.sql\", `CREATE TABLE u (pwd_hash TEXT NOT NULL);`)\n    f, _ := Scan(dir)\n    require.Empty(t, f)\n}\n\nfunc TestScan_AllowlistSkipped(t *testing.T) {\n    dir := writeFixture(t, \"x.go\", `var token = jwtInternal() // audit:allow JWT noi bo, khong phai token san`)\n    f, _ := Scan(dir)\n    require.Empty(t, f)\n}\n\n// services/comply/internal/audit/payload_guard_test.go\nfunc TestGuard_RejectsCookie(t *testing.T) {\n    err := GuardPayload(map[string]any{\"productId\": 1, \"cookie\": \"abc\"})\n    require.ErrorIs(t, err, ErrForbiddenField)\n}\n\nfunc TestGuard_RejectsAuthorizationAnyCase(t *testing.T) {\n    err := GuardPayload(map[string]any{\"Authorization\": \"Bearer x\"})\n    require.ErrorIs(t, err, ErrForbiddenField)\n}\n\nfunc TestGuard_MinimalPayloadPasses(t *testing.T) {\n    err := GuardPayload(map[string]any{\"productId\": 1, \"price\": 89000, \"qty\": 2})\n    require.NoError(t, err)\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: rules.go (tập quy tắc) -&gt; scan.go (walk + match + allowlist + sort) -&gt; scan_test.go -&gt; payload_guard.go -&gt; payload_guard_test.go -&gt; no_cleartext_gate.sh -&gt; nối vào ci.yml. Gate chạy ở mọi PR, chặn merge nếu đỏ. <code>GuardPayload</code> được gọi tại biên BFF/EXT-ingest (TASK-EXT-003) trên mọi payload nhận từ extension trước khi xử lý. Quy tắc nhắm danh sách token SÀN cụ thể để hạn chế false-positive; allowlist dòng cho ngoại lệ hiếm và hợp lệ.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-INFRA-003</strong> - Vault/Secrets Manager; quy tắc <code>hardcoded_secret</code> cưỡng chế đọc secret từ đây.</li><li><strong>TASK-AUTH-001</strong> - argon2id PHC; quy tắc <code>cleartext_password</code>/<code>weak_password_hash</code> cưỡng chế.</li><li><strong>TASK-AUTH-003 (liên quan)</strong> - liên kết tài khoản sàn KHÔNG lưu token; quy tắc <code>platform_session_token</code> bảo vệ.</li><li><strong>TASK-EXT-003 (liên quan)</strong> - tối thiểu hóa dữ liệu client; <code>GuardPayload</code> là lớp biên backend tương ứng.</li><li><strong>TASK-TRUST-003 (downstream)</strong> - audit độc lập dùng bộ quét + guard làm bằng chứng tự động.</li><li>Lib: <code>regexp</code>, <code>strings</code>; CI: GitHub Actions.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Payload hợp lệ từ extension (tối thiểu hóa, qua được guard)</h3>\n<pre><code class=\"language-json\">{\n  \"platform\": \"shopee\",\n  \"items\": [ { \"productId\": \"90112\", \"price\": 89000, \"qty\": 2 } ]\n}</code></pre>\n<h3>Payload bị chặn (chứa trường cấm) - guard trả lỗi</h3>\n<pre><code class=\"language-json\">{\n  \"platform\": \"shopee\",\n  \"items\": [ { \"productId\": \"90112\", \"price\": 89000 } ],\n  \"cookie\": \"SPC_SI=...; SPC_EC=...\"\n}</code></pre>\n<pre><code>400 Bad Request\n{ \"error\": \"forbidden_field\", \"field\": \"cookie\" }</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt khung. Hoãn:</p>\n<ul><li>Mở rộng quy tắc sang quét bí mật trong lịch sử git (secret scanning sâu) - bổ sung công cụ chuyên dụng sau.</li><li>Quét nhị phân/artifact build cho secret nhúng - giai đoạn CI nâng cao.</li><li>Tự động phân loại false-positive bằng heuristic ngữ cảnh - hiện dựa allowlist thủ công có lý do.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Thêm cột lưu token sàn</td><td><code>Scan</code> rule platform_session_token + AC #2</td><td>vi phạm bất biến niềm tin</td><td>CI gate chặn merge</td></tr><tr><td>Mật khẩu cleartext/băm yếu</td><td><code>Scan</code> rule cleartext/weak + AC #1,#4</td><td>rủi ro lộ DB + PDPL</td><td>Gate đỏ; ép argon2id</td></tr><tr><td>Secret hardcode trong mã</td><td><code>Scan</code> rule hardcoded_secret + AC #3</td><td>rò khóa</td><td>Ép đọc từ Vault (TASK-INFRA-003)</td></tr><tr><td>Payload extension lọt cookie</td><td><code>GuardPayload</code> + AC #8</td><td>token rời client lên server</td><td>Guard reject tại biên</td></tr><tr><td>False-positive làm gate luôn đỏ</td><td>allowlist §1 #9</td><td>gate bị bỏ qua</td><td><code>// audit:allow</code> kèm lý do, có review</td></tr><tr><td>Allowlist lạm dụng thành lỗ hổng</td><td>đếm/liệt kê §1 #11</td><td>quy tắc vô nghĩa</td><td>Review định kỳ danh sách allowlist</td></tr><tr><td>Finding thứ tự ngẫu nhiên</td><td>sort §1 #8 + AC #11</td><td>CI log khó đọc, test giòn</td><td>Sắp xếp xác định</td></tr><tr><td>Gate không chạy ở PR</td><td>ci.yml §1 #3</td><td>quy tắc không cưỡng chế</td><td>Bước gate bắt buộc, chặn merge</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Đây là điểm chứng minh kỹ thuật cho lời hứa niềm tin hậu-Honey: no-cleartext + token-not-on-server.</li><li>Hai lớp cưỡng chế: quét tĩnh (bắt mã sai) + payload guard động (bắt dữ liệu sai tại biên).</li><li>Token phiên sàn là bất biến tuyệt đối: backend không có cột/biến/log nào chứa nó; quy tắc nhắm danh sách token sàn cụ thể.</li><li>Tái dùng argon2id (TASK-AUTH-001) và Vault (TASK-INFRA-003): task này cưỡng chế hai quy tắc đó tự động, không phát minh lại.</li><li>Allowlist có lý do bắt buộc cân bằng giữa gate cứng và false-positive; mọi ngoại lệ có dấu vết và bị review.</li><li>TASK-TRUST-003 (audit độc lập) dựa trên bộ quét + guard này làm bằng chứng tự động lặp lại được.</li></ul>\n<hr>\n<p><em>Hết TASK-COMPLY-005. Status: done (mục tiêu audit 10/10).</em></p>";
