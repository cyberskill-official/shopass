window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SCRAPE-002"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Adapter Shopee <strong>MUST</strong> triển khai interface <code>PlatformAdapter</code> của TASK-SCRAPE-001, lấy giá qua internal JSON endpoint của Shopee khi truy cập được, parse về <code>price.PriceSnapshot</code>, và rơi xuống Playwright farm khi gặp challenge. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> triển khai <code>Fetch(ctx, job ScrapeJob) (price.PriceSnapshot, error)</code> và <code>PlatformID() int16</code> trả mã của Shopee.</li><li><strong>MUST</strong> gọi endpoint <code>/api/v4/pdp/get_pc?item_id={itemid}&amp;shop_id={shopid}</code> làm đường lấy giá chính (DEC-SCRAPE-06); <code>item_id</code>/<code>shop_id</code> lấy từ <code>tracked_product.platform_item_id</code> (TASK-PRICE-001).</li><li><strong>MUST</strong> truy cập theo ngữ cảnh <code>is_login:false</code> (không phiên đăng nhập) - adapter backend KHÔNG dùng cookie phiên của bất kỳ người dùng nào (DEC-SCRAPE-09, §3.2). Yêu cầu đi qua proxy residential (TASK-SCRAPE-004) và fingerprint của farm, tách hẳn khỏi extension.</li><li><strong>MUST</strong> parse JSON Shopee về <code>PriceSnapshot</code> (DEC-SCRAPE-07):</li></ol>\n<ul><li><code>price</code> &lt;- trường giá hiện tại, quy đổi đơn vị Shopee (giá Shopee thường là micro-đơn-vị x100000) về BIGINT VND nguyên.</li><li><code>list_price</code> &lt;- giá gốc/niêm yết (before-discount), cùng quy đổi.</li><li><code>stock</code>, <code>sold</code> &lt;- tồn kho và đã bán nếu có.</li><li><code>flash_sale</code> &lt;- true khi payload báo đang flash sale (<code>flash_sale</code>/<code>is_flash_sale</code>/<code>upcoming_flash_sale</code> đang chạy).</li></ul>\n<ol><li><strong>MUST</strong> quy đổi giá KHÔNG dùng float trung gian: chia số nguyên <code>raw / 100000</code> (hoặc đọc trường đã là VND nếu Shopee trả vậy), tránh sai số float (đồng bộ DEC-PRICE-05 của TASK-PRICE-002).</li><li><strong>MUST</strong> phát hiện challenge/anti-bot: nếu phản hồi không phải JSON hợp lệ (HTML, trang verify, HTTP 4xx/5xx của WAF), adapter <strong>MUST</strong> rơi xuống Playwright farm (TASK-SCRAPE-003) qua <code>DEC-SCRAPE-08</code> thay vì trả snapshot rỗng/sai.</li><li><strong>MUST</strong> trả error (không panic) khi cả endpoint lẫn fallback đều thất bại, để orchestrator (TASK-SCRAPE-001 #5) áp retry/backoff.</li><li><strong>SHOULD</strong> dùng <code>/api/v4/recommend/recommend</code> như nguồn phụ để khám phá SKU liên quan/đối thủ (phục vụ canonical_key của TASK-PRICE-005), nhưng đường giá chính là <code>pdp/get_pc</code>.</li><li><strong>MUST</strong> xử lý đúng item không tồn tại / đã gỡ: Shopee trả <code>error != 0</code> hoặc thiếu <code>item</code> -&gt; adapter trả lỗi phân loại <code>ErrItemGone</code>, để orchestrator hạ tier/ngừng quét thay vì retry vô ích.</li><li><strong>SHOULD</strong> phát OTel metric: <code>shopee_api_status_total{http_code}</code> (counter), <code>shopee_fallback_total{reason}</code> (counter), <code>shopee_parse_fail_total</code> (counter).</li><li><strong>MUST</strong> không lưu trữ phản hồi thô chứa dữ liệu cá nhân; chỉ trích <code>(price, list_price, stock, sold, flash_sale, ts)</code> rồi bỏ phần còn lại (tối thiểu hóa, §5.4).</li><li><strong>MUST</strong> đặt <code>ts = time.Now()</code> (thời điểm quét) và <code>ProductID = job.ProductID</code> trên snapshot trước khi trả về orchestrator.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao ưu tiên internal JSON (DEC-SCRAPE-06)?</strong> Gọi <code>/api/v4/pdp/get_pc</code> trả JSON cấu trúc là cách rẻ và nhanh nhất: không cần render trình duyệt, không tốn CPU farm, băng thông nhỏ. Playwright đắt hơn nhiều lần. Dùng JSON khi được, chỉ trả tiền cho farm khi buộc phải.</p>\n<p><strong>Vì sao is_login:false và không cookie người dùng (DEC-SCRAPE-09)?</strong> Đây là ranh giới đạo đức và pháp lý cốt lõi của SănDeal (§5.4). Scraping backend là hành vi của hệ thống, phải đứng bằng hạ tầng riêng (proxy + fingerprint farm), tuyệt đối không mượn phiên của người dùng. Extension đọc giỏ hàng client-side là chuyện khác (first-party, TASK-EXT-002); hai đường này không được trộn.</p>\n<p><strong>Vì sao quy đổi giá bằng số nguyên (§1 #5)?</strong> Giá Shopee trong payload thường là số rất lớn (micro-đơn-vị). Chia bằng float đưa sai số vào tiền tệ, làm hỏng phép so <code>current_price &gt;= median90 * 0.97</code> của sale ảo (TASK-DEAL-001). Chia số nguyên giữ chính xác tuyệt đối, đồng bộ với BIGINT VND của TASK-PRICE-002.</p>\n<p><strong>Vì sao bắt buộc fallback Playwright (DEC-SCRAPE-08)?</strong> Shopee có anti-bot Medium-High (§3.9): chạy thư viện JS riêng + fingerprinting, đôi khi trả HTML challenge thay vì JSON. Nếu adapter coi HTML là \"không có giá\" và bỏ qua, dữ liệu Shopee sẽ thủng từng mảng mỗi khi sàn siết. Fallback giữ độ phủ.</p>\n<p><strong>Vì sao phân loại ErrItemGone (§1 #9)?</strong> Item bị gỡ sẽ luôn trả lỗi; retry mãi là lãng phí proxy và làm bẩn dead-letter. Phân biệt \"lỗi tạm thời cần retry\" với \"item đã chết cần ngừng quét\" giúp farm không tự đốt tài nguyên.</p>\n<p><strong>Vì sao tối thiểu hóa phản hồi (§1 #11)?</strong> Payload Shopee có thể chứa thông tin shop/seller/review. SănDeal chỉ cần các trường giá. Vứt phần còn lại ngay khi parse xong là cách rẻ nhất để tuân nguyên tắc tối thiểu hóa dữ liệu (§5.4) và giảm rủi ro lưu nhầm dữ liệu cá nhân.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Endpoints (Go)</h3>\n<pre><code class=\"language-go\">// services/scrape/internal/adapters/shopee/endpoints.go\nconst (\n    pdpPath       = \"/api/v4/pdp/get_pc\"\n    recommendPath = \"/api/v4/recommend/recommend\"\n)\n\n// pdpURL dựng URL lấy giá chính cho một item Shopee.\nfunc pdpURL(base string, itemID, shopID int64) string {\n    return fmt.Sprintf(\"%s%s?item_id=%d&amp;shop_id=%d&amp;detail_level=0\",\n        base, pdpPath, itemID, shopID)\n}</code></pre>\n<h3>Parse (Go)</h3>\n<pre><code class=\"language-go\">// services/scrape/internal/adapters/shopee/parse.go\nconst shopeePriceUnit = 100_000 // Shopee trả giá theo micro-đơn-vị\n\ntype pdpResponse struct {\n    Error int `json:\"error\"`\n    Data  struct {\n        Item struct {\n            Price          int64 `json:\"price\"`            // micro-VND\n            PriceBeforeDisc int64 `json:\"price_before_discount\"`\n            Stock          int32 `json:\"stock\"`\n            HistoricalSold int32 `json:\"historical_sold\"`\n            FlashSale      *struct{ Status int `json:\"status\"` } `json:\"flash_sale\"`\n        } `json:\"item\"`\n    } `json:\"data\"`\n}\n\n// toSnapshot quy đổi micro-VND -&gt; BIGINT VND bằng phép chia số nguyên (không float).\nfunc (r pdpResponse) toSnapshot(productID int64, ts time.Time) (price.PriceSnapshot, error) {\n    if r.Error != 0 {\n        return price.PriceSnapshot{}, ErrItemGone\n    }\n    it := r.Data.Item\n    snap := price.PriceSnapshot{\n        ProductID: productID,\n        TS:        ts,\n        Price:     it.Price / shopeePriceUnit,\n        Stock:     &amp;it.Stock,\n        Sold:      &amp;it.HistoricalSold,\n        FlashSale: it.FlashSale != nil &amp;&amp; it.FlashSale.Status == 1,\n    }\n    if it.PriceBeforeDisc &gt; 0 {\n        lp := it.PriceBeforeDisc / shopeePriceUnit\n        snap.ListPrice = &amp;lp\n    }\n    return snap, nil\n}</code></pre>\n<h3>Adapter (Go)</h3>\n<pre><code class=\"language-go\">// services/scrape/internal/adapters/shopee/adapter.go\nvar ErrItemGone = errors.New(\"shopee: item removed or unavailable\")\n\nfunc (a *ShopeeAdapter) Fetch(ctx context.Context, job orchestrator.ScrapeJob) (price.PriceSnapshot, error) {\n    itemID, shopID := splitRef(job)         // từ tracked_product.platform_item_id\n    body, ct, err := a.http.GetViaProxy(ctx, pdpURL(a.base, itemID, shopID))\n    if err == nil &amp;&amp; isJSON(ct) {\n        var resp pdpResponse\n        if jsonErr := json.Unmarshal(body, &amp;resp); jsonErr == nil {\n            return resp.toSnapshot(job.ProductID, time.Now())\n        }\n        metrics.ShopeeParseFail()\n    }\n    // Challenge/HTML/lỗi WAF -&gt; rơi xuống Playwright farm (TASK-SCRAPE-003)\n    metrics.ShopeeFallback(\"challenge_or_parse\")\n    return a.farm.RenderPrice(ctx, job)\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>ShopeeAdapter</code> thỏa interface <code>PlatformAdapter</code> (compile-time assertion <code>var _ orchestrator.PlatformAdapter = (*ShopeeAdapter)(nil)</code>).</li><li><code>pdpURL</code> dựng đúng <code>/api/v4/pdp/get_pc?item_id=...&amp;shop_id=...</code> từ ref của <code>tracked_product</code>.</li><li>Parse fixture JSON hợp lệ -&gt; <code>Price</code> đúng VND (đã chia 100000), <code>ListPrice</code> đúng, <code>FlashSale</code> đúng theo <code>flash_sale.status</code>.</li><li>Quy đổi giá dùng phép chia số nguyên; với <code>price=89_000_00000</code> -&gt; <code>Price=89_000</code> chính xác, không sai số.</li><li><code>error != 0</code> trong payload -&gt; <code>Fetch</code> trả <code>ErrItemGone</code> (không retry vô ích).</li><li>Phản hồi là HTML challenge (content-type không phải JSON) -&gt; adapter gọi <code>farm.RenderPrice</code> (fallback Playwright).</li><li>Cả endpoint lẫn fallback fail -&gt; <code>Fetch</code> trả error (orchestrator sẽ retry/backoff), không panic.</li><li>Adapter KHÔNG đính kèm cookie/token phiên người dùng vào request (kiểm qua header request trong test).</li><li>Snapshot trả về có <code>ProductID = job.ProductID</code> và <code>ts</code> ~ thời điểm quét.</li><li>Chỉ các trường giá được giữ; phản hồi thô không được lưu (kiểm không có field dư rò ra ngoài <code>PriceSnapshot</code>).</li><li>Metric <code>shopee_api_status_total</code>, <code>shopee_fallback_total</code> tăng đúng theo nhánh xử lý.</li><li>Tích hợp orchestrator: job Shopee qua pool -&gt; adapter chạy -&gt; <code>InsertSnapshot</code> delta-only được gọi (TASK-SCRAPE-001 #8).</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/scrape/internal/adapters/shopee/parse_test.go\nfunc TestParse_ValidPDP(t *testing.T) {\n    raw := readFixture(t, \"pdp_get_pc.json\")\n    var resp pdpResponse\n    require.NoError(t, json.Unmarshal(raw, &amp;resp))\n    snap, err := resp.toSnapshot(90112, t0)\n    require.NoError(t, err)\n    require.Equal(t, int64(89_000), snap.Price)         // 89_000_00000 / 100000\n    require.Equal(t, int64(149_000), *snap.ListPrice)\n    require.True(t, snap.FlashSale)\n}\n\nfunc TestParse_IntegerDivision_NoFloatError(t *testing.T) {\n    resp := pdpResponse{}\n    resp.Data.Item.Price = 333_333_00000   // 333_333 VND chính xác\n    snap, _ := resp.toSnapshot(1, t0)\n    require.Equal(t, int64(333_333), snap.Price)\n}\n\nfunc TestParse_ItemGone(t *testing.T) {\n    resp := pdpResponse{Error: 4}\n    _, err := resp.toSnapshot(1, t0)\n    require.ErrorIs(t, err, ErrItemGone)\n}</code></pre>\n<pre><code class=\"language-go\">// services/scrape/internal/adapters/shopee/adapter_test.go\nvar _ orchestrator.PlatformAdapter = (*ShopeeAdapter)(nil)\n\nfunc TestFetch_HTMLChallenge_FallsBackToFarm(t *testing.T) {\n    a := newAdapter(t, stubHTTP(\"&lt;html&gt;verify&lt;/html&gt;\", \"text/html\"))\n    called := false\n    a.farm = farmFunc(func(ctx context.Context, j orchestrator.ScrapeJob) (price.PriceSnapshot, error) {\n        called = true\n        return price.PriceSnapshot{ProductID: j.ProductID, Price: 1}, nil\n    })\n    _, err := a.Fetch(ctx, job(90112))\n    require.NoError(t, err)\n    require.True(t, called) // đã rơi xuống Playwright\n}\n\nfunc TestFetch_NoUserCookieSent(t *testing.T) {\n    var sentCookie string\n    a := newAdapter(t, captureHeader(\"Cookie\", &amp;sentCookie))\n    a.Fetch(ctx, job(90112))\n    require.Empty(t, sentCookie) // backend không mượn phiên người dùng\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: endpoints.go (URL builder) -&gt; parse.go (JSON -&gt; snapshot, test trước với fixture) -&gt; adapter.go (Fetch + fallback) -&gt; registry.go (đăng ký vào orchestrator) -&gt; tests. Fixture <code>pdp_get_pc.json</code> là JSON thật đã ẩn danh (xóa field shop/seller/review, chỉ giữ cấu trúc giá). Đường lấy giá chính là <code>pdp/get_pc</code>; <code>recommend</code> là tùy chọn phụ cho TASK-PRICE-005.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-SCRAPE-001</strong> - interface <code>PlatformAdapter</code>, orchestrator điều phối + gọi <code>InsertSnapshot</code>.</li><li><strong>TASK-SCRAPE-003 (fallback)</strong> - Playwright farm <code>RenderPrice</code> khi gặp challenge.</li><li><strong>TASK-SCRAPE-004</strong> - proxy residential cho <code>GetViaProxy</code>.</li><li><strong>TASK-PRICE-001</strong> - <code>tracked_product.platform_item_id</code> cung cấp <code>item_id</code>/<code>shop_id</code>.</li><li><strong>TASK-PRICE-002 (downstream)</strong> - snapshot ghi qua <code>InsertSnapshot</code> delta-only.</li><li><strong>TASK-PRICE-005 (tùy chọn)</strong> - <code>recommend</code> hỗ trợ canonical_key matching.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Phản hồi /api/v4/pdp/get_pc (ẩn danh, chỉ phần giá)</h3>\n<pre><code class=\"language-json\">{\n  \"error\": 0,\n  \"data\": {\n    \"item\": {\n      \"price\": 8900000000,\n      \"price_before_discount\": 14900000000,\n      \"stock\": 37,\n      \"historical_sold\": 1240,\n      \"flash_sale\": { \"status\": 1 }\n    }\n  }\n}</code></pre>\n<h3>Snapshot adapter trả về orchestrator</h3>\n<pre><code class=\"language-go\">price.PriceSnapshot{\n    ProductID: 90112,\n    TS:        time.Now(),\n    Price:     89_000,        // VND (8900000000 / 100000)\n    ListPrice: ptr(int64(149_000)),\n    Stock:     ptr(int32(37)),\n    Sold:      ptr(int32(1240)),\n    FlashSale: true,\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Đơn vị giá chính xác của Shopee (x100000 hay đọc trường VND trực tiếp) - xác minh trên fixture thật lúc build; hằng <code>shopeePriceUnit</code> tập trung một chỗ để chỉnh.</li><li>Tham số ký/anti-bot bổ sung cho <code>pdp/get_pc</code> nếu Shopee siết là-login-only - khi đó fallback farm gánh nhiều hơn.</li><li>Khai thác <code>recommend</code> để dò biến thể (màu/size) cùng SKU - gắn khi TASK-PRICE-005.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Shopee trả HTML challenge</td><td>content-type không JSON</td><td>mất giá nếu bỏ qua</td><td>Fallback Playwright (§1 #6)</td></tr><tr><td>Đơn vị giá đổi (x100000 sai)</td><td>parse_test trên fixture</td><td>giá sai 10^5 lần</td><td>Hằng <code>shopeePriceUnit</code> một chỗ, test khóa</td></tr><tr><td>Item đã gỡ</td><td><code>error != 0</code></td><td>retry vô ích</td><td><code>ErrItemGone</code> -&gt; orchestrator ngừng quét</td></tr><tr><td>Gửi nhầm cookie người dùng</td><td>test <code>NoUserCookieSent</code></td><td>vi phạm §5.4</td><td>Adapter backend không bao giờ đính cookie</td></tr><tr><td>Float trong quy đổi</td><td>parse_test</td><td>sai số tiền tệ</td><td>Phép chia số nguyên (§1 #5)</td></tr><tr><td>JSON đổi schema (Shopee A/B)</td><td><code>shopee_parse_fail_total</code></td><td>parse hụt</td><td>TASK-SCRAPE-006 giám sát + cập nhật struct</td></tr><tr><td>WAF rate-limit 429</td><td><code>shopee_api_status_total</code></td><td>bị siết</td><td>Fallback + orchestrator backoff + proxy rotation</td></tr><tr><td>Lưu nhầm phản hồi thô</td><td>review code</td><td>rủi ro dữ liệu cá nhân</td><td>Chỉ trích trường giá, vứt phần còn lại (§1 #11)</td></tr><tr><td>Fallback farm cũng fail</td><td>error trả về</td><td>quét hụt lần này</td><td>Orchestrator retry/backoff (TASK-SCRAPE-001 #5)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Adapter Shopee là khuôn mẫu cho TikTok (TASK-SCRAPE-007) và Lazada (TASK-SCRAPE-008): cùng interface, khác cách lấy dữ liệu.</li><li>Ranh giới cứng: scraping backend (proxy + fingerprint farm, is_login:false) tách hoàn toàn khỏi extension đọc giỏ hàng (first-party). Không trộn phiên người dùng vào backend.</li><li>Quy đổi giá bằng số nguyên đồng bộ BIGINT VND của TASK-PRICE-002 - không sai số float trên tiền tệ.</li><li>Fallback Playwright là bảo hiểm độ phủ trước anti-bot Medium-High của Shopee (§3.9); JSON là đường nhanh-rẻ mặc định.</li><li>Đường giá chính là <code>pdp/get_pc</code>; <code>recommend</code> để mở rộng độ phủ SKU và phục vụ so sánh chéo sàn về sau.</li></ul>\n<hr>\n<p><em>Hết TASK-SCRAPE-002. Status: done (mục tiêu audit 10/10).</em></p>";
