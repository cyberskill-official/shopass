window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CART-006"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Extension và service CART <strong>MUST</strong> cung cấp checklist xu CHỈ nhắc nhở - đọc trạng thái nhiệm vụ xu hiển thị và hiện danh sách việc cần làm cho người dùng tự bấm trên sàn - và TUYỆT ĐỐI không tự động click/hoàn thành nhiệm vụ xu. Hợp đồng:</p>\n<ol><li>Checklist xu <strong>MUST</strong> chỉ nhắc nhở (DEC-CART-30): đọc trạng thái nhiệm vụ xu hiển thị (đã làm/chưa làm) và hiện danh sách việc; TUYỆT ĐỐI KHÔNG auto-click, KHÔNG tự hoàn thành nhiệm vụ xu (§3.9c xếp tự động hóa xu rủi ro ban High).</li><li><code>coin-task-reader.ts</code> <strong>MUST</strong> chỉ ĐỌC trạng thái nhiệm vụ xu hiển thị qua khung reader TASK-EXT-002 (DEC-CART-31): KHÔNG mô phỏng click (<code>element.click()</code>, dispatch sự kiện chuột), KHÔNG gọi API hoàn thành nhiệm vụ.</li><li>Checklist <strong>MUST</strong> là dữ liệu nhắc (DEC-CART-32): <code>task_type</code> (loại nhiệm vụ), <code>due_date</code> (hạn), <code>done</code> (đã làm chưa) - hiển thị cho người dùng; nút trên UI chỉ DẪN người dùng tới trang nhiệm vụ trên sàn để tự bấm.</li><li>Service <strong>MUST</strong> lưu trạng thái nhắc qua bảng <code>user_activity_coin_task</code> (§3.4): <code>(user_id, platform_id, task_type, due_date, done)</code> - per user/platform (DEC-CART-33).</li><li>Reminder <strong>MUST</strong> gửi qua TASK-NOTIF-001 dạng nhắc nhẹ (DEC-CART-33), KHÔNG spam - tối đa nhắc hợp lý mỗi ngày, tôn trọng tùy chọn người dùng.</li><li>Quá trình đọc trạng thái xu <strong>MUST NOT</strong> thu thập credential (DEC-CART-34): không đọc cookie/token (đồng nhất ranh giới TASK-EXT-002/003); chỉ lấy trạng thái done/chưa-done của nhiệm vụ hiển thị.</li><li>UI checklist <strong>MUST</strong> làm rõ rằng SănDeal chỉ nhắc, người dùng tự thực hiện trên sàn (minh bạch, hợp triết lý hậu-Honey) - không tạo ấn tượng tự động hoàn thành.</li><li>Service <strong>MUST</strong> scope trạng thái theo <code>user_id</code>: người dùng chỉ đọc checklist của chính mình (chống truy cập chéo).</li><li>Đọc trạng thái <strong>MUST</strong> xử lý lịch sự khi không lấy được (DOM đổi, chưa đăng nhập): hiện \"chưa lấy được trạng thái\", không lỗi vỡ; KHÔNG thử click để \"kích hoạt\".</li><li>Toàn bộ đường đọc <strong>MUST</strong> local-first (TASK-TRUST-002): trạng thái đọc trên máy client; chỉ trạng thái tối thiểu (done/chưa) đồng bộ nếu cần nhắc.</li><li>Là tính năng SHOULD: có thể hoãn sau MVP; khi làm <strong>MUST</strong> giữ ranh giới chỉ-nhắc tuyệt đối.</li><li><code>npm test</code> (extension) + <code>go test ./...</code> (cart-svc) xanh; <code>tsc --noEmit</code> sạch.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao CHỈ nhắc, không auto-click (DEC-CART-30)?</strong> §3.9c xếp tự động hóa xu/voucher ở mức rủi ro ban CAO NHẤT (High) - cao hơn cả scraping - vì sàn coi tự động hoàn thành nhiệm vụ xu là farming trực tiếp. Tài liệu nêu rõ giải pháp của SănDeal là checklist nhắc nhở, KHÔNG tự động click. Auto-click kích hoạt đúng cơ chế ban này, khóa tài khoản người dùng và biến SănDeal thành công cụ farming - đụng cả gian lận user (§5.3) và niềm tin (§5.4). Chỉ nhắc là phía an toàn tuyệt đối.</p>\n<p><strong>Vì sao chỉ đọc trạng thái hiển thị (DEC-CART-31)?</strong> Ranh giới giữa \"trợ lý nhắc\" và \"bot farming\" là ai bấm nút. Đọc trạng thái nhiệm vụ (đã làm/chưa) để nhắc là chỉ-đọc, an toàn. Mô phỏng click hay gọi API hoàn thành là thay người dùng thực hiện - vượt ranh giới. Reader chỉ đọc, không bao giờ click.</p>\n<p><strong>Vì sao user tự bấm trên sàn (DEC-CART-32)?</strong> SănDeal hiện checklist \"hôm nay bạn chưa điểm danh, chưa xem live\" và nút dẫn tới trang nhiệm vụ; người dùng tự bấm điểm danh trên sàn. Quyền và hành động ở người dùng - vừa tránh ban, vừa minh bạch (không giả vờ tự động giúp). Đây là cùng triết lý với auto-test mã user-initiated (TASK-CART-005).</p>\n<p><strong>Vì sao không spam reminder (DEC-CART-33)?</strong> Nhắc quá nhiều phản tác dụng - người dùng tắt thông báo. Nhắc nhẹ (một lần mỗi ngày cho nhiệm vụ chưa làm, tôn trọng tùy chọn) giữ giá trị mà không phiền. Reminder qua TASK-NOTIF-001 với tần suất hợp lý.</p>\n<p><strong>Vì sao không thu thập credential (DEC-CART-34)?</strong> Đồng nhất ranh giới niềm tin lõi (TASK-EXT-002/003): đọc trạng thái nhiệm vụ hiển thị không cần và không được chạm cookie/token. Chỉ lấy done/chưa-done. Giữ cam kết tối thiểu hóa ngay cả ở tính năng phụ này.</p>\n<p><strong>Vì sao là SHOULD nhưng ranh giới tuyệt đối (§1 #11)?</strong> Checklist xu tiện nhưng không chặn release - có thể hoãn. Tuy nhiên khi làm, ranh giới chỉ-nhắc không được nới: một lần auto-click là rủi ro ban High cho người dùng. SHOULD về ưu tiên, MUST về ranh giới an toàn.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Reader trạng thái (coin-task-reader.ts) - CHỈ đọc, KHÔNG click</h3>\n<pre><code class=\"language-ts\">// extension/src/content/shared/coin-task-reader.ts\nexport interface CoinTask {\n  taskType: string;   // 'daily_checkin' | 'watch_live' | ... (loại nhiệm vụ hiển thị)\n  done: boolean;      // đã làm chưa (đọc từ trạng thái hiển thị)\n}\n\n// readCoinTasks CHỈ đọc trạng thái nhiệm vụ xu hiển thị (DEC-CART-31).\n// TUYỆT ĐỐI KHÔNG click, KHÔNG dispatch sự kiện, KHÔNG gọi API hoàn thành.\nexport function readCoinTasks(): CoinTask[] {\n  const rows = queryCoinTaskRows(); // đọc DOM khu nhiệm vụ xu (khung reader TASK-EXT-002)\n  return rows.map((el) =&gt; ({\n    taskType: parseTaskType(el),\n    done: parseDoneState(el),       // chỉ đọc trạng thái done/chưa\n  }));\n  // KHÔNG có el.click(), KHÔNG dispatchEvent, KHÔNG fetch hoàn thành nhiệm vụ\n}</code></pre>\n<h3>Migration (golang-migrate)</h3>\n<pre><code class=\"language-sql\">-- db/migrations/0012_coin_task.up.sql\nCREATE TABLE user_activity_coin_task (\n  id          BIGSERIAL   PRIMARY KEY,\n  user_id     BIGINT      NOT NULL REFERENCES app_user(id),\n  platform_id SMALLINT    NOT NULL REFERENCES platform(id),\n  task_type   TEXT        NOT NULL,\n  due_date    DATE        NOT NULL,\n  done        BOOLEAN     NOT NULL DEFAULT false,\n  UNIQUE (user_id, platform_id, task_type, due_date)\n);\nCREATE INDEX idx_coin_due ON user_activity_coin_task (user_id, due_date) WHERE done = false;\n\n-- db/migrations/0012_coin_task.down.sql\nDROP TABLE user_activity_coin_task;</code></pre>\n<h3>Repo (repo.go) - scope user, lưu trạng thái nhắc</h3>\n<pre><code class=\"language-go\">// services/cart/internal/coin/repo.go\nfunc (r *Repo) UpsertTask(ctx context.Context, userID int64, platformID int16, t CoinTask) error {\n    _, err := r.pool.Exec(ctx,\n        `INSERT INTO user_activity_coin_task (user_id, platform_id, task_type, due_date, done)\n         VALUES ($1,$2,$3,$4,$5)\n         ON CONFLICT (user_id, platform_id, task_type, due_date) DO UPDATE SET done = EXCLUDED.done`,\n        userID, platformID, t.TaskType, t.DueDate, t.Done)\n    return err\n}\n\n// ListPending trả nhiệm vụ chưa làm của ĐÚNG user (scope user_id, DEC-CART... §1 #8).\nfunc (r *Repo) ListPending(ctx context.Context, userID int64, day time.Time) ([]CoinTask, error) {\n    // ... WHERE user_id = $1 AND due_date = $2 AND done = false\n}</code></pre>\n<h3>UI checklist (coin-checklist.ts) - dẫn user tự bấm</h3>\n<pre><code class=\"language-ts\">// extension/src/ui/coin-checklist.ts\n// Hiện checklist + nút DẪN tới trang nhiệm vụ trên sàn; user TỰ bấm (DEC-CART-32).\nfunction renderChecklist(tasks: CoinTask[]) {\n  for (const t of tasks) {\n    const row = makeRow(t.taskType, t.done);\n    if (!t.done) {\n      row.appendLink(\"Tới trang nhiệm vụ\", taskUrlOnPlatform(t.taskType)); // mở để user tự làm\n      // KHÔNG có nút \"tự động hoàn thành\"\n    }\n  }\n  showNote(\"SănDeal chỉ nhắc; bạn tự thực hiện trên sàn.\"); // minh bạch (§1 #7)\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Grep <code>extension/src/content/shared/coin-task-reader.ts</code> + <code>coin-checklist.ts</code>: KHÔNG có <code>.click(</code>, KHÔNG <code>dispatchEvent</code>, KHÔNG <code>fetch</code>/API gọi hoàn thành nhiệm vụ xu.</li><li><code>readCoinTasks</code> trả danh sách <code>CoinTask{taskType, done}</code> đọc từ trạng thái hiển thị (không thay đổi trạng thái).</li><li>UI checklist hiện nhiệm vụ chưa làm kèm link DẪN tới trang nhiệm vụ trên sàn; KHÔNG có nút \"tự động hoàn thành\".</li><li>UI có ghi chú minh bạch \"SănDeal chỉ nhắc; bạn tự thực hiện\".</li><li>Migration tạo <code>user_activity_coin_task</code> với <code>UNIQUE(user_id, platform_id, task_type, due_date)</code>.</li><li><code>UpsertTask</code> lưu/cập nhật trạng thái; <code>ListPending</code> trả nhiệm vụ chưa làm của đúng user (scope user_id).</li><li>Reader KHÔNG thu thập credential: grep KHÔNG <code>document.cookie</code>/token khi đọc trạng thái xu.</li><li>Reminder gửi qua TASK-NOTIF-001 nhắc nhẹ (không spam); tôn trọng tùy chọn.</li><li>Đọc thất bại (DOM đổi/chưa đăng nhập) -&gt; \"chưa lấy được trạng thái\", không lỗi, KHÔNG thử click.</li><li>Trạng thái scope user_id: user A không đọc checklist user B.</li><li><code>npm test</code> + <code>go test ./...</code> xanh; <code>tsc --noEmit</code> sạch.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-ts\">// extension/test/coin-no-autoclick.test.ts\nimport { readFile } from \"fs/promises\";\n\ntest(\"reader + UI KHÔNG auto-click / dispatch / gọi API hoàn thành\", async () =&gt; {\n  for (const f of [\"src/content/shared/coin-task-reader.ts\", \"src/ui/coin-checklist.ts\"]) {\n    const src = await readFile(f, \"utf8\");\n    expect(src).not.toMatch(/\\.click\\s*\\(/);        // không mô phỏng click\n    expect(src).not.toMatch(/dispatchEvent/);       // không phát sự kiện chuột\n    expect(src).not.toMatch(/complete.?task|claim.?coin|hoàn thành nhiệm vụ/i); // không gọi hoàn thành\n  }\n});\n\ntest(\"reader chỉ đọc trạng thái, KHÔNG chạm credential\", async () =&gt; {\n  const src = await readFile(\"src/content/shared/coin-task-reader.ts\", \"utf8\");\n  expect(src).not.toMatch(/document\\.cookie/);\n  expect(src).not.toMatch(/Authorization|token/i);\n});\n\ntest(\"readCoinTasks trả trạng thái done không đổi DOM\", () =&gt; {\n  document.body.innerHTML = coinTaskFixture; // 2 nhiệm vụ: 1 done, 1 chưa\n  const before = document.body.innerHTML;\n  const tasks = readCoinTasks();\n  expect(tasks).toHaveLength(2);\n  expect(tasks.some((t) =&gt; t.done)).toBe(true);\n  expect(document.body.innerHTML).toBe(before); // đọc không sửa DOM (không click)\n});</code></pre>\n<pre><code class=\"language-go\">// services/cart/internal/coin/repo_test.go\nfunc TestUpsertTask_Idempotent(t *testing.T) {\n    r, uid := setupCoin(t)\n    task := CoinTask{TaskType: \"daily_checkin\", DueDate: today, Done: false}\n    require.NoError(t, r.UpsertTask(ctx, uid, 1, task))\n    task.Done = true\n    require.NoError(t, r.UpsertTask(ctx, uid, 1, task)) // cùng khóa -&gt; update done\n    pending, _ := r.ListPending(ctx, uid, today)\n    require.Empty(t, pending) // đã done -&gt; không còn pending\n}\n\nfunc TestListPending_ScopedToUser(t *testing.T) {\n    r, uidA := setupCoin(t)\n    r.UpsertTask(ctx, uidA, 1, CoinTask{TaskType: \"watch_live\", DueDate: today, Done: false})\n    uidB := makeUser(t, r)\n    pendingB, _ := r.ListPending(ctx, uidB, today)\n    require.Empty(t, pendingB) // user B không thấy nhiệm vụ của A\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>types.ts</code> (CoinTask, CoinChecklistMessage) -&gt; <code>coin-task-reader.ts</code> (CHỈ đọc trạng thái hiển thị, không click/API) -&gt; <code>0012_coin_task.up/down.sql</code> (bảng user_activity_coin_task) -&gt; <code>coin/types.go</code> + <code>repo.go</code> (lưu/đọc scope user) -&gt; <code>coin-checklist.ts</code> (UI hiện checklist + link dẫn user tự bấm, ghi chú minh bạch) -&gt; tests. Migration 0012 nối tiếp 0011 (TASK-CART-002). Reader tái dùng khung TASK-EXT-002 nhưng tuyệt đối chỉ-đọc (không có ngoại lệ áp-gỡ như TASK-CART-005 vì xu không có thao tác đo). Reminder nối TASK-NOTIF-001 nhắc nhẹ. Test then chốt là grep khẳng định không auto-click/dispatch/API-hoàn-thành - ranh giới chống ban High phải kiểm chứng được.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-EXT-002</strong> - khung content script + reader (chỉ-đọc); <code>coin-task-reader</code> đọc trạng thái nhiệm vụ xu hiển thị, tuyệt đối không click (depends_on cứng).</li><li><strong>TASK-NOTIF-001 (downstream)</strong> - template + routing để gửi reminder nhắc nhẹ; checklist không tự gửi, dùng kênh notif.</li><li><strong>TASK-CART-005 (sibling)</strong> - auto-test mã cùng triết lý user-initiated/không-tự-động; checklist xu còn nghiêm hơn (không có cả thao tác đo, chỉ nhắc).</li><li><strong>NFR-AFFIL-001</strong> - guardrails compliance (né Honey, Chrome policy); checklist chỉ-nhắc là một mặt.</li><li>Extension/lib: TypeScript (reader + UI), Go (repo); golang-migrate; test Jest + jsdom, Go test.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Checklist hiển thị (nhắc, user tự bấm trên sàn)</h3>\n<pre><code class=\"language-json\">{\n  \"platform\": \"shopee\",\n  \"date\": \"2026-06-28\",\n  \"tasks\": [\n    { \"taskType\": \"daily_checkin\", \"done\": false },\n    { \"taskType\": \"watch_live\",    \"done\": true  },\n    { \"taskType\": \"browse_shops\",  \"done\": false }\n  ]\n}</code></pre>\n<h3>Luồng (mô tả, không phải payload)</h3>\n<pre><code>extension đọc trạng thái nhiệm vụ xu hiển thị (CHỈ đọc)\n  -&gt; hiện checklist: việc nào chưa làm\n  -&gt; với việc chưa làm: link \"Tới trang nhiệm vụ\" (user TỰ bấm trên sàn)\n  -&gt; reminder nhắc nhẹ qua TASK-NOTIF-001 (không spam)\n  -&gt; KHÔNG auto-click, KHÔNG tự hoàn thành (chống ban High §3.9c)</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Đồng bộ trạng thái checklist giữa thiết bị - thêm khi cần; giữ scope user.</li><li>Nhắc thông minh theo thói quen người dùng (giờ hay quên điểm danh) - tối ưu reminder sau, không đổi ranh giới chỉ-nhắc.</li><li>Mở rộng đọc nhiệm vụ xu TikTok Shop/Lazada (khung TASK-EXT-007/008) - per-sàn sau, vẫn chỉ-đọc.</li><li>Gamification điểm SănDeal nội bộ cho việc hoàn thành (§6 #10) - tách task riêng, không auto-click sàn.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Auto-click hoàn thành nhiệm vụ</td><td>coin-no-autoclick test + grep</td><td>rủi ro ban High, khóa tài khoản</td><td>CHỈ nhắc, không click (DEC-CART-30)</td></tr><tr><td>Mô phỏng click / dispatch event</td><td>grep .click/dispatchEvent</td><td>farming -&gt; ban</td><td>Chỉ đọc trạng thái (DEC-CART-31)</td></tr><tr><td>Gọi API hoàn thành nhiệm vụ</td><td>grep complete/claim</td><td>né disclosure, abuse</td><td>Không API hoàn thành (DEC-CART-31)</td></tr><tr><td>Thu thập credential khi đọc xu</td><td>grep cookie/token</td><td>phá tối thiểu hóa</td><td>Chỉ done/chưa-done (DEC-CART-34)</td></tr><tr><td>Spam reminder</td><td>review tần suất</td><td>user tắt thông báo</td><td>Nhắc nhẹ qua NOTIF-001 (DEC-CART-33)</td></tr><tr><td>Đọc thất bại thử click kích hoạt</td><td>fail-state test</td><td>vượt ranh giới</td><td>\"Chưa lấy được trạng thái\", không click (§1 #9)</td></tr><tr><td>Đọc checklist user khác</td><td>scope test</td><td>rò rỉ</td><td>Scope user_id (§1 #8)</td></tr><tr><td>UI tạo ấn tượng tự động</td><td>review note</td><td>hiểu lầm + lệch triết lý</td><td>Ghi chú \"bạn tự thực hiện\" (§1 #7)</td></tr><tr><td>Sửa DOM khi đọc</td><td>no-mutate test</td><td>tác động ngoài ý muốn</td><td>Đọc không sửa DOM (§5)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Checklist xu là tiện ích cho persona Huy (SHOULD); ranh giới chỉ-nhắc là MUST tuyệt đối.</li><li>§3.9c xếp tự động hóa xu rủi ro ban CAO NHẤT (High) - cao hơn scraping; giải pháp SănDeal là checklist nhắc, KHÔNG auto-click.</li><li>Ranh giới giữa trợ lý nhắc và bot farming là ai bấm nút: SănDeal đọc trạng thái + nhắc; người dùng tự bấm trên sàn.</li><li>Không thu thập credential khi đọc trạng thái - đồng nhất cam kết tối thiểu hóa lõi (TASK-EXT-002/003).</li><li>Reminder nhắc nhẹ qua TASK-NOTIF-001, không spam; tôn trọng tùy chọn người dùng.</li><li>Test then chốt là grep khẳng định không auto-click/dispatch/API-hoàn-thành - ranh giới chống ban High phải kiểm chứng được, không chỉ quy ước.</li></ul>\n<hr>\n<p><em>Hết TASK-CART-006. Status: done (mục tiêu audit 10/10).</em></p>";
