window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-DEAL-002"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service DEAL <strong>MUST</strong> định nghĩa chính sách cold-start cho bộ phát hiện sale ảo (TASK-DEAL-001) và biểu đồ giá: phân SKU thành 3 trạng thái trưởng thành theo số ngày lịch sử, dùng category priors làm fallback khi thiếu lịch sử, và đặt cổng baseline 90 ngày trước khi bật tính năng công khai cho từng SKU. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> tính <code>daysOfHistory</code> cho mỗi SKU = số ngày từ <code>first_seen</code> của <code>tracked_product</code> (hoặc snapshot <code>price_snapshot</code> sớm nhất, lấy mốc nào sớm hơn) tới hiện tại.</li><li><strong>MUST</strong> ánh xạ <code>daysOfHistory</code> sang trạng thái: <code>NEW</code> khi <code>&lt; 14</code> ngày, <code>WARMING</code> khi <code>14 &lt;= d &lt; 90</code> ngày, <code>MATURE</code> khi <code>&gt;= 90</code> ngày (DEC-DEAL-10). Ngưỡng là hằng số có tên, không phải số ma thuật rải rác.</li><li><strong>MUST</strong> trả verdict <code>UNKNOWN</code> cho mọi SKU ở trạng thái <code>NEW</code> (<code>&lt; 14</code> ngày), nhất quán với TASK-DEAL-001 (DEC-DEAL-13). Không đoán mò khi dữ liệu chưa đủ.</li><li><strong>MUST</strong> ở trạng thái <code>WARMING</code> (14-90 ngày): cho phép verdict độ tin cậy thấp (gắn cờ <code>low_confidence</code>) và hiển thị biểu đồ kèm chú thích <code>\"đang tích lũy\"</code>; KHÔNG được phát verdict độ tin cậy đầy đủ.</li><li><strong>MUST</strong> ở trạng thái <code>MATURE</code> (<code>&gt;= 90</code> ngày): cho phép verdict độ tin cậy đầy đủ và biểu đồ công khai không kèm chú thích cảnh báo dữ liệu mỏng.</li><li><strong>MUST</strong> định nghĩa materialized view <code>category_prior</code> gộp theo <code>category_id</code>: <code>median_price</code> tiêu biểu, <code>typical_discount_depth</code> (độ sâu giảm giá điển hình), <code>sample_count</code> (số SKU đóng góp). Đây là prior để mượn sức mạnh thống kê từ cả category.</li><li><strong>MUST</strong> chỉ gộp <code>category_prior</code> từ SKU ở trạng thái <code>MATURE</code> (DEC-DEAL-14): loại mọi SKU <code>NEW</code>/<code>WARMING</code> khỏi mẫu để tránh thiên lệch phản hồi (SKU non kéo prior lệch, rồi prior lệch lại làm SKU non trông sai).</li><li><strong>MUST</strong> expose hàm cổng <code>IsFeatureReady(product) bool</code> (DEC-DEAL-11): trả <code>true</code> chỉ khi SKU đạt <code>MATURE</code> (<code>&gt;= 90</code> ngày). Sale ảo công khai và biểu đồ công khai cho 1 SKU <strong>MUST</strong> đi qua cổng này; cổng <code>false</code> thì chỉ được hiển thị trạng thái \"đang tích lũy\", không phát verdict công khai.</li><li><strong>MUST</strong> dùng <code>category_prior</code> làm fallback CHỈ khi SKU chưa <code>MATURE</code>: với SKU <code>NEW</code>/<code>WARMING</code>, các ước lượng (median tham chiếu, độ sâu giảm điển hình) lấy từ prior của <code>category_id</code>; với SKU <code>MATURE</code>, dùng lịch sử riêng của SKU, không dùng prior.</li><li><strong>MUST</strong> xử lý SKU không có <code>category_id</code> (NULL): trả <code>UNKNOWN</code> và không có fallback prior (không thể mượn sức mạnh từ category nào).</li><li><strong>SHOULD</strong> tính lại <code>category_prior</code> theo lịch (refresh materialized view hằng ngày) để prior bám theo dữ liệu MATURE mới nhất.</li><li><strong>MUST</strong> đặt sàn <code>min_sample_count</code> cho prior: nếu <code>sample_count</code> của một <code>category_id</code> dưới ngưỡng (ví dụ <code>&lt; 30</code> SKU MATURE), prior của category đó coi như không đủ tin và fallback trả <code>UNKNOWN</code> thay vì prior mỏng.</li><li><strong>SHOULD</strong> phát OTel metric: <code>deal_maturity_state_total{state}</code> (counter theo NEW/WARMING/MATURE), <code>category_prior_fallback_total{category_id}</code> (counter mỗi lần dùng prior), <code>category_prior_sample_count{category_id}</code> (gauge).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao cổng 90 ngày (DEC-DEAL-11, §5.1)?</strong> §5.1 chốt nguyên tắc ra mắt: chỉ bật tính năng khi đủ khoảng 90 ngày dữ liệu cho top SKU. Sale ảo so giá hiện tại với median 90 ngày và đáy gần đây; nếu cửa sổ chưa đủ 90 ngày, \"median 90 ngày\" thực ra chỉ là median của vài tuần và rất dễ bị một đợt flash sale kéo lệch. Cổng <code>IsFeatureReady</code> ép tính năng công khai chờ tới khi cửa sổ đủ dài, đổi độ trễ ra mắt lấy độ chính xác.</p>\n<p><strong>Vì sao UNKNOWN tốt hơn một phán đoán sai (§5.4)?</strong> Tài sản lớn nhất của SănDeal là niềm tin: người dùng tin \"sale ảo\" nghĩa là thật sự ảo. Một verdict sai trên SKU mới (gọi sale xịn thành ảo, hoặc ngược lại) đắt hơn nhiều so với việc nói thẳng \"chưa đủ dữ liệu\". <code>NEW -&gt; UNKNOWN</code> là sự khiêm tốn có chủ đích: thà im lặng còn hơn nói sai và mất uy tín ngay lần đầu người dùng kiểm chứng.</p>\n<p><strong>Vì sao category priors (DEC-DEAL-12)?</strong> Một SKU mới chưa có lịch sử riêng, nhưng category của nó thì có. Điện thoại trong cùng nhóm ngành có biên giảm giá điển hình giống nhau, có mùa double-date giống nhau. Mượn thống kê gộp của category cho ta một điểm tựa hợp lý để ước lượng tạm trong giai đoạn WARMING, thay vì khởi đầu từ con số không. Đây cũng chính là prior mà TASK-DEAL-004 (Prophet baseline) cần để khởi tạo dự đoán đáy cho SKU cold-start.</p>\n<p><strong>Vì sao loại SKU non khỏi prior aggregate (DEC-DEAL-14)?</strong> Nếu category_prior gộp cả SKU NEW/WARMING, ta tạo một vòng phản hồi: SKU non có dữ liệu nhiễu kéo median và discount depth của category lệch đi, rồi prior lệch đó lại được áp ngược lên các SKU non khác làm chúng trông càng sai. Chỉ gộp từ SKU MATURE giữ prior \"sạch\" - nó phản ánh hành vi giá đã ổn định, không phản ánh nhiễu của giai đoạn cold-start.</p>\n<p><strong>Vì sao sàn min_sample_count (§1 #12)?</strong> Một category mới chỉ có 3-4 SKU MATURE thì median của nó không đáng tin hơn một SKU đơn lẻ là bao. Đặt sàn (ví dụ 30 SKU) buộc prior chỉ phát huy khi category đủ dày; dưới sàn thì thà trả UNKNOWN còn hơn dựa vào một prior mỏng và dễ lệch.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/deal/migrations/0001_category_prior.sql\n-- Gộp thống kê theo category_id, CHỈ từ SKU đã MATURE (&gt;= 90 ngày lịch sử).\n-- mature_sku: 1 dòng/SKU MATURE kèm median giá riêng và độ sâu giảm gần nhất.\nCREATE MATERIALIZED VIEW category_prior AS\n  WITH mature_sku AS (\n    SELECT tp.category_id,\n           percentile_cont(0.5) WITHIN GROUP (ORDER BY pd.close_p) AS sku_median,\n           max(pd.max_p) AS sku_list,    -- xấp xỉ giá niêm yết = đỉnh quan sát\n           min(pd.min_p) AS sku_floor\n    FROM tracked_product tp\n    JOIN price_daily pd ON pd.product_id = tp.id\n    WHERE tp.category_id IS NOT NULL\n      AND now() - tp.first_seen &gt;= INTERVAL '90 days'   -- chỉ SKU MATURE\n    GROUP BY tp.id, tp.category_id\n  )\n  SELECT category_id,\n         percentile_cont(0.5) WITHIN GROUP (ORDER BY sku_median)              AS median_price,\n         percentile_cont(0.5) WITHIN GROUP (\n           ORDER BY (sku_list - sku_floor)::float / NULLIF(sku_list, 0))      AS typical_discount_depth,\n         count(*)                                                             AS sample_count\n  FROM mature_sku\n  GROUP BY category_id;\n\nCREATE UNIQUE INDEX idx_category_prior_cat ON category_prior (category_id);\n-- Refresh hằng ngày (REFRESH ... CONCURRENTLY, đăng ký ở scheduler deal-svc, §1 #11).</code></pre>\n<h3>Maturity (Go)</h3>\n<pre><code class=\"language-go\">// services/deal/internal/coldstart/maturity.go\npackage coldstart\n\ntype State int\n\nconst (\n    StateNew     State = iota // &lt; 14 ngày  -&gt; UNKNOWN\n    StateWarming              // 14-90 ngày -&gt; low-confidence + \"đang tích lũy\"\n    StateMature              // &gt;= 90 ngày -&gt; full confidence\n)\n\nconst (\n    warmingDays = 14\n    matureDays  = 90\n    minSamples  = 30 // sàn sample_count cho prior (§1 #12)\n)\n\n// Maturity ánh xạ số ngày lịch sử sang trạng thái trưởng thành (DEC-DEAL-10).\nfunc Maturity(daysOfHistory int) State {\n    switch {\n    case daysOfHistory &lt; warmingDays:\n        return StateNew\n    case daysOfHistory &lt; matureDays:\n        return StateWarming\n    default:\n        return StateMature\n    }\n}\n\n// IsFeatureReady: cổng baseline 90 ngày cho sale ảo/biểu đồ công khai (DEC-DEAL-11).\nfunc IsFeatureReady(p Product) bool {\n    return Maturity(p.DaysOfHistory()) == StateMature\n}</code></pre>\n<h3>Priors (Go)</h3>\n<pre><code class=\"language-go\">// services/deal/internal/coldstart/priors.go\npackage coldstart\n\ntype CategoryPrior struct {\n    CategoryID    int64   `db:\"category_id\"`\n    MedianPrice   int64   `db:\"median_price\"`\n    DiscountDepth float64 `db:\"typical_discount_depth\"`\n    SampleCount   int     `db:\"sample_count\"`\n}\n\n// PriorFor đọc prior của 1 category; ok=false khi không có category,\n// hoặc sample_count dưới sàn min_sample_count (prior quá mỏng, §1 #12).\nfunc (r *Repo) PriorFor(ctx context.Context, categoryID int64) (CategoryPrior, bool, error) {\n    var cp CategoryPrior\n    err := r.pool.QueryRow(ctx,\n        `SELECT category_id, median_price, typical_discount_depth, sample_count\n         FROM category_prior WHERE category_id = $1`, categoryID).\n        Scan(&amp;cp.CategoryID, &amp;cp.MedianPrice, &amp;cp.DiscountDepth, &amp;cp.SampleCount)\n    if errors.Is(err, pgx.ErrNoRows) {\n        return CategoryPrior{}, false, nil\n    }\n    if err != nil {\n        return CategoryPrior{}, false, err\n    }\n    if cp.SampleCount &lt; minSamples {\n        return cp, false, nil // prior mỏng -&gt; coi như không đủ tin\n    }\n    return cp, true, nil\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>daysOfHistory</code> của 1 SKU = số ngày từ <code>first_seen</code> (hoặc snapshot sớm nhất) tới nay, tính đúng kể cả khi snapshot sớm hơn <code>first_seen</code>.</li><li><code>Maturity(d)</code> trả <code>NEW</code> với <code>d&lt;14</code>, <code>WARMING</code> với <code>14&lt;=d&lt;90</code>, <code>MATURE</code> với <code>d&gt;=90</code>.</li><li>SKU <code>NEW</code> (<code>d&lt;14</code>) -&gt; verdict <code>UNKNOWN</code> (handoff TASK-DEAL-001), không có ngoại lệ.</li><li>SKU <code>WARMING</code> -&gt; verdict gắn cờ <code>low_confidence</code> VÀ biểu đồ kèm chú thích <code>\"đang tích lũy\"</code>; không phát verdict đầy đủ.</li><li>SKU <code>MATURE</code> -&gt; cho phép verdict đầy đủ và biểu đồ công khai không cảnh báo dữ liệu mỏng.</li><li>Materialized view <code>category_prior</code> tồn tại, có cột <code>median_price</code>, <code>typical_discount_depth</code>, <code>sample_count</code> theo <code>category_id</code>.</li><li><code>category_prior</code> KHÔNG chứa đóng góp từ SKU <code>NEW</code>/<code>WARMING</code> (chỉ SKU <code>&gt;= 90</code> ngày vào mẫu).</li><li><code>IsFeatureReady(product)</code> trả <code>true</code> chỉ khi SKU <code>MATURE</code>; <code>false</code> cho <code>NEW</code> và <code>WARMING</code>.</li><li>SKU non (<code>NEW</code>/<code>WARMING</code>) dùng <code>PriorFor(category_id)</code> làm fallback; SKU <code>MATURE</code> dùng lịch sử riêng, không gọi prior.</li><li>SKU có <code>category_id</code> NULL -&gt; <code>UNKNOWN</code>, <code>PriorFor</code> trả <code>ok=false</code> (không fallback).</li><li>Sau refresh theo lịch, <code>category_prior</code> phản ánh đúng tập SKU <code>MATURE</code> hiện tại.</li><li><code>PriorFor</code> của category có <code>sample_count &lt; 30</code> trả <code>ok=false</code> (prior mỏng, fallback về UNKNOWN).</li><li>Metric <code>deal_maturity_state_total{state}</code> tăng đúng nhãn; <code>category_prior_fallback_total</code> tăng khi dùng prior.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/deal/internal/coldstart/maturity_test.go\nfunc TestMaturity_Boundaries(t *testing.T) {\n    cases := []struct {\n        days int\n        want State\n    }{\n        {13, StateNew}, {14, StateWarming}, {89, StateWarming}, {90, StateMature},\n    }\n    for _, c := range cases {\n        require.Equal(t, c.want, Maturity(c.days), \"days=%d\", c.days)\n    }\n}\n\nfunc TestIsFeatureReady_Gate90d(t *testing.T) {\n    require.False(t, IsFeatureReady(fakeProduct(89)))\n    require.True(t, IsFeatureReady(fakeProduct(90)))\n    require.True(t, IsFeatureReady(fakeProduct(200)))\n}\n\nfunc TestPrior_ExcludesImmature(t *testing.T) {\n    r := setupRepo(t)\n    seedProduct(t, r, catID, 120, 100_000) // MATURE, vào mẫu\n    seedProduct(t, r, catID, 5, 10_000)    // NEW, PHẢI bị loại\n    refreshCategoryPrior(t, r)\n\n    cp, ok, _ := r.PriorFor(ctx, catID)\n    require.True(t, ok)\n    require.Equal(t, 1, cp.SampleCount)        // chỉ SKU MATURE\n    require.Equal(t, int64(100_000), cp.MedianPrice) // SKU NEW không kéo lệch\n}\n\nfunc TestPriorFallback_WhenNew(t *testing.T) {\n    r := setupRepo(t)\n    seedMatureCategory(t, r, catID, 40) // 40 SKU MATURE -&gt; qua sàn\n    refreshCategoryPrior(t, r)\n\n    newp := fakeProductInCategory(5, catID) // NEW\n    require.Equal(t, StateNew, Maturity(newp.DaysOfHistory()))\n    cp, ok, _ := r.PriorFor(ctx, catID)\n    require.True(t, ok) // SKU non mượn prior của category làm fallback\n    require.Greater(t, cp.MedianPrice, int64(0))\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration <code>0001_category_prior.sql</code> (materialized view + unique index) -&gt; <code>maturity.go</code> (State + Maturity + IsFeatureReady) -&gt; <code>priors.go</code> (PriorFor + sàn min_sample_count) -&gt; chèn cổng vào <code>detect.go</code> (handoff TASK-DEAL-001) -&gt; tests. Materialized view tính từ <code>price_daily</code> (TASK-PRICE-002) và <code>tracked_product</code> (TASK-PRICE-001); refresh hằng ngày qua scheduler của deal-svc (<code>REFRESH MATERIALIZED VIEW CONCURRENTLY category_prior</code>, cần unique index nên đã tạo <code>idx_category_prior_cat</code>). Driver dùng <code>pgx</code>. Cổng <code>IsFeatureReady</code> đặt ngay đầu nhánh surface verdict công khai trong <code>detect.go</code>: nếu <code>false</code>, trả nhánh \"đang tích lũy\" thay vì verdict.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-DEAL-001</strong> - bộ phát hiện sale ảo cung cấp verdict enum (gồm <code>UNKNOWN</code>); task này nối vào cổng maturity trước khi surface verdict tự tin.</li><li><strong>TASK-PRICE-001</strong> - <code>tracked_product.first_seen</code> và <code>category_id</code> là input tính <code>daysOfHistory</code> và khóa gộp prior.</li><li><strong>TASK-PRICE-002</strong> - <code>price_daily</code> (continuous aggregate) và <code>price_snapshot</code> là nguồn cho <code>category_prior</code>.</li><li><strong>TASK-DEAL-004 (downstream)</strong> - Prophet baseline dùng category priors làm điểm khởi đầu cho SKU cold-start.</li><li>Extension/lib: PostgreSQL <code>percentile_cont</code>, materialized view <code>REFRESH ... CONCURRENTLY</code>; driver <code>pgx</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>SKU mới 5 ngày -&gt; UNKNOWN + fallback prior</h3>\n<pre><code class=\"language-go\">p := loadProduct(ctx, 90112) // first_seen = 5 ngày trước, category_id = 4221\nstate := coldstart.Maturity(p.DaysOfHistory())   // StateNew\nready := coldstart.IsFeatureReady(p)             // false -&gt; không bật tính năng công khai\nverdict := \"UNKNOWN\"                              // §1 #3, không đoán mò\n\n// fallback: mượn prior của category để ước lượng tạm trong UI \"đang tích lũy\"\ncp, ok, _ := repo.PriorFor(ctx, 4221)\n// ok=true nếu category 4221 có &gt;= 30 SKU MATURE; dùng cp.MedianPrice / cp.DiscountDepth</code></pre>\n<h3>SKU chín 120 ngày -&gt; IsFeatureReady true</h3>\n<pre><code class=\"language-go\">p := loadProduct(ctx, 77003) // first_seen = 120 ngày trước\nstate := coldstart.Maturity(p.DaysOfHistory())   // StateMature\nready := coldstart.IsFeatureReady(p)             // true -&gt; sale ảo + biểu đồ công khai bật\n// dùng lịch sử riêng của SKU (median90, đáy gần đây), KHÔNG dùng category_prior</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Ngưỡng <code>warmingDays</code>/<code>matureDays</code> có nên khác theo category (hàng thời trang đổi giá nhanh hơn điện máy) - cấu hình per-category ở slice sau.</li><li><code>min_sample_count</code> cố định 30 hay co giãn theo độ phân tán giá của category - tinh chỉnh khi có dữ liệu thực.</li><li>Prior theo trục thời gian (mùa double-date, payday) ngoài median tĩnh - tách sang TASK-DEAL-004 nơi Prophet xử lý mùa vụ.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>category_id NULL</td><td>check trong code</td><td>không có prior để mượn</td><td>Trả UNKNOWN, không fallback (§1 #10)</td></tr><tr><td>prior tính từ quá ít mẫu</td><td>sàn min_sample_count</td><td>prior lệch, ước lượng sai</td><td>sample_count &lt; 30 -&gt; ok=false, fallback UNKNOWN</td></tr><tr><td>SKU non lọt vào prior aggregate</td><td>test ExcludesImmature</td><td>vòng phản hồi thiên lệch</td><td>WHERE first_seen &gt;= 90d trong view</td></tr><tr><td>Lỗ hổng lịch sử undercount số ngày</td><td>so first_seen với snapshot sớm nhất</td><td>SKU chín bị coi là non</td><td>Lấy mốc sớm hơn giữa first_seen và snapshot đầu</td></tr><tr><td>Materialized view stale</td><td>tuổi refresh / log scheduler</td><td>prior trễ vài giờ tới 1 ngày</td><td>Refresh hằng ngày; chạy thủ công nếu cần</td></tr><tr><td>Verdict tự tin lọt qua cho SKU WARMING</td><td>test cổng</td><td>mất niềm tin (§5.4)</td><td>IsFeatureReady chặn ở 90d trước khi surface</td></tr><tr><td>Ngưỡng 14/90 rải rác dạng số ma thuật</td><td>code review</td><td>lệch ngưỡng giữa các nơi</td><td>Hằng số có tên warmingDays/matureDays</td></tr><tr><td>REFRESH CONCURRENTLY thiếu unique index</td><td>lỗi Postgres</td><td>refresh khóa hoặc fail</td><td>idx_category_prior_cat tạo cùng migration</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Cold-start là bài toán con-gà-quả-trứng cốt lõi của SănDeal (§5.1): tính năng cần lịch sử, lịch sử cần thời gian. task này biến nó thành chính sách rõ ràng thay vì hành vi ngầm.</li><li><code>UNKNOWN</code> không phải lỗi mà là câu trả lời trung thực: thà im lặng còn hơn phát verdict sai và mất niềm tin (§5.4).</li><li>Loại SKU non khỏi category_prior là chi tiết dễ sai nhất: quên điều kiện <code>first_seen &gt;= 90d</code> thì prior tự nhiễm nhiễu của chính giai đoạn cold-start.</li><li>Cổng <code>IsFeatureReady</code> tách \"đã có dữ liệu\" khỏi \"đủ dữ liệu để công khai\" - SKU WARMING vẫn tích lũy và hiển thị \"đang tích lũy\", chỉ không phát verdict công khai.</li><li>Ưu tiên backfill sớm cho top SKU (TASK-SCRAPE-002) để chúng qua cổng 90 ngày càng nhanh càng tốt - đây là đòn bẩy rút ngắn thời gian ra mắt tính năng.</li><li>category_prior là cầu nối tới TASK-DEAL-004: cùng một prior dùng cho fallback verdict cũng là điểm khởi đầu cho Prophet baseline trên SKU cold-start.</li></ul>\n<hr>\n<p><em>Hết TASK-DEAL-002. Status: done (mục tiêu audit 10/10).</em></p>";
