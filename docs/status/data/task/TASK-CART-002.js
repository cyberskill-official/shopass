window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CART-002"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service CART <strong>MUST</strong> định nghĩa schema <code>cart_snapshot</code> + <code>cart_item</code> và API nhận ảnh chụp giỏ hàng từ extension, chứa CHỈ dữ liệu tối thiểu hóa (qty, unit_price VND, shop_id), gắn chủ sở hữu từ JWT, làm đầu vào cho optimizer. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> định nghĩa <code>cart_snapshot (id BIGSERIAL PK, user_id BIGINT REFERENCES app_user(id), platform_id SMALLINT REFERENCES platform(id), snapshot_ref UUID, captured_at TIMESTAMPTZ NOT NULL DEFAULT now())</code>.</li><li><strong>MUST</strong> định nghĩa <code>cart_item (id BIGSERIAL PK, cart_snapshot_id BIGINT REFERENCES cart_snapshot(id) ON DELETE CASCADE, product_id BIGINT REFERENCES tracked_product(id), platform_item_id TEXT, shop_id TEXT, qty INTEGER NOT NULL, unit_price BIGINT NOT NULL)</code>.</li><li><strong>MUST</strong> lưu <code>unit_price</code> dạng <code>BIGINT</code> VND và <code>qty</code> <code>INTEGER</code> (DEC-CART-08): <code>qty &gt; 0</code>, <code>unit_price &gt; 0</code> qua CHECK. KHÔNG dùng float.</li><li><code>cart_snapshot</code> và <code>cart_item</code> <strong>MUST NOT</strong> chứa bất kỳ cột nào lưu cookie, session token, header xác thực, hay credential (DEC-CART-09). Chỉ chứa dữ liệu tối thiểu hóa do TASK-EXT-003 cho qua (tham chiếu sản phẩm + qty + unit_price + shop_id).</li><li>API <code>POST /v1/cart/snapshot</code> <strong>MUST</strong> lấy <code>user_id</code> từ JWT (do gateway gắn, TASK-AUTH-002/TASK-INFRA-001), KHÔNG từ payload extension (DEC-CART-10); payload có trường tên user_id/owner bị bỏ qua.</li><li>Handler <strong>MUST</strong> từ chối (hoặc loại bỏ) payload chứa bất kỳ trường nào tên cookie/token/session/authorization/header (defense in depth cùng TASK-EXT-003) - không bao giờ ghi credential vào DB.</li><li>Ghi snapshot <strong>MUST</strong> trong một transaction: tạo <code>cart_snapshot</code> rồi chèn tất cả <code>cart_item</code>; lỗi giữa chừng rollback toàn bộ (ảnh chụp nguyên tử).</li><li>Ghi snapshot <strong>MUST</strong> idempotent theo <code>snapshot_ref</code> (UUID client cung cấp) trong cửa sổ retry (DEC-CART-11): cùng <code>snapshot_ref</code> gửi lại trả snapshot đã ghi, không tạo bản trùng. <code>UNIQUE (user_id, snapshot_ref)</code>.</li><li><code>cart_item.product_id</code> <strong>MUST</strong> tham chiếu <code>tracked_product</code> khi SKU đã biết; nếu chưa track, <strong>MUST</strong> vẫn ghi với <code>product_id</code> NULL + <code>platform_item_id</code> thô + <code>shop_id</code> (DEC-CART-12) - không chặn ghi để optimizer vẫn tính được giỏ tạm.</li><li>Đọc snapshot (<code>GetSnapshot</code>) <strong>MUST</strong> scope theo <code>user_id</code>: user chỉ đọc snapshot của chính mình; truy cập snapshot user khác trả <code>404</code> (không phân biệt không-tồn-tại với của-người-khác).</li><li>Ảnh chụp <strong>MUST</strong> bất biến sau khi ghi (DEC-CART-11): không cập nhật cart_item của snapshot đã ghi; giỏ đổi thì tạo snapshot mới (lịch sử giỏ).</li><li><strong>SHOULD</strong> phát OTel <code>cart_snapshot_written_total{platform_id}</code> và <code>cart_item_count</code> (histogram số item/giỏ) để theo dõi kích thước giỏ.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao tách snapshot khỏi item (DEC-CART-07)?</strong> Một giỏ hàng là một tập SKU tại một thời điểm. <code>cart_snapshot</code> là tiêu đề (ai, sàn nào, lúc nào); <code>cart_item</code> là các dòng. Tách ra cho phép một user có nhiều ảnh chụp theo thời gian (lịch sử giỏ) và optimizer tính trên một ảnh chụp cụ thể. Đây là mẫu header-detail kinh điển.</p>\n<p><strong>Vì sao tuyệt đối không cột credential (DEC-CART-09)?</strong> Định vị lõi của SănDeal hậu-Honey là tối thiểu hóa dữ liệu (§5.4) và token-không-rời-client (§3.2). Nếu schema có dù chỉ một cột lưu cookie/token thì phá vỡ cam kết đó - một sự cố rò rỉ là thảm họa PDPL. Schema chỉ chứa dữ liệu giỏ đã tối thiểu hóa. Đây là ranh giới thiết kế, không chỉ quy ước - không có cột thì không thể lỡ lưu.</p>\n<p><strong>Vì sao user_id từ JWT không từ payload (DEC-CART-10)?</strong> Extension chạy trên máy người dùng - payload có thể bị giả mạo. Nếu tin <code>user_id</code> trong payload thì một payload độc gắn giỏ vào user khác (làm loạn/chiếm dữ liệu). Lấy <code>user_id</code> từ JWT đã verify (gateway) là nguồn chủ sở hữu đáng tin duy nhất. Payload chỉ mang dữ liệu giỏ, không mang danh tính.</p>\n<p><strong>Vì sao idempotent theo snapshot_ref (DEC-CART-11)?</strong> Extension có retry (mạng chập chờn); cùng một lần chụp giỏ có thể gửi hai lần. <code>snapshot_ref</code> (UUID client sinh một lần cho một lần chụp) + UNIQUE làm gửi lại trả snapshot cũ thay vì tạo bản trùng. Ảnh chụp bất biến nên trả lại bản đã ghi là đúng.</p>\n<p><strong>Vì sao vẫn ghi khi SKU chưa track (DEC-CART-12)?</strong> Giỏ người dùng thường có SKU SănDeal chưa từng thấy (chưa có trong <code>tracked_product</code>). Nếu chặn ghi vì FK thì mất khả năng tối ưu giỏ đó. Cho <code>product_id</code> NULL + giữ <code>platform_item_id</code> thô để optimizer vẫn tính giá trị đơn (đủ cho min_spend voucher); track sau (TASK-TRACK-001) khi cần lịch sử giá.</p>\n<p><strong>Vì sao ảnh chụp bất biến (§1 #11)?</strong> Giỏ là sự kiện tại một thời điểm. Sửa ảnh chụp cũ làm mất ý nghĩa \"giỏ lúc đó\". Giỏ đổi thì tạo snapshot mới - giữ lịch sử và tránh ghi đè nhầm.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration (golang-migrate)</h3>\n<pre><code class=\"language-sql\">-- db/migrations/0011_cart_snapshot.up.sql\nCREATE TABLE cart_snapshot (\n  id           BIGSERIAL   PRIMARY KEY,\n  user_id      BIGINT      NOT NULL REFERENCES app_user(id),\n  platform_id  SMALLINT    NOT NULL REFERENCES platform(id),\n  snapshot_ref UUID        NOT NULL,\n  captured_at  TIMESTAMPTZ NOT NULL DEFAULT now(),\n  UNIQUE (user_id, snapshot_ref)        -- idempotent retry (DEC-CART-11)\n);\n\nCREATE TABLE cart_item (\n  id               BIGSERIAL PRIMARY KEY,\n  cart_snapshot_id BIGINT    NOT NULL REFERENCES cart_snapshot(id) ON DELETE CASCADE,\n  product_id       BIGINT    REFERENCES tracked_product(id),  -- NULL nếu chưa track (DEC-CART-12)\n  platform_item_id TEXT,                                      -- giữ ref thô khi product_id NULL\n  shop_id          TEXT,\n  qty              INTEGER   NOT NULL CHECK (qty &gt; 0),\n  unit_price       BIGINT    NOT NULL CHECK (unit_price &gt; 0), -- VND\n  -- phải có ít nhất một cách định danh SKU\n  CONSTRAINT item_identified CHECK (product_id IS NOT NULL OR platform_item_id IS NOT NULL)\n);\nCREATE INDEX idx_ci_snapshot ON cart_item (cart_snapshot_id);\n\n-- db/migrations/0011_cart_snapshot.down.sql\nDROP TABLE cart_item;\nDROP TABLE cart_snapshot;</code></pre>\n<h3>Types + DTO (snapshot_types.go)</h3>\n<pre><code class=\"language-go\">// services/cart/internal/cart/snapshot_types.go\ntype CartItem struct {\n    ProductID      *int64  `db:\"product_id\"`        // NULL nếu chưa track\n    PlatformItemID *string `db:\"platform_item_id\"`\n    ShopID         *string `db:\"shop_id\"`\n    Qty            int32   `db:\"qty\"`\n    UnitPrice      int64   `db:\"unit_price\"`        // VND\n}\ntype CartSnapshot struct {\n    ID          int64     `db:\"id\"`\n    UserID      int64     `db:\"user_id\"`            // gắn từ JWT, KHÔNG từ payload\n    PlatformID  int16     `db:\"platform_id\"`\n    SnapshotRef uuid.UUID `db:\"snapshot_ref\"`\n    CapturedAt  time.Time `db:\"captured_at\"`\n    Items       []CartItem\n}\n\n// DTO nhận từ extension - CHÚ Ý: KHÔNG có user_id, KHÔNG có cookie/token (DEC-CART-09/10)\ntype SnapshotRequest struct {\n    PlatformID  int16     `json:\"platform_id\"`\n    SnapshotRef uuid.UUID `json:\"snapshot_ref\"`\n    Items       []struct {\n        PlatformItemID *string `json:\"platform_item_id\"`\n        ShopID         *string `json:\"shop_id\"`\n        Qty            int32   `json:\"qty\"`\n        UnitPrice      int64   `json:\"unit_price\"` // VND int64\n    } `json:\"items\"`\n}</code></pre>\n<h3>Handler (snapshot.go) - user_id từ JWT, không từ payload</h3>\n<pre><code class=\"language-go\">// services/cart/internal/api/snapshot.go\nfunc (h *Handler) HandleSnapshot(w http.ResponseWriter, req *http.Request) {\n    userID := auth.UserIDFromContext(req.Context()) // từ JWT (gateway), KHÔNG từ payload (DEC-CART-10)\n    if userID == 0 {\n        writeErr(w, http.StatusUnauthorized, \"unauthenticated\"); return\n    }\n    var in SnapshotRequest\n    if err := json.NewDecoder(req.Body).Decode(&amp;in); err != nil {\n        writeErr(w, http.StatusBadRequest, \"invalid payload\"); return\n    }\n    if err := validateSnapshot(in); err != nil {     // qty&gt;0, unit_price&gt;0, có định danh SKU\n        writeErr(w, http.StatusBadRequest, err.Error()); return\n    }\n    snap, err := h.repo.InsertSnapshot(req.Context(), userID, in) // idempotent theo (user_id, snapshot_ref)\n    if err != nil {\n        writeErr(w, http.StatusInternalServerError, \"internal error\"); return\n    }\n    w.WriteHeader(http.StatusCreated)\n    _ = json.NewEncoder(w).Encode(map[string]any{\"snapshot_id\": snap.ID, \"item_count\": len(snap.Items)})\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch (up/down); <code>cart_snapshot</code> + <code>cart_item</code> tồn tại với CHECK <code>qty&gt;0</code>, <code>unit_price&gt;0</code>, <code>item_identified</code>, và <code>UNIQUE(user_id, snapshot_ref)</code>.</li><li>Grep <code>cart_snapshot</code>/<code>cart_item</code> schema + types + DTO: KHÔNG có cột/trường nào tên cookie/token/session/authorization/header.</li><li>INSERT <code>qty&lt;=0</code> hoặc <code>unit_price&lt;=0</code> -&gt; lỗi CHECK; <code>unit_price</code> round-trip int64 (không float).</li><li><code>POST /v1/cart/snapshot</code> lấy <code>user_id</code> từ JWT context; payload có trường <code>user_id</code>/<code>owner</code> bị bỏ qua (không ghi đè chủ sở hữu).</li><li>Payload chứa trường tên cookie/token/... bị từ chối hoặc loại bỏ; DB không bao giờ có giá trị đó.</li><li>Ghi snapshot là transaction: snapshot + items cùng commit; lỗi giữa chừng rollback (không snapshot mồ côi không item).</li><li>Cùng <code>snapshot_ref</code> (cùng user) gửi hai lần -&gt; trả snapshot đã ghi, một bản (idempotent), không nhân item.</li><li>SKU chưa track (không product_id) vẫn ghi với <code>platform_item_id</code> + <code>shop_id</code>; CHECK <code>item_identified</code> chấp nhận.</li><li><code>GetSnapshot</code> của user A không trả snapshot của user B (scope user_id; <code>404</code> cho cross-user).</li><li>Snapshot đã ghi bất biến: không có đường cập nhật cart_item của snapshot cũ (chỉ tạo mới).</li><li><code>go test ./...</code> xanh.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/cart/internal/cart/snapshot_repo_test.go\nfunc TestInsertSnapshot_TransactionalWithItems(t *testing.T) {\n    r, uid := setupCart(t)\n    snap, err := r.InsertSnapshot(ctx, uid, reqWithItems(2)) // 2 item\n    require.NoError(t, err)\n    require.Len(t, snap.Items, 2)\n    require.Equal(t, uid, snap.UserID) // chủ sở hữu từ JWT\n}\n\nfunc TestInsertSnapshot_Idempotent(t *testing.T) {\n    r, uid := setupCart(t)\n    ref := uuid.New()\n    in := reqWithRef(ref, 2)\n    s1, _ := r.InsertSnapshot(ctx, uid, in)\n    s2, _ := r.InsertSnapshot(ctx, uid, in) // cùng (user, snapshot_ref)\n    require.Equal(t, s1.ID, s2.ID)          // một bản\n    require.Equal(t, 2, countItems(t, r, s1.ID)) // không nhân item\n}\n\nfunc TestInsertSnapshot_UntrackedSku_StillWrites(t *testing.T) {\n    r, uid := setupCart(t)\n    in := reqUntracked(\"ITEM-999\", \"shopX\", 1, 50_000) // không product_id\n    snap, err := r.InsertSnapshot(ctx, uid, in)\n    require.NoError(t, err) // CHECK item_identified chấp nhận (DEC-CART-12)\n    require.Nil(t, snap.Items[0].ProductID)\n    require.Equal(t, \"ITEM-999\", *snap.Items[0].PlatformItemID)\n}\n\nfunc TestGetSnapshot_CrossUser_404(t *testing.T) {\n    r, uidA := setupCart(t)\n    snap, _ := r.InsertSnapshot(ctx, uidA, reqWithItems(1))\n    uidB := makeUser(t, r)\n    _, err := r.GetSnapshot(ctx, uidB, snap.ID) // user B đọc snapshot của A\n    require.ErrorIs(t, err, ErrNotFound)\n}\n\nfunc TestCheck_QtyAndPrice(t *testing.T) {\n    r, uid := setupCart(t)\n    _, err := r.InsertSnapshot(ctx, uid, reqBadQty(0))\n    require.Error(t, err) // CHECK qty &gt; 0\n}</code></pre>\n<pre><code class=\"language-go\">// services/cart/internal/api/snapshot_test.go\nfunc TestHandler_UserIDFromJWT_NotPayload(t *testing.T) {\n    h, uid := setupHandler(t)\n    body := `{\"platform_id\":1,\"snapshot_ref\":\"...\",\"user_id\":99999,\"items\":[{\"platform_item_id\":\"A\",\"qty\":1,\"unit_price\":50000}]}`\n    rec := doPostWithJWT(t, h, \"/v1/cart/snapshot\", body, uid)\n    require.Equal(t, 201, rec.Code)\n    snap := lastSnapshot(t, h, uid)\n    require.Equal(t, uid, snap.UserID) // user_id = JWT, KHÔNG phải 99999 trong payload\n}\n\nfunc TestHandler_RejectsCredentialFields(t *testing.T) {\n    h, uid := setupHandler(t)\n    body := `{\"platform_id\":1,\"snapshot_ref\":\"...\",\"cookie\":\"SPC=abc\",\"token\":\"xyz\",\"items\":[{\"platform_item_id\":\"A\",\"qty\":1,\"unit_price\":50000}]}`\n    doPostWithJWT(t, h, \"/v1/cart/snapshot\", body, uid)\n    // khẳng định không giá trị credential nào lọt vào DB\n    require.False(t, anyColumnContains(t, h, \"abc\"))\n    require.False(t, anyColumnContains(t, h, \"xyz\"))\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>0011_cart_snapshot.up/down.sql</code> (hai bảng + CHECK + UNIQUE + index) -&gt; <code>snapshot_types.go</code> (CartSnapshot/CartItem BIGINT + DTO không-credential) -&gt; <code>snapshot_repo.go</code> (<code>InsertSnapshot</code> transaction, <code>GetSnapshot</code> scope user) -&gt; <code>snapshot.go</code> (handler: user_id từ JWT, validate, idempotent) -&gt; đăng ký route -&gt; tests. Migration 0011 nối tiếp 0010 (TASK-CART-001). Handler nằm sau JWT middleware của gateway (TASK-INFRA-001); <code>auth.UserIDFromContext</code> đọc sub đã verify. DTO <code>SnapshotRequest</code> cố tình KHÔNG có trường user_id/cookie/token - decode JSON bỏ qua khóa thừa, và test khẳng định không giá trị credential nào vào DB (defense in depth cùng TASK-EXT-003).</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-EXT-003</strong> - pipeline tối thiểu hóa dữ liệu client quyết định trường nào được gửi (productId/price/qty, không cookie/token); TASK-CART-002 nhận đúng tập đó (depends_on cứng).</li><li><strong>TASK-EXT-002 (nguồn)</strong> - content script đọc giỏ; dữ liệu đi qua TASK-EXT-003 rồi tới API này.</li><li><strong>TASK-INFRA-002</strong> - bảng <code>platform</code> + <code>app_user</code> + <code>tracked_product</code> (FK) phải có trước; migration framework.</li><li><strong>TASK-CART-001 (sibling)</strong> - voucher_catalog; cùng nuôi optimizer TASK-CART-003.</li><li><strong>TASK-CART-003 (downstream)</strong> - optimizer đọc một <code>cart_snapshot</code> làm giỏ để tối ưu voucher.</li><li>Extension/lib: <code>pgx</code>, <code>google/uuid</code>; golang-migrate.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Extension gửi ảnh chụp giỏ (tối thiểu, KHÔNG credential, KHÔNG user_id)</h3>\n<pre><code class=\"language-json\">{\n  \"platform_id\": 1,\n  \"snapshot_ref\": \"2f1c9b7a-1e2d-4c3b-9a0f-7d6e5c4b3a21\",\n  \"items\": [\n    { \"platform_item_id\": \"90112\", \"shop_id\": \"shopA\", \"qty\": 1, \"unit_price\": 89000 },\n    { \"platform_item_id\": \"77310\", \"shop_id\": \"shopB\", \"qty\": 2, \"unit_price\": 245000 }\n  ]\n}</code></pre>\n<h3>Phản hồi (201)</h3>\n<pre><code class=\"language-json\">{ \"snapshot_id\": 5012, \"item_count\": 2 }</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Gắn <code>product_id</code> tự động bằng cách track ngầm SKU chưa biết (gọi TASK-TRACK-001) lúc nhận snapshot - cân nhắc cân bằng với chi phí scraping.</li><li>Dọn snapshot cũ (retention giỏ) - thêm policy khi dữ liệu giỏ phình; ảnh chụp là dữ liệu cá nhân nên gắn DSAR (TASK-COMPLY-003).</li><li>Gộp item trùng SKU trong một giỏ (cùng product, cộng qty) - quyết khi gặp dữ liệu thật.</li><li>Đa tiền tệ khi mở SEA - giữ unit_price BIGINT theo minor unit từng nước.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Cột/trường credential trong giỏ</td><td>grep schema + test</td><td>rò rỉ token -&gt; thảm họa PDPL</td><td>KHÔNG cột credential (DEC-CART-09)</td></tr><tr><td>user_id từ payload</td><td>handler test</td><td>giả mạo chủ sở hữu</td><td>Lấy từ JWT (DEC-CART-10)</td></tr><tr><td>unit_price float</td><td>review + test</td><td>sai số so min_spend</td><td>BIGINT VND (DEC-CART-08)</td></tr><tr><td>Chặn ghi khi SKU chưa track</td><td>untracked test</td><td>mất tối ưu giỏ tạm</td><td>product_id NULL + platform_item_id (DEC-CART-12)</td></tr><tr><td>Snapshot mồ côi không item</td><td>transaction test</td><td>giỏ rỗng giả</td><td>Ghi trong transaction (§1 #7)</td></tr><tr><td>Retry tạo bản trùng</td><td>idempotent test</td><td>giỏ nhân đôi</td><td>UNIQUE(user_id, snapshot_ref) (DEC-CART-11)</td></tr><tr><td>Đọc giỏ user khác</td><td>cross-user test</td><td>rò rỉ ý định mua</td><td>Scope user_id, 404 (§1 #10)</td></tr><tr><td>Sửa ảnh chụp cũ</td><td>review</td><td>mất ý nghĩa lịch sử</td><td>Ảnh chụp bất biến (§1 #11)</td></tr><tr><td>qty/unit_price &lt;= 0</td><td>DB CHECK</td><td>giỏ vô nghĩa</td><td>CHECK qty&gt;0, unit_price&gt;0</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li><code>cart_snapshot</code> là cách backend nhận giỏ extension đọc được để optimizer (TASK-CART-003) tối ưu voucher - đầu vào của tính năng tối ưu giỏ (persona Linh).</li><li>Tuyệt đối không cột credential là ranh giới thiết kế, không chỉ quy ước - không có cột thì không thể lỡ lưu cookie/token, giữ cam kết tối thiểu hóa (§5.4) và token-không-rời-client (§3.2).</li><li>user_id từ JWT (không từ payload) chống giả mạo chủ sở hữu - payload chỉ mang dữ liệu giỏ, không mang danh tính.</li><li>Idempotent theo snapshot_ref cho retry extension không tạo giỏ trùng; ảnh chụp bất biến.</li><li>Cho ghi SKU chưa track (product_id NULL) giữ khả năng tối ưu giỏ tạm - track sau khi cần lịch sử.</li><li>Header-detail (snapshot + item) cho phép lịch sử giỏ và tối ưu trên một ảnh chụp cụ thể.</li></ul>\n<hr>\n<p><em>Hết TASK-CART-002. Status: done (mục tiêu audit 10/10).</em></p>";
