window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CART-003"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service CART <strong>MUST</strong> hiện thực <code>optimizeCart</code> - thuật toán knapsack-like chọn tổ hợp voucher (shop + platform + freeship) cho tổng giảm lớn nhất trên một giỏ hàng, bám đúng cấu trúc pseudo-code §3.5(3), với luật stacking ủy quyền cho TASK-CART-004 và mọi phép tính bằng số nguyên VND. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> hiện thực <code>optimizeCart(cartItems, vouchers, rules)</code> là hàm thuần (DEC-CART-17): không đọc DB; nhận giỏ (từ TASK-CART-002) + voucher còn hiệu lực (từ TASK-CART-001 <code>ListActive</code>) + luật stacking, trả combo tối ưu.</li><li><strong>MUST</strong> bám cấu trúc pseudo-code §3.5(3) (DEC-CART-13): lọc voucher shop theo min_spend (<code>filterByMinSpend</code>), rồi duyệt mọi tổ hợp <code>(platformVoucher hoặc none) x (freeship hoặc none) x chooseBestShopVoucherPerShop</code>, bỏ tổ hợp <code>!validStack</code> hoặc <code>!meetsMinSpend</code>, tính tổng giảm với <code>applyCaps</code>, giữ <code>best.discount</code>.</li><li><strong>MUST</strong> dùng <code>chooseBestShopVoucherPerShop</code> chọn voucher shop tốt nhất cho TỪNG shop độc lập (một shop tối đa một voucher shop) trên các shop có trong giỏ (DEC-CART-16).</li><li><strong>MUST</strong> kiểm <code>validStack(platformVoucher, freeship, shopVouchers)</code> qua interface <code>StackRules</code> do TASK-CART-004 hiện thực (DEC-CART-14); TASK-CART-003 KHÔNG hardcode luật stacking theo nước - chỉ gọi <code>rules.ValidStack</code>.</li><li><strong>MUST</strong> kiểm <code>meetsMinSpend</code>: tổng giá trị đơn (hoặc đơn của shop tương ứng) phải đạt <code>min_spend</code> của voucher trước khi áp; voucher không đạt ngưỡng bị loại khỏi tổ hợp.</li><li><strong>MUST</strong> tính giảm giá bằng số nguyên VND (DEC-CART-15): voucher <code>amount</code> giảm <code>discount_value</code>; voucher <code>percent</code> giảm <code>giá * discount_value / 100</code> (chia nguyên); freeship giảm <code>freeshipValue</code>. KHÔNG dùng float.</li><li><strong>MUST</strong> áp <code>applyCaps</code>: mỗi voucher có <code>cap</code> (trần giảm tuyệt đối) thì phần giảm của nó bị kẹp <code>min(computed, cap)</code> (DEC-CART-15). Bỏ <code>applyCaps</code> là sai pseudo-code và làm tổng giảm vượt trần thực tế.</li><li><strong>MUST</strong> trả <code>OptimizeResult{discount, combo[], breakdown}</code> (DEC-CART-18): <code>discount</code> là tổng giảm tối ưu (int64 VND); <code>combo</code> là danh sách voucher được chọn; <code>breakdown</code> phân rã giảm theo từng phần (shop/platform/freeship) để hiển thị minh bạch người dùng tiết kiệm bao nhiêu từ đâu.</li><li><strong>MUST</strong> xử lý giỏ rỗng và trường hợp không voucher nào áp được: trả <code>discount = 0</code>, <code>combo</code> rỗng - không lỗi (không tối ưu được nghĩa là giảm 0, không phải lỗi).</li><li><strong>MUST</strong> đảm bảo kết quả tất định: cùng input (giỏ + voucher + rules) luôn cho cùng combo (sắp xếp ổn định khi nhiều combo bằng điểm) - để test và để người dùng thấy kết quả nhất quán.</li><li>API <code>POST /v1/cart/optimize</code> <strong>MUST</strong> là caller: nạp giỏ (TASK-CART-002) + voucher còn hiệu lực (TASK-CART-001) cho <code>platform</code>, gọi <code>optimizeCart</code> thuần với <code>rules</code> của nước tương ứng (TASK-CART-004), trả JSON <code>OptimizeResult</code>. Handler nằm sau JWT gateway (TASK-INFRA-001).</li><li><strong>SHOULD</strong> phát OTel <code>cart_optimize_duration_ms</code> (histogram) và <code>cart_optimize_discount_vnd</code> (histogram tổng giảm) để theo dõi hiệu năng + giá trị mang lại.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao bám đúng pseudo-code §3.5(3) (DEC-CART-13)?</strong> Tài liệu nguồn đã đặc tả thuật toán: duyệt tổ hợp (platform x freeship x shop-combo), lọc stack hợp lệ + đạt min_spend, áp cap, giữ max. Đây là knapsack-like với không gian nhỏ (số voucher platform/freeship ít, shop voucher chọn tốt nhất per shop nên không bùng nổ). Bám đúng cấu trúc này giữ thuật toán đúng với thiết kế đã được cân nhắc và dễ đối chiếu.</p>\n<p><strong>Vì sao luật stacking ủy quyền TASK-CART-004 (DEC-CART-14)?</strong> Luật xếp chồng đổi theo nước (VN cho stack 1 shop + 1 platform + freeship; MY/PH 2025 bỏ stack - §3.9, §5.7). Nếu hardcode luật VN vào optimizer thì mở nước mới phải sửa lõi thuật toán - dễ sai và vi phạm tách biệt. <code>optimizeCart</code> chỉ gọi <code>rules.ValidStack</code>; luật cụ thể nằm ở TASK-CART-004 per-country. Lõi tính toán (chọn combo max) tách khỏi luật (combo nào hợp lệ).</p>\n<p><strong>Vì sao số nguyên VND (DEC-CART-15)?</strong> Optimizer cộng nhiều phần giảm (shop + platform + freeship) rồi so với cap và so giữa các combo. Float gây sai số tích lũy - cộng ba số float lệch vài đồng đủ để chọn sai combo hoặc vượt cap. Số nguyên VND chính xác tuyệt đối; percent tính <code>giá * pct / 100</code> bằng chia nguyên (sàn không có phần trăm thập phân).</p>\n<p><strong>Vì sao chooseBestShopVoucherPerShop độc lập per shop (DEC-CART-16)?</strong> Mỗi shop có voucher riêng; một đơn nhiều shop áp một voucher shop cho mỗi shop. Chọn voucher tốt nhất cho từng shop độc lập là tối ưu cục bộ đúng (voucher shop A không ảnh hưởng shop B). Điều này giữ không gian tìm kiếm nhỏ thay vì duyệt mọi tổ hợp voucher shop.</p>\n<p><strong>Vì sao applyCaps bắt buộc (DEC-CART-15, §1 #7)?</strong> Voucher thực tế có trần giảm (<code>cap</code>): \"giảm 20% tối đa 50k\". Bỏ cap thì optimizer tính giảm 20% của đơn lớn ra số vượt 50k - con số người dùng thấy SAI, lúc thanh toán sàn chỉ giảm 50k. <code>applyCaps</code> kẹp đúng trần để con số khớp thực tế.</p>\n<p><strong>Vì sao trả breakdown minh bạch (DEC-CART-18)?</strong> Người dùng cần thấy \"bạn tiết kiệm 110k: 30k từ voucher shop A, 50k từ platform, 30k freeship\" thay vì chỉ một con số. Minh bạch khoản giảm từ đâu vừa xây niềm tin (định vị hậu-Honey) vừa giúp người dùng hiểu vì sao combo này tốt.</p>\n<p><strong>Vì sao hàm thuần, caller nạp DB (DEC-CART-17)?</strong> Tách <code>optimizeCart</code> (logic thuần, dễ test với input cố định) khỏi I/O (handler nạp giỏ + voucher). Hàm thuần test được với mọi ca biên mà không cần DB; handler chỉ ghép dữ liệu rồi gọi.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Interface luật stacking (rules.go) - TASK-CART-004 hiện thực</h3>\n<pre><code class=\"language-go\">// services/cart/internal/optimizer/rules.go\n// StackRules trừu tượng luật xếp chồng per-country; TASK-CART-004 hiện thực cụ thể.\ntype StackRules interface {\n    // ValidStack trả true nếu tổ hợp (platform voucher, freeship, các shop voucher) được phép\n    // theo luật của nước; TASK-CART-003 KHÔNG biết luật cụ thể (DEC-CART-14).\n    ValidStack(pv *Voucher, fs *Voucher, shopVouchers []Voucher) bool\n}</code></pre>\n<h3>Tính giảm giá số nguyên (discount.go)</h3>\n<pre><code class=\"language-go\">// services/cart/internal/optimizer/discount.go\n\n// discountAmount tính phần giảm của một voucher trên một giá trị đơn (int64 VND).\nfunc discountAmount(v Voucher, base int64) int64 {\n    var d int64\n    switch v.DiscountType {\n    case DiscountAmount:\n        d = v.DiscountValue\n    case DiscountPercent:\n        d = base * v.DiscountValue / 100 // chia nguyên, KHÔNG float (DEC-CART-15)\n    }\n    return applyCap(d, v.Cap)\n}\n\n// applyCap kẹp phần giảm theo trần tuyệt đối của voucher (DEC-CART-15, §1 #7).\nfunc applyCap(d int64, cap *int64) int64 {\n    if cap != nil &amp;&amp; d &gt; *cap {\n        return *cap\n    }\n    return d\n}\n\nfunc freeshipValue(fs *Voucher, base int64) int64 {\n    if fs == nil {\n        return 0\n    }\n    return discountAmount(*fs, base)\n}</code></pre>\n<h3>optimizeCart (optimize.go) - bám pseudo-code §3.5(3)</h3>\n<pre><code class=\"language-go\">// services/cart/internal/optimizer/optimize.go\n\n// optimizeCart bám đúng cấu trúc pseudo-code §3.5(3) (DEC-CART-13):\n// duyệt (platformVoucher x freeship x shopCombo), lọc validStack + meetsMinSpend, applyCaps, giữ best.\nfunc optimizeCart(items []CartItem, vouchers Vouchers, rules StackRules) OptimizeResult {\n    cartTotal := totalOf(items)\n    applicableShop := filterByMinSpend(vouchers.Shop, items)       // voucher shop đạt min_spend\n    best := OptimizeResult{Discount: 0, Combo: nil}\n\n    for _, pv := range append(vouchers.Platform, nil) {            // platformVoucher + [none]\n        for _, fs := range append(vouchers.Freeship, nil) {       // freeship + [none]\n            shopCombo := chooseBestShopVoucherPerShop(applicableShop, items) // tốt nhất per shop\n            if !rules.ValidStack(pv, fs, shopCombo) {              // luật per-country (TASK-CART-004)\n                continue\n            }\n            if !meetsMinSpend(shopCombo, pv, fs, items) {          // ngưỡng đơn\n                continue\n            }\n            total := sumShopDiscounts(shopCombo, items) +\n                discountOf(pv, cartTotal) + freeshipValue(fs, cartTotal) // applyCaps trong từng hàm\n            if total &gt; best.Discount {\n                best = OptimizeResult{\n                    Discount:  total,\n                    Combo:     combine(shopCombo, pv, fs),\n                    Breakdown: breakdownOf(shopCombo, pv, fs, items), // minh bạch (DEC-CART-18)\n                }\n            }\n        }\n    }\n    return best // giỏ rỗng / không voucher -&gt; Discount 0, Combo nil (§1 #9)\n}\n\n// chooseBestShopVoucherPerShop chọn voucher shop tốt nhất CHO TỪNG shop (DEC-CART-16).\nfunc chooseBestShopVoucherPerShop(shopVouchers []Voucher, items []CartItem) []Voucher {\n    bestPerShop := map[string]Voucher{}\n    for _, v := range shopVouchers {\n        base := shopSubtotal(items, *v.ShopID)\n        d := discountAmount(v, base)\n        cur, ok := bestPerShop[*v.ShopID]\n        if !ok || d &gt; discountAmount(cur, shopSubtotal(items, *cur.ShopID)) {\n            bestPerShop[*v.ShopID] = v\n        }\n    }\n    return sortedValues(bestPerShop) // tất định (§1 #10)\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>optimizeCart</code> là hàm thuần: grep <code>services/cart/internal/optimizer/**</code>: KHÔNG có truy vấn DB/SQL (caller nạp dữ liệu).</li><li>Cấu trúc bám pseudo-code §3.5(3): có <code>filterByMinSpend</code>, vòng duyệt <code>(platform x freeship x shopCombo)</code>, <code>validStack</code>, <code>meetsMinSpend</code>, <code>applyCaps</code>, giữ <code>best.discount</code>.</li><li><code>chooseBestShopVoucherPerShop</code> chọn đúng một voucher tốt nhất cho mỗi shop trong giỏ (test giỏ 2 shop).</li><li><code>validStack</code> gọi <code>rules.ValidStack</code> (interface TASK-CART-004); grep: optimizer KHÔNG hardcode luật nước (không có hằng VN/MY/PH).</li><li>Ví dụ §3.5(3): giỏ shop A (300k) + B (250k), voucher shop A \"-30k đơn&gt;=250k\", platform \"-50k đơn&gt;=500k\", freeship \"&lt;=30k\", luật VN-stack -&gt; tổng giảm = 110k (30k + 50k + 30k).</li><li>Phép tính percent dùng chia nguyên (<code>base * pct / 100</code>); grep: không float trong tính giảm/cap.</li><li><code>applyCap</code> kẹp phần giảm theo <code>cap</code>: voucher \"giảm 20% tối đa 50k\" trên đơn 400k -&gt; giảm 50k (không 80k).</li><li><code>meetsMinSpend</code> loại voucher khi đơn không đạt ngưỡng (voucher platform \"-50k đơn&gt;=500k\" không áp cho giỏ 400k).</li><li>Giỏ rỗng hoặc không voucher áp được -&gt; <code>Discount = 0</code>, <code>Combo</code> rỗng, không lỗi.</li><li>Kết quả tất định: chạy hai lần cùng input cho cùng <code>combo</code> (sắp xếp ổn định).</li><li><code>OptimizeResult.Breakdown</code> phân rã giảm theo shop/platform/freeship khớp tổng <code>Discount</code>.</li><li><code>go test ./...</code> xanh.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/cart/internal/optimizer/optimize_test.go\n\n// Ví dụ minh họa §3.5(3): VN-stack -&gt; 110k.\nfunc TestOptimize_VNStackExample_110k(t *testing.T) {\n    items := []CartItem{\n        {ShopID: ptr(\"A\"), Qty: 1, UnitPrice: 300_000},\n        {ShopID: ptr(\"B\"), Qty: 1, UnitPrice: 250_000},\n    }\n    vouchers := Vouchers{\n        Shop:     []Voucher{shopV(\"A\", DiscountAmount, 30_000, ptr(int64(250_000)))}, // -30k đơn&gt;=250k\n        Platform: []Voucher{platV(DiscountAmount, 50_000, ptr(int64(500_000)))},      // -50k đơn&gt;=500k\n        Freeship: []Voucher{freeV(30_000, nil)},                                      // freeship &lt;=30k\n    }\n    res := optimizeCart(items, vouchers, VNStackRules{}) // VN cho stack\n    require.Equal(t, int64(110_000), res.Discount)       // 30k + 50k + 30k\n}\n\nfunc TestOptimize_ApplyCap_KepsDiscount(t *testing.T) {\n    items := []CartItem{{ShopID: ptr(\"A\"), Qty: 1, UnitPrice: 400_000}}\n    vouchers := Vouchers{\n        Shop: []Voucher{shopV(\"A\", DiscountPercent, 20, ptr(int64(50_000)))}, // 20% tối đa 50k\n    }\n    res := optimizeCart(items, vouchers, VNStackRules{})\n    require.Equal(t, int64(50_000), res.Discount) // 20% của 400k = 80k, kẹp về cap 50k\n}\n\nfunc TestOptimize_MinSpendGate_ExcludesVoucher(t *testing.T) {\n    items := []CartItem{{ShopID: ptr(\"A\"), Qty: 1, UnitPrice: 400_000}} // &lt;500k\n    vouchers := Vouchers{\n        Platform: []Voucher{platV(DiscountAmount, 50_000, ptr(int64(500_000)))}, // cần &gt;=500k\n    }\n    res := optimizeCart(items, vouchers, VNStackRules{})\n    require.Equal(t, int64(0), res.Discount) // không đạt min_spend -&gt; không áp\n}\n\nfunc TestOptimize_BestShopVoucherPerShop(t *testing.T) {\n    items := []CartItem{{ShopID: ptr(\"A\"), Qty: 1, UnitPrice: 300_000}}\n    vouchers := Vouchers{Shop: []Voucher{\n        shopV(\"A\", DiscountAmount, 20_000, nil),\n        shopV(\"A\", DiscountAmount, 35_000, nil), // tốt hơn cho shop A\n    }}\n    combo := chooseBestShopVoucherPerShop(vouchers.Shop, items)\n    require.Len(t, combo, 1)\n    require.Equal(t, int64(35_000), combo[0].DiscountValue) // chọn voucher tốt hơn\n}\n\nfunc TestOptimize_EmptyCart_ZeroDiscount(t *testing.T) {\n    res := optimizeCart(nil, Vouchers{}, VNStackRules{})\n    require.Equal(t, int64(0), res.Discount)\n    require.Empty(t, res.Combo)\n}\n\nfunc TestOptimize_Deterministic(t *testing.T) {\n    items := []CartItem{{ShopID: ptr(\"A\"), Qty: 1, UnitPrice: 300_000}}\n    v := Vouchers{Shop: []Voucher{shopV(\"A\", DiscountAmount, 30_000, nil)}}\n    a := optimizeCart(items, v, VNStackRules{})\n    b := optimizeCart(items, v, VNStackRules{})\n    require.Equal(t, a.Combo, b.Combo) // cùng input -&gt; cùng combo\n}</code></pre>\n<pre><code class=\"language-go\">// services/cart/internal/optimizer/discount_test.go\nfunc TestDiscountPercent_IntegerDivision(t *testing.T) {\n    v := Voucher{DiscountType: DiscountPercent, DiscountValue: 15}\n    require.Equal(t, int64(45_000), discountAmount(v, 300_000)) // 15% của 300k = 45k (chia nguyên)\n}\n\nfunc TestApplyCap(t *testing.T) {\n    require.Equal(t, int64(50_000), applyCap(80_000, ptr(int64(50_000)))) // kẹp\n    require.Equal(t, int64(30_000), applyCap(30_000, ptr(int64(50_000)))) // dưới cap giữ nguyên\n    require.Equal(t, int64(80_000), applyCap(80_000, nil))                // không cap\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>types.go</code> (CartItem, Voucher, Vouchers, OptimizeResult, Breakdown) -&gt; <code>rules.go</code> (interface StackRules, TASK-CART-004 hiện thực) -&gt; <code>discount.go</code> (<code>discountAmount</code>, <code>applyCap</code>, <code>freeshipValue</code> - int64, percent chia nguyên, test thuần) -&gt; <code>optimize.go</code> (<code>filterByMinSpend</code>, <code>chooseBestShopVoucherPerShop</code>, vòng duyệt bám §3.5(3), <code>meetsMinSpend</code>, <code>breakdownOf</code>) -&gt; <code>optimize.go</code> handler (<code>POST /v1/cart/optimize</code>: nạp giỏ TASK-CART-002 + voucher TASK-CART-001, gọi optimizer thuần với rules nước) -&gt; tests. Optimizer là package thuần, test được với input cố định gồm cả ví dụ §3.5(3). Handler ghép I/O (DB) quanh hàm thuần. Luật <code>VNStackRules</code> dùng trong test là stub đơn giản; hiện thực đầy đủ per-country ở TASK-CART-004.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-CART-001</strong> - <code>voucher_catalog</code> + <code>ListActive</code> cấp tập voucher còn hiệu lực (shop/platform/freeship, cap, min_spend, stack_group) cho optimizer (depends_on cứng).</li><li><strong>TASK-CART-002</strong> - <code>cart_snapshot</code> + <code>cart_item</code> cấp giỏ (qty, unit_price, shop_id) để tối ưu (depends_on cứng).</li><li><strong>TASK-CART-004 (downstream)</strong> - hiện thực <code>StackRules.ValidStack</code> per-country (VN stack vs MY/PH no-stack); optimizer gọi interface này, không hardcode luật.</li><li><strong>TASK-INFRA-001 (gateway)</strong> - JWT auth trước handler <code>/v1/cart/optimize</code>.</li><li>Extension/lib: thuần Go (không lib ngoài cho lõi); handler dùng <code>net/http</code>, <code>encoding/json</code>, <code>pgx</code> (qua repo).</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Request POST /v1/cart/optimize</h3>\n<pre><code class=\"language-json\">{\n  \"platform_id\": 1,\n  \"snapshot_id\": 5012,\n  \"items\": [\n    { \"shop_id\": \"A\", \"qty\": 1, \"unit_price\": 300000 },\n    { \"shop_id\": \"B\", \"qty\": 1, \"unit_price\": 250000 }\n  ]\n}</code></pre>\n<h3>Response (best combo, VN-stack) - breakdown minh bạch</h3>\n<pre><code class=\"language-json\">{\n  \"discount\": 110000,\n  \"combo\": [\n    { \"code\": \"SHOPA30K\", \"type\": \"shop\", \"shop_id\": \"A\" },\n    { \"code\": \"GIAM50K\", \"type\": \"platform\" },\n    { \"code\": \"FREESHIP\", \"type\": \"freeship\" }\n  ],\n  \"breakdown\": { \"shop\": 30000, \"platform\": 50000, \"freeship\": 30000 }\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Tối ưu gom đơn để đạt min_spend (gợi ý thêm SKU) - tính năng group-buy (§6 #4), slice sau.</li><li>Voucher điều kiện bậc thang trong optimizer - chờ TASK-CART-001 mô hình hóa bậc thang.</li><li>Tối ưu khi số voucher platform/freeship lớn (cận biên hiệu năng) - đo trước, thêm cắt tỉa nếu cần.</li><li>Đa tiền tệ - giữ int64 minor unit; luật stacking đã tách per-country (TASK-CART-004).</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Hardcode luật nước trong optimizer</td><td>grep hằng VN/MY</td><td>mở nước phải sửa lõi</td><td>Ủy quyền StackRules (DEC-CART-14)</td></tr><tr><td>Float trong tính giảm/cap</td><td>grep + discount test</td><td>sai số tổng giảm, chọn sai combo</td><td>Số nguyên VND (DEC-CART-15)</td></tr><tr><td>Bỏ applyCaps</td><td>optimize test cap</td><td>tổng giảm vượt trần thực tế</td><td>applyCap kẹp (§1 #7)</td></tr><tr><td>Optimizer đọc DB</td><td>grep SQL</td><td>không test được thuần</td><td>Hàm thuần, caller nạp (DEC-CART-17)</td></tr><tr><td>min_spend không kiểm</td><td>min-spend test</td><td>áp voucher không đủ ngưỡng</td><td>meetsMinSpend gate (§1 #5)</td></tr><tr><td>Chọn nhiều voucher một shop</td><td>best-per-shop test</td><td>vi phạm luật một-voucher-shop</td><td>chooseBestShopVoucherPerShop (DEC-CART-16)</td></tr><tr><td>Kết quả không tất định</td><td>deterministic test</td><td>UX/test bất ổn</td><td>Sắp xếp ổn định (§1 #10)</td></tr><tr><td>Giỏ rỗng panic</td><td>empty-cart test</td><td>crash</td><td>Trả Discount 0 (§1 #9)</td></tr><tr><td>Con số thiếu minh bạch</td><td>review breakdown</td><td>mất niềm tin</td><td>Breakdown shop/platform/freeship (DEC-CART-18)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li><code>optimizeCart</code> là trái tim tối ưu giỏ - khác biệt cạnh tranh mà BeeCost còn yếu (§5.6); trả lời câu hỏi tiền-tươi của persona Linh.</li><li>Bám đúng pseudo-code §3.5(3): duyệt (platform x freeship x shop-combo), lọc validStack + min_spend, applyCaps, giữ max.</li><li>Luật stacking ủy quyền TASK-CART-004 qua interface - lõi tính toán (chọn combo max) tách khỏi luật (combo nào hợp lệ theo nước).</li><li>Số nguyên VND tránh sai số khi cộng shop+platform+freeship rồi so cap/so combo; percent chia nguyên.</li><li>applyCaps khớp con số với thực tế sàn (giảm có trần); breakdown minh bạch xây niềm tin.</li><li>Hàm thuần test được với input cố định gồm ví dụ §3.5(3); handler ghép I/O quanh nó.</li></ul>\n<hr>\n<p><em>Hết TASK-CART-003. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
