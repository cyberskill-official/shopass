window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-B2B-004"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service B2B <strong>MUST</strong> cung cấp API public xác thực bằng API key có tier, rate-limit per-key tại API Gateway, và CHỈ phục vụ dữ liệu đã ẩn danh từ <code>market_trend_daily</code>. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> xác thực API public bằng API key qua header <code>X-API-Key</code> (DEC-B2B-30). Key có dạng <code>prefix.secret</code>; lưu trữ chỉ <code>prefix</code> (tra cứu) + <code>secret_hash</code> (băm có salt). <strong>MUST NOT</strong> lưu phần bí mật dạng cleartext.</li><li><strong>MUST</strong> định nghĩa bảng <code>api_key (id, prefix, secret_hash, org_name, tier, rate_per_min, monthly_quota, revoked, created_at)</code>; <code>tier</code> thuộc <code>{free, pro, enterprise}</code> (DEC-B2B-31).</li><li><strong>MUST</strong> từ chối key sai, không tồn tại, hoặc <code>revoked=true</code> với <code>401</code>. Thu hồi (<code>revoked=true</code>) <strong>MUST</strong> có hiệu lực trong vòng một chu kỳ cache ngắn (&lt;= 60 giây) (DEC-B2B-34).</li><li><strong>MUST</strong> thực thi rate-limit per-key tại API Gateway (TASK-INFRA-001) theo <code>rate_per_min</code> của tier (DEC-B2B-32); vượt -&gt; <code>429</code> + header <code>Retry-After</code>. <strong>MUST NOT</strong> đặt rate-limit chỉ ở service lẻ để có thể né.</li><li><strong>MUST</strong> chỉ phục vụ dữ liệu đã ẩn danh từ <code>market_trend_daily</code> (ô <code>suppressed=false</code>, qua TASK-B2B-001) (DEC-B2B-33). API public <strong>MUST NOT</strong> expose <code>price_snapshot</code> raw, <code>price_daily</code> raw, hay bất kỳ endpoint user-level nào (wishlist/alert/cart/auth).</li><li><strong>MUST</strong> gating phạm vi endpoint theo tier: tier không có quyền endpoint yêu cầu -&gt; <code>403</code>. (vd <code>free</code> chỉ đọc dải tổng hợp giới hạn; <code>enterprise</code> mở phạm vi rộng hơn.)</li><li><strong>MUST</strong> ghi <code>api_usage (api_key_id, endpoint, ts, status_code)</code> cho mỗi lời gọi để tính quota + hóa đơn (DEC-B2B-35); <strong>MUST NOT</strong> ghi nội dung dữ liệu trả về.</li><li><strong>MUST</strong> chặn khi vượt <code>monthly_quota</code> của tier với <code>429</code> (hoặc <code>402</code> nếu chính sách yêu cầu nâng cấp) - phân biệt rõ với rate-limit per-phút.</li><li><strong>MUST</strong> expose endpoint public <code>GET /public/v1/trends?category_id=...&amp;platform_id=...&amp;from=...&amp;to=...</code> trả chuỗi ô <code>market_trend_daily</code> đã phát hành.</li><li><strong>MUST</strong> phân biệt mã trạng thái: <code>200</code> (dữ liệu), <code>401</code> (key sai/revoked), <code>403</code> (tier không có quyền endpoint), <code>429</code> (vượt rate/quota), <code>400</code> (tham số sai).</li><li><strong>SHOULD</strong> phát OTel metric: <code>api_request_total{tier,status}</code> (counter), <code>api_rate_limited_total{tier}</code> (counter), <code>api_request_duration_ms</code> (histogram).</li><li><strong>MUST</strong> đảm bảo so sánh <code>secret_hash</code> dùng so khớp hằng-thời-gian (constant-time) để tránh lộ qua timing.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p>Vì sao API key tách khỏi JWT người dùng (DEC-B2B-30)? Người tiêu thụ API public là server/script của doanh nghiệp, không phải trình duyệt người dùng. JWT có vòng đời ngắn + refresh hợp cho phiên người dùng; API key dài hạn, gắn tổ chức + tier + quota hợp cho tích hợp máy-máy. Hai cơ chế khác mục đích nên tách.</p>\n<p>Vì sao chỉ lưu hash + prefix (DEC-B2B-30, §1 #1, #12)? Bảng <code>api_key</code> là mục tiêu giá trị cao. Lưu cleartext nghĩa là rò DB = rò mọi key. Lưu <code>secret_hash</code> + so khớp hằng-thời-gian làm DB lộ vẫn không cho kẻ tấn công khôi phục key dùng được. <code>prefix</code> công khai chỉ để tra cứu nhanh dòng nào trước khi so hash.</p>\n<p>Vì sao rate-limit ở gateway, không ở service lẻ (DEC-B2B-32, §1 #4)? Nếu mỗi service tự giới hạn thì một key gọi vòng qua nhiều service có thể vượt tổng. Gateway là điểm vào duy nhất - đặt rate-limit per-key ở đó đảm bảo trần là trần thật, không né được. Đây cũng đúng vai trò của API Gateway đã dựng ở TASK-INFRA-001.</p>\n<p>Vì sao API public chỉ mở market_trend_daily (DEC-B2B-33, §1 #5)? Đây là điểm dễ sai nhất và hậu quả lớn nhất. API public là bề mặt ra Internet. Nếu nó chạm được raw hay user-level thì một API key biến thành ống dẫn rút toàn bộ dữ liệu chi tiết - phá k-anonymity và moat. Quy tắc cứng: API public chỉ đọc ô đã phát hành của TASK-B2B-001, không có route nào khác.</p>\n<p>Vì sao thu hồi key phải có hiệu lực ngay (DEC-B2B-34, §1 #3)? Khi key lộ (rò vào repo công khai, log), phải vô hiệu nhanh. Nếu cache tier/quyền quá lâu thì key thu hồi vẫn dùng được hàng giờ. Giới hạn cache &lt;= 60 giây cân bằng giữa hiệu năng và độ trễ thu hồi.</p>\n<p>Vì sao tách rate-limit (phút) khỏi quota (tháng) (§1 #8)? Hai trục kiểm soát khác nhau: rate-limit chống đột biến tức thời (bảo vệ hệ thống), quota chống dùng quá gói (bảo vệ doanh thu). Một key có thể chưa chạm trần phút nhưng đã hết quota tháng - cần báo lý do khác nhau để khách hiểu đúng.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/b2b/migrations/0004_api_key.sql\nCREATE TABLE api_key (\n  id            BIGSERIAL   PRIMARY KEY,\n  prefix        TEXT        NOT NULL UNIQUE,     -- phần công khai để tra cứu\n  secret_hash   TEXT        NOT NULL,            -- băm có salt; KHÔNG cleartext\n  org_name      TEXT        NOT NULL,\n  tier          TEXT        NOT NULL CHECK (tier IN ('free','pro','enterprise')),\n  rate_per_min  INTEGER     NOT NULL CHECK (rate_per_min &gt; 0),\n  monthly_quota INTEGER     NOT NULL CHECK (monthly_quota &gt; 0),\n  revoked       BOOLEAN     NOT NULL DEFAULT false,\n  created_at    TIMESTAMPTZ NOT NULL DEFAULT now()\n);\n\nCREATE TABLE api_usage (\n  id          BIGSERIAL   PRIMARY KEY,\n  api_key_id  BIGINT      NOT NULL REFERENCES api_key(id),\n  endpoint    TEXT        NOT NULL,\n  ts          TIMESTAMPTZ NOT NULL DEFAULT now(),\n  status_code SMALLINT    NOT NULL\n  -- KHÔNG ghi nội dung dữ liệu trả về\n);\nCREATE INDEX idx_usage_key_month ON api_usage (api_key_id, ts);</code></pre>\n<h3>Auth (Go)</h3>\n<pre><code class=\"language-go\">// services/b2b/internal/apikey/auth.go\n// authenticate tách prefix.secret, tra dòng theo prefix, so hash hằng-thời-gian,\n// chặn revoked. Trả APIKey (kèm tier) hoặc lỗi 401.\nfunc (a *Auth) authenticate(ctx context.Context, raw string) (*APIKey, error) {\n    prefix, secret, ok := splitKey(raw) // \"pfx_live_ab12.SECRET\"\n    if !ok {\n        return nil, ErrUnauthorized\n    }\n    k, err := a.cache.GetByPrefix(ctx, prefix) // cache &lt;= 60s để thu hồi nhanh\n    if err != nil || k == nil || k.Revoked {\n        return nil, ErrUnauthorized\n    }\n    if !verifyHashConstantTime(secret, k.SecretHash) {\n        return nil, ErrUnauthorized\n    }\n    return k, nil\n}</code></pre>\n<h3>Rate-limit + endpoint gating (§1 #4, #6)</h3>\n<pre><code class=\"language-go\">// services/b2b/internal/apikey/ratelimit.go\n// allow thực thi token-bucket per-key; điểm này nằm ở lớp gateway (TASK-INFRA-001).\nfunc (rl *RateLimiter) allow(keyID int64, ratePerMin int) (ok bool, retryAfter time.Duration) {\n    b := rl.bucket(keyID, ratePerMin)\n    if b.take() {\n        return true, 0\n    }\n    return false, b.untilNextToken() // -&gt; header Retry-After khi 429\n}\n\n// allowedEndpoint kẹp phạm vi theo tier; tier thiếu quyền -&gt; 403.\nfunc allowedEndpoint(tier, endpoint string) bool {\n    switch tier {\n    case \"enterprise\":\n        return true\n    case \"pro\":\n        return endpoint == \"/public/v1/trends\"\n    default: // free\n        return endpoint == \"/public/v1/trends\" // phạm vi/độ sâu giới hạn thêm ở handler\n    }\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch -&gt; <code>api_key</code> + <code>api_usage</code> tồn tại; <code>api_key.secret_hash</code> không phải cleartext.</li><li>Gọi <code>/public/v1/trends</code> với key hợp lệ -&gt; <code>200</code> + chuỗi ô <code>market_trend_daily</code> đã phát hành.</li><li>Key sai/không tồn tại -&gt; <code>401</code>.</li><li>Key <code>revoked=true</code> -&gt; <code>401</code>; sau khi set revoked, có hiệu lực trong &lt;= 60 giây (qua hết cache).</li><li>Vượt <code>rate_per_min</code> -&gt; <code>429</code> + header <code>Retry-After</code> &gt; 0.</li><li>Vượt <code>monthly_quota</code> -&gt; <code>429</code> (hoặc <code>402</code> theo chính sách), phân biệt với rate-limit phút.</li><li>Tier không có quyền endpoint -&gt; <code>403</code>.</li><li>Không có route public nào trả <code>price_snapshot</code> raw, <code>price_daily</code> raw, hay dữ liệu user-level (review router + test).</li><li>Mỗi lời gọi ghi một dòng <code>api_usage</code> với <code>endpoint</code> + <code>status_code</code>, KHÔNG có nội dung dữ liệu.</li><li>So khớp secret dùng hàm hằng-thời-gian (review + test không phụ thuộc timing để pass/fail logic).</li><li>Phản hồi <code>/public/v1/trends</code> chỉ chứa chỉ số tổng hợp (không khóa định danh).</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/b2b/internal/apikey/auth_test.go\nfunc TestAuth_ValidKey_OK(t *testing.T) {\n    a, raw := setupKeyWith(t, \"pro\", false) // tier pro, chưa revoke\n    k, err := a.authenticate(ctx, raw)\n    require.NoError(t, err)\n    require.Equal(t, \"pro\", k.Tier)\n}\n\nfunc TestAuth_WrongSecret_401(t *testing.T) {\n    a, raw := setupKeyWith(t, \"pro\", false)\n    bad := mangleSecret(raw)\n    _, err := a.authenticate(ctx, bad)\n    require.ErrorIs(t, err, ErrUnauthorized)\n}\n\nfunc TestAuth_Revoked_401(t *testing.T) {\n    a, raw := setupKeyWith(t, \"pro\", true) // revoked\n    _, err := a.authenticate(ctx, raw)\n    require.ErrorIs(t, err, ErrUnauthorized)\n}\n\nfunc TestAuth_RevocationTakesEffect(t *testing.T) {\n    a, raw := setupKeyWith(t, \"pro\", false)\n    _, err := a.authenticate(ctx, raw)\n    require.NoError(t, err)\n    a.revoke(ctx, raw)\n    a.advanceCache(61 * time.Second) // qua chu kỳ cache &lt;= 60s\n    _, err = a.authenticate(ctx, raw)\n    require.ErrorIs(t, err, ErrUnauthorized)\n}\n\n// services/b2b/internal/apikey/ratelimit_test.go\nfunc TestRate_ExceedsLimit_429(t *testing.T) {\n    rl := newRateLimiter()\n    for i := 0; i &lt; 60; i++ {\n        ok, _ := rl.allow(7, 60)\n        require.True(t, ok)\n    }\n    ok, retry := rl.allow(7, 60) // request thứ 61 trong phút\n    require.False(t, ok)\n    require.Greater(t, retry, time.Duration(0))\n}\n\nfunc TestQuota_ExceedsMonthly_429(t *testing.T) {\n    rl := newRateLimiter()\n    const monthly = 10\n    for i := 0; i &lt; monthly; i++ {\n        ok := rl.allowMonthly(7, monthly) // trong hạn tháng\n        require.True(t, ok)\n    }\n    ok := rl.allowMonthly(7, monthly) // request thứ 11 trong tháng\n    require.False(t, ok)              // vượt monthly_quota -&gt; 429 (phân biệt rate phút, §4 #6)\n}\n\nfunc TestEndpoint_FreeTierDenied_403(t *testing.T) {\n    require.False(t, allowedEndpoint(\"free\", \"/public/v1/admin\"))\n    require.True(t, allowedEndpoint(\"free\", \"/public/v1/trends\"))\n    require.True(t, allowedEndpoint(\"enterprise\", \"/public/v1/anything\"))\n}\n\nfunc TestPublic_NoRawRoutes(t *testing.T) {\n    routes := registeredPublicRoutes(t)\n    for _, r := range routes {\n        require.NotContains(t, r, \"price-history\")\n        require.NotContains(t, r, \"snapshot\")\n        require.NotContains(t, r, \"wishlist\")\n        require.NotContains(t, r, \"cart\")\n    }\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration 0004 (api_key + api_usage) -&gt; auth.go (xác thực key + cache thu hồi) -&gt; ratelimit.go (token-bucket + endpoint gating) -&gt; public_trend_handler.go (phục vụ market_trend_daily) -&gt; usage.go -&gt; tests. Rate-limit per-key cắm vào điểm thực thi của API Gateway (TASK-INFRA-001) - task này cung cấp logic + cấu hình, gateway là nơi chạy. Phát key (sinh secret, băm, trả cleartext một lần duy nhất cho khách) là quy trình quản trị; task chỉ xác thực key đã phát.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li>TASK-INFRA-001 - API Gateway/BFF là điểm thực thi rate-limit per-key + định tuyến <code>/public/v1/*</code>.</li><li>TASK-B2B-001 - <code>market_trend_daily</code> + <code>QueryCells</code> (ô đã phát hành) là nguồn dữ liệu duy nhất API public phục vụ.</li><li>TASK-B2B-002 (cùng module) - chia sẻ khái niệm tier/tổ chức B2B; api_key có thể đồng bộ tier với subscription.</li><li>Extension/lib: thư viện băm (argon2id hoặc sha256+salt), <code>crypto/subtle</code> cho so khớp hằng-thời-gian; driver <code>pgx</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Gọi API public (header)</h3>\n<pre><code>GET /public/v1/trends?category_id=7&amp;platform_id=1&amp;from=2026-05-22&amp;to=2026-06-20\nX-API-Key: pfx_live_ab12.s3cr3t_no_log</code></pre>\n<h3>Phản hồi (200) - chỉ chỉ số tổng hợp</h3>\n<pre><code class=\"language-json\">{\n  \"category_id\": 7,\n  \"platform_id\": 1,\n  \"cells\": [\n    { \"day\": \"2026-06-19\", \"median_p\": 318000, \"p25_p\": 249000, \"p75_p\": 405000, \"avg_discount_pct\": 13.80 },\n    { \"day\": \"2026-06-20\", \"median_p\": 320000, \"p25_p\": 250000, \"p75_p\": 410000, \"avg_discount_pct\": 14.30 }\n  ]\n}</code></pre>\n<h3>Vượt rate-limit (429)</h3>\n<pre><code>HTTP/1.1 429 Too Many Requests\nRetry-After: 23</code></pre>\n<pre><code class=\"language-json\">{ \"error\": \"rate_limited\", \"retry_after_seconds\": 23, \"message\": \"Vượt giới hạn req/phút của gói pro.\" }</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Webhook đẩy dữ liệu xu hướng thay vì poll - thêm khi có nhu cầu khách hàng enterprise.</li><li>API key scoping theo category cụ thể (key chỉ đọc category được mua) - mở rộng mô hình entitlement ở slice sau.</li><li>OpenAPI spec công bố + portal tự phục vụ phát key - hạ tầng developer-experience giai đoạn sau.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Lưu key cleartext</td><td>review schema + migration</td><td>rò DB = rò mọi key</td><td>secret_hash băm có salt (DEC-B2B-30)</td></tr><tr><td>API public chạm raw/user-level</td><td>TestPublic_NoRawRoutes + review router</td><td>rò toàn bộ dữ liệu chi tiết ra Internet</td><td>Chỉ route /public/v1/trends -&gt; market_trend_daily (DEC-B2B-33)</td></tr><tr><td>Rate-limit né được ở service lẻ</td><td>review điểm thực thi</td><td>một key rút cạn/quá tải</td><td>Rate-limit per-key tại gateway (DEC-B2B-32)</td></tr><tr><td>Key lộ dùng được lâu</td><td>TestAuth_RevocationTakesEffect</td><td>cửa hậu</td><td>Thu hồi có hiệu lực &lt;= 60s (DEC-B2B-34)</td></tr><tr><td>Timing attack lên so secret</td><td>review dùng crypto/subtle</td><td>đoán key qua timing</td><td>So khớp hằng-thời-gian (§1 #12)</td></tr><tr><td>Vượt quota tháng nhầm thành lỗi khác</td><td>AC #6</td><td>khách hiểu sai lý do</td><td>Tách 429 quota vs 429 rate (§1 #8)</td></tr><tr><td>Usage ghi nội dung dữ liệu</td><td>review usage.go</td><td>log phình + rủi ro lộ</td><td>api_usage chỉ endpoint+status (DEC-B2B-35)</td></tr><tr><td>Tier free gọi endpoint nhạy cảm</td><td>TestEndpoint_FreeTierDenied_403</td><td>vượt quyền gói</td><td>allowedEndpoint gating theo tier</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>API public là bề mặt ra Internet rủi ro cao nhất - quy tắc cứng: chỉ phục vụ ô đã phát hành của market_trend_daily, không route nào khác.</li><li>API key tách khỏi JWT người dùng: máy-máy dài hạn + tier + quota, khác phiên trình duyệt ngắn hạn.</li><li>Lưu hash + so khớp hằng-thời-gian làm DB lộ vẫn không cho khôi phục key dùng được.</li><li>Rate-limit ở gateway (không ở service lẻ) đảm bảo trần là trần thật; đúng vai trò TASK-INFRA-001.</li><li>Thu hồi key phải nhanh (&lt;= 60s) vì key lộ là sự cố cần phản ứng tức thì.</li><li>Hai trục kiểm soát: rate-limit phút (bảo vệ hệ thống) tách khỏi quota tháng (bảo vệ doanh thu).</li></ul>\n<hr>\n<p><em>Hết TASK-B2B-004. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
