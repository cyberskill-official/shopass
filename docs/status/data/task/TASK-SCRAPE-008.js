window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SCRAPE-008"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Adapter Lazada <strong>MUST</strong> lấy giá qua đọc DOM-render trên farm có TLS match (qua Akamai), residential enterprise. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> triển khai interface adapter của TASK-SCRAPE-001 và lấy giá qua farm <code>RenderPrice</code> của TASK-SCRAPE-003 (DEC-SCRAPE-32) - Lazada API thường ký, ưu tiên đọc DOM đã render.</li><li><strong>MUST</strong> dựa vào TLS/HTTP2 fingerprint match của farm (TASK-SCRAPE-003 #4) để qua Akamai (DEC-SCRAPE-33): Akamai đọc JA3/JA4 + HTTP/2 SETTINGS ở tầng bắt tay, trước khi JS chạy; adapter KHÔNG được dùng client thô bỏ qua lớp này.</li><li><strong>MUST</strong> dùng proxy residential tier <code>enterprise</code> (DEC-SCRAPE-34): job Lazada dùng <code>SelectTier(DiffAkamai)</code> của TASK-SCRAPE-004 (= enterprise); datacenter bị từ chối (Akamai chặn chắc).</li><li><strong>MUST</strong> trích giá ưu tiên từ JSON nhúng trong trang (DEC-SCRAPE-35): Lazada thường nhúng window state JSON; parse từ đó, fallback DOM text khi không có hoặc cấu trúc đổi.</li><li><strong>MUST</strong> map về <code>price.PriceSnapshot</code> với <code>price</code>/<code>list_price</code> BIGINT VND số nguyên (đồng bộ DEC-PRICE-05 của TASK-PRICE-002), <code>flash_sale</code> true khi trang báo đang flash sale.</li><li><strong>MUST</strong> chạy hành vi giống người của farm (TASK-SCRAPE-003 #6) trước khi trích.</li><li><strong>MUST</strong> phát hiện Akamai challenge (sensor/verify page) và ném <code>ChallengedError</code> (TASK-SCRAPE-003 #8) để TASK-SCRAPE-005 / orchestrator xử lý, KHÔNG trả snapshot rỗng.</li><li><strong>MUST</strong> báo outcome (success/parse_fail/challenge) cho monitor của TASK-SCRAPE-006.</li><li><strong>MUST</strong> chờ nội dung giá render xong trước khi trích (Lazada có phần render bằng JS); không đọc DOM lúc trang chưa sẵn.</li><li><strong>MUST</strong> tối thiểu hóa: chỉ trích trường giá, không lưu trang thô (§5.4).</li><li><strong>SHOULD</strong> phát OTel metric: <code>lazada_extract_source_total{source=json|dom}</code> (counter), <code>lazada_akamai_challenge_total</code> (counter), <code>lazada_render_duration_ms</code> (histogram).</li><li><strong>MUST</strong> quy đổi giá bằng số nguyên (không float trung gian).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao đọc DOM-render (DEC-SCRAPE-32)?</strong> Lazada (Alibaba) có API thường ký và đứng sau Akamai. Như TikTok, cố gọi/ký API là cuộc đua giòn. Đọc trang đã render trong trình duyệt thật của farm để Akamai thấy một phiên trình duyệt hợp lệ, ta chỉ đọc giá đã hiển thị.</p>\n<p><strong>Vì sao TLS/HTTP2 match là then chốt với Akamai (DEC-SCRAPE-33)?</strong> Đây là điểm khác biệt cốt lõi của Lazada so với Shopee. Akamai chấm điểm ở tầng bắt tay TLS (JA3/JA4) và HTTP/2 SETTINGS - trước khi một dòng JS nào chạy. Client Node/Go mặc định có chữ ký TLS khác Chrome thật, bị Akamai nhận diện ngay dù JS patch hoàn hảo. Adapter phải đi qua farm có TLS match (TASK-SCRAPE-003 #4), không được dùng client thô.</p>\n<p><strong>Vì sao residential enterprise bắt buộc (DEC-SCRAPE-34)?</strong> Tài liệu nói thẳng: datacenter vô dụng với Akamai (§3.3). Akamai chặn dải IP datacenter theo lô. <code>SelectTier(DiffAkamai)</code> của TASK-SCRAPE-004 trả enterprise - residential chất lượng cao nhất - vì Lazada Medium-High không qua được bằng IP rẻ.</p>\n<p><strong>Vì sao ưu tiên JSON nhúng (DEC-SCRAPE-35)?</strong> Như TikTok: window state JSON ổn định hơn cào DOM text trước A/B layout. Lazada thường có state nhúng để hydrate; đọc từ đó trước, fallback DOM giữ độ phủ.</p>\n<p><strong>Vì sao chờ render (§1 #9)?</strong> Lazada PDP có phần giá render bằng JS. Đọc DOM lúc trang chưa sẵn cho parse_fail giả. Chờ selector/state giá xuất hiện trước khi trích.</p>\n<p><strong>Vì sao vẫn tái dùng farm (§1 #1, #2)?</strong> Farm (TASK-SCRAPE-003) đã giải fingerprint + TLS + behavior - đúng những gì Akamai kiểm. Lazada adapter chỉ thêm \"đọc giá ở đâu trên trang Lazada\". Tái dùng farm tránh nhân bản lớp chống Akamai; adapter chỉ là selectors + extract riêng.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Selectors + extract (TypeScript)</h3>\n<pre><code class=\"language-typescript\">// services/scrape/farm/src/adapters/lazada/selectors.ts\nexport const lazadaSelectors = {\n  embeddedState: 'script:has-text(\"window.__moduleData__\"), script:has-text(\"pdpTrackingData\")',\n  priceText:     '.pdp-price--current, [data-price]',\n  listPriceText: '.pdp-price--deleted, .origin-block .price',\n  flashBadge:    '.pdp-mod-flash-sale, [data-spm=\"flashsale\"]',\n  readyAnchor:   '.pdp-price--current', // chờ giá render xong (§1 #9)\n};</code></pre>\n<pre><code class=\"language-typescript\">// services/scrape/farm/src/adapters/lazada/extract.ts\nconst VND_UNIT = 1; // Lazada VN hiển thị VND nguyên; chỉnh nếu phát hiện khác\n\n// extractLazada ưu tiên JSON nhúng (window state), fallback DOM text; giá VND số nguyên.\nexport function extractLazada(page: PageView): RawPrice {\n  const state = page.embeddedJSON(lazadaSelectors.embeddedState);\n  if (state) {\n    const p = pickPriceFromModuleData(state); // đọc price/origin/flash từ __moduleData__\n    if (p) return p;                           // source = json\n  }\n  return {\n    price:     parseVNDInt(page.text(lazadaSelectors.priceText)),\n    listPrice: parseVNDInt(page.text(lazadaSelectors.listPriceText)),\n    flashSale: page.exists(lazadaSelectors.flashBadge),\n    source:    'dom',\n  };\n}</code></pre>\n<h3>Adapter (TypeScript)</h3>\n<pre><code class=\"language-typescript\">// services/scrape/farm/src/adapters/lazada/adapter.ts\nexport class LazadaAdapter implements PlatformAdapter {\n  platformID() { return PLATFORM_LAZADA; }\n\n  async fetch(ctx: BrowserContext, job: ScrapeJob): Promise&lt;PriceSnapshot&gt; {\n    // ctx do farm tạo với TLS match (TASK-SCRAPE-003 #4) + proxy residential enterprise\n    const page = await ctx.newPage();\n    try {\n      await page.goto(job.url, { waitUntil: 'domcontentloaded' });\n      if (await detectAkamaiChallenge(page)) throw new ChallengedError('akamai sensor/verify');\n      await humanize(page);                                  // TASK-SCRAPE-003 behavior\n      await page.waitForSelector(lazadaSelectors.readyAnchor);\n      const raw = extractLazada(viewOf(page));\n      metrics.lazadaSource(raw.source);\n      return toSnapshot(job.productId, raw, VND_UNIT);       // VND số nguyên, set ts\n    } finally {\n      await page.close();\n    }\n  }\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>LazadaAdapter</code> thỏa interface <code>PlatformAdapter</code>; <code>platformID()</code> trả mã Lazada.</li><li>Job Lazada được cấp proxy residential tier <code>enterprise</code> (<code>SelectTier(DiffAkamai)</code> = enterprise); datacenter bị từ chối.</li><li>Adapter chạy trên context farm có TLS/HTTP2 match (không dùng client thô bỏ qua TLS).</li><li>Trang có window state JSON -&gt; <code>extractLazada</code> trả giá từ state (<code>source='json'</code>).</li><li>Trang không có JSON nhúng -&gt; fallback DOM text (<code>source='dom'</code>), giá VND đúng (số nguyên).</li><li><code>flash_sale</code> = true khi trang có badge flash sale Lazada.</li><li>Giá quy đổi bằng số nguyên; với text \" VND89.000\" -&gt; <code>price=89000</code> chính xác.</li><li>Trang Akamai challenge (sensor/verify) -&gt; <code>fetch</code> ném <code>ChallengedError</code>, không trả snapshot.</li><li>Giá chưa render -&gt; adapter chờ <code>readyAnchor</code> trước khi trích (không parse trang chưa sẵn).</li><li>Adapter báo outcome (success/parse_fail/challenge) cho monitor TASK-SCRAPE-006.</li><li>Page đóng sau render; chỉ trường giá được giữ (không lưu trang thô).</li><li>Metric <code>lazada_extract_source_total{source}</code>, <code>lazada_akamai_challenge_total</code> thay đổi đúng.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-typescript\">// services/scrape/farm/src/adapters/lazada/__tests__/extract.test.ts\ntest('ưu tiên window state JSON', () =&gt; {\n  const page = pageWithModuleData({ price: 89000, originPrice: 149000, flash: true });\n  const raw = extractLazada(page);\n  expect(raw.source).toBe('json');\n  expect(raw.price).toBe(89000);\n  expect(raw.listPrice).toBe(149000);\n  expect(raw.flashSale).toBe(true);\n});\n\ntest('fallback DOM text', () =&gt; {\n  const page = pageDomOnly({ price: '₫89.000', origin: '₫149.000', flash: false });\n  const raw = extractLazada(page);\n  expect(raw.source).toBe('dom');\n  expect(raw.price).toBe(89000);\n});\n\ntest('parse VND số nguyên', () =&gt; {\n  expect(parseVNDInt('₫1.234.567')).toBe(1234567);\n});</code></pre>\n<pre><code class=\"language-typescript\">// services/scrape/farm/src/adapters/lazada/__tests__/adapter.test.ts\ntest('Akamai challenge ném ChallengedError', async () =&gt; {\n  const ctx = ctxWithAkamaiSensor();\n  await expect(new LazadaAdapter().fetch(ctx, job('https://lazada.vn/x')))\n    .rejects.toBeInstanceOf(ChallengedError);\n});\n\ntest('chờ render trước khi trích', async () =&gt; {\n  const ctx = ctxSlowRender(lazadaSelectors.readyAnchor);\n  const snap = await new LazadaAdapter().fetch(ctx, job('u'));\n  expect(snap.price).toBeGreaterThan(0); // không parse trang chưa sẵn\n});\n\ntest('proxy datacenter bị từ chối cho Lazada', () =&gt; {\n  expect(() =&gt; assignProxy(job('lazada'), 'datacenter')).toThrow();\n  expect(selectTierFor('lazada')).toBe('enterprise');\n});</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: selectors.ts -&gt; extract.ts (JSON-ưu-tiên + DOM-fallback, test trước với fixture) -&gt; adapter.ts (render qua farm TLS-match + chờ render + Akamai challenge) -&gt; registry.go (đăng ký, gắn proxy enterprise) -&gt; tests. Adapter chạy trên farm Playwright của TASK-SCRAPE-003 (đặc biệt cần TLS/HTTP2 match cho Akamai). Proxy lấy qua <code>SelectTier(DiffAkamai)</code> của TASK-SCRAPE-004. Outcome báo về monitor TASK-SCRAPE-006.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-SCRAPE-003</strong> - farm Playwright với TLS/HTTP2 match (then chốt cho Akamai), behavior, ChallengedError.</li><li><strong>TASK-SCRAPE-004</strong> - proxy residential tier enterprise (DiffAkamai); datacenter bị từ chối.</li><li><strong>TASK-SCRAPE-001</strong> - interface adapter; orchestrator điều phối + gọi sink.</li><li><strong>TASK-SCRAPE-005</strong> - pacing + xử lý ChallengedError; ghi qua delta-only.</li><li><strong>TASK-SCRAPE-006</strong> - monitor nhận outcome.</li><li><strong>TASK-PRICE-002 / TASK-PRICE-004 (downstream)</strong> - snapshot ghi qua InsertSnapshot; Lazada là chân thứ ba của so sánh chéo.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>window state nhúng (rút gọn, ẩn danh)</h3>\n<pre><code class=\"language-json\">{\n  \"__moduleData__\": {\n    \"data\": { \"root\": { \"fields\": {\n      \"price\": { \"value\": 89000, \"originalPrice\": 149000 },\n      \"flashSale\": { \"active\": true }\n    } } }\n  }\n}</code></pre>\n<h3>Snapshot adapter trả về</h3>\n<pre><code class=\"language-typescript\">{\n  productId: 55310,\n  ts: nowISO,\n  price: 89000,            // VND số nguyên\n  listPrice: 149000,\n  flashSale: true,\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Hình dạng <code>__moduleData__</code> chính xác của Lazada VN - xác minh trên fixture thật; <code>pickPriceFromModuleData</code> một chỗ để chỉnh.</li><li>Mức độ Akamai siết theo thời gian (sensor data v2/v3) - dựa farm TLS-match; nâng khi Akamai đổi.</li><li>Đơn vị giá - hằng <code>VND_UNIT</code> một chỗ, khóa bằng test.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Datacenter proxy cho Lazada</td><td>tỷ lệ block</td><td>Akamai chặn chắc</td><td>Residential enterprise (§1 #3)</td></tr><tr><td>Client thô bỏ TLS match</td><td>block ở bắt tay</td><td>lộ trước cả JS</td><td>Farm TLS/HTTP2 match (§1 #2)</td></tr><tr><td>Đọc DOM chưa render</td><td>parse trang chưa sẵn</td><td>parse_fail giả</td><td>Chờ readyAnchor (§1 #9)</td></tr><tr><td>window state đổi shape</td><td><code>lazada_extract_source</code> lệch</td><td>json fail</td><td>Fallback DOM + monitor (§1 #4,#8)</td></tr><tr><td>Float trong quy đổi</td><td>extract test</td><td>sai số tiền</td><td>parseVNDInt số nguyên (§1 #12)</td></tr><tr><td>Akamai challenge coi như \"không giá\"</td><td>ChallengedError</td><td>dữ liệu bẩn</td><td>Ném lỗi, chuyển TASK-SCRAPE-005 (§1 #7)</td></tr><tr><td>Đọc tức thì như bot</td><td>behavior</td><td>dấu vết</td><td>humanize trước trích (§1 #6)</td></tr><tr><td>Cấu trúc Lazada đổi</td><td>parse_fail spike</td><td>dữ liệu chết</td><td>Monitor drift (§1 #8)</td></tr><tr><td>Lưu trang thô</td><td>review code</td><td>rủi ro dữ liệu</td><td>Chỉ trích trường giá (§1 #10)</td></tr><tr><td>Rò page farm</td><td>mem profiler</td><td>RAM phình</td><td>Đóng page finally (§1 #11)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Lazada là chân thứ ba của moat so sánh chéo 3 sàn (TASK-PRICE-004) - thiếu nó thì so sánh giá yếu hẳn.</li><li>Khác biệt cốt lõi của Lazada là Akamai: kiểm fingerprint ở tầng TLS/HTTP2 trước khi JS chạy - TLS match của farm (TASK-SCRAPE-003 #4) là điều kiện sống còn, không phải tùy chọn.</li><li>Residential enterprise bắt buộc vì datacenter vô dụng với Akamai (§3.3) - đây không phải lựa chọn tiết kiệm.</li><li>Cùng khuôn với TikTok (TASK-SCRAPE-007): đọc DOM-render, JSON nhúng ưu tiên, chờ render, né API ký; khác ở lớp Akamai TLS.</li><li>Giá VND số nguyên đồng bộ TASK-PRICE-002 - sai số float làm hỏng so sánh chéo sàn vốn là điểm mạnh của Lazada trong moat.</li></ul>\n<hr>\n<p><em>Hết TASK-SCRAPE-008. Status: done (mục tiêu audit 10/10).</em></p>";
