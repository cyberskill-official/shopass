window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-B2B-001"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service B2B <strong>MUST</strong> sinh bảng dữ liệu xu hướng thị trường ẩn danh <code>market_trend_daily</code> bằng cách tổng hợp giá theo <code>category_id</code> x <code>platform_id</code> x ngày, đọc duy nhất từ continuous aggregate <code>price_daily</code> và <code>tracked_product</code>, qua cổng k-anonymity, và KHÔNG bao giờ phát hành dữ liệu của một SKU, một shop hay một user đơn lẻ. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> định nghĩa bảng <code>market_trend_daily (category_id, platform_id, day, median_p, p25_p, p75_p, avg_discount_pct, sku_count, suppressed)</code> với <code>PRIMARY KEY (category_id, platform_id, day)</code>. Bảng KHÔNG chứa <code>product_id</code>, <code>shop_id</code>, <code>user_id</code> hay bất kỳ khóa định danh nào (DEC-B2B-06).</li><li><strong>MUST</strong> chỉ đọc nguồn từ <code>price_daily</code> (continuous aggregate theo ngày) và <code>tracked_product.category_id</code> khi sinh dữ liệu (DEC-B2B-01). Pipeline <strong>MUST NOT</strong> đọc <code>price_snapshot</code> raw, <code>wishlist</code>, <code>alert</code>, <code>cart_snapshot</code> hay bất kỳ bảng user-level nào.</li><li><strong>MUST</strong> tính cho mỗi ô <code>(category_id, platform_id, day)</code>: <code>median_p = median(close_p)</code>, <code>p25_p = percentile_cont(0.25)</code>, <code>p75_p = percentile_cont(0.75)</code>, <code>avg_discount_pct</code> (trung bình <code>(max_p - close_p) / max_p</code> trong ngày), và <code>sku_count</code> = số <code>product_id</code> phân biệt đóng góp vào ô.</li><li><strong>MUST</strong> áp cổng k-anonymity (DEC-B2B-02): chỉ phát hành chỉ số cho ô có <code>sku_count &gt;= K_MIN</code> với <code>K_MIN = 50</code>. Ô có <code>sku_count &lt; K_MIN</code> <strong>MUST</strong> được ghi với <code>suppressed = true</code> và mọi cột chỉ số (<code>median_p, p25_p, p75_p, avg_discount_pct</code>) để <code>NULL</code> (DEC-B2B-05).</li><li><strong>MUST</strong> phát hành chỉ số tổng hợp ở mức ô; <strong>MUST NOT</strong> phát hành giá của một SKU, một shop hay một user đơn lẻ qua bất kỳ cột nào (DEC-B2B-03).</li><li><strong>MUST</strong> chạy như job batch đêm idempotent (DEC-B2B-04): lặp qua cửa sổ ngày cần tính, UPSERT vào <code>market_trend_daily</code> theo khóa <code>(category_id, platform_id, day)</code>. Chạy lại cùng cửa sổ ngày <strong>MUST</strong> cho kết quả y hệt (không nhân đôi, không trôi).</li><li><strong>MUST</strong> expose hàm repo:</li></ol>\n<ul><li><code>UpsertCells(ctx, cells []MarketTrendCell) error</code> - ghi idempotent.</li><li><code>QueryCells(ctx, categoryID int64, platformID int16, from, to time.Time) ([]MarketTrendCell, error)</code> - đọc ô đã phát hành; mặc định <strong>MUST</strong> lọc bỏ ô <code>suppressed = true</code>.</li></ul>\n<ol><li><strong>MUST</strong> ghi <code>sku_count</code> thật ngay cả khi ô bị suppress (để audit nội bộ), nhưng <code>QueryCells</code> mặc định không trả ô suppressed cho người tiêu thụ downstream (DEC-B2B-05).</li><li><strong>MUST</strong> đảm bảo <code>sku_count &gt;= 0</code> và <code>p25_p &lt;= median_p &lt;= p75_p</code> (khi không suppress) qua CHECK constraint hoặc bất biến kiểm trong test.</li><li><strong>SHOULD</strong> phát OTel metric: <code>trend_cells_published_total{platform_id}</code> (counter), <code>trend_cells_suppressed_total{platform_id}</code> (counter), <code>trend_job_duration_ms</code> (histogram).</li><li><strong>MUST</strong> chạy sau khi <code>price_daily</code> đã refresh cho cửa sổ ngày tính (đọc dữ liệu đã ổn định, tránh tính trên ngày chưa chốt).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p>Vì sao chỉ đọc price_daily, không đọc raw hay user-level (DEC-B2B-01)? Dữ liệu B2B là sản phẩm bán ra ngoài. Mọi đường dẫn dữ liệu từ bảng user-level (wishlist, cart, alert) tới đầu ra bán-được là một kênh rò rỉ tiềm tàng. Cắt tận gốc: pipeline chỉ chạm <code>price_daily</code> (đã là dữ liệu giá thị trường tổng hợp theo ngày) và <code>category_id</code>. Lược đồ đầu ra không có chỗ chứa khóa định danh, nên kể cả lập trình sai cũng không có cột để rò.</p>\n<p>Vì sao k-anonymity với K_MIN = 50 (DEC-B2B-02)? Nếu một ô <code>(category, platform, day)</code> chỉ có 1-2 SKU, thì \"median của ô\" gần như chính là giá của một sản phẩm cụ thể - một bên mua dữ liệu biết category hẹp có thể suy ngược ra giá của đối thủ. Ngưỡng 50 SKU phân biệt làm chỉ số tổng hợp đủ \"đông\" để không quy về một cá thể. Category quá hẹp hoặc ngày quá thưa dữ liệu sẽ bị suppress thay vì phát hành.</p>\n<p>Vì sao suppress ghi rõ trạng thái thay vì bỏ hẳn dòng (DEC-B2B-05)? Để audit phân biệt được \"ô này chưa từng tính\" với \"ô này tính rồi nhưng cố ý không phát hành vì dưới ngưỡng\". Ghi <code>suppressed=true</code> + <code>sku_count</code> thật cho phép đội compliance kiểm tra cổng k-anonymity thực sự chạy. Người tiêu thụ downstream không bao giờ thấy ô suppressed vì <code>QueryCells</code> lọc mặc định.</p>\n<p>Vì sao batch idempotent UPSERT (DEC-B2B-04)? Job đêm có thể chạy lại (retry, backfill, sửa lỗi). UPSERT theo khóa ô làm việc chạy lại an toàn: cùng đầu vào -&gt; cùng đầu ra, không nhân đôi. Dữ liệu B2B bán ra phải tái lập được để chứng minh tính đúng khi khách hàng B2B chất vấn một con số.</p>\n<p>Vì sao chạy sau khi price_daily refresh (§1 #11)? <code>price_daily</code> là continuous aggregate refresh tăng dần (TASK-PRICE-002). Tính xu hướng trên một ngày chưa chốt sẽ cho số chập chờn. Chờ ngày đã ổn định để mỗi con số B2B là cuối cùng, không đổi về sau.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/b2b/migrations/0001_market_trend_daily.sql\nCREATE TABLE market_trend_daily (\n  category_id      BIGINT      NOT NULL,\n  platform_id      SMALLINT    NOT NULL,\n  day              DATE        NOT NULL,\n  median_p         BIGINT,                 -- NULL khi suppressed\n  p25_p            BIGINT,\n  p75_p            BIGINT,\n  avg_discount_pct NUMERIC(5,2),           -- 0..100\n  sku_count        INTEGER     NOT NULL CHECK (sku_count &gt;= 0),\n  suppressed       BOOLEAN     NOT NULL DEFAULT false,\n  computed_at      TIMESTAMPTZ NOT NULL DEFAULT now(),\n  PRIMARY KEY (category_id, platform_id, day),\n  -- bất biến thứ tự phân vị khi không suppress\n  CHECK (suppressed OR (p25_p &lt;= median_p AND median_p &lt;= p75_p))\n);\n\nCREATE INDEX idx_mtd_published ON market_trend_daily (category_id, platform_id, day)\n  WHERE suppressed = false;</code></pre>\n<h3>Types (Go)</h3>\n<pre><code class=\"language-go\">// services/b2b/internal/trend/types.go\ntype MarketTrendCell struct {\n    CategoryID     int64     `db:\"category_id\"`\n    PlatformID     int16     `db:\"platform_id\"`\n    Day            time.Time `db:\"day\"`\n    MedianP        *int64    `db:\"median_p\"`           // NULL khi suppressed\n    P25P           *int64    `db:\"p25_p\"`\n    P75P           *int64    `db:\"p75_p\"`\n    AvgDiscountPct *float64  `db:\"avg_discount_pct\"`\n    SKUCount       int32     `db:\"sku_count\"`\n    Suppressed     bool      `db:\"suppressed\"`\n}\n\nconst KMin = 50 // ngưỡng k-anonymity</code></pre>\n<h3>Cổng k-anonymity (§1 #4)</h3>\n<pre><code class=\"language-go\">// services/b2b/internal/trend/kanon.go\n// applyKAnon trả về ô phát hành được; nếu dưới ngưỡng thì suppress (chỉ số NULL).\nfunc applyKAnon(raw aggRow) MarketTrendCell {\n    c := MarketTrendCell{\n        CategoryID: raw.CategoryID,\n        PlatformID: raw.PlatformID,\n        Day:        raw.Day,\n        SKUCount:   raw.SKUCount,\n    }\n    if raw.SKUCount &lt; KMin {\n        c.Suppressed = true // chỉ số để NULL - không phát hành\n        return c\n    }\n    c.MedianP = &amp;raw.MedianP\n    c.P25P = &amp;raw.P25P\n    c.P75P = &amp;raw.P75P\n    c.AvgDiscountPct = &amp;raw.AvgDiscountPct\n    return c\n}</code></pre>\n<h3>Truy vấn aggregate (§1 #2, #3) - chỉ chạm price_daily + tracked_product</h3>\n<pre><code class=\"language-sql\">-- nguồn DUY NHẤT: price_daily (cagg theo ngày) JOIN tracked_product cho category/platform.\n-- KHÔNG có join tới bảng user-level nào.\nSELECT tp.category_id,\n       tp.platform_id,\n       pd.day,\n       percentile_cont(0.5)  WITHIN GROUP (ORDER BY pd.close_p) AS median_p,\n       percentile_cont(0.25) WITHIN GROUP (ORDER BY pd.close_p) AS p25_p,\n       percentile_cont(0.75) WITHIN GROUP (ORDER BY pd.close_p) AS p75_p,\n       avg(CASE WHEN pd.max_p &gt; 0\n                THEN (pd.max_p - pd.close_p)::numeric / pd.max_p * 100 END) AS avg_discount_pct,\n       count(DISTINCT pd.product_id) AS sku_count\nFROM price_daily pd\nJOIN tracked_product tp ON tp.id = pd.product_id\nWHERE pd.day &gt;= $1 AND pd.day &lt; $2\nGROUP BY tp.category_id, tp.platform_id, pd.day;</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch -&gt; <code>market_trend_daily</code> tồn tại với PK <code>(category_id, platform_id, day)</code> và KHÔNG có cột <code>product_id/shop_id/user_id</code>.</li><li>Job sinh một ô có &gt;= 50 SKU phân biệt -&gt; <code>suppressed=false</code>, các cột chỉ số khác NULL.</li><li>Job sinh một ô có 49 SKU -&gt; <code>suppressed=true</code>, mọi cột chỉ số (<code>median_p, p25_p, p75_p, avg_discount_pct</code>) là NULL.</li><li>Ô có 50 SKU (đúng ngưỡng) -&gt; được phát hành (biên K_MIN là \"lớn hơn hoặc bằng\").</li><li><code>QueryCells</code> mặc định KHÔNG trả ô <code>suppressed=true</code>.</li><li>Chạy job hai lần cho cùng cửa sổ ngày -&gt; số dòng và mọi giá trị y hệt (idempotent, UPSERT).</li><li>Với ô được phát hành: <code>p25_p &lt;= median_p &lt;= p75_p</code> (bất biến phân vị).</li><li><code>avg_discount_pct</code> nằm trong khoảng 0..100.</li><li>Quét mã pipeline: không có truy vấn nào chạm <code>price_snapshot</code>, <code>wishlist</code>, <code>alert</code>, <code>cart_snapshot</code> (kiểm bằng test grep hoặc review).</li><li>Output không chứa khóa định danh: mọi dòng <code>market_trend_daily</code> chỉ có <code>category_id/platform_id/day</code> + chỉ số tổng hợp.</li><li>Metric <code>trend_cells_published_total</code> tăng cho ô phát hành; <code>trend_cells_suppressed_total</code> tăng cho ô suppress.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/b2b/internal/trend/kanon_test.go\nfunc TestKAnon_BelowThreshold_Suppressed(t *testing.T) {\n    raw := aggRow{CategoryID: 7, PlatformID: 1, Day: d0, SKUCount: 49,\n        MedianP: 120_000, P25P: 100_000, P75P: 140_000, AvgDiscountPct: 12.5}\n    c := applyKAnon(raw)\n    require.True(t, c.Suppressed)\n    require.Nil(t, c.MedianP)\n    require.Nil(t, c.P25P)\n    require.Nil(t, c.P75P)\n    require.Nil(t, c.AvgDiscountPct)\n    require.EqualValues(t, 49, c.SKUCount) // sku_count thật vẫn ghi để audit\n}\n\nfunc TestKAnon_AtThreshold_Published(t *testing.T) {\n    raw := aggRow{CategoryID: 7, PlatformID: 1, Day: d0, SKUCount: 50,\n        MedianP: 120_000, P25P: 100_000, P75P: 140_000, AvgDiscountPct: 12.5}\n    c := applyKAnon(raw)\n    require.False(t, c.Suppressed)\n    require.NotNil(t, c.MedianP)\n    require.EqualValues(t, 120_000, *c.MedianP)\n}\n\nfunc TestQueryCells_SkipsSuppressed(t *testing.T) {\n    r := setupRepo(t)\n    r.UpsertCells(ctx, []MarketTrendCell{\n        {CategoryID: 7, PlatformID: 1, Day: d0, SKUCount: 60, MedianP: ptr(int64(120_000)),\n            P25P: ptr(int64(100_000)), P75P: ptr(int64(140_000))},\n        {CategoryID: 7, PlatformID: 1, Day: d1, SKUCount: 10, Suppressed: true},\n    })\n    cells, _ := r.QueryCells(ctx, 7, 1, d0, d1.AddDate(0, 0, 1))\n    require.Len(t, cells, 1) // chỉ ô phát hành\n    require.Equal(t, d0.Unix(), cells[0].Day.Unix())\n}\n\nfunc TestJob_Idempotent(t *testing.T) {\n    r := setupRepo(t)\n    seedPriceDaily(t, /* category 7, platform 1, 60 SKU, 3 ngày */)\n    runJob(t, r, d0, d0.AddDate(0, 0, 3))\n    snap1 := dumpAll(t, r)\n    runJob(t, r, d0, d0.AddDate(0, 0, 3)) // chạy lại\n    snap2 := dumpAll(t, r)\n    require.Equal(t, snap1, snap2) // y hệt\n}\n\nfunc TestAggregate_NoIdentifierColumns(t *testing.T) {\n    cols := tableColumns(t, \"market_trend_daily\")\n    require.NotContains(t, cols, \"product_id\")\n    require.NotContains(t, cols, \"shop_id\")\n    require.NotContains(t, cols, \"user_id\")\n}\n\nfunc TestAggregate_PercentileOrder(t *testing.T) {\n    r := setupRepo(t)\n    seedPriceDaily(t, /* 60 SKU phân bố giá */)\n    runJob(t, r, d0, d0.AddDate(0, 0, 1))\n    cells, _ := r.QueryCells(ctx, 7, 1, d0, d0.AddDate(0, 0, 1))\n    require.LessOrEqual(t, *cells[0].P25P, *cells[0].MedianP)\n    require.LessOrEqual(t, *cells[0].MedianP, *cells[0].P75P)\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration 0001 (bảng) -&gt; aggregate.go (truy vấn nhóm từ price_daily) -&gt; kanon.go (cổng K_MIN) -&gt; job.go (batch idempotent) -&gt; repo.go -&gt; tests. Job lập lịch chạy đêm sau cửa sổ refresh của <code>price_daily</code>. Cửa sổ ngày tính nên trễ một ngày so với hôm nay để chỉ chạm ngày đã chốt. Backfill lịch sử chạy job với cửa sổ ngày rộng, an toàn nhờ UPSERT idempotent.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li>TASK-PRICE-002 - <code>price_daily</code> (continuous aggregate) phải tồn tại và đã refresh; là nguồn dữ liệu duy nhất của pipeline.</li><li>TASK-COMPLY-003 - quyền chủ thể dữ liệu (DSAR); khi user xóa dữ liệu, đường dẫn B2B đã ẩn danh + tổng hợp nên không chứa dữ liệu cá nhân của họ, nhưng pipeline phải nhất quán với chính sách xử lý dữ liệu PDPL.</li><li>TASK-B2B-002 / TASK-B2B-003 / TASK-B2B-004 (downstream) - đọc <code>market_trend_daily</code> qua <code>QueryCells</code>; không bao giờ đọc nguồn raw.</li><li>Extension/lib: PostgreSQL <code>percentile_cont</code>; driver <code>pgx</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Một ô đã phát hành (đọc nội bộ qua QueryCells)</h3>\n<pre><code class=\"language-json\">{\n  \"category_id\": 7,\n  \"platform_id\": 1,\n  \"day\": \"2026-06-20\",\n  \"median_p\": 320000,\n  \"p25_p\": 250000,\n  \"p75_p\": 410000,\n  \"avg_discount_pct\": 14.30,\n  \"sku_count\": 412,\n  \"suppressed\": false\n}</code></pre>\n<h3>Một ô bị suppress (chỉ thấy trong audit nội bộ, downstream không đọc)</h3>\n<pre><code class=\"language-json\">{\n  \"category_id\": 991,\n  \"platform_id\": 2,\n  \"day\": \"2026-06-20\",\n  \"median_p\": null,\n  \"p25_p\": null,\n  \"p75_p\": null,\n  \"avg_discount_pct\": null,\n  \"sku_count\": 18,\n  \"suppressed\": true\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>K_MIN có nên khác nhau theo độ nhạy của category (mỹ phẩm vs điện tử) - giữ 50 cho mọi category ở slice này, hiệu chỉnh sau nếu compliance yêu cầu.</li><li>Thêm chiều <code>region</code>/<code>country</code> cho ô khi mở SEA - gắn khi TASK-COMPLY-007 chốt mô hình per-country (giữ k-anonymity per-country để không gộp chéo nước).</li><li>Bổ sung nhiễu vi sai (differential privacy) lên trên k-anonymity - cân nhắc nếu khách hàng B2B yêu cầu độ chi tiết cao hơn mà vẫn an toàn.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Ô dưới ngưỡng được phát hành</td><td>kanon_test + audit suppressed</td><td>rủi ro tái định danh, vi phạm PDPL</td><td>Cổng K_MIN ép suppress; test biên 49/50</td></tr><tr><td>Pipeline đọc bảng user-level</td><td>review + test grep truy vấn</td><td>rò rỉ dữ liệu cá nhân</td><td>Chỉ join price_daily + tracked_product (DEC-B2B-01)</td></tr><tr><td>Output lọt khóa định danh</td><td>TestAggregate_NoIdentifierColumns</td><td>tái định danh trực tiếp</td><td>Lược đồ không có cột product_id/shop_id/user_id</td></tr><tr><td>Job chạy lại nhân đôi dòng</td><td>TestJob_Idempotent</td><td>dữ liệu B2B sai, mất uy tín</td><td>UPSERT theo khóa ô</td></tr><tr><td>Tính trên ngày chưa chốt</td><td>so số đổi giữa các lần chạy</td><td>con số B2B chập chờn</td><td>Chỉ tính ngày trễ &gt;= 1 sau refresh price_daily</td></tr><tr><td>p25 &gt; median do lỗi tính phân vị</td><td>CHECK constraint + test</td><td>dữ liệu vô lý</td><td>percentile_cont đúng thứ tự + CHECK</td></tr><tr><td>avg_discount_pct ngoài 0..100</td><td>test biên</td><td>chỉ số phi lý</td><td>CASE max_p &gt; 0 + kẹp khoảng</td></tr><tr><td>category_id null trong tracked_product</td><td>join loại bỏ dòng</td><td>thiếu ô</td><td>Chấp nhận; SKU chưa phân loại không vào aggregate</td></tr><tr><td>K_MIN đặt quá thấp do cấu hình</td><td>review hằng số</td><td>rò rỉ</td><td>K_MIN là hằng số mã, đổi phải qua review</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li><code>market_trend_daily</code> là tài sản dữ liệu bán-được của SănDeal; lược đồ cố ý không chứa khóa định danh để bản thân nó không tái định danh được.</li><li>Hai lớp phòng vệ độc lập: (1) chỉ đọc dữ liệu đã tổng hợp theo ngày, (2) cổng k-anonymity K_MIN = 50. Một lớp hỏng thì lớp kia vẫn chặn.</li><li>Suppress ghi rõ trạng thái + sku_count thật là để audit chứng minh cổng k-anonymity thực sự chạy, không phải bị bỏ qua.</li><li>Idempotent UPSERT làm dữ liệu B2B tái lập được - điều kiện để bán dữ liệu cho khách hàng đòi hỏi tính đúng kiểm chứng được.</li><li>Khi mở SEA, giữ k-anonymity riêng từng nước; KHÔNG gộp SKU chéo nước để lấp ô thưa (sẽ trộn thị trường khác nhau và lách ngưỡng).</li></ul>\n<hr>\n<p><em>Hết TASK-B2B-001. Status: ready_to_review (awaiting HITL).</em></p>\n<p><em>HITL accept (operator merge-then-continue): feature PR #104 merge <code>1bc583fce6be2eda10737dfbfe79c43d20157ea5</code> → done.</em></p>";
