window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-INFRA-002"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service INFRA <strong>MUST</strong> thiết lập nền dữ liệu PostgreSQL: framework migration golang-migrate có up/down, bảng <code>platform</code> làm gốc tham chiếu sàn, cột lõi <code>app_user</code>, và một tài liệu quy ước đặt tên/migration. Hợp đồng:</p>\n<ol><li>Hệ thống <strong>MUST</strong> dùng golang-migrate với mỗi migration là một cặp file <code>NNNN_name.up.sql</code> + <code>NNNN_name.down.sql</code>, đánh số tăng dần liên tục, bất biến sau khi merge (DEC-INFRA-06). Thay đổi schema về sau là thêm migration mới, KHÔNG sửa file cũ.</li><li>Migration <code>0001_extensions</code> <strong>MUST</strong> chạy đầu tiên: <code>CREATE EXTENSION IF NOT EXISTS citext</code> (và stub bật <code>timescaledb</code> cho PRICE) trước mọi bảng phụ thuộc kiểu CITEXT (DEC-INFRA-10).</li><li>Migration <code>0002_platform</code> <strong>MUST</strong> tạo bảng <code>platform (id SMALLINT PK, code TEXT UNIQUE NOT NULL, country TEXT NOT NULL, base_url TEXT, created_at TIMESTAMPTZ DEFAULT now())</code>.</li><li>Bảng <code>platform</code> <strong>MUST</strong> ràng buộc <code>code</code> - {<code>'shopee'</code>,<code>'tiktok'</code>,<code>'lazada'</code>} qua CHECK, và <code>country</code> là mã ISO-3166 alpha-2 (hai chữ in hoa) qua CHECK (DEC-INFRA-07).</li><li>Seed <code>0001_platform_seed</code> <strong>MUST</strong> chèn tối thiểu 3 dòng cho thị trường VN: <code>(1,'shopee','VN',...)</code>, <code>(2,'tiktok','VN',...)</code>, <code>(3,'lazada','VN',...)</code>, idempotent qua <code>ON CONFLICT (code) DO NOTHING</code>.</li><li>Migration <code>0003_app_user_core</code> <strong>MUST</strong> tạo <code>app_user</code> với cột lõi: <code>id BIGSERIAL PK</code>, <code>email CITEXT UNIQUE</code>, <code>phone TEXT</code>, <code>display_name TEXT</code>, <code>locale TEXT NOT NULL DEFAULT 'vi-VN'</code>, <code>status TEXT NOT NULL DEFAULT 'active'</code>, <code>created_at TIMESTAMPTZ DEFAULT now()</code> (DEC-INFRA-08).</li><li><code>app_user.email</code> <strong>MUST</strong> là CITEXT để so sánh không phân biệt hoa thường: <code>Foo@x.com</code> và <code>foo@x.com</code> xung đột UNIQUE (chống tạo trùng tài khoản qua biến thể hoa thường).</li><li>TASK-AUTH-001 <strong>MUST</strong> mở rộng <code>app_user</code> ở migration sau (thêm <code>pwd_hash</code>, <code>referral_code_id</code>, ...); task này KHÔNG định nghĩa cột bảo mật để giữ ranh giới module.</li><li>Runner migration <strong>MUST</strong> hỗ trợ: <code>Up()</code> (áp mọi migration chưa chạy), <code>Down(n)</code> (rollback n bước), <code>Version()</code> (đọc version hiện tại), và an toàn idempotent (chạy <code>Up</code> lần hai không lỗi, không đổi schema).</li><li>Mọi bảng <strong>MUST</strong> theo quy ước đặt tên (DEC-INFRA-09): tên bảng snake_case số ít, khoá chính tên <code>id</code>, cột thời gian hậu tố <code>_at</code> kiểu TIMESTAMPTZ. Tài liệu <code>NAMING-AND-MIGRATIONS.md</code> ghi quy ước này như nguồn sự thật.</li><li>Khoá chính của bảng có khả năng lớn (<code>app_user</code> và các bảng nghiệp vụ) <strong>MUST</strong> là <code>BIGSERIAL</code>/<code>BIGINT</code>; chỉ bảng nhỏ cố định (<code>platform</code>) dùng <code>SMALLINT</code>.</li><li>Runner <strong>SHOULD</strong> ghi log có cấu trúc mỗi bước migration (version từ -&gt; đến, hướng up/down, thời gian) để truy vết khi deploy.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p>Vì sao golang-migrate với migration bất biến (DEC-INFRA-06)? Schema phải tái lập y hệt giữa dev, staging và prod. Migration đánh số, cặp up/down, không sửa sau khi merge cho ta một lịch sử tuyến tính kiểm toán được: mọi môi trường chạy cùng chuỗi và tới cùng trạng thái. Sửa file cũ phá tính tái lập (môi trường đã chạy bản cũ sẽ lệch).</p>\n<p>Vì sao <code>platform</code> là gốc (DEC-INFRA-07)? SănDeal đa sàn ngay từ thiết kế. <code>platform_account</code>, <code>tracked_product</code>, <code>voucher_catalog</code>, <code>affiliate_click</code> đều tham chiếu một sàn. Tập trung định nghĩa sàn vào một bảng (với CHECK code) tránh chuỗi <code>'shopee'</code> rải rác và sai chính tả; thêm sàn mới là thêm một dòng, không phải sửa nhiều nơi.</p>\n<p>Vì sao email CITEXT (§1 #7)? Người dùng gõ email với hoa thường tùy hứng. Nếu <code>email</code> là TEXT thường, <code>Foo@x.com</code> và <code>foo@x.com</code> là hai bản ghi khác nhau -&gt; một người tạo được nhiều tài khoản, đăng nhập rối. CITEXT làm UNIQUE so sánh case-insensitive, đúng ngữ nghĩa email.</p>\n<p>Vì sao tách cột lõi <code>app_user</code> khỏi cột bảo mật (DEC-INFRA-08, §1 #8)? INFRA dựng khung dữ liệu; AUTH lo bảo mật. Để cột lõi (id, email, locale, status) ở migration nền cho mọi module gắn vào; để <code>pwd_hash</code>/<code>referral_code_id</code> cho TASK-AUTH-001. Ranh giới này giữ migration nền không bị kẹt chờ quyết định bảo mật.</p>\n<p>Vì sao quy ước đặt tên thành tài liệu (DEC-INFRA-09)? 16 module sẽ tạo hàng chục bảng. Không có quy ước chung thì mỗi module đặt kiểu khác (<code>userId</code> vs <code>user_id</code>, <code>createdAt</code> vs <code>created_at</code>), gây ma sát join và migration. Một tài liệu quy ước là hợp đồng để mọi task sau tuân theo.</p>\n<p>Vì sao <code>locale</code> mặc định <code>'vi-VN'</code> (§1 #6)? Thị trường gốc là Việt Nam; phần lớn người dùng là vi-VN. Đặt mặc định đúng giảm boilerplate và là điểm neo cho i18n và per-country gating (TASK-INFRA-005) khi mở SEA.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migrations</h3>\n<pre><code class=\"language-sql\">-- db/migrations/0001_extensions.up.sql\nCREATE EXTENSION IF NOT EXISTS citext;\n-- timescaledb được bật khi triển khai PRICE (TASK-PRICE-002); stub ở đây để thứ tự rõ ràng:\n-- CREATE EXTENSION IF NOT EXISTS timescaledb;\n\n-- db/migrations/0002_platform.up.sql\nCREATE TABLE platform (\n  id         SMALLINT     PRIMARY KEY,\n  code       TEXT         UNIQUE NOT NULL\n               CHECK (code IN ('shopee','tiktok','lazada')),\n  country    TEXT         NOT NULL\n               CHECK (country ~ '^[A-Z]{2}$'),     -- ISO-3166 alpha-2\n  base_url   TEXT,\n  created_at TIMESTAMPTZ  DEFAULT now()\n);\n\n-- db/migrations/0003_app_user_core.up.sql\nCREATE TABLE app_user (\n  id           BIGSERIAL    PRIMARY KEY,\n  email        CITEXT       UNIQUE,                 -- case-insensitive unique\n  phone        TEXT,\n  display_name TEXT,\n  locale       TEXT         NOT NULL DEFAULT 'vi-VN',\n  status       TEXT         NOT NULL DEFAULT 'active',\n  created_at   TIMESTAMPTZ  DEFAULT now()\n);\n-- Cột bảo mật (pwd_hash, referral_code_id) do TASK-AUTH-001 thêm ở migration sau.</code></pre>\n<h3>Seed (idempotent)</h3>\n<pre><code class=\"language-sql\">-- db/seed/0001_platform_seed.sql\nINSERT INTO platform (id, code, country, base_url) VALUES\n  (1, 'shopee', 'VN', 'https://shopee.vn'),\n  (2, 'tiktok', 'VN', 'https://www.tiktok.com'),\n  (3, 'lazada', 'VN', 'https://www.lazada.vn')\nON CONFLICT (code) DO NOTHING;</code></pre>\n<h3>Migration runner (Go)</h3>\n<pre><code class=\"language-go\">// db/internal/migrate/migrate.go\ntype Migrator struct{ m *migrate.Migrate }\n\nfunc (mg *Migrator) Up() error {\n    if err := mg.m.Up(); err != nil &amp;&amp; !errors.Is(err, migrate.ErrNoChange) {\n        return err // ErrNoChange (đã mới nhất) là idempotent, không phải lỗi\n    }\n    return nil\n}\nfunc (mg *Migrator) Down(n int) error { return mg.m.Steps(-n) }\nfunc (mg *Migrator) Version() (uint, bool, error) { return mg.m.Version() }</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>Up()</code> trên DB rỗng -&gt; mọi migration áp; bảng <code>platform</code> và <code>app_user</code> tồn tại.</li><li><code>Up()</code> lần hai -&gt; không lỗi (idempotent), version không đổi (<code>ErrNoChange</code> được nuốt).</li><li><code>Down(1)</code> sau <code>Up()</code> -&gt; rollback đúng một bước (bảng của migration cuối biến mất).</li><li>Seed -&gt; <code>SELECT count(*) FROM platform</code> &gt;= 3; có đủ <code>shopee/tiktok/lazada</code>.</li><li>Chạy seed lần hai -&gt; vẫn 3 dòng (ON CONFLICT DO NOTHING).</li><li>INSERT <code>platform(code='amazon')</code> -&gt; lỗi CHECK (code ngoài allowlist).</li><li>INSERT <code>platform(country='Vietnam')</code> -&gt; lỗi CHECK (country không phải alpha-2).</li><li>INSERT hai <code>app_user</code> cùng email khác hoa thường (<code>A@x.com</code>, <code>a@x.com</code>) -&gt; vi phạm UNIQUE (CITEXT).</li><li>INSERT <code>app_user</code> không set <code>locale</code> -&gt; mặc định <code>'vi-VN'</code>; không set <code>status</code> -&gt; <code>'active'</code>.</li><li><code>Version()</code> trả version hiện tại đúng sau chuỗi up/down.</li><li>Thứ tự migration: chạy <code>0002_platform</code> trước <code>0001_extensions</code> thất bại nếu CITEXT chưa bật - xác nhận <code>0001</code> đứng trước.</li><li><code>NAMING-AND-MIGRATIONS.md</code> tồn tại và mô tả quy ước (snake_case số ít, <code>id</code>, <code>*_at</code>, BIGSERIAL).</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// db/internal/migrate/migrate_test.go\nfunc TestUp_FromZero(t *testing.T) {\n    mg := newMigratorFreshDB(t)\n    require.NoError(t, mg.Up())\n    require.True(t, tableExists(t, \"platform\"))\n    require.True(t, tableExists(t, \"app_user\"))\n}\n\nfunc TestUp_Idempotent(t *testing.T) {\n    mg := newMigratorFreshDB(t)\n    require.NoError(t, mg.Up())\n    require.NoError(t, mg.Up()) // lần hai không lỗi\n}\n\nfunc TestDown_OneStep(t *testing.T) {\n    mg := newMigratorFreshDB(t)\n    require.NoError(t, mg.Up())\n    require.NoError(t, mg.Down(1))\n    require.False(t, tableExists(t, \"app_user\")) // migration cuối bị rollback\n}\n\nfunc TestPlatformSeed_Idempotent(t *testing.T) {\n    db := upWithSeed(t)\n    seed(t, db) // chạy seed lần hai\n    require.Equal(t, 3, countRows(t, db, \"platform\"))\n}\n\nfunc TestPlatform_CodeCheck(t *testing.T) {\n    db := upWithSeed(t)\n    _, err := db.Exec(`INSERT INTO platform(id,code,country) VALUES (9,'amazon','VN')`)\n    require.Error(t, err) // CHECK code IN (...)\n}\n\nfunc TestPlatform_CountryCheck(t *testing.T) {\n    db := upWithSeed(t)\n    _, err := db.Exec(`INSERT INTO platform(id,code,country) VALUES (9,'shopee','Vietnam')`)\n    require.Error(t, err) // CHECK country ~ '^[A-Z]{2}$'\n}\n\nfunc TestAppUser_EmailCaseInsensitiveUnique(t *testing.T) {\n    db := upWithSeed(t)\n    _, err1 := db.Exec(`INSERT INTO app_user(email) VALUES ('A@x.com')`)\n    require.NoError(t, err1)\n    _, err2 := db.Exec(`INSERT INTO app_user(email) VALUES ('a@x.com')`)\n    require.Error(t, err2) // CITEXT unique\n}\n\nfunc TestAppUser_Defaults(t *testing.T) {\n    db := upWithSeed(t)\n    var locale, status string\n    db.QueryRow(`INSERT INTO app_user(email) VALUES ('d@x.com')\n                 RETURNING locale, status`).Scan(&amp;locale, &amp;status)\n    require.Equal(t, \"vi-VN\", locale)\n    require.Equal(t, \"active\", status)\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự cố định: <code>0001_extensions</code> (CITEXT trước) -&gt; <code>0002_platform</code> -&gt; <code>0003_app_user_core</code> -&gt; seed. Runner bọc golang-migrate; nuốt <code>ErrNoChange</code> để <code>Up</code> idempotent. Seed chạy sau migration, idempotent qua <code>ON CONFLICT</code>. Tài liệu quy ước đặt cạnh migration để mọi task sau tham chiếu. Test chạy trên DB tạm (testcontainers Postgres hoặc instance ephemeral) để kiểm cả CHECK và CITEXT thật, không mock.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li>Không có task phụ thuộc trước (đây là một trong các nền móng P0).</li><li>TASK-AUTH-001 (downstream) - thêm <code>pwd_hash</code>, <code>referral_code_id</code> vào <code>app_user</code> qua migration mới.</li><li>TASK-AUTH-003 / TASK-PRICE-001 / TASK-CART-001 / TASK-AFFIL-001 / TASK-BILL-001 (downstream) - FK tới <code>platform</code> và/hoặc <code>app_user</code>.</li><li>TASK-INFRA-005 (downstream) - đọc <code>platform.country</code> cho per-country gating.</li><li>Hạ tầng: PostgreSQL 16, extension <code>citext</code> (và sau là <code>timescaledb</code> cho PRICE).</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Áp migration lúc deploy</h3>\n<pre><code class=\"language-bash\"># CI/CD: nâng schema lên bản mới nhất trước khi rollout service\nmigrate -path db/migrations -database \"$DATABASE_URL\" up</code></pre>\n<h3>Trạng thái sau seed</h3>\n<pre><code class=\"language-sql\">SELECT id, code, country, base_url FROM platform ORDER BY id;\n--  1 | shopee | VN | https://shopee.vn\n--  2 | tiktok | VN | https://www.tiktok.com\n--  3 | lazada | VN | https://www.lazada.vn</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Phân vùng (partition) <code>app_user</code> theo region khi mở SEA - chưa cần ở quy mô P0/P1.</li><li>Soft-delete chuẩn hóa (cột <code>deleted_at</code>) cho mọi bảng - gắn vào TASK-AUTH-005 (xóa tài khoản DSAR) và nhân rộng sau.</li><li>Quản lý seed theo môi trường (dev seed khác prod seed) - thêm khi cần dữ liệu mẫu phong phú.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Sửa migration đã merge</td><td>diff review/CI</td><td>môi trường lệch schema</td><td>Cấm sửa; thêm migration mới (DEC-INFRA-06)</td></tr><tr><td>Tạo bảng CITEXT trước 0001</td><td>migration fail</td><td>up đứt giữa chừng</td><td>Giữ 0001_extensions đầu chuỗi (§1 #2)</td></tr><tr><td>Migration nửa chừng (lỗi giữa up)</td><td>schema_migrations dirty</td><td>deploy kẹt</td><td><code>force &lt;version&gt;</code> về điểm sạch rồi chạy lại</td></tr><tr><td>Email trùng qua biến thể hoa thường</td><td>UNIQUE CITEXT</td><td>từ chối tạo trùng</td><td>Theo thiết kế (§1 #7)</td></tr><tr><td>Code sàn sai chính tả</td><td>CHECK constraint</td><td>từ chối insert</td><td>Sửa nguồn; chỉ dùng code trong allowlist</td></tr><tr><td>Country không phải alpha-2</td><td>CHECK constraint</td><td>từ chối insert</td><td>Chuẩn hóa về ISO-3166 alpha-2</td></tr><tr><td>Khoá chính dùng SERIAL bị tràn</td><td>giám sát maxval</td><td>hết id khi lớn</td><td>BIGSERIAL cho bảng lớn (§1 #11)</td></tr><tr><td>Seed chạy nhiều lần nhân đôi</td><td>ON CONFLICT</td><td>giữ 3 dòng</td><td>Idempotent seed (§1 #5)</td></tr><tr><td>Down rollback mất dữ liệu prod</td><td>review</td><td>mất bảng/dữ liệu</td><td>Down chỉ ở dev; prod forward-only + backup</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Đây là nền dữ liệu: 9+ task khác FK tới <code>platform</code> hoặc <code>app_user</code>, hoặc đọc <code>platform.country</code>.</li><li>Migration bất biến + đánh số cho lịch sử schema tái lập được giữa mọi môi trường - gốc của deploy tin cậy.</li><li><code>platform</code> tập trung định nghĩa sàn, tránh literal <code>'shopee'</code> rải rác và sai chính tả; thêm sàn là thêm một dòng.</li><li>CITEXT trên email chặn tài khoản trùng qua biến thể hoa thường - một lỗi tinh vi nếu dùng TEXT thường.</li><li>Tách cột lõi (INFRA) khỏi cột bảo mật (AUTH) giữ ranh giới module sạch; AUTH mở rộng <code>app_user</code> bằng migration riêng.</li><li>Tài liệu quy ước đặt tên là hợp đồng cho mọi task sau, giảm ma sát join/migration khi 16 module cùng tạo bảng.</li></ul>\n<hr>\n<p><em>Hết TASK-INFRA-002. Status: done (mục tiêu audit 10/10).</em></p>";
