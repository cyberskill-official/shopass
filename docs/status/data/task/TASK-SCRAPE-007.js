window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SCRAPE-007"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Adapter TikTok Shop <strong>MUST</strong> lấy giá bằng đọc DOM-render qua farm Playwright, né hoàn toàn API ký, dùng proxy enterprise. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> triển khai interface adapter của TASK-SCRAPE-001 và lấy giá qua farm <code>RenderPrice</code> của TASK-SCRAPE-003 (DEC-SCRAPE-28) - TikTok buộc DOM-render, không có đường JSON-không-login như Shopee.</li><li><strong>MUST NOT</strong> tái tạo hay ký <code>msToken</code>/<code>_signature</code>/<code>X-Bogus</code> (DEC-SCRAPE-29): adapter không gọi API nội bộ cần chữ ký; chỉ đọc trang đã render trong ngữ cảnh trình duyệt thật của farm.</li><li><strong>MUST</strong> dùng proxy tier <code>enterprise</code> (DEC-SCRAPE-30): khi cấp proxy cho job TikTok, dùng <code>SelectTier(DiffByteDance)</code> của TASK-SCRAPE-004 (= enterprise) vì rủi ro ban High.</li><li><strong>MUST</strong> trích giá ưu tiên từ JSON nhúng trong trang (DEC-SCRAPE-31): nếu trang có state JSON (kiểu <code>__NEXT_DATA__</code>/<code>SIGI_STATE</code>), parse từ đó (ổn định hơn DOM text); fallback đọc DOM text khi không có hoặc cấu trúc đổi.</li><li><strong>MUST</strong> map về <code>price.PriceSnapshot</code> với <code>price</code>/<code>list_price</code> BIGINT VND số nguyên (đồng bộ DEC-PRICE-05 của TASK-PRICE-002), <code>flash_sale</code> true khi trang báo đang flash/live-deal.</li><li><strong>MUST</strong> chạy hành vi giống người của farm (TASK-SCRAPE-003 #6) trước khi trích - TikTok có hệ hành vi mạnh, đọc tức thì là cờ đỏ.</li><li><strong>MUST</strong> phát hiện challenge/verify và ném <code>ChallengedError</code> (như TASK-SCRAPE-003 #8) để TASK-SCRAPE-005 / orchestrator xử lý, KHÔNG trả snapshot rỗng.</li><li><strong>MUST</strong> báo outcome (success/parse_fail/challenge) cho monitor của TASK-SCRAPE-006 - TikTok là webview/SPA dễ đổi cấu trúc, cần giám sát drift sát.</li><li><strong>MUST</strong> xử lý SPA: chờ nội dung giá render xong (wait cho selector/state) thay vì đọc DOM lúc trang chưa hydrate.</li><li><strong>MUST</strong> tối thiểu hóa: chỉ trích trường giá, không lưu trang thô (§5.4).</li><li><strong>SHOULD</strong> phát OTel metric: <code>tiktok_extract_source_total{source=json|dom}</code> (counter), <code>tiktok_challenge_total</code> (counter), <code>tiktok_render_duration_ms</code> (histogram).</li><li><strong>MUST</strong> quy đổi giá bằng số nguyên (không float trung gian) khi đơn vị TikTok khác VND nguyên.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao đọc DOM-render, né API ký (DEC-SCRAPE-28/29)?</strong> Tài liệu nói rõ (§3.2): TikTok ký request ở tầng API nội bộ (<code>msToken</code>/<code>_signature</code>/<code>X-Bogus</code>) cùng app attestation mạnh. Tái tạo chữ ký là cuộc đua vũ trang: ByteDance đổi thuật toán, adapter hỏng, sửa lại, lặp mãi - giòn và tốn công vô tận. Đọc trang đã render trong trình duyệt thật của farm để chính TikTok tự tạo chữ ký hợp lệ, ta chỉ đọc kết quả.</p>\n<p><strong>Vì sao proxy enterprise (DEC-SCRAPE-30)?</strong> TikTok là rủi ro ban High (§3.9) - cao nhất trong ba sàn. Proxy budget/datacenter bị chặn ngay. <code>SelectTier(DiffByteDance)</code> của TASK-SCRAPE-004 trả enterprise đúng cho trường hợp này: trả tiền cao để đổi lấy độ tin cậy, vì TikTok không tha IP chất lượng thấp.</p>\n<p><strong>Vì sao ưu tiên JSON nhúng (DEC-SCRAPE-31)?</strong> SPA thường nhúng state JSON trong trang để hydrate. Đọc giá từ JSON này ổn định hơn nhiều so với cào DOM text (text đổi theo layout/A/B, JSON state ít đổi hơn). Fallback DOM text giữ độ phủ khi cấu trúc JSON đổi.</p>\n<p><strong>Vì sao chờ hydrate SPA (§1 #9)?</strong> TikTok Shop là webview/SPA: HTML ban đầu rỗng, giá render bằng JS sau. Đọc DOM ngay lúc <code>domcontentloaded</code> sẽ thấy trang trống. Phải chờ selector/state giá xuất hiện trước khi trích, nếu không sẽ parse_fail giả.</p>\n<p><strong>Vì sao giám sát drift sát (§1 #8)?</strong> TikTok là SPA, đổi cấu trúc thường xuyên hơn trang truyền thống. Báo outcome đều cho monitor (TASK-SCRAPE-006) để bắt sớm khi TikTok đổi state shape/selectors - adapter SPA hỏng âm thầm là rủi ro lớn hơn adapter JSON-API.</p>\n<p><strong>Vì sao vẫn dùng farm thay adapter riêng (§1 #1)?</strong> Farm (TASK-SCRAPE-003) đã giải bài fingerprint + TLS + behavior. TikTok adapter chỉ thêm phần \"đọc giá ở đâu trên trang TikTok\". Tái dùng farm tránh nhân bản lớp chống ban; adapter chỉ là selectors + extract logic riêng của TikTok.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Selectors + extract (TypeScript)</h3>\n<pre><code class=\"language-typescript\">// services/scrape/farm/src/adapters/tiktok/selectors.ts\nexport const tiktokSelectors = {\n  embeddedState: 'script#__NEXT_DATA__, script#SIGI_STATE',\n  priceText:     '[data-e2e=\"product-price\"]',\n  listPriceText: '[data-e2e=\"product-origin-price\"]',\n  flashBadge:    '[data-e2e=\"flash-sale-badge\"], [data-e2e=\"live-deal\"]',\n  readyAnchor:   '[data-e2e=\"product-price\"]', // chờ selector này trước khi trích (SPA hydrate)\n};</code></pre>\n<pre><code class=\"language-typescript\">// services/scrape/farm/src/adapters/tiktok/extract.ts\nconst VND_UNIT = 1; // TikTok hiển thị VND nguyên; chỉnh nếu phát hiện micro-đơn-vị\n\n// extractTikTok ưu tiên JSON nhúng, fallback DOM text; trả giá VND số nguyên.\nexport function extractTikTok(page: PageView): RawPrice {\n  const state = page.embeddedJSON(tiktokSelectors.embeddedState);\n  if (state) {\n    const p = pickPriceFromState(state); // đọc price/origin/flash từ state JSON\n    if (p) return p;                      // source = json\n  }\n  // fallback DOM text\n  return {\n    price:     parseVNDInt(page.text(tiktokSelectors.priceText)),       // bỏ ký tự '₫', '.', số nguyên\n    listPrice: parseVNDInt(page.text(tiktokSelectors.listPriceText)),\n    flashSale: page.exists(tiktokSelectors.flashBadge),\n    source:    'dom',\n  };\n}</code></pre>\n<h3>Adapter (TypeScript)</h3>\n<pre><code class=\"language-typescript\">// services/scrape/farm/src/adapters/tiktok/adapter.ts\nexport class TikTokAdapter implements PlatformAdapter {\n  platformID() { return PLATFORM_TIKTOK; }\n\n  async fetch(ctx: BrowserContext, job: ScrapeJob): Promise&lt;PriceSnapshot&gt; {\n    const page = await ctx.newPage();\n    try {\n      await page.goto(job.url, { waitUntil: 'domcontentloaded' });\n      if (await detectChallenge(page)) throw new ChallengedError('tiktok verify'); // không ký API\n      await humanize(page);                                  // TASK-SCRAPE-003 behavior\n      await page.waitForSelector(tiktokSelectors.readyAnchor); // chờ SPA hydrate\n      const raw = extractTikTok(viewOf(page));\n      metrics.tiktokSource(raw.source);\n      return toSnapshot(job.productId, raw, VND_UNIT);       // VND số nguyên, set ts\n    } finally {\n      await page.close();\n    }\n  }\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>TikTokAdapter</code> thỏa interface <code>PlatformAdapter</code>; <code>platformID()</code> trả mã TikTok.</li><li>Job TikTok được cấp proxy tier <code>enterprise</code> (<code>SelectTier(DiffByteDance)</code> = enterprise).</li><li>Adapter KHÔNG gọi API nội bộ cần <code>msToken</code>/<code>_signature</code>/<code>X-Bogus</code> (kiểm không có request tới endpoint ký trong test).</li><li>Trang có JSON nhúng -&gt; <code>extractTikTok</code> trả giá từ state (<code>source='json'</code>).</li><li>Trang không có JSON nhúng -&gt; fallback DOM text (<code>source='dom'</code>), giá VND đúng (số nguyên, đã bỏ ' VND'/'.').</li><li><code>flash_sale</code> = true khi trang có badge flash/live-deal.</li><li>Giá quy đổi bằng số nguyên; với text \" VND89.000\" -&gt; <code>price=89000</code> chính xác.</li><li>Trang verify/challenge -&gt; <code>fetch</code> ném <code>ChallengedError</code>, không trả snapshot.</li><li>SPA chưa hydrate -&gt; adapter chờ <code>readyAnchor</code> trước khi trích (không parse trang trống).</li><li>Adapter báo outcome (success/parse_fail/challenge) cho monitor TASK-SCRAPE-006.</li><li>Page đóng sau render; chỉ trường giá được giữ (không lưu trang thô).</li><li>Metric <code>tiktok_extract_source_total{source}</code>, <code>tiktok_challenge_total</code> thay đổi đúng.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-typescript\">// services/scrape/farm/src/adapters/tiktok/__tests__/extract.test.ts\ntest('ưu tiên JSON nhúng', () =&gt; {\n  const page = pageWithState({ product: { price: 89000, originPrice: 149000, flash: true } });\n  const raw = extractTikTok(page);\n  expect(raw.source).toBe('json');\n  expect(raw.price).toBe(89000);\n  expect(raw.listPrice).toBe(149000);\n  expect(raw.flashSale).toBe(true);\n});\n\ntest('fallback DOM text khi không có JSON', () =&gt; {\n  const page = pageDomOnly({ price: '₫89.000', origin: '₫149.000', flash: false });\n  const raw = extractTikTok(page);\n  expect(raw.source).toBe('dom');\n  expect(raw.price).toBe(89000);   // bỏ '₫' và '.', số nguyên\n});\n\ntest('parse VND số nguyên không float', () =&gt; {\n  expect(parseVNDInt('₫1.234.567')).toBe(1234567);\n});</code></pre>\n<pre><code class=\"language-typescript\">// services/scrape/farm/src/adapters/tiktok/__tests__/adapter.test.ts\ntest('không gọi endpoint ký', async () =&gt; {\n  const calls: string[] = [];\n  const ctx = ctxRecordingRequests(calls);\n  await new TikTokAdapter().fetch(ctx, job('https://shop.tiktok.com/x'));\n  expect(calls.some(u =&gt; /msToken|_signature|X-Bogus/.test(u))).toBe(false);\n});\n\ntest('challenge ném ChallengedError', async () =&gt; {\n  const ctx = ctxWithChallenge();\n  await expect(new TikTokAdapter().fetch(ctx, job('u'))).rejects.toBeInstanceOf(ChallengedError);\n});\n\ntest('chờ hydrate trước khi trích', async () =&gt; {\n  const ctx = ctxSlowHydrate(tiktokSelectors.readyAnchor);\n  const snap = await new TikTokAdapter().fetch(ctx, job('u'));\n  expect(snap.price).toBeGreaterThan(0); // không parse trang trống\n});</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: selectors.ts -&gt; extract.ts (JSON-ưu-tiên + DOM-fallback, test trước với fixture) -&gt; adapter.ts (render qua farm + chờ hydrate + challenge) -&gt; registry.go (đăng ký, gắn proxy enterprise) -&gt; tests. Adapter chạy trên farm Playwright của TASK-SCRAPE-003 (tái dùng fingerprint/TLS/behavior). Proxy lấy qua <code>SelectTier(DiffByteDance)</code> của TASK-SCRAPE-004. Outcome báo về monitor TASK-SCRAPE-006.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-SCRAPE-003</strong> - farm Playwright (<code>RenderPrice</code>, fingerprint, behavior, ChallengedError).</li><li><strong>TASK-SCRAPE-004</strong> - proxy tier enterprise cho TikTok (DiffByteDance).</li><li><strong>TASK-SCRAPE-001</strong> - interface adapter; orchestrator điều phối + gọi sink.</li><li><strong>TASK-SCRAPE-005</strong> - pacing + xử lý ChallengedError; ghi qua delta-only.</li><li><strong>TASK-SCRAPE-006</strong> - monitor nhận outcome (SPA drift cần giám sát sát).</li><li><strong>TASK-PRICE-002 (downstream)</strong> - snapshot ghi qua InsertSnapshot delta-only.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>State JSON nhúng (rút gọn, ẩn danh)</h3>\n<pre><code class=\"language-json\">{\n  \"product\": { \"price\": 89000, \"originPrice\": 149000, \"flash\": true, \"stock\": 52 }\n}</code></pre>\n<h3>Snapshot adapter trả về</h3>\n<pre><code class=\"language-typescript\">{\n  productId: 77231,\n  ts: nowISO,\n  price: 89000,            // VND số nguyên\n  listPrice: 149000,\n  flashSale: true,\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Hình dạng state JSON chính xác của TikTok Shop VN - xác minh trên fixture thật; <code>pickPriceFromState</code> tập trung một chỗ để chỉnh.</li><li>Đơn vị giá (VND nguyên hay micro) - hằng <code>VND_UNIT</code> một chỗ, khóa bằng test.</li><li>Live-commerce (giá trong livestream động theo phút) - tier hot + re-tier xử lý tần suất; đọc giá live cụ thể để sau.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Cố ký API msToken/X-Bogus</td><td>grep request</td><td>giòn + leo thang</td><td>Đọc DOM-render, né API ký (§1 #2)</td></tr><tr><td>Proxy budget cho TikTok</td><td>tỷ lệ block</td><td>ban (risk High)</td><td>Enterprise tier (§1 #3)</td></tr><tr><td>Đọc DOM lúc chưa hydrate</td><td>parse trang trống</td><td>parse_fail giả</td><td>Chờ readyAnchor (§1 #9)</td></tr><tr><td>State JSON đổi shape</td><td><code>tiktok_extract_source</code> lệch</td><td>json fail</td><td>Fallback DOM + monitor (§1 #4,#8)</td></tr><tr><td>Float trong quy đổi</td><td>extract test</td><td>sai số tiền</td><td>parseVNDInt số nguyên (§1 #12)</td></tr><tr><td>Challenge coi như \"không giá\"</td><td>ChallengedError</td><td>dữ liệu bẩn</td><td>Ném lỗi, chuyển TASK-SCRAPE-005 (§1 #7)</td></tr><tr><td>Đọc tức thì như bot</td><td>behavior spy</td><td>dấu vết</td><td>humanize trước trích (§1 #6)</td></tr><tr><td>SPA đổi cấu trúc</td><td>parse_fail spike</td><td>dữ liệu chết</td><td>Monitor drift sát (§1 #8)</td></tr><tr><td>Lưu trang thô</td><td>review code</td><td>rủi ro dữ liệu</td><td>Chỉ trích trường giá (§1 #10)</td></tr><tr><td>Rò page farm</td><td>mem profiler</td><td>RAM phình</td><td>Đóng page finally (§1 #11)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>TikTok Shop (41,31% GMV) là sàn tăng nhanh nhất; bỏ qua là bỏ một nửa thị trường live-commerce.</li><li>Đường an toàn là đọc DOM-render qua farm, né API ký - để chính TikTok tự tạo chữ ký hợp lệ, ta chỉ đọc kết quả, tránh cuộc đua vũ trang với ByteDance.</li><li>Proxy enterprise là bắt buộc cho rủi ro ban High (§3.9) - đây là sàn không tha IP chất lượng thấp.</li><li>JSON nhúng ưu tiên hơn DOM text vì ổn định hơn trước A/B layout; fallback DOM giữ độ phủ.</li><li>TikTok là SPA dễ đổi cấu trúc -&gt; giám sát drift (TASK-SCRAPE-006) quan trọng hơn so với adapter JSON-API; báo outcome đều.</li></ul>\n<hr>\n<p><em>Hết TASK-SCRAPE-007. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
