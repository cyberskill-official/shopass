window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-INFRA-006"]="<h1>TASK-INFRA-006 — Wire gateway into local compose</h1>\n<h2>§1 — Normative</h2>\n<ol><li><strong>MUST</strong> add <code>redis</code> + <code>gateway</code> to <a href=\"../../../deploy/docker-compose.yml\"><code>deploy/docker-compose.yml</code></a> (dev stack), mirroring production upstream env wiring from <a href=\"../../../deploy/docker-compose.production.yml\"><code>deploy/docker-compose.production.yml</code></a>.</li><li><strong>MUST</strong> publish host ports only for <code>gateway</code>, <code>web</code>, and optionally <code>db</code> (loopback-bound). <strong>MUST NOT</strong> publish <code>pricesvc</code>, <code>dealsvc</code>, <code>authsvc</code>, <code>tracksvc</code>, <code>billsvc</code>, <code>bff</code>, or <code>notifsvc</code> to the host.</li><li><strong>MUST</strong> set <code>GATEWAY_INTERNAL_BASE_URL=http://gateway:8080</code> on <code>web</code> so Next.js auth handlers call the gateway.</li><li><strong>MUST</strong> keep internal service-to-service URLs on the compose network (unchanged).</li><li><strong>MUST</strong> update local helpers that previously hit host service ports (e.g. <code>simulate-prices</code>) to reach <code>pricesvc</code> on the compose network.</li><li>Forged <code>X-User-Id</code> on gateway without JWT <strong>MUST</strong> yield <strong>401</strong> (existing gateway unit coverage + compose smoke).</li></ol>\n<h2>§2 — Acceptance</h2>\n<ul><li><code>docker compose -f deploy/docker-compose.yml config</code> shows no host ports on private services.</li><li><code>curl -H 'X-User-Id: 1' http://127.0.0.1:${GATEWAY_PORT}/v1/alerts</code> → 401 without Bearer.</li><li><code>make up</code> brings gateway healthy; <code>make simulate-prices</code> still writes snapshots.</li></ul>\n<h2>§3 — Out of scope</h2>\n<p>Production Caddy/TLS (R11), CSRF (R6), Vault <code>_FILE</code> migration, Google OAuth.</p>";
