window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CART-004"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service CART <strong>MUST</strong> hiện thực engine luật stacking per-country (interface <code>StackRules</code> của TASK-CART-003) đọc <code>CountryPolicy</code> của TASK-INFRA-005: VN cho stack 1 shop + 1 platform + freeship; MY/PH (2025) bỏ stacking đa nhóm và gộp freeship vào nhóm platform; nước chưa cấu hình mặc định no-stack. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> hiện thực <code>StackRules.ValidStack(pv, fs, shopVouchers)</code> (interface TASK-CART-003) cho từng chính sách nước, đọc cấu hình từ <code>CountryPolicy</code> (TASK-INFRA-005) - KHÔNG hardcode <code>if country == ...</code> trong optimizer lõi (DEC-CART-19).</li><li>Chính sách VN <strong>MUST</strong> cho stack đồng thời (DEC-CART-20): tối đa 1 voucher shop cho mỗi shop + 1 voucher platform + 1 voucher freeship. <code>ValidStack</code> trả <code>true</code> cho tổ hợp này khi các voucher khác <code>stack_group</code>.</li><li>Chính sách MY/PH (2025) <strong>MUST</strong> bỏ stacking đa nhóm (DEC-CART-21): tối đa 1 voucher shop + 1 voucher platform; freeship (và cashback) GỘP vào nhóm platform nên loại trừ với voucher platform - không được dùng platform voucher VÀ freeship cùng lúc. Optimizer chỉ chọn <code>max(platform, freeship)</code> cộng voucher shop.</li><li>Việc gộp nhóm <strong>MUST</strong> thể hiện qua <code>stack_group</code> (DEC-CART-22, TASK-CART-001 DEC-CART-04): voucher cùng <code>stack_group</code> loại trừ lẫn nhau. Với MY/PH, freeship mang cùng <code>stack_group</code> với platform; với VN, freeship khác nhóm (stack được). <code>ValidStack</code> từ chối tổ hợp có hai voucher cùng <code>stack_group</code>.</li><li>Engine <strong>MUST</strong> đọc cờ từ <code>CountryPolicy</code>: tối thiểu <code>voucher_stacking_allowed</code> (TASK-INFRA-005 DEC-INFRA-22) và quy tắc gộp freeship-platform; KHÔNG nhúng luật cứng tách rời cấu hình nước.</li><li><code>factory</code> <strong>MUST</strong> chọn <code>StackRules</code> đúng theo country của thao tác (suy từ <code>platform.country</code>, TASK-INFRA-005 DEC-INFRA-25): VN -&gt; VN rules; MY/PH -&gt; MY/PH rules; nước khác -&gt; theo <code>CountryPolicy</code> của nước đó.</li><li>Nước chưa cấu hình <code>CountryPolicy</code> <strong>MUST</strong> mặc định policy hạn chế nhất (no-stack) (DEC-CART-23, TASK-INFRA-005 DEC-INFRA-23) - tối đa một voucher mỗi loại, không stack đa nhóm; KHÔNG mặc định cho stack.</li><li><code>ValidStack</code> <strong>MUST</strong> từ chối tổ hợp vượt số voucher cho phép theo policy: VN cho (shop-per-shop + platform + freeship); MY/PH cho (shop-per-shop + một-trong-{platform, freeship}).</li><li>Engine <strong>MUST NOT</strong> tự tính giá trị giảm (đó là việc của TASK-CART-003 <code>discount.go</code>); engine CHỈ quyết tổ hợp nào hợp lệ (<code>ValidStack</code> trả bool). Tách quyết-định-hợp-lệ khỏi tính-giá-trị.</li><li>Engine <strong>MUST</strong> đảm bảo kết quả per-country khớp ví dụ §3.5(3): cùng giỏ + voucher, VN rules cho phép combo ra 110k; MY/PH rules loại tổ hợp platform+freeship đồng thời nên combo tối ưu ra 80k.</li><li>Mọi quyết định <strong>MUST</strong> tất định theo (policy, voucher set) - cùng input cho cùng kết quả <code>ValidStack</code>.</li><li><strong>SHOULD</strong> phát OTel <code>stack_rules_country{country}</code> và <code>stack_rejected_total{country, reason}</code> để theo dõi luật áp theo nước và tổ hợp bị loại.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao luật đọc CountryPolicy, không hardcode (DEC-CART-19)?</strong> Tài liệu nhấn mạnh per-country gating là bắt buộc (§2): luật voucher khác theo nước và đổi theo thời gian (MY/PH bỏ stack 2025). Nếu hardcode <code>if country == \"VN\"</code> rải khắp thì mở nước mới hoặc đổi luật phải sửa code khắp nơi - dễ sai. Đọc <code>CountryPolicy</code> (dữ liệu, TASK-INFRA-005) tách luật khỏi code: thêm nước là thêm cấu hình, không sửa lõi.</p>\n<p><strong>Vì sao VN cho stack, MY/PH không (DEC-CART-20/21)?</strong> Đây là sự thật thị trường: §3.9 ghi Shopee MY/PH 2025 cho tối đa 1 shop + 1 platform, freeship &amp; cashback gộp nhóm platform; VN vẫn cho stack 1 shop + 1 platform + freeship. Áp sai luật là hiển thị con số sai - cao hơn thực tế ở MY/PH (người dùng thất vọng lúc thanh toán) hoặc thấp hơn ở VN (kém đối thủ). Mỗi nước một policy đúng với luật của nó.</p>\n<p><strong>Vì sao gộp nhóm qua stack_group (DEC-CART-22)?</strong> stack_group (TASK-CART-001) là nhãn loại trừ: voucher cùng nhóm không dùng cùng nhau. MY/PH gộp freeship vào nhóm platform = gán cùng stack_group = loại trừ. VN để freeship khác nhóm = stack được. Cùng một cơ chế (stack_group loại trừ) thể hiện cả hai luật chỉ bằng cách gán nhãn khác nhau - không cần logic riêng cho từng nước.</p>\n<p><strong>Vì sao mặc định no-stack cho nước chưa cấu hình (DEC-CART-23)?</strong> Mặc định an toàn (TASK-INFRA-005 DEC-INFRA-23): nếu một nước chưa được kiểm luật, cho stack là rủi ro - hiển thị giảm cao hơn luật thực tế nước đó, sai con số tiền và lệch compliance. No-stack (chỉ một voucher mỗi loại) là phía an toàn: thà hiển thị giảm thấp hơn rồi mở dần khi xác minh luật, còn hơn hứa quá rồi sai.</p>\n<p><strong>Vì sao engine chỉ quyết hợp lệ, không tính giá trị (§1 #9)?</strong> Tách bạch: TASK-CART-003 <code>discount.go</code> tính giá trị giảm (int64, applyCaps); engine này chỉ trả <code>ValidStack</code> bool (tổ hợp này có được phép không). Hai trách nhiệm riêng dễ test riêng và không lẫn. Optimizer ghép: với mỗi tổ hợp hợp lệ (<code>ValidStack</code>), tính giá trị, giữ max.</p>\n<p><strong>Vì sao khớp ví dụ §3.5(3) (§1 #10)?</strong> Tài liệu cho hai con số kiểm chứng: VN 110k, MY/PH 80k trên cùng giỏ. Đây là test vàng cho engine: VN rules phải cho combo ra 110k; MY/PH rules phải loại tổ hợp platform+freeship đồng thời nên combo tối ưu ra 80k. Khớp hai con số này là bằng chứng luật per-country đúng.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Policy rules đọc CountryPolicy (policy_rules.go)</h3>\n<pre><code class=\"language-go\">// services/cart/internal/optimizer/stacking/policy_rules.go\n// PolicyStackRules đọc CountryPolicy (TASK-INFRA-005) thay vì hardcode luật (DEC-CART-19).\ntype PolicyStackRules struct {\n    StackingAllowed       bool // CountryPolicy.voucher_stacking_allowed\n    FreeshipGroupedWithPF bool // freeship gộp nhóm platform (MY/PH 2025)\n}\n\nfunc (r PolicyStackRules) ValidStack(pv *Voucher, fs *Voucher, shopVouchers []Voucher) bool {\n    // một shop tối đa một voucher shop (đã đảm bảo bởi chooseBestShopVoucherPerShop)\n    if !r.StackingAllowed {\n        // no-stack: tối đa một voucher ngoài shop (platform XOR freeship)\n        if pv != nil &amp;&amp; fs != nil {\n            return false\n        }\n    }\n    if r.FreeshipGroupedWithPF &amp;&amp; pv != nil &amp;&amp; fs != nil {\n        return false // MY/PH: freeship gộp nhóm platform -&gt; loại trừ (DEC-CART-21)\n    }\n    return !hasSameStackGroupConflict(pv, fs, shopVouchers) // stack_group loại trừ (DEC-CART-22)\n}</code></pre>\n<h3>VN và MY/PH rules (qua factory)</h3>\n<pre><code class=\"language-go\">// services/cart/internal/optimizer/stacking/vn_rules.go\n// VN: cho stack 1 shop + 1 platform + freeship (DEC-CART-20).\nfunc newVNRules() StackRules {\n    return PolicyStackRules{StackingAllowed: true, FreeshipGroupedWithPF: false}\n}\n\n// services/cart/internal/optimizer/stacking/mypph_rules.go\n// MY/PH 2025: bỏ stack đa nhóm; freeship gộp nhóm platform (DEC-CART-21).\nfunc newMYPHRules() StackRules {\n    return PolicyStackRules{StackingAllowed: true, FreeshipGroupedWithPF: true}\n    // StackingAllowed=true cho phép shop + một-trong-{platform,freeship};\n    // FreeshipGroupedWithPF=true loại trừ platform &amp; freeship dùng cùng lúc.\n}</code></pre>\n<h3>Factory chọn rules theo country (factory.go)</h3>\n<pre><code class=\"language-go\">// services/cart/internal/optimizer/stacking/factory.go\nimport \"shopass/region\" // CountryPolicy (TASK-INFRA-005)\n\n// RulesForCountry chọn StackRules theo policy của nước; mặc định no-stack (DEC-CART-23).\nfunc RulesForCountry(country string, policy region.CountryPolicy) StackRules {\n    switch country {\n    case \"VN\":\n        return newVNRules()\n    case \"MY\", \"PH\":\n        return newMYPHRules()\n    default:\n        // nước khác: đọc CountryPolicy; chưa cấu hình -&gt; hạn chế nhất (no-stack)\n        return PolicyStackRules{\n            StackingAllowed:       policy.VoucherStackingAllowed, // mặc định false nếu chưa set\n            FreeshipGroupedWithPF: !policy.VoucherStackingAllowed,\n        }\n    }\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>ValidStack</code> đọc cờ từ <code>CountryPolicy</code>/policy struct; grep <code>optimizer/stacking/**</code>: KHÔNG có chuỗi country hardcode trong logic <code>ValidStack</code> (factory map country -&gt; rules là chấp nhận; logic luật không if-country).</li><li>VN rules: <code>ValidStack</code> trả <code>true</code> cho tổ hợp (shop-per-shop + platform + freeship) khi khác stack_group.</li><li>MY/PH rules: <code>ValidStack</code> trả <code>false</code> khi cả <code>pv</code> và <code>fs</code> khác nil (platform + freeship đồng thời) - freeship gộp nhóm platform.</li><li>MY/PH rules: <code>ValidStack</code> trả <code>true</code> cho (shop + platform) hoặc (shop + freeship), không cả hai.</li><li>stack_group loại trừ: hai voucher cùng <code>stack_group</code> -&gt; <code>ValidStack</code> trả <code>false</code> (cả VN lẫn MY/PH).</li><li><code>factory.RulesForCountry(\"VN\", ...)</code> trả VN rules; <code>(\"MY\"|\"PH\", ...)</code> trả MY/PH rules.</li><li>Nước chưa cấu hình (policy mặc định) -&gt; no-stack (StackingAllowed false) - <code>ValidStack</code> từ chối platform + freeship đồng thời.</li><li>Ví dụ §3.5(3) đầu-cuối: với optimizer (TASK-CART-003) + VN rules, combo tối ưu ra 110k; với MY/PH rules, combo tối ưu ra 80k (platform+freeship bị loại, chọn max).</li><li>Engine không tính giá trị giảm (chỉ trả bool); grep: không có phép nhân/cộng tiền trong <code>ValidStack</code>.</li><li>Kết quả tất định: cùng (policy, voucher set) cho cùng <code>ValidStack</code>.</li><li><code>go test ./...</code> xanh.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/cart/internal/optimizer/stacking/vn_rules_test.go\nfunc TestVN_AllowsStack_ShopPlatformFreeship(t *testing.T) {\n    r := newVNRules()\n    pv := platV(DiscountAmount, 50_000, ptr(int64(500_000)), \"platform-grp\")\n    fs := freeV(30_000, \"freeship-grp\")  // khác stack_group platform\n    shop := []Voucher{shopV(\"A\", DiscountAmount, 30_000, \"shop-A-grp\")}\n    require.True(t, r.ValidStack(&amp;pv, &amp;fs, shop)) // VN cho stack cả ba (DEC-CART-20)\n}\n\n// Ví dụ §3.5(3) VN -&gt; 110k (qua optimizer TASK-CART-003).\nfunc TestVN_OptimizeExample_110k(t *testing.T) {\n    items := exampleCart() // shop A 300k, shop B 250k\n    v := exampleVouchers() // shop A -30k, platform -50k&gt;=500k, freeship 30k\n    res := optimizeCart(items, v, newVNRules())\n    require.Equal(t, int64(110_000), res.Discount)\n}</code></pre>\n<pre><code class=\"language-go\">// services/cart/internal/optimizer/stacking/mypph_rules_test.go\nfunc TestMYPH_RejectsPlatformPlusFreeship(t *testing.T) {\n    r := newMYPHRules()\n    pv := platV(DiscountAmount, 50_000, ptr(int64(500_000)), \"platform-grp\")\n    fs := freeV(30_000, \"platform-grp\") // MY/PH: freeship cùng nhóm platform\n    shop := []Voucher{shopV(\"A\", DiscountAmount, 30_000, \"shop-A-grp\")}\n    require.False(t, r.ValidStack(&amp;pv, &amp;fs, shop)) // không stack platform + freeship (DEC-CART-21)\n}\n\nfunc TestMYPH_AllowsShopPlusOne(t *testing.T) {\n    r := newMYPHRules()\n    pv := platV(DiscountAmount, 50_000, ptr(int64(500_000)), \"platform-grp\")\n    shop := []Voucher{shopV(\"A\", DiscountAmount, 30_000, \"shop-A-grp\")}\n    require.True(t, r.ValidStack(&amp;pv, nil, shop)) // shop + platform OK\n}\n\n// Ví dụ §3.5(3) MY/PH -&gt; 80k (platform+freeship bị loại, chọn max(50k,30k)+30k shop).\nfunc TestMYPH_OptimizeExample_80k(t *testing.T) {\n    items := exampleCart()\n    v := exampleVouchersMYPH() // freeship gán cùng stack_group platform\n    res := optimizeCart(items, v, newMYPHRules())\n    require.Equal(t, int64(80_000), res.Discount) // max(50k, 30k) + 30k shop = 80k\n}</code></pre>\n<pre><code class=\"language-go\">// services/cart/internal/optimizer/stacking/factory_test.go\nfunc TestFactory_DefaultsToNoStack(t *testing.T) {\n    r := RulesForCountry(\"XX\", region.CountryPolicy{}) // nước chưa cấu hình\n    pv := platV(DiscountAmount, 50_000, nil, \"p\")\n    fs := freeV(30_000, \"f\")\n    require.False(t, r.ValidStack(&amp;pv, &amp;fs, nil)) // mặc định no-stack (DEC-CART-23)\n}\n\nfunc TestFactory_SelectsByCountry(t *testing.T) {\n    require.IsType(t, newVNRules(), RulesForCountry(\"VN\", region.CountryPolicy{}))\n    require.IsType(t, newMYPHRules(), RulesForCountry(\"MY\", region.CountryPolicy{}))\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>policy_rules.go</code> (<code>PolicyStackRules</code> đọc cờ stacking + gộp nhóm) -&gt; <code>vn_rules.go</code> + <code>mypph_rules.go</code> (cấu hình policy cho VN và MY/PH) -&gt; <code>factory.go</code> (<code>RulesForCountry</code> chọn theo country, mặc định no-stack) -&gt; cập nhật handler <code>optimize.go</code> (suy country từ <code>platform.country</code> rồi chọn rules) -&gt; tests. Engine hiện thực interface <code>StackRules</code> của TASK-CART-003 - optimizer lõi gọi <code>ValidStack</code>, không biết luật cụ thể. <code>CountryPolicy</code> đến từ TASK-INFRA-005 (region package); country suy từ <code>platform.country</code> (DEC-INFRA-25). Test vàng là hai ví dụ §3.5(3): VN ra 110k, MY/PH ra 80k - chạy qua optimizer thật (TASK-CART-003) với rules tương ứng.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-CART-003</strong> - định nghĩa interface <code>StackRules</code> mà engine này hiện thực; optimizer lõi gọi <code>ValidStack</code> (depends_on cứng).</li><li><strong>TASK-INFRA-005</strong> - cung cấp <code>CountryPolicy</code> (<code>voucher_stacking_allowed</code>, quy tắc gộp) + cách suy country từ <code>platform.country</code>; engine đọc policy thay vì hardcode (depends_on cứng).</li><li><strong>TASK-CART-001</strong> - <code>stack_group</code> trên voucher là cơ chế thể hiện gộp nhóm/loại trừ mà engine đọc.</li><li><strong>TASK-COMPLY-006 (đồng hướng, P3)</strong> - khung per-country gating tổng quát; luật stacking là một mặt của gating này.</li><li>Extension/lib: thuần Go; đọc <code>region.CountryPolicy</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Cùng giỏ, khác nước -&gt; khác combo (qua /v1/cart/optimize)</h3>\n<p>VN (platform.country = VN):</p>\n<pre><code class=\"language-json\">{ \"discount\": 110000, \"breakdown\": { \"shop\": 30000, \"platform\": 50000, \"freeship\": 30000 } }</code></pre>\n<p>MY (platform.country = MY, freeship gộp nhóm platform):</p>\n<pre><code class=\"language-json\">{ \"discount\": 80000, \"breakdown\": { \"shop\": 30000, \"platform\": 50000, \"freeship\": 0 } }</code></pre>\n<h3>CountryPolicy đọc từ TASK-INFRA-005 (minh họa)</h3>\n<pre><code class=\"language-yaml\"># region/config/countries.yaml (TASK-INFRA-005)\nVN: { voucher_stacking_allowed: true,  freeship_grouped_with_platform: false }\nMY: { voucher_stacking_allowed: true,  freeship_grouped_with_platform: true }\nPH: { voucher_stacking_allowed: true,  freeship_grouped_with_platform: true }\n# nước chưa liệt kê -&gt; mặc định no-stack (DEC-CART-23)</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Luật stacking các nước SEA còn lại (ID/TH/SG/TW) - thêm policy khi mở từng nước (TASK-COMPLY-007), mặc định no-stack tới khi xác minh.</li><li>Cashback gộp nhóm (MY/PH gộp cả cashback nhóm platform) - mô hình khi có task cashback (TASK-AFFIL-005).</li><li>Luật giới hạn số voucher shop trên một đơn nếu sàn thay đổi - thêm cờ vào CountryPolicy.</li><li>Theo dõi thay đổi luật theo thời gian (versioned policy) - bám config versioned của TASK-INFRA-005.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Hardcode if-country trong optimizer</td><td>grep + factory</td><td>mở nước phải sửa lõi</td><td>Đọc CountryPolicy (DEC-CART-19)</td></tr><tr><td>Áp luật VN cho MY/PH</td><td>mypph test 80k</td><td>hiển thị 110k sai thực tế</td><td>MY/PH rules riêng (DEC-CART-21)</td></tr><tr><td>MY/PH stack platform + freeship</td><td>RejectsPlatformPlusFreeship</td><td>con số cao hơn luật 2025</td><td>Loại trừ qua FreeshipGroupedWithPF</td></tr><tr><td>Mặc định cho stack nước mới</td><td>factory default test</td><td>giảm cao hơn luật thực tế</td><td>Mặc định no-stack (DEC-CART-23)</td></tr><tr><td>stack_group không loại trừ</td><td>same-group test</td><td>combo sai</td><td>hasSameStackGroupConflict (DEC-CART-22)</td></tr><tr><td>Engine tự tính tiền</td><td>grep nhân/cộng</td><td>lẫn trách nhiệm</td><td>Chỉ trả bool (§1 #9)</td></tr><tr><td>Kết quả không tất định</td><td>determinism</td><td>test/UX bất ổn</td><td>Tất định theo policy (§1 #11)</td></tr><tr><td>Sai country suy ra</td><td>review factory</td><td>áp nhầm luật</td><td>Suy từ platform.country (DEC-INFRA-25)</td></tr><tr><td>Áp no-stack cho VN</td><td>vn test 110k</td><td>kém đối thủ</td><td>VN rules cho stack (DEC-CART-20)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Luật stacking per-country là gating bắt buộc để mở SEA đúng con số: VN cho stack (110k), MY/PH 2025 bỏ stack (80k) - §3.5(3), §5.7, §3.9.</li><li>Engine đọc CountryPolicy (TASK-INFRA-005) thay vì hardcode - thêm nước là thêm cấu hình, không sửa lõi optimizer.</li><li>Gộp nhóm thể hiện qua stack_group (cùng cơ chế loại trừ): MY/PH gán freeship cùng nhóm platform; VN để khác nhóm.</li><li>Mặc định no-stack cho nước chưa cấu hình là phía an toàn - thà giảm thấp hơn rồi mở dần khi xác minh luật.</li><li>Engine chỉ quyết tổ hợp hợp lệ (bool); tính giá trị giảm là việc của TASK-CART-003 - hai trách nhiệm tách riêng.</li><li>Test vàng là hai con số §3.5(3): VN 110k, MY/PH 80k qua optimizer thật - bằng chứng luật per-country đúng.</li></ul>\n<hr>\n<p><em>Hết TASK-CART-004. Status: done (mục tiêu audit 10/10).</em></p>";
