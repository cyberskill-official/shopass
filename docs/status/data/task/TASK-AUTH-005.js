window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AUTH-005"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service AUTH <strong>MUST</strong> quản vòng đời tài khoản: reset mật khẩu an toàn, chuyển trạng thái có quy tắc, và xóa tài khoản thỏa DSAR PDPL, đảm bảo token bị thu hồi đúng lúc. Hợp đồng:</p>\n<ol><li><code>app_user.status</code> <strong>MUST</strong> - {<code>active</code>, <code>suspended</code>, <code>deleted</code>} (DEC-AUTH-21). Chỉ <code>active</code> mới đăng nhập được; <code>suspended</code> và <code>deleted</code> bị chặn ở đường đăng nhập (sửa <code>login.go</code>).</li><li>Reset mật khẩu <strong>MUST</strong> dùng token một-lần, hết hạn ngắn (ví dụ 30 phút), gửi qua kênh đã verify (email/phone) qua TASK-NOTIF-001 (DEC-AUTH-22). Token lưu dạng hash trong bảng <code>password_reset</code>.</li><li><code>RequestReset(identifier)</code> <strong>MUST</strong> trả phản hồi ĐỒNG NHẤT bất kể tài khoản tồn tại hay không (DEC-AUTH-22): KHÔNG lộ \"email này có tài khoản\" (chống user enumeration).</li><li><code>ConfirmReset(token, newPassword)</code> <strong>MUST</strong> kiểm token hợp lệ, chưa dùng, chưa hết hạn; đặt mật khẩu mới (băm argon2id qua TASK-AUTH-001); đánh dấu token đã dùng; và <strong>MUST</strong> thu hồi mọi refresh token hiện hữu của user (đổi mật khẩu = đăng xuất mọi phiên).</li><li><code>SetStatus(userID, status)</code> <strong>MUST</strong> chuyển trạng thái theo quy tắc và <strong>MUST</strong> thu hồi mọi refresh token của user khi chuyển sang <code>suspended</code> hoặc <code>deleted</code> (DEC-AUTH-24).</li><li>Đăng nhập <strong>MUST</strong> từ chối khi <code>status != 'active'</code> với lỗi xác định (ví dụ <code>ErrAccountNotActive</code>), KHÔNG tiết lộ chi tiết nội bộ.</li><li><code>DeleteAccount(userID)</code> (DSAR) <strong>MUST</strong> (DEC-AUTH-23): xóa hoặc ẩn danh hóa dữ liệu cá nhân của <code>app_user</code> (email, phone, display_name); gỡ mọi <code>platform_account</code> (TASK-AUTH-003); thu hồi mọi refresh token; đặt <code>status='deleted'</code>.</li><li>Xóa <strong>MUST</strong> dứt khoát theo nghĩa PDPL nhưng <strong>SHOULD</strong> có cửa sổ ân hạn ngắn (ví dụ 7-30 ngày) trước khi purge cứng, chống xóa nhầm (DEC-AUTH-25); trong ân hạn tài khoản đã ở <code>deleted</code> và không đăng nhập được. 8b. <code>DeleteAccount</code> <strong>MUST</strong> phối với TASK-COMPLY-003 (DSAR): là điểm thực thi quyền xóa; đảm bảo dữ liệu cá nhân ở các bảng liên quan cũng được xử lý theo chính sách lưu trữ.</li><li>Sau xóa, dữ liệu cá nhân trực tiếp của <code>app_user</code> <strong>MUST</strong> không còn truy được ở dạng định danh (email/phone bị nullify hoặc thay bằng tombstone ẩn danh).</li><li>Reset và xóa <strong>MUST</strong> KHÔNG log token reset thô hay PII; thao tác nhạy cảm ghi audit (ai/khi/loại) nhưng không kèm dữ liệu nhạy cảm thô.</li><li>Mọi chuyển trạng thái và xóa <strong>SHOULD</strong> phát sự kiện/metric (<code>account_status_changed_total{to}</code>, <code>account_deleted_total</code>) cho observability và đối chiếu DSAR.</li><li><code>ConfirmReset</code> và <code>DeleteAccount</code> <strong>MUST</strong> idempotent ở mức an toàn: token reset đã dùng không dùng lại được; xóa tài khoản đã xóa không gây lỗi nghiêm trọng.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p>Vì sao reset không lộ tài khoản tồn tại (DEC-AUTH-22, §1 #3)? Nếu <code>RequestReset(\"a@x.com\")</code> trả \"đã gửi\" còn <code>RequestReset(\"b@x.com\")</code> trả \"không có tài khoản\", kẻ tấn công dò được email nào đã đăng ký (user enumeration) - bước đầu của tấn công có mục tiêu. Phản hồi đồng nhất (\"nếu tài khoản tồn tại, ta đã gửi hướng dẫn\") che thông tin đó.</p>\n<p>Vì sao token reset một-lần, hết hạn ngắn (§1 #2)? Link reset đi qua email/SMS - kênh có thể bị lưu, chuyển tiếp, hay lộ. Token sống lâu hoặc dùng nhiều lần là cửa sổ rộng để lạm dụng. Một-lần + hết hạn ngắn thu hẹp cửa sổ tới mức một lần dùng hợp lệ của chủ thật.</p>\n<p>Vì sao đổi mật khẩu thu hồi mọi phiên (§1 #4)? Người ta đổi mật khẩu thường vì nghi bị lộ. Nếu các phiên (refresh token) cũ vẫn sống, kẻ chiếm phiên vẫn vào được dù mật khẩu đã đổi - phản tác dụng của việc reset. Thu hồi mọi refresh khi đổi mật khẩu đảm bảo reset thật sự cắt truy cập cũ.</p>\n<p>Vì sao suspend/delete phải thu hồi token (DEC-AUTH-24)? Status là cờ ở DB; nhưng access token tự chứng thực (gateway không truy DB mỗi request). Nếu chỉ đổi status mà không thu hồi refresh, người bị khóa vẫn refresh ra access mới và dùng tiếp. Thu hồi refresh khi đổi status (cộng access TTL ngắn của TASK-AUTH-002) làm việc khóa có hiệu lực thực.</p>\n<p>Vì sao xóa có cửa sổ ân hạn (DEC-AUTH-25)? PDPL đòi quyền xóa, nhưng xóa nhầm/bị lừa xóa là rủi ro thật. Một cửa sổ ân hạn ngắn (tài khoản đã ở <code>deleted</code>, không đăng nhập được, nhưng chưa purge cứng) cân bằng: quyền xóa được tôn trọng ngay (không còn dùng được, PII đã ẩn danh ở bước đầu) mà vẫn có lối khôi phục nếu là nhầm lẫn, trước khi xóa vật lý.</p>\n<p>Vì sao ẩn danh hóa thay vì chỉ đặt cờ (§1 #7, #9)? Đặt <code>status='deleted'</code> mà giữ nguyên email/phone không thỏa quyền xóa PDPL - dữ liệu cá nhân vẫn nằm đó. Phải nullify hoặc thay bằng tombstone ẩn danh để dữ liệu cá nhân trực tiếp thực sự không còn truy được, đúng tinh thần \"xóa\".</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration password_reset</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0008_password_reset.up.sql\nCREATE TABLE password_reset (\n  id          BIGSERIAL   PRIMARY KEY,\n  user_id     BIGINT      NOT NULL REFERENCES app_user(id),\n  token_hash  TEXT        NOT NULL UNIQUE,   -- hash token reset, KHÔNG cleartext\n  expires_at  TIMESTAMPTZ NOT NULL,\n  used_at     TIMESTAMPTZ,                   -- NULL = chưa dùng\n  created_at  TIMESTAMPTZ DEFAULT now()\n);\nCREATE INDEX idx_pr_user ON password_reset (user_id);</code></pre>\n<h3>Reset không lộ tồn tại + một-lần (§1 #2, #3, #4)</h3>\n<pre><code class=\"language-go\">// services/auth/internal/auth/reset.go\nfunc (s *Service) RequestReset(ctx context.Context, identifier string) error {\n    u, ok := s.repo.FindByIdentifier(ctx, identifier)\n    if ok &amp;&amp; u.Status == \"active\" {\n        token := randToken()\n        s.repo.SaveReset(ctx, u.ID, hash(token), time.Now().Add(30*time.Minute))\n        s.notif.SendReset(ctx, u, token) // gửi qua kênh verified (TASK-NOTIF-001)\n    }\n    return nil // LUÔN trả nil — phản hồi đồng nhất (§1 #3), không lộ tồn tại\n}\n\nfunc (s *Service) ConfirmReset(ctx context.Context, token, newPassword string) error {\n    pr, ok := s.repo.FindResetByHash(ctx, hash(token))\n    if !ok || pr.UsedAt != nil || time.Now().After(pr.ExpiresAt) {\n        return ErrInvalidResetToken\n    }\n    if err := checkPasswordStrength(newPassword); err != nil { return err }\n    hash, _ := Hash(newPassword, s.params)\n    s.repo.UpdatePassword(ctx, pr.UserID, hash)\n    s.repo.MarkResetUsed(ctx, pr.ID)           // một-lần (§1 #2)\n    return s.repo.RevokeAllRefresh(ctx, pr.UserID) // đổi mật khẩu = đăng xuất mọi phiên (§1 #4)\n}</code></pre>\n<h3>Lifecycle + erasure (§1 #5, #7, #9)</h3>\n<pre><code class=\"language-go\">// services/auth/internal/auth/lifecycle.go\nfunc (s *Service) SetStatus(ctx context.Context, userID int64, status string) error {\n    if !validStatus(status) { return ErrBadStatus }\n    if err := s.repo.SetStatus(ctx, userID, status); err != nil { return err }\n    if status == \"suspended\" || status == \"deleted\" {\n        return s.repo.RevokeAllRefresh(ctx, userID) // thu hồi token (§1 #5)\n    }\n    return nil\n}\n\n// services/auth/internal/auth/erasure.go\nfunc (s *Service) DeleteAccount(ctx context.Context, userID int64) error {\n    // DSAR PDPL: ẩn danh hóa PII + gỡ liên kết + thu hồi token + đặt deleted (§1 #7,#9).\n    if err := s.repo.AnonymizePII(ctx, userID); err != nil { return err }   // email/phone/display_name -&gt; tombstone\n    if err := s.repo.DeletePlatformAccounts(ctx, userID); err != nil { return err } // TASK-AUTH-003\n    if err := s.repo.RevokeAllRefresh(ctx, userID); err != nil { return err }\n    return s.repo.SetStatus(ctx, userID, \"deleted\") // ân hạn trước purge cứng (§1 #8)\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>RequestReset</code> cho email tồn tại (active) và email không tồn tại -&gt; cùng kiểu phản hồi (không lộ tồn tại).</li><li><code>ConfirmReset</code> với token hợp lệ -&gt; đặt mật khẩu mới; đăng nhập bằng mật khẩu mới thành công.</li><li><code>ConfirmReset</code> dùng lại token đã dùng -&gt; <code>ErrInvalidResetToken</code>.</li><li><code>ConfirmReset</code> với token hết hạn -&gt; <code>ErrInvalidResetToken</code>.</li><li>Sau <code>ConfirmReset</code>, mọi refresh token cũ của user bị thu hồi (refresh cũ không cấp được cặp mới).</li><li><code>SetStatus(u, \"suspended\")</code> -&gt; đăng nhập trả <code>ErrAccountNotActive</code>; refresh token bị thu hồi.</li><li><code>SetStatus(u, \"deleted\")</code> -&gt; đăng nhập bị chặn; refresh thu hồi.</li><li><code>SetStatus(u, \"active\")</code> lại -&gt; đăng nhập được (nếu chưa purge).</li><li><code>DeleteAccount(u)</code> -&gt; <code>app_user.email</code>/<code>phone</code>/<code>display_name</code> bị nullify/tombstone (PII không còn truy ở dạng định danh).</li><li>Sau <code>DeleteAccount</code>, mọi <code>platform_account</code> của u bị gỡ.</li><li>Sau <code>DeleteAccount</code>, refresh token vô hiệu và đăng nhập bị chặn (status <code>deleted</code>).</li><li><code>DeleteAccount</code> gọi lại trên tài khoản đã xóa -&gt; không lỗi nghiêm trọng (idempotent).</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/auth/internal/auth/reset_test.go\nfunc TestRequestReset_NoEnumeration(t *testing.T) {\n    s := newService(t)\n    s.seedUser(t, \"exists@x.com\")\n    err1 := s.RequestReset(ctx, \"exists@x.com\")\n    err2 := s.RequestReset(ctx, \"nope@x.com\")\n    require.NoError(t, err1)\n    require.NoError(t, err2) // cùng phản hồi, không lộ tồn tại\n}\n\nfunc TestConfirmReset_OneTime(t *testing.T) {\n    s := newService(t)\n    tok := s.issueResetFor(t, \"a@x.com\")\n    require.NoError(t, s.ConfirmReset(ctx, tok, \"newp@ss12345\"))\n    require.ErrorIs(t, s.ConfirmReset(ctx, tok, \"againp@ss12\"), ErrInvalidResetToken)\n}\n\nfunc TestConfirmReset_RevokesSessions(t *testing.T) {\n    s := newService(t)\n    uid := s.seedUser(t, \"a@x.com\")\n    pair, _ := s.IssueTokenPair(ctx, uid)\n    tok := s.issueResetFor(t, \"a@x.com\")\n    s.ConfirmReset(ctx, tok, \"newp@ss12345\")\n    _, err := s.Refresh(ctx, pair.Refresh) // refresh cũ vô hiệu\n    require.Error(t, err)\n}\n\n// services/auth/internal/auth/lifecycle_test.go\nfunc TestSuspended_CannotLogin_TokensRevoked(t *testing.T) {\n    s := newService(t)\n    uid := s.seedActiveUser(t, \"a@x.com\", \"p@ss12345\")\n    pair, _ := s.IssueTokenPair(ctx, uid)\n    require.NoError(t, s.SetStatus(ctx, uid, \"suspended\"))\n    _, err := s.Login(ctx, \"a@x.com\", \"p@ss12345\")\n    require.ErrorIs(t, err, ErrAccountNotActive)\n    _, rerr := s.Refresh(ctx, pair.Refresh)\n    require.Error(t, rerr) // refresh thu hồi\n}\n\n// services/auth/internal/auth/erasure_test.go\nfunc TestDelete_ErasesPII_AndLinks(t *testing.T) {\n    s := newService(t)\n    uid := s.seedUserWithLinks(t, \"chi@x.com\")\n    require.NoError(t, s.DeleteAccount(ctx, uid))\n    u := s.repo.rawUser(t, uid)\n    require.NotEqual(t, \"chi@x.com\", u.Email) // email ẩn danh/tombstone\n    require.Empty(t, s.repo.links(t, uid))     // platform_account gỡ\n    require.Equal(t, \"deleted\", u.Status)\n}\n\nfunc TestDelete_Idempotent(t *testing.T) {\n    s := newService(t)\n    uid := s.seedUser(t, \"a@x.com\")\n    require.NoError(t, s.DeleteAccount(ctx, uid))\n    require.NoError(t, s.DeleteAccount(ctx, uid)) // lần hai không lỗi\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Reset token lưu hash, một-lần, TTL ngắn; gửi qua TASK-NOTIF-001. <code>RequestReset</code> luôn trả nil để không lộ tồn tại. <code>ConfirmReset</code> và <code>SetStatus(suspended/deleted)</code> đều gọi <code>RevokeAllRefresh</code> - cùng access TTL ngắn của TASK-AUTH-002 làm khóa/đổi mật khẩu có hiệu lực thực. <code>DeleteAccount</code> ẩn danh hóa PII (tombstone) rồi gỡ liên kết, thu hồi token, đặt <code>deleted</code>; purge cứng chạy sau cửa sổ ân hạn bằng job riêng. Phối TASK-COMPLY-003 để dữ liệu liên quan ở module khác cũng theo chính sách lưu trữ. Audit log thao tác nhạy cảm không kèm token/PII thô.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li>TASK-AUTH-001 - <code>Hash</code> mật khẩu mới (argon2id) khi ConfirmReset; <code>app_user.status</code> cột lõi.</li><li>TASK-AUTH-002 - <code>RevokeAllRefresh</code> dùng cơ chế thu hồi refresh token; access TTL ngắn bổ trợ.</li><li>TASK-AUTH-003 - <code>DeleteAccount</code> gỡ <code>platform_account</code>.</li><li>TASK-COMPLY-003 - DSAR; task này là điểm thực thi quyền xóa.</li><li>TASK-NOTIF-001 - gửi token reset qua kênh verified.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Yêu cầu reset (phản hồi đồng nhất)</h3>\n<pre><code class=\"language-http\">POST /v1/auth/password/reset-request\n{\"identifier\":\"chi@example.com\"}\n-&gt; 200 {\"message\":\"Nếu tài khoản tồn tại, hướng dẫn đặt lại mật khẩu đã được gửi.\"}</code></pre>\n<h3>Xóa tài khoản (DSAR)</h3>\n<pre><code class=\"language-http\">DELETE /v1/account HTTP/1.1\nAuthorization: Bearer &lt;access_jwt&gt;\n-&gt; 200 {\"status\":\"deleted\",\"grace_until\":\"2026-07-27T00:00:00Z\"}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Tự khôi phục trong cửa sổ ân hạn (UI \"hoàn tác xóa\") - trải nghiệm sau.</li><li>Xuất dữ liệu (data portability) trước khi xóa - phối TASK-COMPLY-003 ở slice DSAR đầy đủ.</li><li>Suspend tạm có thời hạn tự hết (ví dụ khóa 24h chống abuse) - gắn TASK-TRUST-004.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Reset lộ tài khoản tồn tại</td><td>no-enumeration test</td><td>user enumeration</td><td>Phản hồi đồng nhất (§1 #3)</td></tr><tr><td>Token reset dùng nhiều lần</td><td>one-time test</td><td>cửa sổ chiếm tài khoản</td><td>Một-lần + hết hạn (§1 #2)</td></tr><tr><td>Đổi mật khẩu không cắt phiên cũ</td><td>revoke test</td><td>kẻ chiếm phiên vẫn vào</td><td>Thu hồi mọi refresh (§1 #4)</td></tr><tr><td>Suspend/delete vẫn dùng phiên</td><td>lifecycle test</td><td>khóa không hiệu lực</td><td>Thu hồi refresh + access TTL ngắn (§1 #5)</td></tr><tr><td>Xóa chỉ đặt cờ, giữ PII</td><td>erasure test</td><td>không thỏa quyền xóa PDPL</td><td>Ẩn danh hóa PII (§1 #7,#9)</td></tr><tr><td>Xóa nhầm/bị lừa xóa</td><td>grace window</td><td>mất tài khoản</td><td>Cửa sổ ân hạn trước purge (§1 #8)</td></tr><tr><td>Log token reset/PII</td><td>review</td><td>rò rỉ</td><td>Cấm log nhạy cảm (§1 #10)</td></tr><tr><td>Xóa bỏ sót platform_account</td><td>erasure test</td><td>liên kết sàn còn lại</td><td>Gỡ platform_account (§1 #7)</td></tr><tr><td>Xóa lặp gây lỗi</td><td>idempotent test</td><td>UX/job gãy</td><td>Idempotent (§1 #12)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>PDPL trao quyền xóa; <code>DeleteAccount</code> là điểm thực thi, ẩn danh hóa PII thật sự thay vì chỉ đặt cờ.</li><li>Phản hồi reset đồng nhất che user enumeration - một rò rỉ tinh vi nếu thông báo khác nhau theo tồn tại.</li><li>Đổi mật khẩu thu hồi mọi phiên là điều người dùng kỳ vọng khi nghi bị lộ; giữ phiên cũ làm reset vô nghĩa.</li><li>Status là cờ DB nhưng access token tự chứng thực - phải thu hồi refresh khi suspend/delete để khóa có hiệu lực thực (cùng access TTL ngắn TASK-AUTH-002).</li><li>Cửa sổ ân hạn cân bằng quyền xóa PDPL với rủi ro xóa nhầm: không dùng được ngay, PII ẩn danh ngay, nhưng còn lối khôi phục trước purge cứng.</li><li>Phối TASK-COMPLY-003 để xóa lan tới dữ liệu cá nhân ở module khác theo chính sách lưu trữ, không chỉ trong <code>app_user</code>.</li></ul>\n<hr>\n<p><em>Hết TASK-AUTH-005. Status: done (mục tiêu audit 10/10).</em></p>";
