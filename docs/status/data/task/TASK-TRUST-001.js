window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-TRUST-001"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Extension SănDeal <strong>MUST</strong> được công khai mã nguồn, build tái lập được (cùng commit cho cùng bundle), và kèm disclosure rõ ràng trên Chrome Web Store khẳng định KHÔNG gửi cookie/mật khẩu/token phiên về server. Hợp đồng:</p>\n<ol><li>Mã nguồn extension <strong>MUST</strong> công khai trên một repo (GitHub) dưới giấy phép mã nguồn mở; mọi bundle ship lên Chrome Web Store <strong>MUST</strong> tương ứng với một commit/tag công khai trong repo đó (DEC-TRUST-01). Không ship bản không truy nguyên được về source công khai.</li><li>Build <strong>MUST</strong> tái lập (deterministic, DEC-TRUST-02): cùng một commit phải cho ra bundle giống nhau bit-by-bit. Cụ thể: sắp xếp entry ổn định, đặt <code>SOURCE_DATE_EPOCH</code> (bỏ timestamp), loại path tuyệt đối khỏi bundle, khóa chặt dependency (lockfile).</li><li>Toolchain build <strong>MUST</strong> được pin (phiên bản node, bundler, lockfile dependency) để bên thứ ba build lại ra cùng kết quả (DEC-TRUST-04).</li><li><strong>MUST</strong> cung cấp <code>verify-reproducible.sh</code>: từ một commit, build lại trong môi trường sạch và so SHA-256 với artifact đã ship; lệch hash <strong>MUST</strong> làm script exit khác 0.</li><li>CI publish gate (<code>reproducible-publish-gate.yml</code>) <strong>MUST</strong> chặn publish nếu (a) <code>verify-reproducible.sh</code> fail, hoặc (b) bundle không có tag công khai tương ứng, hoặc (c) listing data-use không nhất quán với pipeline (§1 #8).</li><li><code>DISCLOSURE.md</code> <strong>MUST</strong> liệt kê CHÍNH XÁC tập dữ liệu extension thu thập và gửi đi - đúng tập tối thiểu <code>{platform, productId, price, qty}</code> (+ voucher hiển thị) của TASK-EXT-003 - và <strong>MUST</strong> khẳng định tường minh KHÔNG gửi cookie, mật khẩu, hay token phiên sàn về server (DEC-TRUST-03).</li><li>Mục data-use trên Chrome Web Store listing (<code>chrome-web-store-listing.md</code>) <strong>MUST</strong> nhất quán với <code>DISCLOSURE.md</code> và với allowlist thực tế của TASK-EXT-003: không khai gì pipeline không làm, không làm gì không khai (DEC-TRUST-05).</li><li><strong>MUST</strong> có test <code>disclosure-consistency.test.ts</code> khẳng định danh sách trường khai trong DISCLOSURE/listing khớp đúng allowlist <code>OutboundPayload</code> của TASK-EXT-003. Nếu pipeline thêm/bớt trường mà disclosure không cập nhật -&gt; test fail (chống \"khai một đằng làm một nẻo\").</li><li><code>manifest.json</code> <strong>MUST</strong> có <code>homepage_url</code> trỏ về repo công khai, và <code>version</code> <strong>MUST</strong> khớp git tag của bản ship (truy nguyên hai chiều store &lt;-&gt; source).</li><li>Disclosure <strong>MUST</strong> nêu lý do từng quyền (<code>host_permissions</code> per-domain, <code>storage</code>, <code>alarms</code> của TASK-EXT-001) bằng ngôn ngữ người dùng hiểu - vì sao extension cần quyền đó, dùng làm gì.</li><li><strong>MUST NOT</strong> chứa mã bị làm rối (obfuscation) hay bundle tối nghĩa cố ý cản kiểm tra; mã ship phải đọc được/map được về source công khai (điều kiện để \"mã nguồn mở\" có ý nghĩa).</li><li><strong>SHOULD</strong> publish hash SHA-256 của mỗi bản release công khai (trong repo release notes) để người dùng kỹ thuật tự đối chiếu với bản tải từ store.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao mã nguồn mở (DEC-TRUST-01)?</strong> Extension đọc cookie phiên sàn của chính user (§3.2) - đây là hành vi nhạy cảm dễ bị nghi scam. Cách duy nhất để một bên ngoài kiểm chứng \"extension không lén gửi cookie\" là đọc được mã. Đóng mã thì lời hứa chỉ là lời nói; mở mã biến nó thành thứ kiểm chứng được. Đây là điểm bán niềm tin trung tâm của SănDeal hậu-Honey.</p>\n<p><strong>Vì sao build phải tái lập (DEC-TRUST-02)?</strong> \"Mã nguồn mở\" mà bản ship khác source là vô nghĩa - kẻ xấu có thể công khai source sạch nhưng ship bundle có cửa hậu. Reproducible build đóng kẽ đó: bất kỳ ai build lại từ commit công khai phải ra bundle giống hệt bản trên store (so theo SHA-256). Nếu hai hash khớp, bản ship đúng là source công khai - không hơn không kém.</p>\n<p><strong>Vì sao pin toolchain (DEC-TRUST-04)?</strong> Tái lập chỉ thành công khi mọi đầu vào build cố định: cùng phiên bản node, cùng bundler, cùng dependency (lockfile). Một phiên bản lệch có thể chèn timestamp hay đổi thứ tự byte, làm hash khác dù mã giống. Pin toolchain để bên thứ ba dựng lại đúng môi trường.</p>\n<p><strong>Vì sao disclosure phải khớp pipeline qua test (§1 #8)?</strong> Lời khai data-use dễ trôi khỏi thực tế: pipeline (TASK-EXT-003) đổi allowlist mà disclosure quên cập nhật. Test nối disclosure với allowlist thật làm hai thứ không thể lệch âm thầm - nếu pipeline thêm trường, disclosure buộc phải khai, nếu không CI đỏ. \"Khai đúng những gì làm\" trở thành ràng buộc máy kiểm.</p>\n<p><strong>Vì sao CI publish gate (§1 #5)?</strong> Quy tắc không cưỡng chế thì bị phá qua một lần publish vội. Gate biến \"phải tái lập + phải có tag + phải nhất quán disclosure\" thành điều kiện kỹ thuật để ship: không đạt thì không lên store được. Đây là cách giữ lời hứa qua thời gian, không phụ thuộc kỷ luật thủ công.</p>\n<p><strong>Vì sao cấm obfuscation (§1 #11)?</strong> Mã ship bị làm rối cố ý thì \"mở source\" thành hình thức - người đọc không map được bundle về mã. Yêu cầu mã ship đọc được/map được là điều kiện để kiểm tra độc lập (TASK-TRUST-003) khả thi.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>scripts/build-deterministic.mjs (build tái lập)</h3>\n<pre><code class=\"language-js\">// extension/scripts/build-deterministic.mjs\nimport { build } from \"esbuild\";\n\n// SOURCE_DATE_EPOCH cố định -&gt; bỏ timestamp khỏi artifact\nprocess.env.SOURCE_DATE_EPOCH = process.env.SOURCE_DATE_EPOCH || \"0\";\n\nawait build({\n  entryPoints: [\"src/background/service-worker.ts\", \"src/content/index.ts\"],\n  bundle: true,\n  outdir: \"dist\",\n  minify: true,\n  // tái lập: thứ tự ổn định, không nhúng path tuyệt đối, không nhúng thời gian\n  absWorkingDir: process.cwd(),\n  sourcemap: false,\n  legalComments: \"none\",\n  define: { \"process.env.BUILD_TIME\": '\"\"' },  // KHÔNG nhúng giờ build\n  metafile: false,\n});\n// Hậu xử lý: zip với thứ tự entry sort + mtime cố định (deterministic zip)</code></pre>\n<h3>scripts/verify-reproducible.sh (đối chiếu hash)</h3>\n<pre><code class=\"language-bash\">#!/usr/bin/env bash\n# extension/scripts/verify-reproducible.sh\n# Dùng: verify-reproducible.sh &lt;commit&gt; &lt;shipped-artifact.zip&gt;\nset -euo pipefail\nCOMMIT=\"$1\"; SHIPPED=\"$2\"\nWORK=\"$(mktemp -d)\"\ngit worktree add --detach \"$WORK\" \"$COMMIT\"\n( cd \"$WORK\" &amp;&amp; npm ci &amp;&amp; node scripts/build-deterministic.mjs &amp;&amp; \\\n  ( cd dist &amp;&amp; zip -rX -D -X ../rebuilt.zip . ) )   # zip xác định, mtime cố định\n\nREBUILT_HASH=\"$(shasum -a 256 \"$WORK/rebuilt.zip\" | awk '{print $1}')\"\nSHIPPED_HASH=\"$(shasum -a 256 \"$SHIPPED\" | awk '{print $1}')\"\ngit worktree remove --force \"$WORK\"\n\necho \"rebuilt:  $REBUILT_HASH\"\necho \"shipped:  $SHIPPED_HASH\"\nif [ \"$REBUILT_HASH\" != \"$SHIPPED_HASH\" ]; then\n  echo \"HASH KHÔNG KHỚP - bản ship khác source công khai\" &gt;&amp;2\n  exit 1\nfi\necho \"OK - reproducible: bản ship khớp source @ $COMMIT\"</code></pre>\n<h3>DISCLOSURE.md (trích cấu trúc bắt buộc)</h3>\n<pre><code class=\"language-markdown\"># SănDeal Extension - Disclosure dữ liệu\n\n## Dữ liệu extension GỬI về server (đúng tập tối thiểu)\n- platform: sàn đang xem (shopee | tiktok | lazada)\n- productId: ID sản phẩm công khai\n- price: giá hiển thị (số nguyên VND)\n- qty: số lượng trong giỏ\n- voucher hiển thị (mã + điều kiện hiển thị công khai)\n\n## Dữ liệu extension KHÔNG BAO GIỜ gửi\n- KHÔNG cookie phiên (Shopee/TikTok/Lazada)\n- KHÔNG mật khẩu\n- KHÔNG token phiên sàn / header xác thực\n- KHÔNG email / số điện thoại / tên / địa chỉ\n\n## Vì sao cần từng quyền\n- host_permissions (shopee.vn, tiktok.com, lazada.vn): đọc giá/giỏ trên đúng trang sàn\n- storage: lưu cấu hình + state cục bộ (service worker ephemeral, TASK-EXT-001)\n- alarms: lập lịch quét định kỳ (&gt;=30s) thay cho setInterval\n\nMã nguồn: &lt;repo công khai&gt; · Bản build này: tag &lt;vX.Y.Z&gt;, SHA-256 &lt;hash&gt;</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Repo extension công khai, có giấy phép mã nguồn mở; <code>manifest.json</code> <code>homepage_url</code> trỏ đúng repo.</li><li><code>version</code> trong <code>manifest.json</code> của bản ship khớp git tag tương ứng (truy nguyên store &lt;-&gt; source).</li><li>Chạy <code>build-deterministic.mjs</code> hai lần trên cùng commit -&gt; hai artifact có SHA-256 giống hệt (tái lập nội bộ).</li><li><code>verify-reproducible.sh &lt;commit&gt; &lt;shipped.zip&gt;</code> với artifact ĐÚNG -&gt; in \"OK\", exit 0.</li><li><code>verify-reproducible.sh</code> với artifact đã bị sửa một byte -&gt; in \"HASH KHÔNG KHỚP\", exit khác 0.</li><li><code>DISCLOSURE.md</code> liệt kê đúng <code>{platform, productId, price, qty}</code> (+ voucher) và khẳng định KHÔNG cookie/mật khẩu/token.</li><li><code>disclosure-consistency.test.ts</code>: danh sách trường khai trong DISCLOSURE khớp allowlist <code>OutboundPayload</code> (TASK-EXT-003); thêm/bớt trường mà không cập nhật disclosure -&gt; test đỏ.</li><li>Mục data-use trên <code>chrome-web-store-listing.md</code> nhất quán với <code>DISCLOSURE.md</code> (so trường-với-trường).</li><li>CI <code>reproducible-publish-gate.yml</code> fail khi: hash không khớp, thiếu tag công khai, hoặc disclosure lệch pipeline.</li><li>Grep bundle ship: KHÔNG có path tuyệt đối máy build, KHÔNG có timestamp build nhúng (điều kiện tái lập).</li><li>Bundle ship KHÔNG bị obfuscate cố ý; map được về source công khai.</li><li>Release notes công khai có SHA-256 của bản ship (SHOULD) để user kỹ thuật tự đối chiếu.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-ts\">// extension/test/disclosure-consistency.test.ts\nimport { readFileSync } from \"node:fs\";\nimport { ALLOWED_ITEM_FIELDS, ALLOWED_VOUCHER_FIELDS } from \"../src/pipeline/allowlist\";\n\ntest(\"DISCLOSURE liệt kê đúng trường pipeline thực gửi (không thừa/thiếu)\", () =&gt; {\n  const disc = readFileSync(\"DISCLOSURE.md\", \"utf8\").toLowerCase();\n  for (const f of ALLOWED_ITEM_FIELDS) expect(disc).toContain(f.toLowerCase());\n  // Cấm khai trường NHẠY CẢM (chống \"khai sai để trông an toàn\" hoặc rò ý đồ)\n  for (const banned of [\"cookie\", \"password\", \"mật khẩu\", \"session token\", \"token phiên\"])\n    expect(disc).toContain(\"không\"); // mục \"KHÔNG gửi\" phải tồn tại\n  expect(disc).toMatch(/không.*(cookie)/);\n});\n\ntest(\"trường mới ở allowlist mà DISCLOSURE chưa khai -&gt; fail\", () =&gt; {\n  const disc = readFileSync(\"DISCLOSURE.md\", \"utf8\").toLowerCase();\n  const undisclosed = [...ALLOWED_ITEM_FIELDS, ...ALLOWED_VOUCHER_FIELDS]\n    .filter(f =&gt; !disc.includes(f.toLowerCase()));\n  expect(undisclosed).toEqual([]);  // mọi trường gửi đi phải được khai\n});</code></pre>\n<pre><code class=\"language-bash\"># Kiểm tái lập (chạy trong CI gate)\nnode scripts/build-deterministic.mjs &amp;&amp; cp -r dist dist_a\nnode scripts/build-deterministic.mjs &amp;&amp; cp -r dist dist_b\ndiff -r dist_a dist_b   # PHẢI rỗng: hai lần build giống hệt\n\n# Đối chiếu bản ship\nbash scripts/verify-reproducible.sh \"$(git rev-parse HEAD)\" shipped.zip</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: bật cờ deterministic trong <code>build.mjs</code> + <code>build-deterministic.mjs</code> (loại timestamp/path/thứ tự ngẫu nhiên) -&gt; <code>verify-reproducible.sh</code> (build lại + so hash) -&gt; <code>DISCLOSURE.md</code> + <code>chrome-web-store-listing.md</code> (khai data-use khớp TASK-EXT-003) -&gt; <code>disclosure-consistency.test.ts</code> (nối disclosure với allowlist) -&gt; <code>reproducible-publish-gate.yml</code> (CI chặn publish khi lệch) -&gt; cập nhật <code>manifest.json</code> (homepage_url + version=tag). Quy trình release: tag commit -&gt; CI build deterministic -&gt; verify hash -&gt; publish lên store -&gt; ghi SHA-256 vào release notes. Mọi bản ship đi qua gate này.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-EXT-001</strong> - scaffold MV3 + build.mjs là nền để bật chế độ deterministic; manifest có sẵn để thêm homepage_url/version.</li><li><strong>TASK-EXT-003</strong> - allowlist <code>OutboundPayload</code> là nguồn sự thật cho danh sách trường khai trong DISCLOSURE; test consistency nối hai bên.</li><li><strong>TASK-EXT-006</strong> - UI consent + disclosure lúc cài đặt dùng cùng nội dung DISCLOSURE.md.</li><li><strong>TASK-TRUST-002 (đồng cấp)</strong> - chính sách tối thiểu hóa + local-first; disclosure phản ánh chính sách này.</li><li><strong>TASK-TRUST-003 (downstream)</strong> - audit độc lập dùng reproducible build + disclosure làm bằng chứng kiểm chứng được; TASK-TRUST-001 cung cấp nền hash + source công khai cho audit.</li><li><strong>TASK-COMPLY-005</strong> - cưỡng chế no-cleartext/token-not-on-server; disclosure khẳng định đúng bất biến mà COMPLY-005 kiểm máy.</li><li>Hạ tầng: GitHub repo công khai + giấy phép mở; CI (GitHub Actions) cho publish gate.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Release notes công khai (kèm hash để đối chiếu)</h3>\n<pre><code>SănDeal Extension v1.4.0\nCommit: a1b2c3d (tag v1.4.0, repo công khai)\nArtifact SHA-256: 9f2c...e71a\nTái lập: `bash scripts/verify-reproducible.sh a1b2c3d sandeal-1.4.0.zip` -&gt; OK\nThay đổi: thêm đọc voucher freeship Shopee; KHÔNG đổi tập dữ liệu gửi đi.</code></pre>\n<h3>Mục data-use trên Chrome Web Store listing (trích)</h3>\n<pre><code>Dữ liệu thu thập: thông tin sản phẩm (ID, giá, số lượng) trên trang sàn bạn đang xem.\nKHÔNG thu thập: cookie, mật khẩu, token đăng nhập, thông tin liên hệ.\nMục đích: theo dõi giá, phát hiện sale ảo, tối ưu voucher - xử lý tối thiểu hóa, ưu tiên cục bộ.\nMã nguồn mở + build tái lập: &lt;repo&gt;. Tự kiểm: build lại commit và so hash với bản này.</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Chọn giấy phép mở cụ thể (MIT vs Apache-2.0 vs GPL) - quyết khi mở repo; không ảnh hưởng cơ chế tái lập.</li><li>Reproducible build cho bản Firefox/Edge/Cốc Cốc (đa kênh §5.2) - cùng cơ chế, thêm target khi ship kênh đó.</li><li>Ký số artifact (sigstore/cosign) ngoài SHA-256 - lớp tin cậy bổ sung, xét ở giai đoạn sau nếu cần chứng minh nguồn gốc mạnh hơn.</li><li>Bug bounty công khai cho extension - thuộc chương trình bảo mật rộng hơn, không thuộc task này.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Bản ship khác source công khai</td><td>verify-reproducible hash lệch</td><td>\"open-source\" giả, mất niềm tin</td><td>Gate chặn publish khi hash không khớp (DEC-TRUST-02/04)</td></tr><tr><td>Bundle nhúng timestamp/path tuyệt đối</td><td>diff hai lần build khác nhau</td><td>không tái lập được</td><td>SOURCE_DATE_EPOCH + bỏ path + zip xác định (§1 #2)</td></tr><tr><td>Toolchain lệch phiên bản</td><td>bên thứ ba build ra hash khác</td><td>tái lập thất bại</td><td>Pin node/bundler/lockfile (DEC-TRUST-04)</td></tr><tr><td>Disclosure khai sai/thiếu trường</td><td>disclosure-consistency test đỏ</td><td>\"khai một đằng làm một nẻo\"</td><td>Test nối disclosure với allowlist TASK-EXT-003 (§1 #8)</td></tr><tr><td>Listing store lệch DISCLOSURE.md</td><td>so trường-với-trường (§4 #8)</td><td>review từ chối / hiểu lầm</td><td>Một nguồn sự thật (TASK-EXT-003) cho cả hai</td></tr><tr><td>Ship bundle không có tag công khai</td><td>CI gate kiểm tag</td><td>không truy nguyên được</td><td>Gate yêu cầu tag tương ứng (§1 #5)</td></tr><tr><td>Mã ship bị obfuscate</td><td>review/grep</td><td>kiểm tra độc lập bất khả</td><td>Cấm obfuscation, mã map được (§1 #11)</td></tr><tr><td>Quyền thừa không giải thích</td><td>review disclosure quyền</td><td>nghi ngờ + risk review từ chối</td><td>DISCLOSURE nêu lý do từng quyền (§1 #10)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Reproducible build là thứ làm \"mã nguồn mở\" có nghĩa thật: bất kỳ ai cũng chứng minh được bản ship đúng là source công khai bằng cách so hash.</li><li>Disclosure không phải văn bản marketing - nó được nối với allowlist thực thi (TASK-EXT-003) qua test, nên không thể lệch khỏi hành vi thật của extension.</li><li>Đây là moat niềm tin hậu-Honey (§5.2): nơi Honey lén thay cookie, SănDeal công khai mọi byte và mời người dùng tự kiểm.</li><li>CI publish gate giữ ba bất biến (tái lập + có tag + disclosure khớp) qua thời gian, không phụ thuộc kỷ luật thủ công của người release.</li><li>TASK-TRUST-003 (audit độc lập) đứng trên nền này: hash + source công khai là đầu vào để bên thứ ba xác minh KHÔNG gửi cookie/mật khẩu.</li><li>~45% người tiêu dùng VN lo ngại lừa đảo/lộ dữ liệu (Ken Research) - minh bạch kiểm chứng được là cách trực tiếp nhất để hóa giải nỗi lo đó.</li></ul>\n<hr>\n<p><em>Hết TASK-TRUST-001. Status: done (mục tiêu audit 10/10).</em></p>";
