window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-COMPLY-004"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service COMPLY <strong>MUST</strong> cung cấp quy trình quản lý sự cố vi phạm dữ liệu với đồng hồ 72 giờ và state machine, theo PDPL Luật 91/2025: từ phát hiện qua phân loại đến thông báo cơ quan và (khi cần) thông báo chủ thể dữ liệu. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> định nghĩa bảng <code>breach_incident (id, summary, severity, status, occurred_at, acknowledged_at, triaged_at, notified_authority_at, notified_subjects_at, closed_at, source_ref, created_at)</code> theo dõi toàn bộ vòng đời sự cố.</li><li><strong>MUST</strong> đếm đồng hồ 72 giờ từ <code>acknowledged_at</code> (thời điểm tổ chức nhận biết), KHÔNG từ <code>occurred_at</code> (DEC-COMPLY-15): <code>authority_due_at = acknowledged_at + INTERVAL '72 hours'</code>.</li><li><strong>MUST</strong> vận hành state machine (DEC-COMPLY-16): <code>detected -&gt; triaged -&gt; notified_authority -&gt; notified_subjects -&gt; closed</code>. Chuyển trạng thái phải tuần tự; cấm nhảy bước (ví dụ <code>detected</code> sang <code>notified_authority</code> mà chưa <code>triaged</code>).</li><li><strong>MUST</strong> đặt dấu thời gian cho từng bước khi chuyển: <code>triaged_at</code>, <code>notified_authority_at</code>, <code>notified_subjects_at</code>, <code>closed_at</code> được set đúng lúc bước đó hoàn tất.</li><li><strong>MUST</strong> phân loại mức nghiêm trọng <code>severity IN ('low','medium','high','critical')</code>; phân loại quyết định nghĩa vụ thông báo chủ thể (DEC-COMPLY-17): <code>high</code>/<code>critical</code> BẮT BUỘC qua <code>notified_subjects</code> trước khi <code>closed</code>.</li><li><strong>MUST</strong> suy cờ deadline từ đồng hồ, KHÔNG nhập tay (DEC-COMPLY-17):</li></ol>\n<ul><li><code>within_window</code> - chưa thông báo cơ quan, còn trong 72h.</li><li><code>breach_overdue</code> - chưa <code>notified_authority_at</code> mà đã quá <code>authority_due_at</code>.</li><li><code>notified</code> - đã thông báo cơ quan đúng hạn.</li></ul>\n<ol><li><strong>MUST</strong> liên kết tín hiệu nguồn (DEC-COMPLY-18): <code>source_ref</code> trỏ tới alert/trace của observability spine (TASK-INFRA-004) đã kích hoạt phát hiện, phục vụ điều tra.</li><li><strong>MUST</strong> expose hàm:</li></ol>\n<ul><li><code>Open(ctx, in BreachInput) (int64, error)</code> - tạo sự cố ở <code>detected</code>, set <code>acknowledged_at = now()</code> (bắt đầu đồng hồ).</li><li><code>Advance(ctx, id int64, to Status) error</code> - chuyển trạng thái hợp lệ tuần tự; set dấu thời gian tương ứng.</li><li><code>Close(ctx, id int64) error</code> - đóng; từ chối đóng <code>high</code>/<code>critical</code> chưa <code>notified_subjects</code>.</li><li><code>Overdue(ctx) ([]BreachIncident, error)</code> - liệt kê sự cố <code>breach_overdue</code> (cần báo động ngay).</li></ul>\n<ol><li><strong>MUST</strong> đảm bảo <code>Advance</code> từ chối chuyển không hợp lệ (lùi trạng thái, nhảy bước) bằng lỗi xác định <code>ErrInvalidTransition</code>.</li><li><strong>SHOULD</strong> phát OTel metric: <code>breach_incident_total{severity}</code>, <code>breach_overdue_total</code> (cờ đỏ); báo động khi <code>breach_overdue_total &gt; 0</code>.</li><li><strong>MUST</strong> chặn <code>Close</code> khi severity <code>high</code>/<code>critical</code> mà <code>notified_subjects_at IS NULL</code> bằng lỗi <code>ErrSubjectsNotNotified</code>.</li><li><strong>MUST</strong> ghi log audit mọi chuyển trạng thái (ai, lúc nào, từ đâu đến đâu) để dựng lại dòng thời gian sự cố khi báo cáo cơ quan.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao đồng hồ từ acknowledged_at, không từ occurred_at (DEC-COMPLY-15)?</strong> Vi phạm có thể đã xảy ra âm thầm từ lâu trước khi ta biết. Luật tính 72 giờ từ thời điểm tổ chức \"nhận biết\" (became aware), không từ thời điểm sự cố vật lý xảy ra. Đếm từ <code>acknowledged_at</code> mới đúng mốc luật. Lưu cả <code>occurred_at</code> để điều tra nhưng đồng hồ pháp lý chạy từ lúc nhận biết.</p>\n<p><strong>Vì sao state machine tuần tự (DEC-COMPLY-16)?</strong> Lúc sự cố, áp lực cao, dễ làm tắt hoặc bỏ bước. Ép <code>detected -&gt; triaged -&gt; notified_authority -&gt; notified_subjects -&gt; closed</code> đảm bảo không ai \"đóng\" sự cố mà chưa phân loại hay chưa thông báo. Mỗi bước là một cổng có dấu thời gian, dựng lại được dòng thời gian khi cơ quan hỏi.</p>\n<p><strong>Vì sao phân loại quyết định thông báo chủ thể (DEC-COMPLY-17)?</strong> Không phải vi phạm nào cũng phải báo cho từng user; nhưng vi phạm nghiêm trọng (lộ dữ liệu nhạy cảm) thì có. Gắn nghĩa vụ thông báo chủ thể vào <code>severity</code>: <code>high</code>/<code>critical</code> không được <code>closed</code> nếu chưa <code>notified_subjects</code>. Đây là rào chặn quên thông báo người bị ảnh hưởng.</p>\n<p><strong>Vì sao cờ deadline tự suy (§1 #6)?</strong> Giống DPIA và DSAR, \"quá hạn\" phải khách quan. Tính <code>authority_due_at</code> từ <code>acknowledged_at</code> cho ra <code>breach_overdue</code> tự động. Khi <code>breach_overdue_total &gt; 0</code>, báo động nổ - không để một sự cố trôi qua mốc 72h vì con người quên.</p>\n<p><strong>Vì sao liên kết observability spine (DEC-COMPLY-18)?</strong> Phát hiện vi phạm thường bắt đầu từ một alert (truy cập bất thường, rò log). <code>source_ref</code> trỏ về alert/trace nguồn nối sự cố pháp lý với bằng chứng kỹ thuật, giúp điều tra nhanh và báo cáo có căn cứ. TASK-INFRA-004 là nơi sinh tín hiệu đó.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/comply/migrations/0006_breach_incident.sql\nCREATE TABLE breach_incident (\n  id                    BIGSERIAL   PRIMARY KEY,\n  summary               TEXT        NOT NULL,\n  severity              TEXT        NOT NULL CHECK (severity IN ('low','medium','high','critical')),\n  status                TEXT        NOT NULL DEFAULT 'detected'\n                          CHECK (status IN ('detected','triaged','notified_authority','notified_subjects','closed')),\n  occurred_at           TIMESTAMPTZ,\n  acknowledged_at       TIMESTAMPTZ NOT NULL,   -- dong ho 72h dem tu day\n  triaged_at            TIMESTAMPTZ,\n  notified_authority_at TIMESTAMPTZ,\n  notified_subjects_at  TIMESTAMPTZ,\n  closed_at             TIMESTAMPTZ,\n  source_ref            TEXT,                   -- alert/trace cua TASK-INFRA-004\n  created_at            TIMESTAMPTZ NOT NULL DEFAULT now()\n);\n\nCREATE INDEX idx_breach_open ON breach_incident (status, acknowledged_at)\n  WHERE status &lt;&gt; 'closed';</code></pre>\n<h3>Clock + state machine (Go)</h3>\n<pre><code class=\"language-go\">// services/comply/internal/breach/clock.go\nconst AuthorityWindow = 72 * time.Hour\n\n// DeadlineFlag suy tu dong ho, KHONG nhap tay.\nfunc DeadlineFlag(b BreachIncident, now time.Time) string {\n    due := b.AcknowledgedAt.Add(AuthorityWindow)\n    if b.NotifiedAuthorityAt == nil {\n        if now.After(due) {\n            return \"breach_overdue\"\n        }\n        return \"within_window\"\n    }\n    return \"notified\"\n}\n\n// services/comply/internal/breach/incident.go\nvar order = map[Status]int{\n    \"detected\": 0, \"triaged\": 1, \"notified_authority\": 2,\n    \"notified_subjects\": 3, \"closed\": 4,\n}\n\n// Advance chi cho phep tien dung mot buoc tuan tu.\nfunc (s *Service) Advance(ctx context.Context, id int64, to Status) error {\n    b, err := s.repo.get(ctx, id)\n    if err != nil {\n        return err\n    }\n    if order[to] != order[Status(b.Status)]+1 {\n        return ErrInvalidTransition // cam lui hoac nhay buoc\n    }\n    return s.repo.transition(ctx, id, to, time.Now())\n}\n\n// Close tu choi dong neu nghiem trong ma chua thong bao chu the.\nfunc (s *Service) Close(ctx context.Context, id int64) error {\n    b, _ := s.repo.get(ctx, id)\n    if (b.Severity == \"high\" || b.Severity == \"critical\") &amp;&amp; b.NotifiedSubjectsAt == nil {\n        return ErrSubjectsNotNotified\n    }\n    return s.repo.transition(ctx, id, \"closed\", time.Now())\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch -&gt; <code>breach_incident</code> tồn tại; CHECK severity + status hoạt động.</li><li><code>Open</code> -&gt; sự cố status <code>detected</code>, <code>acknowledged_at = now()</code> (đồng hồ bắt đầu).</li><li><code>Advance(detected-&gt;triaged)</code> -&gt; status <code>triaged</code>, <code>triaged_at</code> được set.</li><li><code>Advance(detected-&gt;notified_authority)</code> (nhảy bước) -&gt; lỗi <code>ErrInvalidTransition</code>.</li><li><code>Advance</code> lùi trạng thái (triaged-&gt;detected) -&gt; lỗi <code>ErrInvalidTransition</code>.</li><li>Sự cố <code>acknowledged_at</code> cách đây 73 giờ, chưa <code>notified_authority_at</code> -&gt; <code>DeadlineFlag = breach_overdue</code>.</li><li>Sự cố trong 72h chưa thông báo -&gt; <code>DeadlineFlag = within_window</code>.</li><li><code>Close</code> sự cố <code>critical</code> chưa <code>notified_subjects</code> -&gt; lỗi <code>ErrSubjectsNotNotified</code>.</li><li><code>Close</code> sự cố <code>low</code> (không cần thông báo chủ thể) -&gt; đóng được, <code>closed_at</code> set.</li><li>Tiến đủ chuỗi <code>high</code> qua <code>notified_subjects</code> rồi <code>Close</code> -&gt; đóng được.</li><li>INSERT <code>severity = 'urgent'</code> -&gt; lỗi CHECK constraint.</li><li><code>Overdue()</code> trả đúng các sự cố <code>breach_overdue</code>; metric <code>breach_overdue_total</code> phản ánh.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/comply/internal/breach/clock_test.go\nfunc TestClock_OverdueAfter72h(t *testing.T) {\n    b := BreachIncident{AcknowledgedAt: now.Add(-73 * time.Hour), NotifiedAuthorityAt: nil}\n    require.Equal(t, \"breach_overdue\", DeadlineFlag(b, now))\n}\n\nfunc TestClock_WithinWindow(t *testing.T) {\n    b := BreachIncident{AcknowledgedAt: now.Add(-10 * time.Hour), NotifiedAuthorityAt: nil}\n    require.Equal(t, \"within_window\", DeadlineFlag(b, now))\n}\n\n// services/comply/internal/breach/incident_test.go\nfunc TestAdvance_SequentialOnly(t *testing.T) {\n    s := setup(t)\n    id, _ := s.Open(ctx, BreachInput{Summary: \"log leak\", Severity: \"high\"})\n    require.NoError(t, s.Advance(ctx, id, \"triaged\"))\n    err := s.Advance(ctx, id, \"notified_subjects\") // nhay buoc (chua notified_authority)\n    require.ErrorIs(t, err, ErrInvalidTransition)\n}\n\nfunc TestAdvance_NoBackward(t *testing.T) {\n    s := setup(t)\n    id, _ := s.Open(ctx, BreachInput{Summary: \"x\", Severity: \"low\"})\n    s.Advance(ctx, id, \"triaged\")\n    err := s.Advance(ctx, id, \"detected\") // lui\n    require.ErrorIs(t, err, ErrInvalidTransition)\n}\n\nfunc TestClose_CriticalNeedsSubjectNotice(t *testing.T) {\n    s := setup(t)\n    id, _ := s.Open(ctx, BreachInput{Summary: \"PII leak\", Severity: \"critical\"})\n    s.Advance(ctx, id, \"triaged\")\n    s.Advance(ctx, id, \"notified_authority\")\n    err := s.Close(ctx, id) // chua notified_subjects\n    require.ErrorIs(t, err, ErrSubjectsNotNotified)\n}\n\nfunc TestClose_LowSeverityNoSubjectNotice(t *testing.T) {\n    s := setup(t)\n    id, _ := s.Open(ctx, BreachInput{Summary: \"minor\", Severity: \"low\"})\n    s.Advance(ctx, id, \"triaged\")\n    s.Advance(ctx, id, \"notified_authority\")\n    s.Advance(ctx, id, \"notified_subjects\")\n    require.NoError(t, s.Close(ctx, id))\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration 0006 -&gt; types.go -&gt; clock.go (đồng hồ thuần hàm) -&gt; incident.go (state machine + ràng buộc Close) -&gt; repo.go -&gt; tests. <code>DeadlineFlag</code> và logic chuyển trạng thái tách thuần hàm để test không cần DB. Khi tích hợp, <code>Open</code> được gọi từ runbook ứng cứu sự cố (thủ công hoặc tự động từ alert TASK-INFRA-004); <code>source_ref</code> mang ID alert/trace nguồn. Báo động <code>breach_overdue_total &gt; 0</code> nối vào kênh on-call.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-INFRA-004</strong> - observability spine sinh alert/trace; <code>source_ref</code> trỏ về đó.</li><li><strong>TASK-COMPLY-001 (liên quan)</strong> - phân loại dữ liệu bị ảnh hưởng tham chiếu danh mục purpose.</li><li><strong>TASK-COMPLY-003 (liên quan)</strong> - sự cố ảnh hưởng quyền chủ thể; thông báo chủ thể có thể trùng tập user của DSAR.</li><li><strong>TASK-NOTIF-001 (liên quan)</strong> - kênh thông báo chủ thể dữ liệu có thể tái dùng hạ tầng notification.</li><li>Lib: driver <code>pgx</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Mở sự cố từ runbook (kèm tham chiếu alert observability)</h3>\n<pre><code class=\"language-json\">POST /v1/comply/breach\n{\n  \"summary\": \"Truy cap bat thuong vao bang price_snapshot tu IP la\",\n  \"severity\": \"high\",\n  \"occurred_at\": \"2026-06-28T01:10:00+07:00\",\n  \"source_ref\": \"otel-trace:7f3a... / grafana-alert:1180\"\n}</code></pre>\n<pre><code class=\"language-json\">201 Created\n{\n  \"id\": 42,\n  \"status\": \"detected\",\n  \"acknowledged_at\": \"2026-06-28T03:00:00+07:00\",\n  \"authority_due_at\": \"2026-07-01T03:00:00+07:00\",\n  \"deadline_flag\": \"within_window\"\n}</code></pre>\n<h3>Báo cáo sự cố sắp quá 72h chưa thông báo cơ quan</h3>\n<pre><code class=\"language-sql\">SELECT id, summary, severity, acknowledged_at\nFROM breach_incident\nWHERE notified_authority_at IS NULL\n  AND now() &gt; acknowledged_at + INTERVAL '72 hours'\nORDER BY acknowledged_at;</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt khung. Hoãn:</p>\n<ul><li>Tự động hóa soạn nội dung thông báo cơ quan/chủ thể (mẫu) - hiện thao tác thủ công theo runbook.</li><li>Tích hợp gửi thông báo chủ thể hàng loạt qua TASK-NOTIF (khi danh sách ảnh hưởng lớn) - nối sau.</li><li>Phân loại tự động severity từ đặc trưng sự cố - hiện do người ứng cứu phân loại.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Đếm 72h sai mốc (từ occurred_at)</td><td>review §1 #2</td><td>sai hạn pháp lý</td><td>Đồng hồ từ <code>acknowledged_at</code>; test clock</td></tr><tr><td>Nhảy bước state machine</td><td><code>ErrInvalidTransition</code> + AC #4</td><td>bỏ phân loại/thông báo</td><td><code>Advance</code> tuần tự một bước</td></tr><tr><td>Lùi trạng thái</td><td><code>ErrInvalidTransition</code> + AC #5</td><td>dòng thời gian sai</td><td>Cấm lùi qua <code>order</code> map</td></tr><tr><td>Đóng sự cố nghiêm trọng chưa báo chủ thể</td><td><code>ErrSubjectsNotNotified</code> + AC #8</td><td>bỏ thông báo người ảnh hưởng</td><td><code>Close</code> chặn high/critical chưa notified_subjects</td></tr><tr><td>Quá 72h chưa báo cơ quan, không ai biết</td><td><code>breach_overdue</code> + AC #6</td><td>vi phạm hạn</td><td>Cờ tự suy + báo động on-call</td></tr><tr><td>severity lạ</td><td>DB CHECK + AC #11</td><td>phân loại sai</td><td>CHECK IN low/medium/high/critical</td></tr><tr><td>Mất liên kết bằng chứng nguồn</td><td><code>source_ref</code> §1 #7</td><td>điều tra khó</td><td>Trỏ alert/trace TASK-INFRA-004</td></tr><tr><td>Không dựng lại được dòng thời gian</td><td>log audit §1 #12</td><td>báo cáo cơ quan thiếu</td><td>Log mọi transition + dấu thời gian từng bước</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Đồng hồ 72 giờ đếm từ <code>acknowledged_at</code> (thời điểm nhận biết) - đúng mốc PDPL, không phải từ lúc sự cố vật lý xảy ra.</li><li>State machine tuần tự + dấu thời gian từng bước giúp dựng lại dòng thời gian sự cố khi báo cáo cơ quan.</li><li>Phân loại severity quyết định nghĩa vụ thông báo chủ thể: high/critical không đóng được nếu chưa thông báo người ảnh hưởng.</li><li>Cờ <code>breach_overdue</code> tự suy từ đồng hồ; báo động khi <code>breach_overdue_total &gt; 0</code> để không bỏ lỡ mốc 72h.</li><li><code>source_ref</code> nối sự cố pháp lý với bằng chứng kỹ thuật từ observability spine (TASK-INFRA-004).</li><li>Khi danh sách chủ thể ảnh hưởng lớn, bước <code>notified_subjects</code> tái dùng hạ tầng TASK-NOTIF (giai đoạn sau).</li></ul>\n<hr>\n<p><em>Hết TASK-COMPLY-004. Status: done (mục tiêu audit 10/10).</em></p>";
