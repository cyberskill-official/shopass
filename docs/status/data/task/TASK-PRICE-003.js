window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PRICE-003"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service PRICE <strong>MUST</strong> expose endpoint REST <code>GET /v1/products/{id}/price-history?range=90d</code> trả chuỗi thời gian giá của đúng một <code>tracked_product</code>, ghép phần thân từ continuous aggregate <code>price_daily</code> với cái đuôi raw từ <code>price_snapshot</code> để biểu đồ tươi tới phút mà vẫn đạt p95 &lt;500ms. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> phục vụ route <code>GET /v1/products/{id}/price-history</code>; <code>{id}</code> là <code>tracked_product.id</code> (int64). Parse lỗi (id không phải số) trả <code>400</code>.</li><li><strong>MUST</strong> chấp nhận query <code>range</code> chỉ trong allowlist <code>{7d, 30d, 90d, 180d, 1y}</code> (DEC-PRICE-31); thiếu <code>range</code> mặc định <code>90d</code>; giá trị ngoài allowlist trả <code>400</code> với thân lỗi <code>{\"error\":\"invalid range\"}</code>. KHÔNG nhận khoảng tùy ý.</li><li><strong>MUST</strong> đọc phần thân của khoảng từ <code>price_daily</code> (DEC-PRICE-30): các điểm ngày <code>{day, min_p, max_p, close_p}</code> từ <code>now() - range</code> tới mốc cắt đuôi.</li><li><strong>MUST</strong> stitch cái đuôi raw: đọc <code>price_snapshot</code> kể từ bucket ngày gần nhất đã đóng (mốc <code>date_trunc('day', now())</code>) tới hiện tại, trả các điểm <code>{ts, price, flash_sale}</code> (DEC-PRICE-30). Đuôi này luôn nằm trong một chunk nóng.</li><li><strong>MUST</strong> trả JSON đúng hình dạng: <code>{product_id, range, daily:[{day, min_p, max_p, close_p}], tail:[{ts, price, flash_sale}]}</code>.</li><li><strong>MUST</strong> trả <code>404</code> khi <code>product_id</code> không tồn tại trong <code>tracked_product</code> (DEC-PRICE-34), phân biệt với <code>200</code> + <code>daily</code>/<code>tail</code> rỗng khi SKU có thật nhưng chưa có snapshot nào.</li><li><strong>MUST</strong> đảm bảo p95 &lt;500ms (§3.8): chỉ chạm <code>price_daily</code> cho phần thân và tối đa một chunk raw cho đuôi (DEC-PRICE-32). KHÔNG quét raw cho toàn khoảng.</li><li><strong>MUST</strong> dùng <code>close_p</code> (last(price) trong ngày) làm giá hiển thị của điểm ngày (DEC-PRICE-33); <code>min_p</code>/<code>max_p</code> chỉ để vẽ dải biên.</li><li><strong>MUST</strong> trả mọi giá dưới dạng <code>BIGINT</code> VND (int64) trong payload - KHÔNG dùng float hay string (đồng nhất với DEC-PRICE-05 của TASK-PRICE-002).</li><li><strong>MUST</strong> xác thực qua JWT do API Gateway (TASK-INFRA-001) gắn; handler giả định đã qua middleware, không tự parse token. Request thiếu auth bị gateway chặn trước khi tới handler.</li><li><strong>SHOULD</strong> phát OTel: <code>price_history_duration_ms</code> (histogram, nhãn <code>range</code>), <code>price_history_requests_total{range, status}</code> (counter) để soi p95 theo từng range.</li><li><strong>MUST</strong> đặt <code>Content-Type: application/json; charset=utf-8</code> và trả <code>daily</code> sắp tăng dần theo <code>day</code>, <code>tail</code> sắp tăng dần theo <code>ts</code>.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao đọc cagg cho thân nhưng stitch raw cho đuôi (DEC-PRICE-30)?</strong> Continuous aggregate <code>price_daily</code> refresh mỗi giờ (TASK-PRICE-002 §1 #6), nên bucket của ngày hôm nay chưa \"đóng\" và chưa phản ánh giá vừa đổi. Nếu chỉ đọc cagg, người dùng thấy giá trễ tới một giờ - tệ nhất đúng lúc flash sale đang chạy. Đọc <code>price_daily</code> cho phần thân (nhanh, bảng nhỏ đã tổng hợp) rồi nối thêm raw <code>price_snapshot</code> kể từ đầu ngày hôm nay cho ta đường giá tươi tới phút mà không phải quét raw cả khoảng.</p>\n<p><strong>Vì sao range allowlist có cap (DEC-PRICE-31)?</strong> Phần đuôi raw chỉ rẻ vì nó nằm gọn trong một chunk nóng. Nếu cho range tùy ý, một request <code>range=99999d</code> ép đọc raw qua hàng trăm chunk (kể cả chunk đã nén), một câu query đủ vỡ p95 và ngốn bộ nhớ. Allowlist <code>{7d,30d,90d,180d,1y}</code> khớp đúng các nút bấm trên UI biểu đồ và chặn cứng bề mặt tấn công.</p>\n<p><strong>Vì sao close_p là giá hiển thị (DEC-PRICE-33)?</strong> Một ngày có nhiều snapshot (đặc biệt khi flash sale). Đường giá biểu đồ cần một giá trị đại diện cho mỗi ngày, và giá đóng cửa (last trong ngày) là cái người dùng nhớ nhất - \"hôm đó nó còn bao nhiêu\". <code>min_p</code>/<code>max_p</code> vẫn trả kèm để vẽ dải biên độ dao động, nhưng đường chính bám <code>close_p</code>.</p>\n<p><strong>Vì sao 404 tách khỏi 200-rỗng (DEC-PRICE-34)?</strong> \"SKU không tồn tại\" và \"SKU mới chưa có snapshot\" là hai trạng thái khác nhau với UI: cái đầu là lỗi định tuyến (đường dẫn sai), cái sau là màn hình \"đang thu thập dữ liệu giá\". Trộn cả hai thành 200-rỗng làm frontend không phân biệt được nên hiển thị gì.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Truy vấn (Go)</h3>\n<pre><code class=\"language-go\">// services/price/internal/price/history_query.go\n\n// rangeWindows là allowlist range -&gt; khoảng thời gian (DEC-PRICE-31).\nvar rangeWindows = map[string]time.Duration{\n    \"7d\":   7 * 24 * time.Hour,\n    \"30d\":  30 * 24 * time.Hour,\n    \"90d\":  90 * 24 * time.Hour,\n    \"180d\": 180 * 24 * time.Hour,\n    \"1y\":   365 * 24 * time.Hour,\n}\n\n// ParseRange validate range, trả khoảng + ok=false nếu ngoài allowlist.\nfunc ParseRange(raw string) (time.Duration, bool) {\n    if raw == \"\" {\n        return rangeWindows[\"90d\"], true // default 90d\n    }\n    d, ok := rangeWindows[raw]\n    return d, ok\n}\n\n// QueryDailyBody đọc phần thân từ price_daily (DEC-PRICE-30).\nfunc (r *Repo) QueryDailyBody(ctx context.Context, productID int64, from time.Time) ([]DailyPoint, error) {\n    rows, err := r.pool.Query(ctx,\n        `SELECT day, min_p, max_p, close_p\n         FROM price_daily\n         WHERE product_id = $1 AND day &gt;= $2\n         ORDER BY day`, productID, from)\n    if err != nil {\n        return nil, err\n    }\n    return scanDaily(rows)\n}\n\n// QueryRawTail đọc raw kể từ đầu ngày hôm nay - tối đa một chunk nóng (DEC-PRICE-32).\nfunc (r *Repo) QueryRawTail(ctx context.Context, productID int64) ([]TailPoint, error) {\n    rows, err := r.pool.Query(ctx,\n        `SELECT ts, price, flash_sale\n         FROM price_snapshot\n         WHERE product_id = $1 AND ts &gt;= date_trunc('day', now())\n         ORDER BY ts`, productID)\n    if err != nil {\n        return nil, err\n    }\n    return scanTail(rows)\n}\n\n// ProductExists phân biệt 404 với 200-rỗng (DEC-PRICE-34).\nfunc (r *Repo) ProductExists(ctx context.Context, productID int64) (bool, error) {\n    var ok bool\n    err := r.pool.QueryRow(ctx,\n        `SELECT EXISTS(SELECT 1 FROM tracked_product WHERE id = $1)`, productID).Scan(&amp;ok)\n    return ok, err\n}</code></pre>\n<h3>DTO + handler (Go)</h3>\n<pre><code class=\"language-go\">// services/price/internal/api/price_history.go\n\ntype DailyPoint struct {\n    Day    time.Time `json:\"day\"`\n    MinP   int64     `json:\"min_p\"` // VND\n    MaxP   int64     `json:\"max_p\"` // VND\n    CloseP int64     `json:\"close_p\"` // VND, giá hiển thị (DEC-PRICE-33)\n}\n\ntype TailPoint struct {\n    TS        time.Time `json:\"ts\"`\n    Price     int64     `json:\"price\"` // VND\n    FlashSale bool      `json:\"flash_sale\"`\n}\n\ntype HistoryResponse struct {\n    ProductID int64        `json:\"product_id\"`\n    Range     string       `json:\"range\"`\n    Daily     []DailyPoint `json:\"daily\"`\n    Tail      []TailPoint  `json:\"tail\"`\n}\n\n// HandlePriceHistory phục vụ GET /v1/products/{id}/price-history?range=90d.\nfunc (h *Handler) HandlePriceHistory(w http.ResponseWriter, req *http.Request) {\n    id, err := strconv.ParseInt(req.PathValue(\"id\"), 10, 64)\n    if err != nil {\n        writeErr(w, http.StatusBadRequest, \"invalid product id\")\n        return\n    }\n    rangeRaw := req.URL.Query().Get(\"range\")\n    window, ok := price.ParseRange(rangeRaw)\n    if !ok {\n        writeErr(w, http.StatusBadRequest, \"invalid range\")\n        return\n    }\n    if rangeRaw == \"\" {\n        rangeRaw = \"90d\"\n    }\n    exists, err := h.repo.ProductExists(req.Context(), id)\n    if err != nil {\n        writeErr(w, http.StatusInternalServerError, \"internal error\")\n        return\n    }\n    if !exists {\n        writeErr(w, http.StatusNotFound, \"product not found\") // DEC-PRICE-34\n        return\n    }\n    from := time.Now().Add(-window)\n    daily, err := h.repo.QueryDailyBody(req.Context(), id, from)\n    if err != nil {\n        writeErr(w, http.StatusInternalServerError, \"internal error\")\n        return\n    }\n    tail, err := h.repo.QueryRawTail(req.Context(), id)\n    if err != nil {\n        writeErr(w, http.StatusInternalServerError, \"internal error\")\n        return\n    }\n    w.Header().Set(\"Content-Type\", \"application/json; charset=utf-8\")\n    _ = json.NewEncoder(w).Encode(HistoryResponse{\n        ProductID: id, Range: rangeRaw, Daily: daily, Tail: tail,\n    })\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>GET /v1/products/abc/price-history</code> (id không phải số) trả <code>400</code>.</li><li><code>range</code> ngoài allowlist (vd <code>range=5d</code>) trả <code>400</code> + <code>{\"error\":\"invalid range\"}</code>; thiếu <code>range</code> xử lý như <code>90d</code>.</li><li>Phần <code>daily</code> đến từ <code>price_daily</code> cho khoảng <code>now() - range</code> (không quét raw cho phần thân).</li><li>Phần <code>tail</code> đến từ <code>price_snapshot</code> kể từ <code>date_trunc('day', now())</code>, không sớm hơn.</li><li>Response JSON đúng khóa <code>{product_id, range, daily[], tail[]}</code> với <code>daily[*]</code> có <code>{day, min_p, max_p, close_p}</code> và <code>tail[*]</code> có <code>{ts, price, flash_sale}</code>.</li><li><code>product_id</code> không có trong <code>tracked_product</code> trả <code>404</code>; SKU có thật nhưng chưa có snapshot trả <code>200</code> + <code>daily</code>/<code>tail</code> rỗng.</li><li>p95 của endpoint &lt;500ms trên SKU có lịch sử 1 năm (đo qua <code>price_history_duration_ms</code>).</li><li>Điểm <code>daily</code> dùng <code>close_p</code> làm giá hiển thị; <code>min_p</code>/<code>max_p</code> vẫn có mặt.</li><li>Mọi giá trong JSON là số nguyên VND (int64), không float, không string.</li><li>Request đã qua JWT gateway tới được handler; handler không tự xác thực token.</li><li>Metric <code>price_history_requests_total{range, status}</code> tăng đúng theo từng phản hồi.</li><li><code>daily</code> sắp tăng theo <code>day</code>, <code>tail</code> sắp tăng theo <code>ts</code>; <code>Content-Type</code> là <code>application/json; charset=utf-8</code>.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/price/internal/api/price_history_test.go\n\nfunc TestPriceHistory_Default90d(t *testing.T) {\n    h, pid := setupWithHistory(t, 120) // 120 ngày dữ liệu\n    rec := doGET(t, h, \"/v1/products/\"+itoa(pid)+\"/price-history\") // không range\n    require.Equal(t, 200, rec.Code)\n    var body HistoryResponse\n    decode(t, rec, &amp;body)\n    require.Equal(t, \"90d\", body.Range)\n    require.LessOrEqual(t, len(body.Daily), 91) // ~90 ngày, không phải 120\n}\n\nfunc TestPriceHistory_BadRange_400(t *testing.T) {\n    h, pid := setupWithHistory(t, 30)\n    rec := doGET(t, h, \"/v1/products/\"+itoa(pid)+\"/price-history?range=5d\")\n    require.Equal(t, 400, rec.Code)\n    require.Contains(t, rec.Body.String(), \"invalid range\")\n}\n\nfunc TestPriceHistory_UnknownProduct_404(t *testing.T) {\n    h, _ := setupWithHistory(t, 30)\n    rec := doGET(t, h, \"/v1/products/999999/price-history?range=30d\")\n    require.Equal(t, 404, rec.Code) // DEC-PRICE-34\n}\n\nfunc TestPriceHistory_StitchesRawTail(t *testing.T) {\n    h, pid := setupWithHistory(t, 30)\n    // ghi một snapshot raw lúc trưa nay, sau bucket cagg gần nhất\n    insertRaw(t, h, pid, todayAt(12, 0), 79_000, true)\n    rec := doGET(t, h, \"/v1/products/\"+itoa(pid)+\"/price-history?range=30d\")\n    var body HistoryResponse\n    decode(t, rec, &amp;body)\n    require.NotEmpty(t, body.Tail) // đuôi raw có mặt, không chờ cagg refresh\n    require.Equal(t, int64(79_000), body.Tail[len(body.Tail)-1].Price)\n    require.True(t, body.Tail[len(body.Tail)-1].FlashSale)\n}\n\nfunc TestPriceHistory_PayloadShape(t *testing.T) {\n    h, pid := setupWithHistory(t, 90)\n    rec := doGET(t, h, \"/v1/products/\"+itoa(pid)+\"/price-history?range=90d\")\n    var raw map[string]json.RawMessage\n    require.NoError(t, json.Unmarshal(rec.Body.Bytes(), &amp;raw))\n    for _, k := range []string{\"product_id\", \"range\", \"daily\", \"tail\"} {\n        _, ok := raw[k]\n        require.True(t, ok, \"thiếu khóa %s\", k)\n    }\n    require.Equal(t, \"application/json; charset=utf-8\", rec.Header().Get(\"Content-Type\"))\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>history_query.go</code> (ParseRange + 2 truy vấn + ProductExists) -&gt; <code>price_history.go</code> (DTO + handler) -&gt; đăng ký route trong <code>router.go</code> sau JWT middleware của gateway (TASK-INFRA-001) -&gt; tests. Handler dùng <code>http.ServeMux</code> của Go 1.22 với pattern <code>GET /v1/products/{id}/price-history</code> và <code>req.PathValue(\"id\")</code>. Không thêm caching ở slice này; cagg + đuôi một chunk đã đủ cho p95 &lt;500ms.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-PRICE-002</strong> - cung cấp <code>price_daily</code> (continuous aggregate) cho phần thân và <code>price_snapshot</code> (hypertable) cho đuôi raw. Đây là điều kiện cứng.</li><li><strong>TASK-INFRA-001 (gateway)</strong> - gắn JWT auth trước handler; route nằm sau middleware này.</li><li><strong>TASK-WEB-003 (downstream)</strong> - biểu đồ giá tiêu thụ response này; hình dạng JSON ở §3 là hợp đồng với nó.</li><li><strong>TASK-DEAL-003 (sibling)</strong> - API dữ liệu biểu đồ song song (sale ảo); dùng chung quy ước range allowlist + giá int64 VND.</li><li>Lib: <code>pgx</code> (driver), <code>encoding/json</code>, <code>net/http</code> (ServeMux Go 1.22).</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Request</h3>\n<pre><code>curl -s -H \"Authorization: Bearer $JWT\" \\\n  \"https://api.sandeal.vn/v1/products/90112/price-history?range=90d\"</code></pre>\n<h3>Response (200)</h3>\n<pre><code class=\"language-json\">{\n  \"product_id\": 90112,\n  \"range\": \"90d\",\n  \"daily\": [\n    { \"day\": \"2026-03-29T00:00:00Z\", \"min_p\": 149000, \"max_p\": 159000, \"close_p\": 149000 },\n    { \"day\": \"2026-03-30T00:00:00Z\", \"min_p\": 129000, \"max_p\": 149000, \"close_p\": 129000 },\n    { \"day\": \"2026-06-26T00:00:00Z\", \"min_p\": 99000,  \"max_p\": 119000, \"close_p\": 99000 }\n  ],\n  \"tail\": [\n    { \"ts\": \"2026-06-27T08:15:00Z\", \"price\": 99000, \"flash_sale\": false },\n    { \"ts\": \"2026-06-27T12:00:00Z\", \"price\": 79000, \"flash_sale\": true }\n  ]\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Cache lớp HTTP (ETag theo <code>product_id</code> + ngày) nếu QPS biểu đồ cao - slice sau, đo trước.</li><li>Tham số <code>?points=</code> để downsample điểm ngày cho khoảng <code>1y</code> trên màn hình nhỏ - tối ưu UI giai đoạn sau.</li><li>Trả kèm <code>currency</code> khi mở SEA (THB/IDR) - bám theo <code>tracked_product</code>, giữ giá int64 theo minor unit từng nước.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>id không phải số</td><td><code>ParseInt</code> lỗi</td><td>400</td><td>Frontend chỉ gửi id số (theo route biểu đồ)</td></tr><tr><td>range ngoài allowlist</td><td><code>ParseRange</code> ok=false</td><td>400 invalid range</td><td>UI chỉ render các nút 7d/30d/90d/180d/1y</td></tr><tr><td>product_id không tồn tại</td><td><code>ProductExists</code> false</td><td>404</td><td>Phân biệt với 200-rỗng (DEC-PRICE-34)</td></tr><tr><td>SKU có thật, chưa có snapshot</td><td>query trả 0 dòng</td><td>200 + daily/tail rỗng</td><td>UI hiện \"đang thu thập dữ liệu giá\"</td></tr><tr><td>cagg chưa refresh đuôi hôm nay</td><td>thiết kế</td><td>đuôi đến từ raw, không trễ</td><td>Stitch raw tail luôn bù phần cagg thiếu</td></tr><tr><td>quét raw toàn khoảng (bug)</td><td>p95 query metric vọt</td><td>vỡ &lt;500ms</td><td>Giữ đuôi raw trong <code>date_trunc('day', now())</code></td></tr><tr><td>chunk raw đuôi đã nén</td><td>hiếm (đuôi là chunk nóng)</td><td>đọc chậm hơn</td><td>Đuôi luôn &lt;1 ngày nên nằm trong chunk chưa nén (30d)</td></tr><tr><td>range=1y trên SKU lịch sử dài</td><td>p95 metric</td><td>phần thân nhiều điểm</td><td>price_daily nhẹ; cân nhắc <code>?points=</code> sau</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>API này là cầu nối duy nhất giữa kho time-series giá (TASK-PRICE-002) và biểu đồ người dùng (TASK-WEB-003).</li><li>Mẹo \"cagg cho thân + raw cho đuôi\" tách tốc độ (đọc bảng tổng hợp nhỏ) khỏi độ tươi (raw tới phút) mà không phải quét raw cả khoảng.</li><li>Allowlist range vừa khớp các nút UI vừa là rào chắn cứng chống quét raw không giới hạn.</li><li><code>close_p</code> là giá hiển thị; <code>min_p</code>/<code>max_p</code> chỉ vẽ dải biên - đồng nhất với cách TASK-PRICE-002 dựng continuous aggregate.</li><li>Giá luôn là int64 VND suốt đường đi từ DB tới JSON, không có bước chuyển float nào để tránh sai số.</li><li>Khi mở SEA, đính <code>currency</code> vào payload theo <code>tracked_product</code>; giá vẫn là int64 theo minor unit từng nước.</li></ul>\n<hr>\n<p><em>Hết TASK-PRICE-003. Status: done (mục tiêu audit 10/10).</em></p>";
