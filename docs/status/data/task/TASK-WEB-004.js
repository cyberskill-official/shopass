window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-WEB-004"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>UI quản lý wishlist và alert <strong>MUST</strong> là các màn hình trong khu vực đăng nhập, tiêu thụ API REST của TASK-TRACK-002 (wishlist) và TASK-TRACK-003 (alert_rule) qua <code>lib/api.ts</code>, phản chiếu đúng ngữ nghĩa <code>rule_type</code>/<code>threshold</code>/<code>channel</code> của backend, và chỉ thao tác trên tài nguyên của chính user. Hợp đồng:</p>\n<ol><li>UI <strong>MUST</strong> lấy và ghi dữ liệu qua API TASK-TRACK-002/003 (<code>/v1/wishlists...</code>, <code>/v1/alerts...</code>) qua <code>apiFetch</code> (TASK-WEB-001); server là nguồn sự thật, client KHÔNG tự giữ danh sách như nguồn chính (DEC-WEB-16).</li><li>Màn hình wishlist <strong>MUST</strong> cho: tạo wishlist (<code>POST /v1/wishlists {name}</code>), liệt kê wishlist của caller (<code>GET /v1/wishlists</code>), thêm item (<code>POST /v1/wishlists/{id}/items {product_id, target_price?}</code>), xóa item, xóa wishlist - khớp các route TASK-TRACK-002.</li><li><code>target_price</code> <strong>MUST</strong> nhập và hiển thị là số nguyên VND (int64), nullable (để trống = \"chưa đặt giá mong muốn\") (DEC-WEB-19); gửi nguyên số đồng VND lên API, KHÔNG dùng dấu phẩy động.</li><li>Màn hình alert <strong>MUST</strong> cho tạo luật (<code>POST /v1/alerts {product_id, rule_type, threshold?, channel[]?}</code>), liệt kê luật của caller (<code>GET /v1/alerts</code>), bật/tắt (<code>PATCH /v1/alerts/{id} {active}</code>), xóa (<code>DELETE /v1/alerts/{id}</code>) - khớp các route TASK-TRACK-003.</li><li>Bộ chọn <code>rule_type</code> <strong>MUST</strong> chỉ phơi enum <code>{price_below, drop_pct, real_sale, bottom_predicted}</code> của TASK-TRACK-003 (DEC-WEB-17); không cho giá trị ngoài enum.</li><li>Ô nhập <code>threshold</code> <strong>MUST</strong> đổi theo <code>rule_type</code> (DEC-WEB-17, mirror DEC-TRACK-22):</li></ol>\n<ul><li><code>price_below</code>: hiện ô giá VND (int &gt; 0).</li><li><code>drop_pct</code>: hiện ô phần trăm nguyên trong <code>[1, 99]</code>.</li><li><code>real_sale</code> và <code>bottom_predicted</code>: KHÔNG hiện ô threshold (tín hiệu từ engine, không có ngưỡng người dùng).</li></ul>\n<ol><li>UI <strong>MUST</strong> validate phía client quan hệ <code>rule_type</code> &lt;-&gt; <code>threshold</code> trước khi gửi (báo lỗi sớm), nhưng KHÔNG coi đó là kiểm tra cuối: server (TASK-TRACK-003 #5) vẫn validate lần cuối; UI hiển thị lỗi <code>400</code> từ server nếu có.</li><li>Bộ chọn <code>channel</code> <strong>MUST</strong> chỉ phơi <code>{push, email, sms}</code> với <code>push</code> mặc định (DEC-WEB-18), khớp enum kênh TASK-TRACK-003; cho chọn nhiều kênh; KHÔNG cho kênh ngoài tập.</li><li>UI <strong>MUST</strong> chỉ thao tác trên tài nguyên của chính user (DEC-WEB-20): server đã chặn IDOR (TASK-TRACK-002 #6, TASK-TRACK-003), client không cố duyệt id người khác; lỗi <code>403</code>/<code>404</code> từ server hiển thị \"không tìm thấy\" trung lập, không lộ sự tồn tại tài nguyên người khác.</li><li>Các màn hình <strong>MUST</strong> nằm trong route group <code>(app)</code> (sau guard auth TASK-WEB-001); request mang JWT do <code>apiFetch</code> gắn.</li><li>UI <strong>MUST</strong> xử lý trạng thái rỗng (chưa có wishlist/alert) bằng hướng dẫn tạo mới, và trạng thái lỗi mạng bằng thông báo rõ; không vỡ màn hình.</li><li>Toàn bộ <strong>MUST</strong> vượt <code>npx tsc --noEmit</code> sạch và <code>npm test</code> xanh.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao server là nguồn sự thật (DEC-WEB-16)?</strong> Wishlist và alert là dữ liệu bền thuộc người dùng, đồng bộ giữa web/extension/mobile. Client giữ bản sao như nguồn chính dẫn tới lệch trạng thái (một thiết bị sửa, thiết bị khác cũ). UI đọc/ghi qua API và phản ánh phản hồi server; cache phía client chỉ để hiển thị, làm tươi từ server.</p>\n<p><strong>Vì sao form phản chiếu đúng ngữ nghĩa threshold (DEC-WEB-17)?</strong> Backend TASK-TRACK-003 diễn giải <code>threshold</code> khác nhau theo <code>rule_type</code>: VND cho price_below, % cho drop_pct, không dùng cho real_sale/bottom_predicted. Nếu form cho nhập sai (vd % cho price_below) thì hoặc server trả 400 (trải nghiệm xấu), hoặc tạo luật vô nghĩa không bao giờ bắn. Ô threshold động theo loại giữ form khớp đúng ngữ nghĩa server.</p>\n<p><strong>Vì sao validate client nhưng không tin client (§1 #7)?</strong> Validate phía client cho phản hồi tức thì (người dùng thấy lỗi ngay khi gõ), tốt cho UX. Nhưng client có thể bị bỏ qua (gọi API trực tiếp), nên server vẫn là kiểm tra cuối. UI báo sớm + tôn trọng lỗi server là defense in depth, không thay thế.</p>\n<p><strong>Vì sao channel chỉ allowlist {push,email,sms} (DEC-WEB-18)?</strong> Mỗi kênh cần một dispatcher (FCM, email, SMS). Cho chọn kênh không có dispatcher tạo luật gửi vào hư không. Allowlist khớp đúng enum TASK-TRACK-003; push mặc định khớp mô hình chi phí push &gt; email &gt; sms (§3.6).</p>\n<p><strong>Vì sao target_price/threshold là int VND (DEC-WEB-19)?</strong> Tiền VN là số nguyên đồng. Nhập/lưu float gây sai số khi engine so <code>giá hiện tại &lt;= target_price</code>. Giữ int64 suốt từ ô nhập tới API đồng nhất DEC-PRICE-05, tránh sai số.</p>\n<p><strong>Vì sao lỗi 403/404 trung lập (DEC-WEB-20)?</strong> Nếu user đoán id wishlist người khác, server chặn (IDOR). UI không nên phân biệt \"không tồn tại\" với \"tồn tại nhưng không phải của bạn\" - cả hai hiển thị \"không tìm thấy\" để không rò rỉ sự tồn tại tài nguyên người khác (quyền riêng tư, đụng PDPL).</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Client wishlist (lib/wishlist/api.ts)</h3>\n<pre><code class=\"language-ts\">// web/lib/wishlist/api.ts\nimport { apiFetch } from \"@/lib/api\";\n\nexport interface Wishlist { id: number; name: string; itemCount: number }\nexport interface WishlistItem { id: number; productId: number; targetPrice: number | null } // VND int64\n\nexport async function listWishlists(): Promise&lt;Wishlist[]&gt; {\n  const res = await apiFetch(\"/v1/wishlists\");           // GET của TASK-TRACK-002\n  return (await res.json()) as Wishlist[];\n}\nexport async function addItem(wishlistId: number, productId: number, targetPrice: number | null) {\n  return apiFetch(`/v1/wishlists/${wishlistId}/items`, {\n    method: \"POST\", headers: { \"Content-Type\": \"application/json\" },\n    body: JSON.stringify({ product_id: productId, target_price: targetPrice }), // int VND (DEC-WEB-19)\n  });\n}</code></pre>\n<h3>Validate alert theo rule_type (lib/alerts/validate.ts)</h3>\n<pre><code class=\"language-ts\">// web/lib/alerts/validate.ts - mirror DEC-TRACK-22 của TASK-TRACK-003\nexport type RuleType = \"price_below\" | \"drop_pct\" | \"real_sale\" | \"bottom_predicted\";\nexport type Channel = \"push\" | \"email\" | \"sms\";\nexport const CHANNELS: Channel[] = [\"push\", \"email\", \"sms\"];          // khớp enum (DEC-WEB-18)\n\nexport function needsThreshold(rt: RuleType): boolean {\n  return rt === \"price_below\" || rt === \"drop_pct\";                   // còn lại KHÔNG có threshold\n}\n\nexport function validateAlert(rt: RuleType, threshold: number | null, channels: Channel[]): string | null {\n  if (channels.length === 0) return \"Chọn ít nhất một kênh\";\n  if (channels.some((c) =&gt; !CHANNELS.includes(c))) return \"Kênh không hợp lệ\";\n  if (rt === \"price_below\") {\n    if (threshold == null || !Number.isInteger(threshold) || threshold &lt;= 0) return \"Giá phải là số nguyên dương (VND)\";\n  } else if (rt === \"drop_pct\") {\n    if (threshold == null || !Number.isInteger(threshold) || threshold &lt; 1 || threshold &gt; 99) return \"Phần trăm phải trong 1..99\";\n  } else {\n    if (threshold != null) return \"Loại này không nhận ngưỡng\"; // real_sale / bottom_predicted\n  }\n  return null; // hợp lệ\n}</code></pre>\n<h3>Form alert (ô threshold động theo loại)</h3>\n<pre><code class=\"language-tsx\">// web/components/alerts/alert-form.tsx (đoạn lõi)\nconst [ruleType, setRuleType] = useState&lt;RuleType&gt;(\"price_below\");\nconst [threshold, setThreshold] = useState&lt;number | null&gt;(null);\nconst [channels, setChannels] = useState&lt;Channel[]&gt;([\"push\"]);\n\nconst err = validateAlert(ruleType, threshold, channels); // báo sớm (DEC-WEB-17), server vẫn validate\n\nreturn (\n  &lt;form onSubmit={submit}&gt;\n    &lt;select value={ruleType} onChange={(e) =&gt; setRuleType(e.target.value as RuleType)}&gt;\n      &lt;option value=\"price_below\"&gt;Báo khi về giá&lt;/option&gt;\n      &lt;option value=\"drop_pct\"&gt;Báo khi giảm %&lt;/option&gt;\n      &lt;option value=\"real_sale\"&gt;Báo khi sale thật&lt;/option&gt;\n      &lt;option value=\"bottom_predicted\"&gt;Báo khi sắp chạm đáy&lt;/option&gt;\n    &lt;/select&gt;\n    {needsThreshold(ruleType) &amp;&amp; (\n      &lt;input type=\"number\" step={1} value={threshold ?? \"\"}    // int VND hoặc % (DEC-WEB-19)\n        onChange={(e) =&gt; setThreshold(e.target.value === \"\" ? null : parseInt(e.target.value, 10))} /&gt;\n    )}\n    {CHANNELS.map((c) =&gt; (/* checkbox push/email/sms */ null))}\n    &lt;button disabled={err != null}&gt;Tạo cảnh báo&lt;/button&gt;\n  &lt;/form&gt;\n);</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>lib/wishlist/api.ts</code> và <code>lib/alerts/api.ts</code> gọi đúng route TASK-TRACK-002/003 qua <code>apiFetch</code>; grep: client không giữ danh sách như nguồn sự thật (đọc từ API).</li><li>Tạo/liệt kê/thêm item/xóa wishlist khớp các route TASK-TRACK-002; <code>target_price</code> gửi là số nguyên hoặc null.</li><li><code>target_price</code> nhập là số nguyên VND; grep: không có parse float cho tiền; để trống gửi <code>null</code>.</li><li>Tạo/liệt kê/bật-tắt/xóa alert khớp các route TASK-TRACK-003.</li><li>Bộ chọn <code>rule_type</code> chỉ có 4 giá trị enum; không render lựa chọn ngoài enum.</li><li>Ô <code>threshold</code> hiện cho <code>price_below</code> (giá) và <code>drop_pct</code> (%); ẩn cho <code>real_sale</code>/<code>bottom_predicted</code>.</li><li><code>validateAlert</code> trả lỗi đúng: price_below cần int &gt; 0; drop_pct cần 1..99; real_sale/bottom_predicted không nhận threshold; channel rỗng/lạ báo lỗi.</li><li>Bộ chọn <code>channel</code> chỉ <code>{push,email,sms}</code>, push mặc định; cho chọn nhiều; không kênh ngoài tập.</li><li>Lỗi <code>403</code>/<code>404</code> từ server hiển thị \"không tìm thấy\" trung lập; client không phân biệt tồn-tại-nhưng-không-phải-của-bạn.</li><li>Màn hình nằm trong <code>(app)</code> (sau guard auth); request mang JWT qua <code>apiFetch</code>.</li><li>Trạng thái rỗng có hướng dẫn tạo mới; lỗi mạng có thông báo; không vỡ.</li><li><code>npx tsc --noEmit</code> sạch; <code>npm test</code> xanh.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-ts\">// web/test/alert-form-validate.test.ts\nimport { validateAlert } from \"../lib/alerts/validate\";\n\ntest(\"price_below cần số nguyên dương VND\", () =&gt; {\n  expect(validateAlert(\"price_below\", 0, [\"push\"])).toMatch(/số nguyên dương/);\n  expect(validateAlert(\"price_below\", 89000, [\"push\"])).toBeNull();\n});\n\ntest(\"drop_pct cần 1..99\", () =&gt; {\n  expect(validateAlert(\"drop_pct\", 0, [\"push\"])).toMatch(/1\\.\\.99/);\n  expect(validateAlert(\"drop_pct\", 150, [\"push\"])).toMatch(/1\\.\\.99/);\n  expect(validateAlert(\"drop_pct\", 30, [\"push\"])).toBeNull();\n});\n\ntest(\"real_sale / bottom_predicted KHÔNG nhận threshold\", () =&gt; {\n  expect(validateAlert(\"real_sale\", 100, [\"push\"])).toMatch(/không nhận ngưỡng/);\n  expect(validateAlert(\"bottom_predicted\", null, [\"push\"])).toBeNull();\n});</code></pre>\n<pre><code class=\"language-ts\">// web/test/alert-channel.test.ts\nimport { validateAlert, CHANNELS } from \"../lib/alerts/validate\";\n\ntest(\"channel chỉ {push,email,sms}; rỗng hoặc lạ báo lỗi\", () =&gt; {\n  expect(CHANNELS).toEqual([\"push\", \"email\", \"sms\"]);\n  expect(validateAlert(\"real_sale\", null, [])).toMatch(/ít nhất một kênh/);\n  expect(validateAlert(\"real_sale\", null, [\"zalo\" as any])).toMatch(/không hợp lệ/);\n  expect(validateAlert(\"real_sale\", null, [\"push\", \"email\"])).toBeNull();\n});</code></pre>\n<pre><code class=\"language-ts\">// web/test/wishlist-api.test.ts\nimport { addItem } from \"../lib/wishlist/api\";\n\ntest(\"addItem gửi target_price là số nguyên VND (không float)\", async () =&gt; {\n  const spy = mockFetchOk();\n  await addItem(3, 90112, 99000);\n  const body = JSON.parse(spy.mock.calls[0][1].body);\n  expect(body).toEqual({ product_id: 90112, target_price: 99000 });\n  expect(Number.isInteger(body.target_price)).toBe(true);\n});\n\ntest(\"addItem để trống target_price gửi null\", async () =&gt; {\n  const spy = mockFetchOk();\n  await addItem(3, 90112, null);\n  expect(JSON.parse(spy.mock.calls[0][1].body).target_price).toBeNull();\n});</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>lib/wishlist/api.ts</code> + <code>lib/alerts/api.ts</code> (client gọi đúng route TASK-TRACK-002/003 qua apiFetch) -&gt; <code>lib/alerts/validate.ts</code> (mirror ngữ nghĩa threshold/channel, test thuần) -&gt; <code>wishlist-panel.tsx</code> + <code>wishlist-item-row.tsx</code> (CRUD wishlist + sửa target_price) -&gt; <code>alert-form.tsx</code> (rule_type + ô threshold động + channel) + <code>alert-list.tsx</code> (bật/tắt/xóa) -&gt; <code>wishlist/page.tsx</code> + <code>alerts/page.tsx</code> (màn hình trong shell (app)) -&gt; thêm điều hướng vào <code>app-shell.tsx</code> -&gt; tests. Các form là client component (<code>\"use client\"</code>) cho tương tác; trang gọi API phía client sau khi shell (app) qua guard auth. Validate phía client báo sớm; server (TASK-TRACK-003) vẫn là kiểm tra cuối.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-TRACK-002</strong> - schema + API wishlist/wishlist_item; UI này là mặt tiền của các route <code>/v1/wishlists...</code> (depends_on cứng).</li><li><strong>TASK-TRACK-003</strong> - schema + API alert_rule; UI phản chiếu đúng enum <code>rule_type</code>/<code>channel</code> và ngữ nghĩa <code>threshold</code> của nó (depends_on cứng).</li><li><strong>TASK-WEB-001</strong> - scaffold + <code>lib/api.ts</code> (JWT) + shell <code>(app)</code> + guard; màn hình nằm trong khu vực bảo vệ (depends_on cứng).</li><li><strong>TASK-TRACK-004 (downstream)</strong> - engine kích hoạt đọc các luật người dùng tạo qua UI này; UI là nơi luật được sinh ra.</li><li>Lib: <code>next</code> 14, React 18, <code>@testing-library/react</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Tạo luật price_below (push + email)</h3>\n<pre><code class=\"language-ts\">await createAlert({\n  product_id: 90112,\n  rule_type: \"price_below\",\n  threshold: 89000,            // VND int64 (DEC-WEB-19)\n  channel: [\"push\", \"email\"],  // allowlist (DEC-WEB-18)\n});\n// POST /v1/alerts -&gt; 201 (TASK-TRACK-003)</code></pre>\n<h3>Tạo luật real_sale (không threshold)</h3>\n<pre><code class=\"language-json\">{ \"product_id\": 90112, \"rule_type\": \"real_sale\", \"channel\": [\"push\"] }</code></pre>\n<h3>Thêm item vào wishlist với giá mong muốn</h3>\n<pre><code class=\"language-json\">{ \"product_id\": 77310, \"target_price\": 199000 }</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Kéo-thả sắp xếp item giữa các wishlist - tinh chỉnh UX sau, không đổi API.</li><li>Gợi ý target_price dựa median90/trailing_min (đọc feed TASK-DEAL-003) khi đặt giá mong muốn - thêm khi tích hợp biểu đồ.</li><li>Tạo nhanh alert ngay từ màn hình biểu đồ (TASK-WEB-003) - liên kết hai màn hình ở slice sau.</li><li>Hiển thị lịch sử bắn (alert history) inline - dùng <code>GET /v1/alerts/{id}/history</code> của TASK-TRACK-003 khi cần.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Chọn rule_type/channel ngoài enum</td><td>alert-form/channel test</td><td>server 400 hoặc luật rác</td><td>Chỉ phơi enum TASK-TRACK-003 (DEC-WEB-17/18)</td></tr><tr><td>Ô threshold sai ngữ nghĩa</td><td>alert-form-validate test</td><td>luật không bắn / bắn loạn</td><td>Ô động theo rule_type (DEC-WEB-17)</td></tr><tr><td>target_price/threshold float</td><td>wishlist-api test</td><td>sai số so giá</td><td>Int VND (DEC-WEB-19)</td></tr><tr><td>Client giữ danh sách lệch server</td><td>review</td><td>trạng thái cũ</td><td>Server là nguồn sự thật (DEC-WEB-16)</td></tr><tr><td>Lộ tài nguyên user khác qua 403/404</td><td>review thông báo</td><td>rò rỉ quyền riêng tư</td><td>\"Không tìm thấy\" trung lập (DEC-WEB-20)</td></tr><tr><td>Màn hình lộ khi chưa đăng nhập</td><td>guard (app)</td><td>rò rỉ</td><td>Nằm trong (app), JWT qua apiFetch (§1 #10)</td></tr><tr><td>Channel rỗng gửi lên</td><td>validate test</td><td>luật không gửi đâu</td><td>Bắt buộc &gt;=1 kênh (validateAlert)</td></tr><tr><td>Trạng thái rỗng làm vỡ</td><td>empty-state</td><td>UX kém</td><td>Hướng dẫn tạo mới (§1 #11)</td></tr><tr><td>Bỏ qua validate client (gọi API thẳng)</td><td>server validate</td><td>luật xấu</td><td>Server TASK-TRACK-003 validate cuối (§1 #7)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>wishlist + alert biến SănDeal từ công cụ tra cứu thành trợ lý chủ động; đây là mặt tiền của backend TASK-TRACK-002/003.</li><li>Server là nguồn sự thật; UI đọc/ghi qua API và đồng bộ giữa web/extension/mobile, không giữ bản sao làm nguồn chính.</li><li>Form alert phản chiếu đúng ngữ nghĩa threshold theo rule_type của TASK-TRACK-003 - ô động tránh tạo luật vô nghĩa.</li><li>Validate phía client báo sớm cho UX, nhưng server vẫn là kiểm tra cuối (defense in depth).</li><li>channel allowlist {push,email,sms} khớp dispatcher có thật; push mặc định khớp mô hình chi phí push &gt; email &gt; sms.</li><li>Lỗi 403/404 trung lập bảo vệ quyền riêng tư - không lộ sự tồn tại tài nguyên người khác, đụng đúng yêu cầu PDPL.</li></ul>\n<hr>\n<p><em>Hết TASK-WEB-004. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
