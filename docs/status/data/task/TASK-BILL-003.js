window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-BILL-003"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service BILL <strong>MUST</strong> định nghĩa bảng <code>payment</code>, nhận IPN/webhook từ cổng (có xác thực chữ ký, idempotent, khớp số tiền), kích hoạt subscription khi thanh toán hợp lệ, và đối soát định kỳ để bù IPN bị mất. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> định nghĩa bảng <code>payment (id, order_ref, subscription_id, gateway, amount, fee, status, transaction_id, paid_at, created_at)</code> với <code>order_ref TEXT NOT NULL UNIQUE</code> (idempotency từ TASK-BILL-002) và <code>subscription_id</code> REFERENCES <code>subscription(id)</code>.</li><li><strong>MUST</strong> lưu <code>amount</code> và <code>fee</code> dạng <code>BIGINT</code> (VND, không thập phân) (DEC-BILL-11) - KHÔNG float; <code>fee</code> là phí cổng (~1,5-2,5% với QR, §4.1) để tính doanh thu ròng <code>amount - fee</code>.</li><li><strong>MUST</strong> ràng buộc <code>payment.status</code> - {<code>'pending'</code>,<code>'paid'</code>,<code>'failed'</code>,<code>'mismatch'</code>} qua CHECK; <code>amount &gt;= 0</code>, <code>fee &gt;= 0</code> qua CHECK.</li><li><strong>MUST</strong> phục vụ webhook <code>POST /v1/billing/ipn/{gateway}</code> nhận IPN của cổng mang tối thiểu <code>{order_ref, transaction_id, amount, status}</code>.</li><li><strong>MUST</strong> xác thực chữ ký IPN bằng khóa của <code>{gateway}</code> đọc từ Vault (TASK-INFRA-003) trước khi tin (DEC-BILL-12); chữ ký sai/thiếu -&gt; bỏ qua (trả <code>200</code>/<code>400</code> theo spec cổng để cổng ngừng retry), KHÔNG đổi <code>payment</code>.</li><li><strong>MUST</strong> idempotent theo <code>order_ref</code> + <code>transaction_id</code> (DEC-BILL-13): cùng IPN gửi lại (cổng retry) <strong>MUST NOT</strong> đổi trạng thái payment hai lần hay gia hạn subscription hai lần. IPN trên payment đã <code>paid</code> là no-op.</li><li><strong>MUST</strong> khớp <code>amount</code> trong IPN với <code>payment.amount</code> đã ghi (DEC-BILL-14): bằng -&gt; tiếp tục; lệch -&gt; đặt <code>status='mismatch'</code>, ghi log/alert, và <strong>MUST NOT</strong> kích hoạt subscription (số tiền không khớp là dấu hiệu lỗi/gian lận).</li><li>Khi IPN hợp lệ và khớp với <code>status='paid'</code> từ cổng, <strong>MUST</strong> đặt <code>payment.status='paid'</code>, <code>paid_at=now()</code>, lưu <code>transaction_id</code>, rồi kích hoạt subscription (DEC-BILL-15): gọi TASK-BILL-001 <code>SetRenewsAt</code> (đẩy kỳ kế) + <code>UpdateStatus</code> về <code>active</code>.</li><li>Khi IPN báo cổng <code>failed</code>/hủy, <strong>MUST</strong> đặt <code>payment.status='failed'</code> và KHÔNG kích hoạt subscription; subscription giữ trạng thái cũ (hoặc <code>past_due</code> nếu là kỳ gia hạn).</li><li><strong>MUST</strong> có job reconciliation định kỳ (<code>reconcile.go</code>) quét <code>payment</code> <code>pending</code> quá ngưỡng thời gian, đối soát với report/truy vấn cổng, và chốt trạng thái (bù IPN bị mất) - user đã trả không bị kẹt <code>pending</code> mãi.</li><li><strong>MUST</strong> lấy khóa xác thực từ Vault ở thời điểm verify; webhook KHÔNG dùng JWT user (cổng không có), chỉ chữ ký. Mọi IPN (kể cả bị từ chối) <strong>SHOULD</strong> ghi log để truy vết.</li><li><strong>SHOULD</strong> phát OTel: <code>payment_ipn_total{gateway, result}</code> (<code>result - {paid, failed, mismatch, unauthorized, duplicate}</code>), <code>payment_revenue_net_vnd</code> (histogram <code>amount - fee</code>), <code>payment_pending_age_seconds</code> (gauge cho reconciliation).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao xác thực chữ ký IPN (DEC-BILL-12)?</strong> IPN webhook là endpoint công khai. Nếu tin payload chưa xác thực, kẻ xấu gửi IPN giả \"order_ref X đã thanh toán\" để được cấp Premium miễn phí. Xác thực chữ ký bằng khóa chung với cổng đảm bảo chỉ cổng thật mới đổi được trạng thái payment - rào chống gian lận tài chính cốt lõi, đồng nhất cách TASK-AFFIL-003 xử lý postback.</p>\n<p><strong>Vì sao khớp số tiền (DEC-BILL-14)?</strong> Một IPN hợp lệ về chữ ký vẫn có thể mang số tiền không khớp đơn (lỗi cổng, hoặc tấn công tinh vi). Nếu kích hoạt subscription mà không khớp <code>amount</code>, user trả 1k được Premium 79k. Khớp <code>amount</code> với <code>payment.amount</code> đã ghi và từ chối (đánh <code>mismatch</code>) khi lệch đóng lỗ hổng này; <code>mismatch</code> được điều tra thủ công thay vì âm thầm cấp quyền.</p>\n<p><strong>Vì sao idempotent theo order_ref + transaction_id (DEC-BILL-13)?</strong> Cổng gửi lại IPN khi không nhận được phản hồi đúng. Nếu mỗi IPN đổi trạng thái/gia hạn, một thanh toán bị đếm nhiều lần và subscription gia hạn dư. <code>order_ref</code> UNIQUE + kiểm <code>transaction_id</code> làm IPN idempotent: lần đầu chốt, lần sau no-op.</p>\n<p><strong>Vì sao lưu fee (DEC-BILL-11, §1 #2)?</strong> Doanh thu thật là <code>amount - fee</code> (phí cổng). Phí QR ~1,5-2,5% (§4.1) là biến phí trực tiếp trên mỗi giao dịch. Lưu <code>fee</code> cho phép báo cáo doanh thu ròng đúng và theo dõi unit economics - không lưu thì chỉ biết doanh thu gộp, sai bức tranh biên.</p>\n<p><strong>Vì sao reconciliation định kỳ (DEC-BILL-15, §1 #10)?</strong> IPN có thể mất (mạng chập, webhook đỗ). Nếu chỉ dựa IPN, một user đã trả tiền mà IPN không tới sẽ kẹt <code>pending</code> mãi - đã mất tiền mà không có Premium, khiếu nại chính đáng. Job đối soát định kỳ quét <code>pending</code> lâu, hỏi cổng trạng thái thật, và chốt - lưới an toàn cho IPN mất. Nguồn sự thật cuối là cổng, IPN chỉ là thông báo nhanh.</p>\n<p><strong>Vì sao tách kích hoạt subscription khỏi checkout (§1 #8)?</strong> Checkout (TASK-BILL-002) chỉ tạo ý định thanh toán; tiền chưa chắc vào. Chỉ khi IPN xác nhận <code>paid</code> + khớp số tiền, ta mới kích hoạt subscription. Tách \"tạo ý định\" (BILL-002) khỏi \"xác nhận đã trả -&gt; cấp quyền\" (BILL-003) đảm bảo Premium chỉ bật sau khi tiền thực sự vào, đúng thứ tự nhân quả.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/bill/migrations/0003_payment.sql\nCREATE TABLE payment (\n  id             BIGSERIAL   PRIMARY KEY,\n  order_ref      TEXT        NOT NULL UNIQUE,        -- idempotency (TASK-BILL-002)\n  subscription_id BIGINT     REFERENCES subscription(id),\n  gateway        TEXT        NOT NULL,\n  amount         BIGINT      NOT NULL CHECK (amount &gt;= 0),  -- VND\n  fee            BIGINT      NOT NULL DEFAULT 0 CHECK (fee &gt;= 0), -- phí cổng VND (§1 #2)\n  status         TEXT        NOT NULL DEFAULT 'pending'\n                   CHECK (status IN ('pending','paid','failed','mismatch')),\n  transaction_id TEXT,                               -- mã giao dịch cổng (idempotent)\n  paid_at        TIMESTAMPTZ,\n  created_at     TIMESTAMPTZ NOT NULL DEFAULT now()\n);\nCREATE INDEX idx_payment_pending ON payment (created_at) WHERE status = 'pending'; -- reconciliation</code></pre>\n<h3>IPN handler (Go)</h3>\n<pre><code class=\"language-go\">// services/bill/internal/api/ipn.go\nfunc (h *Handler) HandleIPN(w http.ResponseWriter, req *http.Request) {\n    gateway := req.PathValue(\"gateway\")\n    body, _ := io.ReadAll(req.Body)\n    sig := req.Header.Get(\"X-Signature\")\n\n    ok, err := pay.VerifyIPN(req.Context(), h.secrets, gateway, body, sig)\n    if err != nil { writeErr(w, 500, \"verify error\"); return }\n    if !ok {\n        metrics.IPN(gateway, \"unauthorized\")\n        writeErr(w, 400, \"invalid signature\"); return // KHÔNG đổi payment (§1 #5)\n    }\n\n    var ipn IPNPayload\n    if err := json.Unmarshal(body, &amp;ipn); err != nil { writeErr(w, 400, \"bad payload\"); return }\n\n    p, found := h.payments.ByOrderRef(req.Context(), ipn.OrderRef)\n    if !found { writeErr(w, 404, \"unknown order_ref\"); return }\n    if p.Status == \"paid\" { // idempotent: IPN lặp (§1 #6)\n        metrics.IPN(gateway, \"duplicate\"); w.WriteHeader(200); return\n    }\n    if ipn.Amount != p.Amount { // khớp số tiền (§1 #7)\n        h.payments.MarkMismatch(req.Context(), p.ID, ipn.Amount)\n        metrics.IPN(gateway, \"mismatch\")\n        log.Warn(\"payment amount mismatch\", \"order_ref\", ipn.OrderRef,\n            \"expected\", p.Amount, \"got\", ipn.Amount)\n        w.WriteHeader(200); return // KHÔNG kích hoạt subscription\n    }\n    switch ipn.Status {\n    case \"paid\":\n        h.payments.MarkPaid(req.Context(), p.ID, ipn.TransactionID)\n        h.subs.Activate(req.Context(), p.SubscriptionID) // TASK-BILL-001 SetRenewsAt + active (§1 #8)\n        metrics.IPN(gateway, \"paid\")\n    default:\n        h.payments.MarkFailed(req.Context(), p.ID)\n        metrics.IPN(gateway, \"failed\")\n    }\n    w.WriteHeader(200)\n}</code></pre>\n<h3>Reconciliation (Go)</h3>\n<pre><code class=\"language-go\">// services/bill/internal/bill/reconcile.go\n// ReconcilePending quét payment pending quá ngưỡng, hỏi cổng trạng thái thật, chốt (§1 #10).\nfunc (r *Reconciler) ReconcilePending(ctx context.Context, olderThan time.Duration) error {\n    rows, err := r.payments.ListPending(ctx, time.Now().Add(-olderThan))\n    if err != nil {\n        return err\n    }\n    for _, p := range rows {\n        st, txID, err := r.gateways.QueryStatus(ctx, p.Gateway, p.OrderRef) // hỏi cổng\n        if err != nil { continue }\n        if st == \"paid\" {\n            r.payments.MarkPaid(ctx, p.ID, txID)\n            r.subs.Activate(ctx, p.SubscriptionID) // bù kích hoạt IPN bị mất\n        } else if st == \"failed\" {\n            r.payments.MarkFailed(ctx, p.ID)\n        }\n    }\n    return nil\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch -&gt; <code>payment</code> tồn tại với <code>order_ref</code> UNIQUE, CHECK status/amount/fee.</li><li>IPN chữ ký sai -&gt; KHÔNG đổi <code>payment</code> (status vẫn <code>pending</code>); metric <code>unauthorized</code>.</li><li>IPN hợp lệ, <code>amount</code> khớp, cổng <code>paid</code> -&gt; <code>payment.status='paid'</code>, <code>paid_at</code> set, <code>transaction_id</code> lưu, subscription được kích hoạt (active + renews_at đẩy).</li><li>IPN hợp lệ nhưng <code>amount</code> lệch <code>payment.amount</code> -&gt; <code>status='mismatch'</code>, subscription KHÔNG kích hoạt, log cảnh báo.</li><li>IPN báo cổng <code>failed</code> -&gt; <code>status='failed'</code>, subscription không kích hoạt.</li><li>IPN lặp (cùng order_ref, payment đã <code>paid</code>) -&gt; no-op, subscription không gia hạn lần hai; metric <code>duplicate</code>.</li><li>IPN với <code>order_ref</code> không tồn tại -&gt; <code>404</code>, không tạo payment.</li><li><code>amount</code>/<code>fee</code> lưu BIGINT; <code>INSERT</code> <code>amount &lt; 0</code> hoặc <code>fee &lt; 0</code> -&gt; lỗi CHECK.</li><li><code>INSERT payment</code> <code>status='refunded'</code> (ngoài tập) -&gt; lỗi CHECK.</li><li><code>ReconcilePending</code> trên payment <code>pending</code> lâu mà cổng báo <code>paid</code> -&gt; chốt <code>paid</code> + kích hoạt subscription (bù IPN mất).</li><li>Doanh thu ròng tính đúng <code>amount - fee</code> (kiểm metric/report).</li><li>Khóa xác thực IPN đọc từ Vault (mock); review xác nhận không có khóa cleartext.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/bill/internal/api/ipn_test.go\nfunc TestIPN_BadSignature_NoChange(t *testing.T) {\n    h, repo := setupIPN(t, \"o1\", 29000) // payment pending\n    rec := doSignedIPN(t, h, \"momo\", `{\"order_ref\":\"o1\",\"transaction_id\":\"t1\",\"amount\":29000,\"status\":\"paid\"}`, \"WRONG\")\n    require.Equal(t, 400, rec.Code)\n    require.Equal(t, \"pending\", repo.StatusByOrderRef(\"o1\")) // không đổi\n}\n\nfunc TestIPN_Paid_ActivatesSubscription(t *testing.T) {\n    h, repo := setupIPN(t, \"o2\", 29000)\n    body := `{\"order_ref\":\"o2\",\"transaction_id\":\"t2\",\"amount\":29000,\"status\":\"paid\"}`\n    doSignedIPN(t, h, \"momo\", body, sign(body))\n    require.Equal(t, \"paid\", repo.StatusByOrderRef(\"o2\"))\n    require.True(t, repo.SubscriptionActive(\"o2\")) // subscription kích hoạt\n}\n\nfunc TestIPN_AmountMismatch_NoActivate(t *testing.T) {\n    h, repo := setupIPN(t, \"o3\", 79000) // payment ghi 79000\n    body := `{\"order_ref\":\"o3\",\"transaction_id\":\"t3\",\"amount\":1000,\"status\":\"paid\"}` // IPN 1000\n    doSignedIPN(t, h, \"momo\", body, sign(body))\n    require.Equal(t, \"mismatch\", repo.StatusByOrderRef(\"o3\"))\n    require.False(t, repo.SubscriptionActive(\"o3\")) // KHÔNG cấp Premium (§1 #7)\n}\n\nfunc TestIPN_Duplicate_Idempotent(t *testing.T) {\n    h, repo := setupIPN(t, \"o4\", 29000)\n    body := `{\"order_ref\":\"o4\",\"transaction_id\":\"t4\",\"amount\":29000,\"status\":\"paid\"}`\n    doSignedIPN(t, h, \"momo\", body, sign(body))\n    doSignedIPN(t, h, \"momo\", body, sign(body)) // cổng retry\n    require.Equal(t, 1, repo.ActivateCount(\"o4\")) // không gia hạn hai lần\n}\n\nfunc TestIPN_NegativeAmount_CheckRejects(t *testing.T) {\n    repo := setupRepo(t)\n    err := repo.InsertRaw(ctx, \"o5\", \"momo\", -1, 0)\n    require.Error(t, err) // CHECK amount &gt;= 0\n}</code></pre>\n<pre><code class=\"language-go\">// services/bill/internal/bill/reconcile_test.go\nfunc TestReconcile_PaidBackfill(t *testing.T) {\n    r, repo := setupReconciler(t, \"o6\", 29000) // pending lâu, cổng báo paid\n    require.NoError(t, r.ReconcilePending(ctx, time.Hour))\n    require.Equal(t, \"paid\", repo.StatusByOrderRef(\"o6\"))\n    require.True(t, repo.SubscriptionActive(\"o6\")) // bù kích hoạt IPN mất\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration <code>0003_payment.sql</code> (order_ref UNIQUE + CHECK + index pending) -&gt; <code>payment_repo.go</code> (<code>InsertPending</code> từ TASK-BILL-002, <code>MarkPaid</code>/<code>MarkFailed</code>/<code>MarkMismatch</code>, <code>ByOrderRef</code>) -&gt; <code>ipn.go</code> (verify chữ ký Vault -&gt; idempotent check -&gt; khớp amount -&gt; MarkPaid + kích hoạt subscription TASK-BILL-001) -&gt; <code>reconcile.go</code> (job định kỳ đối soát pending) -&gt; đăng ký <code>POST /v1/billing/ipn/{gateway}</code> (<code>http.ServeMux</code> <code>PathValue</code>) -&gt; tests. Webhook KHÔNG dùng JWT user; xác thực bằng chữ ký HMAC, khóa từ TASK-INFRA-003. <code>subs.Activate</code> gọi <code>SetRenewsAt</code> + <code>UpdateStatus(active)</code> của TASK-BILL-001.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-BILL-002</strong> - tạo <code>payment</code> <code>pending</code> + <code>order_ref</code>; IPN chốt trạng thái thanh toán đó.</li><li><strong>TASK-BILL-001</strong> - <code>subscription</code> + <code>SetRenewsAt</code>/<code>UpdateStatus</code>; kích hoạt khi payment <code>paid</code>.</li><li><strong>TASK-INFRA-003 (Vault)</strong> - khóa xác thực IPN mỗi cổng; không nằm trong DB/code.</li><li><strong>TASK-INFRA-001 (gateway)</strong> - định tuyến webhook IPN; bỏ qua JWT user, dùng chữ ký.</li><li><strong>TASK-INFRA-004 (observability)</strong> - alert khi <code>mismatch</code> / pending tồn lâu / IPN lỗi.</li><li><strong>TASK-BILL-005 (downstream)</strong> - feature gating bật khi subscription active (sau payment paid).</li><li><strong>TASK-COMPLY-005</strong> - audit no-cleartext: khóa IPN từ Vault.</li><li>Lib: <code>crypto/hmac</code>, <code>crypto/sha256</code>, <code>net/http</code>, <code>encoding/json</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>IPN từ cổng (MoMo, thanh toán thành công)</h3>\n<pre><code class=\"language-http\">POST /v1/billing/ipn/momo HTTP/1.1\nContent-Type: application/json\nX-Signature: 3bf4f1b2b0b822cd15d6c15b0f00a089f86d081884c7d659a2feaa0c55ad015a\n\n{\n  \"order_ref\": \"sd_bill_7_premium_basic_1719500000\",\n  \"transaction_id\": \"MOMO2026062812345\",\n  \"amount\": 29000,\n  \"status\": \"paid\"\n}</code></pre>\n<h3>payment sau khi chốt</h3>\n<pre><code class=\"language-sql\">SELECT order_ref, gateway, amount, fee, status, transaction_id FROM payment WHERE order_ref='sd_bill_7_premium_basic_1719500000';\n--  ...basic_1719500000 | momo | 29000 | 580 | paid | MOMO2026062812345\n-- doanh thu ròng = amount - fee = 29000 - 580 = 28420 VND</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li><code>fee</code> chính xác từng cổng (§10 tài liệu nguồn - cần xác minh theo hợp đồng merchant) - hiện ghi theo phản hồi cổng nếu có, hoặc tính theo tỷ lệ cấu hình ~1,5-2,5%.</li><li>Chargeback/refund làm đảo payment <code>paid</code> -&gt; hủy/hạ subscription - cần IPN <code>refunded</code>; map khi cổng hỗ trợ.</li><li>Tần suất job reconciliation + ngưỡng pending - cấu hình; mặc định quét pending &gt; 1 giờ.</li><li>Đối soát tổng định kỳ (cuối ngày so tổng doanh thu với report cổng) - thêm báo cáo khi có dashboard tài chính.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Tin IPN chưa xác thực</td><td>bad-signature test</td><td>Premium miễn phí qua IPN giả</td><td>Verify chữ ký Vault trước (§1 #5)</td></tr><tr><td>Kích hoạt khi amount lệch</td><td>mismatch test</td><td>trả 1k được Premium 79k</td><td>Khớp amount, đánh mismatch (§1 #7)</td></tr><tr><td>IPN lặp đổi trạng thái 2 lần</td><td>duplicate test</td><td>gia hạn dư, đếm doanh thu trùng</td><td>Idempotent order_ref+tx (§1 #6)</td></tr><tr><td>amount/fee float</td><td>review + CHECK</td><td>sai số doanh thu ròng</td><td>BIGINT VND (§1 #2)</td></tr><tr><td>IPN mất, user kẹt pending</td><td>reconcile test</td><td>đã trả không có Premium</td><td>Reconciliation định kỳ bù (§1 #10)</td></tr><tr><td>status ngoài tập</td><td>DB CHECK</td><td>trạng thái rác</td><td>CHECK status IN (...) (§1 #3)</td></tr><tr><td>order_ref lạ</td><td>404 test</td><td>IPN trỏ payment không tồn tại</td><td>Tra ByOrderRef trước (§1 #4)</td></tr><tr><td>Khóa IPN cleartext</td><td>review (§4 #12)</td><td>giả mạo IPN</td><td>Khóa từ Vault (§1 #11)</td></tr><tr><td>Không tính được biên</td><td>fee lưu + net metric</td><td>unit economics sai</td><td>Doanh thu ròng amount-fee (§1 #2)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>payment + reconciliation là nơi tiền Premium được chốt: chỉ khi IPN xác nhận paid + khớp số tiền, subscription mới bật.</li><li>Xác thực chữ ký IPN chặn IPN giả \"đã thanh toán\" - webhook công khai, không xác thực là mời gian lận.</li><li>Khớp số tiền đóng lỗ hổng \"trả ít được nhiều\"; lệch -&gt; mismatch điều tra, không âm thầm cấp quyền.</li><li>Idempotent theo order_ref + transaction_id chịu được cổng retry mà không gia hạn/đếm trùng.</li><li>Lưu fee cho phép tính doanh thu ròng (phí QR ~1,5-2,5% §4.1) - đúng bức tranh unit economics.</li><li>Reconciliation định kỳ là lưới an toàn cho IPN mất: nguồn sự thật cuối là cổng, không phải thông báo nhanh.</li><li>Tách kích hoạt subscription (BILL-003) khỏi tạo ý định (BILL-002): Premium chỉ bật sau khi tiền thực sự vào.</li></ul>\n<hr>\n<p><em>Hết TASK-BILL-003. Status: done (mục tiêu audit 10/10).</em></p>";
