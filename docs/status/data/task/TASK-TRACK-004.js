window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-TRACK-004"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service TRACK <strong>MUST</strong> chạy engine đánh giá <code>alert_rule</code> mỗi khi <code>price_snapshot</code> có dòng mới thật (TASK-PRICE-002 <code>written=true</code>), áp dedup rising-edge, tạo dòng <code>alert</code> (status <code>pending</code>), rồi bàn giao cho notification (TASK-NOTIF-001). Engine KHÔNG tự gửi. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> kích hoạt theo sự kiện (DEC-TRACK-30): sau mỗi <code>InsertSnapshot</code> trả <code>written=true</code> cho một <code>product_id</code>, engine đánh giá các <code>alert_rule</code> có <code>product_id</code> đó và <code>active = true</code> (đọc qua <code>idx_ar_eval</code> của TASK-TRACK-003). KHÔNG đánh giá khi <code>written=false</code> (giá không đổi).</li><li><strong>MUST</strong> lọc luật theo <code>(product_id, active=true)</code> (DEC-TRACK-30) - KHÔNG quét toàn bảng <code>alert_rule</code> mỗi lần. Một SKU không có luật active nào -&gt; engine không làm gì cho SKU đó.</li><li><strong>MUST</strong> đánh giá <code>rule_type = price_below</code>: điều kiện met khi <code>current_price &lt;= threshold</code> (so sánh BIGINT VND, đồng nhất DEC-PRICE-05).</li><li><strong>MUST</strong> đánh giá <code>rule_type = drop_pct</code>: tính <code>ref = median giá 7 ngày</code> đọc từ <code>price_daily</code> (TASK-PRICE-002, DEC-TRACK-34); điều kiện met khi <code>current_price &lt;= ref * (100 - threshold) / 100</code> (giảm &gt;= threshold% so mốc tham chiếu ổn định).</li><li><strong>MUST</strong> đánh giá <code>rule_type = real_sale</code>: gọi <code>TASK-DEAL-001 DetectFakeSale(product_id, current_price, list_price)</code>; điều kiện met khi kết quả là <code>SALE_XIN</code> (sale thật). <code>SALE_AO</code>/<code>TAM_DUOC</code>/<code>UNKNOWN</code> không met.</li><li><strong>MUST</strong> bỏ qua <code>rule_type = bottom_predicted</code> trong engine này (DEC-TRACK-31): loại đó do batch đêm TASK-DEAL-006 đánh giá và bắn, không gắn vào sự kiện giá tức thời. Engine này KHÔNG xử lý bottom_predicted để tránh bắn trùng.</li><li><strong>MUST</strong> áp dedup rising-edge (DEC-TRACK-32): chỉ tạo <code>alert</code> khi điều kiện met chuyển từ <code>false</code> (lần đánh giá trước) sang <code>true</code>. Nếu điều kiện đã met ở lần trước và vẫn met, <strong>MUST NOT</strong> bắn lại. Trạng thái lưu ở bảng <code>alert_fired_state (alert_rule_id PK, last_condition_met BOOLEAN, last_fired_at TIMESTAMPTZ)</code>.</li><li><strong>MUST</strong> reset cạnh khi điều kiện rời <code>true</code> về <code>false</code> (giá tăng lại trên ngưỡng): cập nhật <code>last_condition_met=false</code> để lần met tiếp theo lại là một cạnh lên mới (cho phép cảnh báo lần sau khi deal quay lại).</li><li><strong>MUST</strong> với mỗi luật vừa met (qua dedup), tạo một dòng <code>alert (alert_rule_id, fired_at=now(), payload, status='pending')</code> (DEC-TRACK-33); <code>payload</code> JSONB gồm <code>{price, ref_price, rule_type, ...}</code> đủ để NOTIF dựng nội dung.</li><li><strong>MUST</strong> bàn giao mỗi <code>alert</code> vừa tạo cho notification (TASK-NOTIF-001) bằng cách enqueue một <code>notification</code> request gồm <code>user_id</code> (từ luật), <code>channel[]</code> (từ luật), và <code>alert_id</code>. Engine <strong>MUST NOT</strong> tự gọi FCM/APNs/email/SMS (DEC-TRACK-33) - tầng NOTIF lo chọn kênh, rate-limit, fan-out.</li><li><strong>MUST</strong> bền vững với lỗi cục bộ: một luật lỗi khi đánh giá (vd thiếu dữ liệu median7) <strong>MUST NOT</strong> làm hỏng việc đánh giá các luật khác của cùng SKU; ghi log và tiếp tục. Nếu enqueue notification lỗi, giữ <code>alert.status='pending'</code> để retry, không mất dòng alert.</li><li><strong>SHOULD</strong> phát OTel: <code>alert_evaluated_total{rule_type}</code> (counter), <code>alert_fired_total{rule_type}</code> (counter), <code>alert_dedup_skipped_total{rule_type}</code> (counter), <code>alert_eval_duration_ms</code> (histogram).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao kích hoạt theo sự kiện written=true (DEC-TRACK-30)?</strong> Giá chỉ đổi ở những thời điểm rời rạc (delta-only của TASK-PRICE-002 chỉ ghi khi giá thật đổi). Đánh giá luật đúng vào lúc có dòng <code>price_snapshot</code> mới là tự nhiên và tiết kiệm: không cần cron quét định kỳ toàn bộ luật. <code>written=false</code> nghĩa giá không đổi -&gt; không có gì để đánh giá lại. Lọc theo <code>product_id</code> + <code>active</code> (partial index của TASK-TRACK-003) giữ mỗi sự kiện chỉ chạm đúng các luật liên quan.</p>\n<p><strong>Vì sao drop_pct lấy median 7 ngày làm mốc (DEC-TRACK-34)?</strong> \"Giảm 20%\" cần một mốc gốc. Lấy \"giá hôm qua\" thì nhiễu: một flash sale ngắn hôm qua làm mốc thấp giả, hôm nay giá thường lại trông như \"tăng\". Median 7 ngày (đọc từ <code>price_daily</code>) là mốc ổn định phản ánh mặt bằng giá gần đây, nên \"giảm X%\" có nghĩa thật. Đọc từ continuous aggregate cũng nhanh (bảng tổng hợp nhỏ).</p>\n<p><strong>Vì sao real_sale gọi TASK-DEAL-001 thay vì tự tính (DEC-TRACK-31)?</strong> Phân biệt sale thật/ảo là thuật toán thống kê riêng (median90, p10, trailing_min - §3.5) đã đặc tả ở TASK-DEAL-001. Engine alert không nên nhân bản logic đó; nó gọi <code>DetectFakeSale</code> và chỉ bắn khi <code>SALE_XIN</code>. Giữ một nguồn sự thật cho định nghĩa \"sale thật\".</p>\n<p><strong>Vì sao dedup rising-edge (DEC-TRACK-32, §1 #7, #8)?</strong> Trong một đợt flash sale, giá có thể nằm dưới ngưỡng suốt 30 phút với nhiều snapshot. Bắn một cảnh báo mỗi snapshot là tra tấn người dùng - họ nhận chục push cho cùng một deal rồi tắt thông báo. Rising-edge chỉ bắn ở khoảnh khắc điều kiện chuyển thành đúng (lần đầu giá chạm ngưỡng). Khi giá tăng lại rồi giảm lần nữa, đó là cạnh lên mới, đáng một cảnh báo mới - nên ta reset trạng thái khi điều kiện rời true.</p>\n<p><strong>Vì sao tạo alert rồi bàn giao NOTIF, không tự gửi (DEC-TRACK-33)?</strong> Gửi thông báo ở quy mô là bài toán riêng: fan-out qua Kafka/Redis Streams, rate-limit FCM 600k/phút, flatten-the-curve đỉnh 00:00, dead-letter, backoff (§3.6 - cả module NOTIF). Nếu engine tự gọi FCM, nó vượt qua mọi cơ chế đó, bắn thẳng lúc cao điểm và ăn 429. Engine chỉ làm phần nó biết (quyết định \"có nên cảnh báo\"), ghi <code>alert</code> làm bằng chứng, rồi đẩy cho NOTIF lo phần gửi. Hai trách nhiệm tách rời, mỗi tầng test được độc lập.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration (trạng thái dedup)</h3>\n<pre><code class=\"language-sql\">-- services/track/migrations/0004_alert_fired_state.sql\nCREATE TABLE alert_fired_state (\n  alert_rule_id      BIGINT      PRIMARY KEY REFERENCES alert_rule(id) ON DELETE CASCADE,\n  last_condition_met BOOLEAN     NOT NULL DEFAULT false,\n  last_fired_at      TIMESTAMPTZ\n);</code></pre>\n<h3>Đánh giá + dedup (Go)</h3>\n<pre><code class=\"language-go\">// services/track/internal/engine/evaluate.go\n\ntype Snapshot struct {\n    ProductID int64\n    Price     int64  // VND\n    ListPrice *int64\n}\n\n// EvaluateForProduct đánh giá mọi luật active của một SKU sau khi giá đổi (DEC-TRACK-30).\nfunc (e *Engine) EvaluateForProduct(ctx context.Context, snap Snapshot) error {\n    rules, err := e.rules.ActiveByProduct(ctx, snap.ProductID) // dùng idx_ar_eval\n    if err != nil {\n        return err\n    }\n    for _, r := range rules {\n        met, payload, err := e.conditionMet(ctx, r, snap)\n        if err != nil {\n            log.Warn(\"eval rule lỗi\", \"rule_id\", r.ID, \"err\", err) // §1 #11: không làm hỏng luật khác\n            continue\n        }\n        if err := e.fireIfRisingEdge(ctx, r, met, payload); err != nil {\n            log.Warn(\"fire lỗi\", \"rule_id\", r.ID, \"err\", err)\n        }\n    }\n    return nil\n}\n\nfunc (e *Engine) conditionMet(ctx context.Context, r AlertRule, s Snapshot) (bool, map[string]any, error) {\n    switch r.RuleType {\n    case \"price_below\":\n        met := s.Price &lt;= *r.Threshold\n        return met, map[string]any{\"price\": s.Price, \"threshold\": *r.Threshold}, nil\n    case \"drop_pct\":\n        ref, err := e.price.Median7d(ctx, s.ProductID) // price_daily (DEC-TRACK-34)\n        if err != nil {\n            return false, nil, err\n        }\n        limit := ref * (100 - *r.Threshold) / 100\n        return s.Price &lt;= limit, map[string]any{\"price\": s.Price, \"ref_price\": ref}, nil\n    case \"real_sale\":\n        verdict, err := e.deal.DetectFakeSale(ctx, s.ProductID, s.Price, s.ListPrice) // TASK-DEAL-001\n        if err != nil {\n            return false, nil, err\n        }\n        return verdict == deal.SaleXin, map[string]any{\"price\": s.Price, \"verdict\": verdict}, nil\n    case \"bottom_predicted\":\n        return false, nil, nil // DEC-TRACK-31: do TASK-DEAL-006 lo, engine này bỏ qua\n    default:\n        return false, nil, fmt.Errorf(\"rule_type lạ: %s\", r.RuleType)\n    }\n}</code></pre>\n<pre><code class=\"language-go\">// services/track/internal/engine/dedup.go\n\n// fireIfRisingEdge chỉ bắn khi điều kiện chuyển false-&gt;true (DEC-TRACK-32).\nfunc (e *Engine) fireIfRisingEdge(ctx context.Context, r AlertRule, met bool, payload map[string]any) error {\n    prev, err := e.state.LastConditionMet(ctx, r.ID) // mặc định false nếu chưa có\n    if err != nil {\n        return err\n    }\n    if met &amp;&amp; !prev {\n        alertID, err := e.handoff.CreateAndEnqueue(ctx, r, payload) // tạo alert pending + enqueue NOTIF\n        if err != nil {\n            return err\n        }\n        _ = alertID\n        return e.state.Set(ctx, r.ID, true) // ghi cạnh đã bắn\n    }\n    if !met &amp;&amp; prev {\n        return e.state.Set(ctx, r.ID, false) // reset cạnh (§1 #8) - cho phép cảnh báo lần sau\n    }\n    return nil // met&amp;&amp;prev (vẫn thỏa) hoặc !met&amp;&amp;!prev: không bắn\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>InsertSnapshot</code> trả <code>written=true</code> cho SKU -&gt; engine đánh giá các luật active của SKU đó; <code>written=false</code> -&gt; engine không chạy.</li><li>Engine chỉ lọc luật <code>(product_id, active=true)</code> (qua <code>idx_ar_eval</code>), không quét toàn bảng.</li><li><code>price_below threshold=89000</code>, giá mới <code>79000</code> -&gt; điều kiện met, tạo một <code>alert</code>.</li><li><code>drop_pct threshold=20</code>, median7 = <code>100000</code>, giá mới <code>75000</code> (giảm 25% &gt;= 20%) -&gt; met; giá <code>85000</code> (giảm 15%) -&gt; không met.</li><li><code>real_sale</code>: khi <code>DetectFakeSale</code> trả <code>SALE_XIN</code> -&gt; met; trả <code>SALE_AO</code>/<code>TAM_DUOC</code>/<code>UNKNOWN</code> -&gt; không met.</li><li><code>bottom_predicted</code> -&gt; engine bỏ qua, không tạo alert (TASK-DEAL-006 lo).</li><li>Rising-edge: giá nằm dưới ngưỡng qua 3 snapshot liên tiếp -&gt; chỉ một <code>alert</code> (cạnh lên đầu tiên), không ba.</li><li>Reset cạnh: giá lên trên ngưỡng (không met) rồi lại xuống dưới (met) -&gt; cảnh báo thứ hai được bắn (cạnh lên mới).</li><li>Mỗi <code>alert</code> tạo có <code>status='pending'</code> và <code>payload</code> JSONB chứa <code>price</code> + mốc tham chiếu phù hợp loại.</li><li>Engine bàn giao mỗi alert cho NOTIF (enqueue <code>notification</code> với <code>user_id</code> + <code>channel[]</code> + <code>alert_id</code>); engine KHÔNG tự gọi FCM/email/SMS (kiểm qua mock dispatcher không bị chạm).</li><li>Một luật lỗi khi đánh giá (median7 thiếu) không làm hỏng đánh giá luật khác cùng SKU; log được ghi.</li><li>Metric <code>alert_fired_total</code> + <code>alert_dedup_skipped_total</code> tăng đúng theo cạnh lên vs lần bị dedup.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/track/internal/engine/evaluate_test.go\nfunc TestPriceBelow_Met(t *testing.T) {\n    e, h := setupEngine(t)\n    rid := seedRule(t, e, \"price_below\", ptr(int64(89_000)))\n    require.NoError(t, e.EvaluateForProduct(ctx, Snapshot{ProductID: pid, Price: 79_000}))\n    require.Equal(t, 1, h.AlertCount(rid)) // tạo một alert\n    require.Equal(t, 1, h.EnqueueCount())  // bàn giao NOTIF\n    require.Equal(t, 0, h.DirectSendCount()) // KHÔNG tự gửi (DEC-TRACK-33)\n}\n\nfunc TestDropPct_Median7Reference(t *testing.T) {\n    e, _ := setupEngineWithMedian(t, 100_000) // median7 = 100k\n    seedRule(t, e, \"drop_pct\", ptr(int64(20)))\n    metLow := e.evalOnce(t, Snapshot{ProductID: pid, Price: 75_000})  // -25%\n    metHigh := e.evalOnce(t, Snapshot{ProductID: pid, Price: 85_000}) // -15%\n    require.True(t, metLow)\n    require.False(t, metHigh)\n}\n\nfunc TestRealSale_OnlyOnSaleXin(t *testing.T) {\n    e, _ := setupEngine(t)\n    seedRule(t, e, \"real_sale\", nil)\n    e.deal.SetVerdict(deal.SaleXin)\n    require.True(t, e.evalOnce(t, Snapshot{ProductID: pid, Price: 79_000}))\n    e.deal.SetVerdict(deal.SaleAo)\n    require.False(t, e.evalOnce(t, Snapshot{ProductID: pid, Price: 79_000}))\n}\n\nfunc TestBottomPredicted_Skipped(t *testing.T) {\n    e, h := setupEngine(t)\n    rid := seedRule(t, e, \"bottom_predicted\", nil)\n    e.EvaluateForProduct(ctx, Snapshot{ProductID: pid, Price: 1})\n    require.Equal(t, 0, h.AlertCount(rid)) // engine này bỏ qua (TASK-DEAL-006 lo)\n}\n\n// services/track/internal/engine/dedup_test.go\nfunc TestRisingEdge_NoSpam(t *testing.T) {\n    e, h := setupEngine(t)\n    rid := seedRule(t, e, \"price_below\", ptr(int64(89_000)))\n    for i := 0; i &lt; 3; i++ { // ba snapshot đều dưới ngưỡng\n        e.EvaluateForProduct(ctx, Snapshot{ProductID: pid, Price: 79_000})\n    }\n    require.Equal(t, 1, h.AlertCount(rid)) // chỉ một alert, không ba\n}\n\nfunc TestRisingEdge_ResetAllowsRefire(t *testing.T) {\n    e, h := setupEngine(t)\n    rid := seedRule(t, e, \"price_below\", ptr(int64(89_000)))\n    e.EvaluateForProduct(ctx, Snapshot{ProductID: pid, Price: 79_000}) // met -&gt; bắn 1\n    e.EvaluateForProduct(ctx, Snapshot{ProductID: pid, Price: 99_000}) // lên trên -&gt; reset\n    e.EvaluateForProduct(ctx, Snapshot{ProductID: pid, Price: 79_000}) // met lại -&gt; bắn 2\n    require.Equal(t, 2, h.AlertCount(rid))\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration <code>0004_alert_fired_state.sql</code> -&gt; <code>evaluate.go</code> (bốn nhánh rule_type) -&gt; <code>dedup.go</code> (rising-edge + reset) -&gt; <code>handoff.go</code> (tạo alert pending + enqueue NOTIF) -&gt; đăng ký consumer sự kiện price-written trong <code>main.go</code> -&gt; tests. Engine chạy như consumer của sự kiện do TASK-PRICE-002 phát sau <code>InsertSnapshot</code> (in-process hook hoặc qua hàng đợi nội bộ). <code>Median7d</code> đọc <code>price_daily</code>. <code>DetectFakeSale</code> gọi package <code>deal</code> (TASK-DEAL-001). <code>CreateAndEnqueue</code> ghi <code>alert</code> rồi đẩy <code>notification</code> request - không có lời gọi FCM/SMS nào trong package <code>engine</code>.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-TRACK-003</strong> - cung cấp <code>alert_rule</code> (đọc qua <code>idx_ar_eval</code>) và bảng <code>alert</code> (engine ghi dòng <code>status=pending</code>).</li><li><strong>TASK-PRICE-002</strong> - cung cấp sự kiện <code>written=true</code>, <code>price_snapshot</code> (giá hiện tại), <code>price_daily</code> (median 7 ngày cho drop_pct).</li><li><strong>TASK-NOTIF-001</strong> - nhận bàn giao: engine enqueue <code>notification</code> request; NOTIF lo chọn kênh + fan-out + rate-limit.</li><li><strong>TASK-DEAL-001</strong> - <code>DetectFakeSale</code> cho nhánh <code>real_sale</code>.</li><li><strong>TASK-DEAL-006 (ranh giới)</strong> - lo <code>bottom_predicted</code> ở batch đêm; engine này cố ý bỏ qua loại đó để tránh bắn trùng.</li><li>Lib: <code>pgx</code>, <code>encoding/json</code> (payload JSONB).</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>notification request engine đẩy cho TASK-NOTIF-001 (nội bộ)</h3>\n<pre><code class=\"language-json\">{\n  \"user_id\": 4021,\n  \"channel\": [\"push\", \"email\"],\n  \"alert_id\": 88123,\n  \"template\": \"price_below\",\n  \"data\": { \"price\": 79000, \"threshold\": 89000, \"product_id\": 90112 }\n}</code></pre>\n<h3>Dòng alert engine tạo</h3>\n<pre><code class=\"language-sql\">SELECT id, alert_rule_id, fired_at, status, payload\nFROM alert WHERE alert_rule_id = 5012 ORDER BY fired_at DESC LIMIT 1;\n-- 88123 | 5012 | 2026-06-27 12:00:05+07 | pending | {\"price\":79000,\"threshold\":89000}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Cửa sổ cooldown thời gian (vd không bắn lại trong 6 giờ kể cả khi reset cạnh) - thêm nếu rising-edge vẫn quá ồn với SKU dao động mạnh; đo trước.</li><li>Đánh giá batch khi nhiều SKU đổi giá cùng lúc (gom theo product_id) - tối ưu throughput giai đoạn sau.</li><li>Mốc tham chiếu drop_pct cấu hình được (7d/14d/30d) - giữ 7d cho slice này.</li><li>Gộp nhiều alert của một user trong khoảng ngắn thành một digest - thuộc NOTIF, không phải engine.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Đánh giá khi giá không đổi</td><td>chỉ chạy khi written=true</td><td>bỏ qua</td><td>Hook theo sự kiện delta-only (DEC-TRACK-30)</td></tr><tr><td>Quét toàn bảng alert_rule</td><td>EXPLAIN</td><td>chậm khi nhiều luật</td><td>Lọc product_id + active qua idx_ar_eval</td></tr><tr><td>Spam mỗi snapshot</td><td>rising-edge</td><td>một cảnh báo/cạnh lên</td><td>last_condition_met (DEC-TRACK-32)</td></tr><tr><td>Không cảnh báo lại sau khi deal quay lại</td><td>reset cạnh</td><td>cạnh lên mới bắn được</td><td>Set false khi điều kiện rời true (§1 #8)</td></tr><tr><td>Engine tự gọi FCM</td><td>code review + mock</td><td>vượt rate-limit, trùng logic</td><td>Chỉ enqueue NOTIF (DEC-TRACK-33)</td></tr><tr><td>median7 thiếu (SKU mới)</td><td>nhánh trả lỗi</td><td>luật drop_pct skip, log</td><td>Không làm hỏng luật khác (§1 #11)</td></tr><tr><td>bottom_predicted bắn trùng</td><td>engine bỏ qua</td><td>tránh trùng TASK-DEAL-006</td><td>DEC-TRACK-31</td></tr><tr><td>Enqueue NOTIF lỗi</td><td>alert.status=pending</td><td>giữ để retry</td><td>Không mất dòng alert (§1 #11)</td></tr><tr><td>Race hai snapshot cùng SKU</td><td>đánh giá tuần tự theo rule</td><td>trạng thái nhất quán</td><td>Khóa trên alert_fired_state nếu cần</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Engine là mắt xích \"quyết định có nên cảnh báo\"; việc gửi tách hẳn sang NOTIF (TASK-NOTIF-001..007).</li><li>Kích hoạt theo sự kiện <code>written=true</code> ăn khớp với delta-only của TASK-PRICE-002: chỉ đánh giá đúng lúc giá thật đổi, không cron quét.</li><li>Dedup rising-edge là rào chống spam quan trọng nhất - bắn ở khoảnh khắc chạm ngưỡng, không bắn lại suốt thời gian còn thỏa; reset khi rời ngưỡng cho phép cảnh báo lần sau.</li><li>drop_pct lấy median 7 ngày (price_daily) làm mốc để \"giảm X%\" có nghĩa ổn định, tránh nhiễu flash sale ngắn.</li><li>real_sale tái dùng <code>DetectFakeSale</code> của TASK-DEAL-001 - một nguồn sự thật cho định nghĩa sale thật, engine không nhân bản.</li><li>Tạo <code>alert</code> (bằng chứng, status pending) rồi bàn giao NOTIF giữ ranh giới rõ: engine test được độc lập với tầng gửi.</li></ul>\n<hr>\n<p><em>Hết TASK-TRACK-004. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
