window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SCRAPE-006"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Module health <strong>MUST</strong> theo dõi tỷ lệ parse-failure theo cửa sổ trượt, suy ra trạng thái adapter, alert khi đột biến, và điều khiển orchestrator hạ tải khi broken. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> đếm outcome mỗi lần quét theo cửa sổ trượt per <code>(platform_id, adapter_version)</code> (DEC-SCRAPE-24): phân loại <code>success | parse_fail | challenge | network_err</code>. <code>parse_fail</code> là trọng tâm (selector drift / schema đổi).</li><li><strong>MUST</strong> tính baseline động: tỷ lệ <code>parse_fail</code> \"bình thường\" được ước lượng từ lịch sử gần (cửa sổ dài), KHÔNG dùng hằng số tuyệt đối cố định (mùa sale, traffic biến động làm nền thay đổi).</li><li><strong>MUST</strong> suy ra health state qua state machine (DEC-SCRAPE-26): <code>healthy -&gt; degraded -&gt; broken</code>:</li></ol>\n<ul><li><code>healthy</code>: tỷ lệ parse_fail gần baseline.</li><li><code>degraded</code>: parse_fail vượt baseline đáng kể nhưng chưa toàn diện.</li><li><code>broken</code>: phần lớn request parse_fail (sàn đã đổi DOM/schema). Chuyển trạng thái <strong>MUST</strong> có hysteresis (ngưỡng lên khác ngưỡng xuống) để không nhấp nháy.</li></ul>\n<ol><li><strong>MUST</strong> phát alert khi vượt ngưỡng đột biến (DEC-SCRAPE-25): khi state chuyển sang <code>degraded</code> hoặc <code>broken</code>, gửi alert qua observability (TASK-INFRA-004) kèm <code>(platform, adapter_version, fail_rate, sample_count)</code>.</li><li><strong>MUST</strong> dedup + cooldown alert: một adapter chuyển <code>broken</code> chỉ alert một lần trong cửa sổ cooldown, KHÔNG spam mỗi request hỏng.</li><li><strong>MUST</strong> điều khiển orchestrator hạ tải khi <code>broken</code> (DEC-SCRAPE-27): expose <code>ShouldThrottle(platformID, adapterVersion) (bool, factor)</code>; khi broken, orchestrator giảm tần suất quét target đó (tăng <code>next_run_at</code>) để không đốt proxy vào parse hỏng hàng loạt.</li><li><strong>MUST</strong> yêu cầu số mẫu tối thiểu trước khi đổi state: không tuyên bố <code>broken</code> chỉ vì 2-3 request đầu fail (tránh báo động giả từ mẫu nhỏ).</li><li><strong>MUST</strong> tự hồi phục: khi parse_fail trở lại gần baseline (sau khi adapter được sửa/sàn revert), state quay về <code>healthy</code> và bỏ throttle.</li><li><strong>MUST</strong> phân biệt <code>parse_fail</code> (lỗi của ta - selector drift, đáng sửa adapter) với <code>challenge</code>/<code>network_err</code> (lỗi môi trường - CAPTCHA, proxy) để alert đúng nguyên nhân; chỉ <code>parse_fail</code> cao mới báo \"sàn đổi DOM\".</li><li><strong>SHOULD</strong> phát OTel metric: <code>adapter_parse_fail_rate{platform, version}</code> (gauge), <code>adapter_health_state{platform, version}</code> (gauge enum), <code>adapter_alert_total{platform, transition}</code> (counter).</li><li><strong>MUST</strong> an toàn đồng thời: nhiều worker báo outcome song song vào cùng window không gây race (đếm có khóa/atomic).</li><li><strong>SHOULD</strong> giữ lịch sử chuyển trạng thái (state transition log) đủ để điều tra \"adapter Shopee hỏng lúc nào, tự hồi phục hay người sửa\".</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao baseline động (DEC-SCRAPE-24)?</strong> Tỷ lệ fail \"bình thường\" không cố định. Mùa sale traffic tăng, một số SKU bị gỡ, sàn đôi khi chậm - nền nhiễu thay đổi. Ngưỡng tuyệt đối cố định sẽ hoặc quá nhạy (báo động giả mùa cao điểm) hoặc quá điếc (bỏ lỡ drift khi nền thấp). Baseline ước lượng từ lịch sử gần cho ngưỡng tự điều chỉnh.</p>\n<p><strong>Vì sao state machine có hysteresis (§1 #3)?</strong> Tỷ lệ fail dao động quanh ngưỡng sẽ làm state nhấp nháy healthy/degraded liên tục nếu dùng một ngưỡng. Hysteresis (lên ở ngưỡng cao, xuống ở ngưỡng thấp hơn) cho trạng thái ổn định - chỉ chuyển khi tín hiệu rõ ràng, không phản ứng với nhiễu.</p>\n<p><strong>Vì sao hạ tải khi broken (DEC-SCRAPE-27)?</strong> Khi Shopee đổi DOM, mọi request tới adapter cũ đều parse_fail. Quét full tần suất nghĩa là đốt proxy (§4.1) vào hàng nghìn request chắc chắn hỏng cho tới khi người sửa adapter. Hạ tải target broken giữ một nhịp tối thiểu (để biết khi nào sàn revert) nhưng không lãng phí băng thông vào thất bại đã biết.</p>\n<p><strong>Vì sao số mẫu tối thiểu (§1 #7)?</strong> 2-3 request đầu fail có thể là ngẫu nhiên (một SKU gỡ, một timeout). Tuyên bố <code>broken</code> từ mẫu nhỏ là báo động giả, kéo theo throttle nhầm. Đợi đủ mẫu cho tín hiệu đáng tin trước khi hành động.</p>\n<p><strong>Vì sao phân biệt parse_fail với challenge/network (§1 #9)?</strong> Ba loại lỗi cần ba phản ứng khác nhau. <code>challenge</code> cao -&gt; việc của TASK-SCRAPE-005 (CAPTCHA) và TASK-SCRAPE-004 (proxy). <code>network_err</code> cao -&gt; hạ tầng. Chỉ <code>parse_fail</code> cao mới nghĩa \"sàn đổi DOM, cần người sửa adapter\". Gộp chung làm alert sai địa chỉ, người trực sửa nhầm chỗ.</p>\n<p><strong>Vì sao tự hồi phục (§1 #8)?</strong> Sàn đôi khi revert A/B test, hoặc người vừa deploy adapter mới. State phải tự thấy parse_fail trở lại bình thường và bỏ throttle - không bắt người gỡ cờ thủ công, tránh quên để target chạy nửa công suất mãi.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Rolling window (Go)</h3>\n<pre><code class=\"language-go\">// services/scrape/internal/health/window.go\ntype Outcome int\nconst (\n    OutcomeSuccess Outcome = iota\n    OutcomeParseFail\n    OutcomeChallenge\n    OutcomeNetworkErr\n)\n\ntype Window struct {\n    mu      sync.Mutex\n    samples []Outcome   // ring buffer cửa sổ trượt\n}\n\n// Record thêm outcome; thread-safe (nhiều worker song song).\nfunc (w *Window) Record(o Outcome) { /* khóa + ring buffer */ }\n\n// ParseFailRate trả tỷ lệ parse_fail hiện tại và số mẫu.\nfunc (w *Window) ParseFailRate() (rate float64, n int)</code></pre>\n<h3>Health state machine (Go)</h3>\n<pre><code class=\"language-go\">// services/scrape/internal/health/state.go\ntype Health int\nconst (\n    Healthy Health = iota\n    Degraded\n    Broken\n)\n\nconst minSamples = 30 // số mẫu tối thiểu trước khi đổi state (§1 #7)\n\n// Next suy ra state mới từ tỷ lệ fail, baseline, và state hiện tại (có hysteresis).\nfunc Next(cur Health, failRate, baseline float64, n int) Health {\n    if n &lt; minSamples {\n        return cur // chưa đủ mẫu, giữ nguyên\n    }\n    up := baseline + 0.25   // ngưỡng lên\n    upHard := 0.70          // phần lớn fail -&gt; broken\n    down := baseline + 0.10 // ngưỡng xuống (hysteresis)\n    switch {\n    case failRate &gt;= upHard:\n        return Broken\n    case failRate &gt;= up:\n        return Degraded\n    case failRate &lt;= down:\n        return Healthy\n    default:\n        return cur // vùng hysteresis -&gt; giữ nguyên\n    }\n}</code></pre>\n<h3>Throttle control (Go)</h3>\n<pre><code class=\"language-go\">// services/scrape/internal/health/monitor.go\n// ShouldThrottle: khi adapter broken, orchestrator giảm tần suất quét target.\nfunc (m *Monitor) ShouldThrottle(platformID int16, version string) (bool, float64) {\n    switch m.stateOf(platformID, version) {\n    case Broken:\n        return true, 0.1   // còn ~10% tần suất để dò sàn revert\n    case Degraded:\n        return true, 0.5\n    default:\n        return false, 1.0\n    }\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>Window.Record</code> đếm đúng outcome; <code>ParseFailRate</code> trả tỷ lệ và số mẫu chính xác.</li><li>Baseline được tính động từ cửa sổ dài, không phải hằng số (đổi khi nền fail đổi).</li><li><code>Next</code> giữ state khi <code>n &lt; minSamples</code> (không đổi vì mẫu nhỏ).</li><li>Tỷ lệ parse_fail vượt <code>baseline + 0.25</code> (đủ mẫu) -&gt; <code>Degraded</code>; vượt 0.70 -&gt; <code>Broken</code>.</li><li>Hysteresis: từ <code>Degraded</code>, tỷ lệ trong vùng giữa <code>down</code> và <code>up</code> -&gt; giữ <code>Degraded</code> (không nhấp nháy).</li><li>Tỷ lệ fail trở lại &lt;= <code>baseline + 0.10</code> -&gt; <code>Healthy</code> (tự hồi phục).</li><li>Chuyển sang <code>Degraded</code>/<code>Broken</code> -&gt; phát đúng một alert qua observability với <code>(platform, version, fail_rate, n)</code>.</li><li>Cùng adapter ở <code>Broken</code> không alert lại trong cooldown (dedup).</li><li><code>ShouldThrottle</code> trả <code>(true, 0.1)</code> khi <code>Broken</code>; <code>(false, 1.0)</code> khi <code>Healthy</code>.</li><li><code>parse_fail</code> cao -&gt; alert \"sàn đổi DOM\"; <code>challenge</code> cao (không phải parse_fail) -&gt; KHÔNG alert DOM (phân loại đúng).</li><li>Nhiều worker <code>Record</code> song song không gây race (chạy với <code>-race</code>).</li><li>Metric <code>adapter_parse_fail_rate</code>, <code>adapter_health_state</code>, <code>adapter_alert_total</code> thay đổi đúng theo chuyển trạng thái.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/scrape/internal/health/window_test.go\nfunc TestWindow_RateAndCount(t *testing.T) {\n    w := newWindow(100)\n    for i := 0; i &lt; 30; i++ { w.Record(OutcomeSuccess) }\n    for i := 0; i &lt; 10; i++ { w.Record(OutcomeParseFail) }\n    rate, n := w.ParseFailRate()\n    require.Equal(t, 40, n)\n    require.InDelta(t, 0.25, rate, 0.01)\n}\n\nfunc TestWindow_RaceSafe(t *testing.T) {\n    w := newWindow(1000)\n    var wg sync.WaitGroup\n    for g := 0; g &lt; 8; g++ {\n        wg.Add(1)\n        go func() { defer wg.Done(); for i := 0; i &lt; 200; i++ { w.Record(OutcomeSuccess) } }()\n    }\n    wg.Wait()\n    _, n := w.ParseFailRate()\n    require.Equal(t, 1000, n) // không mất bản ghi do race\n}</code></pre>\n<pre><code class=\"language-go\">// services/scrape/internal/health/state_test.go\nfunc TestNext_SpikeToBroken(t *testing.T) {\n    require.Equal(t, Broken, Next(Healthy, 0.85, 0.05, 100))\n}\n\nfunc TestNext_MinSamplesGuard(t *testing.T) {\n    require.Equal(t, Healthy, Next(Healthy, 0.90, 0.05, 5)) // mẫu nhỏ -&gt; giữ nguyên\n}\n\nfunc TestNext_Hysteresis_NoFlicker(t *testing.T) {\n    // vùng giữa down(0.15) và up(0.30) với baseline 0.05 -&gt; giữ Degraded\n    require.Equal(t, Degraded, Next(Degraded, 0.20, 0.05, 100))\n}\n\nfunc TestNext_Recovers(t *testing.T) {\n    require.Equal(t, Healthy, Next(Broken, 0.08, 0.05, 100)) // trở lại &lt;= baseline+0.10\n}</code></pre>\n<pre><code class=\"language-go\">// services/scrape/internal/health/monitor_test.go\nfunc TestMonitor_AlertDedup(t *testing.T) {\n    m, sink := newMonitor(t)\n    feedFails(t, m, 1, \"v1\", 100) // đẩy lên broken\n    feedFails(t, m, 1, \"v1\", 100) // vẫn broken\n    require.Equal(t, 1, sink.alertCount) // chỉ alert một lần trong cooldown\n}\n\nfunc TestMonitor_ParseFailNotChallenge(t *testing.T) {\n    m, sink := newMonitor(t)\n    feedOutcome(t, m, 1, \"v1\", OutcomeChallenge, 100) // challenge cao, parse_fail thấp\n    require.Zero(t, sink.domAlerts) // không báo \"sàn đổi DOM\"\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: window.go (ring buffer thread-safe, test trước) -&gt; state.go (Next thuần với hysteresis) -&gt; monitor.go (ghép window + state + baseline động) -&gt; alert.go (dedup + cooldown) -&gt; tích hợp pool.go (báo outcome + đọc ShouldThrottle) -&gt; tests. Alert đi qua observability của TASK-INFRA-004. Orchestrator (TASK-SCRAPE-001) đọc <code>ShouldThrottle</code> để nhân <code>next_run_at</code> khi target broken.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-SCRAPE-002 / 007 / 008</strong> - adapter báo outcome (success/parse_fail) vào monitor.</li><li><strong>TASK-SCRAPE-001</strong> - orchestrator đọc <code>ShouldThrottle</code> để hạ tải target broken.</li><li><strong>TASK-SCRAPE-005</strong> - <code>challenge</code> outcome đến từ CAPTCHA detect; monitor phân biệt với parse_fail.</li><li><strong>TASK-INFRA-004</strong> - alert đi qua observability spine (Prometheus/Grafana/log).</li><li>§5.2 - giám sát DOM là mitigation cho rủi ro phụ thuộc nền tảng existential.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Adapter báo outcome (nội bộ)</h3>\n<pre><code class=\"language-go\">// trong adapter sau khi parse\nif err != nil {\n    monitor.Report(job.PlatformID, adapterVersion, health.OutcomeParseFail)\n} else {\n    monitor.Report(job.PlatformID, adapterVersion, health.OutcomeSuccess)\n}</code></pre>\n<h3>Alert khi Shopee đổi DOM (gửi qua observability)</h3>\n<pre><code class=\"language-json\">{\n  \"alert\": \"adapter_broken\",\n  \"platform\": \"shopee\",\n  \"adapter_version\": \"v1.4.2\",\n  \"parse_fail_rate\": 0.91,\n  \"sample_count\": 240,\n  \"baseline\": 0.04,\n  \"action\": \"throttled_to_10pct\",\n  \"hint\": \"Shopee có thể đã đổi DOM/JSON schema (A/B test §3.2) - kiểm tra selectors/struct\"\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Tự động fallback sang adapter version cũ/mới khi broken - hiện chỉ alert + throttle, người sửa; tự chuyển để sau.</li><li>Học baseline theo mùa (double-date có nền fail cao hơn) - bắt đầu baseline trượt đơn giản, thêm seasonality sau.</li><li>Phát hiện drift ở cấp trường (giá vẫn parse được nhưng stock thì không) - hiện coi parse_fail nhị phân, tinh chỉnh sau.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Sàn đổi DOM (Shopee A/B)</td><td>parse_fail spike</td><td>dữ liệu giá chết</td><td>Alert + throttle (§1 #4,#6)</td></tr><tr><td>Ngưỡng tuyệt đối cố định</td><td>báo động giả mùa sale</td><td>nhiễu/điếc</td><td>Baseline động (§1 #2)</td></tr><tr><td>State nhấp nháy</td><td>log transition</td><td>alert spam</td><td>Hysteresis (§1 #3)</td></tr><tr><td>Báo broken từ mẫu nhỏ</td><td><code>MinSamplesGuard</code> test</td><td>throttle nhầm</td><td>minSamples=30 (§1 #7)</td></tr><tr><td>Quét full khi broken</td><td><code>proxy_cost</code> không giảm</td><td>đốt proxy</td><td>ShouldThrottle 0.1 (§1 #6)</td></tr><tr><td>Alert spam mỗi request</td><td>dedup test</td><td>nhiễu cho người trực</td><td>Cooldown + dedup (§1 #5)</td></tr><tr><td>challenge gây alert DOM sai</td><td><code>ParseFailNotChallenge</code></td><td>sửa nhầm chỗ</td><td>Phân loại outcome (§1 #9)</td></tr><tr><td>Race khi nhiều worker Record</td><td><code>-race</code> test</td><td>mất bản ghi</td><td>Khóa/atomic (§1 #11)</td></tr><tr><td>Không tự hồi phục</td><td>giữ throttle mãi</td><td>nửa công suất</td><td>Tự về Healthy (§1 #8)</td></tr><tr><td>Monitor không nhận outcome</td><td>metric phẳng</td><td>mù</td><td>Pool báo mọi outcome (§3 §8)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Đây là hệ thần kinh cảm giác của farm: phát hiện sớm khi sàn đổi DOM/schema để người sửa adapter trước khi dữ liệu thối.</li><li>Rủi ro phụ thuộc nền tảng là existential (§5.2); giám sát DOM + đa sàn là hai mitigation chính, task này lo phần giám sát.</li><li>Phân biệt parse_fail (lỗi của ta, sửa adapter) với challenge/network (lỗi môi trường) để alert đúng địa chỉ - khác biệt quan trọng cho người trực.</li><li>Hạ tải target broken bảo vệ unit economics (§4.1): không đốt proxy vào hàng nghìn request chắc chắn parse_fail.</li><li>Baseline động + hysteresis + số mẫu tối thiểu là ba lớp chống báo động giả; tự hồi phục tránh để target chạy nửa công suất vì quên gỡ cờ.</li></ul>\n<hr>\n<p><em>Hết TASK-SCRAPE-006. Status: done (mục tiêu audit 10/10).</em></p>";
