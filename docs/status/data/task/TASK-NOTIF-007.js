window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-NOTIF-007"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Dispatcher SMS <strong>MUST</strong> gửi tin nhắn SMS tới người dùng VN qua một nhà cung cấp nội địa (SpeedSMS / eSMS-ViHAT / VietGuys / Mobifone) với Twilio làm fallback, là consumer downstream của fan-out (TASK-NOTIF-003): nó nhặt các dòng <code>notification</code> có <code>channel='sms'</code> đang <code>status='queued'</code>, gửi, rồi cập nhật <code>status</code> -&gt; <code>sent</code>/<code>failed</code> + <code>sent_at</code>. Vì SMS là kênh đắt nhất trong cost model (push &gt; email &gt; sms), dispatcher <strong>MUST</strong> guard để chỉ gửi cho thông báo high-value/OTP. Dispatcher <strong>MUST NOT</strong> routing kênh và <strong>MUST NOT</strong> render lại nội dung (việc của TASK-NOTIF-001). Hợp đồng:</p>\n<ol><li><strong>MUST</strong> định nghĩa interface <code>SMSProvider</code> pluggable (DEC-NOTIF-70) với phương thức <code>Send(ctx, msg SMSMessage) (SMSResult, error)</code>, để SpeedSMS / eSMS / VietGuys / Mobifone / Twilio đều cài đặt được và hoán đổi qua cấu hình mà không sửa dispatcher.</li><li><strong>MUST</strong> dùng nhà cung cấp nội địa VN làm <strong>sơ cấp</strong> (DEC-NOTIF-71): SpeedSMS / eSMS (ViHAT) / VietGuys / Mobifone, giá nội địa ~<code>200-500 VND/tin</code> tùy nhà mạng. Twilio (~<code>$0,1552/SMS</code> tới VN, đắt gấp hàng chục lần) <strong>MUST</strong> chỉ đóng vai fallback.</li><li><strong>MUST</strong> kích hoạt Twilio fallback khi provider VN sơ cấp trả lỗi tạm thời hoặc không gửi được (DEC-NOTIF-71), VÀ chỉ khi dòng <code>notification</code> là OTP/high-value (Twilio đắt, không dùng cho khối lượng lớn). Nếu provider VN thành công thì <strong>MUST NOT</strong> gọi Twilio.</li><li><strong>MUST</strong> gửi với sender ID là một <strong>brandname</strong> đã đăng ký/khai báo với \"Cục An toàn thông tin\" (DEC-NOTIF-72). Dispatcher <strong>MUST NOT</strong> gửi với brandname chưa đăng ký - nhà mạng VN chặn hoặc đánh spam tin chưa khai báo. Brandname duy trì phí ~<code>50.000 VND/tháng/nhà mạng</code>.</li><li><strong>MUST</strong> guard chi phí (DEC-NOTIF-73): trước khi gửi, dispatcher <strong>MUST</strong> assert dòng <code>notification</code> là high-value/OTP. Routing của TASK-NOTIF-001 đã đảm bảo <code>channel='sms'</code> chỉ xuất hiện cho high-value/OTP hoặc khi push+email không khả dụng, nhưng dispatcher <strong>MUST</strong> kiểm lại (defense-in-depth) và <strong>MUST</strong> từ chối + <code>MarkFailed</code> nếu một dòng thường lọt xuống đây. SMS <strong>MUST NOT</strong> được gửi cho thông báo giá thông thường khi push/email là lựa chọn rẻ hơn.</li><li><strong>MUST</strong> dựng <code>SMSMessage</code> đúng cấu trúc: <code>to</code> (số điện thoại VN dạng E.164 hoặc nội địa theo yêu cầu provider), <code>body</code> (đã render bởi TASK-NOTIF-001, plaintext, không markup), <code>brandname</code> (sender ID đã đăng ký), và cờ <code>highValue</code> mang từ dòng <code>notification</code>.</li><li><strong>MUST</strong> lấy số điện thoại đích từ <code>user_channel_token</code> của TASK-NOTIF-001: bản ghi <code>channel='sms'</code>, <code>verified=true</code>. Số chưa verified <strong>MUST NOT</strong> được gửi tới (gửi SMS tới số chưa xác minh có thể sai người + tốn tiền vô ích).</li><li><strong>MUST</strong> phân loại phản hồi provider thành ba nhóm và hành xử đúng (mirror TASK-NOTIF-002 §1 #6):</li></ol>\n<ul><li>Thành công (provider chấp nhận, có message id) -&gt; <code>MarkSent</code>: <code>status='sent'</code>, <code>sent_at=now()</code>.</li><li>Lỗi tạm thời (5xx, timeout, hết số dư tạm, rate-limit provider) -&gt; thử Twilio fallback (nếu high-value/OTP) hoặc retry; vượt số lần thử thì <code>MarkFailed</code> để dead-letter (TASK-NOTIF-003).</li><li>Lỗi vĩnh viễn (số sai định dạng, brandname bị từ chối, nội dung vi phạm) -&gt; <code>MarkFailed</code>, không retry.</li></ul>\n<ol><li><strong>MUST</strong> nhặt việc an toàn khi chạy nhiều worker song song: <code>ClaimSMSBatch</code> dùng <code>SELECT ... FOR UPDATE SKIP LOCKED</code> trên <code>notification</code> (<code>channel='sms'</code>, <code>status='queued'</code>) để hai worker không gửi trùng (DEC-NOTIF-74), tái dùng pattern của TASK-NOTIF-002.</li><li><strong>MUST</strong> idempotent ở mức hợp lý: một dòng <code>notification</code> đã <code>status='sent'</code> <strong>MUST NOT</strong> bị gửi lại; <code>ClaimSMSBatch</code> chỉ lấy <code>status='queued'</code>, <code>MarkSent</code>/<code>MarkFailed</code> là cập nhật có điều kiện trên trạng thái hiện tại. SMS gửi trùng vừa tốn tiền vừa phiền người dùng.</li><li><strong>SHOULD</strong> phát OTel metric: <code>sms_send_total{provider, result}</code> (counter: sent|retry|failed), <code>sms_fallback_twilio_total</code> (counter - đếm khi phải dùng Twilio), <code>sms_guard_rejected_total</code> (counter - đếm khi guard từ chối dòng không high-value), <code>sms_cost_estimate_vnd_total{provider}</code> (counter - ước lượng chi phí để giám sát), <code>sms_send_duration_ms</code> (histogram).</li><li><strong>MUST</strong> giữ ranh giới trách nhiệm: dispatcher chỉ gửi SMS cho dòng đã được TASK-NOTIF-001 chọn kênh và render. Nó <strong>MUST NOT</strong> tự chọn kênh, <strong>MUST NOT</strong> đổi nội dung, và <strong>MUST NOT</strong> gọi FCM/APNs/email provider.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao SMS chỉ cho high-value/OTP và phải guard (DEC-NOTIF-73)?</strong> §3.6 xếp SMS là kênh đắt nhất: push (FCM/APNs) gần như miễn phí, email rất rẻ, còn SMS nội địa VN ~200-500 VND/tin và qua Twilio ~$0,1552/SMS (khoảng 4.000 VND). Với mô hình free-tier tài trợ bằng affiliate, mỗi tin SMS là biến phí trực tiếp đắt gấp hàng nghìn lần push. Routing TASK-NOTIF-001 đã chỉ chọn SMS cho high-value/OTP, nhưng dispatcher vẫn kiểm lại vì một lỗi routing hoặc một dòng dựng sai đi nhầm SMS lúc đỉnh 00:00 (hàng chục nghìn alert) đủ đốt sạch ngân sách thông báo cả tháng. Guard là cái van cuối cùng: thà từ chối một dòng đáng ngờ còn hơn để chi phí trượt.</p>\n<p><strong>Vì sao nhà cung cấp VN sơ cấp + Twilio fallback (DEC-NOTIF-71)?</strong> Cùng một tin SMS tới số VN, nhà cung cấp nội địa (SpeedSMS, eSMS, VietGuys, Mobifone) tính ~200-500 VND, còn Twilio tính ~$0,1552 - đắt gấp hàng chục lần. Với khối lượng OTP/alert hằng ngày, gửi sơ cấp qua provider VN giữ chi phí ở sàn. Nhưng provider VN đôi khi lỗi (hết số dư, rate-limit, sự cố), và OTP đăng nhập thì không được rớt. Twilio làm fallback cho đúng các tin đáng (OTP/high-value) khi provider VN fail: đắt nhưng độ tin cậy cao, và chỉ cho phần nhỏ tràn nên tổng chi phí vẫn thấp. Hai lớp bổ trợ: rẻ làm chính, đắt làm lưới an toàn.</p>\n<p><strong>Vì sao brandname phải đăng ký với Cục An toàn thông tin (DEC-NOTIF-72)?</strong> Ở VN, gửi SMS quảng cáo/thông báo với một sender ID (brandname) phải khai báo trước với Cục An toàn thông tin; tin từ brandname chưa đăng ký bị nhà mạng chặn hoặc đánh spam, nghĩa là tin không tới và có thể bị phạt. Brandname còn là uy tín thương hiệu: người dùng thấy \"SănDeal\" thay vì một đầu số lạ thì tin tưởng hơn, chống lừa đảo giả mạo. Phí duy trì ~50.000 VND/tháng/nhà mạng là chi phí cố định nhỏ đổi lấy khả năng gửi hợp lệ và uy tín.</p>\n<p><strong>Vì sao SMSProvider là interface pluggable (DEC-NOTIF-70)?</strong> Thị trường SMS VN có nhiều nhà cung cấp với giá, độ tin cậy và API khác nhau (SpeedSMS, eSMS, VietGuys, Mobifone), và ta có thể cần đổi nhà cung cấp theo giá hợp đồng hoặc cộng thêm nhà cung cấp để phân tải. Đặt <code>SMSProvider</code> là interface cho phép hoán đổi provider qua cấu hình mà không sửa dispatcher; thêm provider mới chỉ là một file cài đặt interface. Twilio cũng cài đúng interface đó nên fallback gọn trong cùng một abstraction. Điều này cũng làm test dễ: stub một <code>SMSProvider</code> giả để kiểm dispatcher mà không gọi mạng thật.</p>\n<p><strong>Vì sao consumer của fan-out chứ không tự lấy thẳng từ DB (DEC-NOTIF-74)?</strong> Giống TASK-NOTIF-002, fan-out (TASK-NOTIF-003) là nơi áp flatten-the-curve và rải tải; dispatcher SMS chỉ là cái miệng cuối nhặt dòng <code>channel='sms'</code> <code>status='queued'</code>. Tách như vậy cho phép nhiều worker SMS chạy song song mà không tự lo việc rải đỉnh, và giữ logic gửi (chọn provider, fallback, guard, brandname) gọn một chỗ. <code>FOR UPDATE SKIP LOCKED</code> làm việc nhặt an toàn khi scale ngang.</p>\n<p><strong>Vì sao ranh giới cứng với TASK-NOTIF-001 (§1 #12)?</strong> TASK-NOTIF-001 đã quyết kênh (SMS chỉ khi đáng) và render nội dung an toàn. Nếu dispatcher lại đụng vào chọn kênh hay nội dung thì hai chỗ cùng sửa một thứ, khó test và dễ lệch - đúng như lập luận ở TASK-NOTIF-002. Dispatcher SMS dừng đúng ở \"gửi số này nội dung này qua provider VN (hoặc Twilio fallback), ghi kết quả\". Trách nhiệm đơn, test gọn.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Repo: nhặt việc SMS (Go)</h3>\n<p>Không cần migration mới - tái dùng <code>notification</code> và <code>user_channel_token</code> của TASK-NOTIF-001, và <code>MarkSent</code>/<code>MarkFailed</code> của TASK-NOTIF-002. Chỉ bổ sung <code>ClaimSMSBatch</code>.</p>\n<pre><code class=\"language-go\">// services/notif/internal/notif/repo.go (bổ sung)\n\n// ClaimSMSBatch nhặt tối đa n dòng SMS đang queued, khóa hàng để worker khác bỏ qua.\n// Dùng FOR UPDATE SKIP LOCKED để nhiều dispatcher SMS chạy song song không gửi trùng.\n// high_value lấy từ payload (do TASK-NOTIF-001 đánh dấu) để guard ở §1 #5.\nfunc (r *Repo) ClaimSMSBatch(ctx context.Context, n int) ([]SMSJob, error) {\n    rows, err := r.pool.Query(ctx, `\n        SELECT n.id, n.user_id, t.address AS phone, n.payload,\n               COALESCE((n.payload-&gt;&gt;'high_value')::bool, false) AS high_value\n        FROM notification n\n        JOIN user_channel_token t\n          ON t.user_id = n.user_id AND t.channel = 'sms' AND t.verified = true\n        WHERE n.channel = 'sms' AND n.status = 'queued'\n        ORDER BY n.scheduled_at NULLS FIRST, n.id\n        FOR UPDATE OF n SKIP LOCKED\n        LIMIT $1`, n)\n    if err != nil {\n        return nil, err\n    }\n    defer rows.Close()\n    var jobs []SMSJob\n    for rows.Next() {\n        var j SMSJob\n        if err := rows.Scan(&amp;j.NotifID, &amp;j.UserID, &amp;j.Phone, &amp;j.Payload, &amp;j.HighValue); err != nil {\n            return nil, err\n        }\n        jobs = append(jobs, j)\n    }\n    return jobs, rows.Err()\n}</code></pre>\n<h3>SMSProvider interface + kiểu kết quả (Go)</h3>\n<pre><code class=\"language-go\">// services/notif/internal/sms/provider.go\n\n// SMSResult phân loại phản hồi provider để dispatcher hành xử (§1 #8).\ntype SMSResult int\n\nconst (\n    SMSSent          SMSResult = iota // provider chấp nhận, có message id\n    SMSRetry                          // 5xx/timeout/hết số dư tạm/rate-limit -&gt; fallback hoặc retry\n    SMSPermanentFail                  // số sai, brandname bị từ chối, nội dung vi phạm\n)\n\n// SMSMessage là nội dung đã render bởi TASK-NOTIF-001; dispatcher chỉ gửi, không render lại.\ntype SMSMessage struct {\n    To        string // số VN E.164 (vd +84906878091) hoặc nội địa theo provider\n    Body      string // plaintext đã render, không markup\n    Brandname string // sender ID đã đăng ký Cục An toàn thông tin (§1 #4)\n    HighValue bool   // OTP/alert giá trị cao - mang từ dòng notification\n}\n\n// SMSProvider pluggable: SpeedSMS/eSMS/VietGuys/Mobifone/Twilio đều cài đặt (DEC-NOTIF-70).\ntype SMSProvider interface {\n    Name() string\n    Send(ctx context.Context, msg SMSMessage) (SMSResult, error)\n}</code></pre>\n<h3>Provider VN sơ cấp - SpeedSMS (Go)</h3>\n<pre><code class=\"language-go\">// services/notif/internal/sms/speedsms.go\n\n// SpeedSMS là provider nội địa VN sơ cấp (~200-500 VND/tin, DEC-NOTIF-71).\ntype SpeedSMS struct {\n    http      *http.Client\n    apiURL    string\n    token     string\n    brandname string // brandname đã đăng ký; gửi đúng sender đã khai báo (§1 #4)\n}\n\nfunc (s *SpeedSMS) Name() string { return \"speedsms\" }\n\nfunc (s *SpeedSMS) Send(ctx context.Context, msg SMSMessage) (SMSResult, error) {\n    // type=2: gửi bằng brandname đã đăng ký (không phải đầu số ngẫu nhiên).\n    body, _ := json.Marshal(map[string]any{\n        \"to\":      []string{msg.To},\n        \"content\": msg.Body,\n        \"sms_type\": 2,\n        \"brandname\": msg.Brandname,\n    })\n    req, _ := http.NewRequestWithContext(ctx, http.MethodPost, s.apiURL, bytes.NewReader(body))\n    req.SetBasicAuth(s.token, \"x\") // SpeedSMS dùng token làm basic-auth user\n    req.Header.Set(\"Content-Type\", \"application/json\")\n    resp, err := s.http.Do(req)\n    if err != nil {\n        return SMSRetry, err // timeout/mạng -&gt; để dispatcher thử Twilio fallback\n    }\n    defer resp.Body.Close()\n    return classifySpeedSMS(resp), nil\n}\n\n// classifySpeedSMS ánh xạ HTTP status + status code provider sang SMSResult.\nfunc classifySpeedSMS(resp *http.Response) SMSResult {\n    switch {\n    case resp.StatusCode == 200:\n        return SMSSent\n    case resp.StatusCode == 429 || resp.StatusCode &gt;= 500:\n        return SMSRetry // rate-limit / sự cố provider -&gt; fallback/retry\n    case resp.StatusCode == 400 || resp.StatusCode == 402:\n        // 400 số/nội dung sai, 402 hết số dư có thể là tạm; coi 400 là vĩnh viễn\n        return SMSPermanentFail\n    default:\n        return SMSPermanentFail\n    }\n}</code></pre>\n<h3>Twilio fallback wrapper (Go)</h3>\n<pre><code class=\"language-go\">// services/notif/internal/sms/twilio.go\n\n// Twilio là fallback (~$0,1552/SMS, DEC-NOTIF-71) - CHỈ dùng khi provider VN fail\n// VÀ dòng là OTP/high-value. Không bao giờ là provider sơ cấp cho SMS nội địa.\ntype Twilio struct {\n    http       *http.Client\n    apiURL     string // .../2010-04-01/Accounts/{sid}/Messages.json\n    sid, token string\n    from       string // số/Alphanumeric Sender ID Twilio\n}\n\nfunc (t *Twilio) Name() string { return \"twilio\" }\n\nfunc (t *Twilio) Send(ctx context.Context, msg SMSMessage) (SMSResult, error) {\n    form := url.Values{}\n    form.Set(\"To\", msg.To)\n    form.Set(\"From\", t.from)\n    form.Set(\"Body\", msg.Body)\n    req, _ := http.NewRequestWithContext(ctx, http.MethodPost, t.apiURL,\n        strings.NewReader(form.Encode()))\n    req.SetBasicAuth(t.sid, t.token)\n    req.Header.Set(\"Content-Type\", \"application/x-www-form-urlencoded\")\n    resp, err := t.http.Do(req)\n    if err != nil {\n        return SMSRetry, err\n    }\n    defer resp.Body.Close()\n    switch {\n    case resp.StatusCode &gt;= 200 &amp;&amp; resp.StatusCode &lt; 300:\n        return SMSSent\n    case resp.StatusCode == 429 || resp.StatusCode &gt;= 500:\n        return SMSRetry\n    default:\n        return SMSPermanentFail\n    }\n}</code></pre>\n<h3>Guard chi phí + dispatcher với fallback (Go)</h3>\n<pre><code class=\"language-go\">// services/notif/internal/sms/guard.go\n\n// ErrNotHighValue: dòng không high-value/OTP lọt xuống SMS dispatcher (DEC-NOTIF-73).\nvar ErrNotHighValue = errors.New(\"sms: dòng không high-value/OTP - từ chối gửi SMS (kênh đắt nhất)\")\n\n// assertHighValue là van chi phí cuối: SMS chỉ cho high-value/OTP.\n// Routing TASK-NOTIF-001 đã đảm bảo, nhưng kiểm lại (defense-in-depth) - không tin mù.\nfunc assertHighValue(j notif.SMSJob) error {\n    if !j.HighValue {\n        return ErrNotHighValue\n    }\n    return nil\n}</code></pre>\n<pre><code class=\"language-go\">// services/notif/internal/sms/dispatcher.go\n\n// sendWithFallback: provider VN sơ cấp trước; nếu SMSRetry và dòng high-value -&gt; Twilio fallback.\nfunc (d *Dispatcher) sendWithFallback(ctx context.Context, msg sms.SMSMessage) (sms.SMSResult, string, error) {\n    res, err := d.primary.Send(ctx, msg) // provider VN (SpeedSMS/eSMS/VietGuys/Mobifone)\n    if res == sms.SMSSent {\n        return res, d.primary.Name(), err\n    }\n    if res == sms.SMSRetry &amp;&amp; msg.HighValue &amp;&amp; d.fallback != nil {\n        metrics.SMSFallbackTwilio()\n        fres, ferr := d.fallback.Send(ctx, msg) // Twilio (chỉ OTP/high-value)\n        return fres, d.fallback.Name(), ferr\n    }\n    return res, d.primary.Name(), err\n}\n\n// RunOnce: claim -&gt; guard -&gt; gửi (VN -&gt; Twilio fallback) -&gt; mark.\nfunc (d *Dispatcher) RunOnce(ctx context.Context) error {\n    jobs, err := d.repo.ClaimSMSBatch(ctx, d.batch)\n    if err != nil {\n        return err\n    }\n    for _, j := range jobs {\n        if err := assertHighValue(j); err != nil {\n            metrics.SMSGuardRejected()\n            _ = d.repo.MarkFailed(ctx, j.NotifID) // không gửi dòng không đáng (§1 #5)\n            continue\n        }\n        msg := sms.SMSMessage{\n            To: j.Phone, Body: bodyOf(j.Payload),\n            Brandname: d.brandname, HighValue: j.HighValue,\n        }\n        res, provider, _ := d.sendWithFallback(ctx, msg)\n        switch res {\n        case sms.SMSSent:\n            _ = d.repo.MarkSent(ctx, j.NotifID)\n        default:\n            _ = d.repo.MarkFailed(ctx, j.NotifID)\n        }\n        metrics.SMSSend(provider, res)\n    }\n    return nil\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>SMSProvider</code> là interface; SpeedSMS, Twilio (và stub test) đều cài đặt <code>Send(ctx, SMSMessage) (SMSResult, error)</code> + <code>Name()</code>.</li><li>Provider VN sơ cấp gửi thành công (HTTP 200, có message id) -&gt; <code>SMSSent</code> -&gt; <code>MarkSent</code>: dòng <code>notification</code> <code>status='sent'</code>, <code>sent_at</code> được set.</li><li>Provider VN trả <code>SMSRetry</code> (429/5xx/timeout) VÀ dòng <code>high_value=true</code> -&gt; Twilio fallback được gọi (<code>sms_fallback_twilio_total</code> tăng).</li><li>Provider VN trả <code>SMSSent</code> -&gt; Twilio <strong>KHÔNG</strong> được gọi (không lãng phí ~$0,1552/SMS khi sơ cấp đã xong).</li><li>Provider VN trả <code>SMSRetry</code> nhưng dòng <code>high_value=false</code> -&gt; KHÔNG Twilio fallback (Twilio chỉ cho OTP/high-value); dòng <code>MarkFailed</code>/retry theo cấu hình.</li><li>Dòng <code>notification</code> không high-value/OTP lọt vào <code>ClaimSMSBatch</code> -&gt; guard <code>assertHighValue</code> từ chối (<code>ErrNotHighValue</code>), <code>sms_guard_rejected_total</code> tăng, dòng <code>MarkFailed</code>, KHÔNG gửi SMS.</li><li><code>SMSMessage</code> gửi đi mang <code>Brandname</code> = brandname đã đăng ký; provider gửi với sender đó (không phải đầu số ngẫu nhiên).</li><li><code>ClaimSMSBatch</code> chỉ trả dòng <code>channel='sms'</code>, <code>status='queued'</code>, ghép với <code>user_channel_token</code> <code>channel='sms'</code> <code>verified=true</code>; số chưa verified KHÔNG được nhặt.</li><li>Hai worker <code>ClaimSMSBatch</code> đồng thời (FOR UPDATE SKIP LOCKED) KHÔNG nhặt trùng cùng một dòng.</li><li>Provider VN trả <code>SMSPermanentFail</code> (số sai / brandname từ chối) -&gt; <code>MarkFailed</code>, KHÔNG retry, KHÔNG Twilio.</li><li>Dòng đã <code>status='sent'</code> -&gt; <code>MarkSent</code>/<code>MarkFailed</code> lần nữa không đổi gì (cập nhật có điều kiện <code>status='queued'</code>); không gửi lại (không tốn tiền đúp).</li><li>Metric <code>sms_send_total{provider,result}</code> tăng đúng nhánh; <code>sms_cost_estimate_vnd_total{provider}</code> tăng theo provider thực gửi (provider VN ~200-500 VND, Twilio ~4.000 VND).</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/notif/internal/sms/provider_test.go\n\n// stubProvider: SMSProvider giả để test dispatcher không gọi mạng thật.\ntype stubProvider struct {\n    name string\n    res  SMSResult\n    got  *SMSMessage\n}\n\nfunc (s *stubProvider) Name() string { return s.name }\nfunc (s *stubProvider) Send(ctx context.Context, m SMSMessage) (SMSResult, error) {\n    *s.got = m\n    return s.res, nil\n}\n\nfunc TestSpeedSMS_Success_ReturnsSent(t *testing.T) {\n    srv := stubHTTP(t, 200, `{\"status\":\"success\",\"code\":\"00\"}`)\n    p := newSpeedSMS(t, srv.URL, \"SANDEAL\")\n    res, err := p.Send(ctx, SMSMessage{To: \"+84906878091\", Body: \"Ma OTP: 123456\", Brandname: \"SANDEAL\", HighValue: true})\n    require.NoError(t, err)\n    require.Equal(t, SMSSent, res)\n}\n\nfunc TestSpeedSMS_5xx_ReturnsRetry(t *testing.T) {\n    srv := stubHTTP(t, 503, `{}`)\n    p := newSpeedSMS(t, srv.URL, \"SANDEAL\")\n    res, _ := p.Send(ctx, SMSMessage{To: \"+84906878091\", Body: \"x\", Brandname: \"SANDEAL\", HighValue: true})\n    require.Equal(t, SMSRetry, res) // sự cố provider VN -&gt; để dispatcher fallback Twilio\n}\n\nfunc TestSpeedSMS_UsesRegisteredBrandname(t *testing.T) {\n    var sentBrand string\n    srv := captureBrandname(t, &amp;sentBrand, 200, `{\"status\":\"success\"}`)\n    p := newSpeedSMS(t, srv.URL, \"SANDEAL\")\n    p.Send(ctx, SMSMessage{To: \"+84906878091\", Body: \"x\", Brandname: \"SANDEAL\", HighValue: true})\n    require.Equal(t, \"SANDEAL\", sentBrand) // gửi đúng brandname đã đăng ký (§1 #4)\n}\n\n// services/notif/internal/sms/guard_test.go\nfunc TestGuard_NonHighValue_Rejected(t *testing.T) {\n    err := assertHighValue(notif.SMSJob{NotifID: 1, HighValue: false})\n    require.ErrorIs(t, err, ErrNotHighValue) // SMS kênh đắt nhất -&gt; chặn dòng thường (DEC-NOTIF-73)\n}\n\nfunc TestGuard_HighValue_Passes(t *testing.T) {\n    require.NoError(t, assertHighValue(notif.SMSJob{NotifID: 1, HighValue: true}))\n}\n\n// services/notif/internal/sms/dispatcher_test.go\nfunc TestDispatch_Success_MarksSent(t *testing.T) {\n    d, repo, nid := setupQueuedSMS(t, &amp;stubProvider{name: \"speedsms\", res: SMSSent, got: &amp;SMSMessage{}}, nil, true)\n    d.RunOnce(ctx)\n    require.Equal(t, \"sent\", repo.status(t, nid))\n    require.NotNil(t, repo.sentAt(t, nid))\n}\n\nfunc TestDispatch_PrimaryFails_FallsBackToTwilio(t *testing.T) {\n    primary := &amp;stubProvider{name: \"speedsms\", res: SMSRetry, got: &amp;SMSMessage{}}\n    fallback := &amp;stubProvider{name: \"twilio\", res: SMSSent, got: &amp;SMSMessage{}}\n    d, repo, nid := setupQueuedSMS(t, primary, fallback, true) // high_value=true\n    d.RunOnce(ctx)\n    require.NotEmpty(t, fallback.got.To)           // Twilio fallback đã được gọi (nhận message)\n    require.Equal(t, \"sent\", repo.status(t, nid))  // gửi thành công qua Twilio\n}\n\nfunc TestDispatch_PrimarySent_NoTwilioCall(t *testing.T) {\n    primary := &amp;stubProvider{name: \"speedsms\", res: SMSSent, got: &amp;SMSMessage{}}\n    fallback := &amp;stubProvider{name: \"twilio\", res: SMSSent, got: &amp;SMSMessage{}}\n    d, repo, nid := setupQueuedSMS(t, primary, fallback, true)\n    d.RunOnce(ctx)\n    require.Empty(t, fallback.got.To)              // Twilio KHÔNG được gọi (sơ cấp đã xong)\n    require.Equal(t, \"sent\", repo.status(t, nid))\n}\n\nfunc TestDispatch_NonHighValue_GuardRejects_NoSend(t *testing.T) {\n    primary := &amp;stubProvider{name: \"speedsms\", res: SMSSent, got: &amp;SMSMessage{}}\n    d, repo, nid := setupQueuedSMS(t, primary, nil, false) // high_value=false lọt xuống\n    d.RunOnce(ctx)\n    require.Empty(t, primary.got.To)               // KHÔNG gửi SMS (guard chặn, DEC-NOTIF-73)\n    require.Equal(t, \"failed\", repo.status(t, nid))\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>repo.go</code> (ClaimSMSBatch trên schema sẵn của TASK-NOTIF-001, tái dùng MarkSent/MarkFailed của TASK-NOTIF-002) -&gt; <code>provider.go</code> (interface SMSProvider + SMSResult) -&gt; <code>speedsms.go</code> (provider VN sơ cấp + classify) -&gt; <code>twilio.go</code> (fallback wrapper) -&gt; <code>guard.go</code> (assertHighValue) -&gt; <code>dispatcher.go</code> (vòng RunOnce: claim -&gt; guard -&gt; sendWithFallback -&gt; mark) -&gt; tests. Dispatcher chạy thành pool worker, mỗi worker <code>ClaimSMSBatch</code> rồi gửi; <code>FOR UPDATE SKIP LOCKED</code> giữ an toàn khi scale ngang. Fan-out (TASK-NOTIF-003) là thứ đẩy dòng <code>channel='sms'</code> vào <code>status='queued'</code>; dispatcher chỉ nhặt và gửi. Provider VN sơ cấp và brandname cấu hình qua env/secret; thêm provider VN khác (eSMS/VietGuys/Mobifone) chỉ là một file cài đặt <code>SMSProvider</code>.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-NOTIF-001</strong> - bảng <code>notification</code> (đọc <code>channel='sms'</code>, cập nhật <code>status</code>/<code>sent_at</code>) và <code>user_channel_token</code> (đọc số điện thoại <code>verified=true</code>). Routing của TASK-NOTIF-001 đã đảm bảo <code>channel='sms'</code> chỉ xuất hiện cho high-value/OTP hoặc khi push+email không khả dụng - dispatcher kiểm lại bằng guard (defense-in-depth). Dispatcher KHÔNG routing/render - hai việc đó thuộc TASK-NOTIF-001.</li><li><strong>TASK-NOTIF-003 (upstream)</strong> - fan-out chuyển dòng <code>notification</code> SMS sang <code>status='queued'</code> và rải đỉnh (flatten-the-curve); dispatcher là consumer downstream nhặt các dòng đó. Dead-letter các dòng <code>status='failed'</code> cũng do fan-out lo.</li><li><strong>TASK-NOTIF-002 (anh em)</strong> - mirror cấu trúc claim/mark/status-update; tái dùng <code>MarkSent</code>/<code>MarkFailed</code>/idempotency pattern. FCM (push) và SMS là hai dispatcher song song trên cùng vòng đời <code>notification</code>.</li><li><strong>TASK-INFRA-002</strong> - <code>app_user</code> cho FK <code>user_id</code> (gián tiếp qua schema TASK-NOTIF-001).</li><li>Lib/dịch vụ: provider VN (SpeedSMS / eSMS-ViHAT / VietGuys / Mobifone) qua HTTP API; Twilio (<code>Messages.json</code>) làm fallback; <code>net/http</code>, <code>encoding/json</code>, <code>net/url</code>, <code>pgx</code> (FOR UPDATE SKIP LOCKED). Brandname đăng ký với Cục An toàn thông tin (quy trình vận hành, ngoài code).</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Provider VN sơ cấp (SpeedSMS) gửi bằng brandname đã đăng ký</h3>\n<pre><code class=\"language-json\">{\n  \"to\": [\"+84906878091\"],\n  \"content\": \"SanDeal: Ma OTP dang nhap cua ban la 123456 (het han sau 5 phut).\",\n  \"sms_type\": 2,\n  \"brandname\": \"SANDEAL\"\n}</code></pre>\n<h3>Twilio fallback (chỉ khi provider VN fail + dòng high-value/OTP)</h3>\n<pre><code class=\"language-json\">{\n  \"To\": \"+84906878091\",\n  \"From\": \"SANDEAL\",\n  \"Body\": \"SanDeal: Ma OTP dang nhap cua ban la 123456 (het han sau 5 phut).\"\n}</code></pre>\n<h3>Dòng notification: queued -&gt; sent sau khi gửi thành công</h3>\n<pre><code class=\"language-sql\">-- Trước (fan-out đã đẩy vào hàng đợi, payload đánh dấu high_value):\n-- 55020 | sms | otp_login | queued | sent_at = NULL | payload.high_value = true\nUPDATE notification SET status='sent', sent_at=now() WHERE id=55020 AND status='queued';\n-- Sau: 55020 | sms | otp_login | sent | sent_at = 2026-06-28 00:00:02+07</code></pre>\n<h3>Dòng thường lọt xuống SMS: guard từ chối, không gửi</h3>\n<pre><code class=\"language-sql\">-- payload.high_value = false (lẽ ra phải đi push) -&gt; guard chặn, không tốn tiền SMS:\nUPDATE notification SET status='failed' WHERE id=55021 AND status='queued';\n-- sms_guard_rejected_total += 1</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>SMS Long Code (rẻ hơn brandname ~50%) thay cho brandname để hạ chi phí - đổi lại mất uy tín thương hiệu và có giới hạn throughput; chốt brandname mặc định, cân nhắc long code cho phân khúc không cần thương hiệu (DEC-NOTIF-75).</li><li>Provider SMS cho SEA (Thái/Indonesia/Philippines khi mở rộng) - cùng interface <code>SMSProvider</code>, thêm nhà cung cấp khu vực; chọn theo nước người dùng giai đoạn sau.</li><li>DLR (delivery receipt) - lấy báo cáo \"đã gửi tới máy\" từ provider qua webhook để biết tin thực sự tới (khác với \"provider chấp nhận\"); cần endpoint nhận callback, để task analytics sau.</li><li>Tự động chuyển provider VN theo giá/độ tin cậy (multi-provider load-balance VN) - tối ưu chi phí khi volume đủ lớn; hiện một provider VN sơ cấp + Twilio fallback là đủ.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>SMS gửi cho dòng thường (không high-value)</td><td>guard <code>assertHighValue</code></td><td>đốt chi phí (SMS đắt nhất)</td><td>Guard từ chối + MarkFailed (DEC-NOTIF-73), <code>sms_guard_rejected_total</code></td></tr><tr><td>Dùng Twilio làm sơ cấp cho SMS nội địa</td><td>code review / cost metric</td><td>mỗi tin đắt gấp hàng chục lần</td><td>Provider VN sơ cấp, Twilio chỉ fallback (DEC-NOTIF-71)</td></tr><tr><td>Brandname chưa đăng ký Cục An toàn thông tin</td><td>nhà mạng chặn / test brandname</td><td>tin không tới, có thể bị phạt</td><td>Chỉ gửi với brandname đã đăng ký (DEC-NOTIF-72)</td></tr><tr><td>Provider VN lỗi/hết số dư lúc gửi OTP</td><td><code>SMSRetry</code> + metric</td><td>OTP đăng nhập rớt</td><td>Twilio fallback cho high-value/OTP (DEC-NOTIF-71)</td></tr><tr><td>Twilio bị gọi cho khối lượng lớn (không cần)</td><td><code>sms_fallback_twilio_total</code> bất thường</td><td>chi phí Twilio tăng</td><td>Fallback chỉ kích hoạt khi high-value + VN fail (§1 #3)</td></tr><tr><td>Số điện thoại chưa verified</td><td>join <code>verified=true</code></td><td>gửi sai người / tốn tiền</td><td>ClaimSMSBatch chỉ ghép token verified (§1 #7)</td></tr><tr><td>Hai worker gửi trùng một dòng</td><td>test concurrency</td><td>SMS đúp tới user, tốn tiền</td><td>FOR UPDATE SKIP LOCKED (§1 #9)</td></tr><tr><td>Gửi lại dòng đã sent</td><td>cập nhật có điều kiện</td><td>spam + tốn tiền đúp</td><td>MarkSent/Claim chỉ chạm status='queued' (§1 #10)</td></tr><tr><td>Số sai định dạng / brandname bị từ chối</td><td><code>SMSPermanentFail</code></td><td>gửi hỏng lặp lại</td><td>MarkFailed, không retry, không Twilio (§1 #8)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Dispatcher SMS là cái miệng cuối của kênh ĐẮT NHẤT trong cost model §3.6 (push &gt; email &gt; sms); nó nhặt dòng <code>queued</code>, guard high-value/OTP, gửi qua provider VN (hoặc Twilio fallback), ghi <code>sent</code>/<code>failed</code>, không hơn.</li><li>Giá tham chiếu: SMS nội địa VN ~200-500 VND/tin tùy nhà mạng; Twilio ~$0,1552/SMS tới VN (khoảng 4.000 VND, đắt gấp hàng chục lần) nên chỉ làm fallback cho OTP/high-value; push gần như 0 đồng - đó là lý do guard tồn tại.</li><li>Brandname (sender ID) phải đăng ký/khai báo với Cục An toàn thông tin; phí duy trì ~50.000 VND/tháng/nhà mạng - chi phí cố định nhỏ đổi lấy khả năng gửi hợp lệ và uy tín thương hiệu (chống giả mạo).</li><li>SMS Long Code rẻ hơn brandname ~50% nhưng mất uy tín thương hiệu và giới hạn throughput; chốt brandname mặc định, long code hoãn (DEC-NOTIF-75).</li><li><code>SMSProvider</code> là interface pluggable: SpeedSMS/eSMS/VietGuys/Mobifone/Twilio hoán đổi qua cấu hình; thêm provider mới chỉ là một file cài đặt interface, không sửa dispatcher.</li><li>Ranh giới cứng với TASK-NOTIF-001: dispatcher không routing, không render lại nội dung; chỉ gửi SMS cho dòng đã định kênh và đã được đánh dấu high-value/OTP. Push có dispatcher riêng (TASK-NOTIF-002), email có dispatcher riêng (TASK-NOTIF-006).</li></ul>\n<hr>\n<p><em>Hết TASK-NOTIF-007. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
