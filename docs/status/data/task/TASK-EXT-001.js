window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-EXT-001"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Extension SănDeal <strong>MUST</strong> dựng trên Manifest V3 với một service worker ephemeral đúng vòng đời Chrome, không giữ state trong biến global, dùng <code>chrome.alarms</code> cho polling và khai báo <code>host_permissions</code> tường minh từng domain sàn. Hợp đồng:</p>\n<ol><li><code>manifest.json</code> <strong>MUST</strong> đặt <code>manifest_version: 3</code> và khai báo <code>background.service_worker</code> (kèm <code>type: \"module\"</code>) - KHÔNG dùng <code>background.page</code> hay background page persistent (DEC-EXT-01).</li><li>Service worker <strong>MUST</strong> coi mình là ephemeral (DEC-EXT-02): Chrome kill SW sau ~30 giây không hoạt động, hoặc khi một sự kiện chạy quá 5 phút, hoặc khi một <code>fetch()</code> không phản hồi quá 30 giây. Mã <strong>MUST NOT</strong> giữ bất kỳ state có ý nghĩa nào trong biến module-global; mọi state bền <strong>MUST</strong> lưu vào <code>chrome.storage.local</code> (hoặc <code>.session</code> cho state nhạy cảm theo phiên).</li><li>Module <code>storage.ts</code> <strong>MUST</strong> là điểm truy cập state duy nhất: bọc <code>chrome.storage</code> thành API typed <code>get/set/remove</code>. Các module khác <strong>MUST</strong> đọc/ghi state qua đây, không truy cập biến toàn cục.</li><li>Khi SW thức dậy (wake) do một sự kiện, handler <strong>MUST</strong> rehydrate state từ <code>chrome.storage</code> trước khi xử lý - không giả định biến trong bộ nhớ còn sống từ lần chạy trước.</li><li>Polling định kỳ <strong>MUST</strong> dùng <code>chrome.alarms</code> với <code>periodInMinutes &gt;= 0.5</code> (tức &gt;=30 giây, ràng buộc Chrome 120+) (DEC-EXT-03). Mã <strong>MUST NOT</strong> dùng <code>setInterval</code> hay <code>setTimeout</code> chu kỳ dài trong service worker để lập lịch - chúng không sống qua chu kỳ kill.</li><li><code>manifest.json</code> <strong>MUST</strong> khai báo <code>host_permissions</code> tường minh cho từng domain sàn được hỗ trợ (ví dụ <code>https://shopee.vn/*</code>; các sàn khác thêm khi task tương ứng tới) - KHÔNG dùng <code>&lt;all_urls&gt;</code> hay wildcard rộng (DEC-EXT-04).</li><li><code>permissions</code> trong manifest <strong>MUST</strong> là tập tối thiểu cần cho slice 1: <code>storage</code>, <code>alarms</code>. Quyền khác (ví dụ <code>offscreen</code>, <code>declarativeNetRequest</code>) chỉ thêm khi task tương ứng (TASK-EXT-004) tới.</li><li>Tác vụ nặng hoặc dài (đọc nhiều tab, đồng bộ khối lượng lớn) <strong>MUST</strong> đẩy lên backend (DEC-EXT-05); extension chỉ giữ vai trò \"đầu đọc\" nhẹ. Service worker <strong>MUST NOT</strong> chạy vòng lặp tính toán dài làm vượt ngưỡng 5 phút.</li><li><code>messaging.ts</code> <strong>MUST</strong> định nghĩa kiểu thông điệp giữa content script và service worker (typed <code>Message</code> discriminated union) và helper <code>sendMessage</code>/<code>onMessage</code> bọc <code>chrome.runtime</code> để hai bên giao tiếp an toàn kiểu.</li><li>Build <strong>MUST</strong> tái lập được (DEC-EXT-06): <code>build.mjs</code> (esbuild) sinh <code>dist/</code> xác định từ mã nguồn; phục vụ yêu cầu mã nguồn mở + reproducible build của TASK-TRUST-001.</li><li>Service worker <strong>MUST</strong> đăng ký listener ở top-level đồng bộ (<code>chrome.runtime.onInstalled</code>, <code>chrome.alarms.onAlarm</code>, <code>chrome.runtime.onMessage</code>) - KHÔNG đăng ký listener bên trong callback bất đồng bộ (MV3 yêu cầu listener đăng ký synchronously lúc SW khởi động để nhận sự kiện wake).</li><li>Extension <strong>MUST</strong> chạy trên Chrome (và Chromium MV3) tối thiểu phiên bản 120 (mốc <code>chrome.alarms</code> 30 giây); manifest <strong>SHOULD</strong> đặt <code>minimum_chrome_version: \"120\"</code>.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao service worker ephemeral, state trong chrome.storage (DEC-EXT-02)?</strong> MV3 thay background page persistent bằng service worker bị kill chủ động để tiết kiệm tài nguyên. Đây là cái bẫy số một khi viết extension: code chạy đúng trong lúc dev (SW còn sống), rồi hỏng trong thực tế khi SW bị kill và biến global mất sạch. Quy ước \"state luôn ở chrome.storage, biến global chỉ là cache nhất thời\" loại bỏ cả lớp bug không tái hiện được. Mọi task sau (đọc giỏ, đồng bộ) dựa vào nền này.</p>\n<p><strong>Vì sao chrome.alarms &gt;=30s, không setInterval (DEC-EXT-03)?</strong> <code>setInterval</code> sống trong bộ nhớ SW; khi SW ngủ, timer chết, polling dừng âm thầm. <code>chrome.alarms</code> được Chrome quản lý ngoài SW và đánh thức SW khi tới hạn - đó là cơ chế hẹn giờ duy nhất sống qua chu kỳ kill. Trần 30 giây (Chrome 120+) là ràng buộc nền tảng, không phải lựa chọn; mã phải tôn trọng nó.</p>\n<p><strong>Vì sao host_permissions per-domain, không &lt;all_urls&gt; (DEC-EXT-04)?</strong> Extension này đọc ngữ cảnh đăng nhập sàn (cookie phiên first-party) - quyền nhạy cảm. Khai báo từng domain (shopee.vn...) làm rõ với Chrome Web Store reviewer và với người dùng rằng extension chỉ chạm các sàn đã công bố. <code>&lt;all_urls&gt;</code> kích hoạt cờ review nghiêm ngặt và làm người dùng nghi malware (§5.4) - đúng điều SănDeal phải né hậu-Honey.</p>\n<p><strong>Vì sao đẩy việc nặng lên backend (DEC-EXT-05)?</strong> SW bị kill nếu một sự kiện chạy &gt;5 phút. Mọi việc tốn thời gian (đồng bộ lớn, tính toán) phải nằm ở backend; extension chỉ trích xuất dữ liệu đã render rồi gửi đi. Ranh giới này vừa hợp ràng buộc MV3, vừa hợp triết lý \"đầu đọc nhẹ + xử lý phía server\".</p>\n<p><strong>Vì sao listener đăng ký top-level đồng bộ (§1 #11)?</strong> MV3 chỉ giao sự kiện wake cho các listener đã đăng ký synchronously khi SW khởi động. Đăng ký trong một callback <code>await</code> làm SW lỡ sự kiện đánh thức nó - một lỗi tinh vi khiến alarm/message thỉnh thoảng không chạy. Quy ước top-level loại bỏ lớp lỗi này.</p>\n<p><strong>Vì sao build tái lập (DEC-EXT-06)?</strong> TASK-TRUST-001 cam kết mã nguồn mở + reproducible build để chứng minh extension không gửi cookie/mật khẩu. Build xác định cho phép bên thứ ba dựng lại bundle từ nguồn và so khớp với bản trên Chrome Web Store. Đặt nền build sạch ngay từ scaffold rẻ hơn nhiều so với việc gỡ rối sau.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>manifest.json (lõi)</h3>\n<pre><code class=\"language-jsonc\">// extension/manifest.json\n{\n  \"manifest_version\": 3,\n  \"name\": \"SănDeal\",\n  \"version\": \"0.1.0\",\n  \"minimum_chrome_version\": \"120\",\n  \"background\": {\n    \"service_worker\": \"background/service-worker.js\",\n    \"type\": \"module\"\n  },\n  \"permissions\": [\"storage\", \"alarms\"],\n  \"host_permissions\": [\n    \"https://shopee.vn/*\"\n  ],\n  \"action\": { \"default_title\": \"SănDeal\" }\n}</code></pre>\n<h3>storage.ts (điểm truy cập state duy nhất)</h3>\n<pre><code class=\"language-ts\">// extension/src/shared/storage.ts\nexport interface ExtState {\n  schemaVersion: number;\n  installedAt: number;        // epoch ms\n  lastSyncAt?: number;\n  pendingReads: string[];     // hàng đợi nhẹ, KHÔNG token/cookie\n}\n\nconst KEY = \"sandeal:state\";\n\nexport async function getState(): Promise&lt;ExtState&gt; {\n  const obj = await chrome.storage.local.get(KEY);\n  return (obj[KEY] as ExtState) ?? defaultState();\n}\n\nexport async function setState(next: ExtState): Promise&lt;void&gt; {\n  await chrome.storage.local.set({ [KEY]: next });\n}\n\nexport function defaultState(): ExtState {\n  return { schemaVersion: 1, installedAt: Date.now(), pendingReads: [] };\n}</code></pre>\n<h3>lifecycle.ts + alarms.ts (top-level, đồng bộ)</h3>\n<pre><code class=\"language-ts\">// extension/src/background/service-worker.ts  (entrypoint)\nimport { onInstalled, onStartup } from \"./lifecycle\";\nimport { registerAlarms, onAlarm } from \"./alarms\";\nimport { onMessage } from \"../shared/messaging\";\n\n// MUST: listener đăng ký top-level đồng bộ (MV3 wake requirement)\nchrome.runtime.onInstalled.addListener(onInstalled);\nchrome.runtime.onStartup.addListener(onStartup);\nchrome.alarms.onAlarm.addListener(onAlarm);\nchrome.runtime.onMessage.addListener(onMessage);\n\nregisterAlarms(); // tạo alarm periodInMinutes &gt;= 0.5</code></pre>\n<pre><code class=\"language-ts\">// extension/src/background/alarms.ts\nexport const TICK = \"sandeal:tick\";\n\nexport function registerAlarms(): void {\n  // 0.5 phút = 30 giây, trần Chrome 120+. KHÔNG setInterval.\n  chrome.alarms.create(TICK, { periodInMinutes: 0.5 });\n}\n\nexport async function onAlarm(alarm: chrome.alarms.Alarm): Promise&lt;void&gt; {\n  if (alarm.name !== TICK) return;\n  const state = await getState();   // rehydrate từ storage, KHÔNG biến global\n  // ... xử lý nhẹ; việc nặng đẩy backend (DEC-EXT-05)\n  await setState({ ...state, lastSyncAt: Date.now() });\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>manifest.json</code> có <code>manifest_version: 3</code> và <code>background.service_worker</code> (type module); KHÔNG có <code>background.page</code>.</li><li><code>host_permissions</code> chỉ chứa domain sàn tường minh (<code>https://shopee.vn/*</code>); KHÔNG chứa <code>&lt;all_urls&gt;</code> hay <code>http(s)://*/*</code>.</li><li><code>permissions</code> đúng tập tối thiểu <code>[\"storage\",\"alarms\"]</code> cho slice 1.</li><li>Đăng ký alarm với <code>periodInMinutes &lt; 0.5</code> -&gt; Chrome kẹp về 30s; mã KHÔNG dựa chu kỳ nhỏ hơn (test xác nhận không có alarm &lt;30s được kỳ vọng chạy đúng chu kỳ nhỏ).</li><li>Mô phỏng SW bị kill rồi wake (khởi tạo lại module): state đọc lại từ <code>chrome.storage</code> khớp giá trị đã ghi - KHÔNG mất.</li><li>Grep mã <code>src/background/**</code>: KHÔNG có biến module-global mang state nghiệp vụ (chỉ hằng số/handler).</li><li>Grep mã service worker: KHÔNG có <code>setInterval(</code> và KHÔNG có <code>setTimeout(</code> với chu kỳ lập lịch dài.</li><li>Listener (<code>onInstalled</code>, <code>onAlarm</code>, <code>onMessage</code>) đăng ký ở top-level của entrypoint, KHÔNG bên trong callback <code>async</code>/<code>await</code>.</li><li><code>build.mjs</code> chạy 2 lần trên cùng nguồn -&gt; byte <code>dist/</code> giống nhau (build xác định/tái lập).</li><li><code>minimum_chrome_version</code> = \"120\" (mốc alarms 30s).</li><li><code>npm test</code> xanh; <code>tsc --noEmit</code> không lỗi kiểu.</li><li><code>messaging.ts</code> định nghĩa <code>Message</code> là discriminated union (theo <code>type</code>) và export helper <code>sendMessage</code>/<code>onMessage</code> bọc <code>chrome.runtime</code>; một thông điệp sai kiểu (thiếu <code>type</code> hoặc payload sai) bị <code>tsc</code> bắt lúc biên dịch (§1 #9).</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-ts\">// extension/test/lifecycle.test.ts\nimport { getState, setState, defaultState } from \"../src/shared/storage\";\nimport { fakeChromeStorage } from \"./helpers\";\n\ntest(\"state survive SW kill (rehydrate từ storage)\", async () =&gt; {\n  globalThis.chrome = fakeChromeStorage();      // storage backing bền\n  await setState({ ...defaultState(), lastSyncAt: 111 });\n\n  // mô phỏng SW kill: vứt mọi biến module, đọc lại sạch từ storage\n  jest.resetModules();\n  const { getState: getState2 } = await import(\"../src/shared/storage\");\n  const s = await getState2();\n  expect(s.lastSyncAt).toBe(111);               // KHÔNG mất qua \"kill\"\n});\n\ntest(\"không có state trong biến global\", async () =&gt; {\n  const src = await readFile(\"src/background/service-worker.ts\", \"utf8\");\n  // entrypoint chỉ đăng ký listener + tạo alarm, không khai biến state\n  expect(src).not.toMatch(/let\\s+\\w+State\\s*=/);\n});</code></pre>\n<pre><code class=\"language-ts\">// extension/test/alarms.test.ts\nimport { registerAlarms, TICK } from \"../src/background/alarms\";\n\ntest(\"alarm chu kỳ &gt;= 30s, không setInterval\", async () =&gt; {\n  const created: any[] = [];\n  globalThis.chrome = { alarms: { create: (n: string, o: any) =&gt; created.push({ n, o }) } } as any;\n  registerAlarms();\n  expect(created[0].n).toBe(TICK);\n  expect(created[0].o.periodInMinutes).toBeGreaterThanOrEqual(0.5); // &gt;=30s\n\n  const swSrc = await readFile(\"src/background/alarms.ts\", \"utf8\");\n  expect(swSrc).not.toMatch(/setInterval\\(/);\n});</code></pre>\n<pre><code class=\"language-ts\">// extension/test/manifest.test.ts\ntest(\"manifest MV3, không &lt;all_urls&gt;, permissions tối thiểu\", async () =&gt; {\n  const m = JSON.parse(await readFile(\"manifest.json\", \"utf8\"));\n  expect(m.manifest_version).toBe(3);\n  expect(m.background.service_worker).toBeTruthy();\n  expect(m.background.page).toBeUndefined();\n  expect(JSON.stringify(m.host_permissions)).not.toMatch(/all_urls|:\\/\\/\\*\\/\\*/);\n  expect(m.permissions.sort()).toEqual([\"alarms\", \"storage\"]);\n});</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>manifest.json</code> + <code>tsconfig.json</code> + <code>build.mjs</code> (khung build) -&gt; <code>storage.ts</code> (điểm state) -&gt; <code>lifecycle.ts</code> + <code>alarms.ts</code> + <code>messaging.ts</code> -&gt; <code>service-worker.ts</code> (entrypoint nối listener top-level) -&gt; tests. Bundler esbuild emit ESM module cho service worker (<code>type: \"module\"</code>). Tránh mọi top-level <code>await</code> trong entrypoint service worker (làm trễ đăng ký listener). Mọi TASK-EXT sau nạp content script / offscreen / sync lên đúng scaffold này.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>depends_on: []</strong> - đây là task gốc của module EXT, không phụ thuộc task khác.</li><li><strong>TASK-EXT-002 (downstream)</strong> - content script Shopee nạp trên scaffold + messaging này.</li><li><strong>TASK-EXT-003/004/005/006 (downstream)</strong> - pipeline tối thiểu hóa, offscreen, đồng bộ backend, UI consent đều dựa scaffold.</li><li><strong>TASK-TRUST-001 (downstream)</strong> - publish mã nguồn mở + reproducible build dùng <code>build.mjs</code> ở đây.</li><li><strong>NFR-EXT-001</strong> - ràng buộc MV3 (ephemeral, alarms &gt;=30s, no global state) mà task này hiện thực hóa.</li><li>Nền tảng: Chrome/Chromium MV3 &gt;=120; TypeScript 5.x; esbuild.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Thông điệp content -&gt; service worker (typed)</h3>\n<pre><code class=\"language-ts\">// content script gửi báo cáo đọc giỏ (KHÔNG cookie/token)\nchrome.runtime.sendMessage({\n  type: \"CART_READ\",\n  platform: \"shopee\",\n  items: [{ productId: \"90112\", price: 89000, qty: 1 }]\n} satisfies Message);</code></pre>\n<h3>State trong chrome.storage.local (ví dụ)</h3>\n<pre><code class=\"language-json\">{\n  \"sandeal:state\": {\n    \"schemaVersion\": 1,\n    \"installedAt\": 1782000000000,\n    \"lastSyncAt\": 1782000030000,\n    \"pendingReads\": []\n  }\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Có dùng <code>chrome.storage.session</code> cho state phiên-nhạy-cảm (RAM-only) hay không - quyết ở TASK-EXT-003 khi định nghĩa dữ liệu tối thiểu hóa.</li><li>Firefox MV3 (service worker khác Chrome) - hoãn tới khi có nhu cầu đa trình duyệt; slice 1 nhắm Chrome/Chromium.</li><li>Cập nhật host_permissions cho tiktok.com / lazada.vn - thêm ở TASK-EXT-007 / TASK-EXT-008 (P2), không khai sớm quyền chưa dùng.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Giữ state trong biến global</td><td>test \"không có state global\" + lỗi runtime không tái hiện</td><td>state mất khi SW kill</td><td>Đẩy mọi state qua storage.ts (DEC-EXT-02)</td></tr><tr><td>Dùng setInterval polling</td><td>grep test + polling chết khi SW ngủ</td><td>mất nhịp đọc/đồng bộ</td><td>Thay bằng chrome.alarms &gt;=30s (DEC-EXT-03)</td></tr><tr><td>host_permissions &lt;all_urls&gt;</td><td>manifest.test + Web Store review từ chối</td><td>reject + nghi malware (§5.4)</td><td>Khai từng domain sàn (DEC-EXT-04)</td></tr><tr><td>Listener đăng ký trong async callback</td><td>sự kiện wake thỉnh thoảng lỡ</td><td>alarm/message không chạy</td><td>Đăng ký top-level đồng bộ (§1 #11)</td></tr><tr><td>Sự kiện chạy &gt;5 phút</td><td>SW bị Chrome kill giữa chừng</td><td>tác vụ dở dang</td><td>Đẩy việc nặng lên backend (DEC-EXT-05)</td></tr><tr><td>fetch không phản hồi &gt;30s</td><td>SW kill, request treo</td><td>mất kết quả</td><td>Timeout fetch &lt; 30s; retry phía backend</td></tr><tr><td>alarm chu kỳ &lt;30s</td><td>Chrome kẹp âm thầm về 30s</td><td>nhịp thực != kỳ vọng</td><td>Thiết kế quanh trần 30s; không kỳ vọng nhỏ hơn</td></tr><tr><td>Build không xác định</td><td>dist khác giữa 2 lần build</td><td>TASK-TRUST-001 reproducible vỡ</td><td>Khóa version + esbuild xác định</td></tr><tr><td>Thừa permission trong manifest</td><td>manifest.test</td><td>quyền thừa, review chậm</td><td>Giữ tập tối thiểu, thêm theo task</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Đây là scaffold nền của toàn module EXT; ba ràng buộc MV3 (SW ephemeral / alarms &gt;=30s / no global state) là bất biến cho mọi task sau, được chốt cứng ở NFR-EXT-001.</li><li>\"State luôn ở chrome.storage\" là quy ước rẻ nhưng loại bỏ lớp bug không tái hiện được lớn nhất của extension MV3.</li><li>host_permissions per-domain vừa hợp lệ Web Store vừa là tín hiệu niềm tin (extension chỉ chạm sàn đã công bố) - mắt xích chiến lược hậu-Honey (§5.4).</li><li>Việc nặng off-device là ranh giới kiến trúc xuyên suốt: extension đọc nhẹ, backend gánh nặng (TASK-EXT-003/005, TASK-SCRAPE-*).</li><li>Reproducible build ở đây là tiền đề cho cam kết minh bạch TASK-TRUST-001.</li></ul>\n<hr>\n<p><em>Hết TASK-EXT-001. Status: done (mục tiêu audit 10/10).</em></p>";
