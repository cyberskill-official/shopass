window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PRICE-001"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service PRICE <strong>MUST</strong> định nghĩa registry chuẩn hóa <code>tracked_product</code>: đúng một dòng cho mỗi cặp (platform_id, platform_item_id), có cột <code>canonical_key</code> làm khóa gom chéo sàn (điền sau bởi TASK-PRICE-005), và là đích FK cho <code>price_snapshot</code>. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> định nghĩa bảng <code>tracked_product (id BIGSERIAL PK, platform_id SMALLINT, platform_item_id TEXT NOT NULL, shop_id TEXT, title TEXT, category_id BIGINT, canonical_key TEXT, first_seen TIMESTAMPTZ DEFAULT now())</code>.</li><li><strong>MUST</strong> ràng buộc <code>platform_id</code> REFERENCES <code>platform(id)</code> (FK tới bảng nền của TASK-INFRA-002). Một SKU luôn thuộc đúng một sàn đã đăng ký.</li><li><strong>MUST</strong> đặt <code>UNIQUE (platform_id, platform_item_id)</code> (DEC-PRICE-08): cùng một sàn không được có hai dòng cho cùng <code>platform_item_id</code>. Đây là khóa idempotent cho upsert.</li><li><strong>MUST</strong> để <code>canonical_key</code> là <code>TEXT</code> nullable (DEC-PRICE-07): task này chỉ tạo cột; TASK-PRICE-005 mới chạy thuật toán so khớp và UPDATE giá trị. Insert ở task này <strong>MUST</strong> để <code>canonical_key = NULL</code>.</li><li><strong>MUST</strong> tạo <code>CREATE INDEX idx_tp_canonical ON tracked_product (canonical_key)</code> (DEC-PRICE-09) phục vụ JOIN gom các SKU cùng sản phẩm trên nhiều sàn. Dòng có <code>canonical_key IS NULL</code> không cản index (B-tree bỏ qua NULL khi tra cứu theo giá trị).</li><li><strong>MUST</strong> đặt <code>first_seen TIMESTAMPTZ DEFAULT now()</code> (DEC-PRICE-10): mốc cold-start ghi nhận thời điểm SKU vào hệ thống, dùng để cắt cửa sổ lịch sử giá cho sale ảo.</li><li><strong>MUST</strong> expose hàm repo <code>Upsert(ctx, p TrackedProduct) (TrackedProduct, error)</code>: chèn dòng mới hoặc cập nhật metadata (<code>shop_id</code>, <code>title</code>, <code>category_id</code>) khi cặp (platform_id, platform_item_id) đã tồn tại; trả về dòng đã ghi kèm <code>id</code>.</li><li><strong>MUST</strong> thực hiện upsert idempotent bằng <code>INSERT ... ON CONFLICT (platform_id, platform_item_id) DO UPDATE</code>: gọi nhiều lần với cùng cặp khóa <strong>MUST</strong> giữ nguyên một dòng (cùng <code>id</code>, cùng <code>first_seen</code>).</li><li><strong>MUST</strong> giữ <code>first_seen</code> bất biến qua các lần upsert: nhánh <code>DO UPDATE</code> <strong>MUST NOT</strong> ghi đè <code>first_seen</code> (mốc cold-start phải là lần thấy đầu tiên, không phải lần gần nhất).</li><li><strong>MUST</strong> expose <code>GetByID(ctx, id int64) (TrackedProduct, error)</code> và <code>FindByPlatformItem(ctx, platformID int16, platformItemID string) (TrackedProduct, error)</code> để tra cứu theo khóa kỹ thuật và theo khóa tự nhiên.</li><li><strong>MUST</strong> expose <code>GetByCanonicalKey(ctx, key string) ([]TrackedProduct, error)</code>: trả về mọi SKU chia sẻ cùng <code>canonical_key</code> (các biến thể của một sản phẩm trên nhiều sàn). Tra cứu bằng <code>key = NULL</code> không hợp lệ, hàm <strong>MUST</strong> trả lỗi tham số.</li><li><strong>SHOULD</strong> phát OTel metric <code>tracked_product_upsert_total{platform_id, action}</code> với <code>action - {insert, update}</code> để theo dõi nhịp tăng trưởng registry và tỷ lệ SKU mới.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao tracked_product là registry chuẩn hóa một dòng (DEC-PRICE-06)?</strong> Cùng một SKU được scraper quét lại nhiều lần mỗi ngày. Nếu mỗi lần quét tạo một dòng, registry phình vô tận và <code>price_snapshot</code> trỏ tới hàng loạt <code>id</code> rác cho cùng một sản phẩm, làm biểu đồ giá vỡ. Một dòng cho mỗi (platform_id, platform_item_id) cho ta một danh tính ổn định để mọi snapshot giá neo vào.</p>\n<p><strong>Vì sao tách canonical_key khỏi thuật toán matching (DEC-PRICE-07)?</strong> So khớp chéo sàn (cùng một chiếc tai nghe bán trên Shopee, TikTok, Lazada) là bài toán khó: tên khác nhau, ảnh khác nhau, không có mã chung. Nhồi thuật toán đó vào task schema này làm phình phạm vi và chặn các task phụ thuộc. task này chỉ dựng cột <code>canonical_key</code> (nullable) và index; TASK-PRICE-005 chạy so khớp và điền giá trị sau. Schema sẵn sàng trước, thuật toán đến sau mà không phải đổi bảng.</p>\n<p><strong>Vì sao UNIQUE(platform_id, platform_item_id) (DEC-PRICE-08)?</strong> <code>platform_item_id</code> chỉ duy nhất trong phạm vi một sàn, không phải toàn cục: Shopee và Lazada có thể trùng chuỗi item_id cho hai sản phẩm khác nhau. Ràng buộc kép (sàn, item) là khóa tự nhiên đúng, đồng thời là khóa <code>ON CONFLICT</code> cho upsert idempotent với retry của scraper.</p>\n<p><strong>Vì sao index idx_tp_canonical (DEC-PRICE-09)?</strong> Moat sản phẩm của SănDeal là so sánh giá cùng một món trên nhiều sàn (§5.6). Truy vấn lõi là \"cho canonical_key X, lấy mọi SKU của mọi sàn\". Không index thì JOIN này quét toàn bảng tracked_product (hàng triệu dòng). Index trên <code>canonical_key</code> biến nó thành tra cứu range nhanh.</p>\n<p><strong>Vì sao first_seen DEFAULT now() và bất biến (DEC-PRICE-10, §1 #9)?</strong> Sale ảo cần biết lịch sử giá đủ dài. <code>first_seen</code> đánh dấu khi SKU vào hệ thống để biết cửa sổ dữ liệu có bao nhiêu. Nếu nhánh upsert ghi đè <code>first_seen</code> mỗi lần quét, mốc cold-start luôn nhảy về hiện tại và mọi SKU trông như \"mới\", phá logic cắt cửa sổ. Vì vậy <code>DO UPDATE</code> chỉ chạm metadata, không chạm <code>first_seen</code>.</p>\n<p><strong>Vì sao canonical_key để NULL khi insert (§1 #4)?</strong> Lúc scraper phát hiện SKU, ta chưa biết nó là biến thể của sản phẩm nào. NULL nghĩa là \"chưa so khớp\". TASK-PRICE-005 quét các dòng NULL và gán nhóm. Điền sẵn một giá trị đoán mò ở task này sẽ tạo nhóm sai mà sau khó dọn.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/price/migrations/0001_tracked_product.sql\nCREATE TABLE tracked_product (\n  id               BIGSERIAL   PRIMARY KEY,\n  platform_id      SMALLINT    NOT NULL REFERENCES platform(id),\n  platform_item_id TEXT        NOT NULL,\n  shop_id          TEXT,\n  title            TEXT,\n  category_id      BIGINT,\n  canonical_key    TEXT,                                  -- NULL cho tới khi TASK-PRICE-005 so khớp\n  first_seen       TIMESTAMPTZ NOT NULL DEFAULT now(),    -- mốc cold-start, bất biến\n  UNIQUE (platform_id, platform_item_id)\n);\n\n-- Phục vụ JOIN gom SKU cùng sản phẩm trên nhiều sàn (moat so sánh chéo §5.6)\nCREATE INDEX idx_tp_canonical ON tracked_product (canonical_key);</code></pre>\n<h3>Types (Go)</h3>\n<pre><code class=\"language-go\">// services/price/internal/price/product.go\ntype TrackedProduct struct {\n    ID             int64     `db:\"id\"`\n    PlatformID     int16     `db:\"platform_id\"`\n    PlatformItemID string    `db:\"platform_item_id\"`\n    ShopID         *string   `db:\"shop_id\"`\n    Title          *string   `db:\"title\"`\n    CategoryID     *int64    `db:\"category_id\"`\n    CanonicalKey   *string   `db:\"canonical_key\"` // nil khi chưa so khớp (TASK-PRICE-005 điền)\n    FirstSeen      time.Time `db:\"first_seen\"`\n}</code></pre>\n<h3>Upsert idempotent (§1 #7, #8, #9)</h3>\n<pre><code class=\"language-go\">// services/price/internal/price/product_repo.go\n// Upsert chèn SKU mới hoặc cập nhật metadata khi (platform_id, platform_item_id) đã có.\n// canonical_key KHÔNG được ghi ở đây - để NULL, TASK-PRICE-005 điền sau.\n// first_seen KHÔNG bị nhánh DO UPDATE ghi đè (giữ mốc cold-start).\nfunc (r *Repo) Upsert(ctx context.Context, p TrackedProduct) (TrackedProduct, error) {\n    var out TrackedProduct\n    err := r.pool.QueryRow(ctx,\n        `INSERT INTO tracked_product\n            (platform_id, platform_item_id, shop_id, title, category_id)\n         VALUES ($1, $2, $3, $4, $5)\n         ON CONFLICT (platform_id, platform_item_id) DO UPDATE\n            SET shop_id     = EXCLUDED.shop_id,\n                title       = EXCLUDED.title,\n                category_id = EXCLUDED.category_id\n         RETURNING id, platform_id, platform_item_id, shop_id, title,\n                   category_id, canonical_key, first_seen`,\n        p.PlatformID, p.PlatformItemID, p.ShopID, p.Title, p.CategoryID).\n        Scan(&amp;out.ID, &amp;out.PlatformID, &amp;out.PlatformItemID, &amp;out.ShopID,\n            &amp;out.Title, &amp;out.CategoryID, &amp;out.CanonicalKey, &amp;out.FirstSeen)\n    if err != nil {\n        return TrackedProduct{}, err\n    }\n    metrics.ProductUpsert(out.PlatformID)\n    return out, nil\n}\n\nfunc (r *Repo) GetByCanonicalKey(ctx context.Context, key string) ([]TrackedProduct, error) {\n    if key == \"\" {\n        return nil, fmt.Errorf(\"canonical_key rỗng không hợp lệ\")\n    }\n    rows, err := r.pool.Query(ctx,\n        `SELECT id, platform_id, platform_item_id, shop_id, title,\n                category_id, canonical_key, first_seen\n         FROM tracked_product WHERE canonical_key = $1`, key)\n    if err != nil {\n        return nil, err\n    }\n    defer rows.Close()\n    return scanProducts(rows)\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch -&gt; bảng <code>tracked_product</code> tồn tại với đủ cột, PK <code>id</code>, và <code>first_seen</code> DEFAULT now().</li><li>FK <code>platform_id</code> REFERENCES <code>platform(id)</code> tồn tại; INSERT với <code>platform_id</code> không có trong <code>platform</code> -&gt; lỗi FK.</li><li>INSERT hai dòng cùng <code>(platform_id, platform_item_id)</code> qua đường thẳng (không upsert) -&gt; lỗi UNIQUE.</li><li><code>canonical_key</code> chấp nhận NULL; sau insert qua <code>Upsert</code>, giá trị đọc lại là NULL (chưa so khớp).</li><li>Index <code>idx_tp_canonical</code> tồn tại (<code>SELECT ... FROM pg_indexes WHERE indexname='idx_tp_canonical'</code> trả 1 dòng).</li><li><code>Upsert</code> SKU mới -&gt; trả về dòng có <code>id &gt; 0</code> và <code>first_seen</code> được DB gán.</li><li><code>Upsert</code> lần hai cùng <code>(platform_id, platform_item_id)</code> với metadata khác -&gt; trả về CÙNG <code>id</code>, <code>title</code>/<code>shop_id</code>/<code>category_id</code> đã cập nhật, tổng số dòng vẫn là 1.</li><li><code>Upsert</code> lần hai -&gt; <code>first_seen</code> KHÔNG đổi so với lần đầu (mốc cold-start bất biến).</li><li><code>GetByID</code> trả đúng dòng đã upsert; <code>GetByID</code> với id không tồn tại -&gt; lỗi no-rows.</li><li><code>FindByPlatformItem(platformID, itemID)</code> trả đúng dòng theo khóa tự nhiên.</li><li><code>GetByCanonicalKey(key)</code> trả mọi SKU chia sẻ <code>key</code>; gọi với chuỗi rỗng -&gt; lỗi tham số, không truy vấn DB.</li><li>Metric <code>tracked_product_upsert_total</code> tăng mỗi lần <code>Upsert</code> thành công.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/price/internal/price/product_repo_test.go\nfunc TestUpsert_New(t *testing.T) {\n    r, plat := setupRepoWithPlatform(t) // seed platform(id=1,'shopee','VN')\n    p := TrackedProduct{PlatformID: plat, PlatformItemID: \"i-123\", Title: ptr(\"Tai nghe X\")}\n    out, err := r.Upsert(ctx, p)\n    require.NoError(t, err)\n    require.Greater(t, out.ID, int64(0))\n    require.False(t, out.FirstSeen.IsZero())\n    require.Equal(t, 1, countProducts(t, r))\n}\n\nfunc TestUpsert_Conflict_Idempotent(t *testing.T) {\n    r, plat := setupRepoWithPlatform(t)\n    a, _ := r.Upsert(ctx, TrackedProduct{PlatformID: plat, PlatformItemID: \"i-123\", Title: ptr(\"Tên cũ\")})\n    b, _ := r.Upsert(ctx, TrackedProduct{PlatformID: plat, PlatformItemID: \"i-123\", Title: ptr(\"Tên mới\")})\n    require.Equal(t, a.ID, b.ID)              // cùng một dòng\n    require.Equal(t, \"Tên mới\", *b.Title)     // metadata đã cập nhật\n    require.Equal(t, a.FirstSeen, b.FirstSeen) // first_seen bất biến\n    require.Equal(t, 1, countProducts(t, r))   // không nhân bản\n}\n\nfunc TestUnique_PlatformItem(t *testing.T) {\n    r, plat := setupRepoWithPlatform(t)\n    _, err := r.pool.Exec(ctx,\n        `INSERT INTO tracked_product (platform_id, platform_item_id) VALUES ($1,$2),($1,$2)`,\n        plat, \"dup\")\n    require.Error(t, err) // vi phạm UNIQUE(platform_id, platform_item_id)\n}\n\nfunc TestGetByCanonicalKey(t *testing.T) {\n    r, plat := setupRepoWithPlatform(t)\n    a, _ := r.Upsert(ctx, TrackedProduct{PlatformID: plat, PlatformItemID: \"i-1\"})\n    c, _ := r.Upsert(ctx, TrackedProduct{PlatformID: plat, PlatformItemID: \"i-2\"})\n    // mô phỏng TASK-PRICE-005 gán cùng canonical_key\n    r.pool.Exec(ctx, `UPDATE tracked_product SET canonical_key='k-xyz' WHERE id = ANY($1)`,\n        []int64{a.ID, c.ID})\n    rows, err := r.GetByCanonicalKey(ctx, \"k-xyz\")\n    require.NoError(t, err)\n    require.Len(t, rows, 2)\n\n    _, err = r.GetByCanonicalKey(ctx, \"\")\n    require.Error(t, err) // chuỗi rỗng → lỗi tham số\n}\n\nfunc TestCanonicalKey_NullOnInsert(t *testing.T) {\n    r, plat := setupRepoWithPlatform(t)\n    out, _ := r.Upsert(ctx, TrackedProduct{PlatformID: plat, PlatformItemID: \"i-9\"})\n    require.Nil(t, out.CanonicalKey) // task này KHÔNG điền canonical_key\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration <code>0001_tracked_product.sql</code> (bảng + UNIQUE + index) -&gt; <code>product.go</code> (struct) -&gt; <code>product_repo.go</code> (Upsert + 3 hàm tra cứu) -&gt; tests. Migration chạy qua golang-migrate (cùng runner của TASK-INFRA-002). Index <code>idx_tp_canonical</code> tạo cùng migration, trên bảng còn rỗng nên không khóa lâu. Driver dùng <code>pgx</code>. Hàm <code>Upsert</code> dùng <code>RETURNING</code> để lấy lại <code>id</code> và <code>first_seen</code> do DB sinh, tránh một round-trip đọc thêm.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-INFRA-002</strong> - bảng <code>platform</code> và framework migration phải có trước (FK <code>platform_id</code>, runner golang-migrate).</li><li><strong>TASK-PRICE-002 (downstream)</strong> - <code>price_snapshot.product_id</code> REFERENCES <code>tracked_product(id)</code>; cần bảng này tồn tại trước.</li><li><strong>TASK-PRICE-005 (downstream)</strong> - thuật toán so khớp chéo sàn UPDATE <code>canonical_key</code> cho các dòng NULL; đọc qua index <code>idx_tp_canonical</code>.</li><li><strong>TASK-SCRAPE-002 (upstream)</strong> - scraper gọi <code>Upsert</code> khi phát hiện hoặc làm mới một SKU.</li><li><strong>TASK-TRACK-001 (downstream)</strong> - API theo dõi giá gắn người dùng vào <code>tracked_product.id</code>.</li><li>Lib: driver <code>pgx</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Scraper đăng ký một SKU (nội bộ)</h3>\n<pre><code class=\"language-go\">tp, err := productRepo.Upsert(ctx, price.TrackedProduct{\n    PlatformID:     1,                       // shopee (theo platform seed TASK-INFRA-002)\n    PlatformItemID: \"20114455667\",\n    ShopID:         ptr(\"88123\"),\n    Title:          ptr(\"Tai nghe Bluetooth ABC Pro\"),\n    CategoryID:     ptr(int64(7021)),\n    // CanonicalKey để trống - TASK-PRICE-005 điền sau\n})\n// tp.ID dùng làm product_id khi ghi price_snapshot (TASK-PRICE-002)</code></pre>\n<h3>So sánh chéo sàn - gom mọi SKU cùng một sản phẩm</h3>\n<pre><code class=\"language-sql\">-- Dùng index idx_tp_canonical: với một canonical_key, lấy giá mới nhất trên từng sàn\nSELECT tp.platform_id, tp.platform_item_id, tp.title, tp.id\nFROM tracked_product tp\nWHERE tp.canonical_key = 'ck-earbud-abc-pro'\nORDER BY tp.platform_id;</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Định dạng nội bộ của <code>canonical_key</code> (hash chuẩn hóa hay id cụm) - thuộc TASK-PRICE-005, không ảnh hưởng schema ở đây.</li><li>Lưu lịch sử đổi <code>title</code>/<code>category_id</code> (audit metadata) - chỉ thêm nếu cần truy vết, slice sau.</li><li>Đánh dấu SKU \"đã ngừng bán\" (cột <code>delisted_at</code>) - bổ sung khi scraper báo 404; không cản task này.</li><li>Soft-delete vs hard-delete dòng tracked_product - giữ đơn giản tới khi có yêu cầu xóa dữ liệu (TASK-COMPLY).</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>platform_id không có trong platform</td><td>lỗi FK pgx</td><td>từ chối ghi</td><td>Seed platform (TASK-INFRA-002) chạy trước; scraper map đúng id</td></tr><tr><td>Trùng (platform_id, platform_item_id) qua insert thẳng</td><td>DB UNIQUE</td><td>lỗi ghi</td><td>Luôn dùng Upsert (ON CONFLICT), không INSERT trần</td></tr><tr><td>Nhánh DO UPDATE ghi đè first_seen</td><td>test idempotent</td><td>mốc cold-start nhảy về hiện tại</td><td>DO UPDATE chỉ chạm shop_id/title/category_id</td></tr><tr><td>Insert kèm canonical_key (vi phạm ranh giới)</td><td>code review + test NullOnInsert</td><td>nhóm chéo sàn sai</td><td>Upsert không nhận canonical_key; chỉ TASK-PRICE-005 UPDATE</td></tr><tr><td>GetByCanonicalKey gọi với chuỗi rỗng</td><td>guard tham số</td><td>quét nhầm dòng NULL</td><td>Trả lỗi tham số trước khi truy vấn</td></tr><tr><td>platform_item_id NULL hoặc rỗng</td><td>NOT NULL (NULL) / validate (rỗng)</td><td>dòng vô danh</td><td>NOT NULL ở DB; scraper validate rỗng trước khi gửi</td></tr><tr><td>Index idx_tp_canonical thiếu</td><td>EXPLAIN seq scan</td><td>JOIN chéo sàn chậm</td><td>Migration tạo index; AC #5 kiểm tồn tại</td></tr><tr><td>Registry phình do quét tạo dòng mới</td><td>đếm dòng / metric upsert</td><td>price_snapshot trỏ id rác</td><td>UNIQUE + Upsert đảm bảo một dòng mỗi SKU</td></tr><tr><td>Race hai scraper upsert cùng SKU</td><td>ON CONFLICT</td><td>một insert, một update</td><td>Theo thiết kế (idempotent); không vỡ</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li><code>tracked_product</code> là điểm neo của module PRICE: mọi snapshot giá, mọi nhóm chéo sàn, mọi theo dõi của người dùng đều trỏ về <code>id</code> của bảng này.</li><li>Tách <code>canonical_key</code> (cột + index ở task này) khỏi thuật toán so khớp (TASK-PRICE-005) cho phép schema sẵn sàng trước, không chặn các task phụ thuộc chờ thuật toán khó.</li><li><code>UNIQUE(platform_id, platform_item_id)</code> vừa là khóa tự nhiên đúng (item_id chỉ duy nhất trong một sàn), vừa là khóa idempotent cho upsert với retry của scraper.</li><li><code>first_seen</code> bất biến là chi tiết dễ sai nhất: nhánh <code>DO UPDATE</code> phải bỏ qua nó, nếu không mốc cold-start hỏng và logic cắt cửa sổ lịch sử giá lệch.</li><li>Index <code>idx_tp_canonical</code> là hạ tầng cho moat so sánh chéo sàn (§5.6); dòng <code>canonical_key IS NULL</code> (chưa so khớp) không cản tra cứu theo giá trị.</li><li>Khi mở SEA, <code>platform</code> thêm dòng cho sàn nước khác; <code>tracked_product</code> không đổi cấu trúc, chỉ thêm SKU mới với <code>platform_id</code> tương ứng.</li></ul>\n<hr>\n<p><em>Hết TASK-PRICE-001. Status: done (mục tiêu audit 10/10).</em></p>";
