window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-TRUST-003"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>SănDeal <strong>MUST</strong> cung cấp một bộ hook security audit tái chạy được, chứng minh bằng quan sát thực tế (không tin self-report) rằng extension KHÔNG gửi cookie/mật khẩu/token ra ngoài, kèm SBOM + verify reproducible build, và xuất báo cáo bên thứ ba tự chạy lại được. Hợp đồng:</p>\n<ol><li>Audit <strong>MUST</strong> là tập hook TÁI CHẠY ĐƯỢC (egress test + SBOM/vuln scan + verify reproducible build + payload guard), KHÔNG phải báo cáo một lần (DEC-TRUST-11). Bên thứ ba chạy lại phải ra cùng kết luận.</li><li><strong>MUST</strong> có egress test (<code>egress-guard.test.ts</code> + <code>network-trap.ts</code>): chặn toàn bộ mạng, cho extension chạy luồng đọc giỏ thật (qua Playwright), bắt MỌI request outbound, và assert KHÔNG request nào chứa cookie phiên sàn, token, mật khẩu, hay PII (DEC-TRUST-12). Kiểm tại BIÊN MẠNG, không dựa lời khai của code.</li><li>Egress test <strong>MUST</strong> kiểm cả URL, header, và body của mỗi outbound; bất kỳ chuỗi giống cookie/token/PII trong bất kỳ phần nào -&gt; test FAIL.</li><li>Egress test <strong>MUST</strong> chỉ cho phép outbound tới endpoint backend SănDeal đã biết với payload đúng allowlist (<code>OutboundPayload</code> TASK-EXT-003); request tới host lạ -&gt; FAIL (chống rò qua kênh bên).</li><li><strong>MUST</strong> sinh SBOM định dạng CycloneDX (<code>generate-sbom.sh</code>) liệt kê mọi dependency + phiên bản + hash; và quét lỗ hổng đã biết (<code>scan-vulnerabilities.sh</code>) (DEC-TRUST-13). Dependency phải pin để SBOM ổn định.</li><li>SBOM scan <strong>MUST</strong> FAIL audit nếu phát hiện CVE mức cao/nghiêm trọng chưa có miễn trừ ghi rõ lý do.</li><li><code>run-security-audit.sh</code> <strong>MUST</strong> orchestrate và gọi như thành phần con (DEC-TRUST-14): (a) egress test, (b) SBOM + vuln scan, (c) <code>verify-reproducible.sh</code> (TASK-TRUST-001), (d) <code>payload_guard</code> (TASK-COMPLY-005). Audit PASS chỉ khi MỌI hook con PASS (DEC-TRUST-15).</li><li>Audit <strong>MUST</strong> xuất báo cáo có cấu trúc: JSON (máy đọc) + markdown (người đọc) liệt kê từng hook, kết quả, và bằng chứng (hash, danh sách outbound đã quan sát, số CVE).</li><li><strong>MUST</strong> có <code>THIRD-PARTY-AUDIT-GUIDE.md</code>: hướng dẫn người ngoài clone repo + chạy lại toàn bộ audit trên máy họ và đối chiếu kết quả - điều kiện để \"audit độc lập\" có nghĩa.</li><li>CI publish gate (TASK-TRUST-001) <strong>MUST</strong> gọi <code>run-security-audit.sh</code>; audit FAIL <strong>MUST</strong> chặn ship lên store.</li><li>Egress test <strong>MUST</strong> bao phủ luồng nhạy cảm nhất: đọc giỏ hàng có phiên đăng nhập sàn (nơi cookie hiện diện) - đảm bảo ngay cả khi cookie có sẵn trong context, nó KHÔNG rời máy.</li><li><strong>MUST NOT</strong> có nhánh nào trong audit bỏ qua hook con khi \"chạy nhanh\"/\"môi trường CI khác\"; mọi lần chạy phải đủ bốn hook hoặc audit báo INCOMPLETE (không được ngầm PASS).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao kiểm tại biên mạng, không tin self-report (DEC-TRUST-12)?</strong> Code có thể \"nói\" nó không gửi cookie, nhưng một bug, một thư viện bên thứ ba, hay một đường fetch quên lọc vẫn có thể mang cookie ra ngoài. Kiểm tĩnh (grep, TASK-COMPLY-005) bắt được mẫu đã biết, nhưng không thấy hành vi runtime. Egress test chặn tất cả rồi quan sát đúng những gì thật sự rời máy - đây là bằng chứng động, mạnh hơn mọi lời khai trong mã.</p>\n<p><strong>Vì sao audit phải tái chạy được (DEC-TRUST-11)?</strong> Một báo cáo PDF \"đã được hãng X audit\" là \"tin tôi đi\" - người dùng không kiểm lại được, và nó cũ ngay khi mã đổi. Audit của SănDeal là tập hook ai cũng chạy lại được: clone repo, chạy script, xem cùng kết quả. Đây là khác biệt cốt lõi hậu-Honey: bằng chứng kiểm chứng được thay cho uy tín mượn.</p>\n<p><strong>Vì sao SBOM + vuln scan (DEC-TRUST-13)?</strong> \"Không gửi cookie\" không đủ nếu một dependency độc hại làm việc đó sau lưng. Supply-chain là kênh tấn công thật. SBOM liệt kê chính xác mọi thứ extension phụ thuộc (+ hash), vuln scan bắt lỗ hổng đã biết. Pin dependency làm SBOM ổn định để tái lập và để biết chính xác cái gì chạy.</p>\n<p><strong>Vì sao chỉ cho outbound tới endpoint đã biết với payload đúng allowlist (§1 #4)?</strong> Rò dữ liệu không chỉ qua \"gửi cookie tới backend\" - còn có thể qua gửi dữ liệu tới host lạ (kênh bên). Whitelist endpoint + assert payload khớp allowlist TASK-EXT-003 đóng cả hai: đúng đích VÀ đúng nội dung. Bất kỳ outbound ngoài luật -&gt; đỏ.</p>\n<p><strong>Vì sao audit là lớp tổng hợp gọi các hook con (DEC-TRUST-14)?</strong> TASK-TRUST-001 lo reproducible, TASK-COMPLY-005 lo kiểm tĩnh + payload guard. TASK-TRUST-003 không lặp lại chúng mà gọi chúng như mảnh ghép và thêm mảnh còn thiếu (egress động + SBOM), rồi tổng hợp thành \"bằng chứng đầu cuối\". Audit PASS chỉ khi cả chuỗi PASS - một mắt xích đỏ là cả audit đỏ.</p>\n<p><strong>Vì sao bao phủ luồng có phiên đăng nhập (§1 #11)?</strong> Điểm nhạy cảm nhất là khi cookie phiên sàn ĐANG hiện diện trong context (user đã đăng nhập, extension đọc giỏ). Nếu test chạy không có phiên, nó không chứng minh được gì về tình huống thật. Phải đặt cookie vào context rồi chứng minh nó vẫn không rời máy - đó mới là bằng chứng đúng kịch bản lo ngại.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>network-trap.ts (bắt mọi outbound)</h3>\n<pre><code class=\"language-ts\">// audit/egress/network-trap.ts\nimport type { Page, Request } from \"playwright\";\n\nexport interface Outbound { url: string; headers: Record&lt;string,string&gt;; body: string; }\n\nconst BACKEND_HOST = \"api.sandeal.vn\";              // endpoint hợp lệ DUY NHẤT\nconst CRED_RE = /(SPC_|session|sessionid|token|bearer|password|mật khẩu|@[\\w.]+\\.\\w+)/i;\n\nexport function installTrap(page: Page, captured: Outbound[]) {\n  page.on(\"request\", (req: Request) =&gt; {\n    captured.push({ url: req.url(), headers: req.headers(), body: req.postData() ?? \"\" });\n  });\n}\n\nexport function assertNoCredentialEgress(captured: Outbound[]) {\n  for (const o of captured) {\n    const host = new URL(o.url).host;\n    if (host !== BACKEND_HOST)\n      throw new Error(`outbound tới host lạ: ${host} (chỉ cho ${BACKEND_HOST})`);\n    const blob = `${o.url}\\n${JSON.stringify(o.headers)}\\n${o.body}`;\n    if (CRED_RE.test(blob))\n      throw new Error(`PHÁT HIỆN credential/PII rời máy trong request tới ${host}`);\n  }\n}</code></pre>\n<h3>egress-guard.test.ts (chạy luồng thật, có phiên đăng nhập)</h3>\n<pre><code class=\"language-ts\">// audit/egress/egress-guard.test.ts\nimport { chromium } from \"playwright\";\nimport { installTrap, assertNoCredentialEgress, Outbound } from \"./network-trap\";\n\ntest(\"đọc giỏ có cookie phiên sàn -&gt; KHÔNG cookie/token rời máy\", async () =&gt; {\n  const browser = await chromium.launch();\n  const ctx = await browser.newContext();\n  // đặt cookie phiên sàn vào context (mô phỏng user đã đăng nhập - kịch bản nhạy cảm nhất)\n  await ctx.addCookies([{ name: \"SPC_SESSION\", value: \"eyJ...secret\", domain: \".shopee.vn\", path: \"/\" }]);\n  const page = await ctx.newPage();\n  const captured: Outbound[] = [];\n  installTrap(page, captured);\n\n  await loadExtensionAndReadCart(page);     // chạy luồng đọc giỏ thật (TASK-EXT-002/003)\n\n  assertNoCredentialEgress(captured);       // assert tại biên mạng\n  await browser.close();\n});</code></pre>\n<h3>run-security-audit.sh (orchestrate, FAIL nếu hook con đỏ)</h3>\n<pre><code class=\"language-bash\">#!/usr/bin/env bash\n# audit/run-security-audit.sh\nset -euo pipefail\nFAIL=0\n\necho \"== [1/4] Egress test (động) ==\"\n( cd extension &amp;&amp; npx playwright test ../audit/egress ) || FAIL=1\n\necho \"== [2/4] SBOM + vuln scan ==\"\nbash audit/sbom/generate-sbom.sh &amp;&amp; bash audit/sbom/scan-vulnerabilities.sh || FAIL=1\n\necho \"== [3/4] Verify reproducible build (TASK-TRUST-001) ==\"\nbash extension/scripts/verify-reproducible.sh \"$(git rev-parse HEAD)\" \"${SHIPPED:-extension/dist.zip}\" || FAIL=1\n\necho \"== [4/4] Payload guard tĩnh (TASK-COMPLY-005) ==\"\n( cd services/comply &amp;&amp; go test ./internal/audit/... ) || FAIL=1\n\nnode audit/report/build-report.ts          # tổng hợp JSON + markdown\nif [ \"$FAIL\" -ne 0 ]; then echo \"AUDIT FAIL\"; exit 1; fi\necho \"AUDIT PASS - bằng chứng đầu cuối: KHÔNG cookie/mật khẩu rời máy\"</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>run-security-audit.sh</code> chạy đủ bốn hook con; thiếu hook nào -&gt; báo INCOMPLETE, không ngầm PASS.</li><li>Egress test đặt cookie phiên vào context, chạy luồng đọc giỏ, bắt mọi outbound; luồng sạch -&gt; PASS.</li><li>Cố tình chèn một fetch lén gửi cookie -&gt; egress test FAIL (bắt được tại biên mạng).</li><li>Outbound tới host khác <code>api.sandeal.vn</code> -&gt; egress test FAIL (chống kênh bên).</li><li>Outbound mang payload ngoài allowlist (TASK-EXT-003) -&gt; FAIL.</li><li><code>generate-sbom.sh</code> xuất SBOM CycloneDX liệt kê mọi dependency + hash.</li><li><code>scan-vulnerabilities.sh</code> chèn một dependency có CVE cao đã biết -&gt; audit FAIL (trừ khi có miễn trừ ghi lý do).</li><li><code>run-security-audit.sh</code> gọi <code>verify-reproducible.sh</code> (TASK-TRUST-001) + <code>payload_guard</code> (TASK-COMPLY-005); một trong hai đỏ -&gt; audit FAIL.</li><li>Audit xuất báo cáo JSON + markdown liệt kê từng hook + kết quả + bằng chứng (hash, danh sách outbound, số CVE).</li><li><code>THIRD-PARTY-AUDIT-GUIDE.md</code> đủ để người ngoài clone + chạy lại + đối chiếu; chạy thử theo guide ra cùng kết luận.</li><li>CI publish gate gọi audit; audit FAIL chặn ship lên store.</li><li>Audit PASS chỉ khi cả bốn hook PASS (không hook nào bị bỏ qua trong \"chế độ nhanh\").</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-ts\">// audit/egress/egress-guard.test.ts (xem §3) - bằng chứng động chính.\n\n// Test \"negative control\": chèn rò có chủ đích để chứng minh test BẮT được\ntest(\"negative control: fetch lén gửi cookie -&gt; egress test phải FAIL\", async () =&gt; {\n  const captured = [{ url: \"https://api.sandeal.vn/x\", headers: { cookie: \"SPC_SESSION=eyJ...\" }, body: \"\" }];\n  expect(() =&gt; assertNoCredentialEgress(captured)).toThrow(/credential\\/PII/);\n});\n\ntest(\"negative control: outbound host lạ -&gt; FAIL\", () =&gt; {\n  const captured = [{ url: \"https://evil.example/x\", headers: {}, body: \"{}\" }];\n  expect(() =&gt; assertNoCredentialEgress(captured)).toThrow(/host lạ/);\n});</code></pre>\n<pre><code class=\"language-bash\"># Chạy toàn bộ audit (giống bên thứ ba sẽ chạy)\nbash audit/run-security-audit.sh\n\n# Kiểm báo cáo có cấu trúc tồn tại + liệt kê đủ 4 hook\njq '.hooks | length == 4' audit/report/audit-report.json\n\n# Theo THIRD-PARTY-AUDIT-GUIDE.md, một người ngoài chạy:\ngit clone &lt;repo&gt; &amp;&amp; cd &lt;repo&gt; &amp;&amp; bash audit/run-security-audit.sh   # PHẢI ra cùng PASS</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>network-trap.ts</code> (bắt + assert outbound) -&gt; <code>egress-guard.test.ts</code> (chạy luồng đọc giỏ có phiên qua Playwright) -&gt; <code>generate-sbom.sh</code> + <code>scan-vulnerabilities.sh</code> (CycloneDX + CVE) -&gt; <code>run-security-audit.sh</code> (orchestrate bốn hook, gọi <code>verify-reproducible.sh</code> của TASK-TRUST-001 + <code>payload_guard</code> của TASK-COMPLY-005) -&gt; <code>build-report.ts</code> + template (JSON + markdown) -&gt; <code>THIRD-PARTY-AUDIT-GUIDE.md</code> (hướng dẫn tái chạy) -&gt; nối vào CI publish gate. Audit là lớp tổng hợp: nó không lặp lại reproducible/payload_guard mà gọi chúng và thêm egress động + SBOM. Mọi hook đỏ là audit đỏ; mọi lần chạy đủ bốn hook hoặc báo INCOMPLETE.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-EXT-003</strong> - allowlist <code>OutboundPayload</code> là chuẩn để egress test assert payload đúng (đúng nội dung rời máy).</li><li><strong>TASK-COMPLY-005</strong> - <code>payload_guard</code> (kiểm tĩnh + payload nhận từ extension không chứa cookie/token) là một hook con của audit; TASK-TRUST-003 thêm tầng động.</li><li><strong>TASK-TRUST-001</strong> - <code>verify-reproducible.sh</code> là một hook con; reproducible build + source công khai làm bằng chứng \"mã chạy = mã công khai\".</li><li><strong>TASK-TRUST-002</strong> - chính sách + data-flow là tài liệu đối chiếu cho audit (audit chứng minh hành vi khớp chính sách công bố).</li><li>Hạ tầng: Playwright (network intercept), CycloneDX (SBOM), trình quét CVE; CI (GitHub Actions) cho publish gate.</li><li>NFR-TRUST-001 - audit này là phép verification chính cho ngưỡng \"token không rời client\".</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Báo cáo audit (markdown, trích)</h3>\n<pre><code class=\"language-markdown\"># SănDeal Security Audit - v1.4.0 (commit a1b2c3d)\n| Hook | Kết quả | Bằng chứng |\n|---|---|---|\n| Egress (động) | PASS | 3 outbound, tất cả tới api.sandeal.vn, 0 cookie/token/PII |\n| SBOM + vuln | PASS | 41 dependency (hash kèm), 0 CVE cao |\n| Reproducible build | PASS | rebuilt SHA-256 == shipped 9f2c...e71a |\n| Payload guard (tĩnh) | PASS | 0 finding cleartext/token |\n=&gt; AUDIT PASS. Tự kiểm: `bash audit/run-security-audit.sh` (xem THIRD-PARTY-AUDIT-GUIDE.md)</code></pre>\n<h3>Báo cáo audit (JSON, trích để máy đọc)</h3>\n<pre><code class=\"language-json\">{\n  \"version\": \"1.4.0\", \"commit\": \"a1b2c3d\", \"verdict\": \"PASS\",\n  \"hooks\": [\n    { \"name\": \"egress\", \"pass\": true, \"outbound_count\": 3, \"credential_leaks\": 0 },\n    { \"name\": \"sbom\",   \"pass\": true, \"deps\": 41, \"high_cve\": 0 },\n    { \"name\": \"reproducible\", \"pass\": true, \"shipped_sha256\": \"9f2c...e71a\" },\n    { \"name\": \"payload_guard\", \"pass\": true, \"findings\": 0 }\n  ]\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Thuê hãng audit bên ngoài ký xác nhận chạy lại bộ hook - bổ sung uy tín, nhưng cơ chế tái chạy đã là bằng chứng nền; xét khi có ngân sách.</li><li>Egress test cho TikTok Shop/Lazada (P2) - cùng bộ test, thêm khi content script hai sàn đó ship (TASK-EXT-007/008).</li><li>Fuzzing payload đầu vào content script để tìm kênh rò ẩn - lớp kiểm sâu hơn, giai đoạn sau.</li><li>Giám sát egress liên tục ở production (không chỉ trong CI) - cần hạ tầng quan sát phía client; xét ở giai đoạn trưởng thành.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Cookie rời máy qua đường grep không thấy</td><td>egress test (động)</td><td>rò credential runtime</td><td>Kiểm tại biên mạng, không tin self-report (DEC-TRUST-12)</td></tr><tr><td>Rò qua host lạ (kênh bên)</td><td>egress assert host</td><td>dữ liệu ra ngoài backend</td><td>Whitelist endpoint + assert payload (§1 #4)</td></tr><tr><td>Dependency độc hại gửi dữ liệu</td><td>SBOM + vuln scan</td><td>supply-chain compromise</td><td>SBOM + pin + quét CVE (DEC-TRUST-13)</td></tr><tr><td>Báo cáo audit một lần, không tái chạy</td><td>review (thiếu guide)</td><td>\"tin tôi đi\", cũ khi mã đổi</td><td>Hook tái chạy + THIRD-PARTY guide (DEC-TRUST-11)</td></tr><tr><td>Audit PASS dù một hook con đỏ</td><td>run-security-audit exit code</td><td>bằng chứng giả</td><td>PASS chỉ khi cả bốn hook PASS (DEC-TRUST-15)</td></tr><tr><td>Test chạy không có phiên đăng nhập</td><td>review fixture</td><td>không chứng minh kịch bản thật</td><td>Đặt cookie vào context trước khi chạy (§1 #11)</td></tr><tr><td>Bỏ hook trong \"chế độ nhanh\"</td><td>báo INCOMPLETE</td><td>ngầm PASS nguy hiểm</td><td>Mọi lần chạy đủ bốn hook hoặc INCOMPLETE (§1 #12)</td></tr><tr><td>Mã chạy khác mã công khai</td><td>verify-reproducible (hook con)</td><td>audit trên mã sai</td><td>Gọi verify-reproducible của TASK-TRUST-001 (§1 #7)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Egress test là mảnh bằng chứng mạnh nhất: nó quan sát đúng những gì rời máy trong luồng thật có phiên đăng nhập, thay cho mọi lời khai trong mã.</li><li>Audit của SănDeal khác \"báo cáo đã được hãng X kiểm\" ở chỗ tái chạy được: ai cũng clone + chạy + thấy cùng kết quả, không phải tin uy tín mượn.</li><li>TASK-TRUST-003 là lớp tổng hợp: nó gọi reproducible (TASK-TRUST-001) + payload_guard (TASK-COMPLY-005) như mảnh ghép và thêm egress động + SBOM để thành bằng chứng đầu cuối.</li><li>Whitelist endpoint + assert payload đóng cả hai kênh rò: gửi sai đích VÀ gửi sai nội dung.</li><li>SBOM + pin dependency biến \"extension phụ thuộc gì\" thành danh sách chính xác, kiểm chứng được - đóng kênh tấn công supply-chain.</li><li>Đây là mắt xích cuối của ba trụ niềm tin (mở source + chính sách + audit) mà §5.4 đặt ra; cùng nhau, chúng hóa giải nỗi lo malware/scam của ~45% người tiêu dùng VN (Ken Research) bằng bằng chứng chứ không bằng lời hứa.</li></ul>\n<hr>\n<p><em>Hết TASK-TRUST-003. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
