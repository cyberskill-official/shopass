window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-NOTIF-003"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service NOTIF <strong>MUST</strong> cung cấp một fan-out pipeline tiêu thụ các dòng <code>notification</code> ở trạng thái <code>pending</code>, định tuyến mỗi dòng tới đúng per-channel dispatcher, với ngữ nghĩa at-least-once + consumer idempotent, retry backoff có jitter, và dead-letter queue cho job hỏng. Pipeline CHỈ định tuyến và điều phối; nó KHÔNG tự gọi nhà cung cấp. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> dựng đường ống <code>producer -&gt; hàng đợi bền (Kafka hoặc Redis Streams) -&gt; fan-out worker -&gt; per-channel dispatcher</code> (DEC-NOTIF-20). Producer chỉ đẩy <code>notification_id</code> (con trỏ tới dòng đã ghi bởi TASK-NOTIF-001), KHÔNG nhồi toàn bộ nội dung vào message - nguồn sự thật là bảng <code>notification</code>.</li><li><strong>MUST</strong> giữ ngữ nghĩa at-least-once: một message có thể được giao lại (worker crash trước khi ack, re-balance partition). Pipeline <strong>MUST</strong> chịu được giao lại mà KHÔNG gửi đôi nội dung tới người dùng (DEC-NOTIF-21).</li><li><strong>MUST</strong> đảm bảo idempotent bằng claim/lease trên chính dòng <code>notification</code>: worker <strong>MUST</strong> giành quyền xử lý qua <code>UPDATE notification SET status='queued', lease_until=now()+lease, attempts=attempts+1 WHERE id=$1 AND status='pending' RETURNING ...</code>. Chỉ worker nhận được dòng RETURNING (CAS thắng) mới được gửi; worker thua coi như đã có người khác xử lý và bỏ qua. Một dòng <code>notification</code> được gửi đúng một lần.</li><li><strong>MUST</strong> định tuyến theo <code>notification.channel</code> (đã được TASK-NOTIF-001 routing chốt thành đúng một kênh trong enum <code>{push, email, sms}</code>) tới đúng <code>ChannelDispatcher</code> đã đăng ký (DEC-NOTIF-23): <code>push</code> tới dispatcher push (TASK-NOTIF-002 cho Android/Web, TASK-NOTIF-005 cho iOS - phân theo <code>user_channel_token.platform</code>, KHÔNG phải theo một giá trị channel riêng); <code>email -&gt; TASK-NOTIF-006</code>; <code>sms -&gt; TASK-NOTIF-007</code>. <code>channel</code> chỉ nhận <code>{push, email, sms}</code> theo CHECK của TASK-NOTIF-001 - KHÔNG có giá trị <code>apns</code> riêng. Fan-out KHÔNG chứa mã gọi FCM/APNs/SES/SendGrid/nhà-cung-cấp-SMS.</li><li><strong>MUST</strong> dùng exponential backoff + jitter cho retry (DEC-NOTIF-22): lần thử thứ <code>n</code> chờ <code>base * 2^(n-1)</code> rồi cộng jitter ngẫu nhiên, có trần <code>cap</code>. Backoff <strong>MUST</strong> có thành phần jitter để chống đồng pha retry giữa nhiều worker (tránh dồn tải nhà cung cấp lúc đỉnh).</li><li><strong>MUST</strong> có dead-letter queue: khi <code>attempts</code> vượt <code>max_attempts</code> (mặc định 5), pipeline <strong>MUST</strong> ghi dòng vào bảng <code>notification_dlq</code>, đặt <code>notification.status='failed'</code>, và NGỪNG retry. DLQ giữ <code>notification_id</code>, <code>channel</code>, <code>payload</code> snapshot, <code>attempts</code>, <code>last_error</code>, <code>dead_at</code> để con người soi.</li><li><strong>MUST</strong> phân biệt lỗi Permanent với Transient từ dispatcher (DEC-NOTIF-23): dispatcher trả <code>Permanent</code> (vd token không hợp lệ, payload sai, 4xx không-retry) -&gt; đẩy DLQ NGAY không retry; trả <code>Transient</code> (vd 429/5xx/timeout mạng) -&gt; retry theo backoff cho tới <code>max_attempts</code>.</li><li><strong>MUST</strong> cập nhật trạng thái cuối: dispatch thành công -&gt; <code>UPDATE notification SET status='sent', sent_at=now()</code>; thất bại tạm -&gt; giữ <code>queued</code> cho lần retry (đặt lại <code>lease_until</code>); thất bại vĩnh viễn hoặc cạn retry -&gt; <code>status='failed'</code> + DLQ. Vòng đời trạng thái khớp TASK-NOTIF-001: <code>pending -&gt; queued -&gt; sent | failed</code>.</li><li><strong>MUST</strong> re-claim job có lease hết hạn (DEC-NOTIF-24): dòng <code>status='queued'</code> mà <code>lease_until &lt; now()</code> (worker giữ nó đã crash) <strong>MUST</strong> được một worker khác claim lại qua <code>UPDATE ... WHERE id=$1 AND status='queued' AND lease_until &lt; now() RETURNING</code>. Không dòng nào kẹt vĩnh viễn ở <code>queued</code>.</li><li><strong>MUST</strong> dùng <code>idx_notif_dispatch</code> (đã tạo ở TASK-NOTIF-001, partial trên <code>status IN ('pending','queued')</code>) làm nguồn lấy việc cho producer/scheduler; thêm cột <code>attempts</code>, <code>lease_until</code>, <code>last_error</code> vào <code>notification</code> qua migration của task này.</li><li><strong>MUST</strong> giữ thứ tự per-user khi dùng hàng đợi phân vùng: message <strong>MUST</strong> được phân vùng theo khóa <code>user_id</code> (Kafka partition key / Redis Streams consumer group theo user-hash) để các thông báo của cùng một người không bị đảo thứ tự bất thường giữa các partition.</li><li><strong>SHOULD</strong> phát OTel: <code>notif_fanout_dispatched_total{channel}</code> (counter), <code>notif_fanout_retry_total{channel, reason}</code> (counter), <code>notif_fanout_dlq_total{channel, reason}</code> (counter), <code>notif_fanout_dispatch_duration_ms{channel}</code> (histogram), <code>notif_fanout_inflight{channel}</code> (gauge), <code>notif_fanout_double_claim_total</code> (counter - đếm lần CAS thua, kỳ vọng 0 trong điều kiện thường).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao hàng đợi bền Kafka/Redis Streams (DEC-NOTIF-20)?</strong> Đỉnh thông báo của SănDeal dồn vào 00:00 khi các sàn mở flash sale double-date (1.1, 2.2 ... 12.12): hàng chục nghìn alert giá bung trong vài phút. Gọi dispatcher trực tiếp từ engine alert (đồng bộ) sẽ vỡ ngay vì rate-limit FCM/SES và độ trễ mạng. Một hàng đợi bền hấp thụ cơn sóng, tách \"lúc sinh ra alert\" khỏi \"lúc gửi đi\", cho phép worker tiêu thụ theo nhịp nhà cung cấp chịu được. Bền (không in-memory) vì worker sẽ crash; mất message là mất thông báo người dùng đang chờ.</p>\n<p><strong>Vì sao at-least-once + idempotent thay vì exactly-once (DEC-NOTIF-21)?</strong> Exactly-once xuyên qua ranh giới mạng (queue + DB + nhà cung cấp ngoài) là ảo tưởng đắt đỏ. Cách vững là chấp nhận giao lại (at-least-once, rẻ và đáng tin) rồi làm consumer idempotent để giao lại không gây hại. Claim/lease bằng <code>UPDATE ... WHERE status='pending' RETURNING</code> là một compare-and-swap nguyên tử trong Postgres: chỉ một worker thắng, mọi giao lại sau đó thấy <code>status&lt;&gt;'pending'</code> và lặng lẽ bỏ qua. Người dùng nhận đúng một push cho một lần giảm giá - gửi đôi là cách nhanh nhất bị tắt thông báo.</p>\n<p><strong>Vì sao DLQ thay vì retry mãi (DEC-NOTIF-22)?</strong> Một số lỗi không bao giờ tự khỏi: token push đã gỡ app, payload dị dạng, số điện thoại sai định dạng. Retry vô hạn các job này đốt slot worker, đẩy lùi thông báo hợp lệ phía sau, và biến lỗi \"cần người sửa\" thành nhiễu nền vô hình. DLQ tách dòng chết ra một chỗ riêng để soi, giải phóng pipeline, và biến mỗi mục DLQ thành một tín hiệu cụ thể (token rác hàng loạt? adapter kênh hỏng?) thay vì một vòng lặp câm.</p>\n<p><strong>Vì sao backoff cần jitter (DEC-NOTIF-22, §1 #5)?</strong> Nếu FCM trả 429 lúc 00:00, hàng nghìn worker cùng đánh dấu \"thử lại sau 2 giây\" rồi cùng bắn lại ở giây thứ 2 - một đợt sóng đồng pha tự gây ra 429 tiếp, lặp mãi (thundering herd). Cộng jitter ngẫu nhiên vào mỗi khoảng chờ làm các lần retry rải đều theo thời gian, để nhà cung cấp kịp hồi và pipeline thoát khỏi cộng hưởng. Đây là cùng một bài học pacing/jitter của scraping farm (TASK-SCRAPE-001/005), áp vào phía gửi thông báo.</p>\n<p><strong>Vì sao tách per-channel dispatcher khỏi fan-out (DEC-NOTIF-20, DEC-NOTIF-23)?</strong> Mỗi kênh có giao thức, SDK, lược đồ lỗi, và rate-limit riêng: FCM/APNs là push protocol với token; SES/SendGrid là email với bounce; SMS là gateway với mã lỗi nhà mạng. Nhồi hết vào fan-out tạo một khối khổng lồ khó test và khó thay nhà cung cấp. Fan-out chỉ giữ phần chung (claim, retry, backoff, DLQ, định tuyến); mỗi dispatcher (TASK-NOTIF-002/005/006/007) là một implementation <code>ChannelDispatcher</code> cắm vào theo <code>channel</code>. Thêm kênh mới không đụng vào xương sống pipeline.</p>\n<p><strong>Vì sao phân vùng theo user_id (§1 #11)?</strong> Một người có thể nhận chuỗi thông báo liên quan trong khoảng ngắn (giá chạm ngưỡng, rồi giảm sâu thêm). Nếu các message của cùng người rải ngẫu nhiên qua nhiều partition và worker, thứ tự gửi có thể đảo, gây trải nghiệm khó hiểu. Khóa phân vùng <code>user_id</code> giữ thông báo của một người đi qua cùng một partition theo thứ tự, mà vẫn cho phép phân tải ngang giữa nhiều người khác nhau.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/notif/migrations/0003_notification_lease.sql\n-- Bổ sung cột phục vụ claim/lease + retry cho fan-out (TASK-NOTIF-003).\nALTER TABLE notification\n  ADD COLUMN attempts    INTEGER     NOT NULL DEFAULT 0,\n  ADD COLUMN lease_until TIMESTAMPTZ,\n  ADD COLUMN last_error  TEXT;\n\n-- idx_notif_dispatch (status, scheduled_at) WHERE status IN ('pending','queued')\n-- đã tạo ở TASK-NOTIF-001; phục vụ producer/scheduler lấy việc và re-claim lease hết hạn.\n\n-- services/notif/migrations/0004_notification_dlq.sql\n-- Dead-letter: dòng hỏng vĩnh viễn hoặc cạn retry, tách ra để con người soi.\nCREATE TABLE notification_dlq (\n  id              BIGSERIAL   PRIMARY KEY,\n  notification_id BIGINT      NOT NULL REFERENCES notification(id),\n  channel         TEXT        NOT NULL,\n  payload         JSONB,                       -- snapshot lúc chết\n  attempts        INTEGER     NOT NULL,\n  last_error      TEXT        NOT NULL,\n  reason          TEXT        NOT NULL,         -- 'permanent' | 'max_attempts'\n  dead_at         TIMESTAMPTZ NOT NULL DEFAULT now()\n);\n\nCREATE INDEX idx_dlq_channel ON notification_dlq (channel, dead_at DESC);</code></pre>\n<h3>Dispatcher interface + định tuyến (Go)</h3>\n<pre><code class=\"language-go\">// services/notif/internal/fanout/dispatch.go\n\n// errClass phân loại lỗi để pipeline quyết DLQ-ngay vs retry.\ntype errClass int\n\nconst (\n    ClassOK        errClass = iota // gửi thành công\n    ClassTransient                 // 429/5xx/timeout -&gt; retry theo backoff\n    ClassPermanent                 // token rác/payload sai/4xx -&gt; DLQ ngay\n)\n\n// ChannelDispatcher là per-channel gửi thật, hiện thực ở TASK-NOTIF-002/005/006/007.\n// Fan-out KHÔNG hiện thực interface này; nó chỉ định tuyến tới đúng impl theo channel.\ntype ChannelDispatcher interface {\n    // Dispatch gửi một notification đã render. Trả errClass để pipeline xử lý.\n    Dispatch(ctx context.Context, n notif.Notification) (errClass, error)\n    Channel() string // \"push\" | \"email\" | \"sms\" (enum TASK-NOTIF-001; iOS push phân theo platform, không phải channel riêng)\n}\n\n// Router map channel -&gt; dispatcher. Đăng ký lúc khởi động service.\ntype Router struct{ byChannel map[string]ChannelDispatcher }\n\nfunc (r *Router) Route(channel string) (ChannelDispatcher, bool) {\n    d, ok := r.byChannel[channel]\n    return d, ok // không có handler cho channel -&gt; Permanent (cấu hình sai)\n}</code></pre>\n<h3>Fan-out worker: claim/lease + dispatch + retry/DLQ (Go)</h3>\n<pre><code class=\"language-go\">// services/notif/internal/fanout/worker.go\n\n// handle xử lý đúng một notification_id: claim (CAS) -&gt; dispatch -&gt; ack/retry/DLQ.\nfunc (w *Worker) handle(ctx context.Context, id int64) error {\n    // 1) Claim/lease: chỉ worker thắng CAS (RETURNING có dòng) mới được gửi (§1 #3).\n    n, claimed, err := w.repo.ClaimPending(ctx, id, w.lease)\n    if err != nil {\n        return err\n    }\n    if !claimed {\n        metrics.DoubleClaim() // có worker khác đã giành -&gt; bỏ qua, idempotent (§1 #2)\n        return nil\n    }\n\n    // 2) Định tuyến theo channel đã chốt bởi routing TASK-NOTIF-001.\n    d, ok := w.router.Route(n.Channel)\n    if !ok {\n        return w.dead(ctx, n, \"permanent\", \"không có dispatcher cho kênh \"+n.Channel)\n    }\n\n    // 3) Gửi (gọi thật nằm trong dispatcher TASK-NOTIF-002/005/006/007).\n    class, derr := d.Dispatch(ctx, n)\n    switch class {\n    case ClassOK:\n        return w.repo.MarkSent(ctx, n.ID) // status='sent', sent_at=now()\n    case ClassPermanent:\n        return w.dead(ctx, n, \"permanent\", errStr(derr)) // DLQ ngay (§1 #7)\n    default: // ClassTransient\n        if n.Attempts &gt;= w.maxAttempts {\n            return w.dead(ctx, n, \"max_attempts\", errStr(derr)) // cạn retry -&gt; DLQ (§1 #6)\n        }\n        // Giữ 'queued', đặt lại lease theo backoff để re-claim sau (§1 #5, #9).\n        return w.repo.Requeue(ctx, n.ID, backoff(n.Attempts, w.base, w.cap), errStr(derr))\n    }\n}\n\nfunc (w *Worker) dead(ctx context.Context, n notif.Notification, reason, msg string) error {\n    metrics.DLQ(n.Channel, reason)\n    return w.dlq.Publish(ctx, n, reason, msg) // ghi notification_dlq + status='failed'\n}</code></pre>\n<h3>Claim/lease trong repo (Go)</h3>\n<pre><code class=\"language-go\">// services/notif/internal/notif/repo.go (bổ sung)\n\n// ClaimPending giành quyền xử lý một dòng pending HOẶC re-claim một dòng queued\n// có lease đã hết hạn. CAS nguyên tử: chỉ một worker nhận được dòng RETURNING.\nfunc (r *Repo) ClaimPending(ctx context.Context, id int64, lease time.Duration) (Notification, bool, error) {\n    var n Notification\n    err := r.pool.QueryRow(ctx, `\n        UPDATE notification\n           SET status='queued',\n               attempts=attempts+1,\n               lease_until=now() + $2::interval\n         WHERE id=$1\n           AND (status='pending'\n                OR (status='queued' AND lease_until &lt; now()))   -- re-claim lease hết hạn (§1 #9)\n        RETURNING id, user_id, channel, template, payload, attempts`,\n        id, lease.String()).Scan(&amp;n.ID, &amp;n.UserID, &amp;n.Channel, &amp;n.Template, &amp;n.Payload, &amp;n.Attempts)\n    if errors.Is(err, pgx.ErrNoRows) {\n        return Notification{}, false, nil // worker khác đã giành -&gt; không claim được\n    }\n    return n, err == nil, err\n}</code></pre>\n<h3>Exponential backoff + full jitter (Go)</h3>\n<pre><code class=\"language-go\">// services/notif/internal/fanout/backoff.go\n\n// backoff tính khoảng chờ cho lần thử thứ n: base*2^(n-1), trần cap, cộng full jitter.\n// Full jitter (AWS): chờ = random[0, min(cap, base*2^(n-1))] -&gt; rải đều, chống đồng pha (§1 #5).\nfunc backoff(attempt int, base, cap time.Duration) time.Duration {\n    exp := base &lt;&lt; uint(max(0, attempt-1)) // base * 2^(attempt-1)\n    if exp &gt; cap || exp &lt;= 0 {\n        exp = cap\n    }\n    return time.Duration(rand.Int63n(int64(exp) + 1)) // full jitter trong [0, exp]\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch -&gt; <code>notification</code> có thêm <code>attempts</code>, <code>lease_until</code>, <code>last_error</code>; bảng <code>notification_dlq</code> tồn tại với FK tới <code>notification(id)</code>.</li><li>Producer <code>Enqueue(id)</code> đẩy message chứa <code>notification_id</code> vào Kafka/Redis Streams; message KHÔNG nhồi toàn bộ nội dung (chỉ con trỏ).</li><li>Một dòng <code>notification</code> pending được fan-out nhặt, dispatch thành công -&gt; <code>status='sent'</code>, <code>sent_at</code> set, đúng dispatcher của <code>channel</code> được gọi.</li><li>At-least-once: giao lại cùng <code>notification_id</code> 2 lần -&gt; dispatcher chỉ được gọi gửi đúng 1 lần (lần thứ 2 thua CAS, bỏ qua); <code>notif_fanout_double_claim_total</code> tăng.</li><li>Idempotent dưới cạnh tranh: hai worker chạy <code>handle</code> đồng thời trên cùng <code>id</code> -&gt; đúng một worker <code>MarkSent</code>, một worker bỏ qua; bảng chỉ có một lần chuyển sang <code>sent</code>.</li><li>Dispatcher trả <code>ClassTransient</code> -&gt; job giữ <code>queued</code>, <code>attempts</code> tăng, <code>lease_until</code> lùi theo backoff; KHÔNG vào DLQ khi chưa cạn retry.</li><li>Dispatcher trả <code>ClassTransient</code> lặp tới khi <code>attempts &gt; max_attempts</code> -&gt; ghi <code>notification_dlq</code> với <code>reason='max_attempts'</code>, <code>notification.status='failed'</code>, ngừng retry.</li><li>Dispatcher trả <code>ClassPermanent</code> -&gt; vào <code>notification_dlq</code> NGAY với <code>reason='permanent'</code>, KHÔNG retry dù <code>attempts</code> còn thấp.</li><li><code>channel</code> không có dispatcher đăng ký -&gt; coi là Permanent -&gt; DLQ với lý do \"không có dispatcher\".</li><li>Re-claim: dòng <code>queued</code> có <code>lease_until &lt; now()</code> (worker giả lập crash) được worker khác <code>ClaimPending</code> lại; không kẹt vĩnh viễn ở <code>queued</code>.</li><li>Định tuyến đúng kênh: <code>notification.channel='push'</code> -&gt; dispatcher push (TASK-NOTIF-002 cho Android/Web, TASK-NOTIF-005 cho iOS - phân theo <code>user_channel_token.platform</code>); <code>email</code> -&gt; email (TASK-NOTIF-006); <code>sms</code> -&gt; sms (TASK-NOTIF-007). <code>channel</code> không nhận giá trị <code>apns</code> (CHECK TASK-NOTIF-001). Không có lời gọi nhà cung cấp nào trong package <code>fanout</code>.</li><li><code>backoff(n)</code> tăng theo <code>n</code> (kỳ vọng trung bình tăng), luôn nằm trong <code>[0, cap]</code>, và hai lần gọi liên tiếp cho giá trị khác nhau (có jitter), không cố định.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/notif/internal/fanout/worker_test.go\n\n// At-least-once: dispatch đúng 1 lần dù message giao lại 2 lần (CAS bảo vệ).\nfunc TestFanout_AtLeastOnce_NoDoubleSend(t *testing.T) {\n    w, repo := newTestWorker(t)\n    id := seedPending(t, repo, \"push\")\n    var sent int32\n    w.router = routeAll(spyDispatcher(func() (errClass, error) {\n        atomic.AddInt32(&amp;sent, 1)\n        return ClassOK, nil\n    }))\n\n    require.NoError(t, w.handle(ctx, id)) // lần giao 1\n    require.NoError(t, w.handle(ctx, id)) // lần giao 2 (giao lại) -&gt; thua CAS, bỏ qua\n\n    require.Equal(t, int32(1), atomic.LoadInt32(&amp;sent)) // gửi đúng 1 lần\n    require.Equal(t, \"sent\", statusOf(t, repo, id))\n}\n\n// Idempotent dưới cạnh tranh: 2 worker đồng thời -&gt; đúng 1 lần sent.\nfunc TestFanout_ConcurrentClaim_SingleSend(t *testing.T) {\n    w, repo := newTestWorker(t)\n    id := seedPending(t, repo, \"email\")\n    var sent int32\n    w.router = routeAll(spyDispatcher(func() (errClass, error) {\n        atomic.AddInt32(&amp;sent, 1)\n        return ClassOK, nil\n    }))\n    var wg sync.WaitGroup\n    for i := 0; i &lt; 8; i++ {\n        wg.Add(1)\n        go func() { defer wg.Done(); _ = w.handle(ctx, id) }()\n    }\n    wg.Wait()\n    require.Equal(t, int32(1), atomic.LoadInt32(&amp;sent)) // chỉ một worker thắng CAS\n}\n\n// Transient -&gt; retry (giữ queued, attempts tăng), chưa vào DLQ.\nfunc TestFanout_TransientRetries(t *testing.T) {\n    w, repo := newTestWorker(t)\n    id := seedPending(t, repo, \"push\")\n    w.router = routeAll(constDispatcher(ClassTransient, errors.New(\"429\")))\n    require.NoError(t, w.handle(ctx, id))\n    require.Equal(t, \"queued\", statusOf(t, repo, id))\n    require.GreaterOrEqual(t, attemptsOf(t, repo, id), 1)\n    require.Equal(t, 0, dlqCount(t, repo, id)) // chưa cạn retry\n}\n\n// Cạn retry -&gt; DLQ với reason=max_attempts, status=failed.\nfunc TestFanout_MaxAttempts_ToDLQ(t *testing.T) {\n    w, repo := newTestWorker(t)\n    w.maxAttempts = 3\n    id := seedPending(t, repo, \"sms\")\n    w.router = routeAll(constDispatcher(ClassTransient, errors.New(\"timeout\")))\n    for i := 0; i &lt; 5; i++ {\n        expireLease(t, repo, id) // giả lập tới hạn re-claim giữa các lần\n        _ = w.handle(ctx, id)\n    }\n    require.Equal(t, \"failed\", statusOf(t, repo, id))\n    require.Equal(t, \"max_attempts\", dlqReason(t, repo, id))\n}\n\n// Permanent -&gt; DLQ ngay, không retry.\nfunc TestFanout_PermanentStraightToDLQ(t *testing.T) {\n    w, repo := newTestWorker(t)\n    id := seedPending(t, repo, \"push\")\n    w.router = routeAll(constDispatcher(ClassPermanent, errors.New(\"token gỡ app\")))\n    require.NoError(t, w.handle(ctx, id))\n    require.Equal(t, \"failed\", statusOf(t, repo, id))\n    require.Equal(t, \"permanent\", dlqReason(t, repo, id))\n    require.Equal(t, 1, attemptsOf(t, repo, id)) // không retry thêm\n}\n\n// Định tuyến đúng kênh tới đúng dispatcher (không gọi nhà cung cấp thật).\nfunc TestFanout_RoutesByChannel(t *testing.T) {\n    w, repo := newTestWorker(t)\n    hit := map[string]bool{}\n    w.router = &amp;Router{byChannel: map[string]ChannelDispatcher{\n        \"push\":  recordDispatcher(\"push\", hit),\n        \"email\": recordDispatcher(\"email\", hit),\n        \"sms\":   recordDispatcher(\"sms\", hit),\n    }}\n    for _, ch := range []string{\"push\", \"email\", \"sms\"} {\n        _ = w.handle(ctx, seedPending(t, repo, ch))\n    }\n    require.True(t, hit[\"push\"] &amp;&amp; hit[\"email\"] &amp;&amp; hit[\"sms\"])\n}\n\n// Re-claim: dòng queued lease hết hạn được claim lại, không kẹt vĩnh viễn.\nfunc TestFanout_ReclaimExpiredLease(t *testing.T) {\n    _, repo := newTestWorker(t)\n    id := seedPending(t, repo, \"push\")\n    _, ok1, _ := repo.ClaimPending(ctx, id, time.Minute)\n    require.True(t, ok1)                 // worker A giành\n    expireLease(t, repo, id)             // A \"crash\", lease quá hạn\n    _, ok2, _ := repo.ClaimPending(ctx, id, time.Minute)\n    require.True(t, ok2)                 // worker B re-claim\n}\n\n// services/notif/internal/fanout/backoff_test.go\n\nfunc TestBackoff_GrowsAndCaps(t *testing.T) {\n    base, cap := 200*time.Millisecond, 30*time.Second\n    for n := 1; n &lt;= 10; n++ {\n        d := backoff(n, base, cap)\n        require.GreaterOrEqual(t, d, time.Duration(0))\n        require.LessOrEqual(t, d, cap) // luôn trong [0, cap]\n    }\n}\n\nfunc TestBackoff_HasJitter(t *testing.T) {\n    base, cap := 1*time.Second, 60*time.Second\n    a := backoff(5, base, cap)\n    b := backoff(5, base, cap)\n    require.NotEqual(t, a, b) // jitter -&gt; hai lần khác nhau, không đồng pha (§1 #5)\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration <code>0003_notification_lease.sql</code> (cột claim/lease) + <code>0004_notification_dlq.sql</code> (bảng DLQ) -&gt; <code>repo.go</code> bổ sung <code>ClaimPending</code>/<code>MarkSent</code>/<code>Requeue</code> -&gt; <code>backoff.go</code> (thuần, test trước) -&gt; <code>dispatch.go</code> (interface + Router) -&gt; <code>dlq.go</code> (Publish) -&gt; <code>worker.go</code> (vòng claim -&gt; dispatch -&gt; ack/retry/DLQ) -&gt; <code>producer.go</code> (Enqueue) -&gt; tests. Dispatcher thật (push/APNs/email/sms) đến ở TASK-NOTIF-002/005/006/007; ở task này dùng dispatcher giả (stub) để kiểm thử pipeline độc lập. Chọn Kafka khi triển khai đa-node có sẵn Kafka; Redis Streams cho khởi đầu nhẹ - cả hai cùng cung cấp claim/ack + consumer group, interface producer ẩn lựa chọn này.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-NOTIF-001 (upstream)</strong> - định nghĩa bảng <code>notification</code>, routing chốt <code>channel</code>, và <code>idx_notif_dispatch</code>; fan-out tiêu thụ các dòng <code>pending</code> này.</li><li><strong>TASK-NOTIF-002 (downstream)</strong> - dispatcher push (FCM) hiện thực <code>ChannelDispatcher</code> cho <code>channel='push'</code>.</li><li><strong>TASK-NOTIF-005 (downstream)</strong> - dispatcher APNs cho push iOS: nhặt dòng <code>channel='push'</code> lọc thêm <code>user_channel_token.platform='ios'</code> (KHÔNG phải một giá trị <code>channel='apns'</code> riêng).</li><li><strong>TASK-NOTIF-006 (downstream)</strong> - dispatcher email (SES/SendGrid) cho <code>channel='email'</code>.</li><li><strong>TASK-NOTIF-007 (downstream)</strong> - dispatcher SMS cho <code>channel='sms'</code>.</li><li><strong>TASK-NOTIF-004 (upstream điều tiết)</strong> - scheduler chống dồn 00:00 đẩy việc vào pipeline này (rải <code>scheduled_at</code>); fan-out là tầng tiêu thụ.</li><li>Hạ tầng: Kafka hoặc Redis Streams (hàng đợi bền, consumer group, claim/ack); Postgres (<code>notification</code>, <code>notification_dlq</code>); OTel.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Message trên hàng đợi (con trỏ, không nhồi nội dung)</h3>\n<pre><code class=\"language-json\">{ \"notification_id\": 55012, \"user_id\": 4021, \"channel\": \"push\",\n  \"enqueued_at\": \"2026-06-28T17:00:03Z\", \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\" }</code></pre>\n<h3>Một mục dead-letter sau khi cạn retry</h3>\n<pre><code class=\"language-sql\">SELECT notification_id, channel, attempts, reason, last_error\nFROM notification_dlq WHERE notification_id = 55012;\n-- 55012 | push | 6 | max_attempts | FCM 503 Service Unavailable (sau 5 lần thử + backoff)</code></pre>\n<pre><code class=\"language-json\">{ \"id\": 88001, \"notification_id\": 55012, \"channel\": \"push\",\n  \"payload\": { \"template\": \"price_below\", \"price\": 79000 },\n  \"attempts\": 6, \"reason\": \"max_attempts\",\n  \"last_error\": \"FCM 503 Service Unavailable\", \"dead_at\": \"2026-06-28T17:04:11Z\" }</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Cơ chế replay tự động từ DLQ (sau khi sự cố nhà cung cấp hồi) - hiện soi thủ công; thêm worker replay có nút bấm ở giai đoạn vận hành.</li><li>Circuit breaker per-channel (khi một nhà cung cấp sập diện rộng, tạm ngắt để khỏi đốt retry) - phối hợp với dispatcher TASK-NOTIF-002/006 sau.</li><li>Ưu tiên hàng đợi (priority lane) cho OTP/high-value tách khỏi thông báo giá thường - gắn khi BILL/Premium (TASK-BILL-001) lên.</li><li>Exactly-once xuyên nhà cung cấp qua idempotency-key của FCM/SES - tăng cường idempotent phía ngoài ở giai đoạn sau; hiện claim/lease phía DB là đủ.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Worker crash sau claim, trước ack</td><td><code>lease_until</code> quá hạn</td><td>dòng kẹt <code>queued</code></td><td>Re-claim sau lease hết hạn (§1 #9)</td></tr><tr><td>Message giao lại (re-balance/at-least-once)</td><td>CAS thua RETURNING</td><td>nguy cơ gửi đôi</td><td>Claim/lease idempotent, bỏ qua lần thua (§1 #3)</td></tr><tr><td>Hai worker cùng nhặt một id</td><td><code>double_claim_total</code> tăng</td><td>gửi đôi</td><td>Đúng một thắng CAS; còn lại no-op</td></tr><tr><td>Nhà cung cấp 429/5xx lúc đỉnh 00:00</td><td>dispatcher trả Transient</td><td>trễ gửi</td><td>Backoff + jitter, retry tới max (§1 #5)</td></tr><tr><td>Retry đồng pha (thundering herd)</td><td>spike retry metric</td><td>tự dồn tải</td><td>Full jitter rải đều khoảng chờ (§1 #5)</td></tr><tr><td>Token push gỡ app / payload sai</td><td>dispatcher trả Permanent</td><td>gửi hỏng</td><td>DLQ ngay, không retry (§1 #7)</td></tr><tr><td>Job hỏng retry vô hạn</td><td><code>attempts &gt; max</code></td><td>chiếm slot worker</td><td>Cạn retry -&gt; DLQ, ngừng (§1 #6)</td></tr><tr><td><code>channel</code> không có dispatcher</td><td>Route trả false</td><td>không gửi được</td><td>Coi Permanent -&gt; DLQ (cấu hình sai) (§1 #9)</td></tr><tr><td>Fan-out tự gọi nhà cung cấp</td><td>code review</td><td>vượt rate-limit, lệch ranh giới</td><td>Chỉ định tuyến; gọi thật ở TASK-NOTIF-002/005/006/007 (§1 #4)</td></tr><tr><td>Thông báo cùng user đảo thứ tự</td><td>quan sát trải nghiệm</td><td>khó hiểu</td><td>Phân vùng theo <code>user_id</code> (§1 #11)</td></tr><tr><td>DLQ phình (một nhà cung cấp sập diện rộng)</td><td>kích thước <code>notification_dlq</code></td><td>nhiều thông báo trượt</td><td>Alert vận hành, replay sau khi hồi (§9)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Fan-out là xương sống vận chuyển của module thông báo: nó nối engine alert (qua dòng <code>notification</code> pending của TASK-NOTIF-001) tới người dùng cuối, nhưng KHÔNG tự gọi nhà cung cấp.</li><li>Claim/lease bằng <code>UPDATE ... WHERE status='pending' RETURNING</code> là một CAS nguyên tử trong Postgres - đó là cốt lõi biến at-least-once thành \"gửi đúng một lần\" mà không cần exactly-once đắt đỏ.</li><li>Backoff full jitter là tuyến phòng thủ chống thundering herd lúc đỉnh 00:00, song song với pacing/jitter của scraping farm (TASK-SCRAPE-001/005) ở phía thu thập.</li><li>DLQ tách dòng chết khỏi vòng retry, vừa giải phóng slot worker vừa biến lỗi thành tín hiệu cụ thể để con người soi - không có vòng lặp câm.</li><li>Ranh giới cứng: fan-out giữ phần chung (claim, retry, backoff, DLQ, định tuyến); mỗi <code>ChannelDispatcher</code> (TASK-NOTIF-002/005/006/007) là một implementation cắm vào theo <code>channel</code>, thêm kênh không đụng xương sống.</li><li>TASK-NOTIF-004 (scheduler chống dồn 00:00) là tầng điều tiết đầu vào; fan-out là tầng tiêu thụ - hai mảnh khớp nhau ở hàng đợi.</li></ul>\n<hr>\n<p><em>Hết TASK-NOTIF-003. Status: done (mục tiêu audit 10/10).</em></p>";
