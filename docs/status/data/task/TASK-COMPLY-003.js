window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-COMPLY-003"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service COMPLY <strong>MUST</strong> cung cấp quy trình thực thi bốn quyền chủ thể dữ liệu (DSAR) theo PDPL Luật 91/2025: truy cập (access), sửa (rectify), xóa (erase), di chuyển (portability) - qua API thống nhất, có xác minh danh tính, theo dõi SLA. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> định nghĩa bảng <code>dsar_request (id, user_id, kind, status, requested_at, completed_at, sla_due_at, note)</code> với <code>kind IN ('access','rectify','erase','portability')</code> theo dõi vòng đời từng yêu cầu (DEC-COMPLY-10).</li><li><strong>MUST</strong> xác minh danh tính chủ thể trước khi xử lý (DEC-COMPLY-11): chỉ chủ phiên đăng nhập tạo và nhận DSAR của chính <code>user_id</code> đó. Cấm tuyệt đối truy cập DSAR chéo user.</li><li><strong>MUST</strong> thực thi quyền truy cập (access): trả bản tóm tắt dữ liệu cá nhân đang lưu (loại dữ liệu, nguồn, mục đích) cho user.</li><li><strong>MUST</strong> thực thi quyền di chuyển (portability) (DEC-COMPLY-13): xuất dữ liệu cá nhân ở định dạng JSON có cấu trúc, máy đọc được, gồm hồ sơ tài khoản + sản phẩm theo dõi + lịch sử consent (TASK-COMPLY-001). KHÔNG xuất dạng không máy đọc được.</li><li><strong>MUST</strong> thực thi quyền xóa (erase) theo nguyên tắc (DEC-COMPLY-12):</li></ol>\n<ul><li>Hard-delete dữ liệu cá nhân thuần không ràng buộc (ví dụ wishlist, alert rule cá nhân).</li><li>Soft-anonymize dữ liệu có ràng buộc pháp lý/kế toán (ví dụ bản ghi thanh toán phải giữ theo luật kế toán) - thay PII bằng giá trị ẩn danh, không xóa dòng.</li><li>KHÔNG xóa <code>consent_record</code> - đó là chứng cứ pháp lý; thay vào đó ghi nhận xóa qua một DSAR erase đã hoàn tất.</li></ul>\n<ol><li><strong>MUST</strong> thực thi quyền sửa (rectify): cho phép cập nhật trường hồ sơ cá nhân (email, phone, locale) qua luồng có kiểm tra ràng buộc (ví dụ email unique của TASK-AUTH-001).</li><li><strong>MUST</strong> tính <code>sla_due_at</code> và suy trạng thái quá hạn từ thời điểm tạo, KHÔNG nhập tay (DEC-COMPLY-14): <code>open</code> / <code>in_progress</code> / <code>completed</code> / <code>overdue</code>.</li><li><strong>MUST</strong> expose hàm:</li></ol>\n<ul><li><code>CreateRequest(ctx, userID int64, kind string) (int64, error)</code> - tạo DSAR, gán <code>sla_due_at</code>.</li><li><code>Export(ctx, userID int64) (ExportBundle, error)</code> - gom dữ liệu cho access/portability.</li><li><code>Erase(ctx, userID int64) (EraseResult, error)</code> - hard-delete + soft-anonymize theo phân loại, giữ consent.</li><li><code>Overdue(ctx) ([]DSARRequest, error)</code> - liệt kê DSAR quá SLA.</li></ul>\n<ol><li><strong>MUST</strong> đảm bảo <code>Export</code> chỉ gom dữ liệu của đúng <code>user_id</code> truyền vào; property test chống rò dữ liệu chéo user (cổng tiến phase §7 BACKLOG: rò cross-user = 0).</li><li><strong>SHOULD</strong> phát OTel metric: <code>dsar_request_total{kind}</code>, <code>dsar_overdue_total</code>; log mọi DSAR ở mức audit.</li><li><strong>MUST</strong> ghi <code>completed_at</code> khi DSAR hoàn tất; erase sinh một DSAR <code>completed</code> làm dấu vết (đã thực thi quyền xóa lúc nào).</li><li><strong>MUST</strong> đảm bảo <code>Erase</code> idempotent: gọi erase hai lần cho cùng user không lỗi và không để lộ dữ liệu (lần hai là no-op trên dữ liệu đã xóa).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao xác minh danh tính bắt buộc (DEC-COMPLY-11)?</strong> DSAR thao tác trên dữ liệu cá nhân nhạy cảm. Nếu kẻ xấu xin \"xuất dữ liệu của user khác\" mà không bị chặn, ta tự tạo vi phạm rò rỉ. Buộc DSAR chạy trong phiên đăng nhập của chính chủ thể, <code>user_id</code> lấy từ token chứ không từ tham số tùy ý, đóng lỗ hổng truy cập chéo.</p>\n<p><strong>Vì sao xóa là hỗn hợp hard-delete + soft-anonymize (DEC-COMPLY-12)?</strong> \"Xóa hết\" nghe đơn giản nhưng va vào luật khác: bản ghi thanh toán phải giữ theo luật kế toán; consent_record là chứng cứ PDPL. Giải pháp: dữ liệu cá nhân thuần thì xóa hẳn; dữ liệu ràng buộc thì ẩn danh (giữ dòng, bỏ PII); chứng cứ consent thì giữ nguyên. Cách này tôn trọng cả quyền xóa lẫn nghĩa vụ lưu trữ.</p>\n<p><strong>Vì sao giữ consent_record khi erase (§1 #5)?</strong> Nghịch lý: nếu xóa luôn lịch sử consent, ta mất bằng chứng \"user từng đồng ý cái gì\" - chính cái PDPL bắt phải tái lập. Nên erase ghi một DSAR <code>completed</code> đánh dấu đã xóa, nhưng consent log vẫn còn để chứng minh quy trình. User được xóa dữ liệu, ta vẫn giữ được dấu vết tuân thủ.</p>\n<p><strong>Vì sao portability là JSON máy đọc được (DEC-COMPLY-13)?</strong> Quyền di chuyển nghĩa là user mang dữ liệu sang nơi khác. Xuất PDF ảnh hay HTML rối thì không \"di chuyển\" được. JSON có cấu trúc cho phép nhập lại vào hệ thống khác - đúng tinh thần portability.</p>\n<p><strong>Vì sao SLA suy từ thời điểm tạo (DEC-COMPLY-14)?</strong> Giống DPIA, trạng thái quá hạn phải khách quan. Tính <code>sla_due_at</code> từ <code>requested_at</code> cho ra <code>overdue</code> tự động, không để ai \"quên\" một DSAR vô thời hạn. PDPL kỳ vọng phản hồi trong thời hạn hợp lý; deadline rõ ràng làm điều đó thành quy trình.</p>\n<p><strong>Vì sao property test chống rò chéo user (§1 #9)?</strong> Đây là cổng tiến phase cứng của BACKLOG (rò cross-user = 0). <code>Export</code> gom dữ liệu từ nhiều bảng; một lỗi join thiếu điều kiện <code>user_id</code> là rò rỉ. Property test sinh nhiều user, xuất từng user, khẳng định bundle chỉ chứa dữ liệu đúng chủ.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/comply/migrations/0005_dsar_request.sql\nCREATE TABLE dsar_request (\n  id           BIGSERIAL   PRIMARY KEY,\n  user_id      BIGINT      NOT NULL REFERENCES app_user(id),\n  kind         TEXT        NOT NULL CHECK (kind IN ('access','rectify','erase','portability')),\n  status       TEXT        NOT NULL DEFAULT 'open',\n  requested_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  sla_due_at   TIMESTAMPTZ NOT NULL,\n  completed_at TIMESTAMPTZ,\n  note         TEXT\n);\n\nCREATE INDEX idx_dsar_user ON dsar_request (user_id, requested_at DESC);\nCREATE INDEX idx_dsar_open ON dsar_request (status, sla_due_at) WHERE status &lt;&gt; 'completed';</code></pre>\n<h3>Export / Erase (Go)</h3>\n<pre><code class=\"language-go\">// services/comply/internal/dsar/export.go\ntype ExportBundle struct {\n    Account        AccountView      `json:\"account\"`\n    TrackedProducts []ProductView   `json:\"tracked_products\"`\n    ConsentHistory []ConsentView    `json:\"consent_history\"` // gom tu TASK-COMPLY-001\n    GeneratedAt    time.Time        `json:\"generated_at\"`\n}\n\n// Export gom du lieu cua DUNG user_id (chong ro cheo user).\nfunc (s *Service) Export(ctx context.Context, userID int64) (ExportBundle, error) {\n    acc, err := s.users.View(ctx, userID)\n    if err != nil {\n        return ExportBundle{}, err\n    }\n    prods, err := s.track.ByUser(ctx, userID)   // moi truy van rang buoc user_id\n    if err != nil {\n        return ExportBundle{}, err\n    }\n    consent, err := s.consent.HistoryAll(ctx, userID)\n    if err != nil {\n        return ExportBundle{}, err\n    }\n    return ExportBundle{Account: acc, TrackedProducts: prods, ConsentHistory: consent, GeneratedAt: time.Now()}, nil\n}\n\n// services/comply/internal/dsar/erase.go\n// Erase: hard-delete du lieu thuan, soft-anonymize du lieu rang buoc, GIU consent log.\nfunc (s *Service) Erase(ctx context.Context, userID int64) (EraseResult, error) {\n    var r EraseResult\n    r.WishlistDeleted, _ = s.track.HardDeleteByUser(ctx, userID) // du lieu thuan\n    r.PaymentsAnonymized, _ = s.bill.AnonymizeByUser(ctx, userID) // rang buoc ke toan\n    s.users.Anonymize(ctx, userID)   // thay PII app_user bang gia tri an danh\n    // KHONG dong toi consent_record (chung cu phap ly).\n    return r, nil\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch -&gt; <code>dsar_request</code> tồn tại; CHECK <code>kind</code> hoạt động.</li><li><code>CreateRequest(user, \"access\")</code> -&gt; một DSAR, status <code>open</code>, có <code>sla_due_at</code>.</li><li><code>Export(user)</code> -&gt; bundle JSON gồm account + tracked_products + consent_history, chỉ của đúng user.</li><li><code>Export(userA)</code> KHÔNG chứa bất kỳ dữ liệu nào của <code>userB</code> (property test, rò cross-user = 0).</li><li><code>Erase(user)</code> -&gt; wishlist/alert (dữ liệu thuần) bị hard-delete; bản ghi payment bị anonymize (dòng còn, PII mất).</li><li>Sau <code>Erase</code>, <code>consent_record</code> của user VẪN còn (chứng cứ), nhưng app_user PII đã ẩn danh.</li><li><code>Erase</code> gọi lần hai cho cùng user -&gt; no-op, không lỗi, không lộ dữ liệu (idempotent).</li><li>INSERT <code>dsar_request</code> với <code>kind = 'unknown'</code> -&gt; lỗi CHECK constraint.</li><li>DSAR tạo cách đây quá SLA mà chưa <code>completed</code> -&gt; <code>Overdue()</code> trả về nó.</li><li>DSAR hoàn tất -&gt; <code>completed_at</code> được set; status <code>completed</code>.</li><li>Cố <code>Export</code> với <code>user_id</code> không phải chủ phiên (giả lập caller chéo) -&gt; bị tầng auth chặn (xác minh danh tính).</li><li><code>rectify</code> cập nhật email trùng email user khác -&gt; lỗi ràng buộc unique (tôn trọng TASK-AUTH-001), DSAR không hoàn tất.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/comply/internal/dsar/request_test.go\nfunc TestExport_OnlyOwnData(t *testing.T) {\n    s := setup(t)\n    a := seedUserWithData(t, s) // userA + wishlist + consent\n    b := seedUserWithData(t, s) // userB\n\n    bundle, _ := s.Export(ctx, a.ID)\n    require.Equal(t, a.ID, bundle.Account.UserID)\n    for _, p := range bundle.TrackedProducts {\n        require.NotContains(t, idsOf(b.Products), p.ID) // khong lan du lieu userB\n    }\n}\n\nfunc TestExport_PortabilityIsJSON(t *testing.T) {\n    s := setup(t)\n    u := seedUserWithData(t, s)\n    bundle, _ := s.Export(ctx, u.ID)\n    raw, err := json.Marshal(bundle)\n    require.NoError(t, err)\n    require.Contains(t, string(raw), \"consent_history\") // may doc duoc, co consent\n}\n\n// services/comply/internal/dsar/erase_test.go\nfunc TestErase_KeepsConsentLog(t *testing.T) {\n    s := setup(t)\n    u := seedUserWithData(t, s) // co consent_record\n    _, err := s.Erase(ctx, u.ID)\n    require.NoError(t, err)\n\n    require.Zero(t, countWishlist(t, s, u.ID))        // du lieu thuan bi xoa\n    require.True(t, isAnonymized(t, s, u.ID))          // app_user PII an danh\n    require.NotZero(t, countConsentRecord(t, s, u.ID)) // consent VAN con (chung cu)\n}\n\nfunc TestErase_Idempotent(t *testing.T) {\n    s := setup(t)\n    u := seedUserWithData(t, s)\n    _, err1 := s.Erase(ctx, u.ID)\n    _, err2 := s.Erase(ctx, u.ID) // lan hai\n    require.NoError(t, err1)\n    require.NoError(t, err2) // no-op, khong loi\n}\n\nfunc TestDSAR_Overdue(t *testing.T) {\n    s := setup(t)\n    id := seedOldOpenDSAR(t, s, \"access\") // qua SLA\n    list, _ := s.Overdue(ctx)\n    require.Contains(t, idsOf(list), id)\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration 0005 -&gt; types.go -&gt; export.go (gom dữ liệu, mọi truy vấn buộc <code>user_id</code>) -&gt; erase.go (phân loại hard/soft) -&gt; request.go (vòng đời + SLA) -&gt; repo.go -&gt; tests. <code>Export</code> và <code>Erase</code> gọi sang các service khác (users, track, bill, consent) qua interface inject để test mock được. Xác minh danh tính nằm ở tầng BFF/auth: <code>user_id</code> luôn lấy từ token phiên, hàm DSAR không nhận <code>user_id</code> tùy ý từ client.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-COMPLY-001</strong> - <code>Export</code> gom <code>HistoryAll</code> (lịch sử consent); <code>Erase</code> giữ <code>consent_record</code>.</li><li><strong>TASK-AUTH-001</strong> - <code>app_user</code> (anonymize PII); <code>rectify</code> tôn trọng email unique.</li><li><strong>TASK-AUTH-005 (downstream)</strong> - xóa tài khoản gọi <code>Erase</code> ở đây.</li><li><strong>TASK-BILL-003 (liên quan)</strong> - bản ghi payment là dữ liệu ràng buộc kế toán -&gt; soft-anonymize.</li><li><strong>TASK-TRACK-002 (liên quan)</strong> - wishlist là dữ liệu thuần -&gt; hard-delete.</li><li>Lib: <code>encoding/json</code>, driver <code>pgx</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>User xin xuất dữ liệu (portability) - chạy trong phiên của chính họ</h3>\n<pre><code class=\"language-json\">POST /v1/dsar\n{ \"kind\": \"portability\" }</code></pre>\n<pre><code class=\"language-json\">200 OK\n{\n  \"account\": { \"user_id\": 90112, \"email\": \"chi@example.com\", \"locale\": \"vi-VN\" },\n  \"tracked_products\": [ { \"id\": 5521, \"platform\": \"shopee\", \"name\": \"Tai nghe ABC\" } ],\n  \"consent_history\": [\n    { \"purpose_key\": \"cart_read\", \"granted\": true, \"ts\": \"2026-06-28T09:12:00+07:00\" }\n  ],\n  \"generated_at\": \"2026-06-28T10:00:00+07:00\"\n}</code></pre>\n<h3>User yêu cầu xóa</h3>\n<pre><code class=\"language-json\">POST /v1/dsar\n{ \"kind\": \"erase\" }</code></pre>\n<pre><code class=\"language-json\">200 OK\n{\n  \"wishlist_deleted\": 12,\n  \"payments_anonymized\": 3,\n  \"consent_log_retained\": true,\n  \"status\": \"completed\"\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt khung. Hoãn:</p>\n<ul><li>DSAR cho người không có tài khoản (khách vãng lai có dữ liệu) - hiện gắn DSAR vào app_user.</li><li>Hàng đợi xử lý DSAR bất đồng bộ khi dữ liệu lớn (export nền + email link tải) - tối ưu khi quy mô tăng.</li><li>Xác minh danh tính nâng cao (re-auth/OTP) cho erase - cân nhắc khi rủi ro chiếm tài khoản tăng.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Rò dữ liệu chéo user khi export</td><td>property test §1 #9</td><td>vi phạm rò rỉ</td><td>Mọi truy vấn buộc <code>user_id</code>; test <code>TestExport_OnlyOwnData</code></td></tr><tr><td>Caller giả <code>user_id</code> khác</td><td>tầng auth + AC #11</td><td>truy cập chéo</td><td><code>user_id</code> lấy từ token, không từ tham số client</td></tr><tr><td>Erase xóa luôn consent log</td><td>review erase + AC #6</td><td>mất chứng cứ PDPL</td><td><code>Erase</code> không đụng <code>consent_record</code>; test giữ consent</td></tr><tr><td>Erase xóa bản ghi kế toán</td><td>review phân loại</td><td>vi phạm luật kế toán</td><td>Payment soft-anonymize, không hard-delete</td></tr><tr><td>Erase lần hai gây lỗi/lộ</td><td>idempotent + AC #7</td><td>hỏng UX/rò</td><td>No-op trên dữ liệu đã xóa</td></tr><tr><td>DSAR quá SLA không ai biết</td><td><code>Overdue()</code> + AC #9</td><td>vi phạm hạn phản hồi</td><td>Status suy từ <code>sla_due_at</code>; cảnh báo</td></tr><tr><td>rectify phá ràng buộc unique</td><td>DB unique + AC #12</td><td>DB lỗi/UX xấu</td><td>Map lỗi unique, DSAR không hoàn tất</td></tr><tr><td>Portability xuất dạng không máy đọc</td><td>review §1 #4</td><td>quyền di chuyển vô nghĩa</td><td>Ép JSON có cấu trúc</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Bốn quyền DSAR (access/rectify/erase/portability) là quyền cốt lõi PDPL và là bề mặt cơ quan kiểm tra đầu tiên.</li><li>Xác minh danh tính + truy vấn buộc <code>user_id</code> là hai lớp chống rò cross-user (cổng tiến phase = 0 rò rỉ).</li><li>Erase hỗn hợp: hard-delete dữ liệu thuần, soft-anonymize dữ liệu ràng buộc kế toán, giữ consent log làm chứng cứ.</li><li>Portability JSON máy đọc được giữ đúng tinh thần \"di chuyển dữ liệu\" sang hệ thống khác.</li><li>Xóa tài khoản (TASK-AUTH-005) là người tiêu dùng chính của <code>Erase</code>; hai task phải nhất quán về phạm vi xóa.</li><li>SLA suy từ <code>requested_at</code> giữ phản hồi DSAR thành quy trình có deadline, không để treo vô thời hạn.</li></ul>\n<hr>\n<p><em>Hết TASK-COMPLY-003. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
