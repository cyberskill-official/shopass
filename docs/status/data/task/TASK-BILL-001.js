window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-BILL-001"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service BILL <strong>MUST</strong> định nghĩa <code>plan_catalog</code> (bậc giá Premium) và <code>subscription</code> (đăng ký của user), với giá BIGINT VND, một subscription active mỗi user, và vòng đời status có kiểm soát. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> định nghĩa bảng <code>plan_catalog (id, tier, price, billing_period, active)</code>: <code>tier</code> - {<code>'free'</code>,<code>'premium_basic'</code>,<code>'premium_plus'</code>,<code>'premium_pro'</code>} (CHECK); ba bậc Premium ứng giá 29.000 / 49.000 / 79.000 VND/tháng (DEC-BILL-01).</li><li><strong>MUST</strong> lưu <code>plan_catalog.price</code> dạng <code>BIGINT</code> (VND, không thập phân) - KHÔNG float/numeric (DEC-BILL-02); <code>free</code> có <code>price = 0</code>.</li><li><strong>MUST</strong> định nghĩa bảng <code>subscription (id, user_id, plan_id, started_at, renews_at, status)</code> với <code>user_id</code> REFERENCES <code>app_user(id)</code>, <code>plan_id</code> REFERENCES <code>plan_catalog(id)</code>.</li><li><strong>MUST</strong> ràng buộc mỗi user có TỐI ĐA một subscription <code>status='active'</code> tại một thời điểm (DEC-BILL-03) qua partial unique index <code>UNIQUE (user_id) WHERE status='active'</code>.</li><li><strong>MUST</strong> ràng buộc <code>subscription.status</code> - {<code>'active'</code>,<code>'past_due'</code>,<code>'canceled'</code>,<code>'expired'</code>} qua CHECK (DEC-BILL-04).</li><li><strong>MUST</strong> ràng buộc <code>renews_at &gt; started_at</code> qua CHECK (chu kỳ gia hạn phải ở tương lai so với điểm bắt đầu).</li><li><strong>MUST</strong> không hardcode giá Premium trong code nghiệp vụ (DEC-BILL-05): <code>subscription</code> tham chiếu <code>plan_id</code>; giá đọc từ <code>plan_catalog</code>. Đổi giá là cập nhật <code>plan_catalog</code>, không sửa code rải rác.</li><li><strong>MUST</strong> expose hàm repo:</li></ol>\n<ul><li><code>CreateSubscription(ctx, userID, planID int64, renewsAt time.Time) (int64, error)</code> - tạo subscription <code>active</code>; lỗi nếu user đã có active (partial unique).</li><li><code>GetActive(ctx, userID int64) (Subscription, bool, error)</code> - lấy subscription active hiện tại (nếu có).</li><li><code>UpdateStatus(ctx, subID int64, to string) error</code> - chuyển trạng thái (qua <code>lifecycle.go</code>, chỉ chuyển hợp lệ).</li><li><code>SetRenewsAt(ctx, subID int64, t time.Time) error</code> - đẩy mốc gia hạn (do TASK-BILL-002/003 gọi sau thanh toán).</li></ul>\n<ol><li><strong>MUST</strong> thực thi chuyển trạng thái hợp lệ (<code>lifecycle.go</code>): <code>active -&gt; past_due</code> (trễ hạn), <code>past_due -&gt; active</code> (thanh toán lại), <code>active|past_due -&gt; canceled</code> (user hủy), <code>past_due -&gt; expired</code> (quá hạn ân hạn). Chuyển không hợp lệ (ví dụ <code>canceled -&gt; active</code>) -&gt; lỗi, không đổi.</li><li><strong>MUST</strong> seed <code>plan_catalog</code> idempotent với 4 bậc (free + 3 Premium) qua <code>ON CONFLICT (tier) DO NOTHING</code>.</li><li><strong>MUST</strong> đặt mọi cột thời gian kiểu <code>TIMESTAMPTZ</code>; <code>started_at</code> mặc định <code>now()</code> khi tạo.</li><li><strong>SHOULD</strong> phát OTel: <code>subscription_active_total{tier}</code> (gauge), <code>subscription_status_change_total{from,to}</code> (counter), <code>subscription_created_total{tier}</code> (counter).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao giá ở plan_catalog, không hardcode (DEC-BILL-05)?</strong> Giá Premium 29k/49k/79k là quyết định kinh doanh có thể đổi (khuyến mãi, điều chỉnh theo thị trường). Nếu rải literal <code>29000</code> khắp code, một lần đổi giá phải sửa nhiều nơi và dễ bỏ sót, gây tính sai. Một bảng <code>plan_catalog</code> là nguồn sự thật: subscription tham chiếu <code>plan_id</code>, giá đọc từ bảng. Đổi giá là cập nhật dữ liệu, không phải sửa code.</p>\n<p><strong>Vì sao giá BIGINT VND (DEC-BILL-02)?</strong> Giá VN là số nguyên đồng. Float gây sai số khi cộng dồn doanh thu qua hàng nghìn subscription để báo cáo và đối soát với payment gateway. BIGINT chính xác tuyệt đối, đồng nhất với <code>price_snapshot</code> (DEC-PRICE-05) và <code>affiliate_conversion</code> (DEC-AFFIL-04).</p>\n<p><strong>Vì sao một subscription active mỗi user (DEC-BILL-03)?</strong> Một người chỉ nên có một gói Premium đang hiệu lực. Nếu cho nhiều active đồng thời, ta tính tiền trùng và quyền lợi nhập nhằng (theo gói nào?). Partial unique index <code>WHERE status='active'</code> cho phép giữ lịch sử nhiều subscription cũ (canceled/expired) nhưng chỉ một active - vừa sạch vừa giữ vết.</p>\n<p><strong>Vì sao vòng đời status có kiểm soát (DEC-BILL-04, §1 #9)?</strong> Subscription đi qua các trạng thái có ý nghĩa kinh doanh: đang chạy, trễ hạn (chờ thanh toán lại), bị hủy, hết hạn. Cho phép chuyển tùy ý dễ tạo trạng thái vô lý (<code>canceled</code> rồi <code>active</code> lại mà không qua thanh toán). Một máy trạng thái với các chuyển hợp lệ giữ dữ liệu nhất quán và phản ánh đúng quy trình billing.</p>\n<p><strong>Vì sao renews_at tách khỏi thanh toán thực (DEC-BILL-04)?</strong> <code>renews_at</code> là mốc dự kiến gia hạn kế. Thanh toán thực (thành công/thất bại) do TASK-BILL-002/003 xử lý rồi đẩy <code>renews_at</code> tới và đặt status phù hợp. Tách \"lịch gia hạn\" (BILL-001) khỏi \"thực thi thanh toán\" (BILL-002/003) giữ schema này đơn giản và để phần tích hợp gateway ở đúng chỗ.</p>\n<p><strong>Vì sao seed idempotent (§1 #10)?</strong> <code>plan_catalog</code> là dữ liệu tham chiếu cố định (4 bậc). Seed qua <code>ON CONFLICT (tier) DO NOTHING</code> cho phép chạy migration/seed nhiều lần (mọi môi trường) mà không nhân đôi bậc giá - đồng nhất cách seed <code>platform</code> của TASK-INFRA-002.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migrations</h3>\n<pre><code class=\"language-sql\">-- services/bill/migrations/0001_plan_catalog.sql\nCREATE TABLE plan_catalog (\n  id             SMALLSERIAL PRIMARY KEY,\n  tier           TEXT     NOT NULL UNIQUE\n                   CHECK (tier IN ('free','premium_basic','premium_plus','premium_pro')),\n  price          BIGINT   NOT NULL CHECK (price &gt;= 0),   -- VND, không thập phân\n  billing_period TEXT     NOT NULL DEFAULT 'monthly',\n  active         BOOLEAN  NOT NULL DEFAULT true\n);\n\nINSERT INTO plan_catalog (tier, price) VALUES\n  ('free',          0),\n  ('premium_basic', 29000),\n  ('premium_plus',  49000),\n  ('premium_pro',   79000)\nON CONFLICT (tier) DO NOTHING;\n\n-- services/bill/migrations/0002_subscription.sql\nCREATE TABLE subscription (\n  id         BIGSERIAL   PRIMARY KEY,\n  user_id    BIGINT      NOT NULL REFERENCES app_user(id),\n  plan_id    SMALLINT    NOT NULL REFERENCES plan_catalog(id),\n  started_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  renews_at  TIMESTAMPTZ NOT NULL,\n  status     TEXT        NOT NULL DEFAULT 'active'\n               CHECK (status IN ('active','past_due','canceled','expired')),\n  CHECK (renews_at &gt; started_at)\n);\n\n-- Một user tối đa một subscription active (DEC-BILL-03)\nCREATE UNIQUE INDEX uq_sub_active_user\n  ON subscription (user_id) WHERE status = 'active';</code></pre>\n<h3>Types (Go)</h3>\n<pre><code class=\"language-go\">// services/bill/internal/bill/types.go\ntype Subscription struct {\n    ID        int64     `db:\"id\"`\n    UserID    int64     `db:\"user_id\"`\n    PlanID    int16     `db:\"plan_id\"`\n    StartedAt time.Time `db:\"started_at\"`\n    RenewsAt  time.Time `db:\"renews_at\"`\n    Status    string    `db:\"status\"`\n}</code></pre>\n<h3>Lifecycle - chuyển trạng thái hợp lệ (Go)</h3>\n<pre><code class=\"language-go\">// services/bill/internal/bill/lifecycle.go\n\n// valid map từ trạng thái hiện tại sang tập trạng thái được phép chuyển tới (§1 #9).\nvar valid = map[string]map[string]bool{\n    \"active\":   {\"past_due\": true, \"canceled\": true},\n    \"past_due\": {\"active\": true, \"canceled\": true, \"expired\": true},\n    \"canceled\": {}, // cuối\n    \"expired\":  {}, // cuối\n}\n\nfunc CanTransition(from, to string) bool { return valid[from][to] }\n\nfunc (r *Repo) UpdateStatus(ctx context.Context, subID int64, to string) error {\n    cur, err := r.statusOf(ctx, subID)\n    if err != nil {\n        return err\n    }\n    if !CanTransition(cur, to) {\n        return fmt.Errorf(\"%w: %s -&gt; %s\", ErrInvalidTransition, cur, to)\n    }\n    _, err = r.pool.Exec(ctx, `UPDATE subscription SET status=$1 WHERE id=$2`, to, subID)\n    metrics.StatusChange(cur, to)\n    return err\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch -&gt; <code>plan_catalog</code> (4 bậc) và <code>subscription</code> tồn tại với FK đúng.</li><li>Seed -&gt; <code>plan_catalog</code> có đúng <code>premium_basic=29000</code>, <code>premium_plus=49000</code>, <code>premium_pro=79000</code>, <code>free=0</code>.</li><li>Seed chạy lần hai -&gt; vẫn 4 bậc (ON CONFLICT DO NOTHING).</li><li><code>CreateSubscription(user, premium_basic_plan, renews)</code> -&gt; subscription <code>active</code>, <code>started_at</code> đặt, <code>renews_at &gt; started_at</code>.</li><li>Tạo subscription active thứ hai cho cùng user (khi đã có active) -&gt; vi phạm partial unique (<code>uq_sub_active_user</code>).</li><li><code>INSERT subscription</code> với <code>status='trialing'</code> (ngoài tập) -&gt; lỗi CHECK.</li><li><code>INSERT</code> với <code>renews_at &lt;= started_at</code> -&gt; lỗi CHECK.</li><li><code>GetActive(user)</code> trả đúng subscription active; user không có active -&gt; <code>found=false</code>.</li><li><code>UpdateStatus(active -&gt; past_due)</code> hợp lệ; <code>UpdateStatus(past_due -&gt; active)</code> hợp lệ (thanh toán lại).</li><li><code>UpdateStatus(canceled -&gt; active)</code> -&gt; <code>ErrInvalidTransition</code>, status không đổi.</li><li>Sau khi một subscription chuyển <code>canceled</code>, có thể tạo subscription active mới cho cùng user (partial unique chỉ chặn active).</li><li>Metric <code>subscription_status_change_total{from,to}</code> tăng đúng nhãn khi chuyển trạng thái.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/bill/internal/bill/repo_test.go\nfunc TestCreate_Active(t *testing.T) {\n    r, uid := setupWithUser(t)\n    id, err := r.CreateSubscription(ctx, uid, planID(t, r, \"premium_basic\"), time.Now().AddDate(0,1,0))\n    require.NoError(t, err)\n    sub, ok, _ := r.GetActive(ctx, uid)\n    require.True(t, ok)\n    require.Equal(t, id, sub.ID)\n    require.Equal(t, \"active\", sub.Status)\n}\n\nfunc TestCreate_OneActivePerUser(t *testing.T) {\n    r, uid := setupWithUser(t)\n    renews := time.Now().AddDate(0,1,0)\n    _, err1 := r.CreateSubscription(ctx, uid, planID(t, r, \"premium_basic\"), renews)\n    require.NoError(t, err1)\n    _, err2 := r.CreateSubscription(ctx, uid, planID(t, r, \"premium_plus\"), renews)\n    require.Error(t, err2) // partial unique WHERE status='active'\n}\n\nfunc TestPlanCatalog_Prices(t *testing.T) {\n    r := setup(t)\n    require.Equal(t, int64(29000), priceOf(t, r, \"premium_basic\"))\n    require.Equal(t, int64(49000), priceOf(t, r, \"premium_plus\"))\n    require.Equal(t, int64(79000), priceOf(t, r, \"premium_pro\"))\n}\n\nfunc TestSubscription_StatusCheck(t *testing.T) {\n    r, uid := setupWithUser(t)\n    _, err := r.pool.Exec(ctx,\n        `INSERT INTO subscription (user_id, plan_id, renews_at, status)\n         VALUES ($1,$2, now()+interval '1 month', 'trialing')`, uid, planID(t, r, \"free\"))\n    require.Error(t, err) // CHECK status IN (...)\n}\n\nfunc TestSubscription_RenewsAfterStart(t *testing.T) {\n    r, uid := setupWithUser(t)\n    _, err := r.pool.Exec(ctx,\n        `INSERT INTO subscription (user_id, plan_id, started_at, renews_at)\n         VALUES ($1,$2, now(), now()-interval '1 day')`, uid, planID(t, r, \"premium_basic\"))\n    require.Error(t, err) // CHECK renews_at &gt; started_at\n}</code></pre>\n<pre><code class=\"language-go\">// services/bill/internal/bill/lifecycle_test.go\nfunc TestTransition_Valid(t *testing.T) {\n    require.True(t, CanTransition(\"active\", \"past_due\"))\n    require.True(t, CanTransition(\"past_due\", \"active\"))\n    require.True(t, CanTransition(\"active\", \"canceled\"))\n}\n\nfunc TestTransition_Invalid(t *testing.T) {\n    require.False(t, CanTransition(\"canceled\", \"active\")) // canceled là cuối\n    require.False(t, CanTransition(\"expired\", \"active\"))\n}\n\nfunc TestUpdateStatus_RejectsInvalid(t *testing.T) {\n    r, uid := setupWithUser(t)\n    id, _ := r.CreateSubscription(ctx, uid, planID(t, r, \"premium_basic\"), time.Now().AddDate(0,1,0))\n    require.NoError(t, r.UpdateStatus(ctx, id, \"canceled\"))\n    require.ErrorIs(t, r.UpdateStatus(ctx, id, \"active\"), ErrInvalidTransition) // canceled -&gt; active cấm\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration <code>0001_plan_catalog.sql</code> (4 bậc + seed) -&gt; <code>0002_subscription.sql</code> (bảng + partial unique active + CHECK) -&gt; <code>types.go</code> + <code>repo.go</code> (<code>CreateSubscription</code>, <code>GetActive</code>, <code>SetRenewsAt</code>) -&gt; <code>lifecycle.go</code> (máy trạng thái <code>CanTransition</code> + <code>UpdateStatus</code>) -&gt; tests trên Postgres ephemeral (kiểm partial unique + CHECK thật, không mock). TASK-BILL-002 gọi <code>SetRenewsAt</code>/<code>UpdateStatus</code> sau thanh toán; TASK-BILL-005 đọc <code>GetActive</code> cho feature gating. Driver <code>pgx</code>.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-AUTH-001</strong> - <code>app_user</code> phải tồn tại trước (FK <code>user_id</code>).</li><li><strong>TASK-INFRA-002</strong> - bảng <code>app_user</code> cột lõi + quy ước đặt tên/migration.</li><li><strong>TASK-BILL-002 (downstream)</strong> - payment gateway gắn thanh toán vào subscription, đẩy <code>renews_at</code>, đổi status.</li><li><strong>TASK-BILL-003 (downstream)</strong> - reconciliation đối soát payment với subscription.</li><li><strong>TASK-BILL-004 (downstream)</strong> - referral_code gắn vào user khi tạo subscription (attribution).</li><li><strong>TASK-BILL-005 (downstream)</strong> - feature gating đọc <code>GetActive</code> để biết tier.</li><li>Lib: <code>pgx</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Tạo subscription (nội bộ, do TASK-BILL-002 gọi sau thanh toán đầu)</h3>\n<pre><code class=\"language-go\">subID, err := billRepo.CreateSubscription(ctx, userID,\n    planID, // premium_basic\n    time.Now().AddDate(0, 1, 0)) // gia hạn sau 1 tháng</code></pre>\n<h3>Bậc giá trong plan_catalog</h3>\n<pre><code class=\"language-sql\">SELECT tier, price FROM plan_catalog ORDER BY price;\n--  free          | 0\n--  premium_basic | 29000\n--  premium_plus  | 49000\n--  premium_pro   | 79000</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Chu kỳ năm (billing_period='yearly') với giá ưu đãi - thêm bậc vào <code>plan_catalog</code> khi có chiến lược; schema đã sẵn <code>billing_period</code>.</li><li>Proration khi nâng/hạ bậc giữa kỳ - thêm logic ở TASK-BILL-002 khi cần; task này chỉ giữ một active.</li><li>Trial period (dùng thử miễn phí có thời hạn) - thêm trạng thái <code>'trialing'</code> vào CHECK + máy trạng thái khi làm gamified upgrade (TASK-BILL-005).</li><li>Quyền lợi chi tiết per-tier (bảng <code>plan_feature</code>) - gắn vào TASK-BILL-005 (feature gating).</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Giá lưu float</td><td>review + types int64</td><td>sai số doanh thu/đối soát</td><td>BIGINT VND (DEC-BILL-02)</td></tr><tr><td>Nhiều subscription active/user</td><td>partial unique test</td><td>tính tiền trùng, quyền lợi rối</td><td>UNIQUE WHERE active (§1 #4)</td></tr><tr><td>Hardcode giá rải rác</td><td>review</td><td>đổi giá lệch nhiều nơi</td><td>plan_catalog nguồn sự thật (§1 #7)</td></tr><tr><td>status ngoài tập</td><td>DB CHECK</td><td>trạng thái rác</td><td>CHECK status IN (...) (§1 #5)</td></tr><tr><td>renews_at &lt;= started_at</td><td>DB CHECK</td><td>chu kỳ vô lý</td><td>CHECK renews_at &gt; started_at (§1 #6)</td></tr><tr><td>Chuyển trạng thái vô lý</td><td>lifecycle test</td><td>canceled rồi active không qua thanh toán</td><td>Máy trạng thái CanTransition (§1 #9)</td></tr><tr><td>Seed nhân đôi bậc</td><td>ON CONFLICT</td><td>giữ 4 bậc</td><td>Idempotent seed (§1 #10)</td></tr><tr><td>FK user không tồn tại</td><td>lỗi pgx</td><td>từ chối tạo</td><td>Tạo app_user trước (TASK-AUTH-001)</td></tr><tr><td>Mất lịch sử subscription cũ</td><td>partial unique giữ vết</td><td>-</td><td>Chỉ chặn active, giữ canceled/expired (§1 #4)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li><code>subscription</code> + <code>plan_catalog</code> là sổ gốc của dòng doanh thu Premium (§4.1).</li><li>Giá ở <code>plan_catalog</code> (không hardcode) cho phép đổi giá bằng cập nhật dữ liệu, không sửa code rải rác.</li><li>Giá BIGINT VND đồng nhất với price_snapshot và affiliate_conversion - tránh sai số float trên báo cáo doanh thu.</li><li>Partial unique <code>WHERE status='active'</code> cho một active mỗi user mà vẫn giữ lịch sử subscription cũ.</li><li>Máy trạng thái giữ subscription nhất quán: chỉ chuyển hợp lệ, không có trạng thái vô lý.</li><li><code>renews_at</code> là lịch gia hạn; thanh toán thực do TASK-BILL-002/003 đẩy - tách lịch khỏi thực thi.</li><li>Đây là nền cho TASK-BILL-002 (thanh toán), TASK-BILL-003 (đối soát), TASK-BILL-005 (gating theo tier).</li></ul>\n<hr>\n<p><em>Hết TASK-BILL-001. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
