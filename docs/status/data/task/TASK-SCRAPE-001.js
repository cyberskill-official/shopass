window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SCRAPE-001"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service SCRAPE <strong>MUST</strong> cung cấp một orchestrator lõi điều phối việc quét giá 3 sàn theo tần suất phân tầng, qua hàng đợi bền và worker pool giới hạn concurrency, gọi adapter per-sàn rồi ghi delta-only. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> định nghĩa bảng <code>scrape_job (product_id, platform_id, tier, next_run_at, attempts, last_status, locked_until)</code> với <code>PRIMARY KEY (product_id)</code> và index trên <code>(next_run_at)</code> để scheduler quét job đến hạn nhanh.</li><li><strong>MUST</strong> phân tầng tần suất quét (DEC-SCRAPE-01) qua enum <code>tier - {hot, warm, cold}</code>:</li></ol>\n<ul><li><code>hot</code> (đang flash sale hoặc SKU biến động): chu kỳ &lt;= 5 phút.</li><li><code>warm</code> (SKU được theo dõi, ổn định): chu kỳ 1-6 giờ.</li><li><code>cold</code> (SKU ít quan tâm): chu kỳ 24 giờ. Hàm <code>NextRunAt(tier, now) time.Time</code> <strong>MUST</strong> trả mốc kế tiếp đúng theo tier.</li></ul>\n<ol><li><strong>MUST</strong> cho phép promote/demote tier: khi giá vừa thay đổi hoặc <code>flash_sale=true</code> -&gt; promote lên <code>hot</code>; sau N chu kỳ không đổi -&gt; demote dần về <code>warm</code>/<code>cold</code>. Logic này <strong>MUST</strong> là hàm thuần kiểm thử được <code>ReTier(current, changed, flashSale) tier</code>.</li><li><strong>MUST</strong> dùng hàng đợi bền (DEC-SCRAPE-02): scheduler đẩy job đến hạn vào Redis Streams; worker <code>claim</code> job, xử lý, rồi <code>ack</code>. Job chưa <code>ack</code> quá thời gian <code>locked_until</code> <strong>MUST</strong> được re-claim (chống mất job khi worker crash).</li><li><strong>MUST</strong> retry có giới hạn + backoff: mỗi job thất bại tăng <code>attempts</code>; backoff hàm mũ có jitter; vượt <code>max_attempts</code> (mặc định 5) -&gt; đánh <code>last_status='failed'</code> và đẩy sang dead-letter, KHÔNG retry vô hạn.</li><li><strong>MUST</strong> giới hạn concurrency per-platform (DEC-SCRAPE-03): worker pool có cấu hình <code>max_concurrency[platform_id]</code>; số worker đang chạy cho một sàn <strong>MUST</strong> không vượt ngưỡng này (tránh tự kích hoạt anti-bot §3.9).</li><li><strong>MUST</strong> định nghĩa interface adapter chung (DEC-SCRAPE-04): <code>PlatformAdapter.Fetch(ctx, job ScrapeJob) (price.PriceSnapshot, error)</code>. Mỗi sàn là một implementation (TASK-SCRAPE-002 Shopee, TASK-SCRAPE-007 TikTok, TASK-SCRAPE-008 Lazada) đăng ký theo <code>platform_id</code>.</li><li><strong>MUST</strong> sau khi adapter trả snapshot, orchestrator gọi <code>priceRepo.InsertSnapshot</code> delta-only của TASK-PRICE-002 (DEC-SCRAPE-05) - orchestrator KHÔNG tự viết SQL vào <code>price_snapshot</code>.</li><li><strong>MUST</strong> cập nhật <code>next_run_at</code> của job sau mỗi lần xử lý (thành công hay thất bại) theo tier hiện tại, để vòng lặp scheduler tiếp tục.</li><li><strong>SHOULD</strong> phát OTel metric: <code>scrape_job_dispatched_total{platform_id, tier}</code> (counter), <code>scrape_job_duration_ms{platform_id}</code> (histogram), <code>scrape_job_failed_total{platform_id, reason}</code> (counter), <code>scrape_worker_inflight{platform_id}</code> (gauge).</li><li><strong>MUST</strong> idempotent khi re-claim: xử lý cùng một job 2 lần (do timeout re-claim) KHÔNG được tạo dữ liệu sai - delta-only của TASK-PRICE-002 (<code>ON CONFLICT DO NOTHING</code>) đảm bảo điều này.</li><li><strong>MUST</strong> đọc cấu hình bí mật (proxy creds, API base) qua TASK-INFRA-003 (Vault), KHÔNG hardcode trong mã hay env cleartext.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao phân tầng tần suất (DEC-SCRAPE-01)?</strong> Quét mọi SKU mỗi phút là bất khả thi về chi phí proxy (§4.1) và tự gọi ban. Nhưng SKU đang flash sale đổi giá theo phút, quét mỗi ngày thì bỏ lỡ. Tiering tách \"SKU nóng cần nhanh\" khỏi \"SKU nguội quét thưa\" - tiêu proxy đúng chỗ. Đây là đòn bẩy unit economics phía băng thông, song song với delta-only phía storage.</p>\n<p><strong>Vì sao hàng đợi bền + re-claim (DEC-SCRAPE-02)?</strong> Scraping farm chạy hàng nghìn job; worker sẽ crash. Nếu job sống trong bộ nhớ worker, crash là mất job -&gt; lủng lịch sử giá, hỏng baseline 90 ngày (§5.1) vốn là điều kiện ra mắt sale ảo. Redis Streams với <code>claim/ack</code> + <code>locked_until</code> cho phép một worker khác nhặt lại job mồ côi.</p>\n<p><strong>Vì sao giới hạn concurrency per-platform (DEC-SCRAPE-03)?</strong> Anti-bot của Shopee/TikTok/Lazada (§3.9) phản ứng với mật độ request từ một nguồn. Bắn song song không giới hạn là cách nhanh nhất để bị ban. Cap concurrency per-platform giữ áp lực dưới ngưỡng phát hiện, phối hợp với pacing/jitter của TASK-SCRAPE-005.</p>\n<p><strong>Vì sao adapter là interface (DEC-SCRAPE-04)?</strong> 3 sàn có cơ chế truy cập khác hẳn nhau: Shopee có internal API đọc được khi <code>is_login:false</code>; TikTok ký request nên phải đọc DOM; Lazada sau Akamai. Tách orchestrator (điều phối, tiering, queue) khỏi adapter (cách lấy dữ liệu từng sàn) cho phép thêm sàn mà không sửa bộ não.</p>\n<p><strong>Vì sao orchestrator không ghi DB (DEC-SCRAPE-05)?</strong> Delta-only là logic của PRICE (TASK-PRICE-002): chỉ ghi khi giá đổi. Nếu orchestrator tự <code>INSERT</code>, nó sẽ phải nhân bản logic so sánh và dễ lệch. Gọi <code>InsertSnapshot</code> giữ một nguồn sự thật duy nhất cho quy tắc ghi.</p>\n<p><strong>Vì sao re-tier theo biến động (§1 #3)?</strong> SKU không cố định \"nóng\" hay \"nguội\". Một SKU thường có thể vào flash sale bất ngờ. Promote khi vừa thấy thay đổi/flash, demote khi yên tĩnh, giúp farm tự thích nghi mà không cần người chỉnh tay từng SKU.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/scrape/migrations/0001_scrape_job.sql\nCREATE TYPE scrape_tier AS ENUM ('hot', 'warm', 'cold');\n\nCREATE TABLE scrape_job (\n  product_id    BIGINT      PRIMARY KEY REFERENCES tracked_product(id),\n  platform_id   SMALLINT    NOT NULL REFERENCES platform(id),\n  tier          scrape_tier NOT NULL DEFAULT 'cold',\n  next_run_at   TIMESTAMPTZ NOT NULL DEFAULT now(),\n  attempts      INTEGER     NOT NULL DEFAULT 0,\n  last_status   TEXT        NOT NULL DEFAULT 'pending',  -- pending|ok|retry|failed\n  locked_until  TIMESTAMPTZ\n);\n\nCREATE INDEX idx_job_due ON scrape_job (next_run_at)\n  WHERE last_status &lt;&gt; 'failed';\nCREATE INDEX idx_job_platform ON scrape_job (platform_id, tier);</code></pre>\n<h3>Interface adapter (Go)</h3>\n<pre><code class=\"language-go\">// services/scrape/internal/orchestrator/adapter.go\ntype PlatformAdapter interface {\n    // Fetch lấy snapshot giá cho một job. Lỗi mạng/parse trả error;\n    // orchestrator quyết định retry/backoff dựa trên error.\n    Fetch(ctx context.Context, job ScrapeJob) (price.PriceSnapshot, error)\n    PlatformID() int16\n}\n\ntype ScrapeJob struct {\n    ProductID   int64\n    PlatformID  int16\n    Tier        Tier\n    Attempts    int\n}</code></pre>\n<h3>Tiering (Go)</h3>\n<pre><code class=\"language-go\">// services/scrape/internal/orchestrator/tier.go\ntype Tier string\n\nconst (\n    TierHot  Tier = \"hot\"\n    TierWarm Tier = \"warm\"\n    TierCold Tier = \"cold\"\n)\n\n// NextRunAt trả mốc quét kế tiếp theo tier (có jitter nhẹ để rải tải).\nfunc NextRunAt(t Tier, now time.Time) time.Time {\n    switch t {\n    case TierHot:\n        return now.Add(jitter(3*time.Minute, 2*time.Minute))   // ~1-5 phút\n    case TierWarm:\n        return now.Add(jitter(3*time.Hour, 3*time.Hour))       // ~1-6 giờ\n    default:\n        return now.Add(jitter(24*time.Hour, 1*time.Hour))      // ~ngày\n    }\n}\n\n// ReTier quyết định tier mới dựa trên kết quả lần quét gần nhất.\nfunc ReTier(cur Tier, changed, flashSale bool) Tier {\n    if flashSale || changed {\n        return TierHot\n    }\n    switch cur {\n    case TierHot:\n        return TierWarm  // hết biến động -&gt; hạ dần\n    case TierWarm:\n        return TierCold\n    default:\n        return TierCold\n    }\n}</code></pre>\n<h3>Worker pool (Go)</h3>\n<pre><code class=\"language-go\">// services/scrape/internal/orchestrator/pool.go\n// runOne xử lý một job: gọi adapter, ghi delta-only, re-tier, đặt next_run_at.\nfunc (p *Pool) runOne(ctx context.Context, job ScrapeJob) error {\n    a := p.adapters[job.PlatformID]\n    snap, err := a.Fetch(ctx, job)\n    if err != nil {\n        return p.scheduleRetry(ctx, job, err)   // tăng attempts + backoff\n    }\n    written, err := p.price.InsertSnapshot(ctx, snap)  // delta-only (TASK-PRICE-002)\n    if err != nil {\n        return p.scheduleRetry(ctx, job, err)\n    }\n    next := ReTier(job.Tier, written, snap.FlashSale)\n    return p.commit(ctx, job.ProductID, next, NextRunAt(next, time.Now()))\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch -&gt; <code>scrape_job</code> tồn tại với enum <code>scrape_tier</code> và index <code>idx_job_due</code>.</li><li><code>NextRunAt(TierHot, now)</code> trả mốc trong vòng &lt;= 5 phút; <code>TierWarm</code> trong 1-6 giờ; <code>TierCold</code> ~24 giờ.</li><li><code>ReTier(_, changed=true, _)</code> -&gt; <code>hot</code>; <code>ReTier(_, _, flashSale=true)</code> -&gt; <code>hot</code>.</li><li><code>ReTier(hot, false, false)</code> -&gt; <code>warm</code>; <code>ReTier(warm, false, false)</code> -&gt; <code>cold</code>; <code>ReTier(cold, false, false)</code> -&gt; <code>cold</code>.</li><li>Scheduler đẩy đúng các job có <code>next_run_at &lt;= now</code> vào queue; bỏ qua job tương lai và job <code>failed</code>.</li><li>Worker <code>claim</code> job, xử lý xong gọi <code>ack</code>; job được xử lý đúng 1 lần trong điều kiện thường.</li><li>Job bị bỏ dở (worker \"crash\" mô phỏng, không <code>ack</code>) -&gt; sau <code>locked_until</code> được worker khác re-claim.</li><li>Concurrency per-platform được tôn trọng: với <code>max_concurrency[shopee]=N</code>, không bao giờ có &gt;N worker chạy job Shopee đồng thời.</li><li>Job thất bại liên tục -&gt; <code>attempts</code> tăng, backoff giãn dần; vượt <code>max_attempts</code> -&gt; <code>last_status='failed'</code>, vào dead-letter, ngừng retry.</li><li>Sau xử lý thành công, orchestrator gọi <code>InsertSnapshot</code> (không tự viết SQL vào <code>price_snapshot</code>).</li><li>Re-claim cùng job 2 lần không tạo dữ liệu sai (nhờ <code>ON CONFLICT DO NOTHING</code> của TASK-PRICE-002).</li><li>Metric <code>scrape_job_dispatched_total</code>, <code>scrape_job_failed_total</code>, <code>scrape_worker_inflight</code> thay đổi đúng theo vòng đời job.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/scrape/internal/orchestrator/tier_test.go\nfunc TestNextRunAt_Tiers(t *testing.T) {\n    now := time.Now()\n    require.LessOrEqual(t, NextRunAt(TierHot, now).Sub(now), 5*time.Minute)\n    warm := NextRunAt(TierWarm, now).Sub(now)\n    require.True(t, warm &gt;= time.Hour &amp;&amp; warm &lt;= 6*time.Hour)\n    require.GreaterOrEqual(t, NextRunAt(TierCold, now).Sub(now), 23*time.Hour)\n}\n\nfunc TestReTier_PromoteOnChange(t *testing.T) {\n    require.Equal(t, TierHot, ReTier(TierCold, true, false))   // giá đổi -&gt; nóng\n    require.Equal(t, TierHot, ReTier(TierWarm, false, true))   // flash -&gt; nóng\n}\n\nfunc TestReTier_DemoteWhenQuiet(t *testing.T) {\n    require.Equal(t, TierWarm, ReTier(TierHot, false, false))\n    require.Equal(t, TierCold, ReTier(TierWarm, false, false))\n    require.Equal(t, TierCold, ReTier(TierCold, false, false))\n}</code></pre>\n<pre><code class=\"language-go\">// services/scrape/internal/orchestrator/pool_test.go\nfunc TestPool_ConcurrencyCapPerPlatform(t *testing.T) {\n    p := newTestPool(t, map[int16]int{1: 2}) // Shopee cap = 2\n    var peak int32\n    p.adapters[1] = blockingAdapter(func() {\n        n := atomic.AddInt32(&amp;p.inflight[1], 1)\n        if n &gt; atomic.LoadInt32(&amp;peak) { atomic.StoreInt32(&amp;peak, n) }\n        time.Sleep(20 * time.Millisecond)\n        atomic.AddInt32(&amp;p.inflight[1], -1)\n    })\n    enqueueN(t, p, 1, 10) // 10 job Shopee\n    p.drain(t)\n    require.LessOrEqual(t, peak, int32(2)) // không vượt cap\n}\n\nfunc TestPool_RetryThenFail(t *testing.T) {\n    p := newTestPool(t, map[int16]int{1: 1})\n    p.adapters[1] = alwaysErr()\n    job := seedJob(t, p, 1)\n    for i := 0; i &lt; 6; i++ { p.runOne(ctx, job) }\n    require.Equal(t, \"failed\", statusOf(t, p, job.ProductID))\n    require.GreaterOrEqual(t, attemptsOf(t, p, job.ProductID), 5)\n}\n\nfunc TestPool_ReclaimOrphanJob(t *testing.T) {\n    p := newTestPool(t, map[int16]int{1: 1})\n    job := seedJob(t, p, 1)\n    p.claim(ctx, job)                 // worker A nhận\n    advanceClock(p, 2*time.Minute)    // A \"crash\", quá locked_until\n    got, ok := p.claim(ctx, job)      // worker B re-claim\n    require.True(t, ok)\n    require.Equal(t, job.ProductID, got.ProductID)\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration 0001 (scrape_job) -&gt; tier.go (thuần, test trước) -&gt; queue.go (Redis Streams) -&gt; pool.go (worker, gọi adapter + InsertSnapshot) -&gt; scheduler.go (vòng lặp quét job đến hạn) -&gt; tests. Adapter Shopee thật đến ở TASK-SCRAPE-002; ở task này dùng adapter giả (stub) để kiểm thử orchestrator độc lập. Concurrency cap và proxy đọc từ cấu hình Vault (TASK-INFRA-003).</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-INFRA-003</strong> - secrets (proxy creds, API base) qua Vault; orchestrator không hardcode.</li><li><strong>TASK-PRICE-001</strong> - <code>scrape_job.product_id</code> REFERENCES <code>tracked_product(id)</code> (FK cứng); bảng này phải tồn tại trước khi migration <code>0001_scrape_job.sql</code> chạy. <code>platform(id)</code> của TASK-INFRA-002 cũng phải có (FK <code>platform_id</code>).</li><li><strong>TASK-PRICE-002 (downstream của ghi)</strong> - orchestrator gọi <code>InsertSnapshot</code> delta-only.</li><li><strong>TASK-SCRAPE-002 / 007 / 008</strong> - các adapter sàn cắm vào interface <code>PlatformAdapter</code>.</li><li><strong>TASK-SCRAPE-003 / 004 / 005</strong> - farm Playwright, proxy rotation, pacing chạy bên trong adapter/pool.</li><li><strong>TASK-TRACK-001</strong> - tạo <code>tracked_product</code> (và một <code>scrape_job</code> tương ứng) khi user theo dõi SKU.</li><li>Hạ tầng: Redis Streams (queue), Postgres (scrape_job), OTel.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Tạo job khi user theo dõi sản phẩm (nội bộ, gọi từ TASK-TRACK-001)</h3>\n<pre><code class=\"language-go\">orch.Enqueue(ctx, orchestrator.ScrapeJob{\n    ProductID:  90112,\n    PlatformID: 1,            // shopee\n    Tier:       orchestrator.TierWarm,\n})\n// scheduler sẽ đặt next_run_at theo tier và đẩy vào queue khi đến hạn</code></pre>\n<h3>Cấu hình concurrency per-platform (config)</h3>\n<pre><code class=\"language-yaml\">scrape:\n  max_concurrency:\n    shopee: 12\n    tiktok: 6     # ký request khó, đọc DOM nặng hơn -&gt; cap thấp hơn\n    lazada: 8\n  max_attempts: 5\n  backoff_base_ms: 500</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Phân tải scheduler đa-node (leader election) khi farm vượt một node - slice sau.</li><li>Tier \"burst\" riêng cho double-date (1.1, 2.2 ... 12.12) tạm gộp vào <code>hot</code> qua re-tier; tách thành lịch sự kiện ở giai đoạn ML (TASK-DEAL-004).</li><li>Ưu tiên job (priority queue) cho SKU Premium-user - gắn khi BILL (TASK-BILL-001) lên.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Worker crash giữa job</td><td><code>locked_until</code> quá hạn</td><td>job treo</td><td>Re-claim sau timeout (§1 #4)</td></tr><tr><td>Adapter lỗi mạng/parse</td><td>error từ Fetch</td><td>quét hụt</td><td>Retry + backoff; vượt max -&gt; dead-letter</td></tr><tr><td>Concurrency vượt cap (bug)</td><td>gauge inflight</td><td>nguy cơ ban</td><td>Pool chặn cứng tại semaphore per-platform</td></tr><tr><td>Một-tần-suất cho mọi SKU</td><td>review cấu hình</td><td>đốt proxy / trễ flash</td><td>Tiering bắt buộc (§1 #2)</td></tr><tr><td>Scheduler đẩy job tương lai</td><td>test #5</td><td>quét sớm vô ích</td><td>Lọc <code>next_run_at &lt;= now</code></td></tr><tr><td>Job mồ côi re-claim -&gt; xử lý 2 lần</td><td>property test</td><td>ghi trùng</td><td>ON CONFLICT DO NOTHING (TASK-PRICE-002)</td></tr><tr><td>Backoff không jitter -&gt; đồng pha retry</td><td>metric spike</td><td>tự dồn tải / ban</td><td>jitter trong backoff (§1 #5)</td></tr><tr><td>Proxy creds rò qua env cleartext</td><td>audit secrets</td><td>lộ bí mật</td><td>Đọc từ Vault (TASK-INFRA-003)</td></tr><tr><td>Dead-letter phình (sàn đổi DOM hàng loạt)</td><td>DLQ size</td><td>quét hỏng diện rộng</td><td>Alert qua TASK-SCRAPE-006, sửa adapter</td></tr><tr><td>Redis Streams mất kết nối</td><td>health check</td><td>queue đứng</td><td>Reconnect + job vẫn bền trong stream</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Orchestrator là bộ não điều phối; mọi adapter sàn cắm vào interface <code>PlatformAdapter</code>, nên thêm TikTok/Lazada không đụng vào tiering/queue.</li><li>Tiering là đòn bẩy unit economics phía băng thông (§4.1), song song với delta-only phía storage (TASK-PRICE-002).</li><li>Concurrency cap per-platform là tuyến phòng thủ anti-ban đầu tiên (§3.9), trước pacing/jitter (TASK-SCRAPE-005) và proxy rotation (TASK-SCRAPE-004).</li><li>Hàng đợi bền + re-claim bảo vệ tính liên tục của dữ liệu lịch sử, điều kiện sống còn của cold-start 90 ngày (§5.1).</li><li>Orchestrator không ghi DB trực tiếp - một nguồn sự thật duy nhất cho quy tắc ghi nằm ở delta-only của PRICE.</li></ul>\n<hr>\n<p><em>Hết TASK-SCRAPE-001. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
