window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-TRACK-002"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service TRACK <strong>MUST</strong> cung cấp schema và API CRUD cho <code>wishlist</code> (nhóm do user đặt tên) và <code>wishlist_item</code> (một SKU trong nhóm kèm <code>target_price</code>), với mọi thao tác bắt buộc kiểm chủ sở hữu. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> định nghĩa bảng <code>wishlist (id BIGSERIAL PK, user_id BIGINT REFERENCES app_user(id), name TEXT NOT NULL, created_at TIMESTAMPTZ DEFAULT now())</code>.</li><li><strong>MUST</strong> định nghĩa bảng <code>wishlist_item (id BIGSERIAL PK, wishlist_id BIGINT REFERENCES wishlist(id) ON DELETE CASCADE, product_id BIGINT REFERENCES tracked_product(id), target_price BIGINT, added_at TIMESTAMPTZ DEFAULT now())</code>.</li><li><strong>MUST</strong> lưu <code>target_price</code> dạng <code>BIGINT</code> VND nullable (DEC-TRACK-14): NULL nghĩa \"chưa đặt giá mong muốn\". KHÔNG dùng float/numeric (đồng nhất DEC-PRICE-05).</li><li><strong>MUST</strong> đặt <code>UNIQUE (wishlist_id, product_id)</code> (DEC-TRACK-13): một SKU không thể nằm hai lần trong cùng một wishlist; là khóa <code>ON CONFLICT</code> cho thêm idempotent.</li><li><strong>MUST</strong> phục vụ <code>POST /v1/wishlists {name}</code> tạo wishlist mới gắn <code>user_id</code> từ JWT; trả <code>201</code> + <code>{id, name}</code>.</li><li><strong>MUST</strong> phục vụ <code>GET /v1/wishlists</code> trả mọi wishlist của đúng user gọi (kèm số item); KHÔNG trả wishlist của user khác (DEC-TRACK-12).</li><li><strong>MUST</strong> phục vụ <code>POST /v1/wishlists/{id}/items {product_id, target_price?}</code> thêm một SKU; <code>product_id</code> phải tồn tại trong <code>tracked_product</code> (DEC-TRACK-13), nếu không trả <code>400</code>. Thêm trùng (SKU đã trong wishlist) trả <code>200</code> idempotent (no-op), không tạo dòng thứ hai.</li><li><strong>MUST</strong> phục vụ <code>DELETE /v1/wishlists/{id}/items/{product_id}</code> gỡ một SKU khỏi wishlist; <code>DELETE /v1/wishlists/{id}</code> xóa cả wishlist và mọi item (CASCADE).</li><li><strong>MUST</strong> kiểm chủ sở hữu trên MỌI route theo <code>{id}</code> (DEC-TRACK-12): nếu <code>wishlist.user_id != caller</code> thì trả <code>404</code> (không <code>403</code>) để không lộ sự tồn tại của wishlist người khác. KHÔNG có route nào bỏ qua kiểm tra này.</li><li><strong>MUST</strong> lấy <code>user_id</code> từ JWT do API Gateway (TASK-INFRA-001) gắn; handler KHÔNG tự parse token.</li><li><strong>MUST</strong> trả <code>target_price</code> trong JSON là số nguyên VND (int64) hoặc <code>null</code>; KHÔNG float, KHÔNG string. Đặt <code>Content-Type: application/json; charset=utf-8</code>.</li><li><strong>SHOULD</strong> phát OTel: <code>wishlist_ops_total{op, status}</code> (counter, <code>op - {create, list, add_item, remove_item, delete}</code>), <code>wishlist_item_count</code> (gauge theo user) để theo dõi mức dùng.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao wishlist nhiều cấp do user đặt tên (DEC-TRACK-10)?</strong> Người mua sắm phân loại ý định: \"cần mua tháng này\", \"chờ sale 12.12\", \"quà tặng\". Một danh sách phẳng trộn mọi thứ lại. Cho user tạo nhiều wishlist có tên là mô hình quen thuộc (giống giỏ lưu của các sàn) và là khung để gắn <code>target_price</code> theo từng món trong từng ngữ cảnh.</p>\n<p><strong>Vì sao kiểm chủ sở hữu trả 404 thay vì 403 (DEC-TRACK-12, §1 #9)?</strong> Trả <code>403 Forbidden</code> cho id của người khác vô tình xác nhận \"wishlist này tồn tại nhưng không phải của bạn\" - rò rỉ thông tin. Trả <code>404 Not Found</code> đồng nhất cho cả \"không tồn tại\" lẫn \"không phải của bạn\" làm kẻ dò không phân biệt được, đóng kênh liệt kê id. Đây là lỗ IDOR kinh điển nếu bỏ kiểm tra: id tuần tự (BIGSERIAL) dễ đoán.</p>\n<p><strong>Vì sao target_price là BIGINT VND nullable (DEC-TRACK-14)?</strong> Giá mong muốn là số nguyên đồng. NULL phân biệt \"thêm vào để theo dõi\" với \"thêm vào và chờ về giá X\". Giá trị này chảy thẳng vào alert_rule type <code>price_below</code> (TASK-TRACK-003): khi user đặt target, hệ thống có thể tạo luật cảnh báo tương ứng. BIGINT giữ phép so sánh giá chính xác tuyệt đối.</p>\n<p><strong>Vì sao UNIQUE(wishlist_id, product_id) và thêm trùng là no-op (DEC-TRACK-13, §1 #7)?</strong> Người dùng có thể bấm \"thêm vào wishlist\" hai lần. Hai dòng cho cùng SKU trong một danh sách là vô nghĩa và làm rối UI. Ràng buộc kép + <code>ON CONFLICT DO NOTHING</code> (hoặc <code>DO UPDATE</code> target_price) làm thao tác idempotent: bấm lại chỉ cập nhật giá mong muốn, không nhân đôi.</p>\n<p><strong>Vì sao product_id phải tồn tại trong tracked_product (§1 #7)?</strong> <code>wishlist_item</code> chỉ trỏ tới SKU hệ thống đã biết và đang theo dõi giá. Cho thêm một <code>product_id</code> tùy ý tạo FK rác trỏ tới hư không, và sau không có chuỗi giá để hiện. Buộc SKU đã qua <code>POST /v1/track</code> (TASK-TRACK-001) giữ wishlist luôn gắn dữ liệu thật.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/track/migrations/0002_wishlist.sql\nCREATE TABLE wishlist (\n  id         BIGSERIAL   PRIMARY KEY,\n  user_id    BIGINT      NOT NULL REFERENCES app_user(id),\n  name       TEXT        NOT NULL,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now()\n);\nCREATE INDEX idx_wishlist_user ON wishlist (user_id);\n\nCREATE TABLE wishlist_item (\n  id           BIGSERIAL   PRIMARY KEY,\n  wishlist_id  BIGINT      NOT NULL REFERENCES wishlist(id) ON DELETE CASCADE,\n  product_id   BIGINT      NOT NULL REFERENCES tracked_product(id),\n  target_price BIGINT      CHECK (target_price IS NULL OR target_price &gt; 0), -- VND\n  added_at     TIMESTAMPTZ NOT NULL DEFAULT now(),\n  UNIQUE (wishlist_id, product_id)\n);</code></pre>\n<h3>Repo có phân quyền (Go)</h3>\n<pre><code class=\"language-go\">// services/track/internal/track/wishlist_repo.go\n\n// AddItem thêm SKU vào wishlist (idempotent), cập nhật target_price nếu thêm lại.\n// product_id phải tồn tại trong tracked_product (FK chặn id rác).\nfunc (r *Repo) AddItem(ctx context.Context, wishlistID, productID int64, target *int64) error {\n    _, err := r.pool.Exec(ctx,\n        `INSERT INTO wishlist_item (wishlist_id, product_id, target_price)\n         VALUES ($1, $2, $3)\n         ON CONFLICT (wishlist_id, product_id)\n         DO UPDATE SET target_price = EXCLUDED.target_price`, // thêm lại = cập nhật giá mong muốn\n        wishlistID, productID, target)\n    return err\n}\n\n// ownsWishlist trả true nếu wishlist thuộc user (DEC-TRACK-12). Handler gọi trước mọi thao tác theo {id}.\nfunc (r *Repo) ownsWishlist(ctx context.Context, userID, wishlistID int64) (bool, error) {\n    var ok bool\n    err := r.pool.QueryRow(ctx,\n        `SELECT EXISTS(SELECT 1 FROM wishlist WHERE id = $1 AND user_id = $2)`,\n        wishlistID, userID).Scan(&amp;ok)\n    return ok, err\n}</code></pre>\n<h3>Handler (Go) - kiểm chủ sở hữu</h3>\n<pre><code class=\"language-go\">// services/track/internal/api/wishlist.go\nfunc (h *Handler) HandleAddItem(w http.ResponseWriter, req *http.Request) {\n    userID := auth.UserID(req.Context())\n    wid, err := strconv.ParseInt(req.PathValue(\"id\"), 10, 64)\n    if err != nil {\n        writeErr(w, http.StatusBadRequest, \"invalid wishlist id\")\n        return\n    }\n    owns, err := h.repo.OwnsWishlist(req.Context(), userID, wid)\n    if err != nil {\n        writeErr(w, http.StatusInternalServerError, \"internal error\")\n        return\n    }\n    if !owns {\n        writeErr(w, http.StatusNotFound, \"wishlist not found\") // 404, không 403 (DEC-TRACK-12)\n        return\n    }\n    var body struct {\n        ProductID   int64  `json:\"product_id\"`\n        TargetPrice *int64 `json:\"target_price\"`\n    }\n    if err := json.NewDecoder(req.Body).Decode(&amp;body); err != nil {\n        writeErr(w, http.StatusBadRequest, \"invalid body\")\n        return\n    }\n    if err := h.repo.AddItem(req.Context(), wid, body.ProductID, body.TargetPrice); err != nil {\n        if isFKViolation(err) { // product_id chưa có trong tracked_product\n            writeErr(w, http.StatusBadRequest, \"product not tracked\")\n            return\n        }\n        writeErr(w, http.StatusInternalServerError, \"internal error\")\n        return\n    }\n    w.WriteHeader(http.StatusOK) // idempotent: 200 dù mới hay đã có\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>POST /v1/wishlists {name:\"Cần mua\"}</code> trả <code>201</code> + <code>{id, name}</code>; dòng <code>wishlist</code> có <code>user_id</code> của caller.</li><li><code>GET /v1/wishlists</code> chỉ trả wishlist của caller; wishlist của user khác KHÔNG xuất hiện.</li><li><code>POST /v1/wishlists/{id}/items {product_id, target_price:89000}</code> trả <code>200</code>; <code>wishlist_item</code> lưu <code>target_price=89000</code> (int64).</li><li>Thêm cùng <code>product_id</code> lần hai với <code>target_price</code> khác -&gt; <code>200</code>, một dòng, <code>target_price</code> đã cập nhật (idempotent).</li><li>Thêm <code>product_id</code> chưa có trong <code>tracked_product</code> -&gt; <code>400</code> (FK chặn id rác).</li><li>Mọi route theo <code>{id}</code> với wishlist của user khác -&gt; <code>404</code> (không <code>403</code>, không lộ tồn tại).</li><li><code>DELETE /v1/wishlists/{id}</code> xóa wishlist và mọi <code>wishlist_item</code> (CASCADE); item không còn.</li><li><code>DELETE /v1/wishlists/{id}/items/{product_id}</code> gỡ đúng một item; các item khác giữ nguyên.</li><li><code>target_price</code> trong JSON là int64 hoặc <code>null</code>; không float, không string.</li><li><code>UNIQUE (wishlist_id, product_id)</code> chặn hai dòng cùng SKU trong một wishlist qua insert trần.</li><li>Request thiếu JWT bị gateway chặn; handler không tự xác thực.</li><li>Metric <code>wishlist_ops_total{op, status}</code> tăng đúng theo từng thao tác.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/track/internal/track/wishlist_repo_test.go\nfunc TestAddItem_Idempotent_UpdatesTarget(t *testing.T) {\n    r, uid, wid, pid := setupWishlist(t)\n    require.NoError(t, r.AddItem(ctx, wid, pid, ptr(int64(99_000))))\n    require.NoError(t, r.AddItem(ctx, wid, pid, ptr(int64(79_000)))) // thêm lại\n    items := r.listItems(t, wid)\n    require.Len(t, items, 1)                          // không nhân đôi\n    require.Equal(t, int64(79_000), *items[0].TargetPrice) // giá đã cập nhật\n}\n\nfunc TestAddItem_UnknownProduct_FK(t *testing.T) {\n    r, _, wid, _ := setupWishlist(t)\n    err := r.AddItem(ctx, wid, 999999, nil)\n    require.Error(t, err) // FK tracked_product -&gt; handler map 400\n}\n\n// services/track/internal/api/wishlist_test.go\nfunc TestList_ScopedToUser(t *testing.T) {\n    h := setupHandler(t)\n    widA := createWishlist(t, h, userA, \"A\")\n    createWishlist(t, h, userB, \"B\")\n    rec := doGETAs(t, h, userA, \"/v1/wishlists\")\n    var lists []WishlistDTO\n    decode(t, rec, &amp;lists)\n    require.Len(t, lists, 1)\n    require.Equal(t, widA, lists[0].ID) // không thấy wishlist của userB\n}\n\nfunc TestCrossUser_404(t *testing.T) {\n    h := setupHandler(t)\n    widB := createWishlist(t, h, userB, \"B\")\n    rec := doPOSTAs(t, h, userA, \"/v1/wishlists/\"+itoa(widB)+\"/items\",\n        `{\"product_id\":1}`)\n    require.Equal(t, 404, rec.Code) // không 403 (DEC-TRACK-12)\n}\n\nfunc TestDeleteWishlist_Cascade(t *testing.T) {\n    h := setupHandler(t)\n    wid := createWishlist(t, h, userA, \"A\")\n    addItem(t, h, userA, wid, trackedPID(t, h))\n    doDELETEAs(t, h, userA, \"/v1/wishlists/\"+itoa(wid))\n    require.Equal(t, 0, countItems(t, h, wid)) // CASCADE xóa item\n}\n\nfunc TestTargetPrice_Int64InJSON(t *testing.T) {\n    h := setupHandler(t)\n    wid := createWishlist(t, h, userA, \"A\")\n    pid := trackedPID(t, h)\n    addItemWithTarget(t, h, userA, wid, pid, 89_000)\n    rec := doGETAs(t, h, userA, \"/v1/wishlists/\"+itoa(wid)+\"/items\")\n    require.Contains(t, rec.Body.String(), `\"target_price\":89000`) // số nguyên, không \"89000.0\"\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration <code>0002_wishlist.sql</code> (2 bảng + FK + UNIQUE + CASCADE) -&gt; <code>wishlist_repo.go</code> (5 hàm, mỗi hàm theo <code>{id}</code> đi qua <code>OwnsWishlist</code>) -&gt; <code>wishlist.go</code> (5 handler) -&gt; đăng ký route trong <code>router.go</code> sau JWT middleware (TASK-INFRA-001) -&gt; tests. Handler dùng <code>http.ServeMux</code> Go 1.22 với <code>req.PathValue</code>. Map lỗi FK của <code>pgx</code> (<code>SQLSTATE 23503</code>) sang <code>400 product not tracked</code>. Kiểm chủ sở hữu là bước đầu tiên của mọi handler theo <code>{id}</code> - không có ngoại lệ.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-TRACK-001</strong> - <code>tracked_product</code> phải có SKU đã track trước khi thêm vào wishlist (FK <code>product_id</code>).</li><li><strong>TASK-INFRA-002</strong> - bảng <code>app_user</code> cho FK <code>user_id</code>.</li><li><strong>TASK-INFRA-001 (gateway)</strong> - gắn JWT auth và <code>user_id</code> vào context.</li><li><strong>TASK-TRACK-003 (sibling)</strong> - alert_rule type <code>price_below</code> đọc <code>target_price</code> khi user đặt giá mong muốn.</li><li><strong>TASK-WEB-004 (downstream)</strong> - UI quản lý wishlist tiêu thụ các route này.</li><li>Lib: <code>pgx</code>, <code>encoding/json</code>, <code>net/http</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Tạo wishlist + thêm item</h3>\n<pre><code>curl -s -X POST -H \"Authorization: Bearer $JWT\" -H \"Content-Type: application/json\" \\\n  -d '{\"name\":\"Chờ sale 12.12\"}' \"https://api.sandeal.vn/v1/wishlists\"\n# -&gt; {\"id\": 42, \"name\": \"Chờ sale 12.12\"}\n\ncurl -s -X POST -H \"Authorization: Bearer $JWT\" -H \"Content-Type: application/json\" \\\n  -d '{\"product_id\": 90112, \"target_price\": 79000}' \\\n  \"https://api.sandeal.vn/v1/wishlists/42/items\"\n# -&gt; 200</code></pre>\n<h3>GET items (200)</h3>\n<pre><code class=\"language-json\">[\n  { \"product_id\": 90112, \"target_price\": 79000, \"added_at\": \"2026-06-28T03:10:00Z\" },\n  { \"product_id\": 90118, \"target_price\": null,  \"added_at\": \"2026-06-28T03:12:00Z\" }\n]</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Wishlist chia sẻ / công khai (share link cho người khác xem) - thêm khi có nhu cầu social; cần lại mô hình phân quyền.</li><li>Sắp xếp / ghim item trong wishlist (cột <code>position</code>) - tối ưu UI giai đoạn sau.</li><li>Giới hạn số wishlist/số item theo tier (free vs Premium) - gắn vào TASK-BILL khi có gating.</li><li>Tự tạo alert_rule price_below khi user đặt target_price - để TASK-TRACK-003 quyết định liên kết, tránh ghép cứng ở đây.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Truy cập wishlist user khác</td><td>OwnsWishlist false</td><td>404</td><td>Kiểm chủ sở hữu mọi route {id} (DEC-TRACK-12)</td></tr><tr><td>403 vô tình lộ tồn tại</td><td>code review</td><td>rò rỉ id</td><td>Luôn trả 404 cho id không thuộc caller</td></tr><tr><td>Thêm product_id rác</td><td>FK 23503</td><td>400 product not tracked</td><td>Buộc SKU đã qua TASK-TRACK-001</td></tr><tr><td>Thêm trùng SKU</td><td>ON CONFLICT</td><td>200 no-op / cập nhật target</td><td>Idempotent (DEC-TRACK-13)</td></tr><tr><td>target_price float</td><td>CHECK + kiểu int64</td><td>từ chối / sai kiểu</td><td>BIGINT VND, JSON int64</td></tr><tr><td>Xóa wishlist còn item</td><td>ON DELETE CASCADE</td><td>item tự xóa</td><td>CASCADE ở FK</td></tr><tr><td>id wishlist không phải số</td><td>ParseInt lỗi</td><td>400</td><td>Frontend gửi id số</td></tr><tr><td>Race thêm item cùng SKU</td><td>ON CONFLICT</td><td>một dòng</td><td>Theo thiết kế (idempotent)</td></tr><tr><td>user_id giả từ body</td><td>lấy từ JWT</td><td>bỏ qua</td><td>Không nhận user_id từ client</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li><code>wishlist</code> cho người dùng tổ chức ý định mua sắm; <code>target_price</code> là cầu nối sang luồng cảnh báo giá (TASK-TRACK-003).</li><li>Kiểm chủ sở hữu trả 404 (không 403) là rào chống IDOR + chống liệt kê id tuần tự - một chi tiết bảo mật dễ bỏ sót với khóa BIGSERIAL.</li><li>Thêm item idempotent (UNIQUE + ON CONFLICT DO UPDATE) làm \"bấm lại\" chỉ cập nhật giá mong muốn, không nhân đôi.</li><li>FK <code>product_id -&gt; tracked_product</code> giữ wishlist luôn gắn SKU thật đang được theo dõi giá, không có dòng trỏ hư không.</li><li><code>target_price</code> là int64 VND suốt đường DB tới JSON, không có bước float nào để tránh sai số khi engine alert so giá.</li><li>Khi mở SEA, <code>target_price</code> vẫn int64 theo minor unit từng nước (gắn currency vào tracked_product); schema không đổi.</li></ul>\n<hr>\n<p><em>Hết TASK-TRACK-002. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
