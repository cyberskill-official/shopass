window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-EXT-005"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Lớp đồng bộ <strong>MUST</strong> đính JWT của SănDeal (KHÔNG token sàn) cho mọi request lên backend, gửi chỉ <code>OutboundPayload</code> đã sạch qua một hàng đợi bền, và mở WSS chỉ khi cần realtime rồi đóng. Hợp đồng:</p>\n<ol><li><code>auth-bridge.ts</code> <strong>MUST</strong> đính <code>Authorization: Bearer &lt;jwt&gt;</code> với JWT do TASK-AUTH-002 cấp cho mọi request backend (DEC-EXT-23). <strong>MUST NOT</strong> đính, dưới bất kỳ tên header nào, token/cookie phiên của sàn (Shopee/TikTok/Lazada).</li><li>Chỉ <code>OutboundPayload</code> đã qua pipeline tối thiểu hóa (TASK-EXT-003) <strong>MUST</strong> được đưa vào hàng đợi gửi (DEC-EXT-24). <strong>MUST NOT</strong> tồn tại đường gửi <code>CartReadMessage</code> thô bỏ qua <code>minimize()</code>.</li><li><code>queue.ts</code> <strong>MUST</strong> lưu hàng đợi bền trong <code>chrome.storage</code> (DEC-EXT-25) để sống qua chu kỳ SW kill (NFR-EXT-001). Hàng đợi <strong>MUST NOT</strong> nằm trong biến module-global (mất khi SW ngủ).</li><li><code>sender.ts</code> <strong>MUST</strong> gửi qua HTTPS với retry + backoff; chỉ <code>ack</code> (xóa khỏi hàng đợi) khi backend trả 2xx. Lỗi mạng/5xx <strong>MUST</strong> giữ item trong hàng đợi để thử lại - không mất dữ liệu giỏ đã đọc. 4b. Khi backend trả 401 (JWT hết hạn), <code>sender.ts</code> <strong>MUST</strong> gọi refresh (DEC-EXT-27) rồi thử lại; không vứt item.</li><li>Khi thiếu JWT (chưa đăng nhập SănDeal / refresh thất bại), gửi <strong>MUST</strong> fail-closed: giữ item trong hàng đợi, KHÔNG gửi dữ liệu ẩn danh không gắn danh tính (DEC-EXT-25).</li><li>WSS (<code>ws-client.ts</code>) <strong>MUST</strong> chỉ mở khi cần realtime (ví dụ nhận alert giá tức thời) và đóng khi xong (DEC-EXT-26). <strong>MUST NOT</strong> mở WSS thường trực chỉ để giữ SW sống - đó là lạm dụng tài nguyên (§3.2 \"không lạm dụng\").</li><li>JWT SănDeal <strong>MUST</strong> lưu ở <code>chrome.storage.session</code> (RAM-only, mất khi trình duyệt đóng) chứ không bền quá mức ở <code>storage.local</code> (DEC-EXT-27); refresh token theo cơ chế TASK-AUTH-002.</li><li>Mọi giao tiếp backend <strong>MUST</strong> qua HTTPS/WSS (TLS); <strong>MUST NOT</strong> gửi cleartext (DEC-EXT-28, no-cleartext §3.8).</li><li>Lớp đồng bộ <strong>MUST</strong> giữ vai \"đầu đọc nhẹ\": không chạy xử lý nặng/dài (DEC-EXT-05); flush hàng đợi theo nhịp alarm (TASK-EXT-001) hoặc khi có sự kiện, không vòng lặp dài làm SW vượt 5 phút.</li><li>Request gửi lên backend <strong>MUST</strong> gói trong <code>SyncEnvelope</code> typed (<code>payload: OutboundPayload</code>, không kèm bất kỳ trường credential nào); body <strong>MUST</strong> chỉ chứa dữ liệu đã sạch + JWT ở header (không nhúng token trong body).</li><li>Lớp đồng bộ <strong>MUST</strong> ghi metric: số payload gửi thành công, số retry, số fail-closed do thiếu JWT, số lần mở/đóng WSS - làm bằng chứng cho audit (TASK-TRUST-003) và giám sát.</li><li>Khi người dùng đăng xuất SănDeal <strong>MUST</strong> xóa JWT khỏi <code>storage.session</code> và đóng WSS; hàng đợi pending xử lý theo chính sách (giữ chờ đăng nhập lại hoặc xóa theo lựa chọn người dùng), KHÔNG gửi sau khi đã đăng xuất.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao JWT SănDeal, không token sàn (DEC-EXT-23)?</strong> Đây là ranh giới niềm tin cứng nhất của extension. Backend SănDeal chỉ cần biết \"request này thuộc người dùng SănDeal nào\" - đó là việc của JWT SănDeal (TASK-AUTH-002). Token/cookie sàn KHÔNG BAO GIỜ được rời client (cam kết §5.4); chúng ở lại tab để content script mượn phiên qua trình duyệt (TASK-EXT-002), không bao giờ đi lên server. Lẫn hai loại token là lỗi biến extension thành kênh chiếm tài khoản sàn - đúng thảm họa cần né.</p>\n<p><strong>Vì sao hàng đợi bền trong chrome.storage (DEC-EXT-25)?</strong> SW bị kill bất cứ lúc nào (NFR-EXT-001). Nếu hàng đợi nằm trong biến global, một lần SW ngủ giữa lúc gửi là mất dữ liệu giỏ đã đọc. Hàng đợi bền trong storage sống qua chu kỳ kill: SW thức dậy, đọc lại hàng đợi, gửi tiếp. Đây là hệ quả trực tiếp của quy ước \"state luôn ở storage\" từ TASK-EXT-001.</p>\n<p><strong>Vì sao fail-closed khi thiếu JWT (DEC-EXT-25)?</strong> Thà giữ item chờ còn hơn gửi dữ liệu mà không gắn đúng danh tính/đường bảo mật. Gửi ẩn danh \"cho xong\" là rò rỉ: dữ liệu rời máy mà không có chủ thể rõ ràng, vừa sai về quyền riêng tư vừa khó truy vết. Fail-closed giữ tư thế đúng - không gửi khi chưa chắc.</p>\n<p><strong>Vì sao WSS mở theo nhu cầu, không thường trực (DEC-EXT-26)?</strong> WebSocket có thể giữ SW sống (đúng như §3.2 ghi), nhưng tài liệu cũng cảnh báo \"không lạm dụng\". Mở WSS thường trực để né cơ chế kill là chống lại tinh thần MV3 và đốt tài nguyên người dùng. Đúng cách: mở khi thực sự cần realtime (nhận alert tức thời), đóng khi xong. Polling nhẹ qua alarm lo phần còn lại.</p>\n<p><strong>Vì sao JWT ở storage.session, không storage.local (DEC-EXT-27)?</strong> Token nhạy cảm không nên bền lâu hơn cần. <code>storage.session</code> là RAM-only, mất khi trình duyệt đóng - giảm bề mặt rò token nếu máy bị truy cập. Refresh token (theo TASK-AUTH-002) khôi phục phiên khi cần, nên không cần giữ JWT bền quá mức.</p>\n<p><strong>Vì sao token ở header, dữ liệu sạch ở body (§1 #10)?</strong> Tách bạch danh tính (header Authorization) khỏi nội dung (body chỉ OutboundPayload đã sạch) giữ ranh giới rõ: body không bao giờ chứa credential. Audit kiểm body thấy chỉ dữ liệu tối thiểu, kiểm header thấy chỉ JWT SănDeal - không lẫn lộn.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>types.ts (envelope + auth, typed)</h3>\n<pre><code class=\"language-ts\">// extension/src/shared/types.ts (thêm)\nimport type { OutboundPayload } from \"../pipeline/schema\";\n\nexport interface SyncEnvelope {\n  payload: OutboundPayload;      // CHỈ dữ liệu đã sạch; KHÔNG credential trong body\n  clientTs: number;             // epoch ms (đo độ trễ; không phải PII)\n}\n\nexport interface AuthState {\n  jwt?: string;                 // JWT SănDeal (storage.session, RAM-only)\n  // KHÔNG có trường token/cookie sàn\n}</code></pre>\n<h3>auth-bridge.ts (đính JWT SănDeal, KHÔNG token sàn)</h3>\n<pre><code class=\"language-ts\">// extension/src/sync/auth-bridge.ts\nexport async function authedFetch(url: string, env: SyncEnvelope): Promise&lt;Response&gt; {\n  const jwt = await getJwt();                 // TASK-AUTH-002; storage.session\n  if (!jwt) throw new NoAuthError();          // fail-closed (DEC-EXT-25)\n  return fetch(url, {\n    method: \"POST\",\n    headers: {\n      \"Authorization\": `Bearer ${jwt}`,       // JWT SănDeal, KHÔNG token sàn\n      \"Content-Type\": \"application/json\"\n    },\n    body: JSON.stringify(env)                  // body chỉ OutboundPayload đã sạch\n  });\n  // LƯU Ý: KHÔNG đọc document.cookie, KHÔNG đính header token sàn ở đây.\n}</code></pre>\n<h3>sender.ts (retry/backoff, ack 2xx, 401 -&gt; refresh, fail-closed)</h3>\n<pre><code class=\"language-ts\">// extension/src/sync/sender.ts\nexport async function flushQueue(): Promise&lt;void&gt; {\n  for (const item of await queue.peekAll()) {\n    try {\n      const res = await authedFetch(SYNC_URL, item.env);\n      if (res.status === 401) { await refreshJwt(); continue; } // thử lại sau refresh\n      if (res.ok) { await queue.ack(item.id); metrics.sent(); }  // chỉ ack khi 2xx\n      else metrics.retry();                                      // 5xx → giữ lại\n    } catch (e) {\n      if (e instanceof NoAuthError) { metrics.failClosed(); return; } // thiếu JWT → dừng, giữ hàng đợi\n      metrics.retry();                                          // lỗi mạng → giữ lại, backoff\n    }\n  }\n}</code></pre>\n<h3>ws-client.ts (mở theo nhu cầu, đóng khi xong)</h3>\n<pre><code class=\"language-ts\">// extension/src/sync/ws-client.ts\nlet ws: WebSocket | null = null;\n\nexport function openRealtime(): void {\n  if (ws) return;                              // không mở trùng\n  ws = new WebSocket(WSS_URL);                 // CHỈ khi cần realtime\n  ws.onclose = () =&gt; { ws = null; };\n}\nexport function closeRealtime(): void {\n  ws?.close();                                 // đóng khi xong; KHÔNG mở thường trực\n  ws = null;\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Mọi request backend đính <code>Authorization: Bearer &lt;jwt SănDeal&gt;</code>; grep <code>sync/**</code>: KHÔNG đọc <code>document.cookie</code>, KHÔNG header token/cookie sàn.</li><li>Body request (<code>SyncEnvelope</code>) chỉ chứa <code>OutboundPayload</code> + <code>clientTs</code>; KHÔNG khóa cookie/token/session/auth trong body (test introspection).</li><li>Không tồn tại đường gửi <code>CartReadMessage</code> thô bỏ qua <code>minimize()</code> -&gt; queue (grep: mọi enqueue là OutboundPayload).</li><li>Hàng đợi bền: enqueue, mô phỏng SW kill (reset module), đọc lại hàng đợi từ <code>chrome.storage</code> còn nguyên item.</li><li>Gửi 2xx -&gt; item bị <code>ack</code> (rời hàng đợi); gửi 5xx/lỗi mạng -&gt; item ở lại để retry.</li><li>Backend 401 -&gt; gọi <code>refreshJwt</code> rồi thử lại; item không bị vứt.</li><li>Thiếu JWT -&gt; fail-closed: <code>flushQueue</code> dừng, metric <code>failClosed</code> tăng, item ở lại; KHÔNG gửi ẩn danh.</li><li>WSS chỉ mở khi gọi <code>openRealtime</code>; <code>closeRealtime</code> đóng; không có chỗ mở WSS top-level thường trực (grep).</li><li>JWT lưu ở <code>chrome.storage.session</code> (RAM-only), không <code>storage.local</code> (test/grep).</li><li>Đăng xuất -&gt; JWT bị xóa khỏi session + WSS đóng; không gửi sau đăng xuất.</li><li><code>npm test</code> xanh; <code>tsc --noEmit</code> sạch.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-ts\">// extension/test/sync-no-platform-token.test.ts\nimport { authedFetch } from \"../src/sync/auth-bridge\";\n\ntest(\"request đính JWT SănDeal, KHÔNG token/cookie sàn\", async () =&gt; {\n  setJwt(\"eyJ.sandeal.jwt\");\n  const captured = captureFetch();\n  await authedFetch(SYNC_URL, { payload: { platform:\"shopee\", items:[], vouchers:[] }, clientTs: 1 });\n  const h = captured.headers;\n  expect(h[\"Authorization\"]).toBe(\"Bearer eyJ.sandeal.jwt\");\n  const flat = JSON.stringify(captured).toLowerCase();\n  expect(flat).not.toMatch(/cookie|spc_|set-cookie|x-bogus|mstoken/); // không token sàn\n});\n\ntest(\"mã nguồn sync KHÔNG đọc document.cookie / header token sàn\", async () =&gt; {\n  for (const f of [\"auth-bridge\", \"sender\", \"ws-client\", \"queue\"]) {\n    const src = await readFile(`src/sync/${f}.ts`, \"utf8\");\n    expect(src).not.toMatch(/document\\.cookie/);\n  }\n});</code></pre>\n<pre><code class=\"language-ts\">// extension/test/queue-persist.test.ts\ntest(\"hàng đợi sống qua SW kill (đọc lại từ storage)\", async () =&gt; {\n  globalThis.chrome = fakeChromeStorage();\n  await queue.enqueue({ payload: { platform:\"shopee\", items:[{productId:\"1\",price:1,qty:1}], vouchers:[] }, clientTs: 1 });\n  jest.resetModules();                                  // mô phỏng SW kill\n  const { queue: q2 } = await import(\"../src/sync/queue\");\n  expect((await q2.peekAll()).length).toBe(1);          // KHÔNG mất\n});\n\ntest(\"thiếu JWT → fail-closed, item ở lại\", async () =&gt; {\n  setJwt(undefined);\n  await queue.enqueue(anyEnvelope());\n  await flushQueue();\n  expect((await queue.peekAll()).length).toBe(1);       // không gửi ẩn danh\n});</code></pre>\n<pre><code class=\"language-ts\">// extension/test/ws-lifecycle.test.ts\ntest(\"WSS mở theo nhu cầu rồi đóng, không mở thường trực\", async () =&gt; {\n  openRealtime();\n  expect(isWsOpen()).toBe(true);\n  closeRealtime();\n  expect(isWsOpen()).toBe(false);\n  const swSrc = await readFile(\"src/background/service-worker.ts\", \"utf8\");\n  expect(swSrc).not.toMatch(/new WebSocket\\(/);          // không mở WSS top-level thường trực\n});</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>auth-bridge.ts</code> (lấy/refresh JWT SănDeal, đính Bearer, fail-closed thiếu JWT) -&gt; <code>queue.ts</code> (hàng đợi bền trong chrome.storage) -&gt; <code>sender.ts</code> (flush có retry/backoff, ack 2xx, 401-&gt;refresh) -&gt; <code>ws-client.ts</code> (mở/đóng theo nhu cầu) -&gt; nối <code>service-worker.ts</code> (pipeline-done -&gt; enqueue; alarm tick -&gt; flush; mở WSS khi cần realtime) -&gt; tests. JWT ở storage.session; token sàn KHÔNG BAO GIỜ rời client (vẫn ở tab cho TASK-EXT-002 mượn phiên). Body chỉ OutboundPayload đã sạch (TASK-EXT-003), danh tính ở header.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-EXT-003</strong> - cung cấp <code>OutboundPayload</code> đã sạch; lớp đồng bộ chỉ gửi cái này, không gửi thô.</li><li><strong>TASK-AUTH-002</strong> - cấp JWT + refresh token SănDeal mà auth bridge đính vào request.</li><li><strong>TASK-EXT-001</strong> - service worker + chrome.storage + alarm làm khung hàng đợi + flush; messaging nối pipeline.</li><li><strong>TASK-AUTH-003</strong> - liên kết platform_account ẩn danh (ext_user_ref), KHÔNG token sàn - đồng bộ với ranh giới ở đây.</li><li><strong>TASK-TRUST-002 (downstream)</strong> - chính sách local-first; lớp đồng bộ là điểm dữ liệu sạch rời máy.</li><li><strong>NFR-EXT-001</strong> - hàng đợi bền + WSS theo nhu cầu để không phá ràng buộc SW ephemeral.</li><li><strong>TASK-CART-002 / TASK-AFFIL-004 (downstream, P2)</strong> - tiêu thụ dữ liệu giỏ đồng bộ về backend.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Request đồng bộ lên backend (header JWT SănDeal, body sạch)</h3>\n<pre><code class=\"language-http\">POST /v1/ext/sync HTTP/1.1\nHost: api.sandeal.vn\nAuthorization: Bearer eyJ.&lt;jwt SănDeal&gt;...\nContent-Type: application/json\n\n{\n  \"payload\": {\n    \"platform\": \"shopee\",\n    \"items\": [ { \"productId\": \"90112\", \"price\": 89000, \"qty\": 1 } ],\n    \"vouchers\": [ { \"code\": \"FREESHIPXTRA\", \"minSpend\": 0, \"discountText\": \"đến 15k\" } ]\n  },\n  \"clientTs\": 1782000030000\n}</code></pre>\n<h3>Item hàng đợi bền trong chrome.storage (ví dụ)</h3>\n<pre><code class=\"language-json\">{\n  \"sandeal:queue\": [\n    { \"id\": \"q1\", \"env\": { \"payload\": { \"platform\": \"shopee\", \"items\": [], \"vouchers\": [] }, \"clientTs\": 1782000030000 } }\n  ]\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Gộp nhiều OutboundPayload thành batch trước khi gửi (giảm số request) - tối ưu ở slice sau khi đo lưu lượng.</li><li>Kênh realtime cụ thể (WSS topic cho alert giá) - định nghĩa chi tiết khi TASK-TRACK-004 (engine alert) tới.</li><li>Chính sách hàng đợi pending lúc đăng xuất (giữ chờ vs xóa) - chốt cùng TASK-COMPLY-003 (DSAR/xóa dữ liệu).</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Đính token/cookie sàn lên backend</td><td>sync-no-platform-token test</td><td>rò credential sàn -&gt; chiếm tài khoản, PDPL</td><td>Chỉ JWT SănDeal ở header (DEC-EXT-23)</td></tr><tr><td>Gửi CartReadMessage thô</td><td>grep enqueue</td><td>bỏ qua tối thiểu hóa</td><td>Chỉ OutboundPayload vào queue (DEC-EXT-24)</td></tr><tr><td>Hàng đợi trong biến global</td><td>queue-persist test</td><td>mất dữ liệu khi SW kill</td><td>Hàng đợi bền chrome.storage (DEC-EXT-25)</td></tr><tr><td>Mất dữ liệu khi gửi lỗi</td><td>ack-only-2xx test</td><td>dữ liệu giỏ biến mất</td><td>Retry/backoff, ack chỉ khi 2xx (§1 #4)</td></tr><tr><td>Gửi ẩn danh khi thiếu JWT</td><td>fail-closed test</td><td>dữ liệu không gắn danh tính</td><td>Fail-closed, giữ hàng đợi (DEC-EXT-25)</td></tr><tr><td>WSS mở thường trực</td><td>ws-lifecycle test</td><td>lạm dụng tài nguyên, trái ephemeral</td><td>Mở theo nhu cầu, đóng khi xong (DEC-EXT-26)</td></tr><tr><td>JWT bền quá mức (storage.local)</td><td>grep/test</td><td>bề mặt rò token</td><td>Lưu storage.session RAM-only (DEC-EXT-27)</td></tr><tr><td>Gửi cleartext</td><td>review/test scheme</td><td>nghe lén dữ liệu</td><td>HTTPS/WSS bắt buộc (DEC-EXT-28)</td></tr><tr><td>401 vứt item</td><td>sender 401 test</td><td>mất dữ liệu khi token hết hạn</td><td>Refresh rồi thử lại (§1 #4b)</td></tr><tr><td>Gửi sau đăng xuất</td><td>logout test</td><td>dữ liệu rời máy sau khi rút consent</td><td>Xóa JWT + đóng WSS + chặn gửi (§1 #12)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Ranh giới cứng nhất: request lên backend đính JWT SănDeal, token/cookie sàn KHÔNG BAO GIỜ rời client (ở lại tab cho TASK-EXT-002 mượn phiên qua trình duyệt).</li><li>Hàng đợi bền trong chrome.storage là hệ quả trực tiếp của SW ephemeral (NFR-EXT-001) - dữ liệu giỏ đọc được không mất khi SW ngủ giữa lúc gửi.</li><li>Fail-closed khi thiếu JWT: thà giữ hàng đợi chờ còn hơn gửi dữ liệu ẩn danh không gắn danh tính/đường bảo mật.</li><li>WSS mở theo nhu cầu rồi đóng - tài liệu nguồn cho phép WSS giữ SW sống nhưng \"không lạm dụng\" (§3.2); mở thường trực là chống tinh thần MV3.</li><li>Body chỉ OutboundPayload đã sạch, danh tính ở header Authorization - tách bạch để audit (TASK-TRUST-003) kiểm body thấy không credential.</li></ul>\n<hr>\n<p><em>Hết TASK-EXT-005. Status: done (mục tiêu audit 10/10).</em></p>";
