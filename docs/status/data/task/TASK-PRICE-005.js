window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PRICE-005"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service PRICE <strong>MUST</strong> tính <code>canonical_key</code> cho mỗi <code>tracked_product</code> để gộp các listing cùng một sản phẩm vật lý trên Shopee/TikTok/Lazada thành một nhóm so sánh giá, ghi key ngược vào cột <code>tracked_product.canonical_key</code> (cột + index <code>idx_tp_canonical</code> do TASK-PRICE-001 định nghĩa). Pipeline gồm chuẩn hóa title, dựng key xác định, fuzzy match và hàng đợi duyệt tay. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> chuẩn hóa title đa tầng qua <code>Normalize(title string) string</code> (DEC-PRICE-20): fold dấu tiếng Việt (đ-&gt;d, ă-&gt;a, ê-&gt;e...), lowercase, bỏ emoji và ký tự điều khiển, bỏ nhiễu marketing (<code>[chính hãng]</code>, <code>freeship</code>, <code>giảm sốc</code>, <code>mã giảm</code>, <code>hot</code>, <code>sale</code>), bỏ boilerplate seller (tên shop, hashtag, dấu phân tách <code>|</code>, <code>-</code>, <code>chính hãng bảo hành</code>), gom nhiều khoảng trắng thành một.</li><li><strong>MUST</strong> từ title đã chuẩn hóa tách ra <code>brand</code>, <code>model</code> và các thuộc tính nổi bật (<code>capacity</code>, <code>color</code>, <code>size</code>) qua bộ token và từ điển brand; thuộc tính dạng <code>128gb</code>, <code>256 gb</code>, <code>xanh</code>, <code>size l</code> được chuẩn hóa về dạng canonical (<code>128gb</code>, <code>256gb</code>, <code>blue</code>, <code>l</code>).</li><li><strong>MUST</strong> dựng <code>canonical_key</code> xác định qua <code>CanonicalKey(brand, model string, attrs map[string]string) string</code> (DEC-PRICE-21): ghép <code>brand</code> + <code>model</code> chuẩn hóa + hash (SHA-256, cắt 12 hex) của tập thuộc tính nổi bật đã sắp xếp. Cùng input <strong>MUST</strong> cho cùng output (không phụ thuộc thứ tự map, không random).</li><li><strong>MUST</strong> dùng key xác định ở #3 cho bước nhóm chính xác (exact-ish) TRƯỚC, rồi mới fuzzy: hai listing cùng <code>canonical_key</code> thô là cùng nhóm, không cần fuzzy.</li><li><strong>MUST</strong> fuzzy match các listing chưa khớp chính xác qua <code>Match(candidate Candidate) (MatchResult, error)</code> (DEC-PRICE-22): truy vấn <code>pg_trgm</code> similarity trên <code>tracked_product.title</code> đã chuẩn hóa kết hợp token-set ratio, lấy các ứng viên có <code>similarity &gt;= sim_threshold</code> (mặc định 0,55).</li><li><strong>MUST</strong> tính <code>confidence</code> (0..1) cho mỗi cặp ghép từ similarity trigram, độ trùng token brand/model và khớp thuộc tính; chỉ gộp tự động khi <code>confidence &gt;= merge_threshold</code> (mặc định 0,82).</li><li><strong>MUST</strong> đẩy mọi cặp có <code>low_threshold &lt;= confidence &lt; merge_threshold</code> (mặc định <code>low_threshold</code> = 0,60) vào <code>canonical_review_queue</code> để người duyệt quyết định; <strong>MUST NEVER</strong> auto-merge khi <code>confidence &lt; merge_threshold</code> (DEC-PRICE-23). Merge sai làm <code>GET /v1/compare</code> hiển thị giá của sản phẩm khác.</li><li><strong>MUST</strong> bỏ qua (không gộp, không enqueue) các cặp có <code>confidence &lt; low_threshold</code> - coi là sản phẩm khác nhau.</li><li><strong>MUST</strong> ghi <code>canonical_key</code> ngược qua <code>product_repo.SetCanonicalKey(ctx, productID int64, key string) error</code>, idempotent: gọi lại với cùng key không đổi gì, gọi với key mới cập nhật đúng một dòng.</li><li><strong>MUST</strong> recompute idempotent khi title đổi (DEC-PRICE-24): scraper cập nhật <code>title</code> -&gt; chạy lại pipeline; cùng title chuẩn hóa cho cùng <code>canonical_key</code>, không sinh nhóm rác hay tách nhóm vô cớ.</li><li><strong>MUST</strong> bật extension <code>pg_trgm</code> (<code>CREATE EXTENSION IF NOT EXISTS pg_trgm</code>) và tạo <code>GIN</code> trigram index trên <code>tracked_product(title)</code> để truy vấn similarity không quét toàn bảng.</li><li><strong>SHOULD</strong> phát OTel metric: <code>canon_key_computed_total{platform_id}</code> (counter), <code>canon_merge_auto_total</code> (counter), <code>canon_review_enqueued_total</code> (counter), <code>canon_match_duration_ms</code> (histogram).</li><li><strong>MAY</strong> dùng helper Python tính embedding (cosine similarity) cho các cặp khó mà trigram không phân định được; lõi vẫn ở Go, embedding là tín hiệu phụ cộng vào <code>confidence</code>, không thay thế ngưỡng.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao chuẩn hóa title đa tầng (DEC-PRICE-20)?</strong> Title sàn TMĐT VN là nhiễu nặng: <code>[CHÍNH HÃNG] Tai nghe Sony WH-1000XM5 Freeship Giảm Sốc Mã FMCG50 - Shop ABC</code>. So khớp trên chuỗi thô là vô vọng - cùng một tai nghe, ba sàn viết ba kiểu marketing khác nhau. Fold dấu là bắt buộc riêng cho VN: <code>điện thoại</code> và <code>dien thoai</code> phải về cùng một dạng, nếu không mỗi cách gõ thành một nhóm. Bỏ nhiễu marketing và boilerplate seller để chỉ còn <code>brand + model + thuộc tính</code> - phần thực sự định danh sản phẩm.</p>\n<p><strong>Vì sao key xác định trước, fuzzy sau (DEC-PRICE-21)?</strong> Phần lớn listing của cùng sản phẩm, sau khi chuẩn hóa sạch, cho ra <code>brand + model</code> y hệt. Nhóm chính xác bằng key xác định bắt trọn các trường hợp dễ với chi phí gần bằng không (so chuỗi). Chỉ phần đuôi khó - title thiếu model, viết tắt, lệch thuộc tính - mới cần fuzzy đắt hơn. Làm fuzzy cho mọi cặp là lãng phí và làm tăng rủi ro gộp nhầm.</p>\n<p><strong>Vì sao hàng đợi manual-review (DEC-PRICE-23)?</strong> Đây là quyết định bảo vệ niềm tin lớn nhất của task này. Merge sai không phải lỗi vô hại: gộp nhầm <code>iPhone 15</code> với <code>iPhone 15 Pro</code> làm <code>GET /v1/compare</code> hiển thị giá Pro cho người đang xem bản thường - người dùng mất tiền, mất niềm tin, đúng vết xe Honey mà §5.6 cảnh báo. Vùng confidence \"lưng chừng\" (0,60-0,82) là vùng nguy hiểm nhất: đủ giống để cám dỗ gộp, đủ khác để có thể sai. Đẩy vùng này cho người duyệt, không bao giờ tự gộp, là cái giá rẻ để tránh hậu quả đắt.</p>\n<p><strong>Vì sao pg_trgm thay vì so bằng đơn thuần?</strong> So bằng (<code>title_a = title_b</code>) chỉ bắt được trùng tuyệt đối - gần như không xảy ra giữa ba sàn. <code>pg_trgm</code> đo độ tương đồng trigram trực tiếp trong Postgres, có GIN index nên truy vấn <code>WHERE title % $1</code> không quét toàn bảng. Giữ phép so độ tương đồng sát dữ liệu (trong DB) nhanh hơn kéo toàn bộ title ra Go rồi so từng cặp, và token-set ratio bù phần trigram yếu khi thứ tự từ đổi.</p>\n<p><strong>Vì sao recompute idempotent (DEC-PRICE-24)?</strong> Scraper cập nhật <code>title</code> thường xuyên (seller đổi tên, thêm khuyến mãi). Nếu mỗi lần title đổi sinh ra <code>canonical_key</code> khác, nhóm so sánh sẽ vỡ vụn liên tục, biểu đồ so sánh nhảy loạn. Hàm chuẩn hóa và <code>CanonicalKey</code> phải thuần (pure): cùng nội dung định danh cho cùng key, bất kể marketing bề mặt đổi thế nào.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/price/migrations/0005_canonical_review.sql\nCREATE EXTENSION IF NOT EXISTS pg_trgm;\n\n-- GIN trigram index để truy vấn similarity không quét toàn bảng\nCREATE INDEX IF NOT EXISTS idx_tp_title_trgm\n  ON tracked_product USING gin (title gin_trgm_ops);\n\n-- Hàng đợi duyệt tay cho merge confidence thấp (DEC-PRICE-23)\nCREATE TABLE canonical_review_queue (\n  id            BIGSERIAL PRIMARY KEY,\n  product_id    BIGINT      NOT NULL REFERENCES tracked_product(id),\n  candidate_key TEXT        NOT NULL,              -- canonical_key đề xuất gộp vào\n  confidence    REAL        NOT NULL CHECK (confidence &gt;= 0 AND confidence &lt;= 1),\n  status        TEXT        NOT NULL DEFAULT 'pending'\n                CHECK (status IN ('pending','approved','rejected')),\n  created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),\n  decided_at    TIMESTAMPTZ,\n  UNIQUE (product_id, candidate_key)\n);\n\nCREATE INDEX idx_crq_pending ON canonical_review_queue (status, created_at)\n  WHERE status = 'pending';</code></pre>\n<h3>Normalize (Go)</h3>\n<pre><code class=\"language-go\">// services/price/internal/canon/normalize.go\npackage canon\n\n// Normalize đưa title thô về dạng chuẩn để so khớp (DEC-PRICE-20).\n// Thuần: cùng input → cùng output, không phụ thuộc trạng thái ngoài.\nfunc Normalize(title string) string {\n    s := strings.ToLower(title)\n    s = foldDiacritics(s)            // đ→d, ă→a, ê→e... (riêng cho VN)\n    s = emojiRe.ReplaceAllString(s, \" \")\n    s = marketingNoiseRe.ReplaceAllString(s, \" \") // chinh hang, freeship, giam soc, ma giam, hot, sale\n    s = sellerBoilerplateRe.ReplaceAllString(s, \" \")\n    s = nonAlnumRe.ReplaceAllString(s, \" \")       // giữ chữ-số, bỏ ký hiệu phân tách\n    s = wsRe.ReplaceAllString(s, \" \")             // gom khoảng trắng\n    return strings.TrimSpace(s)\n}\n\n// Attrs tách brand + model + thuộc tính nổi bật từ title đã chuẩn hóa (§1 #2).\ntype Attrs struct {\n    Brand  string\n    Model  string\n    Salient map[string]string // capacity/color/size đã chuẩn hóa\n}\n\nfunc Extract(normalized string) Attrs { /* token + từ điển brand + regex thuộc tính */ }</code></pre>\n<h3>CanonicalKey (Go)</h3>\n<pre><code class=\"language-go\">// services/price/internal/canon/key.go\npackage canon\n\n// CanonicalKey dựng key xác định (DEC-PRICE-21): brand + model + hash thuộc tính.\n// Cùng input → cùng output (sắp xếp thuộc tính trước khi hash).\nfunc CanonicalKey(brand, model string, attrs map[string]string) string {\n    keys := make([]string, 0, len(attrs))\n    for k := range attrs {\n        keys = append(keys, k)\n    }\n    sort.Strings(keys) // xác định, không phụ thuộc thứ tự map\n    var b strings.Builder\n    for _, k := range keys {\n        b.WriteString(k)\n        b.WriteByte('=')\n        b.WriteString(attrs[k])\n        b.WriteByte(';')\n    }\n    sum := sha256.Sum256([]byte(b.String()))\n    return brand + \":\" + model + \":\" + hex.EncodeToString(sum[:])[:12]\n}</code></pre>\n<h3>Match (Go + pg_trgm)</h3>\n<pre><code class=\"language-go\">// services/price/internal/canon/match.go\npackage canon\n\ntype Candidate struct {\n    ProductID int64\n    Title     string\n    Attrs     Attrs\n}\n\ntype MatchResult struct {\n    CanonicalKey string\n    Confidence   float64\n    Action       string // \"merge\" | \"review\" | \"skip\"\n}\n\n// Match tìm nhóm cho candidate: key xác định trước, fuzzy pg_trgm sau (§1 #4-#8).\nfunc (m *Matcher) Match(ctx context.Context, c Candidate) (MatchResult, error) {\n    norm := Normalize(c.Title)\n    rows, err := m.pool.Query(ctx,\n        `SELECT id, canonical_key, similarity(title, $1) AS sim\n           FROM tracked_product\n          WHERE title % $1 AND canonical_key IS NOT NULL\n          ORDER BY sim DESC\n          LIMIT 10`, norm)\n    if err != nil {\n        return MatchResult{}, err\n    }\n    best := m.bestCandidate(rows, c) // gộp sim trigram + token-set + khớp attr\n    switch {\n    case best.Confidence &gt;= m.mergeThreshold: // 0,82\n        return MatchResult{best.Key, best.Confidence, \"merge\"}, nil\n    case best.Confidence &gt;= m.lowThreshold:    // 0,60\n        return MatchResult{best.Key, best.Confidence, \"review\"}, nil\n    default:\n        return MatchResult{CanonicalKey(c.Attrs.Brand, c.Attrs.Model, c.Attrs.Salient),\n            best.Confidence, \"skip\"}, nil // sản phẩm mới, key riêng\n    }\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>Normalize(\"[CHÍNH HÃNG] Điện Thoại iPhone 15 Freeship \")</code> trả <code>\"dien thoai iphone 15\"</code> (fold dấu, lowercase, bỏ marketing/emoji, gom khoảng trắng).</li><li><code>Extract</code> của title điện thoại trả <code>Brand=\"apple\"</code> (qua từ điển), <code>Model=\"iphone 15\"</code>, <code>Salient</code> chứa color/capacity nếu có.</li><li><code>CanonicalKey(\"apple\",\"iphone 15\", {\"capacity\":\"128gb\"})</code> gọi 2 lần (map xáo thứ tự) trả CÙNG một chuỗi.</li><li>Key xác định nhóm trước: hai listing chuẩn hóa ra cùng <code>brand+model+attrs</code> cùng <code>canonical_key</code>, không qua fuzzy.</li><li><code>Match</code> của listing Lazada cùng sản phẩm với một listing Shopee đã có key -&gt; <code>Action=\"merge\"</code>, trả đúng <code>canonical_key</code> của nhóm.</li><li><code>Match</code> của sản phẩm khác hẳn (iPhone 15 vs Galaxy S24) -&gt; <code>Action=\"skip\"</code>, không gộp.</li><li><code>Match</code> cặp confidence trong [0,60; 0,82) -&gt; <code>Action=\"review\"</code>, thêm 1 dòng <code>canonical_review_queue</code> status <code>pending</code>.</li><li><code>Match</code> cặp confidence &lt; 0,60 -&gt; <code>Action=\"skip\"</code>, KHÔNG enqueue, KHÔNG gộp.</li><li><code>SetCanonicalKey(pid, key)</code> gọi lại cùng key -&gt; không đổi; gọi với key mới -&gt; cập nhật đúng 1 dòng (idempotent).</li><li>Title đổi rồi recompute -&gt; cùng nội dung định danh cho cùng <code>canonical_key</code> (không sinh nhóm rác).</li><li>Migration chạy sạch -&gt; <code>pg_trgm</code> bật, <code>idx_tp_title_trgm</code> GIN tồn tại, <code>canonical_review_queue</code> tồn tại với CHECK <code>confidence</code> và CHECK <code>status</code>.</li><li>Truy vấn <code>WHERE title % $1</code> dùng GIN trgm index (EXPLAIN không Seq Scan trên bảng lớn).</li><li>Metric <code>canon_merge_auto_total</code> tăng khi <code>merge</code>; <code>canon_review_enqueued_total</code> tăng khi <code>review</code>.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/price/internal/canon/normalize_test.go\nfunc TestNormalize_FoldsDiacritics(t *testing.T) {\n    got := Normalize(\"Điện Thoại Thông Minh\")\n    require.Equal(t, \"dien thoai thong minh\", got)\n}\n\nfunc TestNormalize_StripsMarketingNoise(t *testing.T) {\n    // \\U0001F525 là một emoji thật (lửa) nhúng vào chuỗi để chứng minh Normalize bóc emoji;\n    // nguồn giữ ASCII thuần, runtime vẫn nhận đúng rune emoji.\n    got := Normalize(\"[CHÍNH HÃNG] Tai nghe Sony WH-1000XM5 Freeship Giảm Sốc \\U0001F525 - Shop ABC\")\n    require.Equal(t, \"tai nghe sony wh 1000xm5\", got)\n}\n\nfunc TestCanonicalKey_Deterministic(t *testing.T) {\n    a := CanonicalKey(\"apple\", \"iphone 15\", map[string]string{\"capacity\": \"128gb\", \"color\": \"blue\"})\n    b := CanonicalKey(\"apple\", \"iphone 15\", map[string]string{\"color\": \"blue\", \"capacity\": \"128gb\"})\n    require.Equal(t, a, b) // thứ tự map không đổi key\n}</code></pre>\n<pre><code class=\"language-go\">// services/price/internal/canon/match_test.go\nfunc TestMatch_SameProductDifferentPlatform_Merges(t *testing.T) {\n    m, key := setupWithProduct(t, \"shopee\", \"Tai nghe Sony WH-1000XM5 Chính Hãng\")\n    res, _ := m.Match(ctx, Candidate{\n        ProductID: 0,\n        Title:     \"[LAZMALL] Sony WH 1000XM5 Freeship - Headphone\",\n    })\n    require.Equal(t, \"merge\", res.Action)\n    require.Equal(t, key, res.CanonicalKey)\n}\n\nfunc TestMatch_DifferentProduct_DoesNotMerge(t *testing.T) {\n    m, _ := setupWithProduct(t, \"shopee\", \"iPhone 15 128GB\")\n    res, _ := m.Match(ctx, Candidate{Title: \"Samsung Galaxy S24 Ultra 256GB\"})\n    require.Equal(t, \"skip\", res.Action)\n}\n\nfunc TestMatch_LowConfidence_GoesToReviewQueue(t *testing.T) {\n    m, _ := setupWithProduct(t, \"shopee\", \"iPhone 15 128GB\")\n    res, _ := m.Match(ctx, Candidate{Title: \"iPhone 15 Pro 128GB\"}) // gần nhưng khác bản\n    require.Equal(t, \"review\", res.Action)\n    require.Equal(t, 1, countPending(t, m)) // 1 dòng review, KHÔNG auto-merge\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration 0005 (pg_trgm + GIN index + review queue) -&gt; <code>normalize.go</code> (chuẩn hóa + Extract) -&gt; <code>key.go</code> (CanonicalKey xác định) -&gt; <code>match.go</code> (pg_trgm + confidence + quyết định) -&gt; <code>review_queue.go</code> (enqueue + duyệt) -&gt; <code>product_repo.go::SetCanonicalKey</code> -&gt; tests. Từ điển brand và regex nhiễu marketing để ở file dữ liệu cùng package, mở rộng theo dữ liệu thực mà không sửa logic. Embedding helper Python (nếu bật) chạy ngoài tiến trình, trả điểm cosine qua một endpoint nội bộ, cộng vào <code>confidence</code> như tín hiệu phụ.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-PRICE-001</strong> - <code>tracked_product(title, canonical_key)</code> và <code>idx_tp_canonical</code> phải tồn tại trước (ghi key ngược).</li><li><strong>TASK-PRICE-004 (downstream)</strong> - <code>GET /v1/compare?canonical_key=...</code> JOIN theo <code>canonical_key</code> mà task này sinh ra.</li><li><strong>TASK-SCRAPE-002 (tùy chọn)</strong> - <code>recommend</code> của Shopee cấp SKU liên quan, làm giàu ứng viên matching.</li><li><strong>TASK-PRICE-002 (liên quan)</strong> - sau khi gộp, mỗi <code>canonical_key</code> đọc <code>price_snapshot</code> của các <code>product_id</code> thành viên để dựng dòng so sánh.</li><li>Extension/lib: PostgreSQL <code>pg_trgm</code> (similarity, GIN index); driver <code>pgx</code>; tùy chọn helper embedding Python.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Hai title cùng sản phẩm, chuẩn hóa về cùng canonical_key</h3>\n<pre><code class=\"language-text\">Shopee : \"[CHÍNH HÃNG] Tai nghe Sony WH-1000XM5 Freeship (emoji) Mã FMCG50 - Shop ABC\"\nLazada : \"Sony WH 1000XM5 Headphone Chống Ồn | Bảo Hành 12 Tháng [LAZMALL]\"\n\nNormalize → \"tai nghe sony wh 1000xm5\"   (cả hai)\nExtract   → brand=\"sony\", model=\"wh 1000xm5\", salient={}\nCanonicalKey(\"sony\",\"wh 1000xm5\",{}) → \"sony:wh 1000xm5:9f2a3c1d0b7e\"  (cả hai)</code></pre>\n<h3>JOIN nuôi GET /v1/compare</h3>\n<pre><code class=\"language-sql\">-- TASK-PRICE-004 dùng canonical_key để gom giá 3 sàn\nSELECT p.code AS platform, tp.title, d.close_p AS price\nFROM tracked_product tp\nJOIN platform p          ON p.id = tp.platform_id\nJOIN LATERAL (\n  SELECT close_p FROM price_daily\n  WHERE product_id = tp.id ORDER BY day DESC LIMIT 1\n) d ON true\nWHERE tp.canonical_key = 'sony:wh 1000xm5:9f2a3c1d0b7e';\n-- → 3 dòng (shopee / tiktok / lazada) cho cùng một sản phẩm vật lý</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Embedding cosine cho cặp khó (helper Python) - bật khi tỷ lệ review queue cao hơn ngưỡng vận hành.</li><li>Học ngưỡng <code>merge_threshold</code>/<code>low_threshold</code> từ nhãn duyệt tay tích lũy - tinh chỉnh giai đoạn sau, ban đầu hằng số.</li><li>Gộp theo biến thể (variant) trong cùng listing (màu/dung lượng) - slice sau khi mô hình variant của TASK-PRICE-001 ổn định.</li><li>Từ điển brand đa ngôn ngữ khi mở SEA (TH/ID) - mở rộng theo nước, fold dấu theo ngôn ngữ đích.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Over-merge: gộp hai SKU khác (iPhone 15 vs 15 Pro)</td><td>mẫu review + report giá lệch</td><td><code>GET /v1/compare</code> hiển thị giá sai, mất niềm tin</td><td>Ngưỡng <code>merge_threshold</code> cao + review queue cho vùng lưng chừng (DEC-PRICE-23)</td></tr><tr><td>Under-merge: cùng SKU không gộp</td><td>tỷ lệ key trùng thấp</td><td>so sánh chéo sàn thiếu dòng, moat yếu</td><td>Hạ nhẹ <code>sim_threshold</code>, làm giàu từ điển brand/đồng nghĩa</td></tr><tr><td>Fold dấu phá token brand (<code>Samsung</code>-&gt;<code>samsung</code> ok; nhãn có dấu hiếm)</td><td>test fold + spot-check</td><td>brand sai -&gt; key sai</td><td>Từ điển brand chuẩn hóa sau fold; giữ token số model nguyên</td></tr><tr><td>pg_trgm chậm trên bảng triệu dòng</td><td>p95 <code>canon_match_duration_ms</code></td><td>matching đọng, recompute trễ</td><td>GIN trgm index (§3); giới hạn LIMIT ứng viên; chạy theo lô</td></tr><tr><td>Recompute không idempotent (key đổi vô cớ)</td><td>test recompute</td><td>nhóm so sánh vỡ vụn, biểu đồ nhảy</td><td><code>Normalize</code>/<code>CanonicalKey</code> thuần; hash thuộc tính đã sắp xếp (DEC-PRICE-24)</td></tr><tr><td>Nhiễu marketing mới (seller dùng từ lạ)</td><td>tỷ lệ token rác tăng</td><td>chuẩn hóa sót, similarity giảm</td><td>Bổ sung regex nhiễu trong file dữ liệu, không sửa logic</td></tr><tr><td>Review queue tồn đọng</td><td><code>canon_review_enqueued_total</code> tăng, pending lớn</td><td>merge chậm, dòng so sánh thiếu</td><td>Bổ sung người duyệt; cân nhắc bật embedding để giảm vùng lưng chừng</td></tr><tr><td>Title rỗng/chỉ emoji</td><td><code>Normalize</code> trả chuỗi rỗng</td><td>không tách được brand/model</td><td>Bỏ qua matching, giữ <code>canonical_key</code> NULL, gắn cờ cần scraper lấy lại title</td></tr><tr><td>Trùng <code>(product_id, candidate_key)</code> khi enqueue lặp</td><td>UNIQUE constraint</td><td>từ chối ghi trùng</td><td><code>ON CONFLICT DO NOTHING</code> khi enqueue (idempotent)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li><code>canonical_key</code> là moat đa sàn của SănDeal (§5.6): không có nó, so sánh giá chéo 3 sàn - khoảng trống mà BeeCost bỏ ngỏ - không thực hiện được.</li><li>Triết lý thiết kế: thà under-merge (thiếu một dòng so sánh) còn hơn over-merge (hiển thị giá sai). Niềm tin hậu-Honey là tài sản dễ mất, khó lấy lại.</li><li>Key xác định trước, fuzzy sau: bắt trọn phần dễ với chi phí gần không, chỉ tốn fuzzy cho phần đuôi khó.</li><li>Vùng confidence lưng chừng (0,60-0,82) luôn vào người duyệt, không bao giờ tự gộp - đây là van an toàn chính của task.</li><li>Fold dấu là yêu cầu riêng cho thị trường VN: bỏ qua thì dedup vô dụng vì người Việt gõ có dấu và không dấu lẫn lộn.</li><li>Hàm chuẩn hóa và <code>CanonicalKey</code> thuần (pure) bảo đảm recompute idempotent khi scraper cập nhật title - nhóm so sánh ổn định theo thời gian.</li></ul>\n<hr>\n<p><em>Hết TASK-PRICE-005. Status: done (mục tiêu audit 10/10).</em></p>";
