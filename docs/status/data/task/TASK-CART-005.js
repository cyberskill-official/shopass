window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CART-005"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Extension <strong>MUST</strong> hiện thực <code>testCodes</code> - thử các mã giảm ứng viên trên giỏ một cách an toàn client-side, CHỈ khi người dùng chủ động khởi tạo, với nhịp người giữa các lần thử, gỡ mã sau mỗi lần, và TUYỆT ĐỐI không tự chốt đơn; bám đúng pseudo-code §3.5(4). Hợp đồng:</p>\n<ol><li><code>testCodes</code> <strong>MUST</strong> chỉ chạy khi người dùng chủ động bấm nút \"thử mã\" (<code>userInitiatedApply</code>, user gesture) (DEC-CART-24); KHÔNG tự khởi động nền, KHÔNG <code>setInterval</code>/<code>alarms</code> tự gọi - tuân Chrome Web Store policy 10/06/2025 bắt buộc hành động người dùng (§4.2).</li><li>Giữa mỗi lần thử mã, <code>testCodes</code> <strong>MUST</strong> <code>sleep(random(2.5s, 5s))</code> (DEC-CART-25, §3.5(4)) - nhịp người, tránh anti-bot và tránh bị coi là tự động hóa lạm dụng.</li><li>Sau khi thử mỗi mã, <code>testCodes</code> <strong>MUST</strong> <code>revert</code> (gỡ mã ra khỏi giỏ) (DEC-CART-26): không để mã dính sau khi đo; mỗi lần thử là áp-đo-gỡ độc lập.</li><li><code>testCodes</code> <strong>MUST NOT</strong> tự chốt đơn, đặt hàng, hay thực hiện bất kỳ giao dịch nào (DEC-CART-26, DEC-CART-28, §3.5(4)): nó chỉ ĐO mức giảm khi áp thử rồi gỡ; mọi áp dụng cuối cùng và chốt đơn do người dùng tự làm trên giao diện sàn.</li><li><code>testCodes</code> <strong>MUST</strong> trả kết quả <code>sortDesc</code> theo mức giảm (DEC-CART-27): mã giảm nhiều nhất lên đầu, để người dùng tự quyết áp mã nào (gợi ý, không tự áp).</li><li>Mã ứng viên <strong>MUST</strong> đến từ <code>voucher_catalog</code> (TASK-CART-001) còn hiệu lực (DEC-CART-29); <code>testCodes</code> <strong>MUST NOT</strong> đoán/bruteforce mã (vô đạo đức + kích hoạt anti-bot).</li><li><code>testCodes</code> <strong>MUST</strong> bám cấu trúc pseudo-code §3.5(4): với mỗi mã trong danh sách ứng viên, <code>sleep(random(2.5s,5s))</code>, <code>apply = userInitiatedApply(code)</code>, nếu hợp lệ ghi <code>(code, discount)</code>, <code>revert()</code>, cuối cùng <code>sortDesc(results)</code>.</li><li>Mỗi lần áp thử <strong>MUST</strong> qua giao diện giỏ của sàn (như thao tác người dùng), KHÔNG gọi API mutate ẩn để áp/gỡ; tái dùng khung reader của TASK-EXT-002 (chỉ-đọc trừ thao tác áp-gỡ mã user-initiated này).</li><li><code>testCodes</code> <strong>MUST</strong> xử lý mã không hợp lệ lịch sự: mã sàn từ chối -&gt; ghi nhận không-giảm, gỡ (nếu cần), tiếp mã sau; không lỗi vỡ luồng.</li><li>Toàn bộ quá trình <strong>MUST</strong> dừng được: nếu người dùng rời trang hoặc hủy, <code>testCodes</code> dừng vòng thử (không tiếp tục nền) - nhất quán với chỉ-khi-user-initiated.</li><li><code>testCodes</code> <strong>MUST NOT</strong> gửi mã/kết quả kèm bất kỳ credential nào về backend; chỉ kết quả mức giảm (đồng nhất ranh giới tối thiểu hóa TASK-EXT-003).</li><li><code>npm test</code> xanh; <code>tsc --noEmit</code> sạch.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao chỉ user-initiated (DEC-CART-24)?</strong> Đây là ranh giới sống còn hậu-Honey. Chrome Web Store policy 10/06/2025 (§4.2) - chính chính sách khiến hàng loạt extension bị gỡ - bắt buộc hành động người dùng và cấm tự động hóa né disclosure. Nếu <code>testCodes</code> chạy nền tự động thì SănDeal bị coi như Honey: vi phạm policy, rủi ro bị gỡ khỏi Chrome Web Store (existential, §5.2). Chỉ chạy khi user bấm nút là tuân policy và giữ minh bạch.</p>\n<p><strong>Vì sao sleep nhịp người (DEC-CART-25)?</strong> Thử nhiều mã liên tiếp không nghỉ trông như bot - kích hoạt anti-bot sàn (§3.9 xếp tự động hóa voucher rủi ro ban High) và bị coi là lạm dụng. <code>sleep(random(2.5s,5s))</code> giữa các lần mô phỏng nhịp người thật, giảm rủi ro ban và phù hợp ToS. Đây là pacing như TASK-SCRAPE-005 nhưng phía client.</p>\n<p><strong>Vì sao revert sau mỗi mã (DEC-CART-26)?</strong> Mục tiêu là ĐO mã nào giảm nhiều nhất, không phải áp mã. Để mã dính sau khi thử làm rối giỏ người dùng (mã không tối ưu bị áp nhầm). Áp-đo-gỡ độc lập từng mã giữ giỏ sạch và để người dùng tự chọn mã cuối.</p>\n<p><strong>Vì sao tuyệt đối không tự chốt đơn (DEC-CART-26/28)?</strong> Tự chốt đơn là thực hiện giao dịch thay người dùng - rủi ro cực cao (đặt nhầm đơn) và đúng loại tự động hóa Honey bị phạt. SănDeal chỉ đo và gợi ý; mọi áp dụng cuối + chốt đơn do người dùng tự bấm trên sàn. Ranh giới này phân biệt \"trợ lý gợi ý\" với \"bot thay người dùng\".</p>\n<p><strong>Vì sao sortDesc gợi ý không tự áp (DEC-CART-27)?</strong> Người dùng cần thấy \"mã GIAM50K giảm 50k, mã FREESHIP giảm 30k\" và tự chọn. Sắp giảm-nhiều-nhất lên đầu giúp quyết nhanh. Không tự áp giữ quyền quyết định ở người dùng - vừa hợp policy (hành động người dùng) vừa đúng triết lý.</p>\n<p><strong>Vì sao mã từ catalog, không bruteforce (DEC-CART-29)?</strong> Đoán mã là vô đạo đức (thử mã không phải của mình) và kích hoạt anti-bot (nhiều mã sai liên tiếp). Mã ứng viên đến từ <code>voucher_catalog</code> còn hiệu lực (TASK-CART-001) - các mã hợp lệ đã biết. Thử mã đã biết khác hoàn toàn với dò mã.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Nhịp người (pacing.ts)</h3>\n<pre><code class=\"language-ts\">// extension/src/content/shared/pacing.ts\n// sleep random 2.5s..5s giữa các lần thử (DEC-CART-25, §3.5(4)).\nexport function randomDelayMs(): number {\n  return 2500 + Math.floor(Math.random() * 2500); // [2500, 5000)\n}\nexport function sleep(ms: number): Promise&lt;void&gt; {\n  return new Promise((resolve) =&gt; setTimeout(resolve, ms));\n}</code></pre>\n<h3>Áp - đo - gỡ (code-applier.ts) - KHÔNG chốt đơn</h3>\n<pre><code class=\"language-ts\">// extension/src/content/shared/code-applier.ts\n// applyCodeViaUi áp mã qua giao diện giỏ (như thao tác người), đọc mức giảm, KHÔNG chốt đơn.\nexport interface ApplyOutcome { valid: boolean; discount: number } // VND int\n\nexport async function userInitiatedApply(code: string): Promise&lt;ApplyOutcome&gt; {\n  enterCodeIntoCartField(code);   // gõ mã vào ô voucher của giỏ\n  clickApplyButton();             // bấm \"áp dụng\" trên UI sàn\n  const discount = readDiscountFromCart(); // đọc mức giảm hiển thị\n  return { valid: discount &gt; 0, discount };\n  // LƯU Ý: KHÔNG có bước đặt hàng / chốt đơn (DEC-CART-26/28)\n}\n\nexport function revert(): void {\n  removeAppliedCodeFromCart();    // gỡ mã ra khỏi giỏ (DEC-CART-26)\n}</code></pre>\n<h3>testCodes (test-codes.ts) - bám pseudo-code §3.5(4)</h3>\n<pre><code class=\"language-ts\">// extension/src/content/shared/test-codes.ts\nimport { randomDelayMs, sleep } from \"./pacing\";\nimport { userInitiatedApply, revert } from \"./code-applier\";\n\nexport interface CodeTestResult { code: string; discount: number }\n\n// testCodes bám đúng pseudo-code §3.5(4) (DEC-CART-24..29).\n// CHỈ gọi từ user gesture (nút \"thử mã\"); `userInitiated` phải true.\nexport async function testCodes(\n  candidateCodes: string[],     // từ voucher_catalog còn hiệu lực (DEC-CART-29)\n  opts: { userInitiated: boolean; cancelled: () =&gt; boolean }\n): Promise&lt;CodeTestResult[]&gt; {\n  if (!opts.userInitiated) {\n    throw new Error(\"testCodes chỉ chạy khi user chủ động bấm (DEC-CART-24)\");\n  }\n  const results: CodeTestResult[] = [];\n  for (const code of candidateCodes) {\n    if (opts.cancelled()) break;            // user rời/hủy -&gt; dừng (§1 #10)\n    await sleep(randomDelayMs());           // nhịp người (DEC-CART-25)\n    const apply = await userInitiatedApply(code);\n    if (apply.valid) {\n      results.push({ code, discount: apply.discount });\n    }\n    revert();                               // gỡ mã, KHÔNG chốt đơn (DEC-CART-26)\n  }\n  return results.sort((a, b) =&gt; b.discount - a.discount); // sortDesc (DEC-CART-27)\n}</code></pre>\n<h3>Nút thử mã (test-codes-button.ts) - phát user gesture</h3>\n<pre><code class=\"language-ts\">// extension/src/ui/test-codes-button.ts\n// Nút do người dùng bấm; KHÔNG tự gọi testCodes (DEC-CART-24).\nbutton.addEventListener(\"click\", async () =&gt; {\n  const codes = await getCandidateCodesFromCatalog(); // TASK-CART-001 còn hiệu lực\n  const results = await testCodes(codes, { userInitiated: true, cancelled: () =&gt; userLeft });\n  renderSuggestions(results); // gợi ý, user tự áp mã trên sàn\n});</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>testCodes</code> ném lỗi (không chạy) khi <code>userInitiated</code> là false; chỉ chạy khi true (user gesture).</li><li>Grep <code>extension/src/**</code>: KHÔNG có <code>setInterval</code>/<code>chrome.alarms</code> gọi <code>testCodes</code>; nút phát user gesture là đường gọi duy nhất.</li><li>Giữa mỗi lần thử có <code>sleep(randomDelayMs())</code> với <code>randomDelayMs</code> trong <code>[2500, 5000)</code>.</li><li>Sau mỗi mã, <code>revert()</code> được gọi (gỡ mã); test khẳng định gọi revert đúng số lần = số mã thử.</li><li>Grep <code>code-applier.ts</code> + <code>test-codes.ts</code>: KHÔNG có bước đặt hàng/chốt đơn (không gọi endpoint/UI checkout/place-order).</li><li>Kết quả trả <code>sortDesc</code> theo <code>discount</code> (mã giảm nhiều nhất lên đầu).</li><li>Mã ứng viên đến từ catalog (TASK-CART-001); grep: KHÔNG có vòng sinh/đoán mã (bruteforce).</li><li>Mã sàn từ chối (<code>valid=false</code>) không vào kết quả; vòng tiếp mã sau, không lỗi.</li><li><code>cancelled()</code> true (user rời/hủy) -&gt; vòng dừng, không tiếp tục nền.</li><li>Cấu trúc bám pseudo-code §3.5(4): sleep -&gt; apply -&gt; (ghi nếu hợp lệ) -&gt; revert -&gt; sortDesc.</li><li>Kết quả gửi đi (nếu có) không kèm credential (chỉ code + discount).</li><li><code>npm test</code> xanh; <code>tsc --noEmit</code> sạch.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-ts\">// extension/test/test-codes-user-initiated.test.ts\nimport { testCodes } from \"../src/content/shared/test-codes\";\n\ntest(\"KHÔNG chạy khi không phải user-initiated\", async () =&gt; {\n  await expect(testCodes([\"A\", \"B\"], { userInitiated: false, cancelled: () =&gt; false }))\n    .rejects.toThrow(/user chủ động/);\n});\n\ntest(\"mã nguồn KHÔNG có setInterval/alarms gọi testCodes\", async () =&gt; {\n  const files = [\"src/content/shared/test-codes.ts\", \"src/ui/test-codes-button.ts\"];\n  for (const f of files) {\n    const src = await readFile(f, \"utf8\");\n    expect(src).not.toMatch(/setInterval/);\n    expect(src).not.toMatch(/chrome\\.alarms/);\n  }\n});</code></pre>\n<pre><code class=\"language-ts\">// extension/test/test-codes-revert.test.ts\nimport { testCodes } from \"../src/content/shared/test-codes\";\nimport * as applier from \"../src/content/shared/code-applier\";\n\ntest(\"revert sau mỗi mã; KHÔNG chốt đơn\", async () =&gt; {\n  jest.spyOn(applier, \"userInitiatedApply\").mockResolvedValue({ valid: true, discount: 30_000 });\n  const revertSpy = jest.spyOn(applier, \"revert\").mockImplementation(() =&gt; {});\n  await testCodes([\"A\", \"B\", \"C\"], { userInitiated: true, cancelled: () =&gt; false });\n  expect(revertSpy).toHaveBeenCalledTimes(3); // mỗi mã gỡ một lần\n});\n\ntest(\"mã nguồn KHÔNG có bước chốt đơn\", async () =&gt; {\n  const src = await readFile(\"src/content/shared/code-applier.ts\", \"utf8\");\n  expect(src).not.toMatch(/place.?order|checkout|chốt đơn|submitOrder/i);\n});\n\ntest(\"sortDesc theo discount\", async () =&gt; {\n  const seq = [{ valid: true, discount: 20_000 }, { valid: true, discount: 50_000 }, { valid: true, discount: 30_000 }];\n  let i = 0;\n  jest.spyOn(applier, \"userInitiatedApply\").mockImplementation(async () =&gt; seq[i++]);\n  jest.spyOn(applier, \"revert\").mockImplementation(() =&gt; {});\n  const res = await testCodes([\"A\", \"B\", \"C\"], { userInitiated: true, cancelled: () =&gt; false });\n  expect(res.map((r) =&gt; r.discount)).toEqual([50_000, 30_000, 20_000]); // giảm nhiều nhất đầu\n});</code></pre>\n<pre><code class=\"language-ts\">// extension/test/test-codes-pacing.test.ts\nimport { randomDelayMs } from \"../src/content/shared/pacing\";\n\ntest(\"delay nằm trong [2500, 5000)\", () =&gt; {\n  for (let k = 0; k &lt; 100; k++) {\n    const d = randomDelayMs();\n    expect(d).toBeGreaterThanOrEqual(2500);\n    expect(d).toBeLessThan(5000);\n  }\n});\n\ntest(\"có sleep giữa các lần thử (dùng clock giả)\", async () =&gt; {\n  jest.useFakeTimers();\n  const applier = require(\"../src/content/shared/code-applier\");\n  jest.spyOn(applier, \"userInitiatedApply\").mockResolvedValue({ valid: false, discount: 0 });\n  jest.spyOn(applier, \"revert\").mockImplementation(() =&gt; {});\n  const { testCodes } = require(\"../src/content/shared/test-codes\");\n  const p = testCodes([\"A\", \"B\"], { userInitiated: true, cancelled: () =&gt; false });\n  // mỗi mã chờ &gt;=2.5s trước khi apply\n  await jest.advanceTimersByTimeAsync(2500);\n  await jest.advanceTimersByTimeAsync(5000);\n  await p;\n  jest.useRealTimers();\n});</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>types.ts</code> (CodeTestResult, TestCodesRequest) -&gt; <code>pacing.ts</code> (<code>randomDelayMs</code> + <code>sleep</code>, test với clock giả) -&gt; <code>code-applier.ts</code> (<code>userInitiatedApply</code> áp-đo qua UI giỏ + <code>revert</code>, KHÔNG checkout) -&gt; <code>test-codes.ts</code> (vòng bám pseudo-code §3.5(4): guard userInitiated, sleep, apply, ghi, revert, sortDesc) -&gt; <code>test-codes-button.ts</code> (nút phát user gesture, đường gọi duy nhất) -&gt; tests. Tái dùng khung reader của TASK-EXT-002; thao tác áp-gỡ mã là ngoại lệ user-initiated duy nhất với nguyên tắc chỉ-đọc (áp thử qua UI như thao tác người, gỡ ngay). Mã ứng viên lấy từ <code>voucher_catalog</code> còn hiệu lực (TASK-CART-001), không đoán. Toàn bộ gated sau user gesture; test khẳng định không có đường tự động.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-EXT-002</strong> - khung content script + reader giỏ (session piggyback); <code>testCodes</code> tái dùng để áp-đo mã qua UI giỏ; thao tác áp-gỡ là ngoại lệ user-initiated của nguyên tắc chỉ-đọc (depends_on cứng).</li><li><strong>TASK-CART-001</strong> - <code>voucher_catalog</code> cấp danh sách mã ứng viên còn hiệu lực; <code>testCodes</code> thử mã đã biết, không đoán (depends_on cứng).</li><li><strong>TASK-CART-006 (sibling)</strong> - checklist xu cùng triết lý không-tự-động-click; cùng giữ ranh giới user-initiated.</li><li><strong>TASK-AFFIL-004 / NFR-AFFIL-001</strong> - guardrails né Honey + Chrome policy; <code>testCodes</code> user-initiated là một mặt của compliance này.</li><li>Extension/lib: TypeScript, Manifest V3 content script; test qua Jest + fake timers + jsdom.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Kết quả thử mã (gợi ý cho user, sortDesc)</h3>\n<pre><code class=\"language-json\">[\n  { \"code\": \"GIAM50K\",  \"discount\": 50000 },\n  { \"code\": \"FREESHIP\", \"discount\": 30000 },\n  { \"code\": \"SHOPA20K\", \"discount\": 20000 }\n]</code></pre>\n<h3>Luồng (mô tả, không phải payload)</h3>\n<pre><code>user bấm \"thử mã\"\n  -&gt; lấy mã ứng viên từ voucher_catalog (còn hiệu lực)\n  -&gt; với mỗi mã: sleep(2.5..5s) -&gt; áp thử qua UI giỏ -&gt; đọc giảm -&gt; gỡ mã\n  -&gt; sortDesc -&gt; hiển thị gợi ý\n  -&gt; user TỰ áp mã chọn và TỰ chốt đơn trên sàn</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Giới hạn số mã thử mỗi lần (tránh chuỗi quá dài) - thêm trần khi đo hành vi thật.</li><li>Ghi nhớ mã đã thử gần đây để không thử lại ngay - tối ưu UX, không đổi ranh giới.</li><li>Kết hợp kết quả thử mã với optimizer (TASK-CART-003) để gợi ý combo - cân nhắc khi hai luồng chín.</li><li>Hỗ trợ áp-đo trên TikTok Shop/Lazada (khung reader TASK-EXT-007/008) - mở rộng per-sàn sau.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Chạy nền không user gesture</td><td>user-initiated test + grep</td><td>vi phạm Chrome policy -&gt; gỡ extension</td><td>Chỉ userInitiated (DEC-CART-24)</td></tr><tr><td>Bỏ sleep giữa các lần</td><td>pacing test</td><td>bị coi bot -&gt; ban (§3.9 High)</td><td>sleep random 2.5..5s (DEC-CART-25)</td></tr><tr><td>Tự chốt đơn</td><td>grep checkout/place-order</td><td>giao dịch thay user (Honey-style)</td><td>KHÔNG checkout (DEC-CART-26/28)</td></tr><tr><td>Mã dính sau thử</td><td>revert test</td><td>giỏ rối, áp nhầm mã</td><td>revert mỗi mã (DEC-CART-26)</td></tr><tr><td>Đoán/bruteforce mã</td><td>grep sinh mã</td><td>vô đạo đức + anti-bot</td><td>Mã từ catalog (DEC-CART-29)</td></tr><tr><td>Mã sai làm vỡ luồng</td><td>invalid-code test</td><td>mất các mã sau</td><td>Ghi không-giảm, tiếp tục (§1 #9)</td></tr><tr><td>Không dừng khi user rời</td><td>cancelled test</td><td>tiếp tục nền</td><td>Kiểm cancelled() (§1 #10)</td></tr><tr><td>Gửi credential kèm kết quả</td><td>introspection</td><td>rò rỉ</td><td>Chỉ code + discount (§1 #11)</td></tr><tr><td>Gọi API mutate ẩn áp/gỡ</td><td>grep endpoint mutate</td><td>né disclosure</td><td>Áp-gỡ qua UI như thao tác người (§1 #8)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Auto-test mã là task rủi ro compliance cao nhất của CART: §3.9c xếp tự động hóa voucher rủi ro ban High, §4.2 Chrome policy 10/06/2025 bắt buộc hành động người dùng.</li><li>Ranh giới sống còn: CHỈ user-initiated, sleep nhịp người, revert sau mỗi thử, KHÔNG BAO GIỜ tự chốt đơn - phải test khẳng định, không chỉ quy ước.</li><li>testCodes là read-measure-revert: đo mã nào giảm nhiều nhất rồi gỡ; mọi áp dụng cuối + chốt đơn do user tự làm.</li><li>sortDesc gợi ý không tự áp giữ quyền quyết định ở người dùng - hợp policy và đúng triết lý hậu-Honey.</li><li>Mã ứng viên từ voucher_catalog (đã biết, còn hiệu lực) - thử mã đã biết khác hoàn toàn với dò mã.</li><li>Bám đúng pseudo-code §3.5(4); thao tác áp-gỡ qua UI là ngoại lệ user-initiated duy nhất của nguyên tắc chỉ-đọc TASK-EXT-002.</li></ul>\n<hr>\n<p><em>Hết TASK-CART-005. Status: done (mục tiêu audit 10/10).</em></p>";
