window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AFFIL-001"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service AFFIL <strong>MUST</strong> định nghĩa hai bảng sổ cái affiliate: <code>affiliate_click</code> (ghi mỗi lần user chủ động bấm mua qua SănDeal) và <code>affiliate_conversion</code> (ghi đơn hàng quy về một click, theo last-click attribution), với tiền lưu BIGINT VND và vòng đời status có kiểm soát. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> định nghĩa bảng <code>affiliate_click (id, user_id, platform_id, product_id, sub_id, clicked_at, network)</code> với <code>user_id</code> REFERENCES <code>app_user(id)</code>, <code>platform_id</code> REFERENCES <code>platform(id)</code>, <code>product_id</code> REFERENCES <code>tracked_product(id)</code>.</li><li><strong>MUST</strong> đặt <code>sub_id TEXT NOT NULL UNIQUE</code>: mỗi click có một <code>sub_id</code> đối soát duy nhất, do SănDeal sinh (TASK-AFFIL-001 <code>subid.go</code>), dùng để network postback ánh xạ ngược conversion về click (DEC-AFFIL-01).</li><li><code>sub_id</code> <strong>MUST NOT</strong> nhúng định danh người dùng sàn thật hay PII (email, số điện thoại, tên); nó là một token đối soát ngẫu nhiên (DEC-AFFIL-02). Quan hệ click -&gt; user nằm ở cột <code>user_id</code> nội bộ, không ở trong <code>sub_id</code>.</li><li><code>affiliate_click</code> <strong>MUST</strong> chỉ được ghi qua hàm <code>RecordClick</code> mà caller (TASK-AFFIL-002) khẳng định là user-initiated; KHÔNG có đường ghi click tự động nền (NFR-AFFIL-001). <code>network</code> ghi nguồn (<code>'involve_asia'</code>, <code>'accesstrade'</code>, ...) để đối soát đúng kênh.</li><li><strong>MUST</strong> định nghĩa bảng <code>affiliate_conversion (id, click_id, order_value, commission, status, confirmed_at)</code> với <code>click_id</code> REFERENCES <code>affiliate_click(id)</code>.</li><li><strong>MUST</strong> lưu <code>order_value</code> và <code>commission</code> dạng <code>BIGINT</code> (VND, không thập phân) - KHÔNG float/numeric (DEC-AFFIL-04) - và ràng buộc <code>order_value &gt;= 0</code>, <code>commission &gt;= 0</code> qua CHECK.</li><li><code>affiliate_conversion.status</code> <strong>MUST</strong> thuộc tập <code>{'pending','confirmed','rejected'}</code> qua CHECK; conversion mới <strong>MUST</strong> bắt đầu ở <code>'pending'</code> (DEC-AFFIL-05). <code>confirmed_at</code> chỉ được set khi chuyển sang <code>'confirmed'</code>.</li><li><strong>MUST</strong> áp last-click attribution ở mức dữ liệu (DEC-AFFIL-03): một conversion gắn vào đúng một <code>click_id</code>; khi postback (TASK-AFFIL-003) mang <code>sub_id</code>, repo tra <code>affiliate_click</code> theo <code>sub_id</code> để lấy <code>click_id</code>. Nếu <code>sub_id</code> không khớp click nào -&gt; conversion bị từ chối ghi (không tạo conversion mồ côi).</li><li><strong>MUST</strong> expose hàm repo:</li></ol>\n<ul><li><code>RecordClick(ctx, c AffiliateClick) (int64, error)</code> - chèn một click user-initiated, trả <code>click_id</code>.</li><li><code>RecordConversion(ctx, subID string, orderValue, commission int64, network string) (int64, error)</code> - tra click theo <code>sub_id</code>, chèn conversion <code>pending</code>.</li><li><code>ConfirmConversion(ctx, conversionID int64) error</code> - chuyển <code>pending</code> -&gt; <code>confirmed</code>, set <code>confirmed_at = now()</code>.</li><li><code>RejectConversion(ctx, conversionID int64, reason string) error</code> - chuyển sang <code>rejected</code>.</li></ul>\n<ol><li><strong>MUST</strong> đánh index <code>idx_click_subid</code> trên <code>affiliate_click(sub_id)</code> (đã UNIQUE) cho tra cứu postback nhanh, và <code>idx_click_user_time</code> trên <code>(user_id, clicked_at DESC)</code> cho báo cáo và anti-fraud (TASK-TRUST-004 velocity).</li><li><strong>MUST</strong> idempotent với postback lặp: cùng một <code>sub_id</code> postback hai lần (network retry) <strong>MUST NOT</strong> tạo hai conversion - dùng <code>UNIQUE</code> trên <code>affiliate_conversion(click_id)</code> hoặc kiểm tồn tại trước khi chèn (một click sinh tối đa một conversion).</li><li><strong>SHOULD</strong> phát OTel: <code>affiliate_click_recorded_total{platform_id, network}</code> (counter), <code>affiliate_conversion_total{status}</code> (counter), <code>affiliate_conversion_value_vnd</code> (histogram order_value).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao sub_id ngẫu nhiên không nhúng PII (DEC-AFFIL-02)?</strong> <code>sub_id</code> đi ra ngoài hệ thống: nó nằm trong deep link affiliate và quay lại qua postback của network. Nếu nhúng <code>user_id</code> thật hay email vào đó, ta rò rỉ định danh người dùng cho bên thứ ba và vi phạm tối thiểu hóa dữ liệu của PDPL. Một token ngẫu nhiên đủ entropy làm khóa đối soát mà không tiết lộ ai; ánh xạ về user giữ kín ở cột <code>user_id</code> nội bộ.</p>\n<p><strong>Vì sao click chỉ ghi khi user-initiated (DEC-AFFIL-01, NFR-AFFIL-001)?</strong> Đây là lằn ranh đạo đức trung tâm của SănDeal hậu-Honey (§4.2). Honey âm thầm thay cookie affiliate nền, không cần user bấm - chính điều khiến nó bị gỡ và bị Chrome cấm. Mô hình affiliate hợp lệ duy nhất là user chủ động bấm \"Mua qua SănDeal\". Vì vậy ở mức schema, không tồn tại đường ghi click tự động: bảng chỉ nhận click qua một hàm mà caller phải là luồng người dùng bấm thật (TASK-AFFIL-002).</p>\n<p><strong>Vì sao tiền BIGINT VND (DEC-AFFIL-04)?</strong> <code>order_value</code> và <code>commission</code> là tiền tệ VN, luôn số nguyên đồng. Float gây sai số tích lũy khi cộng dồn hoa hồng qua hàng nghìn conversion để đối soát với báo cáo network. BIGINT chính xác tuyệt đối và đủ lớn cho mọi giá trị đơn hàng thực tế.</p>\n<p><strong>Vì sao conversion bắt đầu 'pending' và chỉ 'confirmed' khi postback (DEC-AFFIL-05)?</strong> Một đơn hàng được tạo không có nghĩa hoa hồng đã chắc chắn: người mua có thể hủy đơn, trả hàng, hoặc network từ chối do nghi gian lận. Nếu ta coi mọi conversion là chắc ngay và trả cashback (TASK-AFFIL-005) liền, SănDeal lỗ khi đơn bị đảo. Vòng đời <code>pending -&gt; confirmed/rejected</code> với delay payout là best practice ngành (§5.3) để chống gaming attribution.</p>\n<p><strong>Vì sao một click tối đa một conversion (§1 #11)?</strong> Network có thể postback lặp do retry. Nếu mỗi postback tạo một conversion, sổ cái phình và tiền hoa hồng bị đếm trùng. Ràng buộc một-một giữa click và conversion (qua UNIQUE click_id) làm postback idempotent, giữ đối soát đúng.</p>\n<p><strong>Vì sao last-click attribution ở mức dữ liệu (DEC-AFFIL-03)?</strong> Cả ba sàn dùng last-click (§4.2): conversion thuộc về click affiliate gần nhất. Bằng cách postback mang <code>sub_id</code> và tra ngược ra đúng click đã sinh <code>sub_id</code> đó, ta thực thi last-click một cách xác định, không đoán.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migrations</h3>\n<pre><code class=\"language-sql\">-- services/affil/migrations/0001_affiliate_click.sql\nCREATE TABLE affiliate_click (\n  id          BIGSERIAL   PRIMARY KEY,\n  user_id     BIGINT      NOT NULL REFERENCES app_user(id),\n  platform_id SMALLINT    NOT NULL REFERENCES platform(id),\n  product_id  BIGINT      REFERENCES tracked_product(id),\n  sub_id      TEXT        NOT NULL UNIQUE,        -- token đối soát; KHÔNG nhúng PII\n  network     TEXT        NOT NULL,               -- 'involve_asia','accesstrade',...\n  clicked_at  TIMESTAMPTZ NOT NULL DEFAULT now()\n);\n\nCREATE INDEX idx_click_user_time ON affiliate_click (user_id, clicked_at DESC);\n-- sub_id đã UNIQUE (B-tree); postback tra theo sub_id dùng chính unique index đó.\n\n-- services/affil/migrations/0002_affiliate_conversion.sql\nCREATE TABLE affiliate_conversion (\n  id           BIGSERIAL   PRIMARY KEY,\n  click_id     BIGINT      NOT NULL UNIQUE REFERENCES affiliate_click(id),  -- 1 click &lt;= 1 conversion\n  order_value  BIGINT      NOT NULL CHECK (order_value &gt;= 0),   -- VND\n  commission   BIGINT      NOT NULL CHECK (commission  &gt;= 0),   -- VND\n  status       TEXT        NOT NULL DEFAULT 'pending'\n                 CHECK (status IN ('pending','confirmed','rejected')),\n  confirmed_at TIMESTAMPTZ\n);</code></pre>\n<h3>Types (Go)</h3>\n<pre><code class=\"language-go\">// services/affil/internal/affil/types.go\ntype AffiliateClick struct {\n    ID         int64     `db:\"id\"`\n    UserID     int64     `db:\"user_id\"`\n    PlatformID int16     `db:\"platform_id\"`\n    ProductID  *int64    `db:\"product_id\"`\n    SubID      string    `db:\"sub_id\"`\n    Network    string    `db:\"network\"`\n    ClickedAt  time.Time `db:\"clicked_at\"`\n}\n\ntype AffiliateConversion struct {\n    ID          int64      `db:\"id\"`\n    ClickID     int64      `db:\"click_id\"`\n    OrderValue  int64      `db:\"order_value\"`  // VND\n    Commission  int64      `db:\"commission\"`   // VND\n    Status      string     `db:\"status\"`       // pending|confirmed|rejected\n    ConfirmedAt *time.Time `db:\"confirmed_at\"`\n}</code></pre>\n<h3>Repo - last-click + vòng đời (Go)</h3>\n<pre><code class=\"language-go\">// services/affil/internal/affil/repo.go\n\n// RecordConversion tra click theo sub_id (last-click), chèn conversion 'pending'.\n// Trả ErrUnknownSubID nếu sub_id không khớp click nào -&gt; KHÔNG tạo conversion mồ côi (§1 #8).\nfunc (r *Repo) RecordConversion(ctx context.Context, subID string, orderValue, commission int64, network string) (int64, error) {\n    var clickID int64\n    err := r.pool.QueryRow(ctx,\n        `SELECT id FROM affiliate_click WHERE sub_id = $1`, subID).Scan(&amp;clickID)\n    if errors.Is(err, pgx.ErrNoRows) {\n        return 0, ErrUnknownSubID\n    } else if err != nil {\n        return 0, err\n    }\n    var id int64\n    err = r.pool.QueryRow(ctx,\n        `INSERT INTO affiliate_conversion (click_id, order_value, commission)\n         VALUES ($1,$2,$3)\n         ON CONFLICT (click_id) DO NOTHING                 -- postback lặp idempotent (§1 #11)\n         RETURNING id`,\n        clickID, orderValue, commission).Scan(&amp;id)\n    if errors.Is(err, pgx.ErrNoRows) {\n        return 0, ErrConversionExists // click đã có conversion\n    }\n    metrics.ConversionRecorded(\"pending\")\n    return id, err\n}\n\nfunc (r *Repo) ConfirmConversion(ctx context.Context, id int64) error {\n    _, err := r.pool.Exec(ctx,\n        `UPDATE affiliate_conversion\n            SET status='confirmed', confirmed_at=now()\n          WHERE id=$1 AND status='pending'`, id)  // chỉ pending -&gt; confirmed\n    return err\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch -&gt; <code>affiliate_click</code> và <code>affiliate_conversion</code> tồn tại với FK đúng tới <code>app_user</code>, <code>platform</code>, <code>tracked_product</code>.</li><li><code>RecordClick</code> chèn một click -&gt; 1 dòng; đọc lại đúng <code>user_id/platform_id/sub_id/network</code>.</li><li>Chèn hai click cùng <code>sub_id</code> -&gt; lần hai vi phạm UNIQUE (sub_id duy nhất).</li><li><code>sub_id</code> sinh ra không chứa chuỗi nào khớp PII (email/phone/<code>user_id</code> thật) - kiểm <code>subid_test</code>.</li><li><code>RecordConversion(subID_hợp_lệ, ...)</code> -&gt; tạo conversion <code>status='pending'</code>, <code>confirmed_at IS NULL</code>, gắn đúng <code>click_id</code>.</li><li><code>RecordConversion(subID_không_tồn_tại, ...)</code> -&gt; trả <code>ErrUnknownSubID</code>, KHÔNG tạo dòng conversion.</li><li><code>RecordConversion</code> cùng <code>sub_id</code> (cùng click) hai lần -&gt; chỉ một conversion (ON CONFLICT click_id), lần hai trả <code>ErrConversionExists</code>.</li><li><code>INSERT affiliate_conversion</code> với <code>order_value &lt; 0</code> hoặc <code>commission &lt; 0</code> -&gt; lỗi CHECK.</li><li><code>INSERT</code> với <code>status='paid'</code> (ngoài tập) -&gt; lỗi CHECK.</li><li><code>ConfirmConversion(id)</code> trên conversion <code>pending</code> -&gt; <code>status='confirmed'</code>, <code>confirmed_at</code> được set.</li><li><code>ConfirmConversion(id)</code> trên conversion đã <code>confirmed</code> -&gt; không đổi (chỉ pending mới chuyển).</li><li>Metric <code>affiliate_click_recorded_total</code> tăng khi RecordClick; <code>affiliate_conversion_total{status=\"pending\"}</code> tăng khi RecordConversion.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/affil/internal/affil/repo_test.go\nfunc TestRecordClick_Insert(t *testing.T) {\n    r, uid, pid := setupWithUserProduct(t)\n    id, err := r.RecordClick(ctx, AffiliateClick{\n        UserID: uid, PlatformID: 1, ProductID: &amp;pid,\n        SubID: \"sd_ab12cd34\", Network: \"involve_asia\"})\n    require.NoError(t, err)\n    require.Greater(t, id, int64(0))\n}\n\nfunc TestRecordClick_SubIDUnique(t *testing.T) {\n    r, uid, _ := setupWithUserProduct(t)\n    c := AffiliateClick{UserID: uid, PlatformID: 1, SubID: \"sd_dup\", Network: \"accesstrade\"}\n    _, err1 := r.RecordClick(ctx, c)\n    require.NoError(t, err1)\n    _, err2 := r.RecordClick(ctx, c)\n    require.Error(t, err2) // UNIQUE(sub_id)\n}\n\nfunc TestConversion_LastClickBySubID(t *testing.T) {\n    r, uid, _ := setupWithUserProduct(t)\n    r.RecordClick(ctx, AffiliateClick{UserID: uid, PlatformID: 1, SubID: \"sd_x\", Network: \"involve_asia\"})\n    cid, err := r.RecordConversion(ctx, \"sd_x\", 250_000, 12_000, \"involve_asia\")\n    require.NoError(t, err)\n    require.Greater(t, cid, int64(0))\n    require.Equal(t, \"pending\", statusOf(t, r, cid))\n}\n\nfunc TestConversion_UnknownSubID_NoOrphan(t *testing.T) {\n    r, _, _ := setupWithUserProduct(t)\n    _, err := r.RecordConversion(ctx, \"sd_unknown\", 100_000, 5_000, \"accesstrade\")\n    require.ErrorIs(t, err, ErrUnknownSubID)\n    require.Equal(t, 0, countConversions(t, r))\n}\n\nfunc TestConversion_PostbackIdempotent(t *testing.T) {\n    r, uid, _ := setupWithUserProduct(t)\n    r.RecordClick(ctx, AffiliateClick{UserID: uid, PlatformID: 1, SubID: \"sd_y\", Network: \"involve_asia\"})\n    _, err1 := r.RecordConversion(ctx, \"sd_y\", 250_000, 12_000, \"involve_asia\")\n    require.NoError(t, err1)\n    _, err2 := r.RecordConversion(ctx, \"sd_y\", 250_000, 12_000, \"involve_asia\")\n    require.ErrorIs(t, err2, ErrConversionExists) // một click một conversion\n    require.Equal(t, 1, countConversions(t, r))\n}\n\nfunc TestConversion_NegativeMoney_Rejected(t *testing.T) {\n    r, uid, _ := setupWithUserProduct(t)\n    r.RecordClick(ctx, AffiliateClick{UserID: uid, PlatformID: 1, SubID: \"sd_z\", Network: \"involve_asia\"})\n    _, err := r.RecordConversion(ctx, \"sd_z\", -1, 0, \"involve_asia\")\n    require.Error(t, err) // CHECK order_value &gt;= 0\n}\n\nfunc TestConfirm_PendingToConfirmed(t *testing.T) {\n    r, uid, _ := setupWithUserProduct(t)\n    r.RecordClick(ctx, AffiliateClick{UserID: uid, PlatformID: 1, SubID: \"sd_c\", Network: \"involve_asia\"})\n    cid, _ := r.RecordConversion(ctx, \"sd_c\", 250_000, 12_000, \"involve_asia\")\n    require.NoError(t, r.ConfirmConversion(ctx, cid))\n    require.Equal(t, \"confirmed\", statusOf(t, r, cid))\n}</code></pre>\n<pre><code class=\"language-go\">// services/affil/internal/affil/subid_test.go\nfunc TestSubID_UniqueAndNoPII(t *testing.T) {\n    seen := map[string]bool{}\n    for i := 0; i &lt; 1000; i++ {\n        s := NewSubID()\n        require.False(t, seen[s]) // duy nhất\n        seen[s] = true\n        require.NotContains(t, s, \"@\")        // không giống email\n        require.GreaterOrEqual(t, len(s), 12) // đủ entropy\n    }\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration <code>0001_affiliate_click.sql</code> -&gt; <code>0002_affiliate_conversion.sql</code> -&gt; <code>subid.go</code> (sinh token ngẫu nhiên qua <code>crypto/rand</code>, tiền tố <code>sd_</code> cho dễ nhận diện trong postback) -&gt; <code>types.go</code> + <code>repo.go</code> (<code>RecordClick</code>, <code>RecordConversion</code> tra last-click, <code>ConfirmConversion</code>/<code>RejectConversion</code>) -&gt; tests. <code>RecordClick</code> được TASK-AFFIL-002 gọi từ handler <code>POST /v1/affiliate/link</code> (luồng user bấm). <code>RecordConversion</code>/<code>ConfirmConversion</code> được TASK-AFFIL-003 gọi từ webhook postback của network. Driver <code>pgx</code>.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-INFRA-002</strong> - bảng <code>app_user</code>, <code>platform</code>, <code>tracked_product</code> phải tồn tại trước (mọi FK của <code>affiliate_click</code>).</li><li><strong>TASK-AFFIL-002 (downstream)</strong> - deep link user-initiated gọi <code>RecordClick</code> khi user bấm \"Mua qua SănDeal\".</li><li><strong>TASK-AFFIL-003 (downstream)</strong> - tích hợp network + webhook postback gọi <code>RecordConversion</code>/<code>ConfirmConversion</code>.</li><li><strong>TASK-AFFIL-005 (downstream, P3)</strong> - cashback layering đọc conversion <code>confirmed</code> để chia % cho user (hold tới khi confirm).</li><li><strong>TASK-TRUST-004 / TASK-TRUST-005 (downstream)</strong> - anti-fraud đọc <code>affiliate_click(user_id, clicked_at)</code> + conversion để phát hiện velocity/gaming attribution.</li><li>Lib: <code>pgx</code>, <code>crypto/rand</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Ghi click (nội bộ, do TASK-AFFIL-002 gọi sau khi user bấm)</h3>\n<pre><code class=\"language-go\">clickID, err := affilRepo.RecordClick(ctx, affil.AffiliateClick{\n    UserID:     7,\n    PlatformID: 1,            // shopee\n    ProductID:  ptr(int64(90112)),\n    SubID:      \"sd_ab12cd34ef56\",   // token đối soát ngẫu nhiên\n    Network:    \"involve_asia\",\n})</code></pre>\n<h3>Ghi conversion từ postback (nội bộ, do TASK-AFFIL-003 gọi)</h3>\n<pre><code class=\"language-sql\">-- network postback mang sub_id; repo tra ngược ra click_id rồi chèn pending\nSELECT id FROM affiliate_click WHERE sub_id = 'sd_ab12cd34ef56';\nINSERT INTO affiliate_conversion (click_id, order_value, commission)\nVALUES (1001, 250000, 12000)        -- VND\nON CONFLICT (click_id) DO NOTHING;</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Cookie window per-sàn/per-network (7 ngày vs 30/7 desktop/mobile - cần xác minh §10 tài liệu nguồn) - lưu ở bảng cấu hình network của TASK-AFFIL-003, không đổi schema này.</li><li>Đa conversion cho một click (đơn nhiều shop tách nhiều conversion) - hiện một-một; nới khi network thực tế trả tách dòng, đổi UNIQUE(click_id) sang khóa khác.</li><li>Lưu raw payload postback để truy vết tranh chấp - thêm bảng phụ <code>affiliate_postback_log</code> ở TASK-AFFIL-003.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Ghi click tự động nền (cookie-stuffing)</td><td>review + NFR-AFFIL-001</td><td>bị Chrome gỡ, network đình chỉ</td><td>Chỉ ghi qua RecordClick user-initiated (DEC-AFFIL-01)</td></tr><tr><td>sub_id nhúng PII</td><td>subid test</td><td>rò rỉ định danh, vi phạm PDPL</td><td>Token ngẫu nhiên, map user nội bộ (DEC-AFFIL-02)</td></tr><tr><td>sub_id postback không khớp click</td><td>ErrUnknownSubID</td><td>conversion mồ côi</td><td>Từ chối ghi, không tạo dòng (§1 #8)</td></tr><tr><td>Postback lặp tạo conversion trùng</td><td>UNIQUE click_id</td><td>đếm hoa hồng trùng</td><td>ON CONFLICT DO NOTHING (§1 #11)</td></tr><tr><td>Tiền lưu float</td><td>review + types int64</td><td>sai số đối soát</td><td>BIGINT VND (DEC-AFFIL-04)</td></tr><tr><td>order_value/commission âm</td><td>DB CHECK</td><td>sổ cái sai</td><td>CHECK &gt;= 0 (§1 #6)</td></tr><tr><td>Conversion vào thẳng 'confirmed'</td><td>CHECK + repo path</td><td>trả cashback đơn chưa xác nhận</td><td>Bắt đầu pending, confirm qua postback (DEC-AFFIL-05)</td></tr><tr><td>status ngoài tập</td><td>DB CHECK</td><td>trạng thái rác</td><td>CHECK status IN (...) (§1 #7)</td></tr><tr><td>FK user/product không tồn tại</td><td>lỗi pgx</td><td>từ chối ghi</td><td>Tạo user/track trước (TASK-INFRA-002/TASK-PRICE-001)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li><code>affiliate_click</code> + <code>affiliate_conversion</code> là sổ cái của dòng doanh thu affiliate - dòng tiền chính tài trợ free-tier (§4.1).</li><li>Click chỉ tồn tại khi user chủ động bấm: ở mức schema không có đường tự động nền, đó là cam kết compliant hậu-Honey (NFR-AFFIL-001).</li><li><code>sub_id</code> là token đối soát ngẫu nhiên, không nhúng PII - đi ra network rồi quay về qua postback mà không lộ ai là người dùng.</li><li>Vòng đời <code>pending -&gt; confirmed/rejected</code> + một-click-một-conversion cho phép delay payout chống gaming (§5.3) và giữ đối soát đúng khi network retry.</li><li>Tiền BIGINT VND tránh sai số tích lũy khi cộng dồn hoa hồng để đối chiếu báo cáo network.</li><li>Đây là nền cho TASK-AFFIL-002 (ghi click), TASK-AFFIL-003 (postback), TASK-AFFIL-005 (cashback) và TASK-TRUST-004/005 (anti-fraud).</li></ul>\n<hr>\n<p><em>Hết TASK-AFFIL-001. Status: done (mục tiêu audit 10/10).</em></p>";
