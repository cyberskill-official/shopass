window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-DEAL-003"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service DEAL <strong>MUST</strong> expose endpoint REST <code>GET /v1/products/{id}/chart?range=90d</code> trả feed dữ liệu đã định hình cho biểu đồ của đúng một <code>tracked_product</code>: phần thân là chuỗi giá theo ngày đọc từ continuous aggregate <code>price_daily</code>, phủ thêm các chú giải tín hiệu deal (đường median90, đường trailing_min, verdict sale ảo, mốc ngày đôi) để biểu đồ tự giải thích đây có phải sale thật không, đạt p95 &lt;500ms. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> phục vụ route <code>GET /v1/products/{id}/chart</code>; <code>{id}</code> là <code>tracked_product.id</code> (int64). Parse lỗi (id không phải số) trả <code>400</code>.</li><li><strong>MUST</strong> chấp nhận query <code>range</code> chỉ trong allowlist <code>{7d, 30d, 90d, 180d, 1y}</code> (DEC-DEAL-24); thiếu <code>range</code> mặc định <code>90d</code>; giá trị ngoài allowlist trả <code>400</code> với thân lỗi <code>{\"error\":\"invalid range\"}</code>. KHÔNG nhận khoảng tùy ý.</li><li><strong>MUST</strong> đọc phần thân biểu đồ từ <code>price_daily</code> (DEC-DEAL-20): các điểm ngày <code>{day, min_p, max_p, close_p}</code> cho khoảng <code>now() - range</code> tới hiện tại, sắp tăng dần theo <code>day</code>. KHÔNG quét raw <code>price_snapshot</code> cho phần thân.</li><li><strong>MUST</strong> tính <code>median90</code> (trung vị <code>close_p</code> trong cửa sổ 90 ngày gần nhất, đơn vị VND) và <code>trailing_min</code> (giá thấp nhất <code>min_p</code> trong toàn khoảng được trả) từ chính chuỗi daily (DEC-DEAL-21). Hai số này là đường tham chiếu để vẽ trên biểu đồ.</li><li><strong>MUST</strong> gắn verdict sale ảo bằng cách gọi đánh giá của <strong>TASK-DEAL-001</strong> (<code>SALE_AO</code>, <code>SALE_XIN</code>, <code>TAM_DUOC</code>, hoặc <code>UNKNOWN</code>) - KHÔNG tự tính lại verdict ở DEAL-003 hay ở client (DEC-DEAL-21). Verdict trên biểu đồ phải khớp nhãn trên thẻ sản phẩm.</li><li><strong>MUST</strong> lấy độ chín dữ liệu từ <strong>TASK-DEAL-002</strong> và trả trong trường <code>maturity</code> (<code>MATURE</code>, <code>WARMING</code>, hoặc <code>NEW</code>) (DEC-DEAL-23). SKU <code>WARMING</code> vẫn trả biểu đồ kèm cờ <code>accumulating=true</code> (đang tích lũy); SKU <code>NEW</code> (&lt;14 ngày) trả dữ liệu nhưng <code>verdict=UNKNOWN</code>.</li><li><strong>MUST</strong> tính tập mốc ngày đôi <code>double_dates</code> nằm trong khoảng được trả (DEC-DEAL-21): các ngày <code>1.1, 2.2, 3.3 ... 12.12</code> (ngày dd bằng tháng mm) rơi vào <code>[now() - range, now()]</code>, trả dạng <code>YYYY-MM-DD</code>. Đây là các mốc lịch sale lớn của sàn VN để biểu đồ đánh dấu.</li><li><strong>MUST</strong> trả JSON đúng hình dạng: <code>{product_id, range, maturity, daily:[{day, min_p, max_p, close_p}], annotations:{median90, trailing_min, verdict, accumulating, double_dates:[...]}}</code>.</li><li><strong>MUST</strong> đảm bảo p95 &lt;500ms (§3.8): chỉ chạm <code>price_daily</code> (bảng tổng hợp nhỏ) cho phần thân, chú giải tính trong bộ nhớ trên chính chuỗi daily đã đọc - không thêm vòng quét raw (DEC-DEAL-20).</li><li><strong>MUST</strong> trả <code>404</code> khi <code>product_id</code> không tồn tại trong <code>tracked_product</code>, phân biệt với <code>200</code> + <code>daily</code> rỗng khi SKU có thật nhưng chưa có snapshot nào.</li><li><strong>MUST</strong> trả mọi giá (<code>min_p</code>, <code>max_p</code>, <code>close_p</code>, <code>median90</code>, <code>trailing_min</code>) dưới dạng <code>BIGINT</code> VND (int64) trong payload - KHÔNG dùng float hay string (đồng nhất với DEC-PRICE-05 của TASK-PRICE-002).</li><li><strong>MUST</strong> xác thực qua JWT do API Gateway (TASK-INFRA-001) gắn; handler giả định đã qua middleware, không tự parse token. Request thiếu auth bị gateway chặn trước khi tới handler.</li><li><strong>SHOULD</strong> phát OTel: <code>chart_duration_ms</code> (histogram, nhãn <code>range</code>), <code>chart_requests_total{range, status, maturity}</code> (counter) để soi p95 theo từng range.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao tách feed biểu đồ riêng thay vì dùng TASK-PRICE-003?</strong> TASK-PRICE-003 trả chuỗi giá thô <code>{daily, tail}</code> - đường giá trần, ghép raw tail cho tươi tới phút, không chú giải. DEAL-003 trả <code>{daily, annotations}</code> - đã downsample về bucket ngày để vẽ và phủ thêm dải verdict, đường median90/trailing_min, mốc ngày đôi. Hai endpoint phục vụ hai nhu cầu khác nhau: PRICE-003 cho ai cần số giá nguyên bản (API tích hợp, B2B), DEAL-003 cho biểu đồ deal mà người dùng cuối nhìn để quyết \"mua hay đợi\". Trộn cả hai vào một endpoint làm hợp đồng rối và buộc client tự suy ra annotation.</p>\n<p><strong>Vì sao tính chú giải phía server (DEC-DEAL-21)?</strong> verdict sale ảo phải đồng nhất giữa thẻ sản phẩm và biểu đồ - nếu thẻ ghi SALE_XIN mà dải trên biểu đồ vẽ SALE_AO thì người dùng mất tin ngay. Gọi chung một nguồn (TASK-DEAL-001) đảm bảo một verdict. median90 và trailing_min cũng nên tính một lần ở server: client chỉ vẽ, không phải tải cả chuỗi raw rồi tự tính lại - vừa lệch, vừa nặng.</p>\n<p><strong>Vì sao mốc ngày đôi (DEC-DEAL-21)?</strong> Sàn VN chạy sale lớn vào các ngày trùng số (1.1, 2.2 ... 12.12). Đánh dấu các mốc này trên biểu đồ giúp người dùng đọc đúng bối cảnh: một cú giảm giá đúng ngày 12.12 là sale lịch chứ không phải tín hiệu hiếm, còn giá thấp giữa tháng thường thì đáng chú ý hơn. Đây là kiến thức lịch bản địa mà biểu đồ giá thuần không thể hiện được.</p>\n<p><strong>Vì sao bucket ngày đạt &lt;500ms (DEC-DEAL-20)?</strong> Biểu đồ không cần độ phân giải giây - cần min/max/close theo ngày để vẽ. Đọc <code>price_daily</code> (continuous aggregate đã tổng hợp, bảng nhỏ) cho phần thân thay vì quét raw <code>price_snapshot</code> (bảng tỷ dòng) là khác biệt giữa vài mili-giây và vài trăm mili-giây. Chú giải tính trên chính chuỗi daily đã nằm trong bộ nhớ nên gần như miễn phí.</p>\n<p><strong>Vì sao tôn trọng cổng độ chín TASK-DEAL-002 (DEC-DEAL-23)?</strong> verdict trên 3 điểm dữ liệu là rác. SKU mới &lt;14 ngày chưa đủ lịch sử để biết giá nền thật, nên trả <code>verdict=UNKNOWN</code> thay vì đoán liều. SKU WARMING (đủ vài tuần nhưng chưa đủ 90 ngày) vẫn vẽ được biểu đồ nhưng gắn cờ đang tích lũy để frontend hiển thị lưu ý. Đây là lời hứa cốt lõi của SănDeal: không kết luận ẩu.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Chú giải (Go)</h3>\n<pre><code class=\"language-go\">// services/deal/internal/chart/annotate.go\n\ntype DailyPoint struct {\n    Day    time.Time `json:\"day\"`\n    MinP   int64     `json:\"min_p\"`   // VND\n    MaxP   int64     `json:\"max_p\"`   // VND\n    CloseP int64     `json:\"close_p\"` // VND, giá hiển thị của điểm ngày\n}\n\ntype Annotations struct {\n    Median90     int64    `json:\"median90\"`     // VND, trung vị close_p 90 ngày\n    TrailingMin  int64    `json:\"trailing_min\"` // VND, đáy min_p trong khoảng\n    Verdict      string   `json:\"verdict\"`      // từ TASK-DEAL-001\n    Accumulating bool     `json:\"accumulating\"` // true khi maturity=WARMING\n    DoubleDates  []string `json:\"double_dates\"` // YYYY-MM-DD trong khoảng\n}\n\n// Build tính median90 + trailing_min từ chuỗi daily và sinh mốc ngày đôi (DEC-DEAL-21).\n// verdict + accumulating được nhồi bởi caller (chart.go) từ TASK-DEAL-001/002.\nfunc Build(daily []DailyPoint, from, to time.Time) Annotations {\n    return Annotations{\n        Median90:    median90(daily),\n        TrailingMin: trailingMin(daily),\n        DoubleDates: doubleDates(from, to),\n    }\n}\n\n// median90 lấy trung vị close_p của các điểm trong 90 ngày gần nhất.\nfunc median90(d []DailyPoint) int64 {\n    cut := time.Now().AddDate(0, 0, -90)\n    var v []int64\n    for _, p := range d {\n        if !p.Day.Before(cut) {\n            v = append(v, p.CloseP)\n        }\n    }\n    if len(v) == 0 {\n        return 0\n    }\n    sort.Slice(v, func(i, j int) bool { return v[i] &lt; v[j] })\n    return v[len(v)/2]\n}\n\n// trailingMin lấy đáy min_p trong toàn chuỗi được trả.\nfunc trailingMin(d []DailyPoint) int64 {\n    if len(d) == 0 {\n        return 0\n    }\n    m := d[0].MinP\n    for _, p := range d[1:] {\n        if p.MinP &lt; m {\n            m = p.MinP\n        }\n    }\n    return m\n}\n\n// doubleDates liệt kê các ngày dd==mm (1.1...12.12) rơi trong [from, to] (DEC-DEAL-21).\nfunc doubleDates(from, to time.Time) []string {\n    var out []string\n    for y := from.Year(); y &lt;= to.Year(); y++ {\n        for m := 1; m &lt;= 12; m++ {\n            d := time.Date(y, time.Month(m), m, 0, 0, 0, 0, time.UTC)\n            if !d.Before(from) &amp;&amp; !d.After(to) {\n                out = append(out, d.Format(\"2006-01-02\"))\n            }\n        }\n    }\n    return out\n}</code></pre>\n<h3>DTO + handler (Go)</h3>\n<pre><code class=\"language-go\">// services/deal/internal/api/chart.go\n\ntype ChartResponse struct {\n    ProductID   int64              `json:\"product_id\"`\n    Range       string             `json:\"range\"`\n    Maturity    string             `json:\"maturity\"` // MATURE | WARMING | NEW (TASK-DEAL-002)\n    Daily       []chart.DailyPoint `json:\"daily\"`\n    Annotations chart.Annotations  `json:\"annotations\"`\n}\n\nvar rangeWindows = map[string]time.Duration{\n    \"7d\": 7 * 24 * time.Hour, \"30d\": 30 * 24 * time.Hour,\n    \"90d\": 90 * 24 * time.Hour, \"180d\": 180 * 24 * time.Hour,\n    \"1y\": 365 * 24 * time.Hour,\n}\n\n// HandleChart phục vụ GET /v1/products/{id}/chart?range=90d.\nfunc (h *Handler) HandleChart(w http.ResponseWriter, req *http.Request) {\n    id, err := strconv.ParseInt(req.PathValue(\"id\"), 10, 64)\n    if err != nil {\n        writeErr(w, http.StatusBadRequest, \"invalid product id\")\n        return\n    }\n    rng := req.URL.Query().Get(\"range\")\n    if rng == \"\" {\n        rng = \"90d\"\n    }\n    window, ok := rangeWindows[rng]\n    if !ok {\n        writeErr(w, http.StatusBadRequest, \"invalid range\")\n        return\n    }\n    exists, err := h.repo.ProductExists(req.Context(), id)\n    if err != nil {\n        writeErr(w, http.StatusInternalServerError, \"internal error\")\n        return\n    }\n    if !exists {\n        writeErr(w, http.StatusNotFound, \"product not found\")\n        return\n    }\n    from, to := time.Now().Add(-window), time.Now()\n    daily, err := h.repo.QueryDaily(req.Context(), id, from) // đọc price_daily\n    if err != nil {\n        writeErr(w, http.StatusInternalServerError, \"internal error\")\n        return\n    }\n    ann := chart.Build(daily, from, to)\n    mat := h.deal.Maturity(req.Context(), id)        // TASK-DEAL-002\n    ann.Accumulating = mat == \"WARMING\"\n    ann.Verdict = h.deal.Verdict(req.Context(), id)  // TASK-DEAL-001\n    if mat == \"NEW\" {\n        ann.Verdict = \"UNKNOWN\" // &lt;14 ngày: không kết luận (DEC-DEAL-23)\n    }\n    w.Header().Set(\"Content-Type\", \"application/json; charset=utf-8\")\n    _ = json.NewEncoder(w).Encode(ChartResponse{\n        ProductID: id, Range: rng, Maturity: mat, Daily: daily, Annotations: ann,\n    })\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>GET /v1/products/abc/chart</code> (id không phải số) trả <code>400</code>.</li><li><code>range</code> ngoài allowlist (vd <code>range=5d</code>) trả <code>400</code> + <code>{\"error\":\"invalid range\"}</code>; thiếu <code>range</code> xử lý như <code>90d</code>.</li><li>Phần <code>daily</code> đến từ <code>price_daily</code> cho khoảng <code>now() - range</code>, sắp tăng theo <code>day</code>; không quét raw cho phần thân.</li><li><code>annotations.median90</code> = trung vị <code>close_p</code> 90 ngày; <code>annotations.trailing_min</code> = đáy <code>min_p</code> trong khoảng, cả hai là int64 VND.</li><li><code>annotations.verdict</code> lấy từ TASK-DEAL-001 (không tính lại); khớp nhãn thẻ sản phẩm.</li><li><code>maturity</code> lấy từ TASK-DEAL-002; <code>WARMING</code> kèm <code>annotations.accumulating=true</code>; <code>NEW</code> (&lt;14 ngày) trả <code>verdict=UNKNOWN</code>.</li><li><code>annotations.double_dates</code> chứa đúng các ngày dd==mm (1.1...12.12) rơi trong khoảng, dạng <code>YYYY-MM-DD</code>.</li><li>Response JSON đúng khóa <code>{product_id, range, maturity, daily[], annotations{median90, trailing_min, verdict, accumulating, double_dates[]}}</code>.</li><li>p95 của endpoint &lt;500ms trên SKU có lịch sử 1 năm (đo qua <code>chart_duration_ms</code>); chú giải tính trong bộ nhớ, không thêm vòng quét raw.</li><li><code>product_id</code> không có trong <code>tracked_product</code> trả <code>404</code>; SKU có thật chưa có snapshot trả <code>200</code> + <code>daily</code> rỗng.</li><li>Mọi giá trong JSON là số nguyên VND (int64), không float, không string.</li><li>Request đã qua JWT gateway tới được handler; handler không tự xác thực token; metric <code>chart_requests_total{range, status, maturity}</code> tăng đúng.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/deal/internal/api/chart_test.go\n\nfunc TestChart_Default90d(t *testing.T) {\n    h, pid := setupWithHistory(t, 120) // 120 ngày dữ liệu\n    rec := doGET(t, h, \"/v1/products/\"+itoa(pid)+\"/chart\") // không range\n    require.Equal(t, 200, rec.Code)\n    var body ChartResponse\n    decode(t, rec, &amp;body)\n    require.Equal(t, \"90d\", body.Range)\n    require.LessOrEqual(t, len(body.Daily), 91) // ~90 ngày, không phải 120\n}\n\nfunc TestChart_Annotations_MedianTrailingMin(t *testing.T) {\n    h, pid := setupWithHistory(t, 90)\n    seedDailyCloses(t, h, pid, []int64{120_000, 100_000, 80_000, 100_000}) // đáy 80k\n    rec := doGET(t, h, \"/v1/products/\"+itoa(pid)+\"/chart?range=90d\")\n    var body ChartResponse\n    decode(t, rec, &amp;body)\n    require.Equal(t, int64(100_000), body.Annotations.Median90)    // trung vị\n    require.Equal(t, int64(80_000), body.Annotations.TrailingMin)  // đáy\n}\n\nfunc TestChart_DoubleDateMarkers(t *testing.T) {\n    h, pid := setupWithRange(t, \"2026-04-01\", \"2026-06-27\") // chứa 4.4, 5.5, 6.6\n    rec := doGET(t, h, \"/v1/products/\"+itoa(pid)+\"/chart?range=90d\")\n    var body ChartResponse\n    decode(t, rec, &amp;body)\n    require.Contains(t, body.Annotations.DoubleDates, \"2026-04-04\")\n    require.Contains(t, body.Annotations.DoubleDates, \"2026-05-05\")\n    require.NotContains(t, body.Annotations.DoubleDates, \"2026-03-03\") // ngoài khoảng\n}\n\nfunc TestChart_MaturityFlag_Warming(t *testing.T) {\n    h, pid := setupWithMaturity(t, \"WARMING\", 40) // 40 ngày: đủ vẽ, chưa đủ 90\n    rec := doGET(t, h, \"/v1/products/\"+itoa(pid)+\"/chart?range=90d\")\n    var body ChartResponse\n    decode(t, rec, &amp;body)\n    require.Equal(t, \"WARMING\", body.Maturity)\n    require.True(t, body.Annotations.Accumulating) // cờ đang tích lũy\n}\n\nfunc TestChart_UnknownProduct_404(t *testing.T) {\n    h, _ := setupWithHistory(t, 30)\n    rec := doGET(t, h, \"/v1/products/999999/chart?range=30d\")\n    require.Equal(t, 404, rec.Code)\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>annotate.go</code> (median90 + trailing_min + double_dates thuần, dễ unit-test) -&gt; <code>chart.go</code> (DTO + handler: parse range, ProductExists, đọc price_daily, ghép verdict TASK-DEAL-001 + maturity TASK-DEAL-002) -&gt; đăng ký route trong <code>router.go</code> sau JWT middleware của gateway (TASK-INFRA-001) -&gt; tests. Handler dùng <code>http.ServeMux</code> của Go 1.22 với pattern <code>GET /v1/products/{id}/chart</code> và <code>req.PathValue(\"id\")</code>. Không thêm caching ở slice này; đọc price_daily + tính chú giải trong bộ nhớ đã đủ cho p95 &lt;500ms.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-PRICE-002</strong> - cung cấp continuous aggregate <code>price_daily</code> cho phần thân biểu đồ. Đây là điều kiện cứng (depends_on).</li><li><strong>TASK-DEAL-001 (sibling)</strong> - nguồn verdict sale ảo (<code>SALE_AO</code>/<code>SALE_XIN</code>/<code>TAM_DUOC</code>); DEAL-003 gọi để gắn vào <code>annotations.verdict</code>, không tự tính.</li><li><strong>TASK-DEAL-002 (sibling)</strong> - cổng độ chín dữ liệu; DEAL-003 lấy <code>maturity</code> và áp quy tắc WARMING/NEW.</li><li><strong>TASK-INFRA-001 (gateway)</strong> - gắn JWT auth trước handler; route nằm sau middleware này.</li><li><strong>TASK-WEB-003 (downstream)</strong> - biểu đồ giá có chú giải tiêu thụ response này; hình dạng JSON ở §3 là hợp đồng với nó.</li><li>Lib: <code>pgx</code> (driver), <code>encoding/json</code>, <code>net/http</code> (ServeMux Go 1.22), <code>sort</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Request</h3>\n<pre><code>curl -s -H \"Authorization: Bearer $JWT\" \\\n  \"https://api.sandeal.vn/v1/products/90112/chart?range=90d\"</code></pre>\n<h3>Response (200)</h3>\n<pre><code class=\"language-json\">{\n  \"product_id\": 90112,\n  \"range\": \"90d\",\n  \"maturity\": \"MATURE\",\n  \"daily\": [\n    { \"day\": \"2026-03-29T00:00:00Z\", \"min_p\": 149000, \"max_p\": 159000, \"close_p\": 149000 },\n    { \"day\": \"2026-04-04T00:00:00Z\", \"min_p\": 119000, \"max_p\": 149000, \"close_p\": 119000 },\n    { \"day\": \"2026-05-05T00:00:00Z\", \"min_p\": 109000, \"max_p\": 139000, \"close_p\": 109000 },\n    { \"day\": \"2026-06-26T00:00:00Z\", \"min_p\": 99000,  \"max_p\": 119000, \"close_p\": 99000 }\n  ],\n  \"annotations\": {\n    \"median90\": 129000,\n    \"trailing_min\": 99000,\n    \"verdict\": \"TAM_DUOC\",\n    \"accumulating\": false,\n    \"double_dates\": [\"2026-04-04\", \"2026-05-05\", \"2026-06-06\"]\n  }\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Cache lớp HTTP (ETag theo <code>product_id</code> + ngày + range) nếu QPS biểu đồ cao - slice sau, đo trước.</li><li>Tham số <code>?points=</code> để downsample điểm ngày cho khoảng <code>1y</code> trên màn hình nhỏ - tối ưu UI giai đoạn sau.</li><li>Dải verdict theo từng đoạn thời gian (band per khoảng) thay vì một verdict tổng - cân nhắc khi UI biểu đồ cần tô màu nhiều vùng.</li><li>Trả kèm <code>currency</code> khi mở SEA (THB/IDR) - bám theo <code>tracked_product</code>, giữ giá int64 theo minor unit từng nước.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>id không phải số</td><td><code>ParseInt</code> lỗi</td><td>400</td><td>Frontend chỉ gửi id số (theo route biểu đồ)</td></tr><tr><td>range ngoài allowlist</td><td>tra map ok=false</td><td>400 invalid range</td><td>UI chỉ render các nút 7d/30d/90d/180d/1y</td></tr><tr><td>product_id không tồn tại</td><td><code>ProductExists</code> false</td><td>404</td><td>Phân biệt với 200-rỗng (SKU mới chưa có snapshot)</td></tr><tr><td>SKU có thật, chưa có snapshot</td><td>query trả 0 dòng</td><td>200 + daily rỗng</td><td>UI hiện \"đang thu thập dữ liệu giá\"</td></tr><tr><td>quét raw toàn khoảng (bug)</td><td>p95 query metric vọt</td><td>vỡ &lt;500ms</td><td>Chỉ đọc price_daily, chú giải tính trong bộ nhớ</td></tr><tr><td>verdict client lệch verdict thẻ</td><td>so chéo nhãn</td><td>mất tin người dùng</td><td>Một nguồn verdict duy nhất TASK-DEAL-001 (DEC-DEAL-21)</td></tr><tr><td>SKU NEW bị gắn verdict ẩu</td><td>maturity=NEW</td><td>kết luận sai trên ít dữ liệu</td><td>Ép <code>verdict=UNKNOWN</code> khi &lt;14 ngày (DEC-DEAL-23)</td></tr><tr><td>TASK-DEAL-002 chưa sẵn lúc gọi</td><td>maturity rỗng</td><td>thiếu cờ tích lũy</td><td>Mặc định an toàn: coi như WARMING tới khi đủ 90 ngày</td></tr><tr><td>median90 trên chuỗi rỗng</td><td>guard len==0</td><td>trả 0 thay vì panic</td><td><code>median90</code>/<code>trailingMin</code> trả 0 khi không có điểm</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li><strong>Phân biệt với TASK-PRICE-003:</strong> DEAL-003 và PRICE-003 đều phục vụ biểu đồ nhưng khác hợp đồng. PRICE-003 trả <code>{daily, tail}</code> - chuỗi giá thô, ghép raw tail cho tươi tới phút, KHÔNG chú giải; dành cho ai cần số giá nguyên bản. DEAL-003 trả <code>{daily, annotations}</code> - đã downsample về bucket ngày để vẽ và phủ thêm verdict sale ảo, đường median90/trailing_min, mốc ngày đôi; dành cho biểu đồ deal mà người dùng cuối đọc để quyết mua hay đợi. Một bên là feed thô, một bên là feed đã trang trí cho rendering.</li><li>Chú giải tính phía server cho một verdict đồng nhất giữa thẻ và biểu đồ, và client chỉ vẽ chứ không tải cả chuỗi raw rồi tự tính.</li><li>Mốc ngày đôi là kiến thức lịch sale bản địa VN - biểu đồ giá thuần không thể hiện được bối cảnh này.</li><li>Đọc <code>price_daily</code> cho phần thân tách tốc độ (bảng tổng hợp nhỏ) khỏi nhu cầu chú giải, giữ p95 &lt;500ms mà không quét raw cả khoảng.</li><li>Cổng độ chín TASK-DEAL-002 là rào chắn chống kết luận ẩu: NEW trả UNKNOWN, WARMING gắn cờ tích lũy - đúng lời hứa cốt lõi của SănDeal.</li><li>Giá luôn là int64 VND suốt đường đi từ DB tới JSON, không có bước chuyển float nào để tránh sai số.</li></ul>\n<hr>\n<p><em>Hết TASK-DEAL-003. Status: done (mục tiêu audit 10/10).</em></p>";
