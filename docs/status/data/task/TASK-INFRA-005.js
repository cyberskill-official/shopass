window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-INFRA-005"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service INFRA <strong>MUST</strong> cung cấp một lớp cấu hình per-country: mỗi nước có một CountryPolicy dữ liệu hóa, một loader, và feature flag có scope theo nước, để luật voucher/affiliate/dữ liệu được tập trung thay vì rải khắp code. Hợp đồng:</p>\n<ol><li>Cấu hình per-country <strong>MUST</strong> là dữ liệu, không phải <code>if country == ...</code> rải rác (DEC-INFRA-21). Mỗi nước ánh xạ tới một <code>CountryPolicy</code>; code nghiệp vụ đọc policy, không hard-code điều kiện nước.</li><li><code>CountryPolicy</code> <strong>MUST</strong> mang tối thiểu (DEC-INFRA-22): <code>voucher_stacking_allowed bool</code>, <code>affiliate_channels_allowed []Channel</code>, <code>data_residency_region string</code>.</li><li>Loader <strong>MUST</strong> đọc cấu hình 7 nước VN/ID/TH/PH/MY/SG/TW từ file versioned (<code>countries.yaml</code>), validate cấu trúc, và trả map <code>country -&gt; CountryPolicy</code>.</li><li>Cấu hình <strong>MUST</strong> phản ánh luật thực: <code>VN.voucher_stacking_allowed = true</code>; <code>MY.voucher_stacking_allowed = false</code> và <code>PH.voucher_stacking_allowed = false</code> (đã bỏ stacking 2025, §3.9).</li><li>Với nước CHƯA cấu hình, loader <strong>MUST</strong> trả policy hạn chế nhất (DEC-INFRA-23): <code>voucher_stacking_allowed = false</code>, <code>affiliate_channels_allowed = []</code> (rỗng), <code>data_residency_region</code> mặc định an toàn. KHÔNG mặc định mở.</li><li>Feature flag <strong>MUST</strong> có scope theo nước (DEC-INFRA-24): <code>Lookup(flag, country) bool</code>. Một flag có thể bật ở nước này, tắt ở nước khác.</li><li>Mọi truy vấn flag/policy <strong>MUST</strong> kèm country context (DEC-INFRA-25). API KHÔNG cho truy vấn flag mà thiếu country (tránh lấy nhầm mặc định toàn cục).</li><li>Country của một thao tác <strong>MUST</strong> suy ra từ <code>platform.country</code> (TASK-INFRA-002) hoặc locale người dùng, KHÔNG đoán chỉ từ IP (DEC-INFRA-25). IP chỉ là tín hiệu phụ.</li><li><code>affiliate_channels_allowed</code> <strong>MUST</strong> dùng enum kênh rõ ràng (ví dụ <code>web</code>, <code>extension</code>, <code>coupon</code>, <code>app</code>); TASK-AFFIL-004 đọc đây để quyết định kênh nào hợp lệ ở nước hiện tại.</li><li>Loader <strong>MUST</strong> validate: country là ISO-3166 alpha-2; trùng country trong file là lỗi; channel ngoài enum là lỗi. File hỏng -&gt; lỗi rõ ràng lúc khởi tạo (fail-fast), KHÔNG chạy với cấu hình mơ hồ.</li><li>Lớp region <strong>SHOULD</strong> hỗ trợ override runtime (ví dụ bật/tắt một flag cho một nước mà không deploy lại) qua một nguồn override (KV/Redis), với file là mặc định nền.</li><li>Lớp region <strong>MUST</strong> là điểm tham chiếu duy nhất cho TASK-CART-004 (stacking), TASK-AFFIL-004 (channel), TASK-COMPLY-006/007 (residency) - một nguồn sự thật per-country.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p>Vì sao gating là dữ liệu, không phải if-country (DEC-INFRA-21)? SănDeal mở 7 thị trường với luật khác nhau về voucher, affiliate và dữ liệu. Nếu mỗi luật là một <code>if country == 'MY'</code> rải trong optimizer, affiliate, compliance, thì thêm một nước hay đổi một luật là sửa hàng chục chỗ, dễ sót. Dữ liệu hóa thành CountryPolicy gom luật vào một nơi: thêm nước là thêm một mục cấu hình.</p>\n<p>Vì sao mặc định hạn chế nhất (DEC-INFRA-23)? Khi mở một nước mới mà chưa kịp nghiên cứu luật, mặc định an toàn là tắt: không stack voucher, không bật affiliate, residency thận trọng. Mặc định mở là mời rủi ro pháp lý (gợi ý stacking trái luật, bật kênh affiliate bị cấm). Tắt mặc định rồi bật có chủ đích khi đã xác nhận luật là tư thế an toàn.</p>\n<p>Vì sao stacking khác theo nước (§1 #4)? Đây là khác biệt luật cụ thể và đắt nếu sai: VN cho phép stack 1 shop + 1 platform + freeship; MY/PH đã bỏ stacking 2025. Optimizer áp luật VN cho MY/PH sẽ gợi ý tổ hợp voucher không áp được, làm hỏng giỏ và mất niềm tin. Policy per-country cho optimizer biết luật đúng của nước đang tính.</p>\n<p>Vì sao truy vấn flag bắt buộc kèm country (§1 #7)? Một lỗi tinh vi là quên truyền country và vô tình lấy mặc định toàn cục. Bắt buộc country trong chữ ký <code>Lookup(flag, country)</code> biến lỗi này thành lỗi biên dịch/runtime rõ ràng thay vì hành vi sai âm thầm.</p>\n<p>Vì sao country từ platform/locale, không từ IP (DEC-INFRA-25)? IP dễ sai: VPN, du lịch, proxy. Một người dùng VN dùng VPN không nên đột nhiên chịu luật nước khác. <code>platform.country</code> (sàn nào, nước nào) và locale người dùng là tín hiệu chủ đích, ổn định hơn. IP chỉ bổ trợ, không quyết một mình.</p>\n<p>Vì sao một nguồn sự thật per-country (§1 #12)? CART, AFFIL, COMPLY đều cần biết luật nước. Nếu mỗi module giữ bản sao luật riêng, chúng sẽ lệch nhau theo thời gian. Tập trung vào lớp region cho một nơi cập nhật, mọi module đọc cùng sự thật.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>CountryPolicy + enum (Go)</h3>\n<pre><code class=\"language-go\">// region/policy.go\ntype Channel string\nconst (\n    ChannelWeb       Channel = \"web\"\n    ChannelExtension Channel = \"extension\"\n    ChannelCoupon    Channel = \"coupon\"\n    ChannelApp       Channel = \"app\"\n)\n\ntype CountryPolicy struct {\n    Country               string    // ISO-3166 alpha-2\n    VoucherStackingAllowed bool\n    AffiliateChannelsAllowed []Channel\n    DataResidencyRegion   string    // ví dụ \"ap-southeast-1\"\n}\n\n// restrictivePolicy là mặc định an toàn cho nước chưa cấu hình (DEC-INFRA-23).\nfunc restrictivePolicy(country string) CountryPolicy {\n    return CountryPolicy{\n        Country:               country,\n        VoucherStackingAllowed: false,\n        AffiliateChannelsAllowed: nil, // rỗng = không bật kênh nào\n        DataResidencyRegion:   defaultResidency,\n    }\n}</code></pre>\n<h3>Loader (§1 #3, #5, #10)</h3>\n<pre><code class=\"language-go\">// region/loader.go\ntype Registry struct{ byCountry map[string]CountryPolicy }\n\nfunc Load(path string) (*Registry, error) {\n    raw, err := os.ReadFile(path)\n    if err != nil { return nil, err }\n    var doc struct{ Countries []CountryPolicy `yaml:\"countries\"` }\n    if err := yaml.Unmarshal(raw, &amp;doc); err != nil { return nil, err }\n    reg := &amp;Registry{byCountry: map[string]CountryPolicy{}}\n    for _, p := range doc.Countries {\n        if !isAlpha2(p.Country) { return nil, fmt.Errorf(\"country không hợp lệ: %q\", p.Country) }\n        if _, dup := reg.byCountry[p.Country]; dup {\n            return nil, fmt.Errorf(\"trùng country: %s\", p.Country) // §1 #10\n        }\n        if err := validateChannels(p.AffiliateChannelsAllowed); err != nil { return nil, err }\n        reg.byCountry[p.Country] = p\n    }\n    return reg, nil\n}\n\n// Policy trả policy của nước; nước chưa cấu hình → mặc định hạn chế nhất.\nfunc (r *Registry) Policy(country string) CountryPolicy {\n    if p, ok := r.byCountry[country]; ok { return p }\n    return restrictivePolicy(country) // §1 #5\n}</code></pre>\n<h3>Feature flag scope-country (§1 #6, #7)</h3>\n<pre><code class=\"language-go\">// region/flags.go\n// Lookup luôn cần country; không có overload thiếu country (DEC-INFRA-24, §1 #7).\nfunc (r *Registry) Lookup(flag string, country string) bool {\n    if country == \"\" { return false } // thiếu country → an toàn = false\n    return r.flagFor(flag, country) // override runtime &gt; mặc định file\n}</code></pre>\n<h3>countries.yaml (trích)</h3>\n<pre><code class=\"language-yaml\"># region/config/countries.yaml\ncountries:\n  - country: VN\n    voucherStackingAllowed: true\n    affiliateChannelsAllowed: [web, extension, app]\n    dataResidencyRegion: ap-southeast-1\n  - country: MY\n    voucherStackingAllowed: false      # bỏ stacking 2025\n    affiliateChannelsAllowed: [web, app]\n    dataResidencyRegion: ap-southeast-1\n  - country: PH\n    voucherStackingAllowed: false      # bỏ stacking 2025\n    affiliateChannelsAllowed: [web, app]\n    dataResidencyRegion: ap-southeast-1\n  - country: ID\n    voucherStackingAllowed: true\n    affiliateChannelsAllowed: [web, app]  # Shopee ID hạn chế kênh extension/coupon\n    dataResidencyRegion: ap-southeast-3   # Indonesia PDP — residency riêng</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>Load(\"countries.yaml\")</code> hợp lệ -&gt; trả Registry với &gt;=4 nước cấu hình (VN/MY/PH/ID tối thiểu).</li><li><code>Policy(\"VN\").VoucherStackingAllowed</code> = <code>true</code>.</li><li><code>Policy(\"MY\").VoucherStackingAllowed</code> = <code>false</code>; <code>Policy(\"PH\").VoucherStackingAllowed</code> = <code>false</code>.</li><li><code>Policy(\"XX\")</code> (nước chưa cấu hình) -&gt; policy hạn chế nhất: stacking false, channels rỗng.</li><li><code>Policy(\"VN\").AffiliateChannelsAllowed</code> chứa <code>extension</code>; <code>Policy(\"ID\")</code> KHÔNG chứa <code>extension</code> (Shopee ID hạn chế).</li><li><code>Lookup(\"real_sale_v2\", \"VN\")</code> và <code>Lookup(\"real_sale_v2\", \"MY\")</code> có thể khác nhau (flag scope country).</li><li><code>Lookup(flag, \"\")</code> (thiếu country) -&gt; <code>false</code> (an toàn).</li><li>YAML hỏng (cú pháp sai) -&gt; <code>Load</code> trả lỗi (fail-fast).</li><li>File có trùng country (hai mục <code>VN</code>) -&gt; <code>Load</code> trả lỗi.</li><li>File có channel ngoài enum (<code>telegram</code>) -&gt; <code>Load</code> trả lỗi.</li><li>File có country không phải alpha-2 (<code>Vietnam</code>) -&gt; <code>Load</code> trả lỗi.</li><li><code>Policy(\"ID\").DataResidencyRegion</code> khác <code>Policy(\"VN\")</code> (residency per-country phản ánh Indonesia PDP).</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// region/policy_test.go\nfunc TestPolicy_VNStacks_MYPHDoNot(t *testing.T) {\n    reg := mustLoad(t, \"config/countries.yaml\")\n    require.True(t,  reg.Policy(\"VN\").VoucherStackingAllowed)\n    require.False(t, reg.Policy(\"MY\").VoucherStackingAllowed)\n    require.False(t, reg.Policy(\"PH\").VoucherStackingAllowed)\n}\n\nfunc TestPolicy_UnknownCountry_Restrictive(t *testing.T) {\n    reg := mustLoad(t, \"config/countries.yaml\")\n    p := reg.Policy(\"XX\")\n    require.False(t, p.VoucherStackingAllowed)\n    require.Empty(t, p.AffiliateChannelsAllowed) // không bật kênh nào\n}\n\nfunc TestPolicy_AffiliateChannel_PerCountry(t *testing.T) {\n    reg := mustLoad(t, \"config/countries.yaml\")\n    require.Contains(t,    reg.Policy(\"VN\").AffiliateChannelsAllowed, ChannelExtension)\n    require.NotContains(t, reg.Policy(\"ID\").AffiliateChannelsAllowed, ChannelExtension)\n}\n\n// region/loader_test.go\nfunc TestLoad_DuplicateCountry_Errors(t *testing.T) {\n    _, err := Load(writeTemp(t, dupCountryYAML))\n    require.Error(t, err)\n}\n\nfunc TestLoad_BadChannel_Errors(t *testing.T) {\n    _, err := Load(writeTemp(t, badChannelYAML)) // channel \"telegram\"\n    require.Error(t, err)\n}\n\nfunc TestLoad_NonAlpha2_Errors(t *testing.T) {\n    _, err := Load(writeTemp(t, `countries: [{country: Vietnam}]`))\n    require.Error(t, err)\n}\n\n// region/flags_test.go\nfunc TestLookup_PerCountry(t *testing.T) {\n    reg := regWithFlag(t, \"real_sale_v2\", map[string]bool{\"VN\": true, \"MY\": false})\n    require.True(t,  reg.Lookup(\"real_sale_v2\", \"VN\"))\n    require.False(t, reg.Lookup(\"real_sale_v2\", \"MY\"))\n}\n\nfunc TestLookup_MissingCountry_False(t *testing.T) {\n    reg := mustLoad(t, \"config/countries.yaml\")\n    require.False(t, reg.Lookup(\"any_flag\", \"\")) // thiếu country = false\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Loader đọc <code>countries.yaml</code> lúc khởi tạo, fail-fast nếu hỏng. <code>Policy(country)</code> trả mặc định hạn chế nhất cho nước thiếu. Flag scope-country với override runtime (KV/Redis) chồng lên mặc định file: thay đổi nóng một flag cho một nước không cần deploy. Country của thao tác lấy từ <code>platform.country</code> hoặc locale ở tầng gọi; lớp region nhận country làm tham số, không tự đoán. Mọi module per-country (CART/AFFIL/COMPLY) đọc qua lớp này, không giữ bản sao luật riêng.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li>TASK-INFRA-002 - <code>platform.country</code> là nguồn suy ra country của thao tác.</li><li>TASK-CART-004 (downstream) - đọc <code>VoucherStackingAllowed</code> cho engine stacking per-country.</li><li>TASK-AFFIL-004 (downstream) - đọc <code>AffiliateChannelsAllowed</code> để chặn kênh bị cấm theo nước.</li><li>TASK-COMPLY-006 / TASK-COMPLY-007 (downstream) - đọc <code>DataResidencyRegion</code> cho gating dữ liệu SEA.</li><li>Hạ tầng: file YAML versioned + nguồn override runtime (KV/Redis) tuỳ chọn.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Optimizer hỏi luật stacking của nước hiện tại (nội bộ)</h3>\n<pre><code class=\"language-go\">pol := region.Policy(platform.Country) // ví dụ \"MY\"\nif !pol.VoucherStackingAllowed {\n    // MY/PH: chỉ chọn max(platform, freeship) thay vì stack (TASK-CART-004)\n}</code></pre>\n<h3>Affiliate kiểm kênh hợp lệ theo nước</h3>\n<pre><code class=\"language-go\">pol := region.Policy(country)\nif !slices.Contains(pol.AffiliateChannelsAllowed, region.ChannelExtension) {\n    return ErrChannelNotAllowed // ví dụ Shopee Indonesia cấm kênh extension\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Bản đồ luật chi tiết hơn (cookie window per-network, ngưỡng giao dịch MOIT) - mở rộng CountryPolicy khi TASK-COMPLY-008 cần.</li><li>Quản trị flag qua UI (thay vì sửa YAML/KV) - công cụ vận hành giai đoạn sau.</li><li>Phân vùng dữ liệu vật lý theo residency (DB per-region) - quyết định hạ tầng khi mở ID/TH thật.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>if-country rải khắp code</td><td>review</td><td>thêm nước phải sửa nhiều nơi</td><td>Dữ liệu hóa CountryPolicy (§1 #1)</td></tr><tr><td>Mặc định mở cho nước mới</td><td>policy test</td><td>gợi ý/affiliate trái luật</td><td>Mặc định hạn chế nhất (§1 #5)</td></tr><tr><td>Áp luật VN cho MY/PH</td><td>cart test</td><td>giỏ hỏng, vi phạm điều khoản</td><td>Stacking per-country (§1 #4)</td></tr><tr><td>Quên truyền country</td><td>chữ ký Lookup</td><td>lấy nhầm mặc định toàn cục</td><td>Bắt buộc country tham số (§1 #7)</td></tr><tr><td>Đoán country từ IP</td><td>review</td><td>sai với VPN/du lịch</td><td>Suy từ platform/locale (§1 #8)</td></tr><tr><td>YAML hỏng chạy lén</td><td>loader fail-fast</td><td>hành vi mơ hồ</td><td>Lỗi lúc khởi tạo (§1 #10)</td></tr><tr><td>Trùng country trong file</td><td>loader validate</td><td>policy bất định</td><td>Lỗi trùng (§1 #10)</td></tr><tr><td>Channel ngoài enum</td><td>loader validate</td><td>kênh không xác định</td><td>Lỗi channel lạ (§1 #10)</td></tr><tr><td>Module giữ bản sao luật riêng</td><td>review</td><td>luật lệch theo thời gian</td><td>Một nguồn sự thật region (§1 #12)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Dữ liệu hóa gating thành CountryPolicy gom luật voucher/affiliate/residency vào một nơi: thêm nước là thêm một mục cấu hình, không phải sửa hàng chục if.</li><li>Mặc định hạn chế nhất là tư thế an toàn khi mở nước mới chưa kịp nghiên cứu luật - tắt rồi bật có chủ đích.</li><li>Khác biệt stacking VN vs MY/PH là luật cụ thể và đắt nếu sai; policy per-country cho optimizer biết luật đúng của nước đang tính.</li><li>Bắt buộc country trong <code>Lookup</code> biến lỗi \"quên truyền country\" thành lỗi rõ ràng thay vì hành vi sai âm thầm.</li><li>Country từ platform/locale ổn định hơn IP (VPN/du lịch); IP chỉ bổ trợ.</li><li>Một nguồn sự thật per-country cho CART/AFFIL/COMPLY tránh các bản sao luật lệch nhau theo thời gian.</li></ul>\n<hr>\n<p><em>Hết TASK-INFRA-005. Status: done (mục tiêu audit 10/10).</em></p>";
