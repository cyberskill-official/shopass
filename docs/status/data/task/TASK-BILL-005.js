window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-BILL-005"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service BILL <strong>MUST</strong> thực thi feature gating ở backend theo subscription tier (không tin client), với quyền lợi tier ở bảng <code>plan_feature</code>, giữ tính năng miễn phí lõi không bị khóa cứng, và phát tín hiệu trigger upgrade đúng lúc mà không ép buộc. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> định nghĩa bảng <code>plan_feature (id, tier, feature_key, limit_value)</code>: ánh xạ mỗi <code>tier</code> (TASK-BILL-001) tới giới hạn của từng <code>feature_key</code> (DEC-BILL-22). <code>limit_value = -1</code> nghĩa không giới hạn (unlimited); <code>0</code> nghĩa không có quyền.</li><li><strong>MUST</strong> thực thi gating ở BACKEND (DEC-BILL-21): hàm <code>Allow(ctx, userID, feature_key)</code> đọc tier hiện tại qua TASK-BILL-001 <code>GetActive</code> rồi tra <code>plan_feature</code>. KHÔNG tin bất kỳ cờ tier/quyền nào do client gửi.</li><li><strong>MUST</strong> giữ tính năng miễn phí lõi KHÔNG bị khóa cứng (DEC-BILL-23): theo dõi giá, phát hiện sale ảo (TASK-DEAL-001), biểu đồ giá (TASK-DEAL-003) phải khả dụng cho tier <code>free</code>. Gating chỉ áp GIỚI HẠN (số lượng/tần suất) và tính năng NÂNG CAO, không chặn giá trị lõi.</li><li><strong>MUST</strong> so sánh mức dùng hiện tại với <code>limit_value</code> cho feature có đếm (ví dụ số wishlist, số sản phẩm theo dõi, dự đoán đáy nâng cao): dùng <code>&lt; limit_value</code> -&gt; cho phép; <code>&gt;= limit_value</code> (và <code>limit_value &gt;= 0</code>) -&gt; từ chối với mã xác định <code>ErrLimitReached</code>. Khi đọc tier/<code>plan_feature</code> lỗi, <code>Allow</code> <strong>MUST</strong> fail-safe coi user như <code>free</code> (DEC-BILL-25): an toàn nghiêng về \"ít quyền hơn\", KHÔNG cấp nhầm Premium.</li><li><strong>MUST</strong> đặt quyền lợi tier ở <code>plan_feature</code> làm nguồn sự thật (DEC-BILL-22): KHÔNG hardcode giới hạn per-tier rải rác trong code. Đổi quyền lợi là cập nhật <code>plan_feature</code>.</li><li><strong>MUST</strong> trả mã lỗi xác định khi vượt giới hạn (<code>ErrLimitReached</code>) để tầng API ánh xạ sang một phản hồi rõ (ví dụ <code>402 Payment Required</code> hoặc <code>403</code> kèm gợi ý nâng cấp), KHÔNG để lỗi thô lọt ra.</li><li><strong>MUST</strong> đánh giá trigger upgrade là tín hiệu gợi ý (DEC-BILL-24): khi user chạm giới hạn hoặc thử dùng tính năng Premium, <code>triggers.go</code> trả một tín hiệu <code>{trigger, suggested_tier}</code> cho client hiển thị CTA nâng cấp - tín hiệu, KHÔNG tự thu tiền, KHÔNG ép.</li><li>Trigger upgrade <strong>MUST NOT</strong> dùng dark pattern (DEC-BILL-24): không che nút đóng, không hù dọa, không ép quyết định ngay. CTA là một gợi ý người dùng có thể bỏ qua và tiếp tục dùng free.</li><li><strong>MUST</strong> seed <code>plan_feature</code> idempotent với quyền lợi free + 3 bậc Premium qua <code>ON CONFLICT (tier, feature_key) DO NOTHING</code>; tính năng lõi để <code>free</code> ở mức khả dụng (limit hợp lý hoặc unlimited).</li><li><strong>MUST</strong> expose:</li></ol>\n<ul><li><code>Allow(ctx, userID int64, featureKey string) (bool, error)</code> - quyết định gating.</li><li><code>LimitFor(ctx, tier, featureKey string) (int64, error)</code> - đọc giới hạn.</li><li><code>EvaluateTriggers(ctx, userID int64, event UsageEvent) (*UpgradeSignal, error)</code> - tín hiệu CTA.</li></ul>\n<ol><li>Gating <strong>MUST</strong> tách bạch \"có quyền không\" (boolean/limit) khỏi \"thanh toán\" (TASK-BILL-002/003): <code>Allow</code> chỉ đọc tier hiện tại; nâng cấp thật đi qua checkout. <code>Allow</code> không tự kích hoạt subscription.</li><li><strong>SHOULD</strong> phát OTel: <code>feature_gate_denied_total{feature_key}</code> (counter), <code>upgrade_trigger_shown_total{trigger}</code> (counter), <code>feature_gate_eval_total{feature_key, decision}</code> (counter).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao gating ở backend, không tin client (DEC-BILL-21)?</strong> Nếu client tự quyết \"user này là Premium\" qua một cờ, ai cũng sửa được cờ đó để mở Premium miễn phí - thất thoát doanh thu. Quyền lợi phải được quyết ở nơi không sửa được từ ngoài: backend đọc subscription thật (TASK-BILL-001) rồi tra quyền. Client chỉ hiển thị theo những gì backend cho phép, không tự cấp quyền.</p>\n<p><strong>Vì sao không khóa cứng tính năng lõi (DEC-BILL-23)?</strong> Chiến lược của SănDeal ở thị trường WTP thấp là free-tier mạnh thu hút đông người dùng, tài trợ bằng affiliate, rồi một phần nhỏ (2-5%) lên Premium (§4.3). Nếu khóa theo dõi giá/sale ảo/biểu đồ sau paywall, ta phá chính sức hút đó - người dùng bỏ đi, mất cả base lẫn affiliate. Gating đúng là áp giới hạn (số wishlist, tần suất alert) và tính năng nâng cao, để giá trị lõi luôn miễn phí.</p>\n<p><strong>Vì sao quyền lợi ở plan_feature, không hardcode (DEC-BILL-22)?</strong> Quyền lợi từng tier sẽ được tinh chỉnh nhiều lần khi tìm điểm cân bằng free/paid. Rải <code>if tier == \"premium_basic\" { limit = 50 }</code> khắp code làm mỗi lần điều chỉnh phải sửa nhiều nơi và dễ lệch. Một bảng ánh xạ (tier, feature_key, limit) là nguồn sự thật; thử nghiệm quyền lợi là cập nhật dữ liệu.</p>\n<p><strong>Vì sao fail-safe về free (DEC-BILL-25)?</strong> Khi đọc tier lỗi (DB chập), có hai lựa chọn sai: coi user là Premium (cấp nhầm quyền, thất thoát) hoặc free (giới hạn nhầm người đã trả). Nghiêng về free an toàn hơn về mặt doanh thu - tệ nhất là một Premium tạm bị giới hạn (sửa được khi DB hồi), không phải cấp Premium miễn phí hàng loạt. An toàn nghiêng về \"ít quyền\".</p>\n<p><strong>Vì sao trigger là tín hiệu, không ép (DEC-BILL-24, §1 #8)?</strong> Niềm tin là moat của SănDeal (hậu-Honey). Dark pattern ép upgrade (che nút đóng, hù dọa) thu được vài chuyển đổi ngắn hạn nhưng phá niềm tin dài hạn. Trigger đúng lúc (user vừa chạm giới hạn, đang thấy giá trị) là gợi ý lịch sự người dùng bỏ qua được - chuyển đổi từ giá trị thật, không từ ép buộc.</p>\n<p><strong>Vì sao tách gating khỏi thanh toán (§1 #11)?</strong> <code>Allow</code> chỉ trả lời \"user hiện có quyền không\" dựa subscription hiện tại. Nó không nên tự thu tiền hay kích hoạt Premium - đó là việc của checkout (TASK-BILL-002) và IPN (TASK-BILL-003). Tách bạch giữ gating đơn giản, dễ test, và tránh lẫn \"kiểm quyền\" với \"đổi quyền\".</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/bill/migrations/0005_plan_feature.sql\nCREATE TABLE plan_feature (\n  id          BIGSERIAL PRIMARY KEY,\n  tier        TEXT     NOT NULL\n                CHECK (tier IN ('free','premium_basic','premium_plus','premium_pro')),\n  feature_key TEXT     NOT NULL,\n  limit_value BIGINT   NOT NULL,   -- -1 = unlimited; 0 = không có quyền; &gt;0 = giới hạn\n  UNIQUE (tier, feature_key)\n);\n\n-- Tính năng lõi: free vẫn khả dụng (KHÔNG khóa cứng) (§1 #3)\nINSERT INTO plan_feature (tier, feature_key, limit_value) VALUES\n  ('free',          'price_tracking',   -1),   -- theo dõi giá: unlimited cho free\n  ('free',          'fake_sale_detect', -1),   -- sale ảo: free\n  ('free',          'price_chart',      -1),   -- biểu đồ: free\n  ('free',          'wishlist_items',   20),   -- giới hạn số wishlist item (gating mềm)\n  ('free',          'bottom_predict',    0),   -- dự đoán đáy nâng cao: chỉ Premium\n  ('premium_basic', 'wishlist_items',  100),\n  ('premium_basic', 'bottom_predict',   -1),\n  ('premium_plus',  'wishlist_items',  500),\n  ('premium_plus',  'bottom_predict',   -1),\n  ('premium_pro',   'wishlist_items',   -1),\n  ('premium_pro',   'bottom_predict',   -1)\nON CONFLICT (tier, feature_key) DO NOTHING;</code></pre>\n<h3>Gate (Go)</h3>\n<pre><code class=\"language-go\">// services/bill/internal/gating/gate.go\n\n// Allow quyết định gating ở backend theo subscription tier (§1 #2).\n// Fail-safe: lỗi đọc tier -&gt; coi như free (§1 #4).\nfunc (g *Gate) Allow(ctx context.Context, userID int64, featureKey string) (bool, error) {\n    tier := \"free\"\n    if sub, ok, err := g.subs.GetActive(ctx, userID); err == nil &amp;&amp; ok {\n        tier = g.plans.TierOf(sub.PlanID)\n    } // lỗi/không active -&gt; giữ \"free\" (fail-safe, §1 #4)\n\n    limit, err := g.repo.LimitFor(ctx, tier, featureKey)\n    if err != nil {\n        return false, nil // fail-safe: từ chối an toàn thay vì cấp nhầm\n    }\n    switch {\n    case limit == 0:\n        return false, nil          // không có quyền (§1 #1)\n    case limit &lt; 0:\n        return true, nil           // unlimited\n    default:\n        used, err := g.repo.CountUsage(ctx, userID, featureKey)\n        if err != nil { return false, nil }\n        return used &lt; limit, nil   // còn dưới giới hạn (§1 #4)\n    }\n}</code></pre>\n<h3>Triggers (Go)</h3>\n<pre><code class=\"language-go\">// services/bill/internal/gating/triggers.go\n\ntype UpgradeSignal struct {\n    Trigger       string `json:\"trigger\"`        // ví dụ \"wishlist_limit_reached\"\n    SuggestedTier string `json:\"suggested_tier\"` // ví dụ \"premium_basic\"\n}\n\n// EvaluateTriggers trả tín hiệu CTA gợi ý (KHÔNG ép, KHÔNG thu tiền) (§1 #7,#8).\nfunc (g *Gate) EvaluateTriggers(ctx context.Context, userID int64, ev UsageEvent) (*UpgradeSignal, error) {\n    allowed, _ := g.Allow(ctx, userID, ev.FeatureKey)\n    if !allowed &amp;&amp; ev.FeatureKey == \"wishlist_items\" {\n        return &amp;UpgradeSignal{Trigger: \"wishlist_limit_reached\", SuggestedTier: \"premium_basic\"}, nil\n    }\n    if ev.FeatureKey == \"bottom_predict\" &amp;&amp; !allowed {\n        return &amp;UpgradeSignal{Trigger: \"premium_feature_touch\", SuggestedTier: \"premium_basic\"}, nil\n    }\n    return nil, nil // không có tín hiệu -&gt; không hiển thị gì\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch -&gt; <code>plan_feature</code> tồn tại với UNIQUE <code>(tier, feature_key)</code> + CHECK tier.</li><li>Seed -&gt; tính năng lõi (<code>price_tracking</code>, <code>fake_sale_detect</code>, <code>price_chart</code>) có <code>limit_value = -1</code> cho <code>free</code> (không khóa cứng).</li><li><code>Allow(free_user, \"price_tracking\")</code> -&gt; <code>true</code> (tính năng lõi luôn khả dụng cho free).</li><li><code>Allow(free_user, \"wishlist_items\")</code> khi đã có 20 item (đạt giới hạn free) -&gt; <code>false</code> (<code>ErrLimitReached</code> ở tầng trên).</li><li><code>Allow(premium_basic_user, \"wishlist_items\")</code> khi có 50 item -&gt; <code>true</code> (giới hạn 100).</li><li><code>Allow(free_user, \"bottom_predict\")</code> -&gt; <code>false</code> (limit 0, chỉ Premium); <code>Allow(premium_basic_user, \"bottom_predict\")</code> -&gt; <code>true</code>.</li><li>Client gửi cờ <code>tier=premium_pro</code> trong request -&gt; bị bỏ qua; <code>Allow</code> vẫn dùng subscription thật từ backend (free user vẫn bị giới hạn).</li><li>Lỗi đọc tier (mock subs fail) -&gt; <code>Allow</code> coi như free (fail-safe), không cấp nhầm Premium.</li><li>Đổi <code>plan_feature.limit_value</code> cho một tier -&gt; hành vi gating đổi theo, KHÔNG cần sửa code (nguồn sự thật là bảng).</li><li><code>EvaluateTriggers(free_user, wishlist khi đầy)</code> -&gt; trả <code>UpgradeSignal{trigger:\"wishlist_limit_reached\"}</code>; khi chưa đầy -&gt; <code>nil</code> (không CTA).</li><li><code>EvaluateTriggers</code> chỉ trả tín hiệu; KHÔNG kích hoạt subscription hay thu tiền (kiểm không gọi checkout).</li><li>Metric <code>feature_gate_denied_total{feature_key}</code> tăng khi từ chối; <code>upgrade_trigger_shown_total</code> tăng khi phát tín hiệu.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/bill/internal/gating/gate_test.go\nfunc TestAllow_CoreFeatureFreeUser(t *testing.T) {\n    g, free := setupFreeUser(t)\n    for _, f := range []string{\"price_tracking\", \"fake_sale_detect\", \"price_chart\"} {\n        ok, _ := g.Allow(ctx, free, f)\n        require.True(t, ok, \"tính năng lõi %s phải khả dụng cho free\", f) // §1 #3\n    }\n}\n\nfunc TestAllow_WishlistLimit_Free(t *testing.T) {\n    g, free := setupFreeUser(t)\n    seedWishlistItems(t, free, 20) // đạt giới hạn free (20)\n    ok, _ := g.Allow(ctx, free, \"wishlist_items\")\n    require.False(t, ok) // &gt;= limit -&gt; chặn\n}\n\nfunc TestAllow_WishlistLimit_Premium(t *testing.T) {\n    g, prem := setupPremiumUser(t, \"premium_basic\")\n    seedWishlistItems(t, prem, 50)\n    ok, _ := g.Allow(ctx, prem, \"wishlist_items\")\n    require.True(t, ok) // giới hạn 100, mới 50\n}\n\nfunc TestAllow_PremiumOnlyFeature(t *testing.T) {\n    g, free := setupFreeUser(t)\n    ok, _ := g.Allow(ctx, free, \"bottom_predict\")\n    require.False(t, ok) // limit 0 cho free\n    g2, prem := setupPremiumUser(t, \"premium_basic\")\n    ok2, _ := g2.Allow(ctx, prem, \"bottom_predict\")\n    require.True(t, ok2)\n}\n\nfunc TestAllow_IgnoresClientTierFlag(t *testing.T) {\n    g, free := setupFreeUser(t)\n    // request \"tự xưng\" premium_pro không ảnh hưởng - Allow đọc subscription thật\n    ok, _ := g.Allow(withClaimedTier(ctx, \"premium_pro\"), free, \"bottom_predict\")\n    require.False(t, ok) // vẫn free (§1 #2)\n}\n\nfunc TestAllow_FailSafeFree(t *testing.T) {\n    g, prem := setupPremiumUserButSubsErrors(t) // đọc tier lỗi\n    ok, _ := g.Allow(ctx, prem, \"bottom_predict\")\n    require.False(t, ok) // fail-safe coi như free (§1 #4)\n}</code></pre>\n<pre><code class=\"language-go\">// services/bill/internal/gating/triggers_test.go\nfunc TestTriggers_WishlistFull_ShowsCTA(t *testing.T) {\n    g, free := setupFreeUser(t)\n    seedWishlistItems(t, free, 20)\n    sig, _ := g.EvaluateTriggers(ctx, free, UsageEvent{FeatureKey: \"wishlist_items\"})\n    require.NotNil(t, sig)\n    require.Equal(t, \"wishlist_limit_reached\", sig.Trigger)\n}\n\nfunc TestTriggers_NoSignalWhenWithinLimit(t *testing.T) {\n    g, free := setupFreeUser(t)\n    seedWishlistItems(t, free, 5)\n    sig, _ := g.EvaluateTriggers(ctx, free, UsageEvent{FeatureKey: \"wishlist_items\"})\n    require.Nil(t, sig) // chưa chạm giới hạn -&gt; không CTA (§1 #8)\n}\n\nfunc TestTriggers_DoesNotChargeOrActivate(t *testing.T) {\n    g, free := setupFreeUser(t)\n    seedWishlistItems(t, free, 20)\n    g.EvaluateTriggers(ctx, free, UsageEvent{FeatureKey: \"wishlist_items\"})\n    require.Equal(t, 0, g.checkoutCalls()) // chỉ tín hiệu, không thu tiền (§1 #11)\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration <code>0005_plan_feature.sql</code> (ánh xạ tier-&gt;feature-&gt;limit + seed; tính năng lõi free unlimited) -&gt; <code>repo.go</code> (<code>LimitFor</code>, <code>CountUsage</code>) -&gt; <code>gate.go</code> (<code>Allow</code> đọc tier TASK-BILL-001 + tra plan_feature + fail-safe free) -&gt; <code>triggers.go</code> (<code>EvaluateTriggers</code> trả tín hiệu CTA) -&gt; tests. <code>Allow</code> dùng làm middleware/kiểm trong các handler có giới hạn (ví dụ TASK-TRACK-002 wishlist gọi <code>Allow</code> trước khi thêm). Quyền lợi đổi bằng cập nhật <code>plan_feature</code>, không sửa code. Nâng cấp thật đi qua checkout (TASK-BILL-002), không phải <code>Allow</code>.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-BILL-001</strong> - <code>subscription</code> + <code>GetActive</code> + <code>plan_catalog</code> (tier); gating đọc tier hiện tại từ đây.</li><li><strong>TASK-BILL-003 (liên quan)</strong> - subscription chuyển <code>active</code> sau payment paid; gating phản ánh tier mới ngay.</li><li><strong>TASK-INFRA-001 (gateway)</strong> - gắn JWT + <code>user_id</code> cho các handler dùng <code>Allow</code>.</li><li><strong>TASK-TRACK-002</strong> - wishlist là feature có giới hạn (<code>wishlist_items</code>); gọi <code>Allow</code> trước khi thêm item.</li><li><strong>TASK-DEAL-004</strong> - dự đoán đáy nâng cao (<code>bottom_predict</code>) là tính năng Premium; gating kiểm tier.</li><li>Lib: <code>pgx</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Kiểm gating khi thêm wishlist item (nội bộ, TASK-TRACK-002 gọi)</h3>\n<pre><code class=\"language-go\">ok, _ := gate.Allow(ctx, userID, \"wishlist_items\")\nif !ok {\n    // tầng API ánh xạ sang 402/403 + gợi ý nâng cấp\n    sig, _ := gate.EvaluateTriggers(ctx, userID, gating.UsageEvent{FeatureKey: \"wishlist_items\"})\n    writeUpgradeHint(w, sig) // client hiển thị CTA, user bỏ qua được\n    return\n}</code></pre>\n<h3>Tín hiệu upgrade trả cho client (gợi ý, không ép)</h3>\n<pre><code class=\"language-json\">{\n  \"trigger\": \"wishlist_limit_reached\",\n  \"suggested_tier\": \"premium_basic\"\n}</code></pre>\n<h3>Quyền lợi tier trong plan_feature</h3>\n<pre><code class=\"language-sql\">SELECT tier, feature_key, limit_value FROM plan_feature WHERE feature_key='wishlist_items' ORDER BY limit_value;\n--  free          | wishlist_items | 20\n--  premium_basic | wishlist_items | 100\n--  premium_plus  | wishlist_items | 500\n--  premium_pro   | wishlist_items | -1   (unlimited)</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>A/B test ngưỡng giới hạn free (20 wishlist hợp lý chưa) - điều chỉnh qua <code>plan_feature</code> khi có dữ liệu conversion; schema cho phép đổi không sửa code.</li><li>Gamification sâu hơn (điểm, streak, mở khóa tạm tính năng Premium) - thêm lớp trên trigger; task này là nền gating + tín hiệu.</li><li>Giới hạn theo tần suất thời gian (ví dụ X alert/ngày cho free) - thêm feature_key dạng rate + cửa sổ; hiện limit là đếm tổng.</li><li>Hiển thị CTA cụ thể (copy, vị trí) - thuộc TASK-WEB/TASK-MOBILE; backend chỉ trả tín hiệu trung lập.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Tin cờ tier client</td><td>ignores-client-flag test</td><td>Premium miễn phí qua sửa cờ</td><td>Gating ở backend, đọc subscription thật (§1 #2)</td></tr><tr><td>Khóa cứng tính năng lõi</td><td>core-feature test</td><td>phá free-tier, đuổi user</td><td>Lõi unlimited cho free (§1 #3)</td></tr><tr><td>Hardcode giới hạn rải rác</td><td>review + đổi-bảng test</td><td>đổi quyền lợi lệch</td><td>plan_feature nguồn sự thật (§1 #5)</td></tr><tr><td>Lỗi tier cấp nhầm Premium</td><td>fail-safe test</td><td>thất thoát</td><td>Fail-safe về free (§1 #4)</td></tr><tr><td>Dark pattern ép upgrade</td><td>review (§1 #8)</td><td>tổn hại niềm tin</td><td>CTA bỏ qua được, không ép (§1 #8)</td></tr><tr><td>Lỗi limit thô lọt API</td><td>mã lỗi xác định</td><td>UX xấu</td><td>ErrLimitReached -&gt; 402/403 (§1 #6)</td></tr><tr><td>Gating tự thu tiền</td><td>no-charge test</td><td>lẫn kiểm quyền với đổi quyền</td><td>Tách Allow khỏi checkout (§1 #11)</td></tr><tr><td>Vượt giới hạn không chặn</td><td>wishlist-limit test</td><td>Premium value rò cho free</td><td>So used &lt; limit (§1 #4)</td></tr><tr><td>Seed quyền lợi nhân đôi</td><td>ON CONFLICT</td><td>giữ một dòng/cặp</td><td>Idempotent seed (§1 #9)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Gating + upgrade trigger là cơ chế chuyển free-&gt;paid (conversion 2-5%, §4.1) - trụ thu tiền của mô hình free-tier mạnh.</li><li>Gating ở backend (đọc subscription thật) đóng lỗ hổng \"client tự xưng Premium\"; client chỉ hiển thị theo backend cho phép.</li><li>Tính năng lõi (theo dõi giá/sale ảo/biểu đồ) KHÔNG khóa cứng - free-tier mạnh là sức hút ở thị trường WTP thấp (§4.3).</li><li>Quyền lợi ở <code>plan_feature</code> cho phép A/B test ngưỡng bằng cập nhật dữ liệu, không sửa code.</li><li>Fail-safe về free khi lỗi: an toàn nghiêng về \"ít quyền\", không cấp nhầm Premium hàng loạt.</li><li>Trigger là tín hiệu lịch sự bỏ qua được, không dark pattern - bảo vệ moat niềm tin.</li><li>Tách gating khỏi thanh toán: <code>Allow</code> kiểm quyền, checkout (TASK-BILL-002) đổi quyền.</li></ul>\n<hr>\n<p><em>Hết TASK-BILL-005. Status: done (mục tiêu audit 10/10).</em></p>";
