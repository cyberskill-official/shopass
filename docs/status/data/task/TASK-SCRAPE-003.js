window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SCRAPE-003"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Farm headless <strong>MUST</strong> chạy Playwright với fingerprint nhất quán và hành vi giống người, expose <code>RenderPrice</code> cho adapter fallback. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> sinh fingerprint profile nhất quán nội bộ (DEC-SCRAPE-10): <code>userAgent</code>, <code>platform</code>, <code>languages</code>, <code>timezone</code>, <code>screen</code> (width/height/dpr), <code>hardwareConcurrency</code>, <code>deviceMemory</code>, WebGL <code>vendor</code>/<code>renderer</code> - mọi trường phải đồng bộ (ví dụ timezone <code>Asia/Ho_Chi_Minh</code> &lt;-&gt; <code>languages</code> chứa <code>vi-VN</code>).</li><li><strong>MUST</strong> spoof readback fingerprint qua <code>addInitScript</code>/CDP để cùng một profile cho cùng giá trị Canvas hash, WebGL parameters, AudioContext fingerprint giữa các lần render (không random mỗi request - bất nhất cũng là cờ đỏ).</li><li><strong>MUST</strong> ẩn dấu vết automation: <code>navigator.webdriver=false</code>, không để lộ biến CDP/Playwright, các thuộc tính <code>window.chrome</code> hợp lý cho profile.</li><li><strong>MUST</strong> khớp TLS fingerprint (JA3/JA4) và HTTP/2 SETTINGS với trình duyệt thật của profile (DEC-SCRAPE-11) - cipher order, ALPN, HTTP/2 pseudo-header order, SETTINGS frame phải giống Chrome/Firefox thật, không để lộ dấu Go/Node default client. Đặc biệt cần cho Akamai của Lazada (§3.9).</li><li><strong>MUST</strong> gắn một fingerprint profile với một proxy session (DEC-SCRAPE-12): profile có timezone/locale VN phải đi cùng IP residential VN của TASK-SCRAPE-004; KHÔNG trộn timezone nước này với IP nước khác.</li><li><strong>MUST</strong> chèn hành vi giống người trước khi trích dữ liệu (DEC-SCRAPE-13): di chuyển chuột theo đường cong, scroll, thời gian dừng đọc (dwell) ngẫu nhiên trong khoảng người-thật, không bắn request và trích DOM tức thì.</li><li><strong>MUST</strong> expose <code>RenderPrice(ctx, job ScrapeJob) (PriceSnapshot, error)</code> (DEC-SCRAPE-14): mở trang sản phẩm, chạy behavior, trích giá từ DOM render, trả <code>PriceSnapshot</code> cho adapter gọi fallback (TASK-SCRAPE-002/007/008).</li><li><strong>MUST</strong> phát hiện khi bị challenge (CAPTCHA/slider/verify page) và trả error phân loại <code>ErrChallenged</code> để TASK-SCRAPE-005 xử lý CAPTCHA hoặc orchestrator backoff, KHÔNG trả snapshot rỗng coi như giá hợp lệ.</li><li><strong>MUST</strong> giới hạn vòng đời context/page và dọn tài nguyên (đóng context sau render) để tránh rò bộ nhớ trên farm dài hạn.</li><li><strong>SHOULD</strong> xoay vòng profile pool: chọn profile ít dùng gần đây, không tái dùng cùng một (fingerprint + IP) liên tục cho cùng một sàn trong cửa sổ ngắn.</li><li><strong>SHOULD</strong> phát OTel metric: <code>farm_render_total{platform, outcome}</code> (counter), <code>farm_detected_total{platform, kind}</code> (counter, kind=captcha|block|tls), <code>farm_render_duration_ms{platform}</code> (histogram).</li><li><strong>MUST</strong> không lưu trữ trang thô; chỉ trích các trường giá rồi đóng trang (tối thiểu hóa dữ liệu, §5.4).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao fingerprint nhất quán nội bộ (DEC-SCRAPE-10)?</strong> Anti-bot không chỉ chặn \"headless\"; nó chấm điểm mâu thuẫn. Một trình duyệt khai timezone <code>Asia/Ho_Chi_Minh</code> nhưng <code>languages=en-US</code>, screen lạ, WebGL vendor không khớp UA là cờ đỏ. Profile phải tự nhất quán như một thiết bị thật, không phải một mớ trường random.</p>\n<p><strong>Vì sao spoof readback ổn định (§1 #2)?</strong> Fingerprinting đọc Canvas/WebGL/AudioContext để tạo hash thiết bị (§3.9 nói rõ Shopee làm vậy). Nếu hash đổi mỗi request, server thấy \"một người dùng\" có thiết bị biến hình từng giây - bất thường rõ rệt. Spoof phải ổn định cho mỗi profile, đổi khi đổi profile.</p>\n<p><strong>Vì sao khớp TLS/HTTP2 (DEC-SCRAPE-11)?</strong> Akamai (Lazada) và nhiều WAF đọc JA3/JA4 ở tầng bắt tay TLS, trước cả khi JS chạy. Client Go/Node mặc định có chữ ký TLS khác hẳn Chrome thật - bị nhận diện ngay dù JS có patch hoàn hảo. Phải khớp cipher order, ALPN, HTTP/2 SETTINGS để qua tầng này.</p>\n<p><strong>Vì sao gắn fingerprint với proxy session (DEC-SCRAPE-12)?</strong> IP residential mang theo geolocation. Một profile VN (timezone, ngôn ngữ VN) đi qua IP Mỹ là mâu thuẫn không thể giải thích. Buộc cặp (profile &lt;-&gt; proxy cùng nước) giữ câu chuyện nhất quán từ tầng mạng tới tầng trình duyệt.</p>\n<p><strong>Vì sao hành vi giống người (DEC-SCRAPE-13)?</strong> Hệ thống hành vi (behavioral) đo nhịp tương tác. Tải trang rồi đọc giá trong 50ms không phải hành vi người. Mouse move, scroll, dwell time đưa farm vào phân bố hành vi người-thật, phối hợp với pacing/jitter của TASK-SCRAPE-005.</p>\n<p><strong>Vì sao tách ErrChallenged (§1 #8)?</strong> Khi bị challenge, trả \"không có giá\" làm bẩn dữ liệu và làm orchestrator coi như parse fail. Phân loại challenge rõ ràng để TASK-SCRAPE-005 quyết định giải CAPTCHA hay lùi, và để metric <code>farm_detected_total</code> cảnh báo sớm khi tỷ lệ challenge tăng.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Fingerprint profile (TypeScript)</h3>\n<pre><code class=\"language-typescript\">// services/scrape/farm/src/fingerprint.ts\nexport interface FingerprintProfile {\n  userAgent: string;\n  platform: string;            // 'Win32' | 'Linux x86_64' | ...\n  languages: string[];         // ['vi-VN','vi','en-US']\n  timezone: string;            // 'Asia/Ho_Chi_Minh'\n  screen: { width: number; height: number; dpr: number };\n  hardwareConcurrency: number;\n  deviceMemory: number;\n  webgl: { vendor: string; renderer: string };\n  canvasNoiseSeed: number;     // seed ổn định cho readback spoof của profile\n}\n\n// Tạo profile nhất quán: timezone, languages, UA, WebGL khớp nhau theo nước.\nexport function makeProfile(country: 'VN' | 'ID' | 'TH', seed: number): FingerprintProfile;\n\n// Kiểm tra nội nhất quán (dùng trong test + guard runtime).\nexport function isCoherent(p: FingerprintProfile): boolean;</code></pre>\n<h3>Patch trình duyệt (TypeScript)</h3>\n<pre><code class=\"language-typescript\">// services/scrape/farm/src/browser.ts\nexport async function newPatchedContext(\n  browser: Browser, p: FingerprintProfile, proxy: ProxySession,\n): Promise&lt;BrowserContext&gt; {\n  const ctx = await browser.newContext({\n    userAgent: p.userAgent,\n    locale: p.languages[0],\n    timezoneId: p.timezone,\n    viewport: { width: p.screen.width, height: p.screen.height },\n    deviceScaleFactor: p.screen.dpr,\n    proxy: { server: proxy.url, username: proxy.user, password: proxy.pass },\n  });\n  await ctx.addInitScript(spoofScript(p)); // navigator.webdriver=false, Canvas/WebGL/AudioContext spoof\n  return ctx;\n}</code></pre>\n<h3>Render fallback (TypeScript)</h3>\n<pre><code class=\"language-typescript\">// services/scrape/farm/src/render.ts\nexport class ChallengedError extends Error {}\n\nexport async function renderPrice(\n  ctx: BrowserContext, job: ScrapeJob, sel: PriceSelectors,\n): Promise&lt;PriceSnapshot&gt; {\n  const page = await ctx.newPage();\n  try {\n    await page.goto(job.url, { waitUntil: 'domcontentloaded' });\n    if (await detectChallenge(page)) throw new ChallengedError('captcha/slider/verify');\n    await humanize(page);                       // behavior.ts: mouse/scroll/dwell\n    const raw = await extractDom(page, sel);    // đọc price/list_price/stock/flash\n    return toSnapshot(job.productId, raw);      // quy đổi VND số nguyên, set ts\n  } finally {\n    await page.close();                         // dọn tài nguyên (§1 #9)\n  }\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>makeProfile('VN', seed)</code> trả profile có <code>timezone='Asia/Ho_Chi_Minh'</code> và <code>languages</code> chứa <code>vi-VN</code>; <code>isCoherent</code> = true.</li><li><code>isCoherent</code> trả false khi timezone và locale mâu thuẫn (ví dụ timezone VN + <code>languages=['en-US']</code> không kèm vi).</li><li>Cùng một profile (cùng seed) -&gt; Canvas readback hash giống nhau giữa 2 lần render; profile khác -&gt; hash khác.</li><li>Context đã patch: <code>navigator.webdriver === false</code> và không lộ biến automation (kiểm qua page evaluate).</li><li>WebGL <code>UNMASKED_VENDOR/RENDERER</code> trả đúng giá trị của profile, không phải giá trị mặc định của môi trường CI.</li><li>TLS client của farm tạo JA3/JA4 khớp profile mục tiêu (so với chữ ký Chrome thật trong fixture), khác chữ ký Node/Go default.</li><li>Profile VN luôn được ghép với proxy session nước VN; ghép sai nước bị từ chối (guard <code>bindProfileProxy</code>).</li><li><code>renderPrice</code> chạy <code>humanize</code> (có mouse move/scroll/dwell) TRƯỚC khi <code>extractDom</code> (kiểm thứ tự qua spy).</li><li>Trang challenge (fixture có slider/verify) -&gt; <code>renderPrice</code> ném <code>ChallengedError</code>, không trả snapshot.</li><li>DOM giá hợp lệ -&gt; <code>renderPrice</code> trả <code>PriceSnapshot</code> với giá VND đúng (số nguyên) và <code>flash_sale</code> đúng.</li><li>Page được đóng sau mỗi render (kiểm không rò context/page).</li><li>Metric <code>farm_render_total{outcome}</code> và <code>farm_detected_total{kind}</code> tăng đúng theo nhánh.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-typescript\">// services/scrape/farm/src/__tests__/fingerprint.test.ts\ntest('profile VN nhất quán timezone/locale', () =&gt; {\n  const p = makeProfile('VN', 42);\n  expect(p.timezone).toBe('Asia/Ho_Chi_Minh');\n  expect(p.languages).toContain('vi-VN');\n  expect(isCoherent(p)).toBe(true);\n});\n\ntest('mâu thuẫn timezone vs locale bị bắt', () =&gt; {\n  const p = { ...makeProfile('VN', 1), languages: ['en-US'] };\n  expect(isCoherent(p)).toBe(false);\n});\n\ntest('Canvas readback ổn định theo seed', async () =&gt; {\n  const a = await canvasHash(makeProfile('VN', 7));\n  const b = await canvasHash(makeProfile('VN', 7));\n  const c = await canvasHash(makeProfile('VN', 8));\n  expect(a).toBe(b);          // cùng profile -&gt; cùng hash\n  expect(a).not.toBe(c);      // khác profile -&gt; khác hash\n});</code></pre>\n<pre><code class=\"language-typescript\">// services/scrape/farm/src/__tests__/behavior.test.ts\ntest('humanize chạy trước extract', async () =&gt; {\n  const calls: string[] = [];\n  const page = fakePage({\n    onHumanize: () =&gt; calls.push('humanize'),\n    onExtract: () =&gt; calls.push('extract'),\n  });\n  await renderPrice(ctxFor(page), job('https://shopee.vn/x'), selectors);\n  expect(calls).toEqual(['humanize', 'extract']); // không trích thô tức thì\n});\n\n// services/scrape/farm/src/__tests__/render.test.ts\ntest('trang challenge ném ChallengedError', async () =&gt; {\n  const page = fakePage({ challenge: true });\n  await expect(renderPrice(ctxFor(page), job('u'), selectors))\n    .rejects.toBeInstanceOf(ChallengedError);\n});\n\ntest('webdriver ẩn sau patch', async () =&gt; {\n  const ctx = await newPatchedContext(browser, makeProfile('VN', 1), proxyVN);\n  const page = await ctx.newPage();\n  const wd = await page.evaluate(() =&gt; (navigator as any).webdriver);\n  expect(wd).toBe(false);\n});</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: fingerprint.ts (profile + isCoherent, test trước) -&gt; browser.ts (context + spoof script) -&gt; tuning TLS/HTTP2 -&gt; behavior.ts (humanize) -&gt; render.ts (RenderPrice + detectChallenge) -&gt; tests. Spoof script chèn qua <code>addInitScript</code> chạy trước script trang. Profile pool và binding proxy session ở config.ts; mỗi (profile &lt;-&gt; proxy) cố định theo nước. Selectors giá per-sàn đến từ adapter gọi (Shopee/TikTok/Lazada).</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-SCRAPE-001</strong> - orchestrator gọi adapter; adapter gọi farm khi fallback; kiểu <code>ScrapeJob</code>.</li><li><strong>TASK-SCRAPE-002 / 007 / 008</strong> - các adapter gọi <code>RenderPrice</code> của farm khi gặp challenge / sàn buộc DOM-render.</li><li><strong>TASK-SCRAPE-004</strong> - proxy session residential gắn theo profile (cùng nước).</li><li><strong>TASK-SCRAPE-005 (downstream)</strong> - <code>ChallengedError</code> chuyển sang xử lý CAPTCHA + pacing.</li><li><strong>TASK-PRICE-002 (downstream)</strong> - snapshot từ farm ghi qua <code>InsertSnapshot</code> delta-only.</li><li>Lib: Playwright, undici/boringssl tuning cho TLS, CDP cho spoof readback.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Profile VN sinh ra (rút gọn)</h3>\n<pre><code class=\"language-json\">{\n  \"userAgent\": \"Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 ... Chrome/...\",\n  \"platform\": \"Win32\",\n  \"languages\": [\"vi-VN\", \"vi\", \"en-US\"],\n  \"timezone\": \"Asia/Ho_Chi_Minh\",\n  \"screen\": { \"width\": 1920, \"height\": 1080, \"dpr\": 1 },\n  \"hardwareConcurrency\": 8,\n  \"deviceMemory\": 8,\n  \"webgl\": { \"vendor\": \"Google Inc. (NVIDIA)\", \"renderer\": \"ANGLE (NVIDIA ...)\" }\n}</code></pre>\n<h3>Adapter gọi farm khi fallback (nội bộ)</h3>\n<pre><code class=\"language-typescript\">// trong ShopeeAdapter khi nhận HTML challenge (TASK-SCRAPE-002 #6)\nconst snap = await farm.renderPrice(ctx, job, shopeeSelectors);\n// farm tự chọn profile + proxy VN, chạy humanize, trích giá, trả PriceSnapshot</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Thư viện anti-detect cụ thể (patch tự viết vs giải pháp thương mại) - đánh giá khi đo <code>farm_detected_total</code> thực tế.</li><li>Mức độ giống người của behavior (mô hình hóa quỹ đạo chuột) - bắt đầu đơn giản, nâng theo tỷ lệ phát hiện.</li><li>Pool profile lớn tới đâu để không tái dùng cặp (fp+IP) quá dày - tinh chỉnh theo quy mô farm.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Playwright mặc định lộ headless</td><td><code>farm_detected_total</code> cao</td><td>ban tức thì</td><td>Patch fingerprint bắt buộc (§1 #1-#3)</td></tr><tr><td>Hash Canvas đổi mỗi request</td><td>property test</td><td>bất nhất -&gt; cờ đỏ</td><td>Spoof ổn định theo seed (§1 #2)</td></tr><tr><td>JA3/JA4 lộ Node/Go client</td><td>test TLS</td><td>Akamai chặn (Lazada)</td><td>Khớp TLS/HTTP2 profile (§1 #4)</td></tr><tr><td>Profile VN + IP nước khác</td><td>guard bindProfileProxy</td><td>mâu thuẫn geo</td><td>Cặp (fp &lt;-&gt; proxy) cùng nước (§1 #5)</td></tr><tr><td>Trích DOM tức thì không behavior</td><td>spy thứ tự</td><td>hành vi bot</td><td>humanize trước extract (§1 #6)</td></tr><tr><td>Challenge coi như \"không giá\"</td><td><code>ChallengedError</code></td><td>dữ liệu bẩn</td><td>Phân loại + chuyển TASK-SCRAPE-005 (§1 #8)</td></tr><tr><td>Rò context/page</td><td>mem profiler</td><td>farm phình RAM</td><td>Đóng page trong finally (§1 #9)</td></tr><tr><td>Tái dùng cặp fp+IP quá dày</td><td>tần suất render</td><td>cụm bị ban chùm</td><td>Xoay profile pool (§1 #10)</td></tr><tr><td>Lưu nhầm trang thô</td><td>review code</td><td>rủi ro dữ liệu</td><td>Chỉ trích trường giá (§1 #12)</td></tr><tr><td>Selector DOM đổi (A/B)</td><td>render fail tăng</td><td>parse hụt</td><td>TASK-SCRAPE-006 giám sát + cập nhật selectors</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Farm là lớp chống ban tốn công nhất và là điều kiện để Lazada/TikTok có dữ liệu (cả hai buộc DOM-render, §3.2).</li><li>Anti-bot chấm điểm mâu thuẫn, không chỉ chặn headless: nhất quán (timezone&lt;-&gt;locale&lt;-&gt;WebGL&lt;-&gt;IP) quan trọng hơn từng trick lẻ.</li><li>TLS/HTTP2 fingerprint nằm dưới JS - phải khớp trước khi script trang chạy, đặc biệt với Akamai của Lazada.</li><li>Farm là đường đắt; adapter ưu tiên JSON (TASK-SCRAPE-002) và chỉ gọi farm khi buộc, để giữ unit economics (§4.1).</li><li>Behavior người-thật ở đây phối hợp với pacing/jitter của TASK-SCRAPE-005 - hai lớp chống hành vi-bot bổ trợ nhau.</li></ul>\n<hr>\n<p><em>Hết TASK-SCRAPE-003. Status: done (mục tiêu audit 10/10).</em></p>";
