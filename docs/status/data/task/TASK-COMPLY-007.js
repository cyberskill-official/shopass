window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-COMPLY-007"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service COMPLY <strong>SHOULD</strong> cung cấp lớp adapter bảo vệ dữ liệu SEA: mỗi chế độ (regime) là một adapter khai báo khác biệt so với baseline PDPL VN, được chọn theo <code>data_protection_regime</code> của TASK-COMPLY-006, áp dụng cho nước đã mở. Hợp đồng:</p>\n<ol><li><strong>SHOULD</strong> định nghĩa interface <code>RegimeAdapter</code> trả <code>RegimeProfile</code> - tập tham số tuân thủ theo nước: yêu cầu consent (granularity, ngôn ngữ), hạn DSAR phản hồi, hạn/ngưỡng thông báo breach, yêu cầu bản địa hóa thông báo.</li><li><strong>SHOULD</strong> cung cấp adapter baseline <code>vn_pdpl</code> làm mặc định (DEC-COMPLY-27): phản ánh khung lõi đã có (breach 72h, DPIA 60 ngày + review 6 tháng, consent PDPL). Các adapter khác kế thừa baseline và override phần khác biệt.</li><li><strong>SHOULD</strong> cung cấp adapter <code>id_pdp</code> (Indonesia PDP Law) khai báo khác biệt cụ thể so với baseline; phần không khác giữ nguyên baseline (DEC-COMPLY-29).</li><li><strong>SHOULD</strong> cung cấp adapter <code>th_pdpa</code> (Thailand PDPA) khai báo khác biệt cụ thể so với baseline; phần không khác giữ nguyên baseline.</li><li><strong>SHOULD</strong> chọn adapter theo regime từ TASK-COMPLY-006 (DEC-COMPLY-28): <code>registry.For(ctx, country)</code> đọc <code>data_protection_regime</code> của nước qua gating, trả adapter tương ứng. KHÔNG hardcode regime.</li><li><strong>SHOULD</strong> chỉ kích hoạt adapter cho nước đã mở (DEC-COMPLY-30): nước chưa mở (gating deny-by-default) -&gt; <code>For</code> trả lỗi/không adapter; tính năng SEA không chạy cho nước chưa sẵn sàng.</li><li><strong>SHOULD</strong> đảm bảo phần chung tái dùng baseline: nếu một regime không override một tham số, giá trị baseline VN được dùng (tránh trùng lặp khai báo).</li><li><strong>SHOULD</strong> expose <code>Profile(ctx, country) (RegimeProfile, error)</code> cho các task khác (consent UI, DSAR SLA, breach window) đọc tham số đúng theo nước user.</li><li><strong>SHOULD</strong> validate regime trả từ gating thuộc tập đã hỗ trợ (PDPL/PDP_ID/PDPA_TH); regime lạ -&gt; lỗi xác định, không im lặng dùng baseline sai.</li><li><strong>MAY</strong> phát OTel metric <code>regime_profile_resolved_total{regime}</code> để thấy phân bố nước đang phục vụ.</li><li><strong>SHOULD</strong> khai báo khác biệt dưới dạng dữ liệu (RegimeProfile field) chứ không nhánh điều kiện rải rác, để thêm regime mới là thêm một profile.</li><li><strong>SHOULD</strong> ghi rõ trong từng adapter nguồn khác biệt (chú thích quy định) để rà soát pháp lý truy được về luật gốc.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao adapter trên baseline, không viết lại mỗi nước (DEC-COMPLY-27)?</strong> Các chế độ bảo vệ dữ liệu SEA chia sẻ phần lớn nguyên tắc (consent, quyền chủ thể, thông báo vi phạm). Viết lại consent/DSAR/breach cho từng nước tạo bốn bản gần giống nhau, lệch dần theo thời gian. Lấy PDPL VN làm baseline và để mỗi nước override phần khác biệt giữ một lõi chung + một lớp mỏng khác biệt - dễ bảo trì, khó lệch.</p>\n<p><strong>Vì sao chọn adapter theo regime của TASK-COMPLY-006 (DEC-COMPLY-28)?</strong> TASK-COMPLY-006 đã khai báo <code>data_protection_regime</code> cho mỗi nước (PDPL/PDP_ID/PDPA_TH). Tái dùng điểm nối đó: <code>registry.For(country)</code> hỏi gating \"nước này chế độ gì\" rồi trả adapter tương ứng. Không hardcode regime trong adapter layer tránh hai nguồn sự thật mâu thuẫn.</p>\n<p><strong>Vì sao chỉ kích hoạt cho nước đã mở (DEC-COMPLY-30)?</strong> SEA sequencing có thứ tự (VN -&gt; ID/TH -&gt; PH/MY -&gt; SG/TW). Một nước chưa mở thì chưa nghiên cứu xong luật và gating để deny-by-default. Adapter không nên chạy cho nước chưa sẵn sàng. <code>For</code> trả lỗi khi gating chưa mở nước đó - nhất quán với deny-by-default của TASK-COMPLY-006.</p>\n<p><strong>Vì sao khác biệt là dữ liệu, không nhánh điều kiện (§1 #11)?</strong> Nếu khác biệt regime nằm trong <code>if regime == \"PDPA_TH\"</code> rải rác, thêm một nước SEA mới phải sửa khắp nơi. Đặt khác biệt vào <code>RegimeProfile</code> (các field tham số) làm việc thêm nước thành thêm một file profile khai báo - cùng triết lý khai báo của TASK-COMPLY-006.</p>\n<p><strong>Vì sao SHOULD chứ không MUST (priority)?</strong> Đây là tính năng P3, chỉ cần khi nước SEA tương ứng được mở. Trước khi mở ID/TH, baseline VN đủ phục vụ thị trường VN. Đánh dấu SHOULD phản ánh đúng: cần khi mở rộng, không chặn release P1/P2. Nhưng khi đã mở một nước SEA, adapter của nước đó trở thành bắt buộc cho nước đó.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Interface + profile (Go)</h3>\n<pre><code class=\"language-go\">// services/comply/internal/regime/types.go\ntype RegimeProfile struct {\n    Regime            string        // PDPL / PDP_ID / PDPA_TH\n    BreachWindow      time.Duration // baseline 72h\n    DPIAFilingWindow  time.Duration // baseline 60 ngay\n    ConsentLanguages  []string      // ngon ngu thong bao consent bat buoc\n    DSARResponseSLA   time.Duration // han phan hoi DSAR\n    Notes             string        // nguon khac biet (quy dinh goc)\n}\n\ntype RegimeAdapter interface {\n    Profile() RegimeProfile\n}</code></pre>\n<h3>Baseline + override (Go)</h3>\n<pre><code class=\"language-go\">// services/comply/internal/regime/vn_pdpl.go\nfunc baseline() RegimeProfile {\n    return RegimeProfile{\n        Regime:           \"PDPL\",\n        BreachWindow:     72 * time.Hour,\n        DPIAFilingWindow: 60 * 24 * time.Hour,\n        ConsentLanguages: []string{\"vi\"},\n        DSARResponseSLA:  30 * 24 * time.Hour,\n        Notes:            \"Luat 91/2025/QH15 + NĐ 356/2025 (baseline VN)\",\n    }\n}\n\n// services/comply/internal/regime/th_pdpa.go\n// Thailand PDPA: ke thua baseline, override phan khac biet (ngon ngu, ghi chu nguon).\nfunc thPDPA() RegimeProfile {\n    p := baseline()\n    p.Regime = \"PDPA_TH\"\n    p.ConsentLanguages = []string{\"th\", \"en\"} // ban dia hoa thong bao\n    p.Notes = \"Thailand PDPA - khac biet ngon ngu thong bao; tham so khac giu baseline\"\n    return p\n}\n\n// services/comply/internal/regime/registry.go\n// For chon adapter theo regime tu TASK-COMPLY-006; chi nuoc da mo.\nfunc (r *Registry) For(ctx context.Context, country string) (RegimeAdapter, error) {\n    rv, err := r.gating.Value(ctx, country, gating.GateDataRegime)\n    if err != nil {\n        return nil, err\n    }\n    if rv.Denied {\n        return nil, ErrCountryNotOpen // nuoc chua mo (deny-by-default)\n    }\n    a, ok := r.byRegime[rv.Regime]\n    if !ok {\n        return nil, ErrUnsupportedRegime // regime la -&gt; khong im lang dung baseline sai\n    }\n    return a, nil\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>Profile(\"VN\")</code> -&gt; regime PDPL, BreachWindow 72h, DPIAFilingWindow 60 ngày (baseline).</li><li><code>Profile(\"TH\")</code> -&gt; regime PDPA_TH; ConsentLanguages chứa <code>th</code>; tham số không override = baseline.</li><li><code>Profile(\"ID\")</code> -&gt; regime PDP_ID; khác biệt khai báo của adapter id_pdp được phản ánh.</li><li><code>For(\"KR\")</code> (nước chưa mở, gating deny) -&gt; lỗi <code>ErrCountryNotOpen</code>.</li><li>Regime lạ (gating trả regime không hỗ trợ) -&gt; <code>For</code> lỗi <code>ErrUnsupportedRegime</code>, KHÔNG im lặng dùng baseline.</li><li>Adapter TH không override <code>BreachWindow</code> -&gt; giá trị 72h baseline được dùng (tái dùng phần chung).</li><li>Thêm một regime mới = thêm một file profile; nước/regime cũ không đổi hành vi.</li><li><code>For(\"VN\")</code> -&gt; trả adapter vn_pdpl (baseline) đúng.</li><li><code>Profile</code> đọc regime qua gating (TASK-COMPLY-006), KHÔNG hardcode trong adapter.</li><li>Mỗi adapter có <code>Notes</code> không rỗng trỏ nguồn khác biệt (truy về luật gốc).</li><li>Metric <code>regime_profile_resolved_total{regime}</code> tăng khi resolve một profile.</li><li>Phần chung không trùng lặp: override chỉ khai báo field khác baseline (review code).</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/comply/internal/regime/adapter_test.go\nfunc TestProfile_VNBaseline(t *testing.T) {\n    p := baseline()\n    require.Equal(t, \"PDPL\", p.Regime)\n    require.Equal(t, 72*time.Hour, p.BreachWindow)\n    require.Equal(t, 60*24*time.Hour, p.DPIAFilingWindow)\n}\n\nfunc TestProfile_THInheritsBaselineBreachWindow(t *testing.T) {\n    p := thPDPA()\n    require.Equal(t, \"PDPA_TH\", p.Regime)\n    require.Contains(t, p.ConsentLanguages, \"th\")\n    require.Equal(t, 72*time.Hour, p.BreachWindow) // khong override -&gt; baseline\n}\n\nfunc TestProfile_AdaptersHaveSourceNotes(t *testing.T) {\n    for _, p := range []RegimeProfile{baseline(), idPDP(), thPDPA()} {\n        require.NotEmpty(t, p.Notes) // truy ve luat goc\n    }\n}\n\n// services/comply/internal/regime/registry_test.go\nfunc TestRegistry_CountryNotOpen(t *testing.T) {\n    r := setupWithGating(t, denyAll())\n    _, err := r.For(ctx, \"KR\")\n    require.ErrorIs(t, err, ErrCountryNotOpen)\n}\n\nfunc TestRegistry_UnsupportedRegimeNotSilent(t *testing.T) {\n    r := setupWithGating(t, regimeFor(\"XX\", \"PDPA_XX\"))\n    _, err := r.For(ctx, \"XX\")\n    require.ErrorIs(t, err, ErrUnsupportedRegime) // khong dung baseline sai\n}\n\nfunc TestRegistry_ResolvesPerRegime(t *testing.T) {\n    r := setupWithGating(t, openSEA()) // VN/ID/TH mo\n    vn, _ := r.For(ctx, \"VN\")\n    th, _ := r.For(ctx, \"TH\")\n    require.Equal(t, \"PDPL\", vn.Profile().Regime)\n    require.Equal(t, \"PDPA_TH\", th.Profile().Regime)\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: types.go (interface + RegimeProfile) -&gt; vn_pdpl.go (baseline) -&gt; id_pdp.go + th_pdpa.go (override) -&gt; registry.go (chọn theo gating, chỉ nước đã mở) -&gt; tests. Baseline là một hàm trả profile VN; mỗi adapter gọi <code>baseline()</code> rồi override field khác biệt - phần chung không lặp lại. <code>registry.For</code> đọc regime qua <code>gating.Value(country, GateDataRegime)</code> (TASK-COMPLY-006), giữ một nguồn sự thật. Các task consent UI / DSAR SLA / breach window đọc <code>Profile(country)</code> để áp tham số đúng nước.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-COMPLY-006</strong> - cung cấp <code>data_protection_regime</code> mỗi nước + deny-by-default cho nước chưa mở; <code>For</code> đọc từ đây.</li><li><strong>TASK-COMPLY-001</strong> - baseline consent PDPL; adapter điều chỉnh granularity/ngôn ngữ theo nước.</li><li><strong>TASK-COMPLY-002 (liên quan)</strong> - DPIA filing window có thể khác theo regime (profile field).</li><li><strong>TASK-COMPLY-003 (liên quan)</strong> - DSAR response SLA lấy từ profile theo nước.</li><li><strong>TASK-COMPLY-004 (liên quan)</strong> - breach window lấy từ profile (baseline 72h).</li><li>Lib: chỉ stdlib <code>time</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Consent UI hỏi profile để bản địa hóa thông báo</h3>\n<pre><code class=\"language-go\">prof, err := regime.Profile(ctx, user.Country)\nif err != nil { return err }\n// TH: hien consent bang th/en; VN: vi\nrenderConsent(prof.ConsentLanguages, purpose)</code></pre>\n<h3>Breach service lấy window theo nước của sự cố</h3>\n<pre><code class=\"language-go\">prof, _ := regime.Profile(ctx, incident.Country)\nauthorityDue := incident.AcknowledgedAt.Add(prof.BreachWindow) // baseline 72h tru khi regime override</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt khung adapter. Hoãn (cần xác minh pháp lý từng nước trước khi mở, §10 tài liệu nguồn):</p>\n<ul><li>Khác biệt chi tiết hạn breach/DSAR của Indonesia PDP và Thailand PDPA so với 72h/30 ngày baseline - chốt với tư vấn pháp lý địa phương trước khi mở.</li><li>Yêu cầu lưu trú dữ liệu (data residency) theo nước - thêm field profile khi có yêu cầu cụ thể.</li><li>Adapter cho PH/MY/SG/TW - thêm khi sequencing tới các nước đó.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Viết lại consent/DSAR/breach mỗi nước</td><td>review §1 #2</td><td>bốn bản lệch nhau</td><td>Adapter override trên baseline</td></tr><tr><td>Áp PDPL VN nguyên cho ID/TH</td><td>profile override + AC #2,#3</td><td>vi phạm luật địa phương</td><td>RegimeProfile khai báo khác biệt</td></tr><tr><td>Kích hoạt adapter nước chưa mở</td><td><code>ErrCountryNotOpen</code> + AC #4</td><td>chạy luật chưa sẵn sàng</td><td><code>For</code> tôn trọng deny-by-default</td></tr><tr><td>Regime lạ im lặng dùng baseline</td><td><code>ErrUnsupportedRegime</code> + AC #5</td><td>tuân thủ sai âm thầm</td><td>Lỗi xác định, không fallback baseline</td></tr><tr><td>Trùng lặp khai báo phần chung</td><td>review §1 #7 + AC #12</td><td>bảo trì lệch</td><td>Override chỉ field khác baseline</td></tr><tr><td>Hardcode regime trong adapter</td><td>review §1 #5</td><td>hai nguồn sự thật</td><td>Đọc regime qua gating TASK-COMPLY-006</td></tr><tr><td>Mất truy vết nguồn khác biệt</td><td><code>Notes</code> §1 #12 + AC #10</td><td>rà soát pháp lý khó</td><td>Mỗi adapter ghi nguồn quy định</td></tr><tr><td>Thêm regime phá nước cũ</td><td>AC #7</td><td>hồi quy chéo</td><td>Mở rộng cộng dồn; test nước cũ</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Bảo vệ dữ liệu SEA là lớp adapter trên baseline PDPL VN, không viết lại mỗi nước: một lõi chung + lớp mỏng khác biệt.</li><li>Adapter chọn theo <code>data_protection_regime</code> của TASK-COMPLY-006; giữ một nguồn sự thật về \"nước nào chế độ gì\".</li><li>Chỉ kích hoạt adapter cho nước đã mở; nước chưa mở giữ deny-by-default - nhất quán với gating.</li><li>Khác biệt regime là dữ liệu (RegimeProfile field), không nhánh điều kiện: thêm nước = thêm một profile.</li><li>SHOULD vì là tính năng P3, cần khi mở nước SEA tương ứng; nhưng khi đã mở một nước, adapter của nước đó là bắt buộc cho nước đó.</li><li>Khác biệt chi tiết hạn breach/DSAR của ID/TH cần xác minh với tư vấn pháp lý địa phương trước khi mở (§9, §10 tài liệu nguồn) - khung sẵn sàng nhận giá trị đã xác minh.</li></ul>\n<hr>\n<p><em>Hết TASK-COMPLY-007. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
