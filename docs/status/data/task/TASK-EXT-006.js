window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-EXT-006"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>UI consent + settings <strong>MUST</strong> thu đồng thuận PDPL tường minh, granular per-purpose lúc cài (im lặng không phải đồng thuận), chặn mọi đường dữ liệu cho tới khi có opt-in, và ghi consent record tái lập được. Hợp đồng:</p>\n<ol><li>Extension <strong>MUST</strong> hiển thị màn hình consent + disclosure lúc cài (onboarding, mở khi <code>chrome.runtime.onInstalled</code>) trước khi đọc/gửi bất cứ dữ liệu nào (DEC-EXT-29). Người dùng <strong>MUST</strong> opt-in tường minh; mặc định mọi mục là TẮT.</li><li>Đồng thuận <strong>MUST</strong> granular per-purpose (DEC-EXT-30): các mục riêng cho \"đọc giỏ hàng\", \"đọc voucher\", \"đồng bộ backend\". Người dùng <strong>MUST</strong> bật/tắt từng mục độc lập. <strong>MUST NOT</strong> gộp thành một consent \"tất cả hoặc không\".</li><li>Im lặng hoặc mặc định-bật <strong>MUST NOT</strong> được coi là đồng thuận (PDPL §5.5): không checkbox tick sẵn, không \"tiếp tục nghĩa là đồng ý\" cho việc xử lý dữ liệu. Đồng thuận chỉ hợp lệ khi người dùng chủ động bật.</li><li>Trước mọi đọc/gửi của một mục, <code>consent-gate.ts</code> <strong>MUST</strong> kiểm consent của mục đó; nếu chưa đồng ý, đường dữ liệu <strong>MUST</strong> bị chặn (DEC-EXT-33). Content script (TASK-EXT-002) đọc giỏ chỉ chạy khi consent \"đọc giỏ\" bật; đồng bộ (TASK-EXT-005) chỉ chạy khi consent \"đồng bộ\" bật.</li><li><code>consent-store.ts</code> <strong>MUST</strong> ghi <code>ConsentRecord</code> tái lập được (DEC-EXT-31): phiên bản chính sách, thời điểm (epoch), danh sách mục đã đồng ý. Record <strong>MUST</strong> gửi cho TASK-COMPLY-001 (khung consent trung tâm) để chứng minh đã đồng ý gì, khi nào.</li><li>Disclosure <strong>MUST</strong> nêu rõ, bằng ngôn ngữ người dùng hiểu (tiếng Việt): KHÔNG thu thập mật khẩu; token/cookie sàn KHÔNG rời máy client; chỉ gửi <code>productId</code>/giá/số lượng về backend (khớp TASK-EXT-003) (DEC-EXT-32).</li><li>Rút consent <strong>MUST</strong> có hiệu lực ngay: khi người dùng tắt một mục trong settings, đường dữ liệu của mục đó <strong>MUST</strong> dừng đọc/gửi tức thì (DEC-EXT-33); và ghi consent record mới (đã rút).</li><li><code>settings.ts</code> (options page) <strong>MUST</strong> cho người dùng xem trạng thái consent hiện tại, đổi từng mục, và truy lối xóa dữ liệu (liên kết DSAR TASK-COMPLY-003) (DEC-EXT-34) bất cứ lúc nào.</li><li>Khi phiên bản chính sách đổi (disclosure cập nhật), extension <strong>MUST</strong> yêu cầu lại consent cho phần thay đổi - không tự gia hạn consent cũ cho chính sách mới.</li><li>Consent state <strong>MUST</strong> lưu bền trong <code>chrome.storage</code> (sống qua SW kill, NFR-EXT-001) và là nguồn sự thật mà consent-gate đọc - KHÔNG giữ trong biến global.</li><li>UI <strong>MUST</strong> không dùng pattern lừa (dark pattern): nút đồng ý và từ chối ngang nhau về độ nổi bật; không ẩn nút từ chối; không gây hiểu lầm rằng phải đồng ý mới dùng được phần không liên quan.</li><li>Trước khi có BẤT KỲ consent nào, extension <strong>MUST</strong> ở trạng thái trơ về dữ liệu: không content script đọc, không đồng bộ; chỉ hiển thị onboarding. Cài đặt extension không tự nó là đồng thuận xử lý dữ liệu.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao consent tường minh lúc cài, mặc định tắt (DEC-EXT-29)?</strong> PDPL (§5.5) nói rõ: đồng thuận phải tự nguyện, cụ thể, và im lặng không phải đồng thuận. Một extension đọc ngữ cảnh đăng nhập sàn là xử lý dữ liệu nhạy cảm; bật mặc định hay coi việc cài là đồng ý đều khiến đồng thuận vô hiệu - và mời chế tài tới 5% doanh thu. Mặc định tắt + opt-in chủ động là tư thế đúng luật và đúng niềm tin.</p>\n<p><strong>Vì sao granular per-purpose (DEC-EXT-30)?</strong> PDPL đòi đồng thuận \"đơn mục đích\". Gộp \"đọc giỏ + đọc voucher + đồng bộ\" vào một nút là ép người dùng đồng ý trọn gói - không hợp lệ. Tách từng mục để người dùng đồng ý đúng cái họ muốn (ví dụ chỉ đọc giỏ tại máy, chưa cho đồng bộ) là bản chất của đồng thuận hợp lệ.</p>\n<p><strong>Vì sao consent gate chặn trước đọc/gửi (DEC-EXT-33)?</strong> Consent trên giấy mà code vẫn đọc trước khi đồng ý thì vô nghĩa. Gate biến đồng thuận thành ràng buộc thực thi: mỗi đường dữ liệu hỏi gate trước, gate đọc consent state, chưa đồng ý thì chặn. Đây là điểm nối giữa UI consent và hành vi thực của extension - không phải trang trí.</p>\n<p><strong>Vì sao consent record tái lập được (DEC-EXT-31)?</strong> PDPL đòi đồng thuận \"tái lập được\". Khi cơ quan kiểm tra hỏi \"người dùng đã đồng ý gì, phiên bản chính sách nào, khi nào\", ta phải trả lời bằng bằng chứng. ConsentRecord (version + ts + mục) gửi TASK-COMPLY-001 là bằng chứng đó. Không ghi record là không chứng minh được tuân thủ.</p>\n<p><strong>Vì sao disclosure nêu rõ ranh giới kỹ thuật (DEC-EXT-32)?</strong> Hậu-Honey, người dùng VN nghi extension đọc cookie là scam (~45% lo lộ dữ liệu, §5.4). Nói thẳng \"không thu mật khẩu, token không rời máy, chỉ gửi productId/giá/số lượng\" - và để TASK-EXT-003 + TASK-TRUST-003 chứng minh - biến disclosure từ lời hứa thành cam kết kiểm chứng được. Đây vừa là tuân thủ vừa là điểm bán.</p>\n<p><strong>Vì sao rút consent có hiệu lực ngay (§1 #7)?</strong> Đồng thuận rút được là quyền chủ thể dữ liệu. Nếu tắt mục mà đường dữ liệu vẫn chạy thêm một lúc, đó là xử lý không có cơ sở pháp lý trong khoảng đó. Hiệu lực tức thì (gate đọc state mới ngay) giữ đúng nguyên tắc.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>types.ts (consent, typed)</h3>\n<pre><code class=\"language-ts\">// extension/src/shared/types.ts (thêm)\nexport type ConsentPurpose = \"read_cart\" | \"read_voucher\" | \"sync_backend\";\n\nexport interface ConsentRecord {\n  policyVersion: string;          // ví dụ \"2026-06-27\"\n  decidedAt: number;              // epoch ms - \"khi nào\"\n  granted: ConsentPurpose[];      // các mục đã đồng ý - \"đồng ý gì\"\n  // tái lập được: đủ để chứng minh đã đồng ý gì, phiên bản nào, khi nào\n}</code></pre>\n<h3>consent-store.ts (ghi record bền + gửi TASK-COMPLY-001)</h3>\n<pre><code class=\"language-ts\">// extension/src/consent/consent-store.ts\nconst KEY = \"sandeal:consent\";\n\nexport async function getConsent(): Promise&lt;ConsentRecord&gt; {\n  const o = await chrome.storage.local.get(KEY);\n  return (o[KEY] as ConsentRecord) ?? { policyVersion: POLICY_VERSION, decidedAt: 0, granted: [] };\n  // mặc định granted: [] - KHÔNG mục nào bật (DEC-EXT-29)\n}\n\nexport async function setConsent(granted: ConsentPurpose[]): Promise&lt;void&gt; {\n  const rec: ConsentRecord = { policyVersion: POLICY_VERSION, decidedAt: Date.now(), granted };\n  await chrome.storage.local.set({ [KEY]: rec });\n  await reportConsentToCompliance(rec);   // TASK-COMPLY-001: tái lập được\n}</code></pre>\n<h3>consent-gate.ts (chặn trước mọi đọc/gửi)</h3>\n<pre><code class=\"language-ts\">// extension/src/consent/consent-gate.ts\nexport async function ensureConsent(p: ConsentPurpose): Promise&lt;boolean&gt; {\n  const rec = await getConsent();\n  return rec.granted.includes(p);   // chưa đồng ý → false → đường dữ liệu bị chặn\n}\n\n// Ví dụ dùng ở content-script orchestration / sync:\n// if (!(await ensureConsent(\"read_cart\"))) return; // KHÔNG đọc giỏ khi chưa opt-in\n// if (!(await ensureConsent(\"sync_backend\"))) return; // KHÔNG đồng bộ khi chưa opt-in</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>onInstalled</code> mở onboarding; trước khi có consent, không content script đọc + không đồng bộ (test: gate trả false cho mọi mục khi <code>granted: []</code>).</li><li>Consent mặc định TẮT: <code>getConsent()</code> trên cài mới trả <code>granted: []</code> (không mục nào bật).</li><li>Granular: bật riêng \"read_cart\" không tự bật \"sync_backend\" (test từng mục độc lập).</li><li><code>ensureConsent(p)</code> trả false khi mục p chưa đồng ý -&gt; đường dữ liệu của p bị chặn (test gate chặn đọc giỏ/đồng bộ).</li><li><code>setConsent</code> ghi <code>ConsentRecord</code> có <code>policyVersion</code> + <code>decidedAt</code> + <code>granted</code>, và gọi <code>reportConsentToCompliance</code> (TASK-COMPLY-001).</li><li>Rút consent (tắt mục trong settings) -&gt; <code>ensureConsent</code> trả false ngay; ghi record mới với <code>granted</code> đã bỏ mục đó.</li><li>Disclosure UI chứa các câu cốt lõi: không thu mật khẩu, token/cookie sàn không rời máy, chỉ gửi productId/giá/số lượng (test nội dung).</li><li>Không dark pattern: không checkbox tick sẵn (test onboarding mặc định mọi mục off); nút từ chối hiện diện.</li><li>Đổi <code>policyVersion</code> -&gt; yêu cầu lại consent cho phần thay đổi (test record cũ version khác không tự áp cho version mới).</li><li>Consent state bền: ghi consent, mô phỏng SW kill, đọc lại từ <code>chrome.storage</code> còn nguyên.</li><li><code>settings.ts</code> có lối xóa dữ liệu liên kết DSAR (TASK-COMPLY-003) (test có entry point).</li><li><code>npm test</code> xanh; <code>tsc --noEmit</code> sạch.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-ts\">// extension/test/consent-default-off.test.ts\nimport { getConsent } from \"../src/consent/consent-store\";\n\ntest(\"cài mới: mọi consent TẮT (im lặng != đồng thuận)\", async () =&gt; {\n  globalThis.chrome = fakeChromeStorage();           // chưa ghi gì\n  const rec = await getConsent();\n  expect(rec.granted).toEqual([]);                   // KHÔNG mục nào bật\n});\n\ntest(\"onboarding không tick sẵn mục nào\", async () =&gt; {\n  const html = await readFile(\"src/ui/onboarding.html\", \"utf8\");\n  expect(html).not.toMatch(/&lt;input[^&gt;]*type=[\"']checkbox[\"'][^&gt;]*checked/i); // không checked sẵn\n});</code></pre>\n<pre><code class=\"language-ts\">// extension/test/consent-gate.test.ts\nimport { ensureConsent } from \"../src/consent/consent-gate\";\nimport { setConsent } from \"../src/consent/consent-store\";\n\ntest(\"chưa opt-in → gate chặn đọc giỏ + đồng bộ\", async () =&gt; {\n  globalThis.chrome = fakeChromeStorage();\n  expect(await ensureConsent(\"read_cart\")).toBe(false);\n  expect(await ensureConsent(\"sync_backend\")).toBe(false);\n});\n\ntest(\"granular: bật read_cart không tự bật sync_backend\", async () =&gt; {\n  globalThis.chrome = fakeChromeStorage();\n  await setConsent([\"read_cart\"]);\n  expect(await ensureConsent(\"read_cart\")).toBe(true);\n  expect(await ensureConsent(\"sync_backend\")).toBe(false);  // độc lập\n});\n\ntest(\"rút consent có hiệu lực ngay\", async () =&gt; {\n  await setConsent([\"read_cart\"]);\n  await setConsent([]);                               // tắt\n  expect(await ensureConsent(\"read_cart\")).toBe(false);\n});</code></pre>\n<pre><code class=\"language-ts\">// extension/test/consent-record.test.ts\ntest(\"setConsent ghi record tái lập được + gửi compliance\", async () =&gt; {\n  const sent: ConsentRecord[] = [];\n  mockReportConsent(r =&gt; sent.push(r));\n  await setConsent([\"read_cart\", \"read_voucher\"]);\n  expect(sent[0].policyVersion).toBeTruthy();\n  expect(sent[0].decidedAt).toBeGreaterThan(0);\n  expect(sent[0].granted).toEqual([\"read_cart\", \"read_voucher\"]);\n});\n\ntest(\"disclosure nêu rõ ranh giới kỹ thuật\", async () =&gt; {\n  const html = (await readFile(\"src/ui/onboarding.html\", \"utf8\")).toLowerCase();\n  expect(html).toMatch(/mật khẩu/);                  // không thu mật khẩu\n  expect(html).toMatch(/token|cookie/);              // token không rời máy\n  expect(html).toMatch(/productid|số lượng|giá/);    // chỉ gửi tối thiểu\n});</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>consent-store.ts</code> (ConsentRecord bền + gửi TASK-COMPLY-001, mặc định granted rỗng) -&gt; <code>consent-gate.ts</code> (ensureConsent chặn trước đọc/gửi) -&gt; <code>onboarding.html</code> + <code>onboarding.ts</code> (consent lúc cài, mặc định tắt, disclosure rõ) -&gt; <code>settings.html</code> + <code>settings.ts</code> (xem/đổi/thu hồi + lối DSAR) -&gt; nối <code>service-worker.ts</code> (onInstalled mở onboarding; gate chặn TASK-EXT-002/003/005) -&gt; tests. Content script đọc giỏ (TASK-EXT-002) và đồng bộ (TASK-EXT-005) đều gọi <code>ensureConsent</code> trước. Consent state là nguồn sự thật bền trong chrome.storage; mọi đổi consent ghi record mới gửi compliance.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-EXT-001</strong> - scaffold MV3 + chrome.storage + onInstalled + options page làm khung UI consent.</li><li><strong>TASK-COMPLY-001</strong> - khung consent PDPL trung tâm nhận ConsentRecord; task này là bề mặt thu consent của extension.</li><li><strong>TASK-EXT-002</strong> - content script đọc giỏ chỉ chạy khi consent \"read_cart\"/\"read_voucher\" bật (gate).</li><li><strong>TASK-EXT-003 / TASK-EXT-005</strong> - tối thiểu hóa + đồng bộ chỉ chạy khi consent \"sync_backend\" bật.</li><li><strong>TASK-COMPLY-003</strong> - DSAR (xem/xóa dữ liệu); settings cung cấp lối vào.</li><li><strong>TASK-TRUST-001 / TASK-TRUST-002</strong> - open-source + minh bạch local-first; disclosure ở đây khớp cam kết đó.</li><li><strong>NFR-EXT-001</strong> - consent state bền trong storage để sống qua SW kill.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>ConsentRecord lưu trong chrome.storage (tái lập được)</h3>\n<pre><code class=\"language-json\">{\n  \"sandeal:consent\": {\n    \"policyVersion\": \"2026-06-27\",\n    \"decidedAt\": 1782000000000,\n    \"granted\": [\"read_cart\", \"read_voucher\"]\n  }\n}</code></pre>\n<h3>Đoạn disclosure (onboarding, tiếng Việt)</h3>\n<pre><code class=\"language-text\">SănDeal đọc giỏ hàng và voucher trong tab đã đăng nhập của chính bạn để giúp tối ưu giá.\n- KHÔNG thu thập mật khẩu của bạn.\n- Token/cookie phiên của sàn KHÔNG rời khỏi máy bạn.\n- Chỉ gửi về máy chủ: mã sản phẩm (productId), giá, số lượng.\nBạn chọn bật từng mục dưới đây. Không bật mục nào thì SănDeal không đọc/gửi gì.\n[ ] Đọc giỏ hàng    [ ] Đọc voucher    [ ] Đồng bộ với máy chủ SănDeal</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Đa ngôn ngữ disclosure cho SEA (ID/TH...) - thêm khi mở per-country (TASK-COMPLY-006/007), slice 1 tiếng Việt.</li><li>Consent cho mục đích phụ (ví dụ phân tích ẩn danh B2B) - chỉ thêm khi TASK-B2B-001 tới, opt-in riêng.</li><li>Nhắc lại consent định kỳ (re-consent theo thời gian) - chốt cùng TASK-COMPLY-002 (DPIA cập nhật mỗi 6 tháng).</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Mặc định bật consent</td><td>consent-default-off test</td><td>đồng thuận vô hiệu, vi phạm PDPL</td><td>Mặc định granted rỗng (DEC-EXT-29)</td></tr><tr><td>Im lặng coi là đồng ý</td><td>onboarding không-checked test</td><td>vi phạm §5.5</td><td>Opt-in chủ động, không tick sẵn (§1 #3)</td></tr><tr><td>Gộp \"tất cả hoặc không\"</td><td>granular test</td><td>đồng thuận không đơn mục đích</td><td>Per-purpose độc lập (DEC-EXT-30)</td></tr><tr><td>Đọc/gửi trước khi đồng ý</td><td>consent-gate test</td><td>xử lý không cơ sở pháp lý</td><td>Gate chặn trước mọi đọc/gửi (DEC-EXT-33)</td></tr><tr><td>Không ghi consent record</td><td>consent-record test</td><td>không chứng minh được tuân thủ</td><td>Record tái lập được -&gt; TASK-COMPLY-001 (DEC-EXT-31)</td></tr><tr><td>Rút consent không hiệu lực ngay</td><td>rút-consent test</td><td>xử lý sau khi rút</td><td>Gate đọc state mới tức thì (§1 #7)</td></tr><tr><td>Disclosure mơ hồ</td><td>nội dung test</td><td>nghi malware (§5.4)</td><td>Nêu rõ không mật khẩu/token/chỉ tối thiểu (DEC-EXT-32)</td></tr><tr><td>Dark pattern ẩn nút từ chối</td><td>review UI</td><td>đồng thuận ép buộc</td><td>Nút đồng ý/từ chối ngang nhau (§1 #11)</td></tr><tr><td>Consent trong biến global</td><td>persist test</td><td>mất khi SW kill</td><td>Lưu bền chrome.storage (§1 #10)</td></tr><tr><td>Gia hạn consent cũ cho policy mới</td><td>version test</td><td>đồng ý sai phạm vi</td><td>Yêu cầu lại consent khi đổi version (§1 #9)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>UI consent vừa là cổng pháp lý PDPL (§5.5: tự nguyện/cụ thể/đơn mục đích/tái lập được; im lặng != đồng thuận) vừa là tài sản niềm tin hậu-Honey (§5.4).</li><li>Consent gate biến đồng thuận thành ràng buộc thực thi: mỗi đường dữ liệu (TASK-EXT-002/005) hỏi gate trước; chưa opt-in thì chặn - không phải trang trí.</li><li>ConsentRecord tái lập được (version + ts + mục) là bằng chứng tuân thủ gửi TASK-COMPLY-001 - trả lời được \"đồng ý gì, khi nào\".</li><li>Disclosure nêu thẳng ranh giới kỹ thuật (không mật khẩu, token không rời máy, chỉ productId/giá/số lượng) và để TASK-EXT-003 + TASK-TRUST-003 chứng minh - lời hứa kiểm chứng được.</li><li>Mặc định tắt + granular + rút-được-ngay là ba trụ giữ đồng thuận hợp lệ; mọi đổi consent ghi record mới.</li></ul>\n<hr>\n<p><em>Hết TASK-EXT-006. Status: done (mục tiêu audit 10/10).</em></p>";
