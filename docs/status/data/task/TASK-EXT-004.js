window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-EXT-004"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Extension <strong>MUST</strong> đặt mọi tác vụ cần DOM hoặc clipboard ngoài service worker vào một tài liệu offscreen vòng đời ngắn, và <strong>MUST</strong> dùng <code>declarativeNetRequest</code> thay cho webRequest blocking cho mọi can thiệp request. Hợp đồng:</p>\n<ol><li>Service worker <strong>MUST NOT</strong> giả định có DOM: không dựa <code>DOMParser</code>/<code>document</code> trong SW cho việc parse HTML phức tạp. Mọi parse DOM nặng hoặc thao tác clipboard <strong>MUST</strong> chạy trong tài liệu offscreen tạo qua <code>chrome.offscreen.createDocument</code> với <code>reason</code> tường minh (<code>DOM_SCRAPING</code> hoặc <code>CLIPBOARD</code>) (DEC-EXT-18).</li><li><code>manager.ts</code> <strong>MUST</strong> kiểm <code>chrome.offscreen.hasDocument()</code> trước khi tạo - chỉ tồn tại tối đa một tài liệu offscreen tại một thời điểm (Chrome chỉ cho phép một).</li><li>Tài liệu offscreen <strong>MUST</strong> có vòng đời ngắn (DEC-EXT-19): tạo khi cần, đóng ngay bằng <code>chrome.offscreen.closeOffscreenDocument()</code> sau khi tác vụ xong. <strong>MUST NOT</strong> để offscreen mở thường trực - nó giữ SW sống và tốn RAM, trái ràng buộc ephemeral (NFR-EXT-001).</li><li>Mọi can thiệp request (chặn, chuyển hướng, sửa header) <strong>MUST</strong> dùng <code>chrome.declarativeNetRequest</code> (DEC-EXT-20). Mã <strong>MUST NOT</strong> dùng <code>chrome.webRequest</code> ở chế độ blocking (<code>onBeforeRequest</code> blocking) - API này đã bị gỡ ở MV3, Web Store từ chối.</li><li>DNR rule <strong>MUST</strong> là tập tối thiểu, khai báo static trong manifest qua <code>declarative_net_request.rule_resources</code> trỏ <code>rules.json</code> (DEC-EXT-21). Mỗi rule <strong>MUST</strong> có lý do ghi rõ. <strong>MUST NOT</strong> thêm rule rộng để chặn/sửa request của sàn theo cách lạm dụng (rủi ro niềm tin §5.4).</li><li>Tài liệu offscreen <strong>MUST</strong> chỉ parse HTML THÔ đã có sẵn (truyền vào dạng string qua message) hoặc thao tác clipboard do người dùng kích hoạt; <strong>MUST NOT</strong> tự <code>fetch</code> trang sàn từ offscreen (DEC-EXT-22). Việc đọc trang sàn là của content script (TASK-EXT-002).</li><li>Dữ liệu kết quả từ offscreen <strong>MUST</strong> đi tiếp qua pipeline tối thiểu hóa (TASK-EXT-003) trước khi rời client - offscreen không phải đường tắt bỏ qua allowlist.</li><li><code>manifest.json</code> <strong>MUST</strong> thêm vào <code>permissions</code>: <code>\"offscreen\"</code>, <code>\"declarativeNetRequest\"</code> - chỉ khi task này hiện thực hóa, đúng nguyên tắc quyền tối thiểu của TASK-EXT-001.</li><li>Message giữa SW và offscreen <strong>MUST</strong> typed (<code>ParseDomRequest</code> / <code>ParseDomResult</code> trong <code>types.ts</code>), định tuyến rõ <code>target: \"offscreen\"</code> để không lẫn với message content script.</li><li>Khi <code>createOffscreenDocument</code> lỗi (đã có tài liệu / quyền thiếu) <strong>MUST</strong> xử lý lịch sự: tái dùng tài liệu đang mở hoặc trả lỗi rõ, KHÔNG retry vô hạn làm kẹt SW.</li><li><code>dnr.ts</code> <strong>MUST</strong> cung cấp hàm kiểm số lượng + phạm vi rule lúc khởi động (sanity check) để test khẳng định rule không vượt tập tối thiểu.</li><li>Toàn bộ tác vụ offscreen <strong>MUST</strong> đặt giới hạn thời gian (tự đóng nếu quá ngưỡng, ví dụ 10 giây) để một parse treo không giữ tài liệu mở mãi.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao offscreen cho DOM/clipboard (DEC-EXT-18)?</strong> Service worker MV3 không phải trang - nó không có <code>document</code> đầy đủ và <code>DOMParser</code> trong SW không đáng tin cho HTML phức tạp; clipboard cũng cần ngữ cảnh tài liệu. Offscreen API tạo một tài liệu ẩn có DOM thật, đúng nơi để parse HTML nặng hoặc đọc/ghi clipboard. Đây là cơ chế Chrome cung cấp thay cho thói quen cũ (background page có DOM) đã mất ở MV3.</p>\n<p><strong>Vì sao offscreen vòng đời ngắn (DEC-EXT-19)?</strong> Một tài liệu offscreen mở giữ service worker sống và tốn RAM. Nếu để mở thường trực, ta vô tình phá chính ràng buộc ephemeral mà NFR-EXT-001 yêu cầu. Quy ước \"tạo khi cần, đóng ngay\" giữ offscreen như một công cụ nhất thời, không phải một background page trá hình.</p>\n<p><strong>Vì sao declarativeNetRequest, không webRequest blocking (DEC-EXT-20)?</strong> MV3 đã gỡ webRequest blocking (lý do bảo mật + hiệu năng của Chrome). Rule khai báo của DNR được trình duyệt thực thi mà không chạy JS của extension trên từng request - nhanh hơn và minh bạch hơn. Code còn dùng webRequest blocking sẽ bị Web Store từ chối và đơn giản là không hoạt động.</p>\n<p><strong>Vì sao DNR tối thiểu + static (DEC-EXT-21)?</strong> DNR có thể chặn/chuyển hướng/sửa request - đúng loại quyền lực mà một extension \"thao túng\" lạm dụng. SănDeal hậu-Honey phải chứng minh mình không làm vậy. Rule khai static, đếm được, mỗi rule có lý do, cho phép audit (TASK-TRUST-003) xác nhận extension không can thiệp request sàn ngoài mục đích đã công bố.</p>\n<p><strong>Vì sao offscreen không tự fetch trang sàn (DEC-EXT-22)?</strong> Nếu offscreen tự <code>fetch</code> trang sàn, nó trở thành một đường đọc thứ hai vòng qua content script - phá nguyên tắc \"content script là đường đọc duy nhất, đọc trong tab đã đăng nhập\". Offscreen chỉ nhận HTML thô đã có (do content script lấy) để parse, rồi trả kết quả. Mọi dữ liệu vẫn qua pipeline tối thiểu hóa.</p>\n<p><strong>Vì sao giới hạn thời gian tác vụ offscreen (§1 #12)?</strong> Một parse treo sẽ giữ tài liệu offscreen mở mãi - vừa tốn RAM, vừa giữ SW sống. Đặt trần thời gian rồi tự đóng biến lỗi treo thành lỗi đóng được, không để rò tài nguyên.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>manifest.json (bổ sung)</h3>\n<pre><code class=\"language-jsonc\">// extension/manifest.json (thêm)\n{\n  \"permissions\": [\"storage\", \"alarms\", \"offscreen\", \"declarativeNetRequest\"],\n  \"declarative_net_request\": {\n    \"rule_resources\": [\n      { \"id\": \"sandeal_rules\", \"enabled\": true, \"path\": \"dnr/rules.json\" }\n    ]\n  }\n}</code></pre>\n<h3>types.ts (message offscreen, typed)</h3>\n<pre><code class=\"language-ts\">// extension/src/shared/types.ts (thêm)\nexport interface ParseDomRequest {\n  target: \"offscreen\";\n  type: \"PARSE_DOM\";\n  html: string;            // HTML THÔ đã có sẵn (content script lấy), KHÔNG phải URL\n  platform: \"shopee\" | \"tiktok\" | \"lazada\";\n}\nexport interface ParseDomResult {\n  type: \"PARSE_DOM_RESULT\";\n  items: Array&lt;{ productId: string; price: number; qty: number }&gt;;\n}</code></pre>\n<h3>manager.ts (vòng đời ngắn: kiểm -&gt; tạo -&gt; dùng -&gt; đóng)</h3>\n<pre><code class=\"language-ts\">// extension/src/offscreen/manager.ts\nconst OFFSCREEN_PATH = \"offscreen/offscreen.html\";\n\nexport async function parseDomOffscreen(req: ParseDomRequest): Promise&lt;ParseDomResult&gt; {\n  if (!(await chrome.offscreen.hasDocument())) {\n    await chrome.offscreen.createDocument({\n      url: OFFSCREEN_PATH,\n      reasons: [\"DOM_SCRAPING\"],     // reason tường minh\n      justification: \"Parse HTML giỏ hàng đã render ngoài service worker\"\n    });\n  }\n  try {\n    const res = await sendWithTimeout(req, 10_000); // trần 10s\n    return res as ParseDomResult;\n  } finally {\n    await chrome.offscreen.closeOffscreenDocument(); // đóng NGAY sau khi xong\n  }\n}</code></pre>\n<h3>rules.json (DNR static, tối thiểu)</h3>\n<pre><code class=\"language-json\">[]</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>manifest.json</code> có <code>permissions</code> chứa <code>offscreen</code> + <code>declarativeNetRequest</code>, và <code>declarative_net_request.rule_resources</code> trỏ <code>dnr/rules.json</code>.</li><li>Grep <code>src/**</code>: KHÔNG có <code>chrome.webRequest</code> ở chế độ blocking (không <code>onBeforeRequest</code> với <code>[\"blocking\"]</code>).</li><li><code>manager.ts</code> gọi <code>hasDocument()</code> trước khi <code>createDocument</code>; không tạo khi đã có tài liệu (test).</li><li>Sau khi parse xong, <code>closeOffscreenDocument</code> được gọi (test khẳng định offscreen được đóng, không để mở).</li><li>Tài liệu offscreen tạo với <code>reasons</code> tường minh (<code>DOM_SCRAPING</code>/<code>CLIPBOARD</code>) + <code>justification</code> không rỗng.</li><li>Grep <code>offscreen/**</code>: KHÔNG có <code>fetch(</code> tới domain sàn (offscreen không tự đọc trang sàn).</li><li>Kết quả parse offscreen đi qua <code>minimize()</code> (TASK-EXT-003) trước khi rời client (test đường dẫn).</li><li>DNR rule trong <code>rules.json</code> là tập tối thiểu (test đếm + phạm vi); không có rule rộng chặn/sửa request sàn lạm dụng.</li><li>Tác vụ offscreen quá 10s -&gt; tự đóng tài liệu (test timeout), không treo SW.</li><li><code>createDocument</code> lỗi (đã có tài liệu) -&gt; tái dùng/báo lỗi rõ, không retry vô hạn.</li><li>Message SW &lt;-&gt; offscreen là <code>ParseDomRequest</code>/<code>ParseDomResult</code> typed với <code>target: \"offscreen\"</code>; một message thiếu <code>target</code> hoặc sai kiểu bị <code>tsc</code> bắt lúc biên dịch (§1 #9).</li><li><code>npm test</code> xanh; <code>tsc --noEmit</code> sạch.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-ts\">// extension/test/offscreen-lifecycle.test.ts\nimport { parseDomOffscreen } from \"../src/offscreen/manager\";\n\ntest(\"tạo offscreen rồi đóng ngay sau khi xong\", async () =&gt; {\n  const calls: string[] = [];\n  globalThis.chrome = fakeOffscreen(calls); // ghi lại create/close\n  await parseDomOffscreen({ target:\"offscreen\", type:\"PARSE_DOM\", html:\"&lt;div&gt;&lt;/div&gt;\", platform:\"shopee\" });\n  expect(calls).toContain(\"create\");\n  expect(calls).toContain(\"close\");        // đóng NGAY, không để mở thường trực\n});\n\ntest(\"không tạo offscreen mới khi đã có tài liệu\", async () =&gt; {\n  const calls: string[] = [];\n  globalThis.chrome = fakeOffscreen(calls, /*hasDocument*/ true);\n  await parseDomOffscreen({ target:\"offscreen\", type:\"PARSE_DOM\", html:\"&lt;div&gt;&lt;/div&gt;\", platform:\"shopee\" });\n  expect(calls.filter(c =&gt; c === \"create\")).toHaveLength(0); // tái dùng\n});</code></pre>\n<pre><code class=\"language-ts\">// extension/test/dnr-rules.test.ts\nimport rules from \"../src/dnr/rules.json\";\n\ntest(\"DNR rule là tập tối thiểu, không webRequest blocking\", async () =&gt; {\n  expect(Array.isArray(rules)).toBe(true);\n  expect(rules.length).toBeLessThanOrEqual(5);          // tối thiểu\n  const src = await readFile(\"src/dnr/dnr.ts\", \"utf8\");\n  expect(src).not.toMatch(/webRequest/);                // không webRequest blocking\n});</code></pre>\n<pre><code class=\"language-ts\">// extension/test/offscreen-no-fetch.test.ts\ntest(\"offscreen KHÔNG tự fetch trang sàn\", async () =&gt; {\n  for (const f of [\"offscreen\", \"manager\"]) {\n    const src = await readFile(`src/offscreen/${f}.ts`, \"utf8\");\n    expect(src).not.toMatch(/fetch\\([\"'`]https:\\/\\/(shopee|tiktok|lazada)/);\n  }\n});</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: cập nhật <code>manifest.json</code> (permissions offscreen + DNR + rule_resources) + <code>types.ts</code> (ParseDomRequest/Result) -&gt; <code>offscreen.html</code> + <code>offscreen.ts</code> (tài liệu nhận HTML thô, parse, trả) -&gt; <code>manager.ts</code> (kiểm hasDocument -&gt; create reason -&gt; sendWithTimeout -&gt; close) -&gt; <code>rules.json</code> + <code>dnr.ts</code> (rule static tối thiểu + sanity check) -&gt; nối kết quả parse vào <code>minimize()</code> (TASK-EXT-003) -&gt; tests. Offscreen chỉ là công cụ nhất thời cho DOM/clipboard; content script vẫn là đường đọc trang sàn duy nhất. DNR rule khai static để Web Store và audit kiểm được.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-EXT-001</strong> - scaffold MV3 + service worker + manifest + messaging làm khung cho offscreen/DNR.</li><li><strong>TASK-EXT-002</strong> - content script lấy HTML thô trang sàn rồi giao cho offscreen parse (offscreen không tự đọc).</li><li><strong>TASK-EXT-003 (downstream)</strong> - kết quả parse offscreen đi qua pipeline tối thiểu hóa trước khi rời client.</li><li><strong>TASK-EXT-005 (downstream)</strong> - lớp đồng bộ gửi payload đã sạch; DNR/offscreen không phải đường tắt ra mạng.</li><li><strong>NFR-EXT-001</strong> - offscreen vòng đời ngắn để không phá ràng buộc SW ephemeral.</li><li>Nền tảng: Chrome/Chromium MV3 &gt;=120 (chrome.offscreen, chrome.declarativeNetRequest).</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>SW yêu cầu offscreen parse HTML thô (typed)</h3>\n<pre><code class=\"language-json\">{\n  \"target\": \"offscreen\",\n  \"type\": \"PARSE_DOM\",\n  \"platform\": \"shopee\",\n  \"html\": \"&lt;div class='cart-item'&gt;...HTML đã render do content script lấy...&lt;/div&gt;\"\n}</code></pre>\n<h3>Offscreen trả kết quả (đi tiếp qua minimize)</h3>\n<pre><code class=\"language-json\">{\n  \"type\": \"PARSE_DOM_RESULT\",\n  \"items\": [ { \"productId\": \"90112\", \"price\": 89000, \"qty\": 1 } ]\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Có cần offscreen cho audio/iframe ngoài DOM_SCRAPING/CLIPBOARD hay không - chỉ thêm reason khi có task tiêu thụ.</li><li>DNR rule cụ thể (nếu có nhu cầu chặn tracker bên thứ ba của chính extension) - thêm khi có yêu cầu rõ + review niềm tin.</li><li>Tái dùng một offscreen cho nhiều parse liên tiếp (gộp) thay vì tạo/đóng mỗi lần - tối ưu sau nếu đo thấy chi phí tạo/đóng đáng kể.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Parse DOM nặng trong SW</td><td>kết quả sai không tái hiện</td><td>parse hỏng thầm lặng</td><td>Đẩy vào offscreen có DOM thật (DEC-EXT-18)</td></tr><tr><td>Offscreen để mở thường trực</td><td>lifecycle test (thiếu close)</td><td>giữ SW sống + tốn RAM</td><td>Đóng ngay sau khi xong (DEC-EXT-19)</td></tr><tr><td>Tạo 2 tài liệu offscreen</td><td>createDocument lỗi</td><td>exception</td><td>Kiểm hasDocument trước (§1 #2)</td></tr><tr><td>webRequest blocking</td><td>grep test + Web Store reject</td><td>can thiệp request không chạy</td><td>Dùng declarativeNetRequest (DEC-EXT-20)</td></tr><tr><td>DNR rule rộng chặn/sửa request sàn</td><td>dnr-rules test + review</td><td>nghi thao túng (§5.4)</td><td>Rule static tối thiểu, có lý do (DEC-EXT-21)</td></tr><tr><td>Offscreen tự fetch trang sàn</td><td>offscreen-no-fetch test</td><td>đường đọc thứ hai vòng pipeline</td><td>Chỉ nhận HTML thô, không fetch (DEC-EXT-22)</td></tr><tr><td>Tác vụ offscreen treo</td><td>timeout test</td><td>tài liệu mở mãi, rò RAM</td><td>Trần 10s rồi tự đóng (§1 #12)</td></tr><tr><td>Kết quả parse bỏ qua minimize</td><td>đường-dẫn test</td><td>rò trường thừa</td><td>Mọi kết quả qua TASK-EXT-003 (§1 #7)</td></tr><tr><td>Quyền offscreen/DNR thiếu trong manifest</td><td>manifest check</td><td>API không chạy</td><td>Thêm permissions tối thiểu (§1 #8)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Offscreen là cách MV3 thay background-page-có-DOM: dùng cho parse HTML nặng và clipboard, nhưng phải vòng đời ngắn để không phá ephemeral.</li><li>declarativeNetRequest thay webRequest blocking là bắt buộc ở MV3; DNR rule khai static + tối thiểu để Web Store và audit (TASK-TRUST-003) kiểm được.</li><li>Offscreen KHÔNG tự đọc trang sàn - content script (TASK-EXT-002) là đường đọc duy nhất; offscreen chỉ parse HTML thô đã có.</li><li>Mọi dữ liệu ra từ offscreen vẫn qua pipeline tối thiểu hóa (TASK-EXT-003) - không có đường tắt bỏ qua allowlist.</li><li>Khả năng can thiệp request (DNR) là quyền lực nhạy cảm hậu-Honey; giữ nó tối thiểu và minh bạch là mắt xích niềm tin (§5.4).</li></ul>\n<hr>\n<p><em>Hết TASK-EXT-004. Status: done (mục tiêu audit 10/10).</em></p>";
