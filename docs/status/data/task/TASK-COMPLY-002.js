window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-COMPLY-002"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service COMPLY <strong>MUST</strong> duy trì sổ đăng ký DPIA (Data Protection Impact Assessment) và TIA (Transfer Impact Assessment): mỗi hoạt động xử lý dữ liệu cá nhân có một DPIA với hạn nộp 60 ngày và chu kỳ cập nhật 6 tháng; hoạt động chuyển dữ liệu xuyên biên giới có TIA gắn kèm. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> định nghĩa bảng <code>processing_activity (id, name, purpose_key, data_categories, started_at, cross_border, recipient_country, created_at)</code> - mỗi hoạt động xử lý đăng ký một dòng. <code>purpose_key</code> liên kết tập purpose của TASK-COMPLY-001.</li><li><strong>MUST</strong> định nghĩa bảng <code>dpia (id, activity_id, version, risk_level, mitigation_vi, status, filed_at, last_reviewed_at, created_at)</code> versioned: cập nhật DPIA là tạo version mới, giữ bản cũ (DEC-COMPLY-07).</li><li><strong>MUST</strong> định nghĩa bảng <code>tia (id, dpia_id, recipient_country, safeguard_vi, created_at)</code> cho đánh giá tác động chuyển dữ liệu; bắt buộc tồn tại khi <code>processing_activity.cross_border = true</code> (DEC-COMPLY-08).</li><li><strong>MUST</strong> tính <code>filing_due_at = started_at + INTERVAL '60 days'</code> (DEC-COMPLY-06): hạn nộp DPIA là 60 ngày kể từ khi bắt đầu xử lý.</li><li><strong>MUST</strong> tính <code>review_due_at = COALESCE(last_reviewed_at, filed_at) + INTERVAL '6 months'</code> (DEC-COMPLY-07): DPIA phải được rà soát lại tối thiểu mỗi 6 tháng.</li><li><strong>MUST</strong> suy ra trạng thái filing từ deadline, KHÔNG nhập tay (DEC-COMPLY-09):</li></ol>\n<ul><li><code>draft</code> - chưa <code>filed_at</code>, còn trong 60 ngày.</li><li><code>submitted</code> - đã <code>filed_at</code> trong hạn.</li><li><code>overdue</code> - chưa <code>filed_at</code> mà đã quá <code>filing_due_at</code>.</li><li><code>review_overdue</code> - đã nộp nhưng quá <code>review_due_at</code>.</li></ul>\n<ol><li><strong>MUST</strong> chặn tạo <code>processing_activity</code> với <code>cross_border = true</code> mà không có TIA: hàm tạo phải yêu cầu thông tin <code>recipient_country</code> + <code>safeguard</code> và sinh TIA cùng lúc.</li><li><strong>MUST</strong> expose hàm:</li></ol>\n<ul><li><code>RegisterActivity(ctx, a ProcessingActivity, dpia DPIAInput) (int64, error)</code> - tạo activity + DPIA v1 (+ TIA nếu cross_border); trả activity_id.</li><li><code>ReviewDPIA(ctx, activityID int64, in DPIAInput) error</code> - tạo DPIA version mới, set <code>last_reviewed_at = now()</code>.</li><li><code>MarkFiled(ctx, dpiaID int64) error</code> - set <code>filed_at = now()</code> (đã nộp cho cơ quan).</li><li><code>Overdue(ctx) ([]ActivityStatus, error)</code> - liệt kê hoạt động <code>overdue</code> hoặc <code>review_overdue</code>.</li></ul>\n<ol><li><strong>MUST</strong> liệt kê được \"sắp tới hạn\" (due trong N ngày) để cảnh báo trước khi quá hạn.</li><li><strong>SHOULD</strong> phát OTel gauge <code>dpia_overdue_total</code>, <code>dpia_review_due_soon_total</code>; báo cáo định kỳ qua Grafana.</li><li><strong>MUST</strong> giữ <code>risk_level</code> trong tập <code>('low','medium','high')</code>; hoạt động <code>high</code> SHOULD kèm <code>mitigation_vi</code> không rỗng.</li><li><strong>MUST</strong> sinh báo cáo cho cơ quan: <code>Report(ctx)</code> trả danh sách hoạt động + DPIA hiện hành + TIA + trạng thái + ngày nộp, đủ để xuất nộp.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao deadline tính từ dữ liệu, không nhập tay (DEC-COMPLY-09)?</strong> Trạng thái \"quá hạn\" mà nhập tay dễ bị bỏ sót hoặc tô hồng. Tính <code>filing_due_at</code> và <code>review_due_at</code> trực tiếp từ <code>started_at</code>/<code>last_reviewed_at</code> cho ra trạng thái khách quan: hệ thống tự biết hoạt động nào quá 60 ngày chưa nộp, hoạt động nào quá 6 tháng chưa review. Đây là điểm chống \"compliance hình thức\".</p>\n<p><strong>Vì sao DPIA versioned (DEC-COMPLY-07)?</strong> Cập nhật mỗi 6 tháng nghĩa là DPIA tiến hóa theo thời gian (rủi ro mới, biện pháp mới). Giữ version cho phép chứng minh lịch sử rà soát: tại mỗi mốc 6 tháng, đánh giá là gì. Cơ quan có thể hỏi \"lần review gần nhất ngày nào, thay đổi gì\" - version trả lời được.</p>\n<p><strong>Vì sao TIA bắt buộc khi cross_border (DEC-COMPLY-08)?</strong> Chuyển dữ liệu xuyên biên giới là nhóm rủi ro PDPL chế tài nặng nhất (tới 5% doanh thu năm trước). Khi mở SEA, dữ liệu có thể rời VN. TIA ghi rõ nước nhận + cơ chế bảo vệ (safeguard). Bắt buộc tạo TIA cùng activity tránh việc bật chuyển dữ liệu mà quên đánh giá.</p>\n<p><strong>Vì sao gắn <code>purpose_key</code> với TASK-COMPLY-001 (§1 #1)?</strong> DPIA và consent là hai mặt của cùng một hoạt động xử lý: consent là cơ sở pháp lý, DPIA là đánh giá rủi ro. Liên kết qua <code>purpose_key</code> giữ hai sổ đồng bộ - mỗi mục đích xử lý vừa có consent vừa có DPIA.</p>\n<p><strong>Vì sao \"due trong N ngày\" (§1 #9)?</strong> Phát hiện quá hạn sau khi đã quá hạn là quá muộn. Cảnh báo trước (ví dụ 14 ngày trước <code>filing_due_at</code> hoặc <code>review_due_at</code>) cho đội pháp lý thời gian chuẩn bị, biến deadline thành quy trình chủ động.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migrations</h3>\n<pre><code class=\"language-sql\">-- services/comply/migrations/0003_processing_activity.sql\nCREATE TABLE processing_activity (\n  id               BIGSERIAL   PRIMARY KEY,\n  name             TEXT        NOT NULL,\n  purpose_key      TEXT        NOT NULL,\n  data_categories  TEXT[]      NOT NULL,\n  started_at       TIMESTAMPTZ NOT NULL,\n  cross_border     BOOLEAN     NOT NULL DEFAULT false,\n  recipient_country TEXT,\n  created_at       TIMESTAMPTZ NOT NULL DEFAULT now(),\n  CHECK (NOT cross_border OR recipient_country IS NOT NULL)\n);\n\n-- services/comply/migrations/0004_dpia_register.sql\nCREATE TABLE dpia (\n  id               BIGSERIAL   PRIMARY KEY,\n  activity_id      BIGINT      NOT NULL REFERENCES processing_activity(id),\n  version          INTEGER     NOT NULL,\n  risk_level       TEXT        NOT NULL CHECK (risk_level IN ('low','medium','high')),\n  mitigation_vi    TEXT,\n  status           TEXT        NOT NULL DEFAULT 'draft',\n  filed_at         TIMESTAMPTZ,\n  last_reviewed_at TIMESTAMPTZ,\n  created_at       TIMESTAMPTZ NOT NULL DEFAULT now(),\n  UNIQUE (activity_id, version)\n);\n\nCREATE TABLE tia (\n  id                BIGSERIAL   PRIMARY KEY,\n  dpia_id           BIGINT      NOT NULL REFERENCES dpia(id),\n  recipient_country TEXT        NOT NULL,\n  safeguard_vi      TEXT        NOT NULL,\n  created_at        TIMESTAMPTZ NOT NULL DEFAULT now()\n);\n\n-- Truy van sap toi han / qua han.\nCREATE INDEX idx_dpia_activity ON dpia (activity_id, version DESC);</code></pre>\n<h3>Deadline (Go)</h3>\n<pre><code class=\"language-go\">// services/comply/internal/dpia/deadline.go\nconst (\n    FilingWindow = 60 * 24 * time.Hour       // 60 ngay\n    ReviewCycle  = 6 * 30 * 24 * time.Hour   // ~6 thang (xap xi; doi chieu lich thuc te)\n)\n\n// Status suy tu deadline, KHONG nhap tay.\nfunc Status(a ProcessingActivity, d DPIA, now time.Time) string {\n    filingDue := a.StartedAt.Add(FilingWindow)\n    if d.FiledAt == nil {\n        if now.After(filingDue) {\n            return \"overdue\"\n        }\n        return \"draft\"\n    }\n    base := d.FiledAt\n    if d.LastReviewedAt != nil {\n        base = d.LastReviewedAt\n    }\n    if now.After(base.Add(ReviewCycle)) {\n        return \"review_overdue\"\n    }\n    return \"submitted\"\n}</code></pre>\n<h3>Register (Go)</h3>\n<pre><code class=\"language-go\">// services/comply/internal/dpia/register.go\n// RegisterActivity tao activity + DPIA v1; bat buoc TIA khi cross_border.\nfunc (s *Service) RegisterActivity(ctx context.Context, a ProcessingActivity, in DPIAInput) (int64, error) {\n    if a.CrossBorder &amp;&amp; (in.TIA == nil || in.TIA.Safeguard == \"\") {\n        return 0, ErrTIARequired // cross-border phai co TIA\n    }\n    return s.repo.createWithDPIA(ctx, a, in)\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch -&gt; <code>processing_activity</code>, <code>dpia</code>, <code>tia</code> tồn tại.</li><li><code>RegisterActivity</code> (cross_border=false) -&gt; tạo activity + DPIA version 1, status <code>draft</code>.</li><li>DPIA mới có <code>filing_due_at = started_at + 60 ngày</code> (kiểm qua <code>Status</code>).</li><li>Activity bắt đầu cách đây 61 ngày, chưa <code>filed_at</code> -&gt; <code>Status = overdue</code>.</li><li><code>MarkFiled</code> trong hạn -&gt; <code>Status = submitted</code>.</li><li>DPIA đã nộp, <code>last_reviewed_at</code> cách đây hơn 6 tháng -&gt; <code>Status = review_overdue</code>.</li><li><code>RegisterActivity</code> (cross_border=true) thiếu TIA -&gt; lỗi <code>ErrTIARequired</code>, không tạo gì.</li><li><code>RegisterActivity</code> (cross_border=true) có TIA -&gt; tạo activity + DPIA + 1 dòng <code>tia</code> với recipient_country + safeguard.</li><li><code>ReviewDPIA</code> -&gt; tạo DPIA version 2, <code>last_reviewed_at = now()</code>; version 1 vẫn còn.</li><li>INSERT <code>risk_level = 'critical'</code> -&gt; lỗi CHECK constraint.</li><li>INSERT <code>processing_activity</code> cross_border=true mà <code>recipient_country IS NULL</code> -&gt; lỗi CHECK.</li><li><code>Overdue()</code> trả đúng các activity <code>overdue</code> và <code>review_overdue</code>; <code>Report()</code> xuất đủ activity + DPIA hiện hành + TIA + status.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/comply/internal/dpia/deadline_test.go\nfunc TestDeadline_FilingOverdueAfter60d(t *testing.T) {\n    a := ProcessingActivity{StartedAt: now.Add(-61 * 24 * time.Hour)}\n    d := DPIA{FiledAt: nil}\n    require.Equal(t, \"overdue\", Status(a, d, now))\n}\n\nfunc TestDeadline_DraftWithin60d(t *testing.T) {\n    a := ProcessingActivity{StartedAt: now.Add(-10 * 24 * time.Hour)}\n    require.Equal(t, \"draft\", Status(a, DPIA{FiledAt: nil}, now))\n}\n\nfunc TestDeadline_ReviewOverdueAfter6m(t *testing.T) {\n    filed := now.Add(-200 * 24 * time.Hour) // &gt; 6 thang\n    a := ProcessingActivity{StartedAt: now.Add(-220 * 24 * time.Hour)}\n    d := DPIA{FiledAt: &amp;filed}\n    require.Equal(t, \"review_overdue\", Status(a, d, now))\n}\n\n// services/comply/internal/dpia/repo_test.go\nfunc TestRegister_CrossBorderRequiresTIA(t *testing.T) {\n    s := setup(t)\n    a := ProcessingActivity{Name: \"SEA analytics\", CrossBorder: true, RecipientCountry: \"ID\"}\n    _, err := s.RegisterActivity(ctx, a, DPIAInput{RiskLevel: \"high\"}) // thieu TIA\n    require.ErrorIs(t, err, ErrTIARequired)\n}\n\nfunc TestRegister_CrossBorderWithTIA_CreatesTIA(t *testing.T) {\n    s := setup(t)\n    a := ProcessingActivity{Name: \"SEA analytics\", CrossBorder: true, RecipientCountry: \"ID\"}\n    in := DPIAInput{RiskLevel: \"high\", TIA: &amp;TIAInput{RecipientCountry: \"ID\", Safeguard: \"SCC + ma hoa\"}}\n    id, err := s.RegisterActivity(ctx, a, in)\n    require.NoError(t, err)\n    require.Equal(t, 1, countTIA(t, s, id))\n}\n\nfunc TestReview_CreatesNewVersion(t *testing.T) {\n    s := setup(t)\n    id, _ := s.RegisterActivity(ctx, basicActivity(), DPIAInput{RiskLevel: \"low\"})\n    require.NoError(t, s.ReviewDPIA(ctx, id, DPIAInput{RiskLevel: \"medium\"}))\n    require.Equal(t, 2, maxDPIAVersion(t, s, id))\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration 0003 (processing_activity) -&gt; 0004 (dpia + tia) -&gt; types.go + deadline.go (logic deadline thuần, dễ test) -&gt; register.go (ràng buộc TIA) -&gt; repo.go (overdue/report) -&gt; tests. Hằng <code>ReviewCycle</code> dùng xấp xỉ 6 tháng theo ngày; nếu cần chính xác theo lịch (tháng dương lịch), dùng <code>AddDate(0, 6, 0)</code> ở tầng query thay vì cộng Duration. Logic <code>Status</code> tách thuần hàm để test không cần DB.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-COMPLY-001</strong> - tập purpose (<code>purpose_key</code>) là cơ sở liên kết hoạt động xử lý với consent.</li><li><strong>TASK-COMPLY-007 (downstream)</strong> - adapter SEA dùng cross_border + TIA khi dữ liệu rời VN sang ID/TH.</li><li><strong>TASK-B2B-001 (liên quan)</strong> - pipeline dữ liệu B2B ẩn danh là một <code>processing_activity</code> cần DPIA riêng.</li><li><strong>TASK-COMPLY-003 (liên quan)</strong> - DSAR có thể tham chiếu danh mục <code>data_categories</code> của hoạt động.</li><li>Lib: driver <code>pgx</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Đăng ký hoạt động xử lý có chuyển dữ liệu xuyên biên giới</h3>\n<pre><code class=\"language-json\">POST /v1/comply/processing-activity\n{\n  \"name\": \"Bao cao xu huong gia SEA\",\n  \"purpose_key\": \"analytics_b2b\",\n  \"data_categories\": [\"price_behavior_anonymized\"],\n  \"started_at\": \"2026-06-28T00:00:00+07:00\",\n  \"cross_border\": true,\n  \"recipient_country\": \"ID\",\n  \"dpia\": {\n    \"risk_level\": \"high\",\n    \"mitigation_vi\": \"An danh hoa k-anonymity truoc khi chuyen; ma hoa khi luu/chuyen.\",\n    \"tia\": { \"recipient_country\": \"ID\", \"safeguard_vi\": \"SCC + ma hoa AES-256\" }\n  }\n}</code></pre>\n<h3>Báo cáo hoạt động quá hạn (cho đội pháp lý)</h3>\n<pre><code class=\"language-sql\">-- Liet ke DPIA hien hanh qua han nop (&gt;60 ngay tu started_at, chua filed).\nSELECT pa.name, pa.started_at, d.version, d.status\nFROM processing_activity pa\nJOIN LATERAL (\n  SELECT * FROM dpia WHERE activity_id = pa.id ORDER BY version DESC LIMIT 1\n) d ON true\nWHERE d.filed_at IS NULL\n  AND now() &gt; pa.started_at + INTERVAL '60 days';</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt khung. Hoãn:</p>\n<ul><li>Tự động hóa nộp DPIA điện tử qua cổng cơ quan (nếu có API) - hiện xuất báo cáo thủ công.</li><li>Template DPIA theo loại hoạt động (scraping vs B2B vs marketing) - bổ sung mẫu sau.</li><li>Liên thông DPIA với risk register §9 tài liệu nguồn - đồng bộ thủ công giai đoạn đầu.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Quá 60 ngày chưa nộp DPIA</td><td><code>Status=overdue</code> + AC #4</td><td>vi phạm PDPL</td><td>Cảnh báo due-soon §1 #9; quy trình nộp trước hạn</td></tr><tr><td>Quá 6 tháng chưa review</td><td><code>Status=review_overdue</code> + AC #6</td><td>DPIA lỗi thời</td><td>Gauge review_due_soon; lịch review định kỳ</td></tr><tr><td>Cross-border thiếu TIA</td><td><code>ErrTIARequired</code> + AC #7</td><td>chuyển dữ liệu không đánh giá</td><td>Hàm tạo bắt buộc TIA; CHECK recipient_country</td></tr><tr><td>Nhập trạng thái filing tay</td><td>review §1 #6</td><td>che giấu quá hạn</td><td>Status suy từ deadline, không có setter tay</td></tr><tr><td>recipient_country trống khi cross_border</td><td>DB CHECK + AC #11</td><td>TIA thiếu đối tượng</td><td>CHECK constraint chặn</td></tr><tr><td>risk_level lạ</td><td>DB CHECK + AC #10</td><td>phân loại sai</td><td>CHECK IN low/medium/high</td></tr><tr><td>Review ghi đè bản cũ</td><td>versioned + AC #9</td><td>mất lịch sử rà soát</td><td>DPIA versioned, tạo dòng mới</td></tr><tr><td>Hoạt động bật mà quên đăng ký</td><td>audit coverage</td><td>xử lý không có DPIA</td><td>Quy trình: bật hoạt động phải qua RegisterActivity</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>DPIA/TIA là tài liệu trách nhiệm giải trình PDPL: nộp trong 60 ngày, cập nhật mỗi 6 tháng.</li><li>Trạng thái quá hạn tính khách quan từ deadline, không phụ thuộc người nhập - chống compliance hình thức.</li><li>TIA bắt buộc cho chuyển dữ liệu xuyên biên giới, nhóm rủi ro chế tài nặng nhất (tới 5% doanh thu năm trước).</li><li>Liên kết <code>purpose_key</code> giữ sổ DPIA đồng bộ với sổ consent (TASK-COMPLY-001): mỗi mục đích xử lý có cả hai.</li><li>Khi mở SEA (TASK-COMPLY-007), mỗi luồng chuyển dữ liệu là một activity cross_border kèm TIA ghi rõ nước nhận và cơ chế bảo vệ.</li><li>DPIA versioned cho phép chứng minh lịch sử rà soát từng chu kỳ 6 tháng trước cơ quan.</li></ul>\n<hr>\n<p><em>Hết TASK-COMPLY-002. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
