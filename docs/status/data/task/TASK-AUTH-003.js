window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AUTH-003"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service AUTH <strong>MUST</strong> cung cấp liên kết tài khoản sàn qua bảng <code>platform_account</code> chỉ lưu định danh ẩn danh, ràng buộc một liên kết mỗi (user, sàn), và TUYỆT ĐỐI không lưu token/cookie/mật khẩu sàn. Hợp đồng:</p>\n<ol><li>Migration <code>0006_platform_account</code> <strong>MUST</strong> tạo bảng theo §3.4: <code>id BIGSERIAL PK</code>, <code>user_id BIGINT REFERENCES app_user(id)</code>, <code>platform_id SMALLINT REFERENCES platform(id)</code>, <code>ext_user_ref TEXT</code>, <code>linked_at TIMESTAMPTZ DEFAULT now()</code>, với <code>UNIQUE(user_id, platform_id)</code> (DEC-AUTH-11).</li><li>Bảng <code>platform_account</code> <strong>MUST NOT</strong> có bất kỳ cột nào lưu cookie, session token, hay mật khẩu sàn (DEC-AUTH-12, DEC-AUTH-14). Đây là cấm về cấu trúc: không cột <code>token</code>, <code>cookie</code>, <code>session</code>, <code>password</code>.</li><li><code>ext_user_ref</code> <strong>MUST</strong> là định danh ẩn danh hóa (hash/pseudonym), KHÔNG phải username/email/số điện thoại sàn thật (DEC-AUTH-13). Mục đích chỉ để nhận biết liên kết, không để định danh chéo.</li><li>Liên kết <strong>MUST</strong> ép <code>UNIQUE(user_id, platform_id)</code> (DEC-AUTH-11): một người dùng chỉ một liên kết cho mỗi sàn. Liên kết lại cùng sàn -&gt; cập nhật (upsert), không tạo dòng trùng.</li><li>AUTH <strong>MUST</strong> expose: <code>LinkAccount(ctx, userID int64, platformID int16, extUserRef string) error</code>, <code>ListLinks(ctx, userID int64) ([]PlatformAccount, error)</code>, <code>Unlink(ctx, userID, platformID) error</code>.</li><li><code>LinkAccount</code> <strong>MUST</strong> validate <code>ext_user_ref</code>: không rỗng, độ dài trong giới hạn hợp lý (CHECK), và (theo quy ước) là dạng ẩn danh - KHÔNG nhận giá trị trông như email/cookie/token thô.</li><li>Liên kết <strong>MUST</strong> khởi tạo từ phía extension theo nguyên tắc session piggyback (DEC-AUTH-15, §3.2): extension đọc ngữ cảnh đăng nhập của chính người dùng và gửi về backend chỉ một <code>ext_user_ref</code> đã ẩn danh - KHÔNG gửi cookie/token.</li><li><code>Unlink</code> <strong>MUST</strong> xóa liên kết của (user, sàn); thao tác idempotent (unlink khi chưa liên kết không lỗi nghiêm trọng).</li><li>Hệ thống <strong>MUST</strong> KHÔNG log <code>ext_user_ref</code> ở mức có thể tái định danh; coi nó như dữ liệu cá nhân giả danh theo PDPL.</li><li>Schema + lớp truy cập <strong>MUST</strong> là điểm chứng minh cho audit TASK-TRUST-003 và TASK-COMPLY-005 rằng không có credential sàn nào (cookie/token/mật khẩu) tồn tại trên server.</li><li><code>ListLinks</code> <strong>MUST</strong> chỉ trả liên kết của đúng <code>user_id</code> truyền vào (cô lập theo người dùng, không rò rỉ chéo).</li><li>AUTH <strong>SHOULD</strong> phát metric <code>platform_account_linked_total{platform_id}</code> / <code>platform_account_unlinked_total{platform_id}</code> - không kèm <code>ext_user_ref</code>.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p>Vì sao tuyệt đối không lưu token phiên (DEC-AUTH-12)? Đây là cam kết niềm tin trung tâm của SănDeal sau vụ Honey (§5.4). Extension đọc cookie phiên sàn của chính người dùng để lấy giỏ hàng - một hành vi vốn đã dễ bị nghi là malware. Cách duy nhất biến nghi ngờ thành niềm tin là token phiên KHÔNG bao giờ rời máy người dùng và KHÔNG bao giờ chạm server. Nếu server lưu token, một lần DB lộ là chiếm tài khoản sàn hàng loạt - vừa vi phạm PDPL nặng vừa kết liễu sản phẩm.</p>\n<p>Vì sao cấm token về cấu trúc, không chỉ quy ước (§1 #2, DEC-AUTH-14)? Quy ước \"đừng lưu token\" dễ bị phá bởi một commit vô tình thêm cột tiện tay. Làm schema không có chỗ chứa token (không cột nào hợp) biến cam kết thành bất biến kiểm được: một test introspection xác nhận bảng không có cột token là bằng chứng máy kiểm, không phụ thuộc kỷ luật con người.</p>\n<p>Vì sao ext_user_ref ẩn danh, không username sàn thật (DEC-AUTH-13)? Nếu lưu username/email sàn thật, server nắm bản đồ \"người dùng SănDeal X = tài khoản Shopee Y = tài khoản Lazada Z\" - một kho định danh chéo sàn nhạy cảm, hấp dẫn kẻ tấn công và rủi ro PDPL. Một pseudonym chỉ đủ để biết \"có một liên kết\" mà không tái dựng được danh tính thật.</p>\n<p>Vì sao UNIQUE(user_id, platform_id) (DEC-AUTH-11)? Một người dùng SănDeal ứng với một tài khoản trên mỗi sàn trong mô hình này. Cho phép nhiều liên kết cùng sàn gây nhập nhằng (liên kết nào là chính?) và mở đường lạm dụng. Ràng buộc một-liên-kết-mỗi-sàn giữ mô hình rõ; liên kết lại là cập nhật, không nhân bản.</p>\n<p>Vì sao liên kết khởi tạo từ extension tối thiểu hóa (DEC-AUTH-15)? Kiến trúc session piggyback (§3.2) đặt mọi tiếp xúc với phiên sàn ở phía client. Backend chỉ nhận kết quả đã ẩn danh (<code>ext_user_ref</code>), không nhận nguyên liệu nhạy cảm (cookie/token). Giữ ranh giới này là cách kỹ thuật để cam kết \"token không rời client\" thành thật, không chỉ là lời hứa.</p>\n<p>Vì sao coi ext_user_ref là dữ liệu giả danh PDPL (§1 #9)? Dù đã ẩn danh, một pseudonym vẫn có thể là dữ liệu cá nhân giả danh nếu ghép thêm dữ liệu khác. Đối xử thận trọng (không log tái-định-danh được, đưa vào phạm vi DSAR của TASK-AUTH-005) giữ ta đúng tinh thần PDPL.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration (cấm token về cấu trúc)</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0006_platform_account.up.sql\nCREATE TABLE platform_account (\n  id           BIGSERIAL    PRIMARY KEY,\n  user_id      BIGINT       NOT NULL REFERENCES app_user(id),\n  platform_id  SMALLINT     NOT NULL REFERENCES platform(id),\n  ext_user_ref TEXT         NOT NULL\n                 CHECK (length(ext_user_ref) BETWEEN 1 AND 128), -- §1 #6\n  linked_at    TIMESTAMPTZ  DEFAULT now(),\n  UNIQUE (user_id, platform_id)                                   -- §1 #4\n  -- CỐ Ý KHÔNG có cột cookie/session/token/password (DEC-AUTH-12, §1 #2).\n);\nCREATE INDEX idx_pa_user ON platform_account (user_id);</code></pre>\n<h3>Liên kết (§1 #5, #6, #7)</h3>\n<pre><code class=\"language-go\">// services/auth/internal/auth/linkacct.go\nfunc (s *Service) LinkAccount(ctx context.Context, userID int64, platformID int16, extUserRef string) error {\n    if extUserRef == \"\" || len(extUserRef) &gt; 128 {\n        return ErrInvalidExtRef\n    }\n    if looksLikeRawCredential(extUserRef) { // chặn giá trị trông như email/cookie/token thô (§1 #6)\n        return ErrExtRefNotAnonymized\n    }\n    // Upsert: liên kết lại cùng sàn → cập nhật, không tạo dòng trùng (§1 #4).\n    return s.repo.Upsert(ctx, PlatformAccount{\n        UserID: userID, PlatformID: platformID, ExtUserRef: extUserRef,\n    })\n}\n\nfunc (s *Service) Unlink(ctx context.Context, userID int64, platformID int16) error {\n    return s.repo.Delete(ctx, userID, platformID) // idempotent (§1 #8)\n}</code></pre>\n<h3>Repo upsert (§1 #4, #11)</h3>\n<pre><code class=\"language-go\">// services/auth/internal/auth/linkacct_repo.go\nfunc (r *Repo) Upsert(ctx context.Context, pa PlatformAccount) error {\n    _, err := r.pool.Exec(ctx,\n        `INSERT INTO platform_account (user_id, platform_id, ext_user_ref)\n         VALUES ($1,$2,$3)\n         ON CONFLICT (user_id, platform_id)\n         DO UPDATE SET ext_user_ref = EXCLUDED.ext_user_ref, linked_at = now()`,\n        pa.UserID, pa.PlatformID, pa.ExtUserRef)\n    return err\n}\n\nfunc (r *Repo) ListByUser(ctx context.Context, userID int64) ([]PlatformAccount, error) {\n    // WHERE user_id = $1 — chỉ liên kết của đúng người dùng (§1 #11).\n    return r.query(ctx, `SELECT * FROM platform_account WHERE user_id=$1`, userID)\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy -&gt; bảng <code>platform_account</code> tồn tại với <code>UNIQUE(user_id, platform_id)</code>.</li><li>Introspection <code>information_schema.columns</code> của <code>platform_account</code> KHÔNG có cột tên chứa <code>token</code>/<code>cookie</code>/<code>session</code>/<code>password</code>.</li><li><code>LinkAccount(u1, shopee, \"ref-abc\")</code> -&gt; tạo một liên kết.</li><li><code>LinkAccount(u1, shopee, \"ref-xyz\")</code> lần hai (cùng sàn) -&gt; cập nhật <code>ext_user_ref</code>, vẫn một dòng (không trùng).</li><li><code>LinkAccount(u1, lazada, \"ref-2\")</code> -&gt; liên kết thứ hai (sàn khác) tồn tại song song.</li><li><code>LinkAccount(u1, shopee, \"\")</code> -&gt; <code>ErrInvalidExtRef</code>.</li><li><code>LinkAccount(u1, shopee, &lt;chuỗi 200 ký tự&gt;)</code> -&gt; lỗi (vượt CHECK độ dài).</li><li><code>LinkAccount(u1, shopee, \"chi@gmail.com\")</code> -&gt; <code>ErrExtRefNotAnonymized</code> (trông như email thật).</li><li><code>ListLinks(u1)</code> -&gt; trả đúng các liên kết của u1; <code>ListLinks(u2)</code> không thấy liên kết của u1.</li><li><code>Unlink(u1, shopee)</code> -&gt; xóa liên kết; <code>ListLinks(u1)</code> không còn shopee.</li><li><code>Unlink(u1, shopee)</code> lần hai (đã xóa) -&gt; không lỗi nghiêm trọng (idempotent).</li><li>Không có log nào chứa <code>ext_user_ref</code> ở dạng tái-định-danh được (kiểm review + test output).</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/auth/internal/auth/linkacct_test.go\nfunc TestSchema_NoCredentialColumns(t *testing.T) {\n    db := upToLatest(t)\n    cols := columnNames(t, db, \"platform_account\")\n    for _, c := range cols {\n        require.NotRegexp(t, `(?i)token|cookie|session|password`, c) // cấm cấu trúc (§1 #2)\n    }\n}\n\nfunc TestLink_NewAndUpsert(t *testing.T) {\n    s := newService(t)\n    require.NoError(t, s.LinkAccount(ctx, u1, shopee, \"ref-abc\"))\n    require.NoError(t, s.LinkAccount(ctx, u1, shopee, \"ref-xyz\")) // cùng sàn → upsert\n    links, _ := s.ListLinks(ctx, u1)\n    require.Len(t, links, 1)\n    require.Equal(t, \"ref-xyz\", links[0].ExtUserRef)\n}\n\nfunc TestLink_MultiPlatform(t *testing.T) {\n    s := newService(t)\n    s.LinkAccount(ctx, u1, shopee, \"ref-1\")\n    s.LinkAccount(ctx, u1, lazada, \"ref-2\")\n    links, _ := s.ListLinks(ctx, u1)\n    require.Len(t, links, 2)\n}\n\nfunc TestLink_RejectsRawCredential(t *testing.T) {\n    s := newService(t)\n    require.ErrorIs(t, s.LinkAccount(ctx, u1, shopee, \"\"), ErrInvalidExtRef)\n    require.ErrorIs(t, s.LinkAccount(ctx, u1, shopee, \"chi@gmail.com\"), ErrExtRefNotAnonymized)\n}\n\nfunc TestList_IsolatedPerUser(t *testing.T) {\n    s := newService(t)\n    s.LinkAccount(ctx, u1, shopee, \"ref-1\")\n    out, _ := s.ListLinks(ctx, u2)\n    require.Empty(t, out) // u2 không thấy liên kết của u1\n}\n\nfunc TestUnlink_Idempotent(t *testing.T) {\n    s := newService(t)\n    s.LinkAccount(ctx, u1, shopee, \"ref-1\")\n    require.NoError(t, s.Unlink(ctx, u1, shopee))\n    require.NoError(t, s.Unlink(ctx, u1, shopee)) // lần hai không lỗi\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Migration cố ý không có cột token - một test introspection (<code>TestSchema_NoCredentialColumns</code>) biến cam kết \"không lưu token\" thành bất biến máy kiểm. <code>LinkAccount</code> upsert theo <code>(user_id, platform_id)</code> nên liên kết lại là cập nhật. <code>looksLikeRawCredential</code> chặn giá trị trông như email/cookie/token thô để ép <code>ext_user_ref</code> đúng dạng ẩn danh. Liên kết khởi tạo từ extension (TASK-EXT-003) gửi chỉ <code>ext_user_ref</code> đã ẩn danh; backend không bao giờ nhận cookie/token. <code>ext_user_ref</code> vào phạm vi DSAR (TASK-AUTH-005) như dữ liệu giả danh.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li>TASK-AUTH-001 - <code>app_user</code> (chủ thể) và TASK-INFRA-002 - <code>platform</code> (sàn) phải tồn tại cho FK.</li><li>TASK-EXT-003 (đồng hàng/downstream) - pipeline tối thiểu hóa client tạo <code>ext_user_ref</code> ẩn danh, gửi về.</li><li>TASK-TRUST-002 / TASK-TRUST-003 (downstream) - minh bạch + audit độc lập dùng schema này làm bằng chứng không lưu credential sàn.</li><li>TASK-COMPLY-005 (đồng hàng) - audit no-cleartext + token-not-on-server.</li><li>TASK-AUTH-005 (downstream) - DSAR xóa cả <code>platform_account</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Liên kết từ extension (REST qua gateway)</h3>\n<pre><code class=\"language-http\">POST /v1/account/link HTTP/1.1\nAuthorization: Bearer &lt;access_jwt&gt;\nContent-Type: application/json\n\n{\"platform\":\"shopee\",\"ext_user_ref\":\"px_9f2c8e...a14\"}\n// CHÚ Ý: KHÔNG có cookie/token sàn trong payload — chỉ ref ẩn danh.</code></pre>\n<h3>Liệt kê liên kết</h3>\n<pre><code class=\"language-json\">{\n  \"links\": [\n    {\"platform\":\"shopee\",\"linked_at\":\"2026-06-27T08:00:00Z\"},\n    {\"platform\":\"lazada\",\"linked_at\":\"2026-06-27T08:01:00Z\"}\n  ]\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Cơ chế chứng thực liên kết (challenge để xác nhận người dùng thật sở hữu tài khoản sàn) mà vẫn không lưu token - nghiên cứu giai đoạn sau.</li><li>Nhiều tài khoản cùng sàn cho một người dùng (nếu mô hình thay đổi) - hiện ràng buộc một-mỗi-sàn.</li><li>Xoay <code>ext_user_ref</code> định kỳ để giảm khả năng tương quan dài hạn - tối ưu quyền riêng tư sau.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Thêm cột token/cookie</td><td>schema introspection test</td><td>thảm họa nếu DB lộ; vi phạm PDPL</td><td>Cấm cấu trúc + test (§1 #2)</td></tr><tr><td>Lưu username sàn thật</td><td>review + reject test</td><td>kho định danh chéo sàn</td><td>ext_user_ref ẩn danh (§1 #3)</td></tr><tr><td>Liên kết trùng cùng sàn</td><td>upsert + UNIQUE</td><td>nhập nhằng liên kết chính</td><td>UNIQUE(user,platform) + upsert (§1 #4)</td></tr><tr><td>ListLinks rò rỉ chéo user</td><td>isolation test</td><td>lộ liên kết người khác</td><td>WHERE user_id (§1 #11)</td></tr><tr><td>Gửi cookie/token trong payload</td><td>review + reject</td><td>token rời client (vỡ cam kết)</td><td>Chỉ nhận ext_user_ref (§1 #7)</td></tr><tr><td>Log ext_user_ref tái-định-danh</td><td>review</td><td>rò rỉ giả danh PDPL</td><td>Không log tái-định-danh (§1 #9)</td></tr><tr><td>ext_user_ref rỗng/quá dài</td><td>CHECK + validate</td><td>dữ liệu rác</td><td>Validate độ dài (§1 #6)</td></tr><tr><td>Unlink khi chưa liên kết lỗi cứng</td><td>idempotent test</td><td>UX gãy</td><td>Idempotent (§1 #8)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Đây là task mang cam kết niềm tin lõi hậu-Honey: token phiên sàn KHÔNG bao giờ rời client và KHÔNG bao giờ chạm server.</li><li>Cấm token về cấu trúc (schema không có chỗ chứa) + test introspection biến cam kết thành bất biến máy kiểm, không phụ thuộc kỷ luật con người.</li><li><code>ext_user_ref</code> ẩn danh tránh biến server thành kho bản đồ định danh chéo sàn nhạy cảm.</li><li>UNIQUE(user, platform) + upsert giữ mô hình một-liên-kết-mỗi-sàn rõ ràng; liên kết lại là cập nhật.</li><li>Ranh giới session piggyback (extension nhận nguyên liệu nhạy cảm, backend chỉ nhận ref ẩn danh) là cách kỹ thuật để \"token không rời client\" thành thật.</li><li>Schema này là bằng chứng cho audit độc lập (TASK-TRUST-003) và no-cleartext (TASK-COMPLY-005): không credential sàn nào tồn tại trên server.</li></ul>\n<hr>\n<p><em>Hết TASK-AUTH-003. Status: done (mục tiêu audit 10/10).</em></p>";
