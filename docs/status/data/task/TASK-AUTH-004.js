window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AUTH-004"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service AUTH <strong>SHOULD</strong> hỗ trợ đăng nhập qua OAuth/OIDC với Google, Facebook, Zalo, liên kết identity vào <code>app_user</code> an toàn, và phát hành cùng TokenPair của TASK-AUTH-002. Hợp đồng:</p>\n<ol><li>AUTH <strong>SHOULD</strong> hỗ trợ ít nhất Google (OIDC) ở slice này, với kiến trúc adapter cho Facebook và Zalo theo cùng interface (DEC-AUTH-16).</li><li>Flow <strong>MUST</strong> dùng Authorization Code + PKCE (DEC-AUTH-17): sinh <code>code_verifier</code>/<code>code_challenge</code>; tham số <code>state</code> ngẫu nhiên chống CSRF; với OIDC dùng <code>nonce</code> chống replay id_token.</li><li>Callback <strong>MUST</strong> kiểm <code>state</code> khớp giá trị đã phát (chống CSRF); <code>state</code> không khớp/thiếu -&gt; từ chối.</li><li>Với OIDC (Google), AUTH <strong>MUST</strong> verify <code>id_token</code> (chữ ký qua JWKS của provider, <code>aud</code> = client id, <code>iss</code> đúng, <code>nonce</code> khớp) trước khi tin claims.</li><li>Migration <code>0007_social_identity</code> <strong>MUST</strong> tạo bảng <code>social_identity (id, user_id, provider, subject, created_at)</code> với <code>UNIQUE(provider, subject)</code> (DEC-AUTH-18): một identity provider chỉ gắn một <code>app_user</code>.</li><li>Liên kết identity vào tài khoản hiện hữu <strong>MUST</strong> chỉ khi email từ provider đã được provider đánh dấu verify (<code>email_verified = true</code>) và khớp <code>app_user.email</code> (DEC-AUTH-19). Email CHƯA verify -&gt; KHÔNG auto-merge; tạo tài khoản mới hoặc yêu cầu bước xác minh.</li><li>Nếu chưa có <code>app_user</code> khớp, AUTH <strong>MUST</strong> tạo <code>app_user</code> mới (qua đường TASK-AUTH-001, <code>pwd_hash</code> có thể NULL cho tài khoản chỉ-social) và gắn <code>social_identity</code>.</li><li>Sau xác thực thành công, AUTH <strong>MUST</strong> phát hành cùng <code>TokenPair</code> của TASK-AUTH-002 (DEC-AUTH-20) - một đường token thống nhất, không token riêng cho social.</li><li>OAuth client secret và cấu hình provider <strong>MUST</strong> lấy qua TASK-INFRA-003, KHÔNG nhúng trong code/env.</li><li>AUTH <strong>MUST</strong> xử lý trường hợp một <code>app_user</code> liên kết nhiều provider (Google + Facebook cùng một người), miễn mỗi <code>(provider, subject)</code> là duy nhất.</li><li><code>state</code> và <code>code_verifier</code> <strong>MUST</strong> lưu tạm có thời hạn ngắn (ví dụ trong Redis với TTL vài phút) và dùng một lần, để callback chậm/lặp không bị lạm dụng.</li><li>AUTH <strong>SHOULD</strong> phát metric <code>social_login_total{provider, result}</code> (counter) - không kèm email/subject thô.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p>Vì sao Authorization Code + PKCE, không Implicit (DEC-AUTH-17)? Implicit flow trả token thẳng trên URL, dễ lộ qua lịch sử/referrer và đã bị khuyến nghị bỏ. Authorization Code đổi code lấy token ở backend; PKCE buộc bên đổi code phải biết <code>code_verifier</code> gốc, chống chặn-code. Đây là chuẩn hiện hành cho cả web và app.</p>\n<p>Vì sao state + nonce (§1 #2, #3, #4)? <code>state</code> chống CSRF: không có nó, kẻ tấn công ép nạn nhân hoàn tất một flow đăng nhập của kẻ tấn công (login CSRF). <code>nonce</code> (OIDC) chống replay id_token: token cũ phát lại sẽ lệch nonce. Hai tham số này nhỏ nhưng là khác biệt giữa OAuth đúng và OAuth hổng.</p>\n<p>Vì sao verify id_token thay vì tin thẳng (§1 #4)? id_token đến từ redirect do trình duyệt trung chuyển; tin claims mà không verify chữ ký là tin dữ liệu chưa xác thực. Verify qua JWKS của provider + kiểm <code>aud</code>/<code>iss</code>/<code>nonce</code> đảm bảo token đúng do provider phát cho đúng client này, không bị giả/chỉnh.</p>\n<p>Vì sao chỉ merge khi email verified (DEC-AUTH-19)? Đây là lỗ hổng account takeover kinh điển: kẻ tấn công tạo tài khoản provider khai email của nạn nhân (provider chưa verify), rồi đăng nhập social và được merge vào tài khoản nạn nhân. Chỉ merge khi <code>email_verified = true</code> cắt đường này: provider đã chứng minh chủ thể sở hữu email đó.</p>\n<p>Vì sao một đường token thống nhất (DEC-AUTH-20)? Sau khi xác thực (mật khẩu hay social), phần còn lại của hệ thống chỉ nên thấy một loại phiên. Phát cùng <code>TokenPair</code> của TASK-AUTH-002 cho social tránh nhân đôi logic phiên/refresh và giữ gateway/route đồng nhất bất kể cách đăng nhập.</p>\n<p>Vì sao state/verifier lưu tạm dùng một lần (§1 #11)? Nếu <code>state</code>/<code>code_verifier</code> sống lâu hoặc tái dùng được, một callback bị chặn/phát lại có thể bị lạm dụng. TTL ngắn + một-lần thu hẹp cửa sổ và khớp tính nhất thời của flow đăng nhập.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration social_identity</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0007_social_identity.up.sql\nCREATE TABLE social_identity (\n  id         BIGSERIAL   PRIMARY KEY,\n  user_id    BIGINT      NOT NULL REFERENCES app_user(id),\n  provider   TEXT        NOT NULL CHECK (provider IN ('google','facebook','zalo')),\n  subject    TEXT        NOT NULL,          -- 'sub' từ provider (định danh ổn định)\n  created_at TIMESTAMPTZ DEFAULT now(),\n  UNIQUE (provider, subject)                 -- §1 #5\n);\nCREATE INDEX idx_si_user ON social_identity (user_id);</code></pre>\n<h3>OAuth flow start + callback (§1 #2, #3, #4, #6)</h3>\n<pre><code class=\"language-go\">// services/auth/internal/auth/oauth.go\nfunc (s *Service) StartOAuth(ctx context.Context, provider string) (authURL string, err error) {\n    verifier := pkce.NewVerifier()\n    state := randToken()\n    s.tmp.Put(ctx, \"oauth:\"+state, oauthTmp{Verifier: verifier, Provider: provider}, 5*time.Minute) // §1 #11\n    p := s.providers[provider]\n    return p.AuthCodeURL(state, pkce.Challenge(verifier), oidcNonce(state)), nil\n}\n\n// services/auth/internal/auth/oauth_callback.go\nfunc (s *Service) OAuthCallback(ctx context.Context, provider, code, state string) (TokenPair, error) {\n    tmp, ok := s.tmp.Take(ctx, \"oauth:\"+state) // dùng một lần (§1 #11)\n    if !ok { return TokenPair{}, ErrBadState } // state sai/thiếu (§1 #3)\n    claims, err := s.providers[provider].ExchangeAndVerify(ctx, code, tmp.Verifier) // verify id_token (§1 #4)\n    if err != nil { return TokenPair{}, err }\n\n    uid, err := s.resolveUser(ctx, provider, claims) // §1 #6,#7\n    if err != nil { return TokenPair{}, err }\n    return s.IssueTokenPair(ctx, uid) // cùng đường token (§1 #8)\n}\n\nfunc (s *Service) resolveUser(ctx context.Context, provider string, c OIDCClaims) (int64, error) {\n    if uid, ok := s.repo.FindBySocial(ctx, provider, c.Subject); ok { return uid, nil }\n    if c.EmailVerified { // chỉ merge khi email verified (§1 #6)\n        if u, ok := s.repo.FindByEmail(ctx, c.Email); ok {\n            s.repo.LinkSocial(ctx, u.ID, provider, c.Subject)\n            return u.ID, nil\n        }\n    }\n    return s.createUserWithSocial(ctx, provider, c) // tạo mới (§1 #7)\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration -&gt; bảng <code>social_identity</code> với <code>UNIQUE(provider, subject)</code>.</li><li><code>StartOAuth(\"google\")</code> -&gt; trả authURL chứa <code>state</code>, <code>code_challenge</code>, <code>nonce</code>.</li><li>Callback với <code>state</code> không khớp/thiếu -&gt; <code>ErrBadState</code>.</li><li>Callback với <code>id_token</code> chữ ký sai (không verify được qua JWKS provider) -&gt; từ chối.</li><li>Callback với <code>id_token</code> <code>nonce</code> không khớp -&gt; từ chối.</li><li>Identity mới (chưa có app_user khớp) -&gt; tạo <code>app_user</code> mới + <code>social_identity</code>; phát TokenPair.</li><li>Identity có email <code>email_verified=true</code> khớp <code>app_user</code> hiện hữu -&gt; liên kết vào tài khoản đó (không tạo trùng).</li><li>Identity có email khớp nhưng <code>email_verified=false</code> -&gt; KHÔNG auto-merge (tạo mới hoặc yêu cầu xác minh).</li><li>Đăng nhập lại cùng provider+subject -&gt; tìm <code>app_user</code> cũ qua <code>FindBySocial</code>, không tạo trùng.</li><li>Một <code>app_user</code> liên kết Google rồi Facebook (subject khác) -&gt; hai dòng <code>social_identity</code>, cùng <code>user_id</code>.</li><li>OAuth client secret đọc qua provider secret (TASK-INFRA-003), không có trong code/env (grep).</li><li>Sau xác thực social -&gt; trả cùng cấu trúc <code>TokenPair</code> như đăng nhập mật khẩu (TASK-AUTH-002).</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/auth/internal/auth/oauth_test.go\nfunc TestCallback_BadState_Rejected(t *testing.T) {\n    s := newServiceWithFakeProvider(t)\n    _, err := s.OAuthCallback(ctx, \"google\", \"code\", \"unknown-state\")\n    require.ErrorIs(t, err, ErrBadState)\n}\n\nfunc TestCallback_NonceMismatch_Rejected(t *testing.T) {\n    s := newServiceWithFakeProvider(t)\n    url, _ := s.StartOAuth(ctx, \"google\")\n    state := extractState(url)\n    s.fakeProvider.SetIDToken(idTokenWithNonce(\"wrong\")) // nonce sai\n    _, err := s.OAuthCallback(ctx, \"google\", \"code\", state)\n    require.Error(t, err)\n}\n\nfunc TestResolve_VerifiedEmail_LinksExisting(t *testing.T) {\n    s := newServiceWithFakeProvider(t)\n    existing := s.seedUser(t, \"chi@x.com\")\n    uid, _ := s.resolveUser(ctx, \"google\", OIDCClaims{Subject: \"g-1\", Email: \"chi@x.com\", EmailVerified: true})\n    require.Equal(t, existing, uid) // liên kết vào tài khoản cũ\n}\n\nfunc TestResolve_UnverifiedEmail_NoMerge(t *testing.T) {\n    s := newServiceWithFakeProvider(t)\n    existing := s.seedUser(t, \"chi@x.com\")\n    uid, _ := s.resolveUser(ctx, \"google\", OIDCClaims{Subject: \"g-2\", Email: \"chi@x.com\", EmailVerified: false})\n    require.NotEqual(t, existing, uid) // KHÔNG merge — tài khoản mới\n}\n\nfunc TestResolve_ReturningUser_NoDuplicate(t *testing.T) {\n    s := newServiceWithFakeProvider(t)\n    u1, _ := s.resolveUser(ctx, \"google\", OIDCClaims{Subject: \"g-3\", Email: \"a@x.com\", EmailVerified: true})\n    u2, _ := s.resolveUser(ctx, \"google\", OIDCClaims{Subject: \"g-3\", Email: \"a@x.com\", EmailVerified: true})\n    require.Equal(t, u1, u2) // cùng subject → cùng user\n}\n\nfunc TestResolve_MultiProvider_OneUser(t *testing.T) {\n    s := newServiceWithFakeProvider(t)\n    uid, _ := s.resolveUser(ctx, \"google\", OIDCClaims{Subject: \"g-4\", Email: \"b@x.com\", EmailVerified: true})\n    s.resolveUser(ctx, \"facebook\", OIDCClaims{Subject: \"f-4\", Email: \"b@x.com\", EmailVerified: true})\n    links := s.repo.SocialLinks(t, uid)\n    require.Len(t, links, 2)\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Adapter provider theo một interface chung (<code>AuthCodeURL</code>, <code>ExchangeAndVerify</code>) - Google triển khai trước, Facebook/Zalo stub cùng interface để mở rộng không sửa lõi. <code>state</code>/<code>code_verifier</code> lưu Redis TTL ngắn, dùng một lần. id_token Google verify qua JWKS của Google. Merge chỉ khi <code>email_verified</code>. Mọi đường kết thúc ở <code>IssueTokenPair</code> (TASK-AUTH-002). Client secret qua TASK-INFRA-003. Là task SHOULD: nếu cắt slice, không chặn release, nhưng nếu làm thì theo đúng chuẩn ở §1.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li>TASK-AUTH-002 - phát hành <code>TokenPair</code> sau xác thực social (đường token thống nhất).</li><li>TASK-AUTH-001 - tạo <code>app_user</code> mới cho tài khoản chỉ-social (pwd_hash NULL hợp lệ).</li><li>TASK-INFRA-003 - OAuth client secret/cấu hình provider qua provider secret.</li><li>TASK-WEB-001 (downstream) - UI nút \"Đăng nhập với Google/Facebook/Zalo\".</li><li>Thư viện: OAuth2/OIDC client + PKCE; hạ tầng Redis cho state tạm.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Bắt đầu flow</h3>\n<pre><code class=\"language-http\">GET /v1/auth/oauth/google/start HTTP/1.1\n-&gt; 302 Location: https://accounts.google.com/o/oauth2/v2/auth?client_id=...&amp;code_challenge=...&amp;state=...&amp;nonce=...&amp;response_type=code</code></pre>\n<h3>Callback -&gt; token (giống đăng nhập mật khẩu)</h3>\n<pre><code class=\"language-json\">{\n  \"access_token\": \"eyJhbGciOiJSUzI1NiI...\",\n  \"refresh_token\": \"rt_...\",\n  \"token_type\": \"Bearer\",\n  \"expires_in\": 900\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Liên kết/gỡ provider từ trang cài đặt (quản lý nhiều identity sau khi đăng nhập) - UI giai đoạn sau.</li><li>Đăng nhập Apple (nếu mở mobile iOS) - thêm adapter khi cần.</li><li>Hợp nhất tài khoản trùng (một người lỡ tạo hai tài khoản qua hai đường) - quy trình merge có xác minh sau.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Thiếu state</td><td>callback test</td><td>login CSRF</td><td>Bắt buộc state khớp (§1 #3)</td></tr><tr><td>Thiếu PKCE</td><td>review</td><td>chặn-code đổi token</td><td>Authorization Code + PKCE (§1 #2)</td></tr><tr><td>Không verify id_token</td><td>review</td><td>tin claims giả</td><td>Verify JWKS provider + aud/iss/nonce (§1 #4)</td></tr><tr><td>Auto-merge email chưa verify</td><td>resolve test</td><td>account takeover</td><td>Chỉ merge khi email_verified (§1 #6)</td></tr><tr><td>state/verifier tái dùng</td><td>take-once</td><td>lạm dụng callback lặp</td><td>TTL ngắn + một lần (§1 #11)</td></tr><tr><td>Client secret trong env</td><td>grep CI</td><td>rò rỉ</td><td>Qua TASK-INFRA-003 (§1 #9)</td></tr><tr><td>(provider,subject) trùng</td><td>UNIQUE</td><td>hai user một identity</td><td>UNIQUE(provider,subject) (§1 #5)</td></tr><tr><td>Token riêng cho social</td><td>thiết kế</td><td>nhân đôi logic phiên</td><td>Cùng TokenPair (§1 #8)</td></tr><tr><td>Email/subject lọt vào metric</td><td>review nhãn</td><td>rò rỉ</td><td>Nhãn chỉ provider/result (§1 #12)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Authorization Code + PKCE là chuẩn hiện hành; Implicit flow đã bị khuyến nghị bỏ vì lộ token qua URL.</li><li><code>state</code> (chống CSRF) và <code>nonce</code> (chống replay) nhỏ nhưng là khác biệt giữa OAuth đúng và OAuth hổng.</li><li>Merge chỉ khi <code>email_verified</code> cắt account takeover kinh điển (kẻ tấn công khai email nạn nhân ở provider chưa verify).</li><li>Một đường token thống nhất (cùng TokenPair TASK-AUTH-002) giữ phần còn lại của hệ thống chỉ thấy một loại phiên.</li><li>Là task SHOULD: giảm ma sát đăng ký cho persona VN (Zalo phổ biến) nhưng không chặn release; nếu ship thì ship đúng chuẩn.</li><li>Adapter interface chung cho Google/Facebook/Zalo để thêm provider không sửa lõi.</li></ul>\n<hr>\n<p><em>Hết TASK-AUTH-004. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
