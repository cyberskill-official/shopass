window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-TRACK-003"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service TRACK <strong>MUST</strong> cung cấp schema và API CRUD cho <code>alert_rule</code> (luật cảnh báo của user trên một SKU) và bảng <code>alert</code> (sổ ghi mỗi lần luật bắn), với validate <code>threshold</code>/<code>channel</code> theo từng <code>rule_type</code> và kiểm chủ sở hữu mọi thao tác. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> định nghĩa <code>alert_rule (id BIGSERIAL PK, user_id BIGINT REFERENCES app_user(id), product_id BIGINT REFERENCES tracked_product(id), rule_type TEXT, threshold BIGINT, channel TEXT[] NOT NULL, active BOOLEAN DEFAULT true, created_at TIMESTAMPTZ DEFAULT now())</code>.</li><li><strong>MUST</strong> ràng buộc <code>rule_type</code> - <code>{price_below, drop_pct, real_sale, bottom_predicted}</code> qua CHECK (DEC-TRACK-20). <code>rule_type</code> ngoài enum bị DB từ chối.</li><li><strong>MUST</strong> lưu <code>channel</code> là <code>TEXT[]</code> với mỗi phần tử - <code>{push, email, sms}</code> (DEC-TRACK-21); validate từng phần tử ở tầng API trước khi ghi (mảng rỗng hoặc kênh lạ trả <code>400</code>). <code>push</code> là kênh mặc định nếu client không gửi.</li><li><strong>MUST</strong> lưu <code>threshold</code> dạng <code>BIGINT</code> và diễn giải theo <code>rule_type</code> (DEC-TRACK-22):</li></ol>\n<ul><li><code>price_below</code>: <code>threshold</code> là giá VND (int64), <strong>MUST</strong> &gt; 0; cảnh báo khi giá hiện tại &lt;= threshold.</li><li><code>drop_pct</code>: <code>threshold</code> là phần trăm nguyên, <strong>MUST</strong> trong <code>[1, 99]</code>; cảnh báo khi giá giảm &gt;= threshold% so với mốc tham chiếu.</li><li><code>real_sale</code> và <code>bottom_predicted</code>: <code>threshold</code> <strong>MUST</strong> là NULL (tín hiệu từ engine DEAL, không có ngưỡng người dùng đặt).</li></ul>\n<ol><li><strong>MUST</strong> validate quan hệ <code>rule_type</code> &lt;-&gt; <code>threshold</code> ở tầng API (DEC-TRACK-22): <code>price_below</code>/<code>drop_pct</code> thiếu <code>threshold</code> hợp lệ trả <code>400</code>; <code>real_sale</code>/<code>bottom_predicted</code> kèm <code>threshold</code> khác NULL trả <code>400</code>.</li><li><strong>MUST</strong> định nghĩa bảng <code>alert (id BIGSERIAL PK, alert_rule_id BIGINT REFERENCES alert_rule(id) ON DELETE CASCADE, fired_at TIMESTAMPTZ NOT NULL, payload JSONB, status TEXT NOT NULL DEFAULT 'pending')</code> (DEC-TRACK-23). task này chỉ định nghĩa schema và đọc lịch sử; TASK-TRACK-004 ghi dòng <code>alert</code>.</li><li><strong>MUST</strong> phục vụ <code>POST /v1/alerts {product_id, rule_type, threshold?, channel[]?}</code> tạo luật gắn <code>user_id</code> từ JWT; <code>product_id</code> phải có trong <code>tracked_product</code> (FK), nếu không trả <code>400</code>. Trả <code>201</code> + luật đã tạo.</li><li><strong>MUST</strong> phục vụ <code>GET /v1/alerts</code> trả mọi luật của caller; <code>PATCH /v1/alerts/{id} {active}</code> bật/tắt; <code>DELETE /v1/alerts/{id}</code> xóa luật (và <code>alert</code> con qua CASCADE).</li><li><strong>MUST</strong> kiểm chủ sở hữu mọi route theo <code>{id}</code> (DEC-TRACK-24): <code>alert_rule.user_id != caller</code> trả <code>404</code> (không <code>403</code>, không lộ tồn tại). KHÔNG route nào bỏ qua.</li><li><strong>MUST</strong> phục vụ <code>GET /v1/alerts/{id}/history</code> trả các dòng <code>alert</code> của luật đó (sắp <code>fired_at</code> giảm dần), chỉ khi caller sở hữu luật.</li><li><strong>MUST</strong> tạo index hỗ trợ engine TASK-TRACK-004 truy vấn luật cần đánh giá: <code>idx_ar_eval ON alert_rule (product_id, rule_type) WHERE active = true</code> (chỉ index luật đang bật, partial index nhẹ).</li><li><strong>SHOULD</strong> phát OTel <code>alert_rule_ops_total{op, rule_type, status}</code> và <code>alert_rule_active_total{rule_type}</code> (gauge số luật đang bật theo loại) để theo dõi phân bố luật.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao rule_type là enum CHECK (DEC-TRACK-20)?</strong> Bốn loại cảnh báo phủ đúng nhu cầu: \"về giá X\" (price_below), \"giảm Y%\" (drop_pct), \"sale thật chứ không ảo\" (real_sale - dùng TASK-DEAL-001), \"sắp chạm đáy\" (bottom_predicted - dùng TASK-DEAL-006). Engine đánh giá (TASK-TRACK-004) viết một nhánh xử lý cho mỗi loại. Cho <code>rule_type</code> tùy ý nghĩa là engine gặp loại nó không có nhánh -&gt; hoặc bỏ sót cảnh báo, hoặc panic. CHECK ở DB là rào cuối cùng đảm bảo chỉ có bốn loại đã biết.</p>\n<p><strong>Vì sao ngữ nghĩa threshold đổi theo rule_type (DEC-TRACK-22)?</strong> Một cột <code>threshold</code> phục vụ nhiều loại luật, nhưng đơn vị khác nhau: VND tuyệt đối với price_below, phần trăm với drop_pct, không dùng với real_sale/bottom_predicted (tín hiệu đến từ engine DEAL). Nếu không validate quan hệ này, một luật <code>drop_pct</code> với threshold = 89000 nghĩa là \"giảm 89000 phần trăm\" - vô nghĩa, không bao giờ bắn. Validate theo loại ở tầng API bắt lỗi ngay lúc tạo thay vì để luật chết âm thầm.</p>\n<p><strong>Vì sao channel là TEXT[] nhiều kênh (DEC-TRACK-21)?</strong> Người dùng muốn \"push cho tôi, và email nếu là deal lớn\". Một luật chọn được nhiều kênh là linh hoạt tự nhiên. Validate từng phần tử thuộc {push, email, sms} chặn kênh không có dispatcher. Mặc định <code>push</code> (kênh rẻ nhất, §3.6) khi client không nói rõ, khớp mô hình chi phí push &gt; email &gt; sms.</p>\n<p><strong>Vì sao tách bảng alert khỏi alert_rule (DEC-TRACK-23)?</strong> <code>alert_rule</code> là cấu hình (ý định, sống lâu); <code>alert</code> là sự kiện (một lần bắn, bất biến). Trộn lại làm rối. Tách ra cho ta sổ lịch sử \"luật này đã bắn khi nào, gửi gì\" để hiển thị cho user và để TASK-TRACK-004 dedup (không bắn lại cùng cảnh báo). task này dựng schema + đọc; engine TASK-TRACK-004 ghi.</p>\n<p><strong>Vì sao partial index chỉ trên active=true (§1 #11)?</strong> Engine TASK-TRACK-004 quét \"mọi luật đang bật của một SKU vừa đổi giá\". Luật <code>active=false</code> không bao giờ được đánh giá, nên không cần nằm trong index. Partial index <code>WHERE active = true</code> nhỏ hơn và nhanh hơn full index, đúng hình dạng truy vấn của engine.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/track/migrations/0003_alert_rule.sql\nCREATE TABLE alert_rule (\n  id         BIGSERIAL   PRIMARY KEY,\n  user_id    BIGINT      NOT NULL REFERENCES app_user(id),\n  product_id BIGINT      NOT NULL REFERENCES tracked_product(id),\n  rule_type  TEXT        NOT NULL\n               CHECK (rule_type IN ('price_below','drop_pct','real_sale','bottom_predicted')),\n  threshold  BIGINT,                                  -- VND (price_below) hoặc % (drop_pct); NULL cho real_sale/bottom_predicted\n  channel    TEXT[]      NOT NULL DEFAULT ARRAY['push'],\n  active      BOOLEAN     NOT NULL DEFAULT true,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now()\n);\n\n-- Engine TASK-TRACK-004 chỉ quét luật đang bật của một SKU -&gt; partial index\nCREATE INDEX idx_ar_eval ON alert_rule (product_id, rule_type) WHERE active = true;\nCREATE INDEX idx_ar_user ON alert_rule (user_id);\n\nCREATE TABLE alert (\n  id            BIGSERIAL   PRIMARY KEY,\n  alert_rule_id BIGINT      NOT NULL REFERENCES alert_rule(id) ON DELETE CASCADE,\n  fired_at      TIMESTAMPTZ NOT NULL,\n  payload       JSONB,\n  status        TEXT        NOT NULL DEFAULT 'pending' -- pending|sent|failed (TASK-TRACK-004/NOTIF cập nhật)\n);\nCREATE INDEX idx_alert_rule_time ON alert (alert_rule_id, fired_at DESC);</code></pre>\n<h3>Validate threshold theo rule_type (Go)</h3>\n<pre><code class=\"language-go\">// services/track/internal/track/alert_validate.go\n\nvar validChannels = map[string]bool{\"push\": true, \"email\": true, \"sms\": true}\n\n// ValidateRule kiểm quan hệ rule_type &lt;-&gt; threshold &lt;-&gt; channel (DEC-TRACK-21/22).\n// Trả lỗi mô tả để handler map 400.\nfunc ValidateRule(ruleType string, threshold *int64, channel []string) error {\n    if len(channel) == 0 {\n        return errors.New(\"channel rỗng\")\n    }\n    for _, c := range channel {\n        if !validChannels[c] {\n            return fmt.Errorf(\"channel không hợp lệ: %s\", c)\n        }\n    }\n    switch ruleType {\n    case \"price_below\":\n        if threshold == nil || *threshold &lt;= 0 {\n            return errors.New(\"price_below cần threshold (VND) &gt; 0\")\n        }\n    case \"drop_pct\":\n        if threshold == nil || *threshold &lt; 1 || *threshold &gt; 99 {\n            return errors.New(\"drop_pct cần threshold trong [1,99]\")\n        }\n    case \"real_sale\", \"bottom_predicted\":\n        if threshold != nil {\n            return fmt.Errorf(\"%s không nhận threshold\", ruleType)\n        }\n    default:\n        return fmt.Errorf(\"rule_type không hợp lệ: %s\", ruleType)\n    }\n    return nil\n}</code></pre>\n<h3>Handler (Go) - tạo luật</h3>\n<pre><code class=\"language-go\">// services/track/internal/api/alert_rule.go\nfunc (h *Handler) HandleCreateRule(w http.ResponseWriter, req *http.Request) {\n    userID := auth.UserID(req.Context())\n    var body struct {\n        ProductID int64    `json:\"product_id\"`\n        RuleType  string   `json:\"rule_type\"`\n        Threshold *int64   `json:\"threshold\"`\n        Channel   []string `json:\"channel\"`\n    }\n    if err := json.NewDecoder(req.Body).Decode(&amp;body); err != nil {\n        writeErr(w, http.StatusBadRequest, \"invalid body\")\n        return\n    }\n    if len(body.Channel) == 0 {\n        body.Channel = []string{\"push\"} // mặc định kênh rẻ nhất\n    }\n    if err := track.ValidateRule(body.RuleType, body.Threshold, body.Channel); err != nil {\n        writeErr(w, http.StatusBadRequest, err.Error())\n        return\n    }\n    rule, err := h.repo.CreateRule(req.Context(), userID, body.ProductID,\n        body.RuleType, body.Threshold, body.Channel)\n    if err != nil {\n        if isFKViolation(err) { // product_id chưa track\n            writeErr(w, http.StatusBadRequest, \"product not tracked\")\n            return\n        }\n        writeErr(w, http.StatusInternalServerError, \"internal error\")\n        return\n    }\n    w.Header().Set(\"Content-Type\", \"application/json; charset=utf-8\")\n    w.WriteHeader(http.StatusCreated)\n    _ = json.NewEncoder(w).Encode(rule)\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>POST /v1/alerts {product_id, rule_type:\"price_below\", threshold:89000, channel:[\"push\",\"email\"]}</code> trả <code>201</code>; luật lưu <code>threshold=89000</code> (int64), <code>channel={push,email}</code>.</li><li><code>rule_type</code> ngoài enum (vd <code>\"price_above\"</code>) trả <code>400</code> (validate API) và bị CHECK chặn nếu chạm DB.</li><li><code>channel</code> chứa phần tử lạ (vd <code>[\"telegram\"]</code>) trả <code>400</code>; <code>channel</code> rỗng -&gt; mặc định <code>[\"push\"]</code>.</li><li><code>price_below</code> thiếu <code>threshold</code> hoặc <code>threshold &lt;= 0</code> trả <code>400</code>.</li><li><code>drop_pct</code> với <code>threshold</code> ngoài <code>[1,99]</code> (vd <code>500</code>) trả <code>400</code>.</li><li><code>real_sale</code>/<code>bottom_predicted</code> kèm <code>threshold</code> khác NULL trả <code>400</code>; không kèm <code>threshold</code> -&gt; <code>201</code>.</li><li><code>product_id</code> chưa có trong <code>tracked_product</code> -&gt; <code>400</code> (FK).</li><li><code>GET /v1/alerts</code> chỉ trả luật của caller; luật user khác không xuất hiện.</li><li>Route <code>{id}</code> với luật user khác -&gt; <code>404</code> (không <code>403</code>).</li><li><code>PATCH /v1/alerts/{id} {active:false}</code> tắt luật; engine TASK-TRACK-004 sau đó không đánh giá luật này (không còn trong partial index).</li><li><code>DELETE /v1/alerts/{id}</code> xóa luật và mọi dòng <code>alert</code> con (CASCADE); <code>GET .../history</code> sau đó trả rỗng/404.</li><li>Index <code>idx_ar_eval</code> tồn tại dạng partial (<code>WHERE active = true</code>); metric <code>alert_rule_ops_total</code> tăng theo thao tác.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/track/internal/track/alert_validate_test.go\nfunc TestValidate_PriceBelow(t *testing.T) {\n    require.NoError(t, ValidateRule(\"price_below\", ptr(int64(89_000)), []string{\"push\"}))\n    require.Error(t, ValidateRule(\"price_below\", nil, []string{\"push\"}))          // thiếu threshold\n    require.Error(t, ValidateRule(\"price_below\", ptr(int64(0)), []string{\"push\"})) // &lt;= 0\n}\n\nfunc TestValidate_DropPct(t *testing.T) {\n    require.NoError(t, ValidateRule(\"drop_pct\", ptr(int64(20)), []string{\"push\"}))\n    require.Error(t, ValidateRule(\"drop_pct\", ptr(int64(500)), []string{\"push\"})) // ngoài [1,99]\n}\n\nfunc TestValidate_SignalRules_NoThreshold(t *testing.T) {\n    require.NoError(t, ValidateRule(\"real_sale\", nil, []string{\"push\"}))\n    require.Error(t, ValidateRule(\"real_sale\", ptr(int64(10)), []string{\"push\"})) // không nhận threshold\n    require.NoError(t, ValidateRule(\"bottom_predicted\", nil, []string{\"email\"}))\n}\n\nfunc TestValidate_Channel(t *testing.T) {\n    require.Error(t, ValidateRule(\"real_sale\", nil, []string{\"telegram\"})) // kênh lạ\n    require.Error(t, ValidateRule(\"real_sale\", nil, []string{}))           // rỗng\n}\n\n// services/track/internal/api/alert_rule_test.go\nfunc TestCreate_UnknownProduct_400(t *testing.T) {\n    h := setupHandler(t)\n    rec := doPOSTAs(t, h, userA, \"/v1/alerts\",\n        `{\"product_id\":999999,\"rule_type\":\"real_sale\",\"channel\":[\"push\"]}`)\n    require.Equal(t, 400, rec.Code) // FK product not tracked\n}\n\nfunc TestCrossUser_404(t *testing.T) {\n    h := setupHandler(t)\n    rid := createRule(t, h, userB) // luật của userB\n    rec := doPATCHAs(t, h, userA, \"/v1/alerts/\"+itoa(rid), `{\"active\":false}`)\n    require.Equal(t, 404, rec.Code) // không 403 (DEC-TRACK-24)\n}\n\nfunc TestToggleActive_RemovesFromEvalIndex(t *testing.T) {\n    h := setupHandler(t)\n    rid := createRule(t, h, userA)\n    doPATCHAs(t, h, userA, \"/v1/alerts/\"+itoa(rid), `{\"active\":false}`)\n    require.False(t, ruleActive(t, h, rid)) // engine TASK-TRACK-004 sẽ bỏ qua\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration <code>0003_alert_rule.sql</code> (2 bảng + CHECK rule_type + partial index) -&gt; <code>alert_validate.go</code> (ValidateRule theo loại) -&gt; <code>alert_rule_repo.go</code> (5 hàm scope user, mỗi route {id} qua kiểm chủ sở hữu) -&gt; <code>alert_rule.go</code> (handler) -&gt; đăng ký route trong <code>router.go</code> sau JWT middleware (TASK-INFRA-001) -&gt; tests. Validate ở tầng API là tuyến đầu (thông báo lỗi rõ); CHECK ở DB là tuyến cuối (an toàn dữ liệu). Lưu ý alias: một số task DEAL gọi cờ này là <code>enabled</code>; schema chuẩn dùng <code>active</code> theo §3.4 tài liệu nguồn - engine TASK-TRACK-004 đọc đúng tên <code>active</code>.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-TRACK-001</strong> - SKU phải đã track (FK <code>product_id</code> tới <code>tracked_product</code>).</li><li><strong>TASK-INFRA-002</strong> - bảng <code>app_user</code> cho FK <code>user_id</code>.</li><li><strong>TASK-INFRA-001 (gateway)</strong> - gắn JWT auth và <code>user_id</code>.</li><li><strong>TASK-TRACK-004 (downstream)</strong> - engine đọc <code>alert_rule</code> (qua <code>idx_ar_eval</code>) và ghi dòng <code>alert</code>; tên cột <code>active</code>/<code>channel</code>/<code>threshold</code> là hợp đồng với nó.</li><li><strong>TASK-DEAL-006 (downstream)</strong> - batch đáy giá khớp <code>alert_rule</code> type <code>bottom_predicted</code>.</li><li><strong>TASK-WEB-004 (downstream)</strong> - UI quản lý alert tiêu thụ các route này.</li><li>Lib: <code>pgx</code> (hỗ trợ <code>TEXT[]</code>), <code>encoding/json</code>, <code>net/http</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Tạo luật price_below</h3>\n<pre><code>curl -s -X POST -H \"Authorization: Bearer $JWT\" -H \"Content-Type: application/json\" \\\n  -d '{\"product_id\":90112,\"rule_type\":\"price_below\",\"threshold\":89000,\"channel\":[\"push\",\"email\"]}' \\\n  \"https://api.sandeal.vn/v1/alerts\"</code></pre>\n<h3>Response (201)</h3>\n<pre><code class=\"language-json\">{\n  \"id\": 5012,\n  \"product_id\": 90112,\n  \"rule_type\": \"price_below\",\n  \"threshold\": 89000,\n  \"channel\": [\"push\", \"email\"],\n  \"active\": true\n}</code></pre>\n<h3>Lịch sử bắn (GET /v1/alerts/5012/history)</h3>\n<pre><code class=\"language-json\">[\n  { \"fired_at\": \"2026-06-27T12:00:05Z\", \"status\": \"sent\",\n    \"payload\": { \"price\": 79000, \"old_price\": 99000 } }\n]</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Mốc tham chiếu cho <code>drop_pct</code> (so với giá hôm qua, median 7 ngày, hay giá lúc tạo luật) - chốt ở TASK-TRACK-004 khi định nghĩa cách đánh giá; task này chỉ lưu phần trăm.</li><li>Lịch quiet-hours (không gửi cảnh báo ban đêm trừ khi user đồng ý) - gắn vào TASK-NOTIF khi có tùy chọn người dùng.</li><li>Giới hạn số luật theo tier (free vs Premium) - gắn vào TASK-BILL.</li><li>Luật trên <code>canonical_key</code> (cảnh báo khi bất kỳ sàn nào về giá X) - chờ TASK-PRICE-005; mở rộng sau.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>rule_type ngoài enum</td><td>validate API + CHECK</td><td>400 / từ chối ghi</td><td>Hai tầng chặn (DEC-TRACK-20)</td></tr><tr><td>channel lạ</td><td>ValidateRule</td><td>400</td><td>Chỉ push/email/sms có dispatcher</td></tr><tr><td>price_below threshold &lt;= 0</td><td>ValidateRule</td><td>400</td><td>Buộc VND &gt; 0</td></tr><tr><td>drop_pct ngoài [1,99]</td><td>ValidateRule</td><td>400</td><td>Phần trăm hợp lệ</td></tr><tr><td>real_sale kèm threshold</td><td>ValidateRule</td><td>400</td><td>Tín hiệu engine, không ngưỡng</td></tr><tr><td>product_id chưa track</td><td>FK 23503</td><td>400 product not tracked</td><td>Buộc qua TASK-TRACK-001</td></tr><tr><td>Truy cập luật user khác</td><td>kiểm chủ sở hữu</td><td>404</td><td>DEC-TRACK-24 (chống IDOR)</td></tr><tr><td>Xóa luật còn alert con</td><td>ON DELETE CASCADE</td><td>alert tự xóa</td><td>CASCADE ở FK</td></tr><tr><td>Engine đọc luật active=false</td><td>partial index</td><td>bỏ qua</td><td>WHERE active=true (§1 #11)</td></tr><tr><td>Nhầm tên cột active/enabled</td><td>code review</td><td>engine không khớp</td><td>Chuẩn hóa <code>active</code> theo §3.4</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li><code>alert_rule</code> là nơi người dùng nói \"báo tôi khi nào\"; bốn <code>rule_type</code> phủ giá tuyệt đối, phần trăm giảm, sale thật, và đáy dự đoán.</li><li>Ngữ nghĩa <code>threshold</code> đổi theo loại là điểm dễ sai nhất - validate quan hệ rule_type &lt;-&gt; threshold ở tầng API bắt luật chết âm thầm ngay lúc tạo.</li><li>Tách <code>alert</code> (sự kiện bất biến) khỏi <code>alert_rule</code> (cấu hình sống lâu) cho ta sổ lịch sử và nền dedup cho TASK-TRACK-004.</li><li>Partial index <code>WHERE active=true</code> khớp đúng truy vấn của engine (chỉ luật đang bật), nhẹ hơn full index.</li><li>Kiểm chủ sở hữu trả 404 (không 403) đóng lỗ IDOR trên khóa BIGSERIAL tuần tự, đồng nhất với TASK-TRACK-002.</li><li>Khi mở SEA, <code>threshold</code> của price_below vẫn int64 theo minor unit từng nước; enum rule_type và validate không đổi.</li></ul>\n<hr>\n<p><em>Hết TASK-TRACK-003. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
