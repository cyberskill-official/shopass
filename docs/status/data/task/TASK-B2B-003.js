window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-B2B-003"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service B2B <strong>MUST</strong> cho seller xem vị thế giá CỦA CHÍNH SELLER so với dải phân vị thị trường ẩn danh (<code>median_p/p25_p/p75_p</code>) theo category, đọc từ <code>market_trend_daily</code>, sau khi xác minh quyền sở hữu shop, và <strong>MUST NOT</strong> bao giờ lộ giá của một đối thủ/shop/SKU cụ thể. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> chỉ trả cho seller: (a) giá SKU của chính seller, (b) dải phân vị thị trường tổng hợp của category (<code>median_p/p25_p/p75_p</code> từ <code>market_trend_daily</code>), (c) percentile_rank của giá seller trong dải đó. <strong>MUST NOT</strong> trả giá của bất kỳ SKU/shop nào khác (DEC-B2B-20).</li><li><strong>MUST</strong> định nghĩa bảng <code>seller_owned_sku (id, seller_org_id, shop_id, product_id, verified, linked_at)</code>; chỉ SKU có <code>verified=true</code> mới được dùng (DEC-B2B-21).</li><li><strong>MUST</strong> xác minh quyền sở hữu shop trước khi phục vụ analytics (DEC-B2B-24): seller chỉ xem được SKU thuộc <code>shop_id</code> mà tổ chức của họ đã được xác minh sở hữu. Yêu cầu xem SKU/shop chưa xác minh -&gt; <code>403</code>.</li><li><strong>MUST</strong> biểu diễn vị thế bằng <code>percentile_rank</code> của giá seller trong dải phân vị category (DEC-B2B-22), ví dụ \"giá của bạn ở khoảng p60 thị trường\". <strong>MUST NOT</strong> quy vị thế về một cá thể đối thủ.</li><li><strong>MUST</strong> chỉ render vị thế khi ô <code>market_trend_daily</code> nguồn của category đó <code>suppressed=false</code> (DEC-B2B-23). Nếu ô bị suppress -&gt; trả <code>422</code> với lý do <code>\"insufficient_market_data\"</code>; <strong>MUST NOT</strong> dựng vị thế trên ô suppress.</li><li><strong>MUST</strong> đọc dải phân vị thị trường DUY NHẤT từ <code>market_trend_daily</code> qua repo của TASK-B2B-001 (ô đã phát hành); giá của chính seller đọc từ <code>price_daily</code> của các <code>product_id</code> đã xác minh sở hữu.</li><li><strong>MUST</strong> expose endpoint <code>GET /v1/b2b/seller/position?shop_id=...&amp;category_id=...&amp;platform_id=...&amp;day=...</code> trả vị thế cho SKU seller trong category.</li><li><strong>MUST</strong> phân biệt mã trạng thái: <code>200</code> (có vị thế), <code>403</code> (chưa xác minh sở hữu shop/SKU), <code>422</code> (không đủ dữ liệu thị trường - ô suppress), <code>400</code> (tham số sai).</li><li><strong>MUST</strong> không log truy vấn theo cách lộ seller đang theo dõi ai (DEC-B2B-25): log/metric chỉ giữ ở mức tổng hợp (đếm theo tier/lý do), không ghi cặp (seller -&gt; đối thủ).</li><li><strong>SHOULD</strong> phát OTel metric: <code>seller_position_served_total</code> (counter), <code>seller_position_denied_total{reason}</code> (counter), <code>seller_position_build_ms</code> (histogram).</li><li><strong>MUST</strong> resolve <code>seller_org_id</code> từ phiên/claim của caller; <strong>MUST NOT</strong> tin <code>seller_org_id</code>/<code>shop_id</code> do client tự khai mà không đối chiếu <code>seller_owned_sku</code>.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p>Vì sao chỉ trả vị thế của chính seller, không giá đối thủ (DEC-B2B-20)? Đây là ranh giới sống còn. \"Theo dõi giá đối thủ\" (§6 mục 8) phải hiểu là \"biết giá của mình ở đâu so với mặt bằng\", KHÔNG phải \"xem giá cụ thể của shop X\". Cái sau biến SănDeal thành công cụ do thám giá, vi phạm PDPL và phá niềm tin. Vị thế phân vị tổng hợp cho seller đủ tín hiệu để định giá mà không lộ bất kỳ cá thể nào.</p>\n<p>Vì sao xác minh quyền sở hữu shop (DEC-B2B-24)? Nếu bất kỳ seller nào cũng truy vấn analytics cho bất kỳ shop_id nào thì họ dò được vị thế shop đối thủ - lách ngay nguyên tắc trên bằng cách \"đóng vai\" shop khác. Bắt buộc <code>verified=true</code> cho cặp (seller_org, shop) đảm bảo seller chỉ soi chính mình.</p>\n<p>Vì sao chỉ dùng dải phân vị từ market_trend_daily đã phát hành (DEC-B2B-23, §1 #6)? Dải median/p25/p75 đã qua cổng k-anonymity (TASK-B2B-001) nên tự nó an toàn - không quy về cá thể. Nếu category quá hẹp khiến ô bị suppress thì cũng nghĩa là dải phân vị không đủ \"đông\" để an toàn; render vị thế lúc đó có thể vô tình lộ. Nên khi ô suppress, trả 422 thay vì cố dựng.</p>\n<p>Vì sao không log cặp seller -&gt; đối thủ (DEC-B2B-25)? Bản thân hành vi \"seller A đang xem category mỹ phẩm tầm giá X\" là thông tin nhạy cảm về chiến lược của A. Giữ log ở mức tổng hợp tránh tạo ra một kho dữ liệu cạnh tranh thứ cấp mà chính SănDeal lại phải bảo vệ.</p>\n<p>Vì sao percentile_rank thay vì khoảng cách tới một SKU (DEC-B2B-22)? percentile_rank (\"bạn ở p60\") là phát biểu về vị trí trong phân phối, không tham chiếu sản phẩm nào. Nó cho seller hiểu \"đắt hơn 60% thị trường\" mà không cần và không thể chỉ ra \"đắt hơn shop nào\".</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/b2b/migrations/0003_seller_owned_sku.sql\nCREATE TABLE seller_owned_sku (\n  id           BIGSERIAL   PRIMARY KEY,\n  seller_org_id BIGINT     NOT NULL,        -- tham chiếu tổ chức B2B (b2b_subscription.id hoặc org riêng)\n  shop_id      TEXT        NOT NULL,\n  product_id   BIGINT      NOT NULL,        -- tracked_product.id\n  verified     BOOLEAN     NOT NULL DEFAULT false,\n  linked_at    TIMESTAMPTZ NOT NULL DEFAULT now(),\n  UNIQUE (seller_org_id, product_id)\n);\n\nCREATE INDEX idx_sos_verified ON seller_owned_sku (seller_org_id, shop_id)\n  WHERE verified = true;</code></pre>\n<h3>Vị thế (Go)</h3>\n<pre><code class=\"language-go\">// services/b2b/internal/seller/position.go\ntype Position struct {\n    CategoryID     int64    `json:\"category_id\"`\n    PlatformID     int16    `json:\"platform_id\"`\n    Day            string   `json:\"day\"`\n    SellerPrice    int64    `json:\"seller_price\"`      // giá của CHÍNH seller\n    MarketMedian   int64    `json:\"market_median_p\"`   // dải tổng hợp ẩn danh\n    MarketP25      int64    `json:\"market_p25_p\"`\n    MarketP75      int64    `json:\"market_p75_p\"`\n    PercentileRank float64  `json:\"percentile_rank\"`   // 0..100, vị thế của seller\n    // KHÔNG có trường nào chứa giá/khóa của đối thủ\n}\n\n// rank tính vị thế của giá seller trong dải phân vị category (xấp xỉ tuyến tính\n// p25-&gt;p75); chỉ dùng số tổng hợp, không tham chiếu SKU đối thủ.\nfunc rank(sellerPrice, p25, median, p75 int64) float64 {\n    switch {\n    case sellerPrice &lt;= p25:\n        return 25 * float64(sellerPrice) / float64(maxI(p25, 1))\n    case sellerPrice &lt;= median:\n        return 25 + 25*float64(sellerPrice-p25)/float64(maxI(median-p25, 1))\n    case sellerPrice &lt;= p75:\n        return 50 + 25*float64(sellerPrice-median)/float64(maxI(p75-median, 1))\n    default:\n        return 75 + min(25, 25*float64(sellerPrice-p75)/float64(maxI(p75, 1)))\n    }\n}</code></pre>\n<h3>Ownership gate (§1 #3)</h3>\n<pre><code class=\"language-go\">// services/b2b/internal/seller/ownership.go\n// assertOwned chặn truy vấn nếu seller chưa được xác minh sở hữu shop_id.\nfunc (o *Ownership) assertOwned(ctx context.Context, sellerOrgID int64, shopID string) error {\n    var n int\n    err := o.pool.QueryRow(ctx,\n        `SELECT count(*) FROM seller_owned_sku\n         WHERE seller_org_id=$1 AND shop_id=$2 AND verified=true`,\n        sellerOrgID, shopID).Scan(&amp;n)\n    if err != nil {\n        return err\n    }\n    if n == 0 {\n        return ErrNotVerifiedOwner{ShopID: shopID} // -&gt; 403\n    }\n    return nil\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch -&gt; <code>seller_owned_sku</code> tồn tại với UNIQUE <code>(seller_org_id, product_id)</code>.</li><li>Seller đã xác minh sở hữu shop -&gt; <code>200</code> + vị thế (giá seller + dải phân vị + percentile_rank).</li><li>Seller truy vấn <code>shop_id</code> chưa xác minh sở hữu -&gt; <code>403</code>.</li><li>Seller truy vấn <code>product_id</code> thuộc shop khác -&gt; <code>403</code>.</li><li>Category có ô <code>market_trend_daily</code> bị suppress -&gt; <code>422</code> <code>\"insufficient_market_data\"</code>; KHÔNG render vị thế.</li><li>Response KHÔNG chứa giá/khóa của bất kỳ SKU/shop nào khác (chỉ giá của chính seller + dải tổng hợp).</li><li><code>percentile_rank</code> nằm trong 0..100 và tăng đơn điệu theo giá seller (giá cao hơn -&gt; rank cao hơn) trong cùng dải.</li><li><code>seller_org_id</code> lấy từ phiên caller; tham số tự khai không khớp <code>seller_owned_sku</code> bị từ chối.</li><li>Log/metric KHÔNG ghi cặp (seller -&gt; category/đối thủ cụ thể); chỉ đếm tổng hợp theo lý do.</li><li>Dải phân vị đọc từ <code>market_trend_daily</code> (ô phát hành); giá seller đọc từ <code>price_daily</code> của SKU đã xác minh.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/b2b/internal/seller/ownership_test.go\nfunc TestOwnership_Verified_OK(t *testing.T) {\n    o := setupOwnership(t, seedOwned(1, \"shopA\", 100, true))\n    require.NoError(t, o.assertOwned(ctx, 1, \"shopA\"))\n}\n\nfunc TestOwnership_NotVerified_403(t *testing.T) {\n    o := setupOwnership(t, seedOwned(1, \"shopA\", 100, false)) // chưa verified\n    err := o.assertOwned(ctx, 1, \"shopA\")\n    require.ErrorAs(t, err, &amp;ErrNotVerifiedOwner{})\n}\n\nfunc TestOwnership_OtherShop_403(t *testing.T) {\n    o := setupOwnership(t, seedOwned(1, \"shopA\", 100, true))\n    err := o.assertOwned(ctx, 1, \"shopB\") // seller 1 không sở hữu shopB\n    require.ErrorAs(t, err, &amp;ErrNotVerifiedOwner{})\n}\n\n// services/b2b/internal/seller/position_test.go\nfunc TestPosition_SuppressedMarket_422(t *testing.T) {\n    h := setupHandler(t, withTrendCell(/* category 991 suppressed */), withOwned(1, \"shopA\", 100, true))\n    res := h.get(\"/v1/b2b/seller/position?shop_id=shopA&amp;category_id=991&amp;platform_id=1&amp;day=2026-06-20\")\n    require.Equal(t, 422, res.Code)\n}\n\nfunc TestPosition_NoCompetitorPrice(t *testing.T) {\n    body := servePosition(t /* dải median 300k, seller 320k */)\n    // chỉ có giá seller + dải tổng hợp\n    require.Contains(t, body, \"seller_price\")\n    require.Contains(t, body, \"market_median_p\")\n    require.NotContains(t, body, \"competitor\")\n    require.NotContains(t, body, \"shop_id\\\":\\\"shopB\") // không lộ shop khác\n}\n\nfunc TestRank_Monotonic(t *testing.T) {\n    r1 := rank(240_000, 250_000, 300_000, 410_000)\n    r2 := rank(320_000, 250_000, 300_000, 410_000)\n    r3 := rank(500_000, 250_000, 300_000, 410_000)\n    require.Less(t, r1, r2)\n    require.Less(t, r2, r3)\n    require.GreaterOrEqual(t, r1, 0.0)\n    require.LessOrEqual(t, r3, 100.0)\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration 0003 (seller_owned_sku) -&gt; ownership.go (cổng xác minh) -&gt; position.go (percentile_rank từ dải phân vị) -&gt; seller_handler.go -&gt; tests. Handler chạy sau gateway (TASK-INFRA-001). Xác minh quyền sở hữu shop (gắn <code>verified=true</code>) là quy trình ngoài phạm vi task này (vd seller chứng minh qua kênh chính thức của sàn) - task chỉ tiêu thụ cờ <code>verified</code>. Dải phân vị lấy qua <code>QueryCells</code> của TASK-B2B-001.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li>TASK-B2B-001 - <code>market_trend_daily</code> + <code>QueryCells</code> (ô đã phát hành) là nguồn dải phân vị thị trường.</li><li>TASK-PRICE-001 - <code>tracked_product</code> (product_id, shop_id) để liên kết SKU seller.</li><li>TASK-B2B-002 (cùng module) - chia sẻ khái niệm tổ chức/subscription B2B.</li><li>TASK-INFRA-001 (gateway) - xác thực + rate-limit.</li><li>Extension/lib: driver <code>pgx</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Vị thế (200)</h3>\n<pre><code class=\"language-json\">{\n  \"category_id\": 7,\n  \"platform_id\": 1,\n  \"day\": \"2026-06-20\",\n  \"seller_price\": 320000,\n  \"market_median_p\": 320000,\n  \"market_p25_p\": 250000,\n  \"market_p75_p\": 410000,\n  \"percentile_rank\": 50.0\n}</code></pre>\n<h3>Chưa xác minh sở hữu (403)</h3>\n<pre><code class=\"language-json\">{ \"error\": \"not_verified_owner\", \"shop_id\": \"shopB\", \"message\": \"Cần xác minh quyền sở hữu shop trước khi xem analytics.\" }</code></pre>\n<h3>Không đủ dữ liệu thị trường (422)</h3>\n<pre><code class=\"language-json\">{ \"error\": \"insufficient_market_data\", \"category_id\": 991, \"message\": \"Category này chưa đủ dữ liệu thị trường ẩn danh để hiển thị vị thế.\" }</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Vị thế theo chuỗi thời gian (xu hướng percentile_rank của seller qua nhiều ngày) - mở rộng từ một ngày sang khoảng ở slice sau.</li><li>Gợi ý định giá (vd \"giảm 5% để vào p40\") - thêm khi có nhu cầu, vẫn chỉ dựa dải tổng hợp.</li><li>Quy trình tự động xác minh quyền sở hữu shop qua OAuth của sàn - tích hợp khi sàn mở API seller.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Lộ giá đối thủ cụ thể</td><td>TestPosition_NoCompetitorPrice</td><td>do thám giá, vi phạm PDPL</td><td>Response chỉ giá seller + dải tổng hợp (DEC-B2B-20)</td></tr><tr><td>Seller dò shop chưa sở hữu</td><td>TestOwnership_NotVerified_403 + OtherShop</td><td>xem vị thế đối thủ</td><td>Cổng verified=true (DEC-B2B-24)</td></tr><tr><td>Render trên ô suppress</td><td>TestPosition_SuppressedMarket_422</td><td>rủi ro lộ qua dải thưa</td><td>422 khi ô suppress (DEC-B2B-23)</td></tr><tr><td>Tin shop_id client tự khai</td><td>review handler</td><td>giả mạo quyền sở hữu</td><td>Đối chiếu seller_owned_sku (§1 #11)</td></tr><tr><td>Log lộ cặp seller -&gt; đối thủ</td><td>review log/metric</td><td>kho dữ liệu cạnh tranh thứ cấp</td><td>Log mức tổng hợp (DEC-B2B-25)</td></tr><tr><td>percentile_rank phi lý (&gt;100/&lt;0)</td><td>TestRank_Monotonic</td><td>hiển thị sai</td><td>Kẹp 0..100 + đơn điệu</td></tr><tr><td>Đọc giá đối thủ từ price_daily trực tiếp</td><td>review position.go</td><td>lách k-anonymity</td><td>Dải lấy từ market_trend_daily, không từ raw đối thủ</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Ranh giới sống còn: \"theo dõi giá đối thủ\" = biết vị thế của mình trong dải tổng hợp, KHÔNG = xem giá shop cụ thể.</li><li>Hai cổng độc lập: xác minh quyền sở hữu shop (seller chỉ soi chính mình) + dải phân vị đã qua k-anonymity (tự an toàn).</li><li>Khi ô thị trường bị suppress, từ chối render (422) thay vì cố dựng - dải thưa là dải có rủi ro lộ.</li><li>percentile_rank là phát biểu về phân phối, không tham chiếu sản phẩm nào - cho tín hiệu định giá mà không lộ cá thể.</li><li>Không biến chính SănDeal thành kho dữ liệu cạnh tranh: không log cặp seller -&gt; đối thủ.</li></ul>\n<hr>\n<p><em>Hết TASK-B2B-003. Status: ready_to_review (awaiting HITL) (mục tiêu audit 10/10).</em></p>";
