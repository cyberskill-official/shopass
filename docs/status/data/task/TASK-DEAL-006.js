window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-DEAL-006"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service DEAL <strong>MUST</strong> chạy một batch hằng đêm chấm điểm mọi SKU đủ điều kiện bằng tín hiệu đáy giá chung <code>p_bottom_14d</code> (đọc từ bảng <code>price_forecast</code> do Python sinh), và bắn cảnh báo khi <code>p_bottom_14d &gt; 0.7</code>, khớp vào <code>alert_rule</code> type <code>'bottom_predicted'</code> (TASK-TRACK-003) rồi enqueue vào notification fan-out (TASK-NOTIF-003). Batch là model-agnostic và idempotent. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> đăng ký một cron job chạy hằng đêm vào khung off-peak <code>02:00 Asia/Ho_Chi_Minh</code> (DEC-DEAL-51); job dùng khóa (advisory lock) để không chạy chồng nếu lần trước chưa xong.</li><li><strong>MUST</strong> chỉ chấm điểm SKU đã đạt <code>MATURE</code> qua maturity gate <code>IsFeatureReady</code> của TASK-DEAL-002 (DEC-DEAL-54): query <code>price_forecast</code> JOIN điều kiện trưởng thành, loại SKU <code>NEW</code>/<code>WARMING</code> khỏi vòng chấm điểm.</li><li><strong>MUST</strong> đọc cột <code>p_bottom_14d</code> chung của <code>price_forecast</code> (DEC-DEAL-52): batch KHÔNG biết và KHÔNG quan tâm dòng đó do Prophet (TASK-DEAL-004) hay LightGBM (TASK-DEAL-005) sinh; hai model hoán đổi được qua cùng một hợp đồng bảng.</li><li><strong>MUST</strong> chỉ lấy bản dự báo còn tươi: dòng <code>price_forecast</code> có <code>scored_at &gt;= now() - INTERVAL '36 hours'</code> (bao một nhịp batch dự báo lỡ); dự báo cũ hơn coi là stale và bị loại khỏi chấm điểm đêm nay.</li><li><strong>MUST</strong> áp ngưỡng <code>p_bottom_14d &gt; 0.7</code> đúng nguyên văn §3.5 (DEC-DEAL-50): so sánh strict greater-than, <code>0.7</code> là hằng load-bearing; <code>p_bottom_14d == 0.7</code> KHÔNG bắn cảnh báo.</li><li><strong>MUST</strong> với mỗi SKU vượt ngưỡng, join sang <code>alert_rule</code> type <code>'bottom_predicted'</code> đang bật (<code>active = true</code>) để lấy danh sách user đang theo dõi SKU đó muốn nhận cảnh báo đáy (TASK-TRACK-003); SKU không có luật khớp thì bỏ qua, không enqueue gì.</li><li><strong>MUST</strong> idempotent ở mức (user, product, ngày): tối đa một cảnh báo đáy cho mỗi cặp <code>(user_id, product_id)</code> trong một ngày lịch (<code>fired_on</code>), được bảo đảm bởi <code>UNIQUE (user_id, product_id, fired_on)</code> trên <code>bottom_alert_log</code> (DEC-DEAL-53). Retry batch trong cùng ngày KHÔNG sinh cảnh báo trùng.</li><li><strong>MUST</strong> chống spam liên ngày bằng cooldown rising-edge (DEC-DEAL-53): KHÔNG bắn lại cảnh báo cho cùng <code>(user_id, product_id)</code> nếu đã có alert trong vòng <code>cooldown</code> ngày gần nhất (mặc định 7 ngày) mà <code>p_bottom_14d</code> vẫn liên tục <code>&gt; 0.7</code>. Chỉ bắn trên cạnh lên (P vượt ngưỡng sau khi đã rơi xuống dưới, hoặc đã hết cooldown), không bắn đêm-này-qua-đêm-khác khi P dính ngưỡng cao.</li><li><strong>MUST</strong> enqueue mỗi cảnh báo đã qua dedupe vào notification fan-out (TASK-NOTIF-003) với payload gồm <code>user_id</code>, <code>product_id</code>, <code>p_bottom_14d</code>, và lý do <code>reason = \"bottom_predicted\"</code>; fan-out lo chuyện chọn kênh và rate-limit phía người dùng.</li><li><strong>MUST</strong> ghi mọi cảnh báo đã bắn vào <code>bottom_alert_log (user_id, product_id, fired_on, p_bottom)</code> ngay khi enqueue thành công, để dedupe ngày và cooldown đọc lại ở đêm sau.</li><li><strong>MUST</strong> xử lý lỗi enqueue cục bộ: nếu fan-out từ chối một mục, KHÔNG ghi <code>bottom_alert_log</code> cho mục đó (để đêm sau thử lại), ghi log lỗi và tiếp tục các mục còn lại - một SKU lỗi không làm hỏng cả batch.</li><li><strong>SHOULD</strong> phát OTel metric: <code>deal_nightly_scored_total</code> (counter số SKU đã chấm), <code>deal_bottom_alert_fired_total</code> (counter cảnh báo đã enqueue), <code>deal_alert_dedupe_skipped_total{reason}</code> (counter bỏ qua vì trùng-ngày hoặc cooldown), <code>deal_nightly_batch_duration_ms</code> (histogram).</li><li><strong>MUST</strong> chạy toàn batch trong một transaction-per-user-batch hợp lý (đọc forecast và alert_rule nhất quán trong một ảnh chụp), nhưng commit <code>bottom_alert_log</code> theo từng mục đã enqueue để lỗi giữa chừng không mất dấu các cảnh báo đã gửi.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao batch hằng đêm off-peak chứ không realtime (DEC-DEAL-51)?</strong> Dự báo đáy giá đổi chậm: <code>p_bottom_14d</code> là xác suất chạm đáy trong cửa sổ 14 ngày, không nhảy theo từng phút. Chấm lại mỗi đêm một lần là đủ độ tươi mà rẻ hơn nhiều so với streaming. Chạy lúc 02:00 giờ Việt Nam né giờ cao điểm đọc giá ban ngày, để tải nặng (quét toàn bộ SKU MATURE) không tranh tài nguyên với truy vấn người dùng.</p>\n<p><strong>Vì sao ngưỡng đúng 0,7, so sánh strict (DEC-DEAL-50, §3.5)?</strong> §3.5 chốt nguyên văn \"alert nếu P(bottom within 14d) &gt; 0.7\". Đây là điểm đánh đổi precision-recall: 0,7 đủ cao để chỉ bắn khi mô hình khá chắc, ưu tiên precision hơn recall. Một cảnh báo \"sắp chạm đáy\" mà sai thì người dùng mua hớ và mất tin (§5.4); thà bỏ lỡ vài đáy còn hơn kêu sói nhầm. Strict greater-than giữ đúng ranh giới: tại đúng 0,7 mô hình mới ở mức cân bằng, chưa đủ chắc để làm phiền người dùng.</p>\n<p><strong>Vì sao model-agnostic qua bảng chung (DEC-DEAL-52)?</strong> Prophet (TASK-DEAL-004) và LightGBM (TASK-DEAL-005) là hai cách tính ra cùng một đại lượng: xác suất chạm đáy 14 ngày. Nếu batch serving gắn cứng vào một model, đổi model là phải sửa batch. Đọc cột <code>p_bottom_14d</code> chung của <code>price_forecast</code> tách hẳn tầng serving (Go) khỏi tầng sinh dự báo (Python): nâng cấp từ Prophet sang LightGBM, hay chạy A/B hai model, không đụng gì tới code cảnh báo. Batch chỉ tin một hợp đồng: bảng có cột xác suất tươi cho SKU MATURE.</p>\n<p><strong>Vì sao idempotent ngày + cooldown rising-edge (DEC-DEAL-53)?</strong> Một cảnh báo lặp lại mỗi đêm trong khi <code>p_bottom_14d</code> cứ dính trên 0,7 là cách nhanh nhất huấn luyện người dùng phớt lờ thông báo của SănDeal. Khi đã phớt lờ, họ tắt push, và ta mất kênh tới người dùng vĩnh viễn. <code>UNIQUE (user_id, product_id, fired_on)</code> chặn trùng trong một ngày (kể cả khi batch retry). Cooldown rising-edge chặn trùng liên ngày: một xác suất đáy cao kéo dài một tuần chỉ đáng một cảnh báo, không phải bảy. Người dùng nhận đúng một tín hiệu \"giờ là lúc mua\" thay vì bảy tiếng ồn.</p>\n<p><strong>Vì sao chỉ chấm SKU MATURE (DEC-DEAL-54)?</strong> Dự báo đáy trên SKU chưa đủ 90 ngày lịch sử là dự báo trên cát: cửa sổ quá ngắn để mô hình mùa vụ có nghĩa. TASK-DEAL-002 đã chốt cổng <code>IsFeatureReady</code> cho mọi tính năng công khai; cảnh báo đáy đi qua cùng cổng đó để không bao giờ bắn dựa trên dự báo từ dữ liệu mỏng. Giữ nhất quán một định nghĩa \"đủ chín\" toàn module.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/deal/migrations/0002_bottom_alert_log.sql\n-- Nhật ký cảnh báo đáy đã bắn. Dùng cho dedupe ngày (UNIQUE) và cooldown liên ngày.\nCREATE TABLE bottom_alert_log (\n  user_id    BIGINT       NOT NULL,\n  product_id BIGINT       NOT NULL REFERENCES tracked_product(id),\n  fired_on   DATE         NOT NULL,                 -- ngày lịch theo Asia/Ho_Chi_Minh\n  p_bottom   DOUBLE PRECISION NOT NULL CHECK (p_bottom &gt; 0.7),\n  fired_at   TIMESTAMPTZ  NOT NULL DEFAULT now(),\n  UNIQUE (user_id, product_id, fired_on)            -- idempotent 1 alert/cặp/ngày (§1 #7)\n);\n\n-- Tra cooldown liên ngày: alert gần nhất của 1 cặp (user, product).\nCREATE INDEX idx_bal_cooldown ON bottom_alert_log (user_id, product_id, fired_on DESC);</code></pre>\n<h3>Vòng batch (Go)</h3>\n<pre><code class=\"language-go\">// services/deal/internal/batch/nightly_score.go\npackage batch\n\n// RunNightlyScore chấm điểm mọi SKU MATURE, bắn cảnh báo đáy khi p_bottom_14d &gt; 0.7.\n// Model-agnostic: chỉ đọc price_forecast.p_bottom_14d (Prophet hoặc LightGBM).\nfunc (b *Batch) RunNightlyScore(ctx context.Context, today time.Time) error {\n    // §1 #2 #3 #4 #5: chỉ SKU MATURE, dự báo còn tươi, p_bottom_14d &gt; 0.7 (strict).\n    rows, err := b.pool.Query(ctx, `\n        SELECT f.product_id, f.p_bottom_14d\n        FROM price_forecast f\n        JOIN tracked_product tp ON tp.id = f.product_id\n        WHERE f.p_bottom_14d &gt; 0.7                              -- DEC-DEAL-50, strict\n          AND f.scored_at  &gt;= now() - INTERVAL '36 hours'       -- §1 #4, dự báo tươi\n          AND now() - tp.first_seen &gt;= INTERVAL '90 days'`)     // §1 #2, maturity gate\n    if err != nil {\n        return err\n    }\n    defer rows.Close()\n\n    var scored, fired, skipped int\n    for rows.Next() {\n        var productID int64\n        var pBottom float64\n        if err := rows.Scan(&amp;productID, &amp;pBottom); err != nil {\n            return err\n        }\n        scored++\n        // §1 #6: gom user từ alert_rule type 'bottom_predicted' đang bật.\n        users, err := b.matchBottomRules(ctx, productID)\n        if err != nil {\n            b.log.Warn(\"match rules failed\", \"product\", productID, \"err\", err)\n            continue // §1 #11: một SKU lỗi không làm hỏng batch\n        }\n        for _, userID := range users {\n            if b.shouldSkip(ctx, userID, productID, today) { // §1 #7 #8 dedupe + cooldown\n                skipped++\n                continue\n            }\n            if err := b.enqueueAndLog(ctx, userID, productID, pBottom, today); err != nil {\n                b.log.Warn(\"enqueue failed\", \"user\", userID, \"product\", productID, \"err\", err)\n                continue // §1 #11: không ghi log -&gt; đêm sau thử lại\n            }\n            fired++\n        }\n    }\n    metrics.NightlyScored(scored)\n    metrics.BottomAlertFired(fired)\n    metrics.DedupeSkipped(skipped)\n    return rows.Err()\n}</code></pre>\n<h3>Khớp luật + dedupe + enqueue (Go)</h3>\n<pre><code class=\"language-go\">// services/deal/internal/batch/alert_match.go\n// matchBottomRules trả các user_id có alert_rule type 'bottom_predicted' đang bật cho SKU.\nfunc (b *Batch) matchBottomRules(ctx context.Context, productID int64) ([]int64, error) {\n    rows, err := b.pool.Query(ctx, `\n        SELECT user_id FROM alert_rule\n        WHERE product_id = $1 AND rule_type = 'bottom_predicted' AND active = true`,\n        productID)\n    if err != nil {\n        return nil, err\n    }\n    defer rows.Close()\n    var users []int64\n    for rows.Next() {\n        var u int64\n        if err := rows.Scan(&amp;u); err != nil {\n            return nil, err\n        }\n        users = append(users, u)\n    }\n    return users, rows.Err()\n}</code></pre>\n<pre><code class=\"language-go\">// services/deal/internal/batch/dedupe.go\nconst cooldownDays = 7 // cooldown rising-edge liên ngày (DEC-DEAL-53)\n\n// shouldSkip: true nếu (user, product) đã có alert hôm nay (dedupe ngày)\n// hoặc còn trong cooldown rising-edge (đã bắn trong cooldownDays ngày gần nhất).\nfunc (b *Batch) shouldSkip(ctx context.Context, userID, productID int64, today time.Time) bool {\n    var lastFired *time.Time\n    err := b.pool.QueryRow(ctx, `\n        SELECT max(fired_on) FROM bottom_alert_log\n        WHERE user_id = $1 AND product_id = $2`, userID, productID).Scan(&amp;lastFired)\n    if err != nil || lastFired == nil {\n        return false // chưa từng bắn -&gt; cạnh lên, cho phép\n    }\n    days := int(today.Sub(*lastFired).Hours() / 24)\n    return days &lt; cooldownDays // trong cooldown (gồm cả cùng ngày = 0) -&gt; bỏ qua\n}\n\n// enqueueAndLog enqueue vào notification fan-out rồi ghi bottom_alert_log.\n// Ghi log CHỈ khi enqueue thành công (§1 #10 #11).\nfunc (b *Batch) enqueueAndLog(ctx context.Context, userID, productID int64, p float64, today time.Time) error {\n    if err := b.notif.Enqueue(ctx, notif.Item{\n        UserID:    userID,\n        ProductID: productID,\n        Reason:    \"bottom_predicted\",\n        Payload:   map[string]any{\"p_bottom_14d\": p},\n    }); err != nil {\n        return err\n    }\n    _, err := b.pool.Exec(ctx, `\n        INSERT INTO bottom_alert_log (user_id, product_id, fired_on, p_bottom)\n        VALUES ($1, $2, $3, $4)\n        ON CONFLICT (user_id, product_id, fired_on) DO NOTHING`, // idempotent retry\n        userID, productID, today, p)\n    return err\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Cron đăng ký chạy <code>02:00 Asia/Ho_Chi_Minh</code>; advisory lock chặn chạy chồng khi lần trước chưa xong.</li><li>Vòng chấm điểm chỉ lấy SKU <code>MATURE</code> (<code>first_seen &gt;= 90 ngày</code>); SKU <code>NEW</code>/<code>WARMING</code> không vào kết quả query.</li><li>Batch đọc <code>p_bottom_14d</code> từ <code>price_forecast</code> không phụ thuộc model; dòng do Prophet hay LightGBM sinh đều được chấm như nhau.</li><li>Dòng <code>price_forecast</code> có <code>scored_at</code> cũ hơn 36 giờ bị loại; chỉ dự báo tươi vào chấm điểm.</li><li><code>p_bottom_14d = 0.71</code> -&gt; vượt ngưỡng (ứng viên bắn); <code>p_bottom_14d = 0.69</code> -&gt; không vượt; <code>p_bottom_14d = 0.70</code> -&gt; KHÔNG bắn (strict greater-than).</li><li>SKU vượt ngưỡng join đúng <code>alert_rule</code> type <code>'bottom_predicted'</code> đang bật; SKU không có luật khớp -&gt; không enqueue.</li><li>Hai lần chạy batch trong cùng một ngày cho cùng <code>(user, product)</code> -&gt; chỉ một dòng <code>bottom_alert_log</code>, chỉ một enqueue (idempotent ngày).</li><li><code>(user, product)</code> đã có alert trong vòng 7 ngày, <code>p_bottom_14d</code> vẫn <code>&gt; 0.7</code> ở đêm sau -&gt; bị cooldown bỏ qua, không enqueue lại.</li><li>Mỗi cảnh báo qua dedupe enqueue vào fan-out với payload <code>user_id</code>, <code>product_id</code>, <code>p_bottom_14d</code>, <code>reason = \"bottom_predicted\"</code>.</li><li><code>bottom_alert_log</code> được ghi ngay sau khi enqueue thành công, với <code>p_bottom</code> đúng giá trị đã bắn.</li><li>Khi fan-out từ chối một mục, mục đó KHÔNG được ghi <code>bottom_alert_log</code> và các mục khác vẫn chạy tiếp (một lỗi không làm hỏng batch).</li><li>Metric <code>deal_nightly_scored_total</code>, <code>deal_bottom_alert_fired_total</code>, <code>deal_alert_dedupe_skipped_total{reason}</code> tăng đúng theo từng nhánh.</li><li><code>CHECK (p_bottom &gt; 0.7)</code> trên <code>bottom_alert_log</code> từ chối ghi giá trị tại hoặc dưới ngưỡng (bảo hiểm bất biến ngưỡng ở tầng DB).</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/deal/internal/batch/nightly_score_test.go\npackage batch\n\nfunc TestNightly_FiresAboveThreshold(t *testing.T) {\n    b, deps := setupBatch(t)\n    seedMatureForecast(t, deps, productID, 0.71) // trên ngưỡng\n    seedBottomRule(t, deps, userID, productID)   // có luật khớp\n    require.NoError(t, b.RunNightlyScore(ctx, today))\n    require.Equal(t, 1, deps.notif.Count()) // 0.71 -&gt; bắn\n\n    // 0.69 không bắn; 0.70 (đúng biên) cũng không bắn (strict &gt;).\n    for _, p := range []float64{0.69, 0.70} {\n        b2, d2 := setupBatch(t)\n        seedMatureForecast(t, d2, productID, p)\n        seedBottomRule(t, d2, userID, productID)\n        require.NoError(t, b2.RunNightlyScore(ctx, today))\n        require.Equal(t, 0, d2.notif.Count(), \"p=%.2f không được bắn\", p)\n    }\n}\n\nfunc TestNightly_RespectsMaturityGate(t *testing.T) {\n    b, deps := setupBatch(t)\n    seedImmatureForecast(t, deps, productID, 0.95) // SKU 30 ngày, P rất cao\n    seedBottomRule(t, deps, userID, productID)\n    require.NoError(t, b.RunNightlyScore(ctx, today))\n    require.Equal(t, 0, deps.notif.Count()) // chưa MATURE -&gt; không chấm, không bắn\n}\n\nfunc TestNightly_DedupePerDay(t *testing.T) {\n    b, deps := setupBatch(t)\n    seedMatureForecast(t, deps, productID, 0.80)\n    seedBottomRule(t, deps, userID, productID)\n    require.NoError(t, b.RunNightlyScore(ctx, today))\n    require.NoError(t, b.RunNightlyScore(ctx, today)) // chạy lại cùng ngày\n    require.Equal(t, 1, deps.notif.Count())            // chỉ 1 alert\n    require.Equal(t, 1, countAlertLog(t, deps, userID, productID))\n}\n\nfunc TestNightly_Cooldown_NoRepeatWhileHigh(t *testing.T) {\n    b, deps := setupBatch(t)\n    seedMatureForecast(t, deps, productID, 0.85)\n    seedBottomRule(t, deps, userID, productID)\n    require.NoError(t, b.RunNightlyScore(ctx, today))            // ngày 0: bắn\n    require.NoError(t, b.RunNightlyScore(ctx, today.AddDate(0, 0, 3))) // ngày 3, P vẫn cao\n    require.Equal(t, 1, deps.notif.Count()) // trong cooldown 7 ngày -&gt; không bắn lại\n    // sau cooldown (ngày 8) thì cạnh lên lại -&gt; bắn.\n    require.NoError(t, b.RunNightlyScore(ctx, today.AddDate(0, 0, 8)))\n    require.Equal(t, 2, deps.notif.Count())\n}\n\nfunc TestNightly_MatchesAlertRuleType(t *testing.T) {\n    b, deps := setupBatch(t)\n    seedMatureForecast(t, deps, productID, 0.90)\n    seedRule(t, deps, userID, productID, \"price_drop\") // sai type\n    require.NoError(t, b.RunNightlyScore(ctx, today))\n    require.Equal(t, 0, deps.notif.Count()) // chỉ 'bottom_predicted' mới khớp\n}\n\nfunc TestNightly_EnqueuesNotification(t *testing.T) {\n    b, deps := setupBatch(t)\n    seedMatureForecast(t, deps, productID, 0.78)\n    seedBottomRule(t, deps, userID, productID)\n    require.NoError(t, b.RunNightlyScore(ctx, today))\n    item := deps.notif.Last()\n    require.Equal(t, \"bottom_predicted\", item.Reason)\n    require.Equal(t, userID, item.UserID)\n    require.InDelta(t, 0.78, item.Payload[\"p_bottom_14d\"], 1e-9)\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration <code>0002_bottom_alert_log.sql</code> (bảng + UNIQUE + index cooldown) -&gt; <code>dedupe.go</code> (shouldSkip + enqueueAndLog) -&gt; <code>alert_match.go</code> (matchBottomRules) -&gt; <code>nightly_score.go</code> (RunNightlyScore) -&gt; đăng ký cron trong <code>main.go</code> -&gt; tests. Cron dùng scheduler sẵn của deal-svc (cùng cơ chế refresh <code>category_prior</code> của TASK-DEAL-002), đặt khung <code>02:00 Asia/Ho_Chi_Minh</code> và bọc advisory lock <code>pg_try_advisory_lock</code> để một instance chạy tại một thời điểm. Driver dùng <code>pgx</code>. Notification fan-out là interface <code>notif.Enqueuer</code> (TASK-NOTIF-003) inject vào <code>Batch</code>, nên test thay bằng fake đếm mục, không cần hệ thống thật. Bảng <code>price_forecast</code> do tầng Python (TASK-DEAL-004/005) sinh và là hợp đồng đọc-chỉ của batch này.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-DEAL-004</strong> (depends_on) - Prophet baseline sinh <code>price_forecast.p_bottom_14d</code>; là nguồn dự báo chính cho batch.</li><li><strong>TASK-DEAL-005</strong> (related) - LightGBM thay thế/song song, ghi cùng cột <code>p_bottom_14d</code> - đây là điều làm batch model-agnostic.</li><li><strong>TASK-TRACK-003</strong> (depends_on) - định nghĩa <code>alert_rule</code> type <code>'bottom_predicted'</code>; batch join vào để biết user nào muốn cảnh báo đáy cho SKU nào.</li><li><strong>TASK-NOTIF-003</strong> (consumer) - notification fan-out nhận mục enqueue, lo chọn kênh và rate-limit phía người dùng.</li><li><strong>TASK-DEAL-002</strong> (related) - maturity gate <code>IsFeatureReady</code>/<code>MATURE</code> quyết định SKU nào đủ chín để chấm điểm.</li><li>Extension/lib: PostgreSQL advisory lock; driver <code>pgx</code>; scheduler nội bộ deal-svc.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Mục enqueue vào notification fan-out (SKU sắp chạm đáy, P = 0,78)</h3>\n<pre><code class=\"language-json\">{\n  \"user_id\": 50231,\n  \"product_id\": 90112,\n  \"reason\": \"bottom_predicted\",\n  \"payload\": {\n    \"p_bottom_14d\": 0.78\n  }\n}</code></pre>\n<h3>Dòng log bỏ qua vì cooldown (đã bắn 3 ngày trước, P vẫn cao)</h3>\n<pre><code>level=info msg=\"bottom alert skipped\" user=50231 product=90112 reason=cooldown last_fired=2026-06-24 today=2026-06-27 p_bottom_14d=0.81</code></pre>\n<h3>Dòng ghi vào bottom_alert_log sau khi enqueue thành công</h3>\n<pre><code class=\"language-sql\">INSERT INTO bottom_alert_log (user_id, product_id, fired_on, p_bottom)\nVALUES (50231, 90112, '2026-06-27', 0.78)\nON CONFLICT (user_id, product_id, fired_on) DO NOTHING;</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li><code>cooldownDays</code> cố định 7 hay co giãn theo độ biến động giá của SKU - tinh chỉnh khi có dữ liệu phản hồi thực.</li><li>Có nên kèm \"khoảng cách tới đáy ước lượng\" (giá đáy dự báo) vào payload ngoài xác suất - chờ TASK-DEAL-004/005 expose thêm cột rồi mở rộng hợp đồng.</li><li>Gộp nhiều SKU vượt ngưỡng của cùng một user thành một thông báo tóm tắt (digest) thay vì từng cái - cân nhắc ở tầng fan-out (TASK-NOTIF-003), không ở batch này.</li><li>Ngưỡng <code>0.7</code> có nên khác theo ngành hàng (thời trang biến động khác điện máy) - giữ một hằng toàn cục theo §3.5 ở v1, để ML TASK-DEAL-004/005 hấp thụ khác biệt ngành.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td><code>price_forecast</code> stale (Python không chạy)</td><td>điều kiện <code>scored_at &gt;= now()-36h</code></td><td>bỏ qua dòng cũ, ít/không cảnh báo đêm đó</td><td>Loại dự báo cũ; cảnh báo lỗi pipeline dự báo qua metric</td></tr><tr><td>Batch retry trong ngày bắn trùng</td><td>UNIQUE(user, product, fired_on)</td><td>enqueue/log trùng</td><td><code>ON CONFLICT DO NOTHING</code> + dedupe ngày (§1 #7)</td></tr><tr><td>Bắn lại mỗi đêm khi P dính ngưỡng cao</td><td>test Cooldown</td><td>mệt mỏi cảnh báo, người dùng tắt push</td><td>Cooldown rising-edge 7 ngày (§1 #8)</td></tr><tr><td>Enqueue fan-out thất bại</td><td>lỗi từ <code>Enqueue</code></td><td>mục đó không tới người dùng</td><td>Không ghi log -&gt; đêm sau thử lại; một lỗi không hỏng batch (§1 #11)</td></tr><tr><td>Hạ ngưỡng dưới 0,7 hoặc đổi sang <code>&gt;=</code></td><td>code review + AC #5 + CHECK</td><td>tràn cảnh báo nhiễu, mất precision</td><td>Hằng load-bearing 0.7 strict; CHECK ở DB chặn ghi &lt;= 0.7</td></tr><tr><td>Chấm điểm SKU chưa MATURE</td><td>test MaturityGate</td><td>cảnh báo từ dự báo dữ liệu mỏng</td><td>JOIN điều kiện <code>first_seen &gt;= 90d</code> (§1 #2)</td></tr><tr><td>Gắn cứng batch vào một model</td><td>code review</td><td>đổi model phải sửa serving</td><td>Đọc cột <code>p_bottom_14d</code> chung, không tham chiếu model (§1 #3)</td></tr><tr><td>Cron chạy chồng (lần trước treo)</td><td>advisory lock fail-fast</td><td>tải đôi, chấm trùng</td><td><code>pg_try_advisory_lock</code>, một instance một lúc (§1 #1)</td></tr><tr><td>alert_rule bị tắt (<code>active=false</code>) lọt qua</td><td>điều kiện <code>active=true</code></td><td>cảnh báo cho user đã tắt luật</td><td>Lọc <code>active=true</code> trong join (§1 #6)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>TASK-DEAL-006 là tầng serving của dự đoán đáy: nó biến một cột xác suất trong bảng thành cảnh báo hành động được, đúng như §3.5 mô tả \"batch nightly score -&gt; alert\".</li><li>Ngưỡng <code>0.7</code> là load-bearing và chốt từ §3.5: đổi nó là đổi cán cân precision-recall của cảnh báo đáy, phải sửa kèm đặc tả và audit.</li><li>Model-agnostic qua <code>price_forecast.p_bottom_14d</code> là điểm tách tầng quan trọng: Prophet và LightGBM hoán đổi được mà không đụng code cảnh báo.</li><li>Idempotent ngày (UNIQUE) chống trùng khi retry; cooldown rising-edge chống spam liên ngày - hai cơ chế khác nhau cho hai loại lặp khác nhau.</li><li>Maturity gate dùng lại đúng <code>IsFeatureReady</code> của TASK-DEAL-002, giữ một định nghĩa \"đủ chín\" thống nhất toàn module DEAL.</li><li>Ghi <code>bottom_alert_log</code> chỉ sau khi enqueue thành công là cố ý: nó cho phép đêm sau thử lại mục lỗi mà không mất dấu mục đã gửi.</li><li>Batch đọc-chỉ với <code>price_forecast</code> (Python sinh) và <code>alert_rule</code> (TASK-TRACK-003), chỉ ghi vào <code>bottom_alert_log</code> và hàng đợi fan-out - ranh giới quyền rõ ràng.</li></ul>\n<hr>\n<p><em>Hết TASK-DEAL-006. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
