window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AUTH-001"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service AUTH <strong>MUST</strong> mở rộng <code>app_user</code> với cột bảo mật và cung cấp đăng ký an toàn: mật khẩu băm argon2id, email CITEXT unique đã chuẩn hóa, không bao giờ lưu cleartext. Hợp đồng:</p>\n<ol><li>Migration <code>0004_app_user_secrets</code> <strong>MUST</strong> thêm vào <code>app_user</code> (đã có cột lõi từ TASK-INFRA-002): <code>pwd_hash TEXT</code> và <code>referral_code_id BIGINT</code> (DEC-AUTH-02). KHÔNG định nghĩa lại cột lõi.</li><li>Mật khẩu <strong>MUST</strong> băm bằng argon2id (DEC-AUTH-01). KHÔNG lưu cleartext; KHÔNG dùng md5/sha1/bcrypt-yếu. <code>pwd_hash</code> lưu định dạng PHC chứa thuật toán, tham số (time, memory, parallelism), salt và hash (DEC-AUTH-04).</li><li>Mỗi lần băm <strong>MUST</strong> dùng salt ngẫu nhiên riêng -&gt; hai người dùng cùng mật khẩu có <code>pwd_hash</code> khác nhau.</li><li><code>Verify(password, phcHash)</code> <strong>MUST</strong> đọc tham số từ chính chuỗi PHC để xác thực, nên hash băm bằng tham số cũ vẫn verify được sau khi nâng tham số mặc định (DEC-AUTH-04).</li><li>Đăng ký <strong>MUST</strong> yêu cầu ít nhất một định danh: email HOẶC phone (DEC-AUTH-05). Thiếu cả hai -&gt; từ chối.</li><li>Đăng ký <strong>MUST</strong> chuẩn hóa email trước khi lưu (trim khoảng trắng) (DEC-AUTH-03); nhờ CITEXT unique (TASK-INFRA-002), <code>Chi@Mail.com</code> và <code>chi@mail.com</code> xung đột - từ chối tạo trùng.</li><li>Đăng ký <strong>MUST</strong> ép độ mạnh mật khẩu tối thiểu (DEC-AUTH-05): độ dài &gt;= 8 ký tự (ngưỡng cấu hình được). Mật khẩu yếu -&gt; từ chối với thông báo rõ.</li><li>Repo <strong>MUST</strong> expose: <code>InsertUser(ctx, u AppUser) (int64, error)</code> và <code>FindByEmail(ctx, email string) (AppUser, error)</code> (so sánh CITEXT, case-insensitive).</li><li>Hệ thống <strong>MUST</strong> KHÔNG log mật khẩu thô hay <code>pwd_hash</code> đầy đủ ở bất kỳ đâu (vi phạm là rò rỉ credential).</li><li>Đăng ký trùng email <strong>MUST</strong> trả lỗi xác định (ví dụ <code>ErrEmailTaken</code>) để tầng API ánh xạ sang <code>409 Conflict</code>, KHÔNG để lỗi UNIQUE thô lọt ra ngoài.</li><li><code>app_user.status</code> (cột lõi) <strong>MUST</strong> mặc định <code>'active'</code> khi đăng ký; TASK-AUTH-005 quản vòng đời status về sau.</li><li>Tham số argon2id mặc định <strong>SHOULD</strong> theo khuyến nghị hiện hành (ví dụ memory &gt;= 64 MiB, time &gt;= 3, parallelism phù hợp CPU), cấu hình được để nâng theo phần cứng.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p>Vì sao argon2id, không bcrypt/sha (DEC-AUTH-01)? argon2id là hàm băm mật khẩu memory-hard, thắng Password Hashing Competition, kháng tốt cả tấn công GPU lẫn side-channel. md5/sha1 không phải hàm băm mật khẩu (quá nhanh, dễ brute-force). Với một sản phẩm giữ định danh người dùng VN và chịu PDPL, dùng đúng hàm băm mật khẩu là yêu cầu cơ bản, không phải tùy chọn.</p>\n<p>Vì sao salt ngẫu nhiên mỗi lần (§1 #3)? Không có salt riêng, hai người dùng cùng mật khẩu sẽ có cùng hash, và kẻ tấn công dùng rainbow table phá hàng loạt. Salt riêng làm mỗi hash độc nhất, vô hiệu rainbow table và buộc tấn công từng tài khoản một.</p>\n<p>Vì sao lưu tham số kèm hash dạng PHC (DEC-AUTH-04)? Khuyến nghị bảo mật tăng theo thời gian (memory/time cao hơn khi phần cứng mạnh hơn). Nếu hash chỉ lưu digest mà không lưu tham số, ta không verify được hash cũ sau khi đổi mặc định. Định dạng PHC nhúng tham số vào chính chuỗi hash, nên <code>Verify</code> luôn dùng đúng tham số của thời điểm băm - nâng mặc định không phá tài khoản cũ.</p>\n<p>Vì sao email HOẶC phone (DEC-AUTH-05)? Người dùng VN nhiều người quen đăng nhập bằng số điện thoại hơn email. Bắt buộc cả hai là rào cản; cho phép ít nhất một định danh giữ trải nghiệm mở mà vẫn có một khóa định danh để liên hệ và khôi phục.</p>\n<p>Vì sao chuẩn hóa email + CITEXT (DEC-AUTH-03, §1 #6)? Người dùng gõ email tùy hứng hoa thường và có thể thừa khoảng trắng. Trim + CITEXT unique chặn một người tạo nhiều tài khoản qua <code>Chi@x.com</code> / <code>chi@x.com</code> / <code> chi@x.com </code>, vốn làm rối billing và referral (TASK-BILL-004).</p>\n<p>Vì sao lỗi đăng ký trùng phải xác định (§1 #10)? Để lỗi UNIQUE thô của Postgres lọt ra API là vừa xấu vừa lộ chi tiết schema. Trả <code>ErrEmailTaken</code> cho tầng API ánh xạ sạch sang 409 và thông báo thân thiện, không rò rỉ nội bộ.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0004_app_user_secrets.up.sql\nALTER TABLE app_user\n  ADD COLUMN pwd_hash         TEXT,          -- argon2id định dạng PHC; KHÔNG cleartext\n  ADD COLUMN referral_code_id BIGINT;\n-- FK referral_code_id -&gt; referral_code(id) thêm khi TASK-BILL-004 tạo bảng referral_code.</code></pre>\n<h3>Password hash argon2id (PHC) (§1 #2, #3, #4)</h3>\n<pre><code class=\"language-go\">// services/auth/internal/auth/password.go\ntype Argon2Params struct{ Time, Memory uint32; Parallelism uint8; SaltLen, KeyLen uint32 }\n\n// Hash trả chuỗi PHC: $argon2id$v=19$m=65536,t=3,p=2$&lt;salt_b64&gt;$&lt;hash_b64&gt;\nfunc Hash(password string, p Argon2Params) (string, error) {\n    salt := make([]byte, p.SaltLen)\n    if _, err := rand.Read(salt); err != nil { return \"\", err } // salt ngẫu nhiên (§1 #3)\n    key := argon2.IDKey([]byte(password), salt, p.Time, p.Memory, p.Parallelism, p.KeyLen)\n    return encodePHC(p, salt, key), nil\n}\n\n// Verify đọc tham số từ chính chuỗi PHC → hash cũ vẫn verify sau khi nâng mặc định (§1 #4).\nfunc Verify(password, phc string) (bool, error) {\n    p, salt, want, err := decodePHC(phc)\n    if err != nil { return false, err }\n    got := argon2.IDKey([]byte(password), salt, p.Time, p.Memory, p.Parallelism, uint32(len(want)))\n    return subtle.ConstantTimeCompare(got, want) == 1, nil // so sánh hằng thời gian\n}</code></pre>\n<h3>Đăng ký (§1 #5, #6, #7, #10)</h3>\n<pre><code class=\"language-go\">// services/auth/internal/auth/register.go\nfunc (s *Service) Register(ctx context.Context, in RegisterInput) (int64, error) {\n    if in.Email == \"\" &amp;&amp; in.Phone == \"\" { return 0, ErrNoIdentifier }      // §1 #5\n    if err := checkPasswordStrength(in.Password); err != nil { return 0, err } // §1 #7\n    email := normalizeEmail(in.Email) // trim; CITEXT lo case (§1 #6)\n    hash, err := Hash(in.Password, s.params)\n    if err != nil { return 0, err }\n    id, err := s.repo.InsertUser(ctx, AppUser{Email: email, Phone: in.Phone, PwdHash: hash})\n    if isUniqueViolation(err, \"app_user_email_key\") { return 0, ErrEmailTaken } // §1 #10\n    return id, err\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy -&gt; <code>app_user</code> có cột <code>pwd_hash</code> và <code>referral_code_id</code>.</li><li><code>Hash(\"p@ss12345\")</code> hai lần -&gt; hai chuỗi PHC KHÁC nhau (salt ngẫu nhiên).</li><li><code>Verify(\"p@ss12345\", hash)</code> với hash đúng -&gt; <code>true</code>; với mật khẩu sai -&gt; <code>false</code>.</li><li>Hash băm bằng tham số cũ (memory thấp) vẫn <code>Verify</code> đúng sau khi đổi mặc định sang tham số cao.</li><li><code>pwd_hash</code> lưu định dạng PHC chứa <code>argon2id</code> + tham số (kiểm chuỗi bắt đầu <code>$argon2id$</code>).</li><li>Đăng ký không email và không phone -&gt; <code>ErrNoIdentifier</code>.</li><li>Đăng ký với mật khẩu &lt; 8 ký tự -&gt; lỗi độ mạnh.</li><li>Đăng ký email <code>Chi@Mail.com</code> rồi <code>chi@mail.com</code> -&gt; lần hai trả <code>ErrEmailTaken</code> (CITEXT).</li><li>Đăng ký email có khoảng trắng thừa <code>\" a@x.com \"</code> -&gt; lưu chuẩn hóa <code>a@x.com</code>.</li><li><code>FindByEmail(\"A@X.COM\")</code> tìm được user đăng ký bằng <code>a@x.com</code> (case-insensitive).</li><li>Không có log nào chứa mật khẩu thô hay <code>pwd_hash</code> đầy đủ (kiểm test output + review).</li><li>Đăng ký thành công -&gt; user có <code>status='active'</code> (mặc định cột lõi).</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/auth/internal/auth/password_test.go\nfunc TestHash_DifferentEachTime(t *testing.T) {\n    h1, _ := Hash(\"p@ss12345\", defaultParams)\n    h2, _ := Hash(\"p@ss12345\", defaultParams)\n    require.NotEqual(t, h1, h2) // salt ngẫu nhiên\n    require.True(t, strings.HasPrefix(h1, \"$argon2id$\"))\n}\n\nfunc TestVerify_CorrectAndWrong(t *testing.T) {\n    h, _ := Hash(\"p@ss12345\", defaultParams)\n    ok, _ := Verify(\"p@ss12345\", h); require.True(t, ok)\n    bad, _ := Verify(\"wrong\", h);    require.False(t, bad)\n}\n\nfunc TestVerify_OldParamsStillWork(t *testing.T) {\n    weak := Argon2Params{Time: 1, Memory: 8 * 1024, Parallelism: 1, SaltLen: 16, KeyLen: 32}\n    h, _ := Hash(\"p@ss12345\", weak)          // băm bằng tham số cũ/yếu\n    ok, _ := Verify(\"p@ss12345\", h)          // verify dùng tham số đọc từ PHC\n    require.True(t, ok)\n}\n\n// services/auth/internal/auth/register_test.go\nfunc TestRegister_NoIdentifier(t *testing.T) {\n    s := newService(t)\n    _, err := s.Register(ctx, RegisterInput{Password: \"p@ss12345\"})\n    require.ErrorIs(t, err, ErrNoIdentifier)\n}\n\nfunc TestRegister_WeakPassword(t *testing.T) {\n    s := newService(t)\n    _, err := s.Register(ctx, RegisterInput{Email: \"a@x.com\", Password: \"123\"})\n    require.Error(t, err)\n}\n\nfunc TestRegister_DuplicateEmail_CaseInsensitive(t *testing.T) {\n    s := newService(t)\n    _, err1 := s.Register(ctx, RegisterInput{Email: \"Chi@Mail.com\", Password: \"p@ss12345\"})\n    require.NoError(t, err1)\n    _, err2 := s.Register(ctx, RegisterInput{Email: \"chi@mail.com\", Password: \"p@ss12345\"})\n    require.ErrorIs(t, err2, ErrEmailTaken)\n}\n\nfunc TestRegister_TrimsEmail(t *testing.T) {\n    s := newService(t)\n    id, _ := s.Register(ctx, RegisterInput{Email: \" a@x.com \", Password: \"p@ss12345\"})\n    u, _ := s.repo.FindByEmail(ctx, \"a@x.com\")\n    require.Equal(t, id, u.ID)\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Migration 0004 thêm cột bảo mật vào <code>app_user</code> (cột lõi do TASK-INFRA-002). <code>password.go</code> dùng <code>golang.org/x/crypto/argon2</code> với định dạng PHC tự mã hóa/giải mã tham số. <code>Verify</code> so sánh hằng thời gian (<code>subtle.ConstantTimeCompare</code>) chống timing attack. Tham số mặc định lấy từ cấu hình (cho phép nâng theo phần cứng) - có thể đọc qua TASK-INFRA-003 nếu cần điều chỉnh tập trung. FK <code>referral_code_id -&gt; referral_code(id)</code> hoãn tới khi TASK-BILL-004 tạo bảng đó.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li>TASK-INFRA-002 - <code>app_user</code> cột lõi (id, email CITEXT unique, locale, status) phải tồn tại trước.</li><li>TASK-AUTH-002 (downstream) - phát hành JWT sau khi xác thực mật khẩu qua <code>Verify</code>.</li><li>TASK-AUTH-003 / TASK-AUTH-005 (downstream) - liên kết sàn và vòng đời tài khoản gắn vào <code>app_user</code>.</li><li>TASK-BILL-004 (downstream) - tạo bảng <code>referral_code</code>; khi đó thêm FK cho <code>referral_code_id</code>.</li><li>TASK-COMPLY-005 (đồng hàng) - audit no-cleartext xác nhận chỉ có <code>pwd_hash</code> argon2id, không cleartext.</li><li>Thư viện: <code>golang.org/x/crypto/argon2</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Đăng ký (REST qua gateway)</h3>\n<pre><code class=\"language-http\">POST /v1/auth/register HTTP/1.1\nContent-Type: application/json\n\n{\"email\":\"chi@example.com\",\"phone\":\"+84906878091\",\"password\":\"san-deal-2026!\"}</code></pre>\n<h3>pwd_hash lưu trong DB (định dạng PHC)</h3>\n<pre><code class=\"language-text\">$argon2id$v=19$m=65536,t=3,p=2$Y2hpLXNhbHQtMTYtYnl0ZXM$3sJ...hash_b64...</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Pepper toàn cục (khóa bí mật trộn vào mọi hash, lưu trong Vault) ngoài salt per-hash - tăng phòng thủ khi DB lộ; gắn TASK-INFRA-003.</li><li>Rehash trong suốt khi đăng nhập (nâng tham số hash cũ lúc user login thành công) - tối ưu bảo mật dần.</li><li>Kiểm mật khẩu lộ (HaveIBeenPwned k-anonymity) khi đăng ký - thêm ở slice trải nghiệm sau.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Lưu mật khẩu cleartext/băm yếu</td><td>review + COMPLY-005 audit</td><td>thảm họa nếu DB lộ; vi phạm PDPL</td><td>argon2id PHC bắt buộc (§1 #2)</td></tr><tr><td>Cùng mật khẩu -&gt; cùng hash</td><td>password test</td><td>rainbow table phá hàng loạt</td><td>Salt ngẫu nhiên mỗi lần (§1 #3)</td></tr><tr><td>Nâng tham số phá hash cũ</td><td>test OldParams</td><td>user cũ không đăng nhập được</td><td>PHC lưu tham số kèm hash (§1 #4)</td></tr><tr><td>Trùng tài khoản qua hoa thường</td><td>register test</td><td>rối billing/referral</td><td>Chuẩn hóa + CITEXT (§1 #6)</td></tr><tr><td>Lỗi UNIQUE thô lọt ra API</td><td>register test</td><td>xấu UX + lộ schema</td><td>Ánh xạ <code>ErrEmailTaken</code>-&gt;409 (§1 #10)</td></tr><tr><td>Log mật khẩu/hash</td><td>test output + review</td><td>rò rỉ credential</td><td>Cấm log (§1 #9)</td></tr><tr><td>Timing attack khi Verify</td><td>review</td><td>dò mật khẩu qua thời gian</td><td>ConstantTimeCompare (§6)</td></tr><tr><td>Mật khẩu quá yếu</td><td>register test</td><td>dễ brute-force</td><td>Ép độ mạnh tối thiểu (§1 #7)</td></tr><tr><td>Thiếu cả email lẫn phone</td><td>register test</td><td>không có khóa định danh</td><td>Bắt buộc &gt;=1 định danh (§1 #5)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li><code>app_user</code> là chủ thể trung tâm: AUTH/BILL/TRACK/AFFIL gắn vào; làm đúng phần bảo mật ở đây bảo vệ toàn hệ.</li><li>argon2id là hàm băm mật khẩu đúng nghĩa (memory-hard), khác hẳn md5/sha vốn quá nhanh và dễ brute-force.</li><li>Định dạng PHC nhúng tham số vào hash là chìa khóa để nâng cấu hình bảo mật theo thời gian mà không phá tài khoản cũ.</li><li>Chuẩn hóa email + CITEXT chặn tạo nhiều tài khoản qua biến thể hoa thường, vốn làm rối billing/referral.</li><li>Trả lỗi đăng ký xác định (<code>ErrEmailTaken</code>) giữ API sạch và không lộ chi tiết schema nội bộ.</li><li>Đây là điểm chứng minh no-cleartext cho TASK-COMPLY-005: chỉ <code>pwd_hash</code> argon2id rời lớp này, không có mật khẩu thô.</li></ul>\n<hr>\n<p><em>Hết TASK-AUTH-001. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
