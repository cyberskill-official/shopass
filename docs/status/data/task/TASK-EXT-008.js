window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-EXT-008"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Content script Lazada <strong>MUST</strong> đọc giỏ hàng qua DOM đã render trong tab đăng nhập một cách thụ động (Akamai-aware), KHÔNG né/giả sensor Akamai từ client, và tái dùng khung reader/normalize/health của TASK-EXT-002. Hợp đồng:</p>\n<ol><li><code>manifest.json</code> <strong>MUST</strong> khai <code>content_scripts</code> với <code>matches</code> cho domain Lazada (ví dụ <code>https://www.lazada.vn/*</code>), <code>run_at: \"document_idle\"</code>, và thêm <code>host_permissions</code> tương ứng (per-domain, không <code>&lt;all_urls&gt;</code>, theo TASK-EXT-001).</li><li>Content script <strong>MUST</strong> đọc dữ liệu giỏ bằng cách parse DOM đã render (DEC-EXT-40) - KHÔNG gọi internal API như Shopee, vì Lazada dùng Akamai chặn/đòi sensor.</li><li>Content script <strong>MUST NOT</strong> né, giả, hay sinh Akamai sensor data (<code>_abck</code>, <code>bm_sz</code>, <code>sensor_data</code>...) hay gọi internal API qua tầng chống-bot (DEC-EXT-41). Né bot-detection chủ động bị cấm: giòn (sensor đổi liên tục) + rủi ro ToS/pháp lý.</li><li>Hành vi đọc <strong>MUST</strong> thụ động và \"như người dùng\" (DEC-EXT-42): đọc DOM đã render, KHÔNG bơm request bất thường, KHÔNG thao tác tốc độ/đo hành vi máy để vượt Akamai. \"Akamai-aware\" là tránh gây nghi, không phải vượt qua.</li><li><code>dom-selectors.ts</code> <strong>MUST</strong> cung cấp nhiều selector dự phòng cho mỗi trường (item/giá/qty) vì DOM Lazada đổi theo A/B test (DEC-EXT-43). Selector chính trượt -&gt; thử dự phòng trước khi coi là hỏng.</li><li>Khi mọi selector hỏng, content script <strong>MUST</strong> phát health signal về service worker để TASK-SCRAPE-006 ghi nhận - KHÔNG nuốt lỗi im lặng (tái dùng cơ chế TASK-EXT-002).</li><li>Content script <strong>MUST</strong> tái dùng khung reader + <code>normalize.ts</code> + health signal của TASK-EXT-002 (DEC-EXT-43); chỉ thay lớp selector đặc thù Lazada. <strong>MUST NOT</strong> phân nhánh một pipeline tối thiểu hóa riêng.</li><li>Content script <strong>MUST</strong> giữ nguyên cam kết niềm tin TASK-EXT-002 (DEC-EXT-44): KHÔNG đọc <code>document.cookie</code>/token, KHÔNG thu mật khẩu, token/cookie KHÔNG rời client; chỉ trích <code>productId</code>/giá/qty đã render.</li><li>Content script <strong>MUST</strong> chỉ đọc: KHÔNG tự sửa giỏ, KHÔNG đặt hàng, KHÔNG áp voucher tự động (DEC-EXT-44).</li><li>Kết quả <strong>MUST</strong> gửi cho service worker dạng <code>CartReadMessage</code> typed với <code>platform: \"lazada\"</code>, chứa CHỈ <code>CartItem[]</code> + <code>VoucherItem[]</code> tối thiểu - đi tiếp qua pipeline TASK-EXT-003.</li><li>Content script <strong>MUST</strong> chỉ chạy khi consent \"read_cart\" (TASK-EXT-006) bật; gọi <code>ensureConsent</code> trước khi đọc.</li><li>Content script <strong>MUST</strong> xử lý chưa-đăng-nhập / giỏ rỗng / Akamai chặn trang (challenge) lịch sự: không lỗi, báo \"chưa có dữ liệu giỏ\", KHÔNG thử vượt challenge.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao đọc DOM, không gọi API qua Akamai (DEC-EXT-40/41)?</strong> Lazada (Alibaba) dùng Akamai Bot Manager - sensor JS (<code>_abck</code>, <code>bm_sz</code>) đo hàng loạt tín hiệu để phân biệt người với bot. Né/giả sensor để gọi internal API là cái bẫy kép: sensor đổi liên tục (code vỡ mỗi lần Akamai cập nhật) VÀ né bot-detection chủ động là ranh giới ToS/pháp lý nguy hiểm (§9). Đọc DOM đã render trong tab đăng nhập là đường thụ động, bền, đúng tinh thần session piggyback.</p>\n<p><strong>Vì sao \"Akamai-aware\" là tránh, không phải vượt (DEC-EXT-42)?</strong> Hiểu lầm nguy hiểm là coi \"Akamai-aware\" như \"biết cách qua mặt Akamai\". Ngược lại: ta biết Akamai ở đó để KHÔNG kích hoạt heuristic bot của nó - đọc thụ động DOM người dùng đã thấy, không bơm request, không đo/giả hành vi máy. Hành vi của content script phải không phân biệt được với người dùng thật đọc giỏ của chính họ. Đây vừa là kỹ thuật vừa là ranh giới đạo đức.</p>\n<p><strong>Vì sao tái dùng khung TASK-EXT-002 (DEC-EXT-43)?</strong> Giống TikTok (TASK-EXT-007), phân nhánh pipeline riêng cho Lazada nhân đôi bug và mở bề mặt mới có thể lọt credential. Khung reader + normalize + health của TASK-EXT-002 đã được test cho cam kết \"không rò cookie\"; tái dùng giữ một điểm kiểm soát tối thiểu hóa duy nhất. Chỉ lớp selector là per-sàn - Lazada có DOM riêng nhưng quy trình đọc/chuẩn hóa/báo hỏng là chung.</p>\n<p><strong>Vì sao giữ nguyên cam kết niềm tin (DEC-EXT-44)?</strong> Ranh giới \"không cookie, không mật khẩu, chỉ đọc\" là định vị hậu-Honey của toàn extension (§5.4), không phụ thuộc sàn. Lazada không phải ngoại lệ. Test no-secret-leak tương tự TASK-EXT-002/007 khẳng định ranh giới cho cả Lazada.</p>\n<p><strong>Vì sao không vượt Akamai challenge (§1 #12)?</strong> Khi Akamai nghi và chặn trang bằng challenge, đúng đắn là dừng lại báo \"chưa có dữ liệu giỏ\" - tuyệt đối không thử giải/vượt challenge. Vượt challenge là né bot-detection chủ động (đúng điều cấm), và nếu Akamai chặn thì người dùng thật cũng bị chặn, nên không có gì để đọc một cách hợp lệ. Dừng lịch sự là tư thế đúng.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>manifest.json (bổ sung content script Lazada)</h3>\n<pre><code class=\"language-jsonc\">// extension/manifest.json (thêm)\n{\n  \"host_permissions\": [\"https://shopee.vn/*\", \"https://*.tiktok.com/*\", \"https://www.lazada.vn/*\"],\n  \"content_scripts\": [\n    {\n      \"matches\": [\"https://www.lazada.vn/*\"],\n      \"js\": [\"content/lazada/index.js\"],\n      \"run_at\": \"document_idle\"\n    }\n  ]\n}</code></pre>\n<h3>cart-reader.ts (đọc DOM thụ động, KHÔNG sensor Akamai; tái dùng normalize + health)</h3>\n<pre><code class=\"language-ts\">// extension/src/content/lazada/cart-reader.ts\nimport { normalizeCart } from \"../shared/normalize\";     // tái dùng TASK-EXT-002\nimport { reportHealth } from \"../shared/health\";         // tái dùng TASK-EXT-002\n\nexport async function readLazadaCart(): Promise&lt;CartReadMessage&gt; {\n  if (!(await ensureConsent(\"read_cart\"))) {             // TASK-EXT-006 gate\n    return { type: \"CART_READ\", platform: \"lazada\", items: [], vouchers: [] };\n  }\n  let raw = readCartFromDom();                            // CHỈ DOM render; KHÔNG _abck/bm_sz/sensor\n  if (raw === null) {\n    reportHealth({ platform: \"lazada\", broke: \"cart\", source: \"dom\" }); // TASK-SCRAPE-006\n    raw = [];\n  }\n  const items = normalizeCart(raw);                      // tối thiểu hóa chung\n  return { type: \"CART_READ\", platform: \"lazada\", items, vouchers: readVouchersFromDom() };\n  // LƯU Ý: KHÔNG document.cookie, KHÔNG né/giả sensor Akamai, KHÔNG gọi internal API qua chống-bot.\n}</code></pre>\n<h3>dom-selectors.ts (nhiều selector dự phòng, đặc thù Lazada)</h3>\n<pre><code class=\"language-ts\">// extension/src/content/lazada/dom-selectors.ts\nexport const CART_ITEM_SELECTORS = [\n  \".cart-item\",                 // selector chính\n  \"[data-tracking='cart-item']\",// dự phòng A\n  \".item-content\"               // dự phòng B (biến thể A/B)\n];\nexport function firstMatch(root: ParentNode, sels: string[]): Element[] {\n  for (const s of sels) {\n    const found = root.querySelectorAll(s);\n    if (found.length) return [...found];\n  }\n  return [];                    // không khớp → caller phát health\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>manifest.json</code> có <code>content_scripts</code> match <code>https://www.lazada.vn/*</code>, <code>run_at: document_idle</code>; <code>host_permissions</code> thêm domain Lazada, KHÔNG <code>&lt;all_urls&gt;</code>.</li><li>Grep <code>src/content/lazada/**</code>: KHÔNG có <code>_abck</code>, <code>bm_sz</code>, <code>sensor_data</code>, KHÔNG né/giả Akamai sensor, KHÔNG gọi internal API qua tầng chống-bot.</li><li>Grep <code>src/content/lazada/**</code>: KHÔNG có <code>document.cookie</code>, KHÔNG đọc <code>input[type=password]</code>.</li><li>Reader đọc giỏ từ DOM mẫu Lazada -&gt; trả đúng item tối thiểu (productId/price/qty).</li><li>Selector chính trượt -&gt; dùng selector dự phòng; vẫn parse được item từ DOM \"biến thể A/B\".</li><li>Mọi selector hỏng -&gt; phát health signal (broke: \"cart\") VÀ trả <code>items: []</code>, không ném lỗi.</li><li>Reader tái dùng <code>normalize.ts</code> + health của TASK-EXT-002 (grep import từ <code>content/shared/</code>), không phân nhánh riêng.</li><li><code>CartReadMessage</code> có <code>platform: \"lazada\"</code>, chỉ <code>items</code> + <code>vouchers</code>; không cookie/token (introspection payload).</li><li>Akamai challenge / giỏ rỗng -&gt; trả message rỗng lịch sự, không thử vượt challenge (test path).</li><li>Reader chỉ chạy khi consent \"read_cart\" bật (gọi <code>ensureConsent</code>); chưa bật -&gt; trả rỗng.</li><li><code>npm test</code> xanh; <code>tsc --noEmit</code> sạch.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-ts\">// extension/test/lazada-no-akamai-evasion.test.ts\ntest(\"KHÔNG né/giả Akamai sensor, KHÔNG đọc cookie/mật khẩu\", async () =&gt; {\n  for (const f of [\"cart-reader\", \"dom-selectors\", \"index\"]) {\n    const src = await readFile(`src/content/lazada/${f}.ts`, \"utf8\");\n    expect(src).not.toMatch(/_abck|bm_sz|sensor_data/i);     // không sensor Akamai\n    expect(src).not.toMatch(/document\\.cookie/);             // không cookie\n    expect(src).not.toMatch(/input\\[type=[\"']?password/i);   // không mật khẩu\n  }\n});\n\ntest(\"payload Lazada KHÔNG chứa cookie/token/sensor\", async () =&gt; {\n  setConsent([\"read_cart\"]);\n  document.body.innerHTML = lazadaCartFixtureMain;\n  const msg = await readLazadaCart();\n  const flat = JSON.stringify(msg).toLowerCase();\n  for (const b of [\"cookie\", \"token\", \"_abck\", \"bm_sz\", \"sensor\", \"password\"]) {\n    expect(flat).not.toContain(b);\n  }\n});</code></pre>\n<pre><code class=\"language-ts\">// extension/test/lazada-cart-reader.test.ts\ntest(\"đọc giỏ từ DOM Lazada đã render\", async () =&gt; {\n  setConsent([\"read_cart\"]);\n  document.body.innerHTML = lazadaCartFixtureMain;          // 2 item\n  const msg = await readLazadaCart();\n  expect(msg.platform).toBe(\"lazada\");\n  expect(msg.items.length).toBe(2);\n});\n\ntest(\"reader tái dùng normalize/health TASK-EXT-002\", async () =&gt; {\n  const src = await readFile(\"src/content/lazada/cart-reader.ts\", \"utf8\");\n  expect(src).toMatch(/from [\"']\\.\\.\\/shared\\/normalize[\"']/);\n  expect(src).toMatch(/from [\"']\\.\\.\\/shared\\/health[\"']/);\n});\n\ntest(\"Akamai challenge / giỏ rỗng → rỗng lịch sự, không vượt challenge\", async () =&gt; {\n  setConsent([\"read_cart\"]);\n  document.body.innerHTML = \"&lt;div id='akamai-challenge'&gt;...&lt;/div&gt;\";\n  const msg = await readLazadaCart();\n  expect(msg.items).toEqual([]);                            // không thử vượt\n});</code></pre>\n<pre><code class=\"language-ts\">// extension/test/lazada-dom-fallback.test.ts\ntest(\"selector chính trượt → dùng selector dự phòng (A/B variant)\", () =&gt; {\n  document.body.innerHTML = lazadaCartFixtureVariant;       // class đổi\n  const items = readCartFromDom();\n  expect(items &amp;&amp; items.length).toBeGreaterThan(0);\n});\n\ntest(\"parse hỏng hẳn → health signal + items rỗng\", async () =&gt; {\n  setConsent([\"read_cart\"]);\n  document.body.innerHTML = \"&lt;div&gt;không khớp&lt;/div&gt;\";\n  const sp = jest.fn(); setHealthReporter(sp);\n  const msg = await readLazadaCart();\n  expect(sp).toHaveBeenCalledWith(expect.objectContaining({ broke: \"cart\", platform: \"lazada\" }));\n  expect(msg.items).toEqual([]);\n});</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: cập nhật <code>manifest.json</code> (content_scripts lazada + host_permissions) -&gt; <code>dom-selectors.ts</code> (bảng selector dự phòng Lazada) -&gt; <code>cart-reader.ts</code> (đọc DOM thụ động, tái dùng normalize + health TASK-EXT-002, gate consent) -&gt; <code>index.ts</code> (entry, gửi CartReadMessage platform lazada) -&gt; tests. KHÔNG chạm <code>_abck</code>/<code>bm_sz</code>/sensor, KHÔNG vượt Akamai challenge. Khung reader/normalize/health dùng chung với Shopee (TASK-EXT-002) và TikTok (TASK-EXT-007); chỉ lớp selector là đặc thù Lazada. Kết quả qua pipeline TASK-EXT-003.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-EXT-002</strong> - cung cấp khung reader + normalize + health signal tái dùng; ranh giới niềm tin chung.</li><li><strong>TASK-EXT-003</strong> - pipeline tối thiểu hóa nhận <code>CartReadMessage(platform:lazada)</code>, lọc lần hai.</li><li><strong>TASK-EXT-006</strong> - consent \"read_cart\" gate trước khi đọc.</li><li><strong>TASK-SCRAPE-006</strong> - DOM-change monitoring nhận health signal khi parser Lazada hỏng.</li><li><strong>TASK-SCRAPE-008 (song song, P2)</strong> - adapter Lazada backend (Akamai, residential bắt buộc); reader này là phía client.</li><li><strong>TASK-TRUST-002</strong> - local-first + tối thiểu hóa; reader giữ cam kết.</li><li><strong>NFR-EXT-001</strong> - content script gửi message cho SW ephemeral; không giữ state global.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Message content Lazada -&gt; service worker (tối thiểu, KHÔNG credential/sensor)</h3>\n<pre><code class=\"language-json\">{\n  \"type\": \"CART_READ\",\n  \"platform\": \"lazada\",\n  \"items\": [\n    { \"productId\": \"L-5520118\", \"price\": 215000, \"qty\": 1 },\n    { \"productId\": \"L-4410092\", \"price\": 38000, \"qty\": 2 }\n  ],\n  \"vouchers\": [\n    { \"code\": \"LAZFREESHIP\", \"minSpend\": 0, \"discountText\": \"Freeship toàn sàn\" }\n  ]\n}</code></pre>\n<h3>Health signal khi parser Lazada hỏng (-&gt; TASK-SCRAPE-006)</h3>\n<pre><code class=\"language-json\">{ \"type\": \"PARSER_HEALTH\", \"platform\": \"lazada\", \"broke\": \"cart\", \"source\": \"dom\", \"ts\": 1790000000000 }</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Đọc trang sản phẩm Lazada (PDP) ngoài giỏ - slice sau; P2 slice 1 nhắm giỏ + voucher.</li><li>Phân biệt LazMall vs marketplace seller trong dữ liệu giỏ - hoãn tới TASK-CART-001 (voucher_catalog) nếu cần.</li><li>Xử lý Lazada đa quốc gia SEA (lazada.co.th/.com.my) - thêm khi mở per-country (TASK-COMPLY-006); task này nhắm lazada.vn.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Né/giả Akamai sensor (_abck/bm_sz)</td><td>lazada-no-akamai-evasion test</td><td>giòn + rủi ro ToS/pháp lý §9</td><td>Chỉ đọc DOM render thụ động (DEC-EXT-41)</td></tr><tr><td>Hiểu \"Akamai-aware\" = vượt Akamai</td><td>review + thụ động test</td><td>né bot-detection chủ động</td><td>Đọc như người dùng, không bơm request (DEC-EXT-42)</td></tr><tr><td>Đọc document.cookie/token</td><td>no-secret-leak test</td><td>rò credential §5.4</td><td>Cấm về mã (DEC-EXT-44)</td></tr><tr><td>Thu thập mật khẩu</td><td>grep password</td><td>phá cam kết lõi</td><td>Không hook form login (DEC-EXT-44)</td></tr><tr><td>Selector chính trượt</td><td>health + variant test</td><td>mất dữ liệu giỏ</td><td>Selector dự phòng (DEC-EXT-43)</td></tr><tr><td>Parse hỏng nuốt im lặng</td><td>thiếu health signal</td><td>mất tính năng âm thầm</td><td>Bắt buộc health -&gt; TASK-SCRAPE-006 (§1 #6)</td></tr><tr><td>Phân nhánh pipeline riêng</td><td>grep import shared</td><td>nhân đôi bug + bề mặt rò</td><td>Tái dùng normalize/health TASK-EXT-002 (DEC-EXT-43)</td></tr><tr><td>Thử vượt Akamai challenge</td><td>challenge test</td><td>né bot chủ động, rủi ro pháp lý</td><td>Dừng lịch sự, không vượt (§1 #12)</td></tr><tr><td>Tự sửa giỏ/đặt hàng</td><td>grep mutate</td><td>đặt nhầm + nghi lạm dụng</td><td>Chỉ đọc (DEC-EXT-44)</td></tr><tr><td>Đọc khi chưa consent</td><td>consent gate test</td><td>xử lý không cơ sở pháp lý</td><td>ensureConsent(\"read_cart\") trước (§1 #11)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Lazada đóng bộ ba sàn cho moat so sánh giá chéo - điểm khác biệt cốt lõi vs BeeCost (§5.6).</li><li>Lazada (Alibaba) dùng Akamai Bot Manager; ranh giới cứng: KHÔNG né/giả sensor <code>_abck</code>/<code>bm_sz</code> - giòn + rủi ro ToS/pháp lý (§9, §5.5).</li><li>\"Akamai-aware\" nghĩa là biết để TRÁNH gây nghi (đọc thụ động như người dùng), KHÔNG phải để vượt qua - vừa là kỹ thuật vừa là ranh giới đạo đức.</li><li>Tái dùng khung reader/normalize/health của TASK-EXT-002 giữ một điểm kiểm soát tối thiểu hóa duy nhất; chỉ selector là per-sàn (giống TikTok TASK-EXT-007).</li><li>Cam kết niềm tin (không cookie/mật khẩu, chỉ đọc) không phụ thuộc sàn - test no-secret-leak khẳng định cho cả Lazada.</li></ul>\n<hr>\n<p><em>Hết TASK-EXT-008. Status: done (mục tiêu audit 10/10).</em></p>";
