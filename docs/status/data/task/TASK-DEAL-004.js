window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-DEAL-004"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service ML <strong>MUST</strong> cung cấp một bộ dự đoán đáy giá baseline dựa trên Facebook/Meta Prophet: đọc chuỗi giá ngày từ <code>price_daily</code> (TASK-PRICE-002), dựng khung regressor cho nhịp khuyến mãi VN, fit Prophet với mùa vụ năm + tháng, dự báo 14 ngày tới, suy ra xác suất chạm đáy trong 14 ngày, và lưu kết quả vào bảng <code>price_forecast</code> để batch đêm và alert (TASK-DEAL-006) đọc. Đây là BASELINE; TASK-DEAL-005 nâng cấp lên LightGBM cho SKU có <code>&gt;= 180</code> ngày lịch sử. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> đọc chuỗi đầu vào từ continuous aggregate <code>price_daily</code> (DEC-DEAL-31): map <code>ds = day</code>, <code>y = close_p</code>, sắp theo <code>ds</code> tăng dần - KHÔNG đọc raw <code>price_snapshot</code> (sai độ phân giải và đốt I/O).</li><li><strong>MUST</strong> dựng khung regressor cho từng ngày của lịch sử VÀ 14 ngày tương lai gồm ba cờ: <code>is_double_date</code> (ngày mà <code>day == month</code>, ví dụ 1.1, 2.2, ... 12.12), <code>is_payday_window</code> (cửa sổ quanh ngày 1 và ngày 15 hằng tháng), <code>flash_sale</code> (cờ flash sale của ngày đó nếu có, mặc định 0 cho ngày tương lai chưa biết).</li><li><strong>MUST</strong> cung cấp hàm thuần <code>is_double_date(date) -&gt; bool</code> trả <code>True</code> đúng khi <code>date.day == date.month</code>, và <code>days_to_next_double_date(date) -&gt; int</code> trả số ngày tới ngày double-date kế tiếp (0 khi chính ngày đó là double-date).</li><li><strong>MUST</strong> cung cấp <code>is_payday_window(date) -&gt; bool</code> trả <code>True</code> khi ngày nằm trong cửa sổ lương: quanh ngày 1 (ngày cuối tháng trước tới ngày 2) và quanh ngày 15 (ngày 14 tới 16), là khoảng sức mua tăng.</li><li><strong>MUST</strong> cấu hình Prophet với <code>seasonality_mode='multiplicative'</code>, bật <code>yearly_seasonality=True</code>, thêm mùa vụ tháng (<code>add_seasonality(name='monthly', period=30.5, fourier_order=5)</code>), và <code>add_regressor</code> cho cả ba cờ ở §1 #2 (DEC-DEAL-30).</li><li><strong>MUST</strong> khi SKU đã qua cổng MATURE (<code>&gt;= 90</code> ngày, theo TASK-DEAL-002): fit Prophet trên toàn bộ chuỗi <code>price_daily</code> và predict 14 ngày tới, sinh <code>yhat</code>, <code>yhat_lower</code>, <code>yhat_upper</code> cho mỗi <code>horizon_day</code> từ 1 tới 14.</li><li><strong>MUST</strong> khi SKU CHƯA qua cổng MATURE (cold-start, DEC-DEAL-32): KHÔNG fit Prophet trên chuỗi mỏng. Thay vào đó gọi <code>category_prior</code> (TASK-DEAL-002 <code>PriorFor</code>) và trả forecast phẳng quanh <code>prior.median_price</code> với khoảng tin cậy nới rộng; <code>model_kind = 'category_prior'</code>.</li><li><strong>MUST</strong> suy <code>p_bottom_14d</code> (xác suất giá chạm đáy trong 14 ngày tới) từ dự báo và khoảng tin cậy (DEC-DEAL-33): so cận dưới dự báo <code>min(yhat_lower[1..14])</code> với đáy quan sát gần đây (<code>trailing_min</code> 30/60/90 ngày); xác suất cao khi cận dưới tương lai xuống sát hoặc dưới đáy gần đây.</li><li><strong>MUST</strong> đảm bảo <code>p_bottom_14d</code> đơn điệu không giảm theo độ rộng khoảng tin cậy ở phía dưới: khoảng tin cậy càng nới xuống thấp (cận dưới càng thấp so với đáy gần đây) thì <code>p_bottom_14d</code> càng lớn, kẹp trong <code>[0.0, 1.0]</code>.</li><li><strong>MUST</strong> lưu forecast vào bảng <code>price_forecast</code> (DEC-DEAL-34): mỗi lần chạy ghi 14 dòng (1 dòng/<code>horizon_day</code>) cho <code>(product_id, run_date)</code> kèm <code>yhat</code>, <code>yhat_lower</code>, <code>yhat_upper</code>, <code>p_bottom_14d</code> (lặp cùng giá trị trên 14 dòng), <code>model_kind</code> (<code>'prophet'</code> hoặc <code>'category_prior'</code>). Batch đêm và TASK-DEAL-006 chỉ đọc bảng này, không gọi lại huấn luyện.</li><li><strong>MUST</strong> tất định với seed và <code>uncertainty_samples</code> cố định: cùng chuỗi đầu vào, cùng <code>run_date</code>, cùng cấu hình cho cùng <code>yhat</code>/<code>yhat_lower</code>/<code>p_bottom_14d</code> (đặt <code>mcmc_samples=0</code>, cố định <code>uncertainty_samples</code>, seed numpy) để test lặp lại được.</li><li><strong>MUST</strong> trả giá ở đơn vị VND số nguyên khi ghi xuống <code>price_forecast</code> (làm tròn <code>yhat</code> về <code>BIGINT</code>), đồng bộ DEC-PRICE-05 của TASK-PRICE-002 (không lưu giá dạng thập phân float).</li><li><strong>SHOULD</strong> phát OTel metric: <code>ml_forecast_run_total{model_kind}</code> (counter), <code>ml_forecast_p_bottom</code> (histogram phân bố <code>p_bottom_14d</code>), <code>ml_forecast_duration_ms</code> (histogram thời gian fit + predict).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao Prophet làm baseline (DEC-DEAL-30)?</strong> Prophet xử lý mùa vụ và sự kiện kiểu ngày lễ với rất ít tinh chỉnh: khai báo yearly + monthly seasonality và thêm vài regressor nhị phân là đủ bắt được chu kỳ. Nó chịu được lỗ hổng dữ liệu, cho sẵn khoảng tin cậy <code>yhat_lower/upper</code> mà ta cần để suy xác suất đáy, và là một mốc trung thực để TASK-DEAL-005 (LightGBM) phải vượt qua. Bắt đầu bằng baseline đơn giản, có khoảng tin cậy, dễ giải thích là lựa chọn đúng cho giai đoạn này.</p>\n<p><strong>Vì sao regressor double-date và payday (§1 #2)?</strong> Lịch khuyến mãi VN không trải đều: đáy giá thật sự dồn vào các ngày double-date (1.1, 2.2, ... 12.12 nơi ngày trùng tháng) và quanh kỳ lương (đầu tháng, giữa tháng) khi sức mua bật lên. Một mô hình mùa vụ thuần năm/tháng sẽ bỏ lỡ các đỉnh sự kiện rời rạc này. Đưa chúng vào dưới dạng regressor cho Prophet biết \"ngày 12.12 sắp tới thường là điểm giá thấp\", đúng tín hiệu người dùng cần để được khuyên chờ.</p>\n<p><strong>Vì sao category prior cho cold-start (DEC-DEAL-32)?</strong> Một SKU mới hoặc chuỗi vài tuần không đủ để Prophet ước lượng mùa vụ năm - fit lên nó cho dự báo nhiễu và khoảng tin cậy vô nghĩa. TASK-DEAL-002 đã có sẵn <code>category_prior</code> (chỉ gộp từ SKU MATURE). Khi SKU chưa qua cổng MATURE, ta mượn median của category làm dự báo phẳng tạm, nới khoảng tin cậy để phản ánh sự thiếu chắc chắn, thay vì giả vờ có một mô hình tốt. Đây cũng là cùng prior mà TASK-DEAL-002 dùng cho fallback verdict.</p>\n<p><strong>Vì sao suy P(đáy trong 14 ngày) từ khoảng tin cậy (DEC-DEAL-33)?</strong> Người dùng không cần một con số <code>yhat</code> chính xác, họ cần một câu trả lời \"nên chờ không\". Cận dưới dự báo <code>yhat_lower</code> cho ta kịch bản giá thấp hợp lý trong 14 ngày tới. So nó với đáy quan sát gần đây cho biết khả năng giá phá đáy. Đổi dự báo điểm thành một xác suất hành động được là cách biến mô hình thành lời khuyên cụ thể, đồng thời cho TASK-DEAL-006 một ngưỡng (<code>&gt; 0,7</code>) để quyết định bắn cảnh báo.</p>\n<p><strong>Vì sao lưu price_forecast thay vì tính lúc alert (DEC-DEAL-34)?</strong> Fit Prophet cho hàng triệu SKU là việc nặng, chạy theo mẻ ban đêm. Alert và batch chấm điểm phải nhẹ và nhanh. Tách huấn luyện (ghi bảng) khỏi phục vụ (đọc bảng) cho phép đường alert chỉ là một truy vấn <code>price_forecast</code>, không kéo theo Prophet. Đây là cùng nguyên tắc decouple mà <code>price_daily</code> áp cho biểu đồ: tính sẵn, đọc nhanh.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Regressors (Python)</h3>\n<pre><code class=\"language-python\"># services/ml/bottom/regressors.py\nfrom datetime import date, timedelta\n\n\ndef is_double_date(d: date) -&gt; bool:\n    \"\"\"True đúng khi ngày trùng tháng (1.1, 2.2, ... 12.12).\"\"\"\n    return d.day == d.month\n\n\ndef days_to_next_double_date(d: date) -&gt; int:\n    \"\"\"Số ngày tới ngày double-date kế tiếp; 0 nếu chính ngày đó là double-date.\"\"\"\n    probe = d\n    for _ in range(366):\n        if is_double_date(probe):\n            return (probe - d).days\n        probe += timedelta(days=1)\n    return 0  # không xảy ra (luôn có double-date trong 1 năm)\n\n\ndef is_payday_window(d: date) -&gt; bool:\n    \"\"\"True trong cửa sổ lương: quanh ngày 1 (cuối tháng trước -&gt; ngày 2) và ngày 15 (14 -&gt; 16).\"\"\"\n    if d.day in (1, 2) or d.day in (14, 15, 16):\n        return True\n    nxt = d + timedelta(days=1)\n    return nxt.day == 1  # ngày cuối tháng (liền trước mùng 1)</code></pre>\n<h3>Features (Python)</h3>\n<pre><code class=\"language-python\"># services/ml/bottom/features.py\nimport pandas as pd\nfrom .regressors import is_double_date, is_payday_window\n\nHORIZON_DAYS = 14\n\n\ndef build_regressor_frame(history: pd.DataFrame, horizon: int = HORIZON_DAYS) -&gt; pd.DataFrame:\n    \"\"\"history: cột ds (datetime ngày), y (close_p), flash_sale (0/1).\n    Trả khung ds + 3 cờ regressor, nối thêm `horizon` ngày tương lai (flash_sale=0).\n    \"\"\"\n    future_ds = pd.date_range(\n        history[\"ds\"].max() + pd.Timedelta(days=1), periods=horizon, freq=\"D\"\n    )\n    future = pd.DataFrame({\"ds\": future_ds, \"flash_sale\": 0})\n    frame = pd.concat([history[[\"ds\", \"flash_sale\"]], future], ignore_index=True)\n    d = frame[\"ds\"].dt.date\n    frame[\"is_double_date\"] = d.map(is_double_date).astype(int)\n    frame[\"is_payday_window\"] = d.map(is_payday_window).astype(int)\n    return frame</code></pre>\n<h3>Prophet baseline (Python)</h3>\n<pre><code class=\"language-python\"># services/ml/bottom/prophet_baseline.py\nimport numpy as np\nimport pandas as pd\nfrom prophet import Prophet\nfrom .features import build_regressor_frame, HORIZON_DAYS\n\nREGRESSORS = (\"is_double_date\", \"is_payday_window\", \"flash_sale\")\n\n\ndef build_baseline(seed: int = 42) -&gt; Prophet:\n    \"\"\"Prophet baseline: mùa vụ năm + tháng, regressor double-date/payday/flash (DEC-DEAL-30).\"\"\"\n    np.random.seed(seed)\n    m = Prophet(\n        seasonality_mode=\"multiplicative\",\n        yearly_seasonality=True,\n        weekly_seasonality=False,\n        daily_seasonality=False,\n        mcmc_samples=0,\n        uncertainty_samples=1000,\n    )\n    m.add_seasonality(name=\"monthly\", period=30.5, fourier_order=5)\n    for r in REGRESSORS:\n        m.add_regressor(r)\n    return m\n\n\ndef forecast_bottom(history: pd.DataFrame, prior_median: int | None = None,\n                    seed: int = 42) -&gt; pd.DataFrame:\n    \"\"\"Trả DataFrame 14 dòng: horizon_day, yhat, yhat_lower, yhat_upper, p_bottom_14d, model_kind.\n    Nếu history chưa MATURE (prior_median được truyền) -&gt; forecast phẳng quanh prior (DEC-DEAL-32).\"\"\"\n    if prior_median is not None:\n        return _cold_start_forecast(prior_median)\n\n    frame = build_regressor_frame(history)\n    m = build_baseline(seed)\n    train = history.merge(frame, on=[\"ds\", \"flash_sale\"], how=\"left\")\n    m.fit(train[[\"ds\", \"y\", *REGRESSORS]])\n    fcst = m.predict(frame).tail(HORIZON_DAYS).reset_index(drop=True)\n\n    trailing_min = int(history[\"y\"].tail(90).min())\n    p_bottom = _p_bottom_14d(fcst[\"yhat_lower\"].to_numpy(), trailing_min)\n    out = pd.DataFrame({\n        \"horizon_day\": range(1, HORIZON_DAYS + 1),\n        \"yhat\": fcst[\"yhat\"].round().astype(\"int64\"),\n        \"yhat_lower\": fcst[\"yhat_lower\"].round().astype(\"int64\"),\n        \"yhat_upper\": fcst[\"yhat_upper\"].round().astype(\"int64\"),\n        \"p_bottom_14d\": p_bottom,\n        \"model_kind\": \"prophet\",\n    })\n    return out\n\n\ndef _p_bottom_14d(yhat_lower: np.ndarray, trailing_min: int) -&gt; float:\n    \"\"\"Đơn điệu theo khoảng tin cậy phía dưới (DEC-DEAL-33): cận dưới 14 ngày càng thấp\n    so với đáy gần đây thì xác suất phá đáy càng cao. Kẹp [0, 1].\"\"\"\n    floor14 = float(yhat_lower.min())\n    if trailing_min &lt;= 0:\n        return 0.0\n    drop = (trailing_min - floor14) / trailing_min  # &gt;0 khi cận dưới xuống dưới đáy cũ\n    return float(min(1.0, max(0.0, 0.5 + drop)))\n\n\ndef _cold_start_forecast(prior_median: int) -&gt; pd.DataFrame:\n    band = int(round(prior_median * 0.12))  # khoảng tin cậy nới rộng cho cold-start\n    return pd.DataFrame({\n        \"horizon_day\": range(1, HORIZON_DAYS + 1),\n        \"yhat\": prior_median,\n        \"yhat_lower\": prior_median - band,\n        \"yhat_upper\": prior_median + band,\n        \"p_bottom_14d\": 0.0,        # chưa đủ cơ sở -&gt; không hứa đáy\n        \"model_kind\": \"category_prior\",\n    })</code></pre>\n<h3>Migration (SQL)</h3>\n<pre><code class=\"language-sql\">-- services/ml/migrations/0001_price_forecast.sql\nCREATE TABLE price_forecast (\n  product_id    BIGINT      NOT NULL REFERENCES tracked_product(id),\n  run_date      DATE        NOT NULL,                  -- ngày chạy batch dự báo (Asia/Ho_Chi_Minh)\n  horizon_day   SMALLINT    NOT NULL CHECK (horizon_day BETWEEN 1 AND 14),\n  yhat          BIGINT      NOT NULL,            -- VND, làm tròn (DEC-PRICE-05); cũng là expected_min_14d mà TASK-DEAL-005 tham chiếu\n  yhat_lower    BIGINT      NOT NULL,\n  yhat_upper    BIGINT      NOT NULL,\n  p_bottom_14d  REAL        NOT NULL CHECK (p_bottom_14d BETWEEN 0 AND 1),\n  model_kind    TEXT        NOT NULL CHECK (model_kind IN ('prophet', 'category_prior', 'lgbm')), -- 'lgbm' do TASK-DEAL-005 ghi cùng bảng\n  scored_at     TIMESTAMPTZ NOT NULL DEFAULT now(),    -- mốc sinh dòng dự báo; TASK-DEAL-006 lọc tươi (scored_at &gt;= now()-36h)\n  PRIMARY KEY (product_id, run_date, horizon_day)\n);\n\nCREATE INDEX idx_forecast_latest ON price_forecast (product_id, run_date DESC);</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Bộ dự báo đọc chuỗi từ <code>price_daily</code> map <code>ds=day, y=close_p</code> (kiểm qua frame đầu vào của <code>forecast_bottom</code>), không chạm <code>price_snapshot</code>.</li><li><code>build_regressor_frame</code> trả khung phủ toàn lịch sử + 14 ngày tương lai, có cột <code>is_double_date</code>, <code>is_payday_window</code>, <code>flash_sale</code> (0/1).</li><li><code>is_double_date(d)</code> trả <code>True</code> đúng khi <code>d.day == d.month</code>; <code>days_to_next_double_date(d)</code> trả <code>0</code> khi d là double-date, dương ngược lại.</li><li><code>is_payday_window(d)</code> trả <code>True</code> trong cửa sổ quanh ngày 1 và ngày 15 (gồm ngày cuối tháng), <code>False</code> ngoài đó.</li><li>Prophet cấu hình <code>seasonality_mode='multiplicative'</code>, <code>yearly_seasonality=True</code>, có <code>monthly</code> seasonality và <code>add_regressor</code> cho cả ba cờ.</li><li>SKU MATURE -&gt; fit Prophet, <code>forecast_bottom</code> trả đúng 14 dòng <code>horizon_day</code> 1..14 với <code>yhat</code>/<code>yhat_lower</code>/<code>yhat_upper</code>, <code>model_kind='prophet'</code>.</li><li>SKU cold-start (truyền <code>prior_median</code>) -&gt; KHÔNG fit Prophet, trả forecast phẳng quanh prior, <code>model_kind='category_prior'</code>.</li><li><code>p_bottom_14d</code> suy từ <code>min(yhat_lower)</code> và <code>trailing_min</code> gần đây, nằm trong <code>[0,1]</code>.</li><li><code>p_bottom_14d</code> đơn điệu không giảm khi cận dưới dự báo hạ thấp so với đáy gần đây (test khoảng tin cậy rộng hơn -&gt; xác suất không nhỏ hơn).</li><li>Mỗi lần chạy ghi 14 dòng vào <code>price_forecast</code> cho <code>(product_id, run_date)</code>; alert đọc bảng này, không gọi lại fit.</li><li>Cùng chuỗi + seed + <code>run_date</code> cho cùng <code>yhat</code>/<code>p_bottom_14d</code> (tất định, <code>mcmc_samples=0</code>).</li><li><code>yhat</code> ghi xuống <code>price_forecast</code> là <code>BIGINT</code> VND (đã làm tròn), thỏa CHECK của migration.</li><li>Metric <code>ml_forecast_run_total{model_kind}</code> tăng theo nhãn; <code>ml_forecast_p_bottom</code> ghi nhận phân bố xác suất.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-python\"># services/ml/tests/test_regressors.py\nfrom datetime import date\nfrom bottom.regressors import is_double_date, days_to_next_double_date, is_payday_window\n\n\ndef test_is_double_date():\n    assert is_double_date(date(2026, 2, 2))    # 2.2\n    assert is_double_date(date(2026, 12, 12))  # 12.12\n    assert not is_double_date(date(2026, 4, 3))  # 3.4 không trùng\n\n\ndef test_days_to_next_double_date():\n    assert days_to_next_double_date(date(2026, 12, 12)) == 0      # chính ngày\n    assert days_to_next_double_date(date(2026, 12, 1)) == 11      # tới 12.12\n    assert days_to_next_double_date(date(2026, 12, 13)) &gt; 0       # vòng sang 1.1 năm sau\n\n\ndef test_is_payday_window():\n    assert is_payday_window(date(2026, 6, 1))    # đầu tháng\n    assert is_payday_window(date(2026, 6, 15))   # giữa tháng\n    assert is_payday_window(date(2026, 6, 30))   # cuối tháng (liền mùng 1)\n    assert not is_payday_window(date(2026, 6, 8))  # ngoài cửa sổ</code></pre>\n<pre><code class=\"language-python\"># services/ml/tests/test_prophet_baseline.py\nimport numpy as np\nimport pandas as pd\nfrom bottom.prophet_baseline import forecast_bottom, _p_bottom_14d, HORIZON_DAYS\n\n\ndef _series(days: int, base: int = 100_000) -&gt; pd.DataFrame:\n    ds = pd.date_range(\"2025-01-01\", periods=days, freq=\"D\")\n    y = base + (np.sin(np.arange(days) / 7.0) * 5_000)  # nhịp nhẹ quanh base\n    return pd.DataFrame({\"ds\": ds, \"y\": y, \"flash_sale\": 0})\n\n\ndef test_prophet_forecast_shape():\n    out = forecast_bottom(_series(180))  # MATURE -&gt; fit Prophet\n    assert len(out) == HORIZON_DAYS\n    assert list(out[\"horizon_day\"]) == list(range(1, 15))\n    assert (out[\"model_kind\"] == \"prophet\").all()\n    assert out[\"yhat\"].dtype == np.int64\n\n\ndef test_cold_start_uses_prior():\n    out = forecast_bottom(_series(10), prior_median=88_000)  # cold-start\n    assert (out[\"model_kind\"] == \"category_prior\").all()\n    assert (out[\"yhat\"] == 88_000).all()\n    assert (out[\"p_bottom_14d\"] == 0.0).all()  # không hứa đáy khi thiếu dữ liệu\n\n\ndef test_p_bottom_monotonic_with_interval():\n    trailing_min = 80_000\n    narrow = _p_bottom_14d(np.full(14, 79_000.0), trailing_min)  # cận dưới sát đáy\n    wide = _p_bottom_14d(np.full(14, 70_000.0), trailing_min)    # cận dưới phá đáy sâu hơn\n    assert 0.0 &lt;= narrow &lt;= wide &lt;= 1.0  # khoảng nới xuống thấp -&gt; xác suất không giảm</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>regressors.py</code> (3 hàm lịch thuần) -&gt; <code>features.py</code> (<code>build_regressor_frame</code>) -&gt; <code>prophet_baseline.py</code> (<code>build_baseline</code> + <code>forecast_bottom</code> + <code>_p_bottom_14d</code> + nhánh cold-start) -&gt; migration <code>0001_price_forecast.sql</code> -&gt; tests. <code>forecast_bottom</code> nhận chuỗi <code>price_daily</code> đã nạp sẵn (ds/y/flash_sale) từ deal-svc hoặc job; tầng gọi quyết định MATURE hay cold-start qua cổng TASK-DEAL-002 (<code>IsFeatureReady</code>) rồi truyền <code>prior_median</code> khi cần. Batch đêm: với mỗi SKU, nạp <code>price_daily</code>, gọi <code>forecast_bottom</code>, ghi 14 dòng vào <code>price_forecast</code> (UPSERT theo PK <code>(product_id, run_date, horizon_day)</code>). TASK-DEAL-006 chỉ đọc <code>price_forecast</code> mới nhất. Gói <code>regressors</code>/<code>features</code> là hàm thuần trên ngày/pandas - test nhanh, không cần Prophet; chỉ <code>test_prophet_forecast_shape</code> mới chạm Prophet.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-PRICE-002</strong> - <code>price_daily</code> (continuous aggregate: <code>day</code>, <code>close_p</code>) là chuỗi đầu vào (ds/y). <code>yhat</code> ghi BIGINT VND đồng bộ DEC-PRICE-05.</li><li><strong>TASK-DEAL-002</strong> - cổng <code>IsFeatureReady</code> quyết định MATURE vs cold-start; <code>PriorFor(category_id)</code> cấp <code>prior_median</code> cho nhánh cold-start.</li><li><strong>TASK-DEAL-005 (downstream, blocks)</strong> - LightGBM nâng cấp cho SKU <code>&gt;= 180</code> ngày; ghi cùng bảng <code>price_forecast</code> với <code>model_kind = 'lgbm'</code> (đã có trong CHECK). TASK-DEAL-005 gọi <code>expected_min_14d</code> chính là cột <code>yhat</code> và <code>as_of_date</code> chính là <code>run_date</code> của bảng này; <code>horizon_days</code> của TASK-DEAL-005 ứng với cột <code>horizon_day</code> (1..14) ở đây - không phải cột riêng.</li><li><strong>TASK-DEAL-006 (downstream, blocks)</strong> - batch chấm điểm đêm đọc <code>price_forecast</code>, lọc dòng tươi qua cột <code>scored_at</code> (<code>scored_at &gt;= now() - INTERVAL '36 hours'</code>) rồi bắn cảnh báo khi <code>p_bottom_14d &gt; 0,7</code>. Bảng này là hợp đồng đọc-chỉ của TASK-DEAL-006.</li><li>Thư viện: Prophet, pandas, numpy (ml-svc, Python 3.12); driver Postgres để ghi <code>price_forecast</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Một dòng price_forecast (yhat tụt dần về 12.12, p_bottom ~0,78)</h3>\n<pre><code class=\"language-json\">{\n  \"product_id\": 90112,\n  \"run_date\": \"2026-12-01\",\n  \"horizon_day\": 11,\n  \"yhat\": 92000,\n  \"yhat_lower\": 78000,\n  \"yhat_upper\": 106000,\n  \"p_bottom_14d\": 0.78,\n  \"model_kind\": \"prophet\"\n}</code></pre>\n<p>Chạy ngày 1.12, horizon_day 11 rơi vào 12.12 (double-date): <code>yhat</code> hạ về <code>92000</code> (thấp hơn mặt bằng), <code>yhat_lower = 78000</code> xuống dưới đáy 90 ngày gần đây nên <code>p_bottom_14d = 0,78</code> (vượt ngưỡng 0,7 của TASK-DEAL-006 -&gt; khuyên chờ tới 12.12).</p>\n<h3>Truy vấn alert đọc forecast mới nhất (TASK-DEAL-006)</h3>\n<pre><code class=\"language-sql\">SELECT product_id, p_bottom_14d, model_kind\nFROM price_forecast\nWHERE product_id = 90112\n  AND run_date = (SELECT max(run_date) FROM price_forecast WHERE product_id = 90112)\n  AND horizon_day = 1;   -- p_bottom_14d lặp trên 14 dòng, đọc 1 dòng là đủ</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Đưa <code>days_to_next_double_date</code> thành regressor liên tục (ngoài cờ nhị phân) - thử ở TASK-DEAL-005 nơi LightGBM nhận đặc trưng số.</li><li>Hiệu chỉnh <code>p_bottom_14d</code> thành xác suất có hiệu chuẩn (calibrated) bằng backtest - tách sang TASK-DEAL-005/006 khi có nhãn đáy thực.</li><li>Hệ số nới khoảng tin cậy cold-start (0,12) có nên co giãn theo độ phân tán giá của category - tinh chỉnh khi có dữ liệu thực.</li><li>Lịch nghỉ lễ VN (Tết) như một regressor riêng ngoài double-date/payday - cân nhắc thêm <code>holidays</code> của Prophet ở slice sau.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Thiếu lịch sử (SKU chưa MATURE)</td><td>cổng IsFeatureReady (TASK-DEAL-002)</td><td>Prophet fit chuỗi mỏng -&gt; nhiễu</td><td>Nhánh cold-start trả prior phẳng (DEC-DEAL-32)</td></tr><tr><td>Prophet overfit chuỗi thưa/ngắn</td><td>tuổi lịch sử + test cold-start</td><td>dự báo và khoảng tin cậy vô nghĩa</td><td>Chỉ fit khi MATURE; dưới ngưỡng dùng prior</td></tr><tr><td>Rò rỉ regressor double-date (lệch ngày tương lai)</td><td>test is_double_date theo ds</td><td>mô hình thấy sai ngày sự kiện</td><td>Dựng frame future từ ds, map cờ theo ngày thực</td></tr><tr><td>flash_sale tương lai chưa biết bị đoán bừa</td><td>review features</td><td>dự báo dựa cờ giả định</td><td>Đặt flash_sale=0 cho ngày tương lai (§1 #2)</td></tr><tr><td>Seed/uncertainty không cố định</td><td>test tất định</td><td>yhat/p_bottom đổi giữa các lần chạy</td><td>mcmc_samples=0, uncertainty_samples cố định, seed numpy</td></tr><tr><td>Đọc nhầm raw price_snapshot</td><td>review nguồn nạp</td><td>sai độ phân giải, đốt I/O</td><td>Bắt buộc đọc price_daily (DEC-DEAL-31)</td></tr><tr><td>p_bottom vượt [0,1]</td><td>CHECK price_forecast + kẹp</td><td>alert sai ngưỡng</td><td>Kẹp trong _p_bottom_14d và CHECK constraint</td></tr><tr><td>Ghi forecast cũ đè/thiếu run_date</td><td>PK (product_id, run_date, horizon_day)</td><td>alert đọc dữ liệu lỗi thời</td><td>UPSERT theo PK; alert đọc max(run_date)</td></tr><tr><td>category NULL/prior mỏng ở cold-start</td><td>PriorFor trả ok=false (TASK-DEAL-002)</td><td>không có prior để dựa</td><td>Bỏ qua SKU lần này, chờ đủ lịch sử</td></tr><tr><td>yhat lưu dạng float thập phân</td><td>review ghi DB</td><td>sai số tiền tệ</td><td>Làm tròn về BIGINT VND (DEC-PRICE-05)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Đây là BASELINE Prophet: mục tiêu là một mốc trung thực, có khoảng tin cậy, dễ giải thích để TASK-DEAL-005 (LightGBM) phải vượt qua, không phải mô hình cuối.</li><li>Regressor double-date/payday là phần \"đặc thù VN\" của baseline: đáy giá thật dồn vào 1.1, 2.2, ... 12.12 và quanh kỳ lương, nên mô hình phải nhìn thấy chúng.</li><li>Nhánh cold-start mượn cùng <code>category_prior</code> của TASK-DEAL-002 - một nguồn prior duy nhất phục vụ cả fallback verdict lẫn dự báo đáy, tránh hai định nghĩa lệch nhau.</li><li>Tách huấn luyện (ghi <code>price_forecast</code>) khỏi phục vụ (đọc bảng) là điều kiện để scale tới hàng triệu SKU: batch đêm nặng, alert nhẹ.</li><li><code>p_bottom_14d</code> cố ý suy từ khoảng tin cậy <code>yhat_lower</code> thay vì điểm <code>yhat</code>: người dùng cần biết \"có khả năng còn giảm\" hơn là một con số giá chính xác.</li><li>Tính tất định (seed + uncertainty cố định) là yêu cầu để test lặp lại và để hai lần chạy cùng đầu vào không cho hai lời khuyên khác nhau.</li></ul>\n<hr>\n<p><em>Hết TASK-DEAL-004. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
