window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AFFIL-005"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service AFFIL <strong>SHOULD</strong> chia lại một phần hoa hồng affiliate cho người dùng dưới dạng cashback, theo mô hình hold-then-release để chống gian lận. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> chỉ tạo cashback khi <code>affiliate_conversion.status = 'confirmed'</code> (DEC-AFFIL-50). Cashback KHÔNG được tính trên click hay trên conversion còn <code>pending</code>.</li><li><strong>MUST</strong> định nghĩa bảng <code>cashback_entry (id, user_id, conversion_id, commission, user_share, kept_margin, status, created_at, available_at, paid_at)</code>. <code>status</code> thuộc <code>{pending, available, paid, clawed_back}</code>.</li><li><strong>MUST</strong> tính <code>user_share = floor(commission * share_rate)</code> và <code>kept_margin = commission - user_share</code> (DEC-AFFIL-52). <code>share_rate</code> cấu hình theo tier (ví dụ free 30%, Premium 50%). Tất cả đơn vị VND, BIGINT.</li><li><strong>MUST</strong> giữ <code>cashback_entry</code> ở <code>pending</code> ít nhất qua cửa sổ điều tra (<code>available_at = confirmed_at + investigation_window</code>, mặc định 7-14 ngày) (DEC-AFFIL-51). Chỉ sang <code>available</code> khi cửa sổ đã qua VÀ TASK-TRUST-005 không gắn cờ gian lận trên conversion/user.</li><li><strong>MUST</strong> hỗ trợ clawback: nếu network thu hồi conversion (status đổi từ <code>confirmed</code> về <code>rejected</code>) hoặc TASK-TRUST-005 xác nhận gian lận, cashback_entry sang <code>clawed_back</code>; nếu đã <code>available</code> nhưng chưa <code>paid</code> thì trừ khỏi số dư.</li><li><strong>MUST</strong> gom các <code>available</code> entry của một user thành <code>payout_request</code> chỉ khi tổng đạt ngưỡng tối thiểu (ví dụ 50.000 VND) (DEC-AFFIL-53); payout qua VietQR/số dư nội bộ theo TASK-BILL-002. KHÔNG tự động payout từng giao dịch nhỏ.</li><li><strong>MUST</strong> làm việc tạo entry idempotent theo <code>conversion_id</code> (một conversion confirmed chỉ sinh tối đa một cashback_entry).</li><li><strong>SHOULD</strong> phát metric: <code>cashback_pending_total</code>, <code>cashback_released_total</code>, <code>cashback_clawback_total</code>, <code>cashback_paid_total</code> (VND counter).</li><li><strong>MUST</strong> ghi disclosure rõ cho user: cashback là pending đến ngày <code>available_at</code>, có thể bị hủy nếu đơn bị hủy/hoàn (minh bạch hậu-Honey, §4.2).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao chỉ tính trên confirmed (DEC-AFFIL-50)?</strong> Affiliate là last-click: hoa hồng chỉ có thật khi network xác nhận đơn hợp lệ (qua cửa sổ đối soát). Trả cashback trên click hoặc pending là trả tiền chưa tồn tại - kẻ gian lận tạo đơn rồi hủy sẽ rút sạch.</p>\n<p><strong>Vì sao hold-then-release (DEC-AFFIL-51)?</strong> Đây là lằn ranh chống farm. Đơn affiliate có thể bị hủy/hoàn sau khi confirm. Giữ cashback pending qua cửa sổ điều tra cho phép clawback trước khi tiền rời hệ thống. Best practice ngành là delay payout để điều tra (§5.3).</p>\n<p><strong>Vì sao SănDeal giữ kept_margin (DEC-AFFIL-52)?</strong> Cashback layering chia lại một phần, giữ phần chênh làm doanh thu. Đây là mô hình kiếm tiền của tính năng (§6 mục 2): user-love cao, margin từ phần giữ lại.</p>\n<p><strong>Vì sao ngưỡng payout tối thiểu (DEC-AFFIL-53)?</strong> Payout từng giao dịch nhỏ qua VietQR tăng phí cố định và tạo bề mặt gian lận. Gom đến ngưỡng giảm phí và cho phép một lần điều tra cuối trước khi trả.</p>\n<p><strong>Vì sao buộc qua TASK-TRUST-005 (§1 #4)?</strong> TASK-TRUST-005 phát hiện gaming attribution + delay payout. Cashback release phải hỏi tín hiệu gian lận trước khi mở khóa, nếu không cashback thành kênh rút tiền cho fake-account farming (§5.3).</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/affil/migrations/0005_cashback_ledger.sql\nCREATE TABLE cashback_entry (\n  id            BIGSERIAL PRIMARY KEY,\n  user_id       BIGINT NOT NULL REFERENCES app_user(id),\n  conversion_id BIGINT NOT NULL REFERENCES affiliate_conversion(id),\n  commission    BIGINT NOT NULL CHECK (commission &gt;= 0),   -- VND\n  user_share    BIGINT NOT NULL CHECK (user_share &gt;= 0),\n  kept_margin   BIGINT NOT NULL CHECK (kept_margin &gt;= 0),\n  status        TEXT   NOT NULL CHECK (status IN ('pending','available','paid','clawed_back')),\n  created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),\n  available_at  TIMESTAMPTZ NOT NULL,\n  paid_at       TIMESTAMPTZ,\n  UNIQUE (conversion_id)                                   -- idempotent theo conversion\n);\nCREATE INDEX idx_cashback_release ON cashback_entry (status, available_at)\n  WHERE status = 'pending';\n\nCREATE TABLE payout_request (\n  id          BIGSERIAL PRIMARY KEY,\n  user_id     BIGINT NOT NULL REFERENCES app_user(id),\n  amount      BIGINT NOT NULL CHECK (amount &gt; 0),\n  status      TEXT NOT NULL CHECK (status IN ('queued','sent','failed')),\n  gateway_ref TEXT,\n  created_at  TIMESTAMPTZ NOT NULL DEFAULT now()\n);</code></pre>\n<h3>Tạo entry khi conversion confirmed (Go)</h3>\n<pre><code class=\"language-go\">// services/affil/internal/cashback/ledger.go\nfunc (l *Ledger) OnConversionConfirmed(ctx context.Context, c Conversion) error {\n    rate := l.shareRate(c.UserTier)                 // phan tram nguyen: free 30, premium 50\n    userShare := c.Commission * rate / 100          // BIGINT, chia nguyen (floor); KHONG float\n    kept := c.Commission - userShare\n    availableAt := c.ConfirmedAt.Add(l.investigationWindow) // 7-14 ngay\n    _, err := l.pool.Exec(ctx,\n        `INSERT INTO cashback_entry\n           (user_id, conversion_id, commission, user_share, kept_margin, status, available_at)\n         VALUES ($1,$2,$3,$4,$5,'pending',$6)\n         ON CONFLICT (conversion_id) DO NOTHING`,      // §1 #7 idempotent\n        c.UserID, c.ID, c.Commission, userShare, kept, availableAt)\n    return err\n}</code></pre>\n<h3>Release (pending -&gt; available)</h3>\n<pre><code class=\"language-go\">// services/affil/internal/cashback/release.go\nfunc (r *Releaser) ReleaseDue(ctx context.Context, now time.Time) (int, error) {\n    rows, _ := r.due(ctx, now) // status=pending AND available_at &lt;= now\n    n := 0\n    for _, e := range rows {\n        if r.fraud.IsFlagged(ctx, e.UserID, e.ConversionID) { // TASK-TRUST-005\n            continue // giu pending / cho dieu tra; co the sang clawed_back\n        }\n        r.setStatus(ctx, e.ID, \"available\")\n        n++\n    }\n    return n, nil\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Conversion confirmed -&gt; tạo 1 <code>cashback_entry</code> status <code>pending</code>, <code>available_at = confirmed_at + window</code>.</li><li>Conversion còn <code>pending</code> -&gt; KHÔNG tạo cashback_entry.</li><li><code>user_share + kept_margin == commission</code> (không sai đồng nào).</li><li>Tạo entry 2 lần cho cùng conversion -&gt; chỉ 1 row (UNIQUE conversion_id, ON CONFLICT DO NOTHING).</li><li>Trước <code>available_at</code> -&gt; entry vẫn <code>pending</code>, không vào payout.</li><li>Sau <code>available_at</code> + user không bị flag -&gt; sang <code>available</code>.</li><li>Sau <code>available_at</code> nhưng TASK-TRUST-005 flag -&gt; KHÔNG sang <code>available</code> (giữ/hủy để điều tra).</li><li>Conversion bị network thu hồi (confirmed -&gt; rejected) -&gt; entry sang <code>clawed_back</code>; nếu chưa paid, trừ khỏi số dư.</li><li>Tổng <code>available</code> của user &lt; ngưỡng -&gt; KHÔNG tạo payout_request.</li><li>Tổng <code>available</code> &gt;= ngưỡng -&gt; gom thành 1 payout_request qua VietQR.</li><li>Commission &lt; 0 hoặc user_share &lt; 0 -&gt; lỗi CHECK constraint.</li><li>Endpoint <code>GET /v1/cashback/summary</code> trả disclosure rõ cho user (§1 #9): nêu cashback <code>pending</code> đến ngày <code>available_at</code> và có thể bị hủy nếu đơn bị hủy/hoàn (kiểm field <code>note</code> + <code>next_available_at</code> trong response).</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">func TestCashback_ConfirmedCreatesPending(t *testing.T) {\n    l, uid, cid := setup(t)\n    err := l.OnConversionConfirmed(ctx, Conversion{ID: cid, UserID: uid, Commission: 100_000, UserTier: \"premium\", ConfirmedAt: t0})\n    require.NoError(t, err)\n    e := getEntry(t, l, cid)\n    require.Equal(t, \"pending\", e.Status)\n    require.Equal(t, int64(50_000), e.UserShare)   // 50% premium\n    require.Equal(t, int64(50_000), e.KeptMargin)\n    require.Equal(t, e.Commission, e.UserShare+e.KeptMargin)\n}\n\nfunc TestCashback_Idempotent(t *testing.T) {\n    l, uid, cid := setup(t)\n    c := Conversion{ID: cid, UserID: uid, Commission: 100_000, UserTier: \"free\", ConfirmedAt: t0}\n    l.OnConversionConfirmed(ctx, c)\n    l.OnConversionConfirmed(ctx, c)\n    require.Equal(t, 1, countEntries(t, l, cid))\n}\n\nfunc TestRelease_HoldsBeforeWindow(t *testing.T) {\n    l, uid, cid := setup(t)\n    l.OnConversionConfirmed(ctx, Conversion{ID: cid, UserID: uid, Commission: 100_000, UserTier: \"free\", ConfirmedAt: t0})\n    n, _ := NewReleaser(l).ReleaseDue(ctx, t0.Add(24*time.Hour)) // chua qua window\n    require.Equal(t, 0, n)\n    require.Equal(t, \"pending\", getEntry(t, l, cid).Status)\n}\n\nfunc TestRelease_FlaggedUserNotReleased(t *testing.T) {\n    l, uid, cid := setupFlagged(t, uid) // TASK-TRUST-005 gan co\n    l.OnConversionConfirmed(ctx, Conversion{ID: cid, UserID: uid, Commission: 100_000, UserTier: \"free\", ConfirmedAt: t0})\n    n, _ := NewReleaser(l).ReleaseDue(ctx, t0.Add(30*24*time.Hour))\n    require.Equal(t, 0, n) // giu lai du qua window\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration 0005 -&gt; ledger.go (hook từ conversion.confirmed) -&gt; release.go (cron gọi ReleaseDue mỗi giờ) -&gt; payout.go (gom available &gt;= ngưỡng -&gt; VietQR qua TASK-BILL-002) -&gt; tests. Release chạy như job định kỳ; clawback chạy khi nhận postback rejected từ TASK-AFFIL-003.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-AFFIL-003</strong> - nguồn conversion confirmed/rejected (postback network).</li><li><strong>TASK-BILL-002</strong> - kênh payout VietQR/số dư.</li><li><strong>TASK-TRUST-005</strong> - tín hiệu gian lận + delay payout trước khi release.</li><li><strong>TASK-AFFIL-001</strong> - bảng affiliate_conversion (FK).</li><li>Crates: pgx, cron scheduler.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>User xem cashback</h3>\n<pre><code class=\"language-http\">GET /v1/cashback/summary HTTP/1.1\nAuthorization: Bearer &lt;jwt&gt;\n\n-&gt; 200 OK\n{\n  \"pending\":   { \"count\": 3, \"amount_vnd\": 142000, \"next_available_at\": \"2026-07-11\" },\n  \"available\": { \"count\": 1, \"amount_vnd\": 60000 },\n  \"paid_total_vnd\": 380000,\n  \"payout_threshold_vnd\": 50000,\n  \"note\": \"Cashback pending co the bi huy neu don bi huy hoac hoan.\"\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Tăng share_rate theo streak/loyalty (FR-loyalty tương lai).</li><li>Cashback cross-border khi mở SEA (currency theo nước).</li><li>Cho user rút về MoMo/ZaloPay ngoài VietQR - slice sau.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Trả cashback trước confirm</td><td>test #2</td><td>mất tiền</td><td>Chỉ tạo trên confirmed (DEC-AFFIL-50)</td></tr><tr><td>Đơn hủy sau confirm</td><td>postback rejected</td><td>clawback</td><td>§1 #5 sang clawed_back, trừ số dư</td></tr><tr><td>Farm fake-account rút cashback</td><td>TASK-TRUST-005 flag</td><td>giữ pending</td><td>§1 #4 không release khi flagged</td></tr><tr><td>Tạo trùng entry</td><td>UNIQUE conversion_id</td><td>DO NOTHING</td><td>Theo thiết kế (idempotent)</td></tr><tr><td>Payout giao dịch nhỏ</td><td>ngưỡng tối thiểu</td><td>gộp đến ngưỡng</td><td>DEC-AFFIL-53</td></tr><tr><td>user_share + kept != commission</td><td>property test</td><td>sai số</td><td>floor + trừ, test #3</td></tr><tr><td>commission âm</td><td>DB CHECK</td><td>từ chối</td><td>Sửa nguồn postback</td></tr><tr><td>Release trước window</td><td>idx status+available_at</td><td>giữ pending</td><td>§1 #4</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Cashback là tính năng user-love rất cao nhưng cũng là bề mặt gian lận cao nhất - hold-then-release là lằn ranh.</li><li>Chỉ tính trên confirmed + clawback khi rejected giữ SănDeal không trả tiền không có thật.</li><li>kept_margin là dòng doanh thu của tính năng; share_rate theo tier là đòn bẩy upsell Premium.</li><li>Delay payout + hỏi TASK-TRUST-005 trước release là best practice chống farm (§5.3).</li><li>Disclosure pending/có-thể-hủy giữ minh bạch hậu-Honey (§4.2).</li></ul>\n<hr>\n<p><em>Hết TASK-AFFIL-005. Status: done.</em></p>";
