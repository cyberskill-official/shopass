window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-MOBILE-002"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>App mobile <strong>MUST</strong> cung cấp màn theo dõi giá + wishlist + biểu đồ (đọc từ API backend), nhận push alert mở đúng màn sản phẩm, và universal checkout assistant chạy CHỈ khi người dùng chủ động yêu cầu, gọi optimizer backend để HIỂN THỊ tổ hợp voucher tối ưu. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> đọc danh sách theo dõi/wishlist từ TASK-TRACK-002, lịch sử giá từ TASK-PRICE-003, và quy tắc alert từ TASK-TRACK-003 (DEC-MOBILE-10); mobile là client mỏng. <strong>MUST NOT</strong> tính toán sale ảo hay giá phía client.</li><li><strong>MUST</strong> hiển thị biểu đồ lịch sử giá bằng dữ liệu trả về từ <code>GET /v1/products/{id}/price-history</code> (TASK-PRICE-003); biểu đồ là tầng render, không tính toán giá.</li><li><strong>MUST</strong> nhận push alert qua FCM (TASK-MOBILE-001); chạm vào thông báo mở đúng màn sản phẩm liên quan qua deep-link nội bộ (DEC-MOBILE-14). Alert do engine backend (TASK-TRACK-004) quyết định; mobile chỉ hiển thị.</li><li><strong>MUST</strong> chạy checkout assistant CHỈ khi người dùng chủ động bấm (user-initiated) (DEC-MOBILE-11); <strong>MUST NOT</strong> tự động kích hoạt nền lúc thanh toán.</li><li><strong>MUST</strong> chạy tối ưu voucher/giỏ ở backend (TASK-CART-003) (DEC-MOBILE-12): mobile gửi giỏ đã tối thiểu hóa + danh sách voucher khả dụng, nhận về tổ hợp tối ưu để hiển thị. Mobile <strong>MUST NOT</strong> tự áp voucher hay tự thanh toán.</li><li><strong>MUST</strong> chỉ HIỂN THỊ gợi ý voucher + tổng tiết kiệm rồi để người dùng tự áp mã trong app sàn (DEC-MOBILE-13); <strong>MUST NOT</strong> auto-click/auto-apply.</li><li><strong>MUST</strong> tối thiểu hóa dữ liệu giỏ gửi lên optimizer: chỉ <code>product_id</code>, <code>qty</code>, giá hiển thị (DEC-MOBILE-15); <strong>MUST NOT</strong> gửi cookie/token sàn.</li><li><strong>MUST</strong> xử lý trạng thái rỗng/không đủ dữ liệu nhã nhặn: SKU chưa đủ lịch sử (TASK-DEAL-002 trả UNKNOWN) hiển thị \"chưa đủ dữ liệu\" thay vì kết luận sai; optimizer không tìm được tổ hợp tốt hơn -&gt; nói rõ \"không có voucher áp dụng\".</li><li><strong>MUST</strong> dùng <code>httpClient</code> của TASK-MOBILE-001 cho mọi lời gọi (kế thừa auth + auto-refresh + gateway).</li><li><strong>SHOULD</strong> đo client telemetry tối thiểu (mở checkout assistant, số gợi ý hiển thị) ở mức ẩn danh, tôn trọng consent; <strong>MUST NOT</strong> log nội dung giỏ chi tiết.</li><li><strong>MUST</strong> hiển thị disclosure rõ ràng khi gợi ý có yếu tố affiliate (nhất quán TASK-AFFIL-004), để minh bạch với người dùng.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p>Vì sao mobile là client mỏng, đọc API (DEC-MOBILE-10, §1 #1)? Logic sale ảo (median90/p10/trailing_min) và tối ưu voucher (knapsack stacking) là lõi phức tạp, đã đặc tả ở TASK-DEAL-001 và TASK-CART-003 phía backend. Nếu mobile tự tính lại thì có hai (hoặc ba, cùng web) bản logic dễ lệch nhau - người dùng thấy \"sale xịn\" trên web nhưng \"sale ảo\" trên mobile cho cùng sản phẩm. Một nguồn sự thật ở backend; client chỉ hiển thị.</p>\n<p>Vì sao checkout assistant tuyệt đối user-initiated (DEC-MOBILE-11, §1 #4)? Đây là ranh giới compliance sống còn (§4.2). Honey mang tiếng vì tự động can thiệp nền vào luồng mua. SănDeal dựng cả định vị đạo đức để khác Honey. Checkout assistant chỉ chạy khi người dùng chủ động bấm \"tối ưu voucher\" - không bao giờ tự bật. Vi phạm điều này có thể khiến sàn C&amp;D hoặc store gỡ app.</p>\n<p>Vì sao chỉ hiển thị, người dùng tự áp mã (DEC-MOBILE-13, §1 #6)? Auto-apply/auto-click voucher trong app sàn là hành vi tự động hóa dễ bị coi là abuse và vi phạm policy sàn (tương tự ràng buộc trên web ở TASK-CART-005). Mobile hiển thị \"dùng mã X tiết kiệm Y\" và để người dùng tự nhập trong app sàn - giữ con người trong vòng lặp, tránh tự động hóa rủi ro.</p>\n<p>Vì sao optimizer ở backend, mobile gửi giỏ tối thiểu hóa (DEC-MOBILE-12/15, §1 #5, #7)? Tối ưu voucher cần dữ liệu catalog voucher + luật stacking per-country (TASK-CART-003/004) sống ở backend. Mobile chỉ biết giỏ hiện tại. Gửi giỏ tối thiểu hóa (product_id/qty/giá, KHÔNG cookie/token) lên backend tính rồi nhận kết quả - vừa đúng nơi có dữ liệu, vừa giữ nguyên tắc tối thiểu hóa dữ liệu (không bao giờ gửi token sàn).</p>\n<p>Vì sao xử lý trạng thái rỗng nhã nhặn (§1 #8)? Cold-start nghĩa là nhiều SKU chưa đủ 90 ngày lịch sử (TASK-DEAL-002 trả UNKNOWN). Nếu mobile cố kết luận \"sale xịn/ảo\" trên dữ liệu thiếu thì sai và mất niềm tin. Hiển thị trung thực \"chưa đủ dữ liệu\" tốt hơn một kết luận bịa.</p>\n<hr>\n<h2>§3 - Hợp đồng API / mã</h2>\n<h3>Optimizer client - gửi giỏ tối thiểu hóa (TypeScript)</h3>\n<pre><code class=\"language-ts\">// apps/mobile/src/checkout/optimizerClient.ts\nimport { http } from '../api/httpClient';\n\n// CHỈ gửi dữ liệu tối thiểu hóa: product_id, qty, giá hiển thị. KHÔNG cookie/token sàn.\ntype CartLine = { product_id: number; qty: number; unit_price: number };\ntype OptimizeReq = { platform_id: number; lines: CartLine[]; voucher_codes: string[] };\n\ntype OptimizeResult = {\n  suggested_vouchers: { code: string; saves: number }[];\n  total_saving: number;\n  has_affiliate: boolean; // để hiển thị disclosure\n};\n\n// gọi optimizer backend (TASK-CART-003); chỉ HIỂN THỊ kết quả, KHÔNG tự áp.\nexport async function optimizeCart(req: OptimizeReq): Promise&lt;OptimizeResult&gt; {\n  const res = await http.post('/v1/cart/optimize', req);\n  return res.json();\n}</code></pre>\n<h3>Checkout assistant - user-initiated, chỉ hiển thị (TypeScript, rút gọn)</h3>\n<pre><code class=\"language-tsx\">// apps/mobile/src/checkout/checkoutAssistant.tsx (rút gọn)\nexport function CheckoutAssistant({ cart }: { cart: CartLine[] }) {\n  const [result, setResult] = useState&lt;OptimizeResult | null&gt;(null);\n\n  // CHỈ chạy khi người dùng chủ động bấm - KHÔNG useEffect tự động.\n  async function onOptimizePressed() {\n    const r = await optimizeCart({\n      platform_id: cart.platformId,\n      lines: cart.map(minimize),            // tối thiểu hóa: bỏ mọi field nhạy cảm\n      voucher_codes: cart.availableVouchers,\n    });\n    setResult(r);\n  }\n\n  return (\n    &lt;View&gt;\n      &lt;Button title=\"Tối ưu voucher\" onPress={onOptimizePressed} /&gt;\n      {result &amp;&amp; (\n        &lt;&gt;\n          {result.has_affiliate &amp;&amp; &lt;DisclosureBanner /&gt;}  {/* minh bạch affiliate */}\n          &lt;SavingSummary total={result.total_saving} vouchers={result.suggested_vouchers} /&gt;\n          &lt;Text&gt;Bạn tự áp các mã trên trong app sàn để nhận ưu đãi.&lt;/Text&gt;\n        &lt;/&gt;\n      )}\n    &lt;/View&gt;\n  );\n}</code></pre>\n<h3>Theo dõi giá - client mỏng đọc API (TypeScript)</h3>\n<pre><code class=\"language-ts\">// apps/mobile/src/track/trackClient.ts\nexport async function getPriceHistory(productId: number, range = '90d') {\n  const res = await http.get(`/v1/products/${productId}/price-history?range=${range}`);\n  return res.json(); // backend đã tính daily aggregate - mobile chỉ render\n}\n\nexport async function getWishlist() {\n  const res = await http.get('/v1/wishlist');\n  return res.json();\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Màn theo dõi hiển thị wishlist từ <code>GET /v1/wishlist</code>; không có tính toán giá nào phía client.</li><li>Biểu đồ giá render từ <code>price-history</code>; SKU chưa đủ lịch sử (UNKNOWN) hiển thị \"chưa đủ dữ liệu\" thay vì kết luận.</li><li>Push alert tới -&gt; chạm mở đúng màn sản phẩm qua deep-link nội bộ.</li><li>Checkout assistant KHÔNG tự chạy: không có <code>useEffect</code>/auto-trigger gọi optimizer; chỉ chạy khi bấm nút.</li><li>Bấm \"Tối ưu voucher\" -&gt; <code>POST /v1/cart/optimize</code> với payload chỉ chứa <code>product_id/qty/unit_price</code> + <code>voucher_codes</code>; KHÔNG có trường cookie/token.</li><li>Kết quả optimizer chỉ hiển thị (gợi ý mã + tổng tiết kiệm); KHÔNG có auto-apply/auto-click.</li><li>Optimizer không tìm được tổ hợp tốt hơn -&gt; hiển thị \"không có voucher áp dụng\", không lỗi.</li><li>Kết quả có yếu tố affiliate -&gt; hiển thị disclosure banner.</li><li>Mọi lời gọi đi qua <code>httpClient</code> của TASK-MOBILE-001 (kế thừa auth + auto-refresh).</li><li>Telemetry KHÔNG ghi nội dung giỏ chi tiết (review + test).</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-ts\">// apps/mobile/src/checkout/optimizerClient.test.ts\ntest('payload optimizer chỉ chứa trường tối thiểu hóa, không cookie/token', async () =&gt; {\n  const postSpy = jest.spyOn(http, 'post').mockResolvedValue(jsonRes({ total_saving: 0 }));\n  await optimizeCart({\n    platform_id: 1,\n    lines: [{ product_id: 90112, qty: 2, unit_price: 89000 }],\n    voucher_codes: ['FREESHIP'],\n  });\n  const body = postSpy.mock.calls[0][1] as any;\n  const keys = Object.keys(body.lines[0]);\n  expect(keys.sort()).toEqual(['product_id', 'qty', 'unit_price']);\n  expect(JSON.stringify(body)).not.toContain('cookie');\n  expect(JSON.stringify(body)).not.toContain('token');\n});\n\ntest('optimizer không tìm tổ hợp -&gt; hiển thị không có voucher', async () =&gt; {\n  jest.spyOn(http, 'post').mockResolvedValue(jsonRes({ suggested_vouchers: [], total_saving: 0 }));\n  const r = await optimizeCart(reqWithCart());\n  expect(r.suggested_vouchers).toHaveLength(0);\n});\n\n// apps/mobile/src/checkout/checkoutAssistant.test.tsx (mô tả hành vi)\ntest('checkout assistant KHÔNG tự chạy khi mount', () =&gt; {\n  const postSpy = jest.spyOn(http, 'post');\n  render(&lt;CheckoutAssistant cart={sampleCart} /&gt;);\n  expect(postSpy).not.toHaveBeenCalled(); // chỉ chạy khi bấm\n});\n\ntest('bấm Tối ưu voucher -&gt; gọi optimizer một lần', async () =&gt; {\n  const postSpy = jest.spyOn(http, 'post').mockResolvedValue(jsonRes({ total_saving: 110000 }));\n  const { getByText } = render(&lt;CheckoutAssistant cart={sampleCart} /&gt;);\n  fireEvent.press(getByText('Tối ưu voucher'));\n  await waitFor(() =&gt; expect(postSpy).toHaveBeenCalledTimes(1));\n});\n\ntest('kết quả có affiliate -&gt; hiển thị disclosure', async () =&gt; {\n  jest.spyOn(http, 'post').mockResolvedValue(jsonRes({ total_saving: 50000, has_affiliate: true }));\n  const { getByText, findByTestId } = render(&lt;CheckoutAssistant cart={sampleCart} /&gt;);\n  fireEvent.press(getByText('Tối ưu voucher'));\n  expect(await findByTestId('disclosure-banner')).toBeTruthy();\n});\n\n// apps/mobile/src/track/trackClient.test.ts\ntest('biểu đồ đọc price-history từ API, không tính toán client', async () =&gt; {\n  const getSpy = jest.spyOn(http, 'get').mockResolvedValue(jsonRes({ cells: [] }));\n  await getPriceHistory(90112, '90d');\n  expect(getSpy).toHaveBeenCalledWith('/v1/products/90112/price-history?range=90d');\n});</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: trackClient.ts (đọc wishlist/price-history/alert) -&gt; trackScreen.tsx + priceChart.tsx -&gt; optimizerClient.ts -&gt; checkoutAssistant.tsx -&gt; deep-link handler -&gt; tests. Tất cả dựng trên <code>httpClient</code> của TASK-MOBILE-001. Backend cần endpoint <code>POST /v1/cart/optimize</code> (mặt tiền của TASK-CART-003) nhận giỏ tối thiểu hóa. Deep-link push tái dùng FCM message handler của TASK-MOBILE-001, map <code>data.product_id</code> -&gt; màn chi tiết. Test qua Jest + React Native Testing Library với mock http.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li>TASK-MOBILE-001 - scaffold + <code>httpClient</code> (auth/auto-refresh) + FCM handler; mọi thứ dựng lên trên.</li><li>TASK-CART-003 - optimizer voucher/giỏ ở backend; mobile gửi giỏ tối thiểu hóa, nhận tổ hợp tối ưu.</li><li>TASK-PRICE-003 - API lịch sử giá cho biểu đồ.</li><li>TASK-TRACK-002 / TASK-TRACK-003 - wishlist + alert_rule.</li><li>TASK-TRACK-004 / TASK-DEAL-002 (liên quan) - engine alert + xử lý cold-start UNKNOWN.</li><li>TASK-AFFIL-004 (liên quan) - chuẩn disclosure affiliate.</li><li>Lib: <code>victory-native</code> (biểu đồ), React Navigation.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Gọi optimizer (POST /v1/cart/optimize) - giỏ tối thiểu hóa</h3>\n<pre><code class=\"language-json\">{\n  \"platform_id\": 1,\n  \"lines\": [\n    { \"product_id\": 90112, \"qty\": 2, \"unit_price\": 89000 },\n    { \"product_id\": 90455, \"qty\": 1, \"unit_price\": 250000 }\n  ],\n  \"voucher_codes\": [\"SHOPA30K\", \"PLATFORM50K\", \"FREESHIP\"]\n}</code></pre>\n<h3>Kết quả optimizer (hiển thị, không tự áp)</h3>\n<pre><code class=\"language-json\">{\n  \"suggested_vouchers\": [\n    { \"code\": \"SHOPA30K\", \"saves\": 30000 },\n    { \"code\": \"PLATFORM50K\", \"saves\": 50000 },\n    { \"code\": \"FREESHIP\", \"saves\": 30000 }\n  ],\n  \"total_saving\": 110000,\n  \"has_affiliate\": false\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Thu giỏ tự động từ app sàn trên mobile (nếu có API chia sẻ) - hiện người dùng nhập/chọn sản phẩm thủ công vào assistant; tự động hóa cân nhắc sau, vẫn phải user-initiated.</li><li>So sánh giá chéo sàn ngay trong assistant (TASK-PRICE-004) - thêm khi nhợp UX mobile.</li><li>Widget màn hình chính (home screen widget) hiển thị giá theo dõi - tính năng nền tảng giai đoạn sau.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Checkout assistant tự chạy nền</td><td>test mount không gọi optimizer</td><td>lặp vết Honey, rủi ro C&amp;D/gỡ app</td><td>Chỉ chạy khi bấm (DEC-MOBILE-11)</td></tr><tr><td>Auto-apply voucher trong app sàn</td><td>review + DEC-MOBILE-13</td><td>abuse, vi phạm policy sàn</td><td>Chỉ hiển thị, người dùng tự áp</td></tr><tr><td>Gửi cookie/token sàn lên optimizer</td><td>test payload tối thiểu hóa</td><td>phá tối thiểu hóa dữ liệu</td><td>Chỉ product_id/qty/giá (DEC-MOBILE-15)</td></tr><tr><td>Tính sale ảo phía client</td><td>review + DEC-MOBILE-10</td><td>logic lệch web/backend</td><td>Client mỏng đọc API</td></tr><tr><td>Kết luận trên SKU thiếu lịch sử</td><td>AC #2</td><td>sai + mất niềm tin</td><td>Hiển thị \"chưa đủ dữ liệu\" (UNKNOWN)</td></tr><tr><td>Optimizer rỗng coi là lỗi</td><td>TestOptimizer rỗng</td><td>UX vỡ</td><td>Hiển thị \"không có voucher áp dụng\"</td></tr><tr><td>Thiếu disclosure affiliate</td><td>test disclosure banner</td><td>thiếu minh bạch</td><td>Banner khi has_affiliate (TASK-AFFIL-004)</td></tr><tr><td>Telemetry ghi nội dung giỏ</td><td>review telemetry</td><td>rò dữ liệu cá nhân</td><td>Chỉ đo ẩn danh, không nội dung giỏ</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Ranh giới compliance sống còn: checkout assistant tuyệt đối user-initiated + chỉ-hiển-thị, người dùng tự áp mã trong app sàn (khác Honey).</li><li>Mobile là client mỏng: logic sale ảo + tối ưu voucher ở backend (một nguồn sự thật), mobile chỉ render.</li><li>Tối thiểu hóa dữ liệu giữ nguyên trên mobile: gửi giỏ chỉ product_id/qty/giá, không bao giờ cookie/token sàn.</li><li>Trạng thái rỗng (cold-start UNKNOWN, optimizer không có tổ hợp) hiển thị trung thực, không bịa kết luận.</li><li>Disclosure affiliate hiển thị rõ để minh bạch, nhất quán cam kết hậu-Honey.</li><li>Dựng hoàn toàn trên httpClient của TASK-MOBILE-001 nên kế thừa auth + auto-refresh + gateway sẵn.</li></ul>\n<hr>\n<p><em>Hết TASK-MOBILE-002. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
