window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-BILL-004"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service BILL <strong>MUST</strong> cấp cho mỗi user một <code>referral_code</code> duy nhất, gắn attribution người được giới thiệu lúc đăng ký (bất biến, chặn tự giới thiệu/gắn lại), đếm <code>uses</code> idempotent, và phát sự kiện cho anti-fraud thay vì tự trả thưởng. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> định nghĩa bảng <code>referral_code (id, user_id, code, uses, created_at)</code> với <code>user_id</code> REFERENCES <code>app_user(id)</code>, <code>code TEXT NOT NULL UNIQUE</code>, <code>uses INTEGER NOT NULL DEFAULT 0</code>.</li><li><strong>MUST</strong> đảm bảo mỗi user có đúng một <code>referral_code</code> của riêng mình (DEC-BILL-16): <code>UNIQUE (user_id)</code> trên <code>referral_code</code> (một mã/người).</li><li><strong>MUST</strong> sinh <code>code</code> ngắn, dễ chia sẻ, không trùng, và tránh ký tự gây nhầm (loại <code>O/0/I/1</code> để tránh đọc/gõ sai khi truyền miệng).</li><li><strong>MUST</strong> gắn attribution lúc đăng ký (DEC-BILL-17): khi người mới đăng ký kèm một mã giới thiệu hợp lệ, đặt <code>app_user.referral_code_id</code> (TASK-AUTH-001) trỏ tới <code>referral_code</code> của người giới thiệu. Gắn xong là bất biến.</li><li><strong>MUST</strong> CẤM tự giới thiệu (DEC-BILL-18): nếu mã giới thiệu thuộc về chính user đang đăng ký (cùng <code>user_id</code>) -&gt; từ chối gắn, không tăng <code>uses</code>, trả lỗi xác định <code>ErrSelfReferral</code>.</li><li><strong>MUST</strong> CẤM gắn lại (DEC-BILL-18): user đã có <code>referral_code_id</code> (đã được giới thiệu) -&gt; không đổi sang referrer khác; trả <code>ErrAlreadyAttributed</code>.</li><li><strong>MUST</strong> từ chối mã không tồn tại: mã giới thiệu không khớp <code>referral_code</code> nào -&gt; <code>ErrUnknownCode</code>, không gắn, không tăng <code>uses</code>.</li><li><strong>MUST</strong> tăng <code>uses</code> idempotent theo cặp (referrer, referee) (DEC-BILL-20): một người được giới thiệu chỉ làm tăng <code>uses</code> của referrer đúng một lần (dựa attribution bất biến ở §1 #4/#6).</li><li><strong>MUST NOT</strong> tự trả thưởng referral ngay (DEC-BILL-19): thay vào đó phát một sự kiện <code>referral.attributed {referrer_id, referee_id, at}</code> cho TASK-TRUST-004 (anti-fraud) + cơ chế delay reward; thưởng chỉ được cấp sau khi vượt cửa kiểm tra (velocity, đồ thị quan hệ, §5.3).</li><li><strong>MUST</strong> expose hàm:</li></ol>\n<ul><li><code>CreateCodeForUser(ctx, userID int64) (string, error)</code> - tạo mã duy nhất cho user (idempotent: gọi lại trả mã đã có).</li><li><code>Attribute(ctx, refereeID int64, code string) error</code> - gắn referrer lúc đăng ký, áp mọi ràng buộc §1 #4-#9.</li><li><code>FindByCode(ctx, code string) (ReferralCode, bool, error)</code>.</li></ul>\n<ol><li><strong>MUST</strong> coi attribution là một phần của luồng đăng ký (TASK-AUTH-001) nhưng KHÔNG chặn đăng ký nếu mã lỗi: mã giới thiệu sai/tự giới thiệu -&gt; user vẫn đăng ký được, chỉ không gắn attribution (đăng ký không phụ thuộc tính hợp lệ của mã giới thiệu).</li><li><strong>SHOULD</strong> phát OTel + sự kiện: <code>referral_attributed_total</code> (counter), <code>referral_self_blocked_total</code> (counter), <code>referral_unknown_code_total</code> (counter); sự kiện <code>referral.attributed</code> lên bus cho TASK-TRUST-004.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao một mã mỗi user + tránh ký tự gây nhầm (DEC-BILL-16, §1 #3)?</strong> Mã giới thiệu được chia sẻ qua tin nhắn, đọc miệng, in trên ảnh. <code>O</code> và <code>0</code>, <code>I</code> và <code>1</code> dễ đọc/gõ sai, làm mã hỏng và người giới thiệu mất công. Một mã ngắn không chứa ký tự dễ nhầm tăng tỷ lệ dùng thành công. Một mã mỗi user giữ attribution rõ ràng (mỗi mã trỏ đúng một người giới thiệu).</p>\n<p><strong>Vì sao attribution bất biến (DEC-BILL-17, §1 #6)?</strong> Người giới thiệu một user là một sự thật xảy ra một lần (lúc đăng ký). Cho gắn lại sau đó mở cửa thao túng: user đổi referrer để chuyển thưởng, hoặc bị lừa gắn lại. Bất biến sau khi gắn giữ attribution trung thực và là cơ sở để đếm <code>uses</code> không trùng.</p>\n<p><strong>Vì sao cấm tự giới thiệu (DEC-BILL-18)?</strong> Đây là cách farming đơn giản nhất: tạo mã của mình rồi tự dùng để nhận thưởng. So <code>user_id</code> của chủ mã với người đăng ký và từ chối khi trùng đóng lỗ hổng này ngay tại nguồn - rẻ hơn nhiều việc phát hiện sau qua anti-fraud.</p>\n<p><strong>Vì sao không tự trả thưởng ngay (DEC-BILL-19)?</strong> Tài liệu nguồn (§5.3) nêu rõ delay payout là best practice ngành chống farming. Trả thưởng tức thì cho phép kẻ tạo hàng loạt tài khoản giả rút thưởng trước khi bị phát hiện. Phát một sự kiện cho anti-fraud (TASK-TRUST-004) và chỉ cấp thưởng sau khi qua velocity/đồ thị quan hệ chuyển rủi ro từ \"trả rồi mới phát hiện\" sang \"kiểm rồi mới trả\".</p>\n<p><strong>Vì sao uses idempotent theo cặp (DEC-BILL-20)?</strong> Nếu một người được giới thiệu làm tăng <code>uses</code> nhiều lần (đăng ký lại, race), thống kê tăng trưởng phình và thưởng nhân đôi. Dựa attribution bất biến (mỗi referee gắn đúng một referrer một lần), việc tăng <code>uses</code> cũng chỉ xảy ra một lần cho cặp đó.</p>\n<p><strong>Vì sao mã lỗi không chặn đăng ký (§1 #11)?</strong> Đăng ký là hành động cốt lõi; nó không nên thất bại vì một mã giới thiệu gõ sai. Tách bạch: đăng ký luôn thành công nếu thông tin tài khoản hợp lệ (TASK-AUTH-001); attribution là phần thêm, mã lỗi chỉ làm bỏ qua gắn, không cản người dùng vào hệ thống.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/bill/migrations/0004_referral_code.sql\nCREATE TABLE referral_code (\n  id         BIGSERIAL   PRIMARY KEY,\n  user_id    BIGINT      NOT NULL UNIQUE REFERENCES app_user(id), -- một mã/người (§1 #2)\n  code       TEXT        NOT NULL UNIQUE,                          -- §1 #1\n  uses       INTEGER     NOT NULL DEFAULT 0,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now()\n);\nCREATE INDEX idx_referral_code_lookup ON referral_code (code);\n-- app_user.referral_code_id (TASK-AUTH-001) trỏ tới referral_code(id) của người giới thiệu;\n-- FK đó được thêm khi cả hai bảng tồn tại.</code></pre>\n<h3>Sinh mã (Go)</h3>\n<pre><code class=\"language-go\">// services/bill/internal/referral/code.go\n// alphabet loại ký tự gây nhầm (O/0/I/1) để truyền miệng/gõ không sai (§1 #3).\nconst alphabet = \"ABCDEFGHJKLMNPQRSTUVWXYZ23456789\"\n\nfunc NewCode(n int) string {\n    b := make([]byte, n)\n    for i := range b {\n        b[i] = alphabet[randIndex(len(alphabet))]\n    }\n    return \"SD\" + string(b) // tiền tố thương hiệu: ví dụ \"SD7KQ2M9\"\n}</code></pre>\n<h3>Attribution (Go)</h3>\n<pre><code class=\"language-go\">// services/bill/internal/referral/attribute.go\n\n// Attribute gắn referrer cho người mới đăng ký, áp mọi ràng buộc (§1 #4-#9).\nfunc (s *Service) Attribute(ctx context.Context, refereeID int64, code string) error {\n    rc, ok, err := s.repo.FindByCode(ctx, code)\n    if err != nil { return err }\n    if !ok { return ErrUnknownCode }                 // §1 #7\n    if rc.UserID == refereeID { return ErrSelfReferral } // §1 #5 - tự giới thiệu\n\n    already, err := s.repo.HasReferrer(ctx, refereeID)\n    if err != nil { return err }\n    if already { return ErrAlreadyAttributed }       // §1 #6 - gắn lại\n\n    if err := s.repo.SetReferrer(ctx, refereeID, rc.ID); err != nil {\n        return err\n    }\n    s.repo.IncrementUses(ctx, rc.ID)                 // idempotent theo cặp (§1 #8)\n    s.bus.Publish(ctx, ReferralAttributed{           // §1 #9 - không tự thưởng\n        ReferrerID: rc.UserID, RefereeID: refereeID, At: time.Now(),\n    })\n    metrics.Attributed()\n    return nil\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch -&gt; <code>referral_code</code> tồn tại với <code>code</code> UNIQUE + <code>user_id</code> UNIQUE.</li><li><code>CreateCodeForUser(u)</code> -&gt; mã duy nhất, bắt đầu <code>SD</code>, không chứa <code>O/0/I/1</code>; gọi lại cho cùng user trả mã đã có (idempotent).</li><li>Hai user khác nhau -&gt; hai mã khác nhau (UNIQUE code).</li><li><code>Attribute(referee, code_of_referrer)</code> hợp lệ -&gt; <code>app_user(referee).referral_code_id</code> trỏ tới mã referrer; <code>uses</code> của referrer tăng 1.</li><li><code>Attribute(user, code_của_chính_user)</code> -&gt; <code>ErrSelfReferral</code>, <code>uses</code> KHÔNG tăng.</li><li><code>Attribute</code> khi referee đã có referrer -&gt; <code>ErrAlreadyAttributed</code>, không đổi.</li><li><code>Attribute</code> với mã không tồn tại -&gt; <code>ErrUnknownCode</code>, không gắn, không tăng <code>uses</code>.</li><li>Gọi <code>Attribute</code> cùng cặp (referrer, referee) hai lần -&gt; <code>uses</code> chỉ tăng một lần (lần hai trả <code>ErrAlreadyAttributed</code>).</li><li><code>Attribute</code> thành công phát sự kiện <code>referral.attributed</code> (kiểm bus mock) - KHÔNG tự cộng thưởng/tiền.</li><li>Đăng ký với mã giới thiệu sai (tự giới thiệu/mã lạ) -&gt; user VẪN đăng ký được (đăng ký không phụ thuộc mã), chỉ không gắn attribution.</li><li><code>FindByCode(code)</code> trả đúng <code>referral_code</code>; mã không tồn tại -&gt; <code>found=false</code>.</li><li>Metric <code>referral_self_blocked_total</code> tăng khi chặn tự giới thiệu; <code>referral_attributed_total</code> tăng khi gắn thành công.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/bill/internal/referral/attribute_test.go\nfunc TestAttribute_Valid(t *testing.T) {\n    s, referrer, referee := setupTwoUsers(t)\n    code, _ := s.repo.CodeOf(ctx, referrer)\n    require.NoError(t, s.Attribute(ctx, referee, code))\n    require.Equal(t, 1, s.repo.UsesOf(ctx, code))\n    require.True(t, s.repo.HasReferrerBool(ctx, referee))\n}\n\nfunc TestAttribute_SelfReferral_Blocked(t *testing.T) {\n    s, u, _ := setupTwoUsers(t)\n    code, _ := s.repo.CodeOf(ctx, u)\n    require.ErrorIs(t, s.Attribute(ctx, u, code), ErrSelfReferral) // dùng mã của chính mình\n    require.Equal(t, 0, s.repo.UsesOf(ctx, code)) // uses không tăng\n}\n\nfunc TestAttribute_AlreadyAttributed_Blocked(t *testing.T) {\n    s, referrer, referee := setupTwoUsers(t)\n    code, _ := s.repo.CodeOf(ctx, referrer)\n    require.NoError(t, s.Attribute(ctx, referee, code))\n    require.ErrorIs(t, s.Attribute(ctx, referee, code), ErrAlreadyAttributed) // gắn lại\n    require.Equal(t, 1, s.repo.UsesOf(ctx, code)) // không tăng lần hai\n}\n\nfunc TestAttribute_UnknownCode(t *testing.T) {\n    s, _, referee := setupTwoUsers(t)\n    require.ErrorIs(t, s.Attribute(ctx, referee, \"SDNOPE99\"), ErrUnknownCode)\n}\n\nfunc TestAttribute_PublishesEvent_NoDirectReward(t *testing.T) {\n    s, referrer, referee := setupTwoUsers(t)\n    code, _ := s.repo.CodeOf(ctx, referrer)\n    s.Attribute(ctx, referee, code)\n    require.Equal(t, 1, s.bus.CountOf(\"referral.attributed\")) // phát sự kiện\n    require.Equal(t, 0, s.rewardsGranted())                   // KHÔNG tự thưởng (§1 #9)\n}</code></pre>\n<pre><code class=\"language-go\">// services/bill/internal/referral/code_test.go\nfunc TestNewCode_NoConfusingChars(t *testing.T) {\n    seen := map[string]bool{}\n    for i := 0; i &lt; 1000; i++ {\n        c := NewCode(6)\n        require.False(t, seen[c]); seen[c] = true\n        require.NotContains(t, c[2:], \"O\")\n        require.NotContains(t, c[2:], \"0\")\n        require.NotContains(t, c[2:], \"I\")\n        require.NotContains(t, c[2:], \"1\")\n    }\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration <code>0004_referral_code.sql</code> (code UNIQUE + user_id UNIQUE) -&gt; <code>code.go</code> (sinh mã từ alphabet loại ký tự nhầm) -&gt; <code>repo.go</code> (<code>CreateCodeForUser</code>, <code>FindByCode</code>, <code>IncrementUses</code>, <code>SetReferrer</code>) -&gt; <code>attribute.go</code> (gắn referrer + áp ràng buộc + phát sự kiện) -&gt; tests. <code>Attribute</code> được gọi từ luồng đăng ký TASK-AUTH-001 khi có mã giới thiệu, nhưng lỗi của nó không lăn ngược đăng ký (§1 #11). Sự kiện <code>referral.attributed</code> lên bus (cùng hạ tầng fan-out TASK-NOTIF-003 hoặc bus nội bộ) cho TASK-TRUST-004 tiêu thụ. FK <code>app_user.referral_code_id -&gt; referral_code(id)</code> thêm khi cả hai bảng tồn tại.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-BILL-001</strong> - module BILL + migration nền; referral sống cùng bill-svc.</li><li><strong>TASK-AUTH-001</strong> - <code>app_user.referral_code_id</code> (cột để gắn attribution); luồng đăng ký gọi <code>Attribute</code>.</li><li><strong>TASK-INFRA-002</strong> - <code>app_user</code> + quy ước migration.</li><li><strong>TASK-TRUST-004 (downstream)</strong> - tiêu thụ sự kiện <code>referral.attributed</code>, áp velocity/đồ thị quan hệ, quyết định trả thưởng (delay payout).</li><li><strong>TASK-MOBILE-003 (downstream, P3)</strong> - share-on-sale + referral dùng mã này cho virality.</li><li>Lib: <code>pgx</code>, <code>crypto/rand</code> (sinh mã).</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Đăng ký kèm mã giới thiệu (luồng TASK-AUTH-001 gọi Attribute)</h3>\n<pre><code class=\"language-go\">userID, _ := authSvc.Register(ctx, in) // đăng ký thành công trước\nif in.ReferralCode != \"\" {\n    if err := referral.Attribute(ctx, userID, in.ReferralCode); err != nil {\n        log.Info(\"referral not attributed\", \"err\", err) // không lăn ngược đăng ký (§1 #11)\n    }\n}</code></pre>\n<h3>Sự kiện phát cho anti-fraud (không tự thưởng)</h3>\n<pre><code class=\"language-json\">{\n  \"type\": \"referral.attributed\",\n  \"referrer_id\": 7,\n  \"referee_id\": 4096,\n  \"at\": \"2026-06-28T10:15:00+07:00\"\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Cơ chế thưởng cụ thể (giảm giá Premium, ngày dùng thử) khi referral qua cửa anti-fraud - định ở TASK-TRUST-004 + TASK-BILL-005; task này chỉ attribution + sự kiện.</li><li>Mã giới thiệu nhiều tầng (referee giới thiệu tiếp được chia) - rủi ro pyramid; cân nhắc kỹ, hoãn.</li><li>Hết hạn mã / mã chiến dịch riêng - thêm cột <code>expires_at</code>/<code>campaign</code> khi cần; schema tối thiểu hiện tại đủ.</li><li>Giới hạn số referral/người/ngày - đặt ở TASK-TRUST-004 (velocity), không ở schema.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Tự giới thiệu farm thưởng</td><td>self-referral test</td><td>farming thưởng</td><td>So user_id chủ mã vs referee (§1 #5)</td></tr><tr><td>Gắn lại đổi referrer</td><td>already-attributed test</td><td>thao túng attribution</td><td>Bất biến sau khi gắn (§1 #6)</td></tr><tr><td>Tự trả thưởng ngay</td><td>publish-event test</td><td>fake-account rút thưởng</td><td>Phát sự kiện + delay (DEC-BILL-19)</td></tr><tr><td>uses tăng trùng</td><td>idempotent test</td><td>thống kê sai, thưởng nhân đôi</td><td>Idempotent theo cặp (§1 #8)</td></tr><tr><td>Mã gây nhầm O/0/I/1</td><td>code test</td><td>mã hỏng khi gõ/đọc</td><td>Loại ký tự nhầm (§1 #3)</td></tr><tr><td>Mã lạ</td><td>unknown-code test</td><td>gắn rác</td><td>ErrUnknownCode (§1 #7)</td></tr><tr><td>Mã lỗi chặn đăng ký</td><td>register-not-blocked test</td><td>mất user vì gõ sai mã</td><td>Mã lỗi không lăn ngược đăng ký (§1 #11)</td></tr><tr><td>Hai mã/người</td><td>UNIQUE(user_id)</td><td>attribution mơ hồ</td><td>Một mã/người (§1 #2)</td></tr><tr><td>Race tăng uses</td><td>idempotent + attribution bất biến</td><td>đếm trùng</td><td>Dựa attribution một-lần (§1 #8)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>referral là đòn bẩy viral chi phí thấp (§5.7) nhưng là mục tiêu gian lận hàng đầu (§5.3) - thiết kế phải phòng farming từ đầu.</li><li>Cấm tự giới thiệu + bất biến attribution đóng hai lỗ hổng farming phổ biến nhất ngay tại nguồn.</li><li>Không tự trả thưởng: phát sự kiện cho anti-fraud (TASK-TRUST-004) + delay payout chuyển từ \"trả rồi phát hiện\" sang \"kiểm rồi trả\".</li><li>uses idempotent theo cặp giữ thống kê tăng trưởng đúng và thưởng không nhân đôi.</li><li>Mã loại ký tự gây nhầm (O/0/I/1) tăng tỷ lệ dùng thành công khi truyền miệng/in ảnh.</li><li>Mã lỗi không chặn đăng ký: attribution là phần thêm, không phải điều kiện vào hệ thống.</li><li>Đây là nguồn dữ liệu cho TASK-TRUST-004 (anti-fraud) và virality TASK-MOBILE-003.</li></ul>\n<hr>\n<p><em>Hết TASK-BILL-004. Status: done (mục tiêu audit 10/10).</em></p>";
