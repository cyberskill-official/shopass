window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-BILL-002"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service BILL <strong>MUST</strong> tích hợp bốn cổng thanh toán VN (MoMo, ZaloPay, VNPay, VietQR) qua một interface chung, tạo yêu cầu thanh toán theo cổng user chọn, ưu tiên QR (phí thấp), ký request bằng khóa từ Vault, và idempotent theo <code>order_ref</code>. SănDeal <strong>MUST NOT</strong> lưu thông tin thẻ. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> định nghĩa interface <code>PaymentGateway</code> với <code>CreatePayment(ctx, PaymentRequest) (PaymentResult, error)</code> và một registry chọn adapter theo <code>gateway</code> code - {<code>'momo'</code>,<code>'zalopay'</code>,<code>'vnpay'</code>,<code>'vietqr'</code>} (DEC-BILL-06).</li><li><strong>MUST</strong> phục vụ <code>POST /v1/billing/checkout {plan_tier, gateway}</code>: tra <code>plan_catalog</code> (TASK-BILL-001) lấy <code>price</code> (BIGINT VND), sinh một <code>order_ref</code> idempotency, gọi adapter, trả <code>PaymentResult</code> (pay_url hoặc qr_payload tùy cổng).</li><li><strong>MUST</strong> gửi số tiền cho cổng dạng <code>BIGINT</code> VND (DEC-BILL-08) - KHÔNG float; lấy từ <code>plan_catalog.price</code>, KHÔNG nhận số tiền do client gửi (chống thao túng giá).</li><li><strong>MUST</strong> ký mọi request gửi cổng theo đúng spec ký của cổng đó (HMAC/checksum), với khóa bí mật đọc từ Vault (TASK-INFRA-003) (DEC-BILL-09); khóa KHÔNG nằm trong DB/code cleartext.</li><li>SănDeal <strong>MUST NOT</strong> lưu, log, hay truyền thông tin thẻ (PAN, CVV, ngày hết hạn) ở bất kỳ đâu (DEC-BILL-09): luồng thẻ (nếu có qua VNPay) do cổng xử lý trên trang cổng; SănDeal chỉ giữ <code>order_ref</code> + kết quả.</li><li><strong>MUST</strong> sinh <code>order_ref</code> duy nhất cho mỗi checkout và idempotent theo nó (DEC-BILL-10): gọi <code>CreatePayment</code> lại với cùng <code>order_ref</code> (user bấm lại) <strong>MUST NOT</strong> tạo hai giao dịch ở cổng - trả lại intent đã tạo hoặc từ chối trùng.</li><li><strong>MUST</strong> ưu tiên QR (VietQR/QR động) làm gợi ý mặc định ở tầng gợi ý (DEC-BILL-07) vì phí thấp hơn thẻ (~1,5-2,5%/giao dịch, §4.1); thẻ chỉ khi user chủ động chọn. (Đây là mặc định gợi ý, user vẫn chọn được cổng khác.)</li><li><code>PaymentResult</code> <strong>MUST</strong> chứa: <code>order_ref</code>, <code>gateway</code>, <code>amount</code> (BIGINT VND), và một trong <code>pay_url</code> (MoMo/ZaloPay/VNPay deeplink/redirect) hoặc <code>qr_payload</code> (VietQR chuỗi QR) để client hiển thị.</li><li><strong>MUST</strong> ghi một bản ghi <code>payment</code> trạng thái khởi tạo (<code>pending</code>) gắn <code>order_ref</code> + <code>subscription</code>/user trước khi trả về (để TASK-BILL-003 đối soát khi IPN/webhook về). Tiền <code>amount</code> BIGINT VND.</li><li><strong>MUST</strong> lấy <code>user_id</code> từ JWT (TASK-INFRA-001); plan_tier không tồn tại trong <code>plan_catalog</code> -&gt; <code>400</code>; <code>gateway</code> ngoài tập -&gt; <code>400</code>.</li><li><strong>MUST</strong> đặt timeout + xử lý lỗi gọi cổng: cổng không phản hồi/đỗ -&gt; trả <code>502</code>/<code>503</code> và để <code>payment</code> ở <code>pending</code> (không đánh dấu thành công khi chưa chắc); KHÔNG coi lỗi mạng là thanh toán thành công.</li><li><strong>SHOULD</strong> phát OTel: <code>payment_intent_created_total{gateway}</code> (counter), <code>payment_intent_amount_vnd</code> (histogram), <code>payment_gateway_error_total{gateway}</code> (counter).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao một interface chung + adapter per-cổng (DEC-BILL-06)?</strong> Bốn cổng có API ký và định dạng phản hồi khác nhau (MoMo deeplink, ZaloPay order, VNPay redirect ký, VietQR chuỗi QR). Một interface <code>PaymentGateway</code> chung để phần checkout không phải biết chi tiết từng cổng; mỗi adapter giấu sự khác biệt. Thêm cổng mới là thêm một adapter, không sửa handler.</p>\n<p><strong>Vì sao ưu tiên QR (DEC-BILL-07)?</strong> Phí giao dịch ăn trực tiếp vào biên. QR/VietQR rẻ hơn thẻ (~1,5-2,5% so với thẻ cao hơn, §4.1). Ở thị trường VN với willingness-to-pay thấp và giá Premium nhẹ (29k-79k), mỗi điểm phần trăm phí là đáng kể. Gợi ý QR mặc định hướng người dùng tới phương thức rẻ mà vẫn để họ tự chọn - tối ưu chi phí không ép buộc.</p>\n<p><strong>Vì sao không lưu thông tin thẻ (DEC-BILL-09)?</strong> Lưu PAN/CVV kéo SănDeal vào phạm vi PCI-DSS nặng và biến DB thành mục tiêu giá trị cao. Mô hình đúng cho một startup là không bao giờ chạm thẻ: luồng thẻ xảy ra trên trang cổng (VNPay), SănDeal chỉ giữ <code>order_ref</code> và kết quả. Phạm vi PCI thu về tối thiểu, rủi ro lộ thẻ về gần không.</p>\n<p><strong>Vì sao khóa ký từ Vault (DEC-BILL-09, §1 #4)?</strong> Khóa ký là thứ chứng minh request đến từ SănDeal. Lộ khóa là lộ khả năng giả mạo yêu cầu thanh toán/hoàn tiền. Đọc khóa từ Vault (không để trong DB/code cleartext) tuân no-cleartext của PDPL (§5.5) và thu hẹp bề mặt lộ - đồng nhất cách TASK-AFFIL-003 xử lý postback secret.</p>\n<p><strong>Vì sao idempotent theo order_ref (DEC-BILL-10)?</strong> Người dùng bấm \"thanh toán\" hai lần, hoặc retry khi mạng chậm, là chuyện thường. Không idempotent thì hai giao dịch tạo ra, user bị trừ tiền hai lần - khiếu nại và hoàn tiền tốn kém, mất niềm tin. Một <code>order_ref</code> duy nhất cho mỗi ý định thanh toán, gọi lại cùng ref không tạo giao dịch mới, đóng cửa double-charge.</p>\n<p><strong>Vì sao lỗi mạng không phải thành công (§1 #11)?</strong> Khi gọi cổng đỗ/timeout, ta KHÔNG biết thanh toán có thành công hay không. Coi lỗi là thất bại (giữ <code>pending</code>) an toàn hơn coi là thành công (cấp Premium khi chưa thu tiền). Trạng thái thật được TASK-BILL-003 chốt qua IPN/webhook của cổng - nguồn sự thật về thanh toán là cổng, không phải kết quả lời gọi của ta.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Interface + registry (Go)</h3>\n<pre><code class=\"language-go\">// services/bill/internal/pay/gateway.go\ntype PaymentRequest struct {\n    OrderRef string // idempotency key (§1 #6)\n    Amount   int64  // VND, BIGINT (§1 #3)\n    UserID   int64\n    PlanTier string\n}\ntype PaymentResult struct {\n    OrderRef  string `json:\"order_ref\"`\n    Gateway   string `json:\"gateway\"`\n    Amount    int64  `json:\"amount\"`           // VND\n    PayURL    string `json:\"pay_url,omitempty\"`    // MoMo/ZaloPay/VNPay\n    QRPayload string `json:\"qr_payload,omitempty\"` // VietQR\n}\n\ntype PaymentGateway interface {\n    Code() string\n    CreatePayment(ctx context.Context, r PaymentRequest) (PaymentResult, error)\n}\n\n// Registry chọn adapter theo code (§1 #1).\nfunc (reg *Registry) Get(code string) (PaymentGateway, bool) {\n    g, ok := reg.byCode[code] // momo|zalopay|vnpay|vietqr\n    return g, ok\n}</code></pre>\n<h3>Ký request (Go)</h3>\n<pre><code class=\"language-go\">// services/bill/internal/pay/sign.go\n// SignMoMo ký payload theo spec MoMo (HMAC-SHA256) với khóa từ Vault (§1 #4).\nfunc SignMoMo(ctx context.Context, secrets SecretReader, raw string) (string, error) {\n    key, err := secrets.Get(ctx, \"bill/momo/secret_key\") // Vault (TASK-INFRA-003)\n    if err != nil {\n        return \"\", err\n    }\n    mac := hmac.New(sha256.New, []byte(key))\n    mac.Write([]byte(raw))\n    return hex.EncodeToString(mac.Sum(nil)), nil\n}</code></pre>\n<h3>Checkout handler (Go)</h3>\n<pre><code class=\"language-go\">// services/bill/internal/api/checkout.go\nfunc (h *Handler) HandleCheckout(w http.ResponseWriter, req *http.Request) {\n    userID := auth.UserID(req.Context()) // TASK-INFRA-001\n    var body struct{ PlanTier, Gateway string }\n    if err := json.NewDecoder(req.Body).Decode(&amp;body); err != nil {\n        writeErr(w, 400, \"invalid body\"); return\n    }\n    plan, ok := h.plans.ByTier(req.Context(), body.PlanTier) // TASK-BILL-001\n    if !ok { writeErr(w, 400, \"unknown plan\"); return }\n    gw, ok := h.gateways.Get(body.Gateway)\n    if !ok { writeErr(w, 400, \"unsupported gateway\"); return }\n\n    orderRef := pay.NewOrderRef(userID, plan.Tier) // duy nhất + idempotent (§1 #6)\n    if existing, found := h.payments.ByOrderRef(req.Context(), orderRef); found {\n        _ = json.NewEncoder(w).Encode(existing.Result()); return // không tạo lần hai\n    }\n    res, err := gw.CreatePayment(req.Context(), pay.PaymentRequest{\n        OrderRef: orderRef, Amount: plan.Price, UserID: userID, PlanTier: plan.Tier, // BIGINT VND\n    })\n    if err != nil {\n        metrics.GatewayError(body.Gateway)\n        writeErr(w, 502, \"payment gateway error\"); return // KHÔNG coi là thành công (§1 #11)\n    }\n    h.payments.InsertPending(req.Context(), orderRef, userID, plan.Price, body.Gateway) // §1 #9\n    w.Header().Set(\"Content-Type\", \"application/json; charset=utf-8\")\n    _ = json.NewEncoder(w).Encode(res)\n    metrics.IntentCreated(body.Gateway)\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>POST /v1/billing/checkout {plan_tier:\"premium_basic\", gateway:\"vietqr\"}</code> -&gt; <code>200</code> với <code>PaymentResult</code> chứa <code>qr_payload</code>, <code>amount=29000</code>.</li><li><code>gateway:\"momo\"</code> -&gt; <code>PaymentResult</code> chứa <code>pay_url</code> (deeplink MoMo); <code>amount</code> đúng từ <code>plan_catalog</code>.</li><li><code>amount</code> gửi cổng luôn lấy từ <code>plan_catalog.price</code> (BIGINT), KHÔNG từ body client (gửi kèm <code>amount</code> trong body bị bỏ qua).</li><li><code>plan_tier</code> không có trong <code>plan_catalog</code> -&gt; <code>400</code> \"unknown plan\".</li><li><code>gateway</code> ngoài {momo,zalopay,vnpay,vietqr} -&gt; <code>400</code> \"unsupported gateway\".</li><li>Gọi checkout hai lần cùng <code>order_ref</code> (cùng user+plan trong cửa sổ idempotent) -&gt; chỉ một intent tạo ở cổng; lần hai trả lại intent cũ.</li><li>Ký request: với khóa cố định (test) + payload cố định -&gt; chữ ký khớp vector kỳ vọng của từng cổng.</li><li>Khóa ký đọc từ Vault (mock SecretReader); review xác nhận không có khóa cleartext trong code/DB.</li><li>Không có cột/đường lưu thông tin thẻ (PAN/CVV) trong schema <code>payment</code> (review + grep).</li><li>Cổng trả lỗi/timeout (mock fail) -&gt; <code>502</code>, <code>payment</code> giữ <code>pending</code> (KHÔNG đánh dấu thành công).</li><li>Một bản ghi <code>payment</code> <code>pending</code> được tạo với <code>order_ref</code> + <code>amount</code> BIGINT trước khi trả response.</li><li>Metric <code>payment_intent_created_total{gateway}</code> tăng khi tạo; <code>payment_gateway_error_total</code> tăng khi cổng lỗi.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/bill/internal/pay/gateway_test.go\nfunc TestRegistry_SelectsAdapter(t *testing.T) {\n    reg := newRegistry(t)\n    for _, code := range []string{\"momo\", \"zalopay\", \"vnpay\", \"vietqr\"} {\n        g, ok := reg.Get(code)\n        require.True(t, ok)\n        require.Equal(t, code, g.Code())\n    }\n    _, ok := reg.Get(\"paypal\")\n    require.False(t, ok)\n}\n\nfunc TestVietQR_ReturnsQRPayload(t *testing.T) {\n    g, _ := newRegistry(t).Get(\"vietqr\")\n    res, err := g.CreatePayment(ctx, pay.PaymentRequest{OrderRef: \"o1\", Amount: 29000})\n    require.NoError(t, err)\n    require.NotEmpty(t, res.QRPayload)\n    require.Equal(t, int64(29000), res.Amount)\n}\n\n// services/bill/internal/pay/sign_test.go\nfunc TestSignMoMo_FixedVector(t *testing.T) {\n    secrets := fakeVault{\"bill/momo/secret_key\": \"test-key\"}\n    sig, err := SignMoMo(ctx, secrets, \"amount=29000&amp;orderId=o1\")\n    require.NoError(t, err)\n    require.Equal(t, hmacHex(\"test-key\", \"amount=29000&amp;orderId=o1\"), sig)\n}\n\n// services/bill/internal/api/checkout_test.go\nfunc TestCheckout_HappyPath(t *testing.T) {\n    h := setupCheckout(t)\n    rec := doPOST(t, h, \"/v1/billing/checkout\", `{\"plan_tier\":\"premium_basic\",\"gateway\":\"vietqr\"}`)\n    require.Equal(t, 200, rec.Code)\n    var res pay.PaymentResult\n    decode(t, rec, &amp;res)\n    require.Equal(t, int64(29000), res.Amount) // lấy từ plan_catalog\n}\n\nfunc TestCheckout_AmountFromCatalog_NotBody(t *testing.T) {\n    h := setupCheckout(t)\n    rec := doPOST(t, h, \"/v1/billing/checkout\",\n        `{\"plan_tier\":\"premium_basic\",\"gateway\":\"vietqr\",\"amount\":1}`) // body bịa amount=1\n    var res pay.PaymentResult\n    decode(t, rec, &amp;res)\n    require.Equal(t, int64(29000), res.Amount) // bỏ qua amount client, dùng catalog\n}\n\nfunc TestCheckout_Idempotent_NoDoubleCharge(t *testing.T) {\n    h := setupCheckout(t)\n    body := `{\"plan_tier\":\"premium_basic\",\"gateway\":\"vietqr\"}`\n    doPOST(t, h, \"/v1/billing/checkout\", body)\n    doPOST(t, h, \"/v1/billing/checkout\", body) // bấm lại\n    require.Equal(t, 1, h.gatewayCalls()) // chỉ một intent ở cổng\n}\n\nfunc TestCheckout_GatewayError_502_StaysPending(t *testing.T) {\n    h := setupCheckoutGatewayFail(t)\n    rec := doPOST(t, h, \"/v1/billing/checkout\", `{\"plan_tier\":\"premium_basic\",\"gateway\":\"momo\"}`)\n    require.Equal(t, 502, rec.Code)\n    require.NotContains(t, []string{\"confirmed\", \"paid\"}, h.lastPaymentStatus()) // không đánh dấu thành công\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>gateway.go</code> (interface + registry) -&gt; <code>sign.go</code> (ký per-cổng, khóa Vault TASK-INFRA-003) -&gt; bốn adapter <code>momo.go</code>/<code>zalopay.go</code>/<code>vnpay.go</code>/<code>vietqr.go</code> (tạo yêu cầu + parse phản hồi) -&gt; <code>checkout.go</code> (handler: tra plan, sinh order_ref idempotent, gọi gateway, ghi payment pending) -&gt; đăng ký route sau JWT middleware (TASK-INFRA-001) -&gt; tests. Số tiền luôn lấy từ <code>plan_catalog</code> (TASK-BILL-001), không từ client. HTTP client tới cổng có timeout; lỗi giữ <code>payment</code> pending. Trạng thái thanh toán cuối do TASK-BILL-003 chốt qua IPN/webhook.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-BILL-001</strong> - <code>plan_catalog</code> (giá BIGINT theo tier) + <code>subscription</code>; checkout lấy giá từ đây.</li><li><strong>TASK-INFRA-003 (Vault)</strong> - đọc khóa ký mỗi cổng; khóa không nằm trong DB/code.</li><li><strong>TASK-INFRA-001 (gateway)</strong> - gắn JWT + <code>user_id</code>; định tuyến <code>POST /v1/billing/checkout</code>.</li><li><strong>TASK-BILL-003 (downstream)</strong> - nhận IPN/webhook cổng, đối soát <code>payment</code>, đẩy <code>subscription</code> sang active + <code>renews_at</code>.</li><li><strong>TASK-COMPLY-005</strong> - audit no-cleartext: khóa ký từ Vault, không lưu thẻ.</li><li>Lib: <code>crypto/hmac</code>, <code>crypto/sha256</code>, <code>net/http</code>, <code>encoding/json</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Checkout request (user chọn VietQR)</h3>\n<pre><code class=\"language-http\">POST /v1/billing/checkout HTTP/1.1\nAuthorization: Bearer &lt;JWT-SanDeal&gt;\nContent-Type: application/json\n\n{\"plan_tier\": \"premium_basic\", \"gateway\": \"vietqr\"}</code></pre>\n<h3>PaymentResult (VietQR - trả chuỗi QR)</h3>\n<pre><code class=\"language-json\">{\n  \"order_ref\": \"sd_bill_7_premium_basic_1719500000\",\n  \"gateway\": \"vietqr\",\n  \"amount\": 29000,\n  \"qr_payload\": \"00020101021238540010A00000072701240006970436...\"\n}</code></pre>\n<h3>PaymentResult (MoMo - trả deeplink)</h3>\n<pre><code class=\"language-json\">{\n  \"order_ref\": \"sd_bill_7_premium_basic_1719500050\",\n  \"gateway\": \"momo\",\n  \"amount\": 29000,\n  \"pay_url\": \"https://test-payment.momo.vn/gw_payment/...&amp;signature=...\"\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Phí chính xác từng cổng theo hợp đồng merchant (§10 tài liệu nguồn - cần xác minh) - lưu vào cấu hình khi có hợp đồng; task này ưu tiên QR theo khoảng ~1,5-2,5%.</li><li>Auto-renew (tạo charge định kỳ không cần user bấm lại) - cần token hóa của cổng; thêm khi cổng hỗ trợ và có consent rõ; hiện mỗi kỳ user xác nhận.</li><li>Hoàn tiền (refund) qua API cổng - thêm khi có quy trình hủy/DSAR; task này chỉ tạo thanh toán.</li><li>Chọn cổng theo gợi ý thông minh (lịch sử user) - hiện QR mặc định gợi ý, user chọn.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Lưu thông tin thẻ</td><td>review + grep (§4 #9)</td><td>PCI scope nặng, lộ thẻ</td><td>Không chạm thẻ, cổng xử lý (DEC-BILL-09)</td></tr><tr><td>Khóa ký cleartext</td><td>review (§4 #8)</td><td>lộ DB = giả mạo thanh toán</td><td>Khóa từ Vault (DEC-BILL-09)</td></tr><tr><td>Số tiền từ client</td><td>amount-from-catalog test</td><td>thao túng giá</td><td>Lấy từ plan_catalog (§1 #3)</td></tr><tr><td>Double-charge khi bấm lại</td><td>idempotent test</td><td>trừ tiền hai lần</td><td>order_ref idempotent (§1 #6)</td></tr><tr><td>Coi lỗi mạng là thành công</td><td>gateway-error test</td><td>cấp Premium chưa thu tiền</td><td>Lỗi giữ pending (§1 #11)</td></tr><tr><td>Phí thẻ cao ăn biên</td><td>gợi ý QR mặc định</td><td>biên mỏng thêm</td><td>Ưu tiên QR (§1 #7)</td></tr><tr><td>plan/gateway lạ</td><td>400 test</td><td>request rác</td><td>Validate allowlist (§1 #10)</td></tr><tr><td>Chữ ký sai spec cổng</td><td>sign vector test</td><td>cổng từ chối</td><td>Ký đúng spec từng cổng (§1 #4)</td></tr><tr><td>Trạng thái thật chưa chốt</td><td>payment pending</td><td>-</td><td>TASK-BILL-003 chốt qua IPN</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>TASK-BILL-002 là cửa thu tiền của Premium: không có nó, TASK-BILL-001 không thành doanh thu.</li><li>Interface chung + adapter per-cổng giấu khác biệt MoMo/ZaloPay/VNPay/VietQR; thêm cổng là thêm adapter.</li><li>Ưu tiên QR (phí ~1,5-2,5% so với thẻ cao hơn) bảo vệ biên ở thị trường WTP thấp - mặc định gợi ý, không ép.</li><li>Không lưu thẻ giữ phạm vi PCI tối thiểu; khóa ký từ Vault tuân no-cleartext (§5.5).</li><li>Idempotent theo order_ref đóng cửa double-charge khi user bấm lại/retry.</li><li>Lỗi gọi cổng giữ payment pending - nguồn sự thật về thanh toán là cổng (qua IPN TASK-BILL-003), không phải kết quả lời gọi.</li><li>Số tiền luôn từ plan_catalog, không từ client - chống thao túng giá.</li></ul>\n<hr>\n<p><em>Hết TASK-BILL-002. Status: done (mục tiêu audit 10/10).</em></p>";
