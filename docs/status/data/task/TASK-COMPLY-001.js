window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-COMPLY-001"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service COMPLY <strong>MUST</strong> cung cấp khung quản lý đồng thuận (consent) tuân thủ PDPL Luật 91/2025/QH15 và Nghị định 356/2025/NĐ-CP: mỗi mục đích xử lý dữ liệu cá nhân có một bản ghi consent versioned, tái lập được, và mọi service khác <strong>MUST</strong> kiểm tra consent này trước khi xử lý. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> định nghĩa bảng <code>consent_policy (id, purpose_key, version, title_vi, body_vi, effective_from, created_at)</code> - mỗi (purpose_key, version) là một bản chính sách bất biến (immutable). Sửa nội dung là tạo version mới, KHÔNG update tại chỗ (DEC-COMPLY-03).</li><li><strong>MUST</strong> định nghĩa bảng <code>consent_record (id, user_id, purpose_key, policy_version, granted, source, ts, ip, user_agent)</code> append-only: mỗi lần grant hoặc withdraw là một dòng MỚI; KHÔNG xóa, KHÔNG update dòng cũ (DEC-COMPLY-03, tái lập được).</li><li><strong>MUST</strong> ràng buộc đơn mục đích (DEC-COMPLY-02): một <code>consent_record</code> gắn đúng một <code>purpose_key</code>. Cấm gộp nhiều mục đích vào một lần bấm \"Đồng ý tất cả\". Mỗi mục đích cần một lần đồng thuận riêng, cụ thể.</li><li><strong>MUST</strong> coi đồng thuận là tự nguyện và chủ động (DEC-COMPLY-01): chỉ <code>granted = true</code> khi user CHỦ ĐỘNG bật. Checkbox tích sẵn, opt-out mặc định, hoặc im lặng KHÔNG được coi là đồng thuận. Giá trị mặc định của mọi purpose là chưa đồng thuận.</li><li><strong>MUST</strong> lưu đủ trường chứng minh (DEC-COMPLY-04): <code>purpose_key</code>, <code>policy_version</code> (đúng bản user đã thấy), <code>source</code> (web/extension/mobile), <code>ts</code>, <code>ip</code>, <code>user_agent</code>. Đây là bằng chứng trước cơ quan quản lý rằng đồng thuận hợp lệ.</li><li><strong>MUST</strong> expose hàm service cho các service khác gọi như cổng pháp lý:</li></ol>\n<ul><li><code>Grant(ctx, userID int64, purpose string, src string, meta ReqMeta) error</code> - ghi dòng granted=true với policy_version đang hiệu lực.</li><li><code>Withdraw(ctx, userID int64, purpose string, src string, meta ReqMeta) error</code> - ghi dòng granted=false (thu hồi), KHÔNG xóa lịch sử.</li><li><code>IsAllowed(ctx, userID int64, purpose string) (bool, error)</code> - trả trạng thái hiệu lực hiện tại (dòng mới nhất của (user, purpose)); chưa có bản ghi -&gt; false.</li><li><code>History(ctx, userID int64, purpose string) ([]ConsentRecord, error)</code> - toàn bộ lịch sử cho DSAR (TASK-COMPLY-003) và kiểm toán.</li></ul>\n<ol><li><strong>MUST</strong> từ chối <code>purpose_key</code> không nằm trong tập đã đăng ký (enum <code>Purpose</code>); từ chối <code>policy_version</code> cũ hơn bản đang hiệu lực (user phải đồng thuận trên bản mới nhất).</li><li><strong>MUST</strong> seed tập purpose lõi tối thiểu: <code>cart_read</code> (extension đọc giỏ hàng/voucher), <code>price_tracking</code> (theo dõi giá theo tài khoản), <code>marketing_notification</code> (gửi alert/khuyến mãi), <code>analytics_b2b</code> (đóng góp dữ liệu xu hướng ẩn danh). Mỗi purpose là một bản <code>consent_policy</code> riêng.</li><li><strong>MUST</strong> đảm bảo idempotent với double-submit: grant hai lần liên tiếp cùng trạng thái KHÔNG tạo ra mâu thuẫn ngữ nghĩa (dòng mới nhất vẫn phản ánh đúng); trạng thái suy ra từ dòng mới nhất theo <code>ts</code>.</li><li><strong>SHOULD</strong> phát OTel metric: <code>consent_granted_total{purpose}</code>, <code>consent_withdrawn_total{purpose}</code> (counter); log mọi grant/withdraw ở mức audit (KHÔNG log nội dung dữ liệu cá nhân kèm theo).</li><li><strong>MUST</strong> bảo đảm <code>IsAllowed</code> là nguồn sự thật duy nhất cho cơ sở pháp lý: các service như EXT (TASK-EXT-006), TRACK, NOTIF gọi <code>IsAllowed</code> trước khi xử lý dữ liệu của purpose tương ứng; thiếu consent -&gt; từ chối xử lý.</li><li><strong>MUST</strong> ghi <code>policy_version</code> đúng bản user đã xem lúc bấm; nếu chính sách đổi sau đó, consent cũ vẫn gắn version cũ (không tự \"nâng cấp\" sang version mới - đó là tái lập được).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao consent versioned bất biến (DEC-COMPLY-03)?</strong> PDPL yêu cầu đồng thuận \"tái lập được\": khi cơ quan quản lý hỏi \"user đồng ý cái gì, ngày nào, trên bản chính sách nào\", phải trả lời chính xác. Nếu update nội dung chính sách tại chỗ, ta mất bằng chứng user đã thấy gì lúc bấm. Tách <code>consent_policy</code> thành bản version bất biến + <code>consent_record</code> trỏ tới đúng version giải đúng yêu cầu này.</p>\n<p><strong>Vì sao append-only cho consent_record (§1 #2)?</strong> Thu hồi (withdraw) không được xóa dấu vết \"đã từng đồng ý\". Ghi dòng mới <code>granted=false</code> giữ trọn lịch sử: cấp lúc nào, thu hồi lúc nào. Đây vừa là yêu cầu tái lập của PDPL, vừa là dữ liệu cho DSAR (user có quyền xem lịch sử đồng thuận của mình).</p>\n<p><strong>Vì sao đơn mục đích (DEC-COMPLY-02)?</strong> Luật 91/2025 đòi đồng thuận \"cụ thể\" và \"đơn mục đích\". Gộp \"đọc giỏ hàng + gửi marketing + bán dữ liệu B2B\" vào một nút \"Đồng ý\" là đồng thuận mơ hồ, không hợp lệ. Tách purpose để user đồng ý chọn lọc: có thể bật theo dõi giá nhưng tắt marketing.</p>\n<p><strong>Vì sao mặc định là chưa đồng thuận, im lặng != đồng thuận (DEC-COMPLY-01)?</strong> Đây là điểm cốt lõi PDPL khác với \"implied consent\". Checkbox tích sẵn hay \"tiếp tục dùng tức là đồng ý\" KHÔNG hợp lệ. Hệ thống phải bắt đầu từ trạng thái false và chỉ chuyển true khi có hành động chủ động. <code>IsAllowed</code> trả false khi chưa có bản ghi - không cho phép xử lý theo mặc định.</p>\n<p><strong>Vì sao IsAllowed là cổng pháp lý dùng chung (§1 #11)?</strong> Tập trung cơ sở pháp lý vào một hàm tránh việc mỗi service tự diễn giải consent một kiểu. EXT đọc giỏ hàng phải hỏi <code>IsAllowed(cart_read)</code>; NOTIF gửi marketing phải hỏi <code>IsAllowed(marketing_notification)</code>. Một nguồn sự thật giảm rủi ro lọt bề mặt xử lý không có cơ sở pháp lý (cổng tiến phase §7 BACKLOG: coverage consent = 100%).</p>\n<p><strong>Vì sao lưu ip/user_agent/source (DEC-COMPLY-04)?</strong> Bằng chứng đồng thuận cần ngữ cảnh: ai bấm, từ kênh nào, lúc nào. Khi tranh chấp, bộ trường này chứng minh đồng thuận là hành động thật của chủ thể dữ liệu, không phải hệ thống tự gán.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migrations</h3>\n<pre><code class=\"language-sql\">-- services/comply/migrations/0001_consent_policy.sql\nCREATE TABLE consent_policy (\n  id             BIGSERIAL   PRIMARY KEY,\n  purpose_key    TEXT        NOT NULL,\n  version        INTEGER     NOT NULL,\n  title_vi       TEXT        NOT NULL,\n  body_vi        TEXT        NOT NULL,\n  effective_from TIMESTAMPTZ NOT NULL DEFAULT now(),\n  created_at     TIMESTAMPTZ NOT NULL DEFAULT now(),\n  UNIQUE (purpose_key, version)\n);\n\n-- Seed purpose loi (moi purpose mot ban chinh sach v1).\nINSERT INTO consent_policy (purpose_key, version, title_vi, body_vi) VALUES\n  ('cart_read', 1, 'Doc gio hang va voucher cua ban',\n   'SanDeal doc gio hang/voucher tren trinh duyet cua ban de toi uu. KHONG gui cookie/token.'),\n  ('price_tracking', 1, 'Theo doi gia theo tai khoan',\n   'Luu san pham ban theo doi de canh bao khi gia thay doi.'),\n  ('marketing_notification', 1, 'Nhan thong bao khuyen mai',\n   'Gui alert sale va goi y san pham. Co the thu hoi bat ky luc nao.'),\n  ('analytics_b2b', 1, 'Dong gop du lieu xu huong an danh',\n   'Du lieu gia da an danh hoa (k-anonymity) phuc vu bao cao thi truong.');\n\n-- services/comply/migrations/0002_consent_record.sql\nCREATE TABLE consent_record (\n  id             BIGSERIAL   PRIMARY KEY,\n  user_id        BIGINT      NOT NULL REFERENCES app_user(id),\n  purpose_key    TEXT        NOT NULL,\n  policy_version INTEGER     NOT NULL,\n  granted        BOOLEAN     NOT NULL,\n  source         TEXT        NOT NULL CHECK (source IN ('web','extension','mobile')),\n  ts             TIMESTAMPTZ NOT NULL DEFAULT now(),\n  ip             INET,\n  user_agent     TEXT,\n  FOREIGN KEY (purpose_key, policy_version)\n    REFERENCES consent_policy(purpose_key, version)\n);\n\n-- Tra trang thai hieu luc hien tai = dong moi nhat theo ts cho moi (user, purpose).\nCREATE INDEX idx_consent_latest ON consent_record (user_id, purpose_key, ts DESC);</code></pre>\n<h3>Types (Go)</h3>\n<pre><code class=\"language-go\">// services/comply/internal/consent/types.go\ntype Purpose string\n\nconst (\n    PurposeCartRead    Purpose = \"cart_read\"\n    PurposeTracking    Purpose = \"price_tracking\"\n    PurposeMarketing   Purpose = \"marketing_notification\"\n    PurposeAnalyticsB2B Purpose = \"analytics_b2b\"\n)\n\ntype ConsentRecord struct {\n    ID            int64     `db:\"id\"`\n    UserID        int64     `db:\"user_id\"`\n    PurposeKey    string    `db:\"purpose_key\"`\n    PolicyVersion int32     `db:\"policy_version\"`\n    Granted       bool      `db:\"granted\"`\n    Source        string    `db:\"source\"`\n    TS            time.Time `db:\"ts\"`\n    IP            *netip.Addr `db:\"ip\"`\n    UserAgent     *string   `db:\"user_agent\"`\n}\n\ntype ReqMeta struct {\n    IP        *netip.Addr\n    UserAgent *string\n}</code></pre>\n<h3>Service (cổng pháp lý)</h3>\n<pre><code class=\"language-go\">// services/comply/internal/consent/service.go\n// IsAllowed la nguon su that duy nhat cho co so phap ly cua mot purpose.\nfunc (s *Service) IsAllowed(ctx context.Context, userID int64, p Purpose) (bool, error) {\n    if !validPurpose(p) {\n        return false, ErrUnknownPurpose\n    }\n    rec, err := s.repo.latest(ctx, userID, string(p))\n    if errors.Is(err, pgx.ErrNoRows) {\n        return false, nil // chua co ban ghi -&gt; chua dong thuan (im lang != dong thuan)\n    }\n    if err != nil {\n        return false, err\n    }\n    return rec.Granted, nil\n}\n\n// Grant ghi dong moi granted=true voi policy_version dang hieu luc.\nfunc (s *Service) Grant(ctx context.Context, userID int64, p Purpose, src string, m ReqMeta) error {\n    if !validPurpose(p) {\n        return ErrUnknownPurpose\n    }\n    ver, err := s.repo.effectiveVersion(ctx, string(p))\n    if err != nil {\n        return err\n    }\n    return s.repo.append(ctx, ConsentRecord{\n        UserID: userID, PurposeKey: string(p), PolicyVersion: ver,\n        Granted: true, Source: src, IP: m.IP, UserAgent: m.UserAgent,\n    })\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch -&gt; <code>consent_policy</code> và <code>consent_record</code> tồn tại; 4 purpose lõi được seed (version 1).</li><li><code>Grant(user, cart_read)</code> -&gt; một dòng <code>consent_record</code> granted=true, policy_version = bản hiệu lực, đủ source/ts.</li><li><code>IsAllowed(user, cart_read)</code> sau grant -&gt; true.</li><li><code>IsAllowed(user, marketing_notification)</code> khi chưa có bản ghi -&gt; false (im lặng != đồng thuận).</li><li><code>Withdraw(user, cart_read)</code> -&gt; ghi dòng MỚI granted=false; dòng granted=true cũ vẫn còn (append-only).</li><li><code>IsAllowed(user, cart_read)</code> sau withdraw -&gt; false.</li><li><code>History(user, cart_read)</code> trả cả hai dòng (grant rồi withdraw) theo thứ tự thời gian.</li><li><code>Grant(user, \"bad_purpose\")</code> -&gt; lỗi <code>ErrUnknownPurpose</code>, không ghi dòng nào.</li><li>Cố INSERT <code>consent_record</code> với <code>policy_version</code> không tồn tại -&gt; lỗi FK.</li><li>Cố INSERT <code>source = 'sms'</code> -&gt; lỗi CHECK constraint.</li><li>Sau khi tạo <code>consent_policy (cart_read, version 2)</code>, <code>Grant</code> ghi version 2; consent cũ vẫn giữ version 1 (không tự nâng cấp).</li><li>Metric <code>consent_granted_total{purpose}</code> tăng khi grant; <code>consent_withdrawn_total{purpose}</code> tăng khi withdraw.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/comply/internal/consent/service_test.go\nfunc TestConsent_SilenceIsNotConsent(t *testing.T) {\n    s, uid := setupWithUser(t)\n    ok, err := s.IsAllowed(ctx, uid, PurposeMarketing)\n    require.NoError(t, err)\n    require.False(t, ok) // chua co ban ghi -&gt; false, khong mac dinh dong thuan\n}\n\nfunc TestConsent_GrantThenAllowed(t *testing.T) {\n    s, uid := setupWithUser(t)\n    require.NoError(t, s.Grant(ctx, uid, PurposeCartRead, \"extension\", ReqMeta{}))\n    ok, _ := s.IsAllowed(ctx, uid, PurposeCartRead)\n    require.True(t, ok)\n}\n\nfunc TestConsent_WithdrawAppendsRow_KeepsHistory(t *testing.T) {\n    s, uid := setupWithUser(t)\n    s.Grant(ctx, uid, PurposeCartRead, \"web\", ReqMeta{})\n    require.NoError(t, s.Withdraw(ctx, uid, PurposeCartRead, \"web\", ReqMeta{}))\n\n    ok, _ := s.IsAllowed(ctx, uid, PurposeCartRead)\n    require.False(t, ok) // dong moi nhat granted=false\n\n    h, _ := s.History(ctx, uid, PurposeCartRead)\n    require.Len(t, h, 2) // grant + withdraw, khong xoa lich su\n}\n\nfunc TestConsent_UnknownPurpose_Rejected(t *testing.T) {\n    s, uid := setupWithUser(t)\n    err := s.Grant(ctx, uid, Purpose(\"bad_purpose\"), \"web\", ReqMeta{})\n    require.ErrorIs(t, err, ErrUnknownPurpose)\n}\n\nfunc TestConsent_OldConsentKeepsOldVersion(t *testing.T) {\n    s, uid := setupWithUser(t)\n    s.Grant(ctx, uid, PurposeCartRead, \"web\", ReqMeta{}) // version 1\n    seedPolicyVersion(t, s, \"cart_read\", 2)\n    s.Grant(ctx, uid, PurposeCartRead, \"web\", ReqMeta{}) // version 2\n    h, _ := s.History(ctx, uid, PurposeCartRead)\n    require.Equal(t, int32(1), h[0].PolicyVersion)\n    require.Equal(t, int32(2), h[1].PolicyVersion) // khong tu nang cap dong cu\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration 0001 (consent_policy + seed) -&gt; 0002 (consent_record) -&gt; types.go + policy.go -&gt; record.go + repo.go (append-only) -&gt; service.go (cổng IsAllowed) -&gt; tests. <code>consent_record</code> là append-only ở tầng ứng dụng: repo chỉ có <code>append</code>, KHÔNG có update/delete. Trạng thái hiện tại suy ra qua truy vấn dòng mới nhất (index <code>idx_consent_latest</code>). Các service khác inject <code>consent.Service</code> và gọi <code>IsAllowed</code> trước mọi xử lý dữ liệu cá nhân.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>TASK-INFRA-002</strong> - <code>app_user</code> phải tồn tại trước (FK <code>user_id</code>).</li><li><strong>TASK-EXT-006 (downstream)</strong> - UI consent lúc cài extension gọi <code>Grant(cart_read)</code>; đọc giỏ hàng kiểm <code>IsAllowed(cart_read)</code>.</li><li><strong>TASK-COMPLY-003 (downstream)</strong> - DSAR xuất <code>History</code> của user; xóa tài khoản tham chiếu trạng thái consent.</li><li><strong>TASK-AUTH-005 (downstream)</strong> - vòng đời tài khoản (xóa) phối hợp thu hồi consent.</li><li><strong>TASK-COMPLY-002 (downstream)</strong> - DPIA tham chiếu danh mục purpose làm cơ sở đánh giá tác động.</li><li>Lib: driver <code>pgx</code>; không phụ thuộc dịch vụ ngoài.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Extension xin consent đọc giỏ hàng lúc cài (qua BFF)</h3>\n<pre><code class=\"language-json\">POST /v1/consent/grant\n{\n  \"purpose\": \"cart_read\",\n  \"source\": \"extension\"\n}</code></pre>\n<pre><code class=\"language-json\">200 OK\n{\n  \"purpose\": \"cart_read\",\n  \"policy_version\": 1,\n  \"granted\": true,\n  \"ts\": \"2026-06-28T09:12:00+07:00\"\n}</code></pre>\n<h3>Service nội bộ kiểm cổng pháp lý trước khi xử lý</h3>\n<pre><code class=\"language-go\">ok, err := complySvc.IsAllowed(ctx, userID, consent.PurposeCartRead)\nif err != nil || !ok {\n    return ErrNoLegalBasis // tu choi xu ly khi thieu dong thuan\n}\n// ... tiep tuc doc gio hang</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt khung lõi. Hoãn:</p>\n<ul><li>Consent cho người chưa thành niên (xác minh tuổi/giám hộ) - slice sau khi có luồng định danh tuổi.</li><li>Consent banner đa ngôn ngữ (EN/ID/TH) - gắn vào TASK-COMPLY-007 khi mở SEA.</li><li>Đồng bộ thu hồi consent xuyên thiết bị realtime (push withdraw tới extension đang mở) - tối ưu UX giai đoạn sau.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Checkbox tích sẵn lọt vào UI</td><td>review + test client</td><td>đồng thuận vô hiệu, vi phạm PDPL</td><td>UI bắt đầu false; <code>Grant</code> chỉ gọi khi user chủ động bấm</td></tr><tr><td>Gộp nhiều purpose một nút</td><td>review §1 #3</td><td>đồng thuận không cụ thể</td><td>Một purpose một bản ghi; tách nút trong TASK-EXT-006</td></tr><tr><td>Service quên gọi IsAllowed</td><td>audit coverage §7 BACKLOG</td><td>xử lý không cơ sở pháp lý</td><td>Cổng tiến phase yêu cầu coverage 100%; review từng bề mặt</td></tr><tr><td>Xóa cứng consent khi withdraw</td><td>review repo (không có delete)</td><td>mất tái lập</td><td>Repo chỉ <code>append</code>; withdraw ghi dòng false</td></tr><tr><td>policy_version không khớp bản user thấy</td><td>FK + AC #11</td><td>bằng chứng sai lệch</td><td>Ghi đúng version hiệu lực lúc grant; FK chặn version lạ</td></tr><tr><td>purpose_key gõ sai ở caller</td><td><code>ErrUnknownPurpose</code> + AC #8</td><td>từ chối ghi</td><td>Dùng hằng <code>Purpose</code>, không truyền chuỗi tự do</td></tr><tr><td>Double-submit grant</td><td>AC, idempotent §1 #9</td><td>dòng trùng vô hại</td><td>Trạng thái = dòng mới nhất; trùng không đổi ngữ nghĩa</td></tr><tr><td>Log lộ dữ liệu cá nhân kèm consent</td><td>review log §1 #10</td><td>rò rỉ PII</td><td>Chỉ log purpose/ts/granted, không log nội dung dữ liệu</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Đây là task nền pháp lý của SănDeal: mọi bề mặt xử lý dữ liệu cá nhân lấy cơ sở pháp lý từ <code>IsAllowed</code>.</li><li>PDPL hiệu lực 01/01/2026 (đã đính chính so với đề bài ghi 01/07/2026, §10 tài liệu nguồn); NĐ 356/2025/NĐ-CP thay NĐ 13/2023.</li><li>Bốn nguyên tắc PDPL được mã hóa trực tiếp: tự nguyện (chỉ grant khi chủ động), cụ thể + đơn mục đích (một purpose một bản ghi), tái lập được (append-only + policy versioned).</li><li>Im lặng không phải đồng thuận: mặc định false, <code>IsAllowed</code> trả false khi chưa có bản ghi. Đây là khác biệt cốt lõi so với implied consent.</li><li>Tập purpose mở rộng được: thêm purpose mới = thêm bản <code>consent_policy</code>, không đổi schema.</li><li>Khi mở SEA, adapter TASK-COMPLY-007 tái dùng khung này, chỉ thay nội dung chính sách và bổ sung yêu cầu địa phương (Indonesia PDP, Thailand PDPA).</li></ul>\n<hr>\n<p><em>Hết TASK-COMPLY-001. Status: done (mục tiêu audit 10/10).</em></p>";
