window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AFFIL-002"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service AFFIL <strong>MUST</strong> expose <code>POST /v1/affiliate/link</code> chỉ trả một deep link affiliate khi request là user-initiated (user chủ động bấm \"Mua qua SănDeal\"), kèm disclosure và URL đích hiển thị rõ, và ghi một <code>affiliate_click</code> nguyên tử với hành động đó. Server <strong>MUST NOT</strong> tự chèn/sửa cookie affiliate nền. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> phục vụ <code>POST /v1/affiliate/link</code> với thân <code>{product_id: int64, user_initiated: true}</code>. Trường <code>user_initiated</code> <strong>MUST</strong> là <code>true</code>; nếu thiếu hoặc <code>false</code> -&gt; trả <code>400</code> với <code>{\"error\":\"link requires explicit user action\"}</code> và <strong>MUST NOT</strong> ghi <code>affiliate_click</code> (DEC-AFFIL-06, NFR-AFFIL-001).</li><li><strong>MUST</strong> lấy <code>user_id</code> từ JWT do API Gateway (FR-INFRA-001) gắn; handler KHÔNG tự parse token. Request thiếu auth bị gateway chặn trước.</li><li><strong>MUST</strong> tra <code>product_id</code> trong <code>tracked_product</code> để lấy <code>platform_id</code> + định danh sản phẩm sàn; <code>product_id</code> không tồn tại -&gt; <code>404</code> <code>{\"error\":\"product not found\"}</code>, không ghi click.</li><li><strong>MUST</strong> sinh một <code>sub_id</code> mới (FR-AFFIL-001 <code>subid.go</code>) cho mỗi lần tạo link, và ghi <code>affiliate_click</code> qua <code>RecordClick</code> (FR-AFFIL-001) như một hành động nguyên tử với việc trả link (DEC-AFFIL-07). Nếu <code>RecordClick</code> lỗi -&gt; <code>500</code>, không trả link (không trả link mà không ghi, và không ghi mà không có chủ ý user).</li><li><strong>MUST</strong> build deep link theo template của <code>network</code> cho sàn đó (FR-AFFIL-003), nhúng <code>sub_id</code> làm tham số tracking (DEC-AFFIL-10). Thiếu mapping network cho sàn -&gt; <code>503</code> <code>{\"error\":\"affiliate network unavailable\"}</code>, KHÔNG bịa link tự ghép tay.</li><li>Response thành công (<code>200</code>) <strong>MUST</strong> chứa: <code>deep_link</code> (URL affiliate để client mở), <code>target_url</code> (URL sản phẩm đích trên sàn, hiển thị cho user thấy đi đâu), và <code>disclosure</code> (văn bản tiếng Việt nói rõ SănDeal có thể nhận hoa hồng) (DEC-AFFIL-08).</li><li><code>target_url</code> <strong>MUST</strong> trỏ tới domain sàn hợp lệ của <code>platform_id</code> (shopee.vn / tiktok.com / lazada.vn); client <strong>MUST</strong> được kỳ vọng hiển thị đích này trước khi điều hướng (disclosure-then-redirect, không che giấu).</li><li>Server <strong>MUST NOT</strong> set, sửa, hay drop cookie trên bất kỳ domain sàn nào (DEC-AFFIL-09). Cookie affiliate (nếu có) chỉ được tạo bởi chính trang sàn khi client mở <code>deep_link</code> trong tab mà user bấm - đó là last-click hợp lệ do hành động người dùng.</li><li>Endpoint <strong>MUST NOT</strong> có chế độ batch/prefetch/background tạo link cho nhiều sản phẩm \"phòng khi user mua\" (NFR-AFFIL-001): mỗi link tương ứng một hành động bấm thật của user tại thời điểm đó.</li><li><strong>MUST</strong> trả <code>disclosure</code> không rỗng trong mọi response <code>200</code>; thiếu disclosure là lỗi tạo response (fail-closed) - KHÔNG trả <code>deep_link</code> mà không kèm disclosure.</li><li><strong>MUST</strong> đặt <code>Content-Type: application/json; charset=utf-8</code>. Mọi giá trị tiền (nếu trả kèm) là <code>BIGINT</code> VND, đồng nhất DEC-PRICE-05/DEC-AFFIL-04.</li><li><strong>SHOULD</strong> phát OTel: <code>affiliate_link_created_total{platform_id, network}</code> (counter), <code>affiliate_link_rejected_total{reason}</code> (counter; <code>reason - {not_user_initiated, product_not_found, network_unavailable}</code>).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao bắt buộc cờ user_initiated (DEC-AFFIL-06)?</strong> Toàn bộ định vị của SănDeal hậu-Honey nằm ở một câu: affiliate chỉ hợp lệ khi người dùng chủ động bấm. Honey bị gỡ vì làm ngược lại - thay cookie nền không cần user. Bằng cách yêu cầu một cờ chủ ý tường minh và từ chối (kèm không ghi click) khi thiếu, endpoint không thể bị dùng để tạo link nền. Đây không phải kiểm tra hình thức: nó là hợp đồng kỹ thuật của lời hứa đạo đức.</p>\n<p><strong>Vì sao link và ghi click là nguyên tử (DEC-AFFIL-07)?</strong> Một deep link affiliate luôn ứng với một hành động bấm. Sinh <code>sub_id</code> mới và ghi <code>affiliate_click</code> cùng lúc với việc trả link giữ sổ cái khớp thực tế: mỗi link đã trả đều có một click được ghi, và mỗi click đều do user bấm. Không có link \"ghost\" không ghi, cũng không có click ghi mà không phải do bấm.</p>\n<p><strong>Vì sao bắt buộc disclosure + target_url hiển thị (DEC-AFFIL-08)?</strong> Minh bạch là yêu cầu của chính sách Chrome Web Store mới (§4.2) và là moat niềm tin. Người dùng phải biết hai điều trước khi đi: họ sẽ tới đâu (<code>target_url</code>) và SănDeal có thể hưởng hoa hồng (<code>disclosure</code>). Giấu một trong hai là kiểu hành vi khiến Honey mất niềm tin. Trả cả hai trong response và kỳ vọng client hiển thị chúng là cách biến minh bạch thành mặc định.</p>\n<p><strong>Vì sao server không chạm cookie sàn (DEC-AFFIL-09)?</strong> Chèn/sửa cookie affiliate trên domain sàn từ phía ta chính là cookie-stuffing - thứ bị cấm. Mô hình hợp lệ là: ta chỉ trả một URL; khi user (đã bấm) mở URL đó, chính trang sàn set cookie affiliate của nó qua điều hướng bình thường. Last-click khi đó do hành động người dùng tạo ra, không phải do ta nhét nền.</p>\n<p><strong>Vì sao thiếu network template thì 503 chứ không tự ghép (DEC-AFFIL-10)?</strong> Mỗi network (Involve Asia, Accesstrade) có định dạng deep link riêng với tham số tracking riêng. Tự ghép tay một URL \"trông giống\" dễ tạo link sai không quy được conversion (mất tiền) hoặc vi phạm định dạng network. Thiếu mapping là lỗi cấu hình thật; trả <code>503</code> để lộ ra và sửa, an toàn hơn là bịa một link hỏng.</p>\n<p><strong>Vì sao cấm batch/prefetch (§1 #9)?</strong> Tạo sẵn link cho nhiều sản phẩm \"phòng khi user mua\" là một dạng tự động hóa nền trá hình - tinh thần của nó là gắn affiliate không cần chủ ý từng lần. Một bấm, một link. Ràng buộc này đóng cửa hậu cho hành vi kiểu Honey len vào qua tối ưu hiệu năng.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Handler (Go)</h3>\n<pre><code class=\"language-go\">// services/affil/internal/api/link.go\n\ntype LinkRequest struct {\n    ProductID     int64 `json:\"product_id\"`\n    UserInitiated bool  `json:\"user_initiated\"`   // BẮT BUỘC true (§1 #1)\n}\ntype LinkResponse struct {\n    DeepLink   string `json:\"deep_link\"`\n    TargetURL  string `json:\"target_url\"`         // hiển thị đích cho user (§1 #6,#7)\n    Disclosure string `json:\"disclosure\"`         // không rỗng (§1 #10)\n}\n\nfunc (h *Handler) HandleCreateLink(w http.ResponseWriter, req *http.Request) {\n    userID := auth.UserID(req.Context()) // do gateway gắn (FR-INFRA-001)\n    var body LinkRequest\n    if err := json.NewDecoder(req.Body).Decode(&amp;body); err != nil {\n        writeErr(w, 400, \"invalid body\"); return\n    }\n    if !body.UserInitiated { // §1 #1 - không ghi click, từ chối\n        metrics.LinkRejected(\"not_user_initiated\")\n        writeErr(w, 400, \"link requires explicit user action\"); return\n    }\n    tp, ok := h.products.Get(req.Context(), body.ProductID)\n    if !ok {\n        metrics.LinkRejected(\"product_not_found\")\n        writeErr(w, 404, \"product not found\"); return\n    }\n    network, tmpl, ok := h.networks.TemplateFor(tp.PlatformID) // FR-AFFIL-003\n    if !ok {\n        metrics.LinkRejected(\"network_unavailable\")\n        writeErr(w, 503, \"affiliate network unavailable\"); return // không bịa link (§1 #5)\n    }\n    subID := affil.NewSubID()\n    if _, err := h.repo.RecordClick(req.Context(), affil.AffiliateClick{ // nguyên tử với link (§1 #4)\n        UserID: userID, PlatformID: tp.PlatformID, ProductID: &amp;tp.ID,\n        SubID: subID, Network: network,\n    }); err != nil {\n        writeErr(w, 500, \"internal error\"); return // không trả link nếu không ghi được\n    }\n    deep := affil.BuildDeepLink(tmpl, tp, subID) // nhúng sub_id (§1 #5)\n    w.Header().Set(\"Content-Type\", \"application/json; charset=utf-8\")\n    _ = json.NewEncoder(w).Encode(LinkResponse{\n        DeepLink:   deep,\n        TargetURL:  tp.TargetURL(),              // domain sàn hợp lệ (§1 #7)\n        Disclosure: affil.Disclosure(),          // không rỗng (§1 #10)\n    })\n    metrics.LinkCreated(tp.PlatformID, network)\n}</code></pre>\n<h3>Deep link builder (Go)</h3>\n<pre><code class=\"language-go\">// services/affil/internal/affil/deeplink.go\n\n// BuildDeepLink ghép URL affiliate theo template network, nhúng sub_id làm tham số tracking.\n// KHÔNG set cookie, KHÔNG chạm domain sàn - chỉ trả chuỗi URL (§1 #8).\nfunc BuildDeepLink(tmpl NetworkTemplate, p Product, subID string) string {\n    target := p.TargetURL() // URL sản phẩm trên sàn\n    u, _ := url.Parse(tmpl.BaseURL)\n    q := u.Query()\n    q.Set(tmpl.TargetParam, target) // network bọc URL đích\n    q.Set(tmpl.SubIDParam, subID)   // sub_id để postback đối soát (FR-AFFIL-001)\n    u.RawQuery = q.Encode()\n    return u.String()\n}</code></pre>\n<h3>Disclosure (Go)</h3>\n<pre><code class=\"language-go\">// services/affil/internal/affil/disclosure.go\n\n// Disclosure trả văn bản minh bạch tiếng Việt, không rỗng (§1 #10).\nfunc Disclosure() string {\n    return \"Khi ban mua qua lien ket nay, SanDeal co the nhan mot khoan hoa hong tu san. \" +\n        \"Gia ban tra khong thay doi. Ban dang duoc chuyen toi trang san chinh thuc.\"\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>POST /v1/affiliate/link {product_id:90112, user_initiated:true}</code> (hợp lệ) -&gt; <code>200</code> với <code>deep_link</code>, <code>target_url</code>, <code>disclosure</code> đều không rỗng.</li><li>Request thiếu <code>user_initiated</code> (hoặc <code>false</code>) -&gt; <code>400</code> <code>{\"error\":\"link requires explicit user action\"}</code> VÀ không có dòng <code>affiliate_click</code> nào được ghi (kiểm DB sau request).</li><li><code>product_id</code> không tồn tại -&gt; <code>404</code>, không ghi click.</li><li>Mỗi request hợp lệ sinh một <code>sub_id</code> mới và một dòng <code>affiliate_click</code> đúng <code>user_id/platform_id/network</code>.</li><li><code>deep_link</code> chứa <code>sub_id</code> vừa sinh làm tham số tracking (kiểm query string).</li><li><code>target_url</code> trỏ tới domain sàn của <code>platform_id</code> (ví dụ <code>shopee.vn</code> cho platform 1).</li><li>Thiếu mapping network cho sàn -&gt; <code>503</code>, KHÔNG trả <code>deep_link</code>, không ghi click.</li><li><code>disclosure</code> không rỗng trong mọi response <code>200</code>; chứa cụm \"hoa hong\" (minh bạch).</li><li><code>RecordClick</code> lỗi (mock repo fail) -&gt; <code>500</code>, KHÔNG trả <code>deep_link</code> (không link mà không ghi).</li><li>Không tồn tại endpoint/đường batch tạo nhiều link một lần (grep router: chỉ một route, nhận một <code>product_id</code>).</li><li>Server không gọi bất kỳ API set-cookie nào trên domain sàn (review + test: handler chỉ trả JSON, không HTTP client tới sàn).</li><li>Metric <code>affiliate_link_created_total</code> tăng khi <code>200</code>; <code>affiliate_link_rejected_total{reason}</code> tăng đúng nhãn khi từ chối.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/affil/internal/api/link_test.go\nfunc TestLink_NotUserInitiated_Rejected_NoClick(t *testing.T) {\n    h, repo := setupHandler(t)\n    rec := doPOST(t, h, \"/v1/affiliate/link\", `{\"product_id\":90112,\"user_initiated\":false}`)\n    require.Equal(t, 400, rec.Code)\n    require.Equal(t, 0, repo.ClickCount()) // KHÔNG ghi click khi thiếu chủ ý (§1 #1)\n}\n\nfunc TestLink_MissingFlag_Rejected(t *testing.T) {\n    h, repo := setupHandler(t)\n    rec := doPOST(t, h, \"/v1/affiliate/link\", `{\"product_id\":90112}`) // thiếu user_initiated\n    require.Equal(t, 400, rec.Code)\n    require.Equal(t, 0, repo.ClickCount())\n}\n\nfunc TestLink_HappyPath_HasDisclosureAndTarget(t *testing.T) {\n    h, repo := setupHandler(t)\n    rec := doPOST(t, h, \"/v1/affiliate/link\", `{\"product_id\":90112,\"user_initiated\":true}`)\n    require.Equal(t, 200, rec.Code)\n    var resp LinkResponse\n    decode(t, rec, &amp;resp)\n    require.NotEmpty(t, resp.DeepLink)\n    require.Contains(t, resp.TargetURL, \"shopee.vn\")\n    require.Contains(t, resp.Disclosure, \"hoa hong\")\n    require.Equal(t, 1, repo.ClickCount()) // ghi đúng một click\n}\n\nfunc TestLink_ProductNotFound_404(t *testing.T) {\n    h, repo := setupHandler(t)\n    rec := doPOST(t, h, \"/v1/affiliate/link\", `{\"product_id\":999999,\"user_initiated\":true}`)\n    require.Equal(t, 404, rec.Code)\n    require.Equal(t, 0, repo.ClickCount())\n}\n\nfunc TestLink_NoNetwork_503_NoClick(t *testing.T) {\n    h, repo := setupHandlerNoNetwork(t) // không cấu hình template\n    rec := doPOST(t, h, \"/v1/affiliate/link\", `{\"product_id\":90112,\"user_initiated\":true}`)\n    require.Equal(t, 503, rec.Code)\n    require.Equal(t, 0, repo.ClickCount()) // không bịa link, không ghi\n}\n\nfunc TestLink_RecordClickFails_NoLink(t *testing.T) {\n    h, repo := setupHandler(t)\n    repo.FailNextClick()\n    rec := doPOST(t, h, \"/v1/affiliate/link\", `{\"product_id\":90112,\"user_initiated\":true}`)\n    require.Equal(t, 500, rec.Code)\n    require.NotContains(t, rec.Body.String(), \"deep_link\") // không link mà không ghi (§1 #4)\n}</code></pre>\n<pre><code class=\"language-go\">// services/affil/internal/affil/deeplink_test.go\nfunc TestDeepLink_EmbedsSubID(t *testing.T) {\n    tmpl := NetworkTemplate{BaseURL: \"https://go.involve.asia/aff\", TargetParam: \"url\", SubIDParam: \"sub_id\"}\n    link := BuildDeepLink(tmpl, sampleProduct(), \"sd_abc123\")\n    require.Contains(t, link, \"sub_id=sd_abc123\")\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>deeplink.go</code> (build URL từ template + nhúng sub_id) -&gt; <code>disclosure.go</code> (văn bản minh bạch) -&gt; <code>link.go</code> (handler: chặn nếu thiếu cờ user_initiated, tra product, lấy network template, sinh sub_id, RecordClick nguyên tử, trả disclosure + target_url) -&gt; đăng ký route trong <code>router.go</code> sau JWT middleware (FR-INFRA-001) -&gt; tests. Handler dùng <code>http.ServeMux</code> Go 1.22. Network template lấy từ FR-AFFIL-003 (<code>networks.TemplateFor</code>). Không tạo HTTP client tới domain sàn - handler chỉ trả JSON, mọi điều hướng/cookie xảy ra phía client khi user mở <code>deep_link</code>.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>FR-AFFIL-001</strong> - cung cấp <code>RecordClick</code>, <code>NewSubID</code>, bảng <code>affiliate_click</code>; là điều kiện cứng để ghi click khi tạo link.</li><li><strong>FR-AFFIL-003 (downstream + cấu hình)</strong> - cung cấp network template (<code>TemplateFor</code>) để build deep link; postback dùng <code>sub_id</code> sinh ở đây.</li><li><strong>FR-AFFIL-004 (downstream)</strong> - guardrails né Honey kiểm endpoint này không có đường tự động + có disclosure.</li><li><strong>FR-INFRA-001 (gateway)</strong> - gắn JWT auth và <code>user_id</code> vào context trước handler.</li><li><strong>FR-EXT-003</strong> - extension chỉ gửi dữ liệu sạch (productId); nút \"Mua qua SănDeal\" trong extension/web gọi endpoint này khi user bấm.</li><li><strong>NFR-AFFIL-001</strong> - ràng buộc compliance: endpoint này là điểm thực thi \"chỉ user-initiated + disclosure\".</li><li>Lib: <code>net/http</code>, <code>net/url</code>, <code>encoding/json</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Request (user vừa bấm \"Mua qua SănDeal\")</h3>\n<pre><code class=\"language-http\">POST /v1/affiliate/link HTTP/1.1\nAuthorization: Bearer &lt;JWT-SanDeal&gt;\nContent-Type: application/json\n\n{\"product_id\": 90112, \"user_initiated\": true}</code></pre>\n<h3>Response (200)</h3>\n<pre><code class=\"language-json\">{\n  \"deep_link\": \"https://go.involve.asia/aff?url=https%3A%2F%2Fshopee.vn%2Fproduct%2F88123%2F20114455667&amp;sub_id=sd_ab12cd34ef56\",\n  \"target_url\": \"https://shopee.vn/product/88123/20114455667\",\n  \"disclosure\": \"Khi ban mua qua lien ket nay, SanDeal co the nhan mot khoan hoa hong tu san. Gia ban tra khong thay doi. Ban dang duoc chuyen toi trang san chinh thuc.\"\n}</code></pre>\n<h3>Response (400 - thiếu chủ ý user, KHÔNG ghi click)</h3>\n<pre><code class=\"language-json\">{\"error\": \"link requires explicit user action\"}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Trang trung gian \"interstitial\" của SănDeal hiển thị disclosure + đếm ngược rồi mới redirect (thay vì client tự hiển thị) - cân nhắc cho web; client hiện chịu trách nhiệm hiển thị <code>target_url</code> + <code>disclosure</code>.</li><li>Rút gọn deep link qua dịch vụ link riêng để đẹp URL - không cần ở P2; giữ URL network nguyên bản để đối soát rõ.</li><li>Đa network cho cùng sàn (chọn network trả hoa hồng cao nhất tại thời điểm) - thêm logic chọn ở FR-AFFIL-003; FR này dùng template mặc định của sàn.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Tạo link không có chủ ý user</td><td>link test (§4 #2)</td><td>cookie-stuffing kiểu Honey, bị gỡ</td><td>Bắt buộc user_initiated, không ghi nếu thiếu (§1 #1)</td></tr><tr><td>Response giấu đích / thiếu disclosure</td><td>happy-path test</td><td>mất niềm tin, vi phạm policy</td><td>Bắt buộc target_url + disclosure không rỗng (§1 #6,#10)</td></tr><tr><td>Server tự set cookie sàn</td><td>review (§4 #11)</td><td>chèn cookie nền bất hợp pháp</td><td>Chỉ trả URL, client mở (DEC-AFFIL-09)</td></tr><tr><td>Bịa deep link khi thiếu network</td><td>503 test</td><td>link sai không quy conversion</td><td>Trả 503, không tự ghép (§1 #5)</td></tr><tr><td>Trả link mà không ghi click</td><td>RecordClick-fail test</td><td>sổ cái lệch</td><td>Nguyên tử: lỗi ghi -&gt; không trả link (§1 #4)</td></tr><tr><td>Batch/prefetch nhiều link</td><td>grep router (§4 #10)</td><td>tự động hóa nền trá hình</td><td>Một bấm một link (§1 #9)</td></tr><tr><td>product_id không tồn tại</td><td>404 test</td><td>link trỏ rác</td><td>Tra tracked_product trước (§1 #3)</td></tr><tr><td>Thiếu JWT</td><td>gateway chặn</td><td>-</td><td>Auth ở gateway (FR-INFRA-001)</td></tr><tr><td>sub_id trùng (hi hữu)</td><td>UNIQUE ở FR-AFFIL-001</td><td>RecordClick lỗi -&gt; 500</td><td>Sinh lại sub_id; entropy đủ để cực hiếm</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Endpoint này là điểm thực thi kỹ thuật của lằn ranh đạo đức trung tâm: affiliate chỉ hợp lệ khi user chủ động bấm (§4.2).</li><li>Cờ <code>user_initiated</code> + không-ghi-khi-thiếu đóng cửa với mô hình cookie-stuffing kiểu Honey ngay tại API.</li><li>Response luôn kèm <code>target_url</code> (đi đâu) + <code>disclosure</code> (có thể hưởng hoa hồng): minh bạch là mặc định, không phải tùy chọn.</li><li>Server không bao giờ chạm cookie domain sàn; cookie last-click do chính trang sàn set khi user (đã bấm) mở deep link - đó là last-click hợp lệ.</li><li>Link và ghi click là nguyên tử: mỗi link đã trả đều có một click ghi do user bấm, không có link ghost cũng không có click nền.</li><li>Cấm batch/prefetch đóng cửa hậu cho tự động hóa nền len vào qua tối ưu hiệu năng.</li><li>Đây là nền cho FR-AFFIL-003 (postback dùng sub_id), FR-AFFIL-004 (guardrails kiểm chính endpoint này) và FR-AFFIL-005 (cashback).</li></ul>\n<hr>\n<p><em>Hết FR-AFFIL-002. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
