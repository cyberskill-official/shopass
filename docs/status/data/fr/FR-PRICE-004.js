window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PRICE-004"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service PRICE <strong>MUST</strong> expose REST endpoint <code>GET /v1/compare?canonical_key=...</code>: cho một <code>canonical_key</code> (do FR-PRICE-005 sinh và lưu trên <code>tracked_product</code>), trả giá hiện tại của cùng một sản phẩm vật lý trên từng sàn (Shopee/TikTok/Lazada), kèm cờ rẻ nhất. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> đăng ký route <code>GET /v1/compare</code> sau JWT middleware của API gateway; chỉ request có token hợp lệ mới qua (auth tập trung ở gateway, handler không tự xác thực).</li><li><strong>MUST</strong> yêu cầu query param <code>canonical_key</code>; nếu thiếu hoặc rỗng -&gt; trả <code>400 Bad Request</code> với thân lỗi rõ, KHÔNG truy vấn DB.</li><li><strong>MUST</strong> JOIN <code>tracked_product</code> lọc theo <code>canonical_key</code> -&gt; snapshot giá mới nhất mỗi <code>product_id</code> -&gt; <code>platform</code>, dựng đúng một dòng cho mỗi sàn có listing thuộc key đó (DEC-PRICE-40).</li><li><strong>MUST</strong> lấy giá hiện tại của mỗi <code>product_id</code> qua <code>DISTINCT ON (product_id) ... ORDER BY product_id, ts DESC</code> trên <code>price_snapshot</code> (DEC-PRICE-41); KHÔNG dùng <code>price_daily</code> (cần <code>ts</code> giây/phút cho độ tươi).</li><li><strong>MUST</strong> trả mỗi dòng gồm <code>{platform_code, platform_name, product_id, price, ts, item_url}</code>. <code>platform_code</code> lấy từ <code>platform.code</code>; <code>platform_name</code> suy ra từ <code>code</code> phía server (<code>displayName</code>: shopee-&gt;\"Shopee\", tiktok-&gt;\"TikTok Shop\", lazada-&gt;\"Lazada\") vì bảng <code>platform</code> (FR-INFRA-002) KHÔNG có cột <code>name</code>. <code>item_url</code> lấy từ <code>platform_item_id</code> của <code>tracked_product</code>, hoặc tham chiếu để client dựng link.</li><li><strong>MUST</strong> tính sàn rẻ nhất phía server và set <code>is_cheapest = true</code> đúng cho dòng có <code>price</code> nhỏ nhất (DEC-PRICE-42); các dòng còn lại <code>false</code>. Nếu có nhiều dòng cùng giá nhỏ nhất, set <code>is_cheapest</code> cho mọi dòng bằng giá đó.</li><li><strong>MUST</strong> lưu và trả <code>price</code> dạng <code>BIGINT</code> (VND, <code>int64</code>, không thập phân) - thống nhất với FR-PRICE-002, tránh sai số float.</li><li><strong>MUST</strong> trả <code>ts</code> (TIMESTAMPTZ) của từng dòng để UI hiển thị độ tươi của giá mỗi sàn (DEC-PRICE-43); giá có thể cũ khác nhau giữa các sàn.</li><li><strong>MUST</strong> xử lý <code>canonical_key</code> chỉ có một sàn: vẫn trả về dòng đó với <code>is_cheapest = true</code> (chỉ một lựa chọn), KHÔNG ép phải đủ 3 sàn (DEC-PRICE-44).</li><li><strong>MUST</strong> xử lý <code>canonical_key</code> lạ hoặc không có listing nào -&gt; trả <code>404 Not Found</code> (chọn 404, không trả mảng rỗng <code>200</code>, để client phân biệt rõ \"không có sản phẩm\" với \"có nhưng rỗng\").</li><li><strong>MUST</strong> đạt <code>p95 &lt; 500ms</code> cho truy vấn compare (dựa trên index <code>idx_tp_canonical</code> của FR-PRICE-001 + index <code>ts</code> của hypertable).</li><li><strong>SHOULD</strong> phát OTel metric <code>compare_request_total{result}</code> (<code>result - {ok, not_found, bad_request}</code>) và <code>compare_query_duration_ms</code> (histogram) để theo dõi p95.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao khóa theo canonical_key (DEC-PRICE-40, depends_on FR-PRICE-005)?</strong> So khớp \"cùng một sản phẩm vật lý trên 3 sàn\" là bài toán khó và đã được giải ở FR-PRICE-005 (chuẩn hóa title, key xác định, fuzzy, duyệt tay). Endpoint compare không lặp lại việc đó - nó chỉ tin <code>canonical_key</code> đã gán và JOIN theo. Tách rõ \"so khớp\" khỏi \"đọc so sánh\" giữ handler mỏng, nhanh, và mọi rủi ro gộp nhầm nằm gọn ở một nơi (FR-PRICE-005).</p>\n<p><strong>Vì sao DISTINCT ON latest-per-product thay vì price_daily (DEC-PRICE-41)?</strong> So sánh giá cần giá hiện tại đúng tại thời điểm hỏi, không phải giá đóng cửa của ngày. <code>price_daily</code> chỉ có <code>close_p</code> theo ngày, mất độ phân giải giây/phút và quan trọng hơn là mất <code>ts</code> chính xác. <code>DISTINCT ON (product_id) ORDER BY product_id, ts DESC</code> lấy đúng snapshot mới nhất mỗi sản phẩm kèm <code>ts</code> thật, nên UI biết giá này tươi đến đâu. Index <code>(product_id, ts DESC)</code> của hypertable làm phép này nhanh.</p>\n<p><strong>Vì sao tính rẻ nhất phía server (DEC-PRICE-42)?</strong> Nếu để web và app mỗi nơi tự so giá, hai client dễ lệch nhau khi xử lý giá bằng nhau, giá thiếu, hay làm tròn. Tính một lần ở server và trả cờ <code>is_cheapest</code> cho mọi client đọc giống nhau - một nguồn sự thật, không lệch badge giữa nền tảng.</p>\n<p><strong>Vì sao trả ts mỗi sàn (DEC-PRICE-43, minh bạch §5.4)?</strong> SănDeal định vị quanh niềm tin hậu-Honey: không giấu, không thổi phồng. Một giá Lazada quét cách đây 2 giờ và một giá Shopee quét 5 phút trước không nên trông ngang nhau. Trả <code>ts</code> để UI nói thẳng \"giá cập nhật lúc...\", người dùng tự đánh giá độ tin. Giấu độ tươi là mầm mất niềm tin; phơi nó ra là rẻ và đúng.</p>\n<p><strong>Vì sao 1 sàn vẫn trả, không ép đủ 3 (DEC-PRICE-44)?</strong> Nhiều sản phẩm chỉ bán trên một hoặc hai sàn. Ép phải đủ 3 sàn mới trả về sẽ bỏ rơi phần lớn long-tail. Trả những gì có (dù chỉ một dòng) vẫn hữu ích: người dùng thấy giá hiện có và biết sàn đó là rẻ nhất theo nghĩa \"lựa chọn duy nhất\".</p>\n<p><strong>Vì sao key lạ trả 404 chứ không phải mảng rỗng (§1 #10)?</strong> Mảng rỗng <code>200</code> mơ hồ: client không phân biệt được \"canonical_key này không tồn tại\" với \"tồn tại nhưng tạm không có giá\". <code>404</code> nói rõ không có gì để so sánh, để UI hiện trạng thái đúng (ví dụ \"chưa theo dõi sản phẩm này\") thay vì khung so sánh trống.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Truy vấn (Go + SQL)</h3>\n<pre><code class=\"language-go\">// services/price/internal/price/compare_query.go\npackage price\n\n// CompareRow là một dòng giá hiện tại của một sàn cho một canonical_key.\n// platform chỉ có cột `code` (FR-INFRA-002 sở hữu: id/code/country/base_url) - KHÔNG có cột name;\n// tên hiển thị suy ra từ code phía server (displayName), không SELECT cột không tồn tại.\ntype CompareRow struct {\n    PlatformCode string    `db:\"platform_code\"`\n    ProductID    int64     `db:\"product_id\"`\n    Price        int64     `db:\"price\"`        // VND, không thập phân\n    TS           time.Time `db:\"ts\"`           // độ tươi của giá sàn này\n    PlatformItem string    `db:\"platform_item_id\"`\n}\n\n// CompareByCanonicalKey: snapshot mới nhất mỗi product (DISTINCT ON),\n// JOIN tracked_product lọc theo canonical_key + JOIN platform (chỉ lấy code).\nfunc (r *Repo) CompareByCanonicalKey(ctx context.Context, key string) ([]CompareRow, error) {\n    rows, err := r.pool.Query(ctx, `\n        SELECT pf.code AS platform_code,\n               tp.id   AS product_id,\n               ls.price,\n               ls.ts,\n               tp.platform_item_id\n        FROM tracked_product tp\n        JOIN platform pf ON pf.id = tp.platform_id\n        JOIN LATERAL (\n            SELECT DISTINCT ON (ps.product_id) ps.price, ps.ts\n            FROM price_snapshot ps\n            WHERE ps.product_id = tp.id\n            ORDER BY ps.product_id, ps.ts DESC\n        ) ls ON true\n        WHERE tp.canonical_key = $1\n        ORDER BY ls.price ASC`, key)\n    if err != nil {\n        return nil, err\n    }\n    defer rows.Close()\n    return scanCompareRows(rows)\n}\n\n// displayName suy tên hiển thị từ platform.code (không có cột name trong schema §3.4).\nfunc displayName(code string) string {\n    switch code {\n    case \"shopee\":\n        return \"Shopee\"\n    case \"tiktok\":\n        return \"TikTok Shop\"\n    case \"lazada\":\n        return \"Lazada\"\n    default:\n        return code\n    }\n}</code></pre>\n<h3>Response structs (Go)</h3>\n<pre><code class=\"language-go\">// services/price/internal/api/compare.go\npackage api\n\ntype CompareItem struct {\n    PlatformCode string `json:\"platform_code\"`\n    PlatformName string `json:\"platform_name\"`\n    ProductID    int64  `json:\"product_id\"`\n    Price        int64  `json:\"price\"`        // VND\n    Currency     string `json:\"currency\"`     // \"VND\"\n    TS           string `json:\"ts\"`           // RFC3339, độ tươi\n    ItemURL      string `json:\"item_url\"`\n    IsCheapest   bool   `json:\"is_cheapest\"`\n}\n\ntype CompareResponse struct {\n    CanonicalKey string        `json:\"canonical_key\"`\n    Items        []CompareItem `json:\"items\"`\n}</code></pre>\n<h3>Handler (Go)</h3>\n<pre><code class=\"language-go\">// services/price/internal/api/compare.go\nfunc (h *Handler) Compare(w http.ResponseWriter, r *http.Request) {\n    key := strings.TrimSpace(r.URL.Query().Get(\"canonical_key\"))\n    if key == \"\" {\n        writeErr(w, http.StatusBadRequest, \"canonical_key là bắt buộc\")\n        metrics.Compare(\"bad_request\")\n        return\n    }\n    rows, err := h.repo.CompareByCanonicalKey(r.Context(), key)\n    if err != nil {\n        writeErr(w, http.StatusInternalServerError, \"lỗi truy vấn\")\n        return\n    }\n    if len(rows) == 0 {\n        writeErr(w, http.StatusNotFound, \"không có sản phẩm cho canonical_key này\")\n        metrics.Compare(\"not_found\")\n        return\n    }\n    items := toItems(rows)\n    markCheapest(items) // set is_cheapest phía server (DEC-PRICE-42)\n    writeJSON(w, http.StatusOK, CompareResponse{CanonicalKey: key, Items: items})\n    metrics.Compare(\"ok\")\n}\n\n// markCheapest đánh dấu mọi dòng có price = min (xử lý cả trường hợp bằng giá).\nfunc markCheapest(items []CompareItem) {\n    if len(items) == 0 {\n        return\n    }\n    min := items[0].Price\n    for _, it := range items {\n        if it.Price &lt; min {\n            min = it.Price\n        }\n    }\n    for i := range items {\n        items[i].IsCheapest = items[i].Price == min\n    }\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>GET /v1/compare</code> đăng ký sau JWT middleware của gateway; request không token bị gateway chặn (handler không thấy).</li><li><code>GET /v1/compare</code> thiếu <code>canonical_key</code> (hoặc rỗng) -&gt; <code>400</code>, thân lỗi rõ, KHÔNG truy vấn DB.</li><li><code>canonical_key</code> có listing 3 sàn -&gt; trả 3 dòng, mỗi sàn một dòng (JOIN tracked_product theo key + platform).</li><li>Mỗi dòng lấy giá từ snapshot mới nhất của product đó (DISTINCT ON product_id, ts DESC), KHÔNG từ price_daily.</li><li>Mỗi dòng có đủ <code>{platform_code, platform_name, product_id, price, ts, item_url}</code>.</li><li>Dòng giá nhỏ nhất có <code>is_cheapest=true</code>; các dòng khác <code>false</code>; nhiều dòng bằng giá nhỏ nhất -&gt; tất cả <code>true</code>.</li><li><code>price</code> là số nguyên VND (int64); JSON không có phần thập phân.</li><li>Mỗi dòng trả <code>ts</code> (RFC3339) phản ánh thời điểm snapshot mới nhất của sàn đó.</li><li><code>canonical_key</code> chỉ có 1 sàn -&gt; trả 1 dòng, <code>is_cheapest=true</code>, không lỗi.</li><li><code>canonical_key</code> lạ / không có listing -&gt; <code>404</code> (không phải mảng rỗng <code>200</code>).</li><li>p95 truy vấn compare <code>&lt; 500ms</code> trên dữ liệu thực (đo qua <code>compare_query_duration_ms</code>).</li><li>Metric <code>compare_request_total{result}</code> tăng đúng nhánh (<code>ok</code> / <code>not_found</code> / <code>bad_request</code>).</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/price/internal/api/compare_test.go\nfunc TestCompare_ThreePlatforms_HighlightsCheapest(t *testing.T) {\n    h, key := setupThreePlatforms(t) // shopee 89k, tiktok 92k, lazada 85k cùng canonical_key\n    rec := doGET(t, h, \"/v1/compare?canonical_key=\"+key)\n    require.Equal(t, http.StatusOK, rec.Code)\n    var resp CompareResponse\n    json.Unmarshal(rec.Body.Bytes(), &amp;resp)\n    require.Len(t, resp.Items, 3)\n    cheapest := pickCheapest(resp.Items)\n    require.Equal(t, \"lazada\", cheapest.PlatformCode) // 85k rẻ nhất\n    require.True(t, cheapest.IsCheapest)\n    require.Equal(t, 1, countCheapest(resp.Items)) // đúng một dòng được đánh dấu\n}\n\nfunc TestCompare_MissingKey_400(t *testing.T) {\n    h, _ := setupThreePlatforms(t)\n    rec := doGET(t, h, \"/v1/compare\") // thiếu canonical_key\n    require.Equal(t, http.StatusBadRequest, rec.Code)\n}\n\nfunc TestCompare_UnknownKey_Empty(t *testing.T) {\n    h, _ := setupThreePlatforms(t)\n    rec := doGET(t, h, \"/v1/compare?canonical_key=khong-ton-tai\")\n    require.Equal(t, http.StatusNotFound, rec.Code) // 404, không phải mảng rỗng\n}\n\nfunc TestCompare_SinglePlatform(t *testing.T) {\n    h, key := setupSinglePlatform(t, \"shopee\", 120_000) // chỉ 1 sàn\n    rec := doGET(t, h, \"/v1/compare?canonical_key=\"+key)\n    require.Equal(t, http.StatusOK, rec.Code)\n    var resp CompareResponse\n    json.Unmarshal(rec.Body.Bytes(), &amp;resp)\n    require.Len(t, resp.Items, 1)\n    require.True(t, resp.Items[0].IsCheapest) // lựa chọn duy nhất\n}\n\nfunc TestCompare_PayloadShape(t *testing.T) {\n    h, key := setupThreePlatforms(t)\n    rec := doGET(t, h, \"/v1/compare?canonical_key=\"+key)\n    var resp CompareResponse\n    json.Unmarshal(rec.Body.Bytes(), &amp;resp)\n    it := resp.Items[0]\n    require.NotEmpty(t, it.PlatformCode)\n    require.NotEmpty(t, it.PlatformName)\n    require.Greater(t, it.Price, int64(0))     // int64 VND\n    require.NotEmpty(t, it.TS)                  // ts độ tươi luôn có\n    require.Equal(t, \"VND\", it.Currency)\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>compare_query.go</code> (DISTINCT ON latest + JOIN platform lấy <code>code</code>) -&gt; <code>compare.go</code> (parse + validate + <code>toItems</code> + <code>markCheapest</code> + dựng response) -&gt; <code>router.go</code> (đăng ký <code>GET /v1/compare</code> sau JWT middleware) -&gt; tests. Handler dùng driver <code>pgx</code>, không tự xác thực (gateway lo JWT). <code>toItems</code> đặt <code>PlatformName = displayName(row.PlatformCode)</code> (bảng <code>platform</code> không có cột <code>name</code>). Truy vấn xếp <code>ORDER BY ls.price ASC</code> để dòng đầu là rẻ nhất; <code>markCheapest</code> vẫn quét lại để xử lý đúng trường hợp bằng giá. Item URL dựng từ <code>platform_item_id</code> + <code>platform_code</code> theo bảng quy ước link sàn, hoặc trả thẳng tham chiếu cho client ghép.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>FR-PRICE-005</strong> - sinh và gán <code>canonical_key</code> cho <code>tracked_product</code>; không có key thì compare không có hàng để JOIN.</li><li><strong>FR-PRICE-001</strong> - <code>tracked_product(canonical_key, platform_id, platform_item_id, title)</code> + index <code>idx_tp_canonical</code>.</li><li><strong>FR-INFRA-002</strong> - bảng <code>platform(id, code, country, base_url)</code> (KHÔNG có cột <code>name</code>); compare JOIN lấy <code>code</code>, suy <code>platform_name</code> từ <code>code</code> phía server.</li><li><strong>FR-PRICE-002</strong> - <code>price_snapshot(product_id, ts, price)</code> là nguồn giá hiện tại (snapshot mới nhất).</li><li><strong>FR-PRICE-003 (liên quan)</strong> - chuẩn hóa truy vấn lịch sử/giá hiện tại dùng chung tầng repo.</li><li>Lib: driver <code>pgx</code>; TimescaleDB 2.x (index <code>ts</code> của hypertable cho latest-per-product nhanh).</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Request</h3>\n<pre><code class=\"language-bash\">curl -s -H \"Authorization: Bearer $JWT\" \\\n  \"https://api.sandeal.vn/v1/compare?canonical_key=sony:wh%201000xm5:9f2a3c1d0b7e\"</code></pre>\n<h3>Response (200, 3 sàn, Lazada rẻ nhất)</h3>\n<pre><code class=\"language-json\">{\n  \"canonical_key\": \"sony:wh 1000xm5:9f2a3c1d0b7e\",\n  \"items\": [\n    {\n      \"platform_code\": \"lazada\",\n      \"platform_name\": \"Lazada\",\n      \"product_id\": 90233,\n      \"price\": 5990000,\n      \"currency\": \"VND\",\n      \"ts\": \"2026-06-27T08:55:12Z\",\n      \"item_url\": \"https://www.lazada.vn/products/i90233.html\",\n      \"is_cheapest\": true\n    },\n    {\n      \"platform_code\": \"shopee\",\n      \"platform_name\": \"Shopee\",\n      \"product_id\": 90112,\n      \"price\": 6190000,\n      \"currency\": \"VND\",\n      \"ts\": \"2026-06-27T08:59:40Z\",\n      \"item_url\": \"https://shopee.vn/product/i90112\",\n      \"is_cheapest\": false\n    },\n    {\n      \"platform_code\": \"tiktok\",\n      \"platform_name\": \"TikTok Shop\",\n      \"product_id\": 90150,\n      \"price\": 6290000,\n      \"currency\": \"VND\",\n      \"ts\": \"2026-06-27T07:12:03Z\",\n      \"item_url\": \"https://shop.tiktok.com/view/i90150\",\n      \"is_cheapest\": false\n    }\n  ]\n}</code></pre>\n<p><code>ts</code> của TikTok cũ hơn (07:12) so với Shopee/Lazada (08:55-08:59), UI nên nói rõ độ tươi từng giá.</p>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Ngưỡng \"giá quá cũ\" để gắn cờ stale (ví dụ <code>ts</code> quá 6 giờ) - thêm trường <code>is_stale</code> ở slice sau, dữ liệu <code>ts</code> đã đủ để UI tự quyết tạm thời.</li><li>Trả thêm <code>list_price</code>/<code>flash_sale</code> mỗi dòng để hiện mức giảm - mở rộng response khi UI so sánh cần, không đổi khóa truy vấn.</li><li>Cache kết quả compare theo <code>canonical_key</code> (TTL ngắn) - bật khi tải tăng; ban đầu đọc thẳng để luôn tươi.</li><li>Trả thêm phí ship ước tính mỗi sàn để \"rẻ nhất\" tính cả ship - cần nguồn phí ship, để giai đoạn sau.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>canonical_key sai do over-merge (FR-PRICE-005)</td><td>report giá lệch + mẫu duyệt</td><td>so sánh nhầm sản phẩm khác (giá Pro cho bản thường)</td><td>Ngưỡng merge cao + review queue ở FR-PRICE-005 (DEC-PRICE-23)</td></tr><tr><td>Thiếu param canonical_key</td><td>guard handler</td><td>400 trước khi chạm DB</td><td>Validate sớm, thân lỗi rõ (§1 #2)</td></tr><tr><td>canonical_key lạ / chưa gán</td><td>rows rỗng</td><td>404</td><td>Trả 404 phân biệt với rỗng (§1 #10)</td></tr><tr><td>Giá 1 sàn cũ/stale</td><td>so ts mỗi dòng</td><td>\"rẻ nhất\" dựa giá cũ</td><td>Trả ts để UI cảnh báo độ tươi (DEC-PRICE-43); cờ stale ở slice sau</td></tr><tr><td>Thiếu 1-2 sàn (sản phẩm long-tail)</td><td>đếm items &lt; 3</td><td>so sánh không đủ 3 dòng</td><td>Vẫn trả phần có, không ép đủ 3 (DEC-PRICE-44)</td></tr><tr><td>Lấy nhầm price_daily thay snapshot</td><td>review + mất ts</td><td>giá đóng cửa ngày, không tươi</td><td>Dùng DISTINCT ON price_snapshot (DEC-PRICE-41); AC #4</td></tr><tr><td>Hai dòng bằng giá nhỏ nhất</td><td>test bằng giá</td><td>chỉ một dòng được badge</td><td>markCheapest set mọi dòng = min (§1 #6)</td></tr><tr><td>Truy vấn chậm khi key nhiều listing</td><td>p95 compare_query_duration_ms</td><td>vỡ NFR &lt;500ms</td><td>Index idx_tp_canonical + ts hypertable; LIMIT/giám sát</td></tr><tr><td>Tính rẻ nhất phía client lệch web/app</td><td>so badge giữa nền tảng</td><td>hiển thị khác nhau cùng dữ liệu</td><td>Tính server, trả is_cheapest (DEC-PRICE-42)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li><code>GET /v1/compare</code> là đầu ra người dùng của moat đa sàn: FR-PRICE-005 gom nhóm, FR-PRICE-004 đọc nhóm đó thành so sánh giá 3 sàn.</li><li>Khóa theo <code>canonical_key</code> giữ handler mỏng: mọi rủi ro gộp nhầm nằm ở FR-PRICE-005, compare chỉ tin và JOIN.</li><li><code>DISTINCT ON (product_id) ... ts DESC</code> trả giá mới nhất kèm <code>ts</code> thật - khác <code>price_daily</code> chỉ có giá đóng cửa ngày, mất độ tươi.</li><li>Tính <code>is_cheapest</code> phía server là một nguồn sự thật cho mọi client; trả <code>ts</code> mỗi sàn là cam kết minh bạch hậu-Honey (§5.4).</li><li>Trả về cả khi chỉ một sàn (không ép đủ 3) phục vụ long-tail; key lạ trả 404 để client phân biệt rõ trạng thái.</li><li>Endpoint này underpin UI so sánh và là viên gạch cho gói dữ liệu B2B sau (xuất giá chéo sàn theo canonical_key).</li></ul>\n<hr>\n<p><em>Hết FR-PRICE-004. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
