window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-B2B-002"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service B2B <strong>MUST</strong> sinh báo cáo insights xu hướng giá/thị trường từ <code>market_trend_daily</code>, gating truy cập theo bảng <code>b2b_subscription</code>, và đọc DUY NHẤT dữ liệu đã ẩn danh (ô <code>suppressed=false</code>). Hợp đồng:</p>\n<ol><li><strong>MUST</strong> đọc dữ liệu báo cáo DUY NHẤT từ <code>market_trend_daily</code> qua repo của FR-B2B-001, chỉ ô <code>suppressed=false</code> (DEC-B2B-10). <strong>MUST NOT</strong> chạm <code>price_snapshot</code>, <code>price_daily</code> raw, hay bất kỳ bảng user-level nào.</li><li><strong>MUST</strong> định nghĩa bảng <code>b2b_subscription (id, org_name, tier, max_categories, history_days, can_export, status, started_at, expires_at)</code>; <code>tier</code> thuộc <code>{basic, pro, enterprise}</code> (DEC-B2B-11).</li><li><strong>MUST</strong> chỉ phục vụ báo cáo khi subscription <code>status = 'active'</code> và <code>expires_at &gt; now()</code>. Subscription không active hoặc hết hạn -&gt; trả <code>402</code> (cần thanh toán/gia hạn).</li><li><strong>MUST</strong> mô tả một báo cáo là tổ hợp <code>(category_id[], platform_id[], from, to)</code> ánh xạ sang chuỗi ô <code>market_trend_daily</code> (DEC-B2B-12). Nếu MỌI ô trong phạm vi bị suppress -&gt; trả báo cáo rỗng kèm lý do (<code>reason = \"insufficient_data\"</code>); <strong>MUST NOT</strong> nội suy hay bịa số.</li><li><strong>MUST</strong> kẹp scope theo entitlement của tier (DEC-B2B-13): số <code>category_id</code> không vượt <code>max_categories</code>; khoảng <code>(to - from)</code> không vượt <code>history_days</code>. Request vượt entitlement -&gt; trả <code>403</code> với thông điệp nâng cấp; <strong>MUST NOT</strong> cắt im lặng rồi trả phần dữ liệu.</li><li><strong>MUST</strong> chỉ cho export (CSV/JSON) khi <code>can_export = true</code> (DEC-B2B-14). Mọi dòng export <strong>MUST</strong> là chỉ số tổng hợp đã qua k-anonymity, không chứa khóa định danh.</li><li><strong>MUST</strong> gắn vào báo cáo <code>cached_at</code> (lúc dựng) và <code>source_computed_at</code> (max <code>computed_at</code> của ô nguồn) để khách hàng B2B biết độ tươi (DEC-B2B-15).</li><li><strong>MUST</strong> expose endpoint <code>GET /v1/b2b/reports?categories=...&amp;platforms=...&amp;from=...&amp;to=...</code> (đọc) và <code>GET /v1/b2b/reports/export?...</code> (export, gating <code>can_export</code>).</li><li><strong>MUST</strong> phân biệt mã trạng thái: <code>200</code> (có dữ liệu hoặc rỗng-có-lý-do), <code>402</code> (subscription không active), <code>403</code> (vượt entitlement), <code>400</code> (tham số sai).</li><li><strong>SHOULD</strong> phát OTel metric: <code>b2b_report_served_total{tier}</code> (counter), <code>b2b_report_denied_total{reason}</code> (counter), <code>b2b_report_build_ms</code> (histogram).</li><li><strong>MUST</strong> resolve entitlement từ chính <code>b2b_subscription</code> của caller; <strong>MUST NOT</strong> tin tham số tier/quyền do client gửi.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p>Vì sao chỉ đọc market_trend_daily (DEC-B2B-10)? FR-B2B-001 đã dựng k-anonymity ở tầng pipeline. Nếu báo cáo lại đi đường vòng đọc raw để \"làm giàu\" thì phá tan công sức đó và mở lại cửa rò rỉ. Quy tắc đơn giản: tầng báo cáo chỉ tiêu thụ ô đã phát hành, không bao giờ chạm nguồn chi tiết. Mọi thứ khách hàng B2B thấy đều đã qua cổng ẩn danh.</p>\n<p>Vì sao gating theo b2b_subscription server-side (DEC-B2B-11, §1 #11)? Quyền truy cập là tài sản doanh thu. Nếu tin tham số tier do client gửi thì ai cũng tự xưng enterprise. Entitlement phải nạp từ bản ghi subscription của chính caller trong DB. Tier quyết định ba trục giá trị: bao nhiêu category, lịch sử sâu bao nhiêu, có export không.</p>\n<p>Vì sao trả 403 thay vì cắt im lặng (DEC-B2B-13, §1 #5)? Nếu khách tier basic xin 100 category mà ta lặng lẽ trả 5 category đầu thì họ tưởng dữ liệu thiếu, còn ta thì vừa hỏng trải nghiệm vừa mất cơ hội bán nâng cấp. Trả 403 rõ ràng \"vượt gói, nâng cấp để xem thêm\" biến giới hạn thành điểm chạm bán hàng.</p>\n<p>Vì sao rỗng-có-lý-do thay vì nội suy (DEC-B2B-12, §1 #4)? Khi mọi ô trong phạm vi bị suppress (category hẹp, ngày thưa), cám dỗ là nội suy cho báo cáo \"đẹp\". Nhưng nội suy số ẩn danh là bịa - vừa sai vừa có thể vô tình tái dựng tín hiệu của một cá thể. Trung thực trả rỗng kèm lý do; khách hàng B2B cần số đúng, không cần số đầy.</p>\n<p>Vì sao gắn độ tươi (DEC-B2B-15, §1 #7)? Báo cáo dựa trên job đêm (FR-B2B-001) nên dữ liệu trễ tới một ngày. Khách hàng B2B ra quyết định giá phải biết số này tính tới khi nào. <code>source_computed_at</code> nói rõ thay vì để họ đoán.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/b2b/migrations/0002_b2b_subscription.sql\nCREATE TABLE b2b_subscription (\n  id             BIGSERIAL   PRIMARY KEY,\n  org_name       TEXT        NOT NULL,\n  tier           TEXT        NOT NULL CHECK (tier IN ('basic','pro','enterprise')),\n  max_categories INTEGER     NOT NULL CHECK (max_categories &gt; 0),\n  history_days   INTEGER     NOT NULL CHECK (history_days &gt; 0),\n  can_export     BOOLEAN     NOT NULL DEFAULT false,\n  status         TEXT        NOT NULL DEFAULT 'active'\n                   CHECK (status IN ('active','past_due','canceled')),\n  started_at     TIMESTAMPTZ NOT NULL DEFAULT now(),\n  expires_at     TIMESTAMPTZ NOT NULL\n);\n\nCREATE INDEX idx_b2bsub_active ON b2b_subscription (id)\n  WHERE status = 'active';</code></pre>\n<h3>Types + entitlement (Go)</h3>\n<pre><code class=\"language-go\">// services/b2b/internal/report/entitlement.go\ntype Entitlement struct {\n    Tier          string\n    MaxCategories int\n    HistoryDays   int\n    CanExport     bool\n}\n\ntype ReportScope struct {\n    CategoryIDs []int64\n    PlatformIDs []int16\n    From, To    time.Time\n}\n\n// checkScope kẹp scope theo entitlement; trả lỗi 403 khi vượt (KHÔNG cắt im lặng).\nfunc checkScope(e Entitlement, s ReportScope) error {\n    if len(s.CategoryIDs) &gt; e.MaxCategories {\n        return ErrScopeExceeded{Field: \"categories\", Limit: e.MaxCategories}\n    }\n    if s.To.Sub(s.From) &gt; time.Duration(e.HistoryDays)*24*time.Hour {\n        return ErrScopeExceeded{Field: \"history_days\", Limit: e.HistoryDays}\n    }\n    return nil\n}</code></pre>\n<h3>Builder (§1 #4, #7)</h3>\n<pre><code class=\"language-go\">// services/b2b/internal/report/builder.go\ntype Report struct {\n    Scope           ReportScope     `json:\"scope\"`\n    Cells           []TrendPoint    `json:\"cells\"`\n    Reason          string          `json:\"reason,omitempty\"`     // \"insufficient_data\" khi rỗng\n    CachedAt        time.Time       `json:\"cached_at\"`\n    SourceComputedAt time.Time      `json:\"source_computed_at\"`\n}\n\nfunc (b *Builder) Build(ctx context.Context, s ReportScope) (Report, error) {\n    var pts []TrendPoint\n    var maxComputed time.Time\n    for _, cat := range s.CategoryIDs {\n        for _, pf := range s.PlatformIDs {\n            // chỉ ô suppressed=false (QueryCells lọc mặc định)\n            cells, err := b.trend.QueryCells(ctx, cat, pf, s.From, s.To)\n            if err != nil {\n                return Report{}, err\n            }\n            for _, c := range cells {\n                pts = append(pts, toPoint(c))\n                if c.ComputedAt.After(maxComputed) {\n                    maxComputed = c.ComputedAt\n                }\n            }\n        }\n    }\n    r := Report{Scope: s, Cells: pts, CachedAt: time.Now(), SourceComputedAt: maxComputed}\n    if len(pts) == 0 {\n        r.Reason = \"insufficient_data\" // mọi ô bị suppress - KHÔNG nội suy\n    }\n    return r, nil\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch -&gt; <code>b2b_subscription</code> tồn tại với CHECK <code>tier</code>/<code>status</code>.</li><li>Subscription <code>active</code> + chưa hết hạn, scope trong gói -&gt; <code>200</code> + chuỗi ô đúng.</li><li>Subscription <code>status='past_due'</code> hoặc <code>expires_at &lt; now()</code> -&gt; <code>402</code>, KHÔNG trả dữ liệu.</li><li>Request có số category vượt <code>max_categories</code> -&gt; <code>403</code> với thông điệp nâng cấp.</li><li>Request có khoảng ngày vượt <code>history_days</code> -&gt; <code>403</code>.</li><li>Mọi ô trong phạm vi bị suppress (không có ô phát hành nào) -&gt; <code>200</code> + <code>cells=[]</code> + <code>reason=\"insufficient_data\"</code>; KHÔNG có số nội suy.</li><li>Export khi <code>can_export=false</code> -&gt; <code>403</code>; khi <code>can_export=true</code> -&gt; file CSV/JSON các dòng chỉ số tổng hợp.</li><li>Mọi dòng export KHÔNG chứa <code>product_id/shop_id/user_id</code>.</li><li>Báo cáo có <code>cached_at</code> và <code>source_computed_at</code> đúng (= max <code>computed_at</code> của ô nguồn).</li><li>Tham số tier/quyền gửi từ client bị bỏ qua; entitlement nạp từ DB của caller.</li><li>Builder gọi <code>QueryCells</code> (vốn lọc suppressed) - không có truy vấn nào chạm nguồn raw (review/test grep).</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/b2b/internal/report/entitlement_test.go\nfunc TestScope_WithinTier_OK(t *testing.T) {\n    e := Entitlement{Tier: \"pro\", MaxCategories: 10, HistoryDays: 180, CanExport: true}\n    s := ReportScope{CategoryIDs: ids(5), From: dNow.AddDate(0, 0, -90), To: dNow}\n    require.NoError(t, checkScope(e, s))\n}\n\nfunc TestScope_TooManyCategories_403(t *testing.T) {\n    e := Entitlement{Tier: \"basic\", MaxCategories: 3, HistoryDays: 30}\n    s := ReportScope{CategoryIDs: ids(4), From: dNow.AddDate(0, 0, -7), To: dNow}\n    err := checkScope(e, s)\n    require.ErrorAs(t, err, &amp;ErrScopeExceeded{})\n}\n\nfunc TestScope_HistoryTooDeep_403(t *testing.T) {\n    e := Entitlement{Tier: \"basic\", MaxCategories: 3, HistoryDays: 30}\n    s := ReportScope{CategoryIDs: ids(1), From: dNow.AddDate(0, 0, -90), To: dNow}\n    err := checkScope(e, s)\n    require.ErrorAs(t, err, &amp;ErrScopeExceeded{})\n}\n\nfunc TestReport_InactiveSubscription_402(t *testing.T) {\n    h := setupHandler(t, withSub(\"past_due\"))\n    res := h.get(\"/v1/b2b/reports?categories=7&amp;platforms=1&amp;from=...&amp;to=...\")\n    require.Equal(t, 402, res.Code)\n}\n\n// services/b2b/internal/report/builder_test.go\nfunc TestBuild_AllSuppressed_EmptyWithReason(t *testing.T) {\n    b := setupBuilder(t, withTrendCells(/* mọi ô suppressed=true */))\n    r, _ := b.Build(ctx, ReportScope{CategoryIDs: []int64{991}, PlatformIDs: []int16{1},\n        From: d0, To: d0.AddDate(0, 0, 7)})\n    require.Empty(t, r.Cells)\n    require.Equal(t, \"insufficient_data\", r.Reason)\n}\n\nfunc TestBuild_PublishedCells_Series(t *testing.T) {\n    b := setupBuilder(t, withTrendCells(/* category 7, platform 1, 7 ô phát hành */))\n    r, _ := b.Build(ctx, ReportScope{CategoryIDs: []int64{7}, PlatformIDs: []int16{1},\n        From: d0, To: d0.AddDate(0, 0, 7)})\n    require.Len(t, r.Cells, 7)\n    require.False(t, r.SourceComputedAt.IsZero())\n}\n\nfunc TestExport_NoIdentifierColumns(t *testing.T) {\n    csv := exportCSV(t, /* báo cáo có dữ liệu */)\n    require.NotContains(t, csv, \"product_id\")\n    require.NotContains(t, csv, \"shop_id\")\n    require.NotContains(t, csv, \"user_id\")\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration 0002 (b2b_subscription) -&gt; entitlement.go (nạp + kẹp scope) -&gt; builder.go (dựng từ ô đã phát hành) -&gt; export.go -&gt; report_handler.go -&gt; tests. Handler chạy sau JWT/gateway (FR-INFRA-001) cho xác thực; resolve subscription của org từ claim. Báo cáo có thể cache theo <code>(scope hash, ngày)</code> vì market_trend_daily chỉ đổi sau job đêm.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li>FR-B2B-001 - <code>market_trend_daily</code> + <code>QueryCells</code> (lọc suppressed) là nguồn dữ liệu duy nhất.</li><li>FR-BILL-001 - vòng đời subscription/tier; <code>b2b_subscription</code> đồng bộ trạng thái thanh toán B2B với khung billing chung.</li><li>FR-INFRA-001 (gateway) - xác thực + rate-limit trước handler.</li><li>Extension/lib: driver <code>pgx</code>; thư viện CSV chuẩn thư viện Go.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Báo cáo có dữ liệu (200)</h3>\n<pre><code class=\"language-json\">{\n  \"scope\": { \"category_ids\": [7], \"platform_ids\": [1], \"from\": \"2026-05-22\", \"to\": \"2026-06-20\" },\n  \"cells\": [\n    { \"category_id\": 7, \"platform_id\": 1, \"day\": \"2026-06-19\", \"median_p\": 318000, \"p25_p\": 249000, \"p75_p\": 405000, \"avg_discount_pct\": 13.80 },\n    { \"category_id\": 7, \"platform_id\": 1, \"day\": \"2026-06-20\", \"median_p\": 320000, \"p25_p\": 250000, \"p75_p\": 410000, \"avg_discount_pct\": 14.30 }\n  ],\n  \"cached_at\": \"2026-06-21T02:15:00Z\",\n  \"source_computed_at\": \"2026-06-21T01:40:00Z\"\n}</code></pre>\n<h3>Vượt entitlement (403)</h3>\n<pre><code class=\"language-json\">{ \"error\": \"scope_exceeded\", \"field\": \"categories\", \"limit\": 3, \"message\": \"Gói basic xem tối đa 3 category. Nâng cấp lên pro để xem thêm.\" }</code></pre>\n<h3>Rỗng-có-lý-do (200)</h3>\n<pre><code class=\"language-json\">{ \"scope\": { \"category_ids\": [991], \"platform_ids\": [1], \"from\": \"2026-06-13\", \"to\": \"2026-06-20\" }, \"cells\": [], \"reason\": \"insufficient_data\", \"cached_at\": \"2026-06-21T02:15:00Z\", \"source_computed_at\": null }</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Báo cáo so sánh chéo category (vd điện tử vs mỹ phẩm) - thêm khi có nhu cầu khách hàng cụ thể.</li><li>Lập lịch gửi báo cáo định kỳ qua email (digest tuần) - tái dùng FR-NOTIF-006 ở slice sau.</li><li>Định giá theo lượt gọi (metered) thay vì tier cố định - cân nhắc khi đã có dữ liệu sử dụng thực.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Báo cáo đọc nguồn raw</td><td>review + test grep</td><td>rò rỉ giá SKU/shop, vỡ k-anonymity</td><td>Chỉ gọi QueryCells của FR-B2B-001 (DEC-B2B-10)</td></tr><tr><td>Phục vụ subscription hết hạn</td><td>TestReport_InactiveSubscription_402</td><td>thất thu</td><td>Kiểm status='active' + expires_at &gt; now()</td></tr><tr><td>Cắt scope im lặng</td><td>entitlement_test</td><td>trải nghiệm sai + mất bán nâng cấp</td><td>Trả 403 rõ ràng (DEC-B2B-13)</td></tr><tr><td>Nội suy ô suppress</td><td>TestBuild_AllSuppressed</td><td>bịa số, rủi ro tái dựng</td><td>Rỗng-có-lý-do, KHÔNG nội suy</td></tr><tr><td>Tin tier do client gửi</td><td>review handler</td><td>leo thang quyền</td><td>Entitlement nạp từ DB caller (§1 #11)</td></tr><tr><td>Export lọt khóa định danh</td><td>TestExport_NoIdentifierColumns</td><td>tái định danh</td><td>Export chỉ chỉ số tổng hợp</td></tr><tr><td>Dữ liệu cũ không rõ độ tươi</td><td>kiểm field source_computed_at</td><td>quyết định trên số cũ</td><td>Gắn cached_at + source_computed_at</td></tr><tr><td>Export cho tier không có quyền</td><td>test can_export</td><td>rò rỉ giá trị tier cao</td><td>Gating can_export=true</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>FR-B2B-002 là điểm thu tiền của dòng B2B: pipeline ẩn danh (FR-B2B-001) thành doanh thu qua báo cáo có gating.</li><li>Quy tắc một câu: tầng báo cáo chỉ đọc ô đã phát hành của market_trend_daily, không bao giờ chạm nguồn chi tiết.</li><li>Giới hạn entitlement được trình bày như điểm chạm bán hàng (403 + thông điệp nâng cấp), không phải lỗi câm.</li><li>Trung thực về độ phủ dữ liệu (rỗng-có-lý-do) quan trọng hơn báo cáo trông đầy đủ - khách hàng B2B trả tiền cho số đúng.</li><li>Độ tươi dữ liệu công khai qua source_computed_at để khách hàng biết báo cáo dựa trên job đêm nào.</li></ul>\n<hr>\n<p><em>Hết FR-B2B-002. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
