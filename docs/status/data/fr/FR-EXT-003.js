window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-EXT-003"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Pipeline tối thiểu hóa <strong>MUST</strong> là van an toàn cuối: nhận dữ liệu từ content script, lọc theo allowlist trường, validate schema, và CHỈ cho tập tối thiểu <code>{platform, productId, price, qty}</code> (+ voucher code hiển thị) rời máy client. Hợp đồng:</p>\n<ol><li>Pipeline <strong>MUST</strong> chỉ sản sinh <code>OutboundPayload</code> chứa tập tối thiểu: <code>platform</code> (enum sàn), <code>items: [{ productId, price, qty }]</code>, <code>vouchers: [{ code, minSpend?, discountText? }]</code> (DEC-EXT-13). Không trường nào khác được phép.</li><li>Lọc <strong>MUST</strong> theo ALLOWLIST (whitelist trường), KHÔNG theo denylist (DEC-EXT-14): chỉ trường được liệt kê tường minh trong <code>allowlist.ts</code> mới đi tiếp; mọi trường khác (kể cả trường lạ tầng trên vô tình thêm) bị loại.</li><li>Pipeline <strong>MUST</strong> là lần lọc THỨ HAI độc lập (DEC-EXT-15, defense in depth): không tin payload từ content script (FR-EXT-002) đã sạch - tự lọc lại từ đầu. Nếu tầng trên có bug để lọt credential, pipeline vẫn chặn.</li><li><code>OutboundPayload</code> <strong>MUST NOT</strong> chứa, dưới bất kỳ tên trường nào: cookie, session token, header xác thực, hay PII (email, số điện thoại, tên, địa chỉ, định danh người dùng sàn thật) (DEC-EXT-13).</li><li>Mọi payload <strong>MUST</strong> qua validator schema runtime (<code>schema.ts</code>) trước khi rời client; payload không khớp schema <strong>MUST</strong> bị từ chối (fail-closed) - KHÔNG có nhánh \"gửi đại khi không chắc\" (DEC-EXT-17).</li><li><code>redact.ts</code> <strong>MUST</strong> quét giá trị chuỗi trong payload tìm dấu hiệu credential/PII (chuỗi giống cookie/token dài, giống email, giống số điện thoại); khi phát hiện, trường đó <strong>MUST</strong> bị loại hoặc cả payload bị từ chối - không để lọt qua kẽ allowlist (ví dụ một <code>productId</code> bị nhồi giá trị lạ).</li><li>Toàn bộ chuẩn hóa/khử trùng <strong>MUST</strong> diễn ra trên client (local-first, DEC-EXT-16, FR-TRUST-002); backend chỉ nhận kết quả đã làm sạch. KHÔNG gửi dữ liệu thô để \"backend lọc giúp\".</li><li><code>productId</code> <strong>MUST</strong> được coi là ID sản phẩm công khai, không phải định danh người dùng; pipeline <strong>MUST</strong> kiểm <code>productId</code> khớp mẫu ID hợp lệ (chữ-số) và từ chối giá trị bất thường (tránh kênh rò rỉ qua trường tưởng vô hại).</li><li><code>price</code>/<code>qty</code>/<code>minSpend</code> <strong>MUST</strong> là số nguyên không âm trong ngưỡng hợp lý; giá trị ngoài ngưỡng -&gt; loại item đó (dữ liệu rác không nên rời client).</li><li>Pipeline <strong>MUST</strong> ghi metric đếm: số payload đi tiếp, số trường bị loại bởi allowlist, số payload bị từ chối bởi schema/redact - để FR-TRUST-003 (audit) và giám sát có bằng chứng định lượng pipeline đang lọc.</li><li>Service worker <strong>MUST</strong> nối <code>onMessage(CART_READ)</code> -&gt; <code>minimize()</code> -&gt; đẩy <code>OutboundPayload</code> vào hàng đợi đồng bộ (FR-EXT-005). Không có đường tắt nào gửi <code>CartReadMessage</code> thẳng ra mạng bỏ qua pipeline.</li><li>Pipeline <strong>MUST</strong> không phụ thuộc trạng thái đăng nhập/định danh: nó chỉ biến đổi dữ liệu, không gắn token. Việc gắn danh tính/JWT cho request là của lớp đồng bộ FR-EXT-005 (và token đó là JWT của SănDeal, KHÔNG phải token sàn).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao allowlist chứ không denylist (DEC-EXT-14)?</strong> Denylist (\"gửi mọi thứ trừ cookie, token...\") an toàn đúng tại thời điểm viết, rồi hỏng ngay khi ai đó thêm một trường mới ở tầng trên - trường đó không nằm trong danh sách cấm nên lọt ra. Allowlist đảo mặc định: chỉ trường được kê tên mới đi tiếp; trường mới mặc nhiên bị chặn cho tới khi có người chủ động thêm vào allowlist (và review). Với dữ liệu cá nhân, mặc định an toàn là bắt buộc.</p>\n<p><strong>Vì sao lọc lần hai độc lập (DEC-EXT-15)?</strong> Content script (FR-EXT-002) đã tối thiểu hóa, nhưng đó là cùng một codebase có thể có bug. Defense in depth: pipeline coi như input có thể bẩn và lọc lại từ đầu. Nếu một thay đổi ở tầng đọc vô tình để lọt trường nhạy cảm, van thứ hai này vẫn chặn trước khi dữ liệu rời máy.</p>\n<p><strong>Vì sao fail-closed qua validator (DEC-EXT-17)?</strong> Khi payload không khớp schema kỳ vọng, lựa chọn an toàn là từ chối, không phải \"gửi đại rồi sửa sau\". Dữ liệu cá nhân đã rời máy là không thu hồi được. Fail-closed đắt hơn cho dev (phải xử lý reject) nhưng là tư thế đúng cho quyền riêng tư.</p>\n<p><strong>Vì sao redact quét cả giá trị, không chỉ tên trường (§1 #6)?</strong> Allowlist chặn theo tên trường, nhưng một kẻ tấn công (hoặc bug) có thể nhồi cookie vào một trường được phép như <code>productId</code>. Quét giá trị tìm pattern credential/PII bịt kẽ hở đó - kiểm cả \"trường nào\" lẫn \"nội dung gì\".</p>\n<p><strong>Vì sao local-first (DEC-EXT-16)?</strong> \"Backend lọc giúp\" nghĩa là dữ liệu thô đã rời máy - đã quá muộn. Triết lý SănDeal (§5.4) là xử lý tối thiểu hóa ngay trên client; chỉ kết quả sạch mới truyền đi. Đây vừa là cam kết kỹ thuật vừa là điểm bán niềm tin.</p>\n<p><strong>Vì sao tách việc gắn token sang FR-EXT-005 (§1 #12)?</strong> Pipeline chỉ làm sạch dữ liệu, không biết gì về danh tính. Việc đính JWT (của SănDeal, không phải sàn) cho request là trách nhiệm của lớp đồng bộ. Tách bạch giữ pipeline đơn giản, dễ audit, và tránh lẫn token SănDeal với token sàn.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>schema.ts (OutboundPayload + validator)</h3>\n<pre><code class=\"language-ts\">// extension/src/pipeline/schema.ts\nexport interface OutboundItem  { productId: string; price: number; qty: number; }\nexport interface OutboundVoucher { code: string; minSpend?: number; discountText?: string; }\nexport interface OutboundPayload {\n  platform: \"shopee\" | \"tiktok\" | \"lazada\";\n  items: OutboundItem[];\n  vouchers: OutboundVoucher[];\n  // KHÔNG trường nào khác. Mọi mở rộng phải sửa schema + allowlist + review.\n}\n\nconst ID_RE = /^[A-Za-z0-9._-]{1,64}$/;\n\nexport function validatePayload(p: unknown): p is OutboundPayload {\n  const o = p as OutboundPayload;\n  if (!o || ![\"shopee\", \"tiktok\", \"lazada\"].includes(o.platform)) return false;\n  if (!Array.isArray(o.items) || !Array.isArray(o.vouchers)) return false;\n  return o.items.every(it =&gt;\n    ID_RE.test(it.productId) &amp;&amp;\n    Number.isInteger(it.price) &amp;&amp; it.price &gt;= 0 &amp;&amp; it.price &lt; 1e12 &amp;&amp;\n    Number.isInteger(it.qty) &amp;&amp; it.qty &gt;= 0 &amp;&amp; it.qty &lt; 1e6);\n}</code></pre>\n<h3>allowlist.ts (whitelist trường)</h3>\n<pre><code class=\"language-ts\">// extension/src/pipeline/allowlist.ts\nconst ITEM_FIELDS    = [\"productId\", \"price\", \"qty\"] as const;\nconst VOUCHER_FIELDS = [\"code\", \"minSpend\", \"discountText\"] as const;\n\nexport function pickItem(raw: Record&lt;string, unknown&gt;): OutboundItem {\n  return pick(raw, ITEM_FIELDS) as OutboundItem;   // CHỈ trường được kê\n}\nfunction pick&lt;T extends string&gt;(o: Record&lt;string, unknown&gt;, keys: readonly T[]) {\n  const out: Record&lt;string, unknown&gt; = {};\n  for (const k of keys) if (k in o) out[k] = o[k];\n  return out;\n}</code></pre>\n<h3>minimize.ts (allowlist -&gt; redact -&gt; validate -&gt; fail-closed)</h3>\n<pre><code class=\"language-ts\">// extension/src/pipeline/minimize.ts\nexport function minimize(msg: CartReadMessage): OutboundPayload | null {\n  const items    = msg.items.map(i =&gt; pickItem(i as any)).filter(isCleanItem);\n  const vouchers = msg.vouchers.map(v =&gt; pickVoucher(v as any)).filter(noCredentialLike);\n  const payload: OutboundPayload = { platform: msg.platform, items, vouchers };\n  if (!validatePayload(payload)) { metrics.rejected(\"schema\"); return null; } // fail-closed\n  if (containsPiiOrCredential(payload)) { metrics.rejected(\"redact\"); return null; }\n  metrics.passed(items.length);\n  return payload;\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>OutboundPayload</code> chỉ có khóa <code>platform</code>, <code>items</code>, <code>vouchers</code>; <code>items</code> chỉ <code>{productId,price,qty}</code>; <code>vouchers</code> chỉ <code>{code,minSpend?,discountText?}</code> (test introspection).</li><li>Bơm <code>CartReadMessage</code> có trường thừa (ví dụ <code>cookie</code>, <code>userId</code>, <code>email</code> nhồi vào item) -&gt; allowlist loại; <code>OutboundPayload</code> không chứa chúng.</li><li>Lọc là allowlist: thêm một khóa lạ mới vào input -&gt; mặc nhiên bị loại, không cần sửa code lọc (test khẳng định khóa lạ biến mất).</li><li>Payload không khớp schema (thiếu <code>productId</code>, <code>price</code> âm, <code>platform</code> lạ) -&gt; <code>minimize</code> trả <code>null</code> (fail-closed), metric <code>rejected(\"schema\")</code> tăng.</li><li>Giá trị nghi credential/PII nhồi vào trường được phép (ví dụ <code>productId</code> = chuỗi cookie dài / email) -&gt; bị <code>redact</code>/<code>validate</code> loại; metric <code>rejected(\"redact\")</code> hoặc item bị lọc.</li><li><code>price</code>/<code>qty</code> ngoài ngưỡng (âm, quá lớn) -&gt; item bị loại, không rời client.</li><li>Grep <code>pipeline/**</code>: KHÔNG có denylist kiểu \"gửi mọi thứ trừ\"; chỉ pick theo danh sách trường.</li><li>Không tồn tại đường gửi <code>CartReadMessage</code> ra mạng bỏ qua <code>minimize</code> (grep: mọi outbound đi qua pipeline -&gt; queue FR-EXT-005).</li><li>Metric đếm passed / allowlist-dropped / rejected hoạt động.</li><li>Happy path: input giỏ hợp lệ -&gt; <code>OutboundPayload</code> đúng tập tối thiểu, validate pass.</li><li><code>npm test</code> xanh; <code>tsc --noEmit</code> sạch.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-ts\">// extension/test/no-pii-leak.test.ts\nimport { minimize } from \"../src/pipeline/minimize\";\n\ntest(\"trường thừa (cookie/email/userId) bị loại khỏi OutboundPayload\", () =&gt; {\n  const msg: any = {\n    type: \"CART_READ\", platform: \"shopee\",\n    items: [{ productId: \"90112\", price: 89000, qty: 1, cookie: \"SPC_=abc\", userId: 7, email: \"a@b.vn\" }],\n    vouchers: []\n  };\n  const out = minimize(msg)!;\n  const flat = JSON.stringify(out).toLowerCase();\n  expect(flat).not.toMatch(/cookie|userid|email|@/);\n  expect(out.items[0]).toEqual({ productId: \"90112\", price: 89000, qty: 1 });\n});\n\ntest(\"credential nhồi vào productId bị từ chối (fail-closed)\", () =&gt; {\n  const msg: any = { type: \"CART_READ\", platform: \"shopee\",\n    items: [{ productId: \"SPC_SESSION_eyJhbGciOi...verylong\", price: 1, qty: 1 }], vouchers: [] };\n  expect(minimize(msg)).toBeNull();   // ID_RE/redact loại\n});</code></pre>\n<pre><code class=\"language-ts\">// extension/test/allowlist.test.ts\ntest(\"khóa lạ mới mặc nhiên bị loại (allowlist, không cần sửa code)\", () =&gt; {\n  const out = minimize({ type:\"CART_READ\", platform:\"shopee\",\n    items:[{ productId:\"1\", price:1, qty:1, brandNewField:\"x\" } as any], vouchers:[] })!;\n  expect(\"brandNewField\" in out.items[0]).toBe(false);\n});</code></pre>\n<pre><code class=\"language-ts\">// extension/test/minimize.test.ts\ntest(\"schema lỗi → null (fail-closed) + metric\", () =&gt; {\n  expect(minimize({ type:\"CART_READ\", platform:\"vng\" as any, items:[], vouchers:[] })).toBeNull();\n});\n\ntest(\"happy path đúng tập tối thiểu\", () =&gt; {\n  const out = minimize({ type:\"CART_READ\", platform:\"shopee\",\n    items:[{ productId:\"90112\", price:89000, qty:1 }], vouchers:[{ code:\"FREESHIP\" }] })!;\n  expect(out).toEqual({ platform:\"shopee\",\n    items:[{ productId:\"90112\", price:89000, qty:1 }], vouchers:[{ code:\"FREESHIP\" }] });\n});</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>schema.ts</code> (OutboundPayload + validatePayload) -&gt; <code>allowlist.ts</code> (pick theo danh sách trường) -&gt; <code>redact.ts</code> (quét giá trị credential/PII) -&gt; <code>minimize.ts</code> (allowlist -&gt; redact -&gt; validate, fail-closed) -&gt; nối <code>service-worker.ts</code> <code>onMessage(CART_READ) -&gt; minimize -&gt; queue</code> -&gt; tests. Lớp đồng bộ (FR-EXT-005) nhận <code>OutboundPayload</code> đã sạch từ hàng đợi và đính JWT SănDeal. Mọi mở rộng trường trong tương lai phải sửa cả <code>schema.ts</code> lẫn <code>allowlist.ts</code> và đi qua review - đó là điểm kiểm soát duy nhất.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>FR-EXT-002</strong> - content script Shopee cung cấp <code>CartReadMessage</code> (đã tối thiểu hóa lần một).</li><li><strong>FR-EXT-001</strong> - service worker + messaging + storage làm khung nối pipeline.</li><li><strong>FR-EXT-005 (downstream)</strong> - lớp đồng bộ nhận <code>OutboundPayload</code> sạch, đính JWT, gửi backend.</li><li><strong>FR-TRUST-002 (downstream)</strong> - chính sách local-first + tối thiểu hóa được hiện thực ở đây.</li><li><strong>FR-TRUST-003 (downstream)</strong> - audit độc lập dùng metric + test pipeline làm bằng chứng không gửi cookie/PII.</li><li><strong>FR-COMPLY-005</strong> - cưỡng chế no-cleartext + token-not-on-server; pipeline là điểm thực thi phía client.</li><li><strong>FR-CART-002 / FR-AFFIL-004 (downstream, P2)</strong> - cart_snapshot + guardrails affiliate dựa dữ liệu sạch từ pipeline.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Vào (CartReadMessage, có thể \"bẩn\") -&gt; ra (OutboundPayload, sạch)</h3>\n<pre><code class=\"language-ts\">// VÀO (giả định tầng trên lỡ thêm trường thừa)\n{ type:\"CART_READ\", platform:\"shopee\",\n  items:[{ productId:\"90112\", price:89000, qty:1, cookie:\"SPC_=x\", shopId:55 }],\n  vouchers:[{ code:\"FREESHIPXTRA\", minSpend:0, discountText:\"đến 15k\", internalId:9 }] }\n\n// RA (sau minimize: chỉ trường allowlist, đã validate)\n{ platform:\"shopee\",\n  items:[{ productId:\"90112\", price:89000, qty:1 }],\n  vouchers:[{ code:\"FREESHIPXTRA\", minSpend:0, discountText:\"đến 15k\" }] }</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Có hash <code>productId</code> thêm một lớp trước khi gửi hay không - không cần (productId là công khai); xét lại nếu B2B (FR-B2B-001) yêu cầu ẩn danh mạnh hơn.</li><li>Nén/gộp payload nhiều lần đọc thành batch - tối ưu ở FR-EXT-005 (đồng bộ), không thuộc pipeline lọc.</li><li>Mở rộng allowlist cho trường mới (ví dụ rating) - chỉ khi có FR tiêu thụ; thêm phải qua review.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Dùng denylist thay allowlist</td><td>grep test (§4 #7)</td><td>trường mới lọt ra backend</td><td>Allowlist pick-by-name (DEC-EXT-14)</td></tr><tr><td>Cookie/token/PII trong payload</td><td>no-pii-leak test</td><td>rò rỉ dữ liệu cá nhân, chế tài PDPL</td><td>Allowlist + redact + validate (§1 #4/#6)</td></tr><tr><td>Credential nhồi vào trường được phép</td><td>redact test</td><td>rò qua kẽ allowlist</td><td>Quét giá trị, không chỉ tên trường (§1 #6)</td></tr><tr><td>Payload lệch schema vẫn gửi (fail-open)</td><td>minimize trả null test</td><td>gửi dữ liệu rác/nhạy cảm</td><td>Fail-closed qua validator (DEC-EXT-17)</td></tr><tr><td>Tin tầng content không lọc lại</td><td>review + defense-in-depth test</td><td>bug tầng trên lọt thẳng</td><td>Lọc lần hai độc lập (DEC-EXT-15)</td></tr><tr><td>Đường tắt gửi CartReadMessage thô</td><td>grep outbound (§4 #8)</td><td>bỏ qua van an toàn</td><td>Mọi outbound qua minimize -&gt; queue (§1 #11)</td></tr><tr><td>Gửi thô để \"backend lọc\"</td><td>review</td><td>dữ liệu thô đã rời máy</td><td>Local-first, lọc trên client (DEC-EXT-16)</td></tr><tr><td>productId bất thường (kênh ẩn)</td><td>ID_RE validate</td><td>rò qua trường vô hại</td><td>Kiểm mẫu ID + redact (§1 #8)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Pipeline là van an toàn cuối trước khi dữ liệu rời máy - allowlist (mặc định an toàn) + lọc lần hai + fail-closed là ba trụ của nó.</li><li>Quét giá trị (không chỉ tên trường) bịt kẽ hở \"nhồi credential vào trường được phép\" - kiểm cả tên lẫn nội dung.</li><li>Local-first nghĩa là dữ liệu thô KHÔNG BAO GIỜ rời máy; chỉ kết quả sạch truyền đi - điểm bán niềm tin của §5.4.</li><li>Mọi mở rộng trường tương lai phải sửa schema + allowlist và qua review: đây là điểm kiểm soát quyền riêng tư duy nhất, cố ý làm \"khó nới lỏng\".</li><li>Metric đếm passed/dropped/rejected cung cấp bằng chứng định lượng cho audit độc lập FR-TRUST-003.</li><li>Token gắn cho request là JWT của SănDeal (FR-EXT-005), KHÔNG phải token sàn - pipeline không chạm danh tính.</li></ul>\n<hr>\n<p><em>Hết FR-EXT-003. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
