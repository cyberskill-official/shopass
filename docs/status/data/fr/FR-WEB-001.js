window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-WEB-001"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Web app SănDeal <strong>MUST</strong> là một dự án Next.js 14 (App Router) với layout shell dashboard, tích hợp xác thực bằng JWT do FR-AUTH-002 phát, gọi backend qua một API client tập trung trỏ API Gateway (FR-INFRA-001), và TUYỆT ĐỐI không lưu token nhạy cảm trong web storage hay tự xác thực mật khẩu. Hợp đồng:</p>\n<ol><li>Dự án <strong>MUST</strong> dùng Next.js 14 App Router (React Server Components) với hai route group tách biệt: <code>(auth)</code> cho trang công khai đăng nhập và <code>(app)</code> cho khu vực đã đăng nhập (DEC-WEB-01).</li><li><code>app/layout.tsx</code> (root layout) <strong>MUST</strong> render khung HTML gốc + provider; <code>app/(app)/layout.tsx</code> <strong>MUST</strong> bọc <code>components/app-shell.tsx</code> (header, điều hướng, vùng nội dung) cho mọi trang trong khu vực đăng nhập.</li><li>Access token (JWT ngắn hạn từ FR-AUTH-002) <strong>MUST</strong> chỉ giữ trong bộ nhớ runtime (biến/React state/closure), KHÔNG ghi vào <code>localStorage</code> hay <code>sessionStorage</code> (DEC-WEB-02). Refresh token <strong>MUST</strong> nằm trong cookie <code>httpOnly; Secure; SameSite=Lax</code>, không đọc được từ JavaScript.</li><li>Web app <strong>MUST NOT</strong> tự xác thực mật khẩu, KHÔNG lưu <code>pwd_hash</code>, KHÔNG tự sinh JWT: trang login chỉ gửi thông tin đăng nhập tới auth-svc (qua gateway) và nhận token (DEC-WEB-03). Mọi kiểm tra credential thuộc FR-AUTH-001/002.</li><li><code>lib/api.ts</code> <strong>MUST</strong> là client gọi backend duy nhất: tự gắn header <code>Authorization: Bearer &lt;access_token&gt;</code>, đọc base URL từ biến môi trường <code>NEXT_PUBLIC_API_BASE_URL</code> trỏ gateway (DEC-WEB-04). KHÔNG hardcode URL backend.</li><li>Khi một request trả <code>401</code>, <code>lib/api.ts</code> <strong>MUST</strong> thử làm mới token đúng MỘT lần (gọi luồng refresh của FR-AUTH-002), rồi phát lại request; nếu vẫn <code>401</code>, đăng xuất và chuyển hướng <code>/login</code>. KHÔNG lặp refresh vô hạn.</li><li><code>middleware.ts</code> (Next.js middleware) <strong>MUST</strong> chặn các route trong group <code>(app)</code>: nếu không có phiên hợp lệ (thiếu refresh cookie), redirect <code>307</code> về <code>/login</code> kèm tham số <code>?next=</code> để quay lại sau đăng nhập (DEC-WEB-05).</li><li>Các trang trong <code>(app)</code> <strong>MUST NOT</strong> render dữ liệu cá nhân phía server khi thiếu phiên hợp lệ: không gọi API người dùng nếu middleware chưa xác nhận phiên (defense in depth cùng #7).</li><li>Cấu hình <strong>MUST</strong> đọc base URL gateway từ env (<code>.env.example</code> ghi rõ <code>NEXT_PUBLIC_API_BASE_URL</code>), không nhúng cứng; build dev và prod dùng cùng client, khác env.</li><li><code>app/(app)/dashboard/page.tsx</code> <strong>MUST</strong> tồn tại như shell tối thiểu (khung trống, chỗ chứa cho FR-WEB-003/004) để xác nhận luồng đăng nhập tới khu vực bảo vệ hoạt động đầu-cuối.</li><li>Dự án <strong>MUST</strong> vượt <code>npx tsc --noEmit</code> sạch và <code>npm test</code> xanh; cấu hình TypeScript strict (<code>strict: true</code>).</li><li>Web app <strong>SHOULD</strong> đặt nền tảng i18n locale <code>vi-VN</code> mặc định (khớp <code>app_user.locale</code> ở §3.4) để các FR sau gắn chuỗi đa ngôn ngữ khi mở SEA.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao App Router + route group (DEC-WEB-01)?</strong> Tách <code>(auth)</code> công khai khỏi <code>(app)</code> bảo vệ làm ranh giới bảo mật rõ ràng ở tầng định tuyến: một middleware canh đúng group <code>(app)</code>, không phải rải kiểm tra trong từng trang. React Server Components giảm JS gửi xuống client cho phần landing SEO (FR-WEB-002) - quan trọng cho điểm SEO và tốc độ.</p>\n<p><strong>Vì sao token in-memory + refresh httpOnly (DEC-WEB-02)?</strong> Định vị lõi của SănDeal là niềm tin hậu-Honey và cam kết no-cleartext (§3.8). Token trong <code>localStorage</code> đọc được bằng JavaScript - một lỗ XSS là đánh cắp phiên hàng loạt. Access token ngắn hạn giữ trong bộ nhớ (mất khi reload, lấy lại qua refresh) + refresh token trong cookie <code>httpOnly</code> (JS không chạm) là mẫu chuẩn giảm bề mặt tấn công token.</p>\n<p><strong>Vì sao web không tự xác thực mật khẩu (DEC-WEB-03)?</strong> Danh tính phải có một nguồn sự thật duy nhất là auth-svc (FR-AUTH-001/002). Nếu web cũng hash/đối chiếu mật khẩu thì nhân đôi bề mặt tấn công, lệch logic, và vi phạm no-cleartext nếu lỡ lưu. Web chỉ chuyển tiếp thông tin đăng nhập tới auth-svc và tiêu thụ JWT trả về.</p>\n<p><strong>Vì sao một API client tập trung (DEC-WEB-04)?</strong> Gắn Bearer, refresh-on-401, đọc base URL từ env nên nằm một chỗ. Rải logic gọi API khắp các trang dẫn tới quên gắn token, hardcode URL, hoặc refresh không nhất quán. <code>lib/api.ts</code> là điểm thắt cho mọi tương tác backend, dễ test và audit.</p>\n<p><strong>Vì sao refresh đúng một lần khi 401 (§1 #6)?</strong> Token có thể hết hạn giữa phiên; một lần refresh rồi phát lại request là đủ. Lặp refresh vô hạn khi backend thực sự từ chối tạo vòng lặp và che lỗi đăng nhập thật. Một lần, rồi đăng xuất sạch, là hành vi đúng.</p>\n<p><strong>Vì sao middleware guard ở tầng định tuyến (DEC-WEB-05)?</strong> Chặn sớm ở middleware (trước khi trang render) rẻ và an toàn hơn để mỗi trang tự kiểm tra. Thiếu phiên thì redirect ngay, không bao giờ chạm API người dùng - tránh rò rỉ dữ liệu phía server cho phiên không hợp lệ.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>API client tập trung (lib/api.ts)</h3>\n<pre><code class=\"language-ts\">// web/lib/api.ts\nconst BASE = process.env.NEXT_PUBLIC_API_BASE_URL!; // trỏ gateway (FR-INFRA-001)\n\nlet accessToken: string | null = null; // CHỈ in-memory (DEC-WEB-02)\nexport function setAccessToken(t: string | null) { accessToken = t; }\n\nexport async function apiFetch(path: string, init: RequestInit = {}): Promise&lt;Response&gt; {\n  const headers = new Headers(init.headers);\n  if (accessToken) headers.set(\"Authorization\", `Bearer ${accessToken}`);\n  let res = await fetch(`${BASE}${path}`, { ...init, headers });\n\n  if (res.status === 401) {\n    const refreshed = await tryRefreshOnce();      // gọi FR-AUTH-002 refresh\n    if (!refreshed) { await logout(); throw new UnauthorizedError(); }\n    headers.set(\"Authorization\", `Bearer ${accessToken}`);\n    res = await fetch(`${BASE}${path}`, { ...init, headers }); // phát lại MỘT lần\n  }\n  return res;\n}</code></pre>\n<h3>Refresh + session (lib/auth.ts)</h3>\n<pre><code class=\"language-ts\">// web/lib/auth.ts\n// Refresh token nằm trong httpOnly cookie; trao đổi qua route handler nội bộ,\n// JavaScript KHÔNG đọc giá trị cookie (DEC-WEB-02).\nexport async function tryRefreshOnce(): Promise&lt;boolean&gt; {\n  const res = await fetch(\"/api/auth/refresh\", { method: \"POST\" }); // route handler đặt/đọc cookie\n  if (!res.ok) return false;\n  const { accessToken } = await res.json();\n  setAccessToken(accessToken); // lưu in-memory\n  return true;\n}\n\nexport async function logout(): Promise&lt;void&gt; {\n  setAccessToken(null);\n  await fetch(\"/api/auth/logout\", { method: \"POST\" }); // xóa httpOnly cookie\n}</code></pre>\n<h3>Middleware guard (middleware.ts)</h3>\n<pre><code class=\"language-ts\">// web/middleware.ts\nimport { NextResponse, type NextRequest } from \"next/server\";\n\nexport function middleware(req: NextRequest) {\n  const hasSession = req.cookies.has(\"sd_refresh\"); // httpOnly refresh cookie\n  if (!hasSession) {\n    const url = new URL(\"/login\", req.url);\n    url.searchParams.set(\"next\", req.nextUrl.pathname);\n    return NextResponse.redirect(url, 307); // DEC-WEB-05\n  }\n  return NextResponse.next();\n}\n\nexport const config = { matcher: [\"/dashboard/:path*\", \"/wishlist/:path*\", \"/alerts/:path*\"] };</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>cd web &amp;&amp; npx tsc --noEmit</code> sạch (TypeScript strict bật) và <code>npm test</code> xanh.</li><li>Cây thư mục có route group <code>(auth)</code> và <code>(app)</code>; <code>app/(app)/layout.tsx</code> bọc <code>app-shell</code>.</li><li><code>setAccessToken</code> lưu token in-memory; grep <code>web/**</code>: KHÔNG có <code>localStorage</code>/<code>sessionStorage</code> chứa token.</li><li>Trang login gọi auth-svc qua API client; grep: KHÔNG có hashing mật khẩu, KHÔNG có <code>pwd_hash</code> ở tầng web.</li><li><code>apiFetch</code> gắn <code>Authorization: Bearer</code> khi có access token và đọc base URL từ <code>NEXT_PUBLIC_API_BASE_URL</code>.</li><li>Khi backend trả <code>401</code>, <code>apiFetch</code> gọi refresh đúng MỘT lần rồi phát lại; nếu vẫn <code>401</code>, đăng xuất và không lặp tiếp.</li><li><code>middleware.ts</code> redirect <code>307</code> về <code>/login?next=...</code> khi thiếu cookie <code>sd_refresh</code>; cho qua khi có.</li><li>Truy cập <code>/dashboard</code> chưa đăng nhập -&gt; bị redirect <code>/login</code>; sau đăng nhập (cookie có) -&gt; render shell dashboard.</li><li><code>.env.example</code> chứa <code>NEXT_PUBLIC_API_BASE_URL</code>; không có URL backend hardcode trong mã.</li><li>Refresh token chỉ tồn tại trong cookie <code>httpOnly; Secure; SameSite</code>; không khóa nào tên token đọc được từ JS phía client.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-ts\">// web/test/api-client.test.ts\nimport { apiFetch, setAccessToken } from \"../lib/api\";\n\ntest(\"gắn Authorization: Bearer khi có access token\", async () =&gt; {\n  setAccessToken(\"abc.def.ghi\");\n  const spy = mockFetchOk();\n  await apiFetch(\"/v1/wishlists\");\n  const headers = spy.mock.calls[0][1].headers as Headers;\n  expect(headers.get(\"Authorization\")).toBe(\"Bearer abc.def.ghi\");\n});\n\ntest(\"401 → refresh đúng một lần rồi phát lại; vẫn 401 → logout, không lặp\", async () =&gt; {\n  setAccessToken(\"expired\");\n  const fetchSpy = mockFetchSequence([401, /* refresh */ 200, 401]); // request, refresh, retry\n  await expect(apiFetch(\"/v1/wishlists\")).rejects.toThrow(/Unauthorized/);\n  // request gốc + 1 refresh + 1 retry = 3 lần fetch, KHÔNG hơn\n  expect(fetchSpy).toHaveBeenCalledTimes(3);\n});\n\ntest(\"KHÔNG ghi token vào web storage\", async () =&gt; {\n  setAccessToken(\"xyz\");\n  expect(globalThis.localStorage?.getItem?.(\"accessToken\")).toBeFalsy();\n});</code></pre>\n<pre><code class=\"language-ts\">// web/test/middleware-guard.test.ts\nimport { middleware } from \"../middleware\";\n\ntest(\"thiếu phiên → redirect 307 /login?next=\", () =&gt; {\n  const req = mockRequest(\"/dashboard\", { cookies: {} });\n  const res = middleware(req);\n  expect(res.status).toBe(307);\n  expect(res.headers.get(\"location\")).toContain(\"/login?next=%2Fdashboard\");\n});\n\ntest(\"có phiên → cho qua (next)\", () =&gt; {\n  const req = mockRequest(\"/dashboard\", { cookies: { sd_refresh: \"tok\" } });\n  const res = middleware(req);\n  expect(res.headers.get(\"location\")).toBeNull();\n});</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>package.json</code> + <code>next.config.mjs</code> + <code>.env.example</code> (cấu hình nền) -&gt; <code>app/layout.tsx</code> + <code>components/app-shell.tsx</code> (khung hiển thị) -&gt; <code>lib/auth.ts</code> (session, refresh httpOnly) -&gt; <code>lib/api.ts</code> (client gắn Bearer, refresh-on-401) -&gt; <code>middleware.ts</code> (guard group (app)) -&gt; <code>app/(auth)/login/page.tsx</code> + <code>app/(app)/dashboard/page.tsx</code> (luồng đầu-cuối) -&gt; tests. Route handler <code>/api/auth/refresh</code> và <code>/api/auth/logout</code> đặt/xóa httpOnly cookie và trao access token in-memory; chúng là cầu nối tới luồng JWT của FR-AUTH-002 qua gateway (FR-INFRA-001). Không thêm state management lib ở slice này; React state + closure đủ cho shell.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>FR-AUTH-002</strong> - phát JWT access + refresh; web app tiêu thụ token, không tự sinh (depends_on cứng).</li><li><strong>FR-INFRA-001 (gateway)</strong> - mọi gọi backend đi qua gateway; <code>NEXT_PUBLIC_API_BASE_URL</code> trỏ tới đây.</li><li><strong>FR-WEB-002 (downstream)</strong> - landing SEO mọc trên route group <code>(auth)</code>/công khai của scaffold này.</li><li><strong>FR-WEB-003 / FR-WEB-004 (downstream)</strong> - biểu đồ giá và wishlist/alert UI render trong shell <code>(app)</code>, gọi backend qua <code>lib/api.ts</code>.</li><li><strong>FR-WEB-005 (downstream)</strong> - BFF GraphQL là một backend thay thế REST cho client; <code>lib/api.ts</code> mở rộng để gọi GraphQL endpoint.</li><li>Lib: <code>next</code> 14, <code>react</code> 18, <code>typescript</code> strict; test qua Jest/Vitest + jsdom.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Trang login gọi auth-svc (không tự hash mật khẩu)</h3>\n<pre><code class=\"language-ts\">// app/(auth)/login/page.tsx (đoạn submit)\nconst res = await fetch(\"/api/auth/login\", {            // route handler -&gt; auth-svc qua gateway\n  method: \"POST\",\n  headers: { \"Content-Type\": \"application/json\" },\n  body: JSON.stringify({ email, password }),            // gửi nguyên tới auth-svc; web KHÔNG hash\n});\nif (res.ok) {\n  const { accessToken } = await res.json();             // refresh token đã đặt vào httpOnly cookie phía route handler\n  setAccessToken(accessToken);                          // lưu in-memory\n  router.push(nextParam ?? \"/dashboard\");\n}</code></pre>\n<h3>Biến môi trường (.env.example)</h3>\n<pre><code>NEXT_PUBLIC_API_BASE_URL=https://api.sandeal.vn</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Chọn thư viện UI/component (giữ tối thiểu ở slice 1) - quyết khi FR-WEB-003/004 cần biểu đồ và bảng.</li><li>SSR per-request cho dashboard cá nhân hóa vs CSR - đo sau; slice 1 dashboard là shell.</li><li>State management toàn cục (nếu wishlist/alert phình) - thêm khi FR-WEB-004 cần.</li><li>Đa ngôn ngữ đầy đủ (i18n routing) khi mở SEA - đặt nền locale vi-VN trước (§1 #12).</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Token lưu localStorage</td><td>grep test + review</td><td>XSS đánh cắp phiên</td><td>In-memory access + httpOnly refresh (DEC-WEB-02)</td></tr><tr><td>Web tự hash/đối chiếu mật khẩu</td><td>grep hashing/pwd_hash</td><td>nhân đôi bề mặt tấn công</td><td>Ủy quyền auth-svc (DEC-WEB-03)</td></tr><tr><td>Hardcode URL backend</td><td>grep http(s):// trong mã</td><td>lệch env dev/prod</td><td>Đọc NEXT_PUBLIC_API_BASE_URL (DEC-WEB-04)</td></tr><tr><td>Refresh lặp vô hạn khi 401</td><td>đếm số fetch trong test</td><td>vòng lặp, che lỗi auth</td><td>Refresh đúng một lần rồi logout (§1 #6)</td></tr><tr><td>Route bảo vệ render khi chưa đăng nhập</td><td>middleware-guard test</td><td>rò rỉ dữ liệu phía server</td><td>Middleware redirect + không gọi API (DEC-WEB-05)</td></tr><tr><td>Quên gắn Bearer ở một số call</td><td>api-client test</td><td>request 401 ngoài ý muốn</td><td>Một client tập trung lib/api.ts (DEC-WEB-04)</td></tr><tr><td>tsc lỗi kiểu</td><td>npx tsc --noEmit</td><td>build vỡ</td><td>strict bật, sửa kiểu trước merge</td></tr><tr><td>Cookie refresh thiếu Secure/SameSite</td><td>review route handler</td><td>CSRF/đánh cắp</td><td>Đặt httpOnly; Secure; SameSite=Lax</td></tr><tr><td>Đăng nhập xong không quay lại trang đích</td><td>thiếu ?next=</td><td>UX kém</td><td>middleware gắn next; login đọc next</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Đây là khung nền của toàn bộ web app SănDeal; landing SEO, biểu đồ giá, wishlist/alert, BFF GraphQL đều mọc trên scaffold này.</li><li>Web app là đường sống độc lập extension: nếu sàn chặn extension hay Chrome gỡ extension kiểu Honey (§5.2), web vẫn phục vụ giá trị lõi.</li><li>Token in-memory + refresh httpOnly là mẫu chuẩn no-cleartext; cam kết \"token không rời client an toàn\" được giữ ở cả phía web, không chỉ extension.</li><li>Web không tự xác thực mật khẩu - một nguồn sự thật danh tính (auth-svc) giảm bề mặt tấn công và tránh lệch logic.</li><li>API client tập trung là điểm thắt cho gắn token + refresh + base URL env, dễ audit và mở rộng sang GraphQL (FR-WEB-005).</li></ul>\n<hr>\n<p><em>Hết FR-WEB-001. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
