window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DEAL-005"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service ML (ml-svc) <strong>MUST</strong> nâng cấp bộ dự đoán đáy giá từ Prophet baseline (FR-DEAL-004) lên LightGBM cho các SKU có lịch sử dài, qua một feature store thống nhất giữa train và serve, một nhãn <code>future_min_price_14d</code> dựng bằng forward-join chỉ lúc train, và một cổng đủ điều kiện <code>&gt;=180</code> ngày. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> định nghĩa bảng feature store <code>feature_store</code> với khóa chính <code>(product_id, as_of_date)</code> và đúng 11 cột đặc trưng: <code>day_of_month</code>, <code>is_double_date</code>, <code>days_to_next_double_date</code>, <code>is_payday_window</code>, <code>trailing_min_30</code>, <code>trailing_min_60</code>, <code>trailing_min_90</code>, <code>price_vs_median90</code>, <code>volatility_30d</code>, <code>category_seasonality</code>, <code>flash_sale_flag</code>, <code>platform_id</code> (cùng cột nhãn <code>future_min_price_14d</code> nullable).</li><li><strong>MUST</strong> tính mọi đặc trưng từ <code>price_daily</code>/<code>price_snapshot</code> (FR-PRICE-002) cộng <code>tracked_product.category_id</code> và <code>platform_id</code>, tất cả tính tới mốc <code>as_of_date</code> và KHÔNG dùng bất kỳ điểm dữ liệu nào sau <code>as_of_date</code> (đặc trưng chỉ nhìn về quá khứ).</li><li><strong>MUST</strong> đặc tả từng đặc trưng: <code>day_of_month</code> = ngày trong tháng của <code>as_of_date</code>; <code>is_double_date</code> = 1 khi ngày bằng tháng (<code>d == m</code>, ví dụ 6/6, 12/12); <code>days_to_next_double_date</code> = số ngày tới double-date kế tiếp; <code>is_payday_window</code> = 1 khi <code>as_of_date</code> rơi vào cửa sổ trả lương; <code>trailing_min_30/60/90</code> = giá nhỏ nhất trong 30/60/90 ngày trước <code>as_of_date</code>; <code>price_vs_median90</code> = <code>close_p / median90</code> tại <code>as_of_date</code>; <code>volatility_30d</code> = độ lệch chuẩn log-return 30 ngày; <code>category_seasonality</code> = chỉ số mùa vụ gộp theo <code>category_id</code>; <code>flash_sale_flag</code> = 1 nếu có flash sale tại <code>as_of_date</code>; <code>platform_id</code> = nền tảng của SKU.</li><li><strong>MUST</strong> dựng nhãn <code>future_min_price_14d</code> bằng forward-join: với mỗi dòng <code>(product_id, as_of_date)</code>, lấy <code>min(min_p)</code> của <code>price_daily</code> trong cửa sổ <code>(as_of_date, as_of_date + 14 ngày]</code>. Phép này nhìn về phía trước nên CHỈ chạy lúc train trên dữ liệu lịch sử đã hoàn tất.</li><li><strong>MUST</strong> ở thời điểm serve, KHÔNG bao giờ tính nhãn và KHÔNG chạm bất kỳ dữ liệu nào sau <code>as_of_date</code>: serve chỉ đọc vector đặc trưng (cột nhãn để trống) rồi đưa qua mô hình. Không rò tương lai (DEC-DEAL-43).</li><li><strong>MUST</strong> train và serve đọc đặc trưng từ CÙNG một nguồn là bảng <code>feature_store</code> (hoặc cùng một hàm <code>build_features</code> ghi vào bảng đó), bảo đảm vector đặc trưng giống hệt nhau - không có train/serve skew (DEC-DEAL-42).</li><li><strong>MUST</strong> đặt cổng đủ điều kiện: chỉ train và serve LightGBM cho SKU có <code>&gt;=180</code> ngày lịch sử; SKU dưới ngưỡng <strong>MUST</strong> fallback về Prophet baseline (FR-DEAL-004), và nếu cả Prophet cũng chưa đủ thì về category prior (FR-DEAL-002) (DEC-DEAL-40).</li><li><strong>MUST</strong> train một mô hình hồi quy <code>lightgbm.LGBMRegressor</code> với mục tiêu <code>future_min_price_14d</code>, dùng đúng 11 đặc trưng ở #1, và <code>random_state</code> cố định để kết quả tái lập (deterministic seed).</li><li><strong>MUST</strong> suy ra tín hiệu <code>p_bottom_14d</code> = P(bottom within 14d) từ giá đáy dự đoán so với giá hiện tại cộng một biên: <code>p_bottom_14d</code> cao khi <code>predicted_min &gt;= current_price * (1 - margin)</code>, nghĩa là 14 ngày tới khó có giá thấp hơn đáng kể hiện tại (giờ đang gần đáy).</li><li><strong>MUST</strong> phát ra cùng một hợp đồng tín hiệu như FR-DEAL-004: cùng tên trường <code>p_bottom_14d</code> (thực <code>[0,1]</code>), cùng <code>expected_min_14d</code>, cùng <code>horizon_days = 14</code>, chỉ khác <code>model_kind = 'lgbm'</code>. FR-DEAL-006 tiêu thụ tín hiệu này mà KHÔNG cần biết mô hình nào sinh ra (bất khả tri theo mô hình, DEC-DEAL-44).</li><li><strong>MUST</strong> ghi dự đoán vào bảng chia sẻ <code>price_forecast</code> (cùng bảng FR-DEAL-004 ghi) với <code>model_kind = 'lgbm'</code>, <code>p_bottom_14d</code>, <code>expected_min_14d</code>, <code>as_of_date</code>, <code>horizon_days = 14</code>.</li><li><strong>MUST</strong> bảo đảm tính tái lập: cùng một <code>feature_store</code> đầu vào và cùng <code>random_state</code> cho ra cùng mô hình và cùng dự đoán (seed cố định, không phụ thuộc thứ tự đọc).</li><li><strong>SHOULD</strong> phát OTel metric: <code>lgbm_predict_total{platform_id}</code> (counter), <code>lgbm_fallback_prophet_total{reason}</code> (counter khi SKU &lt;180 ngày), <code>feature_store_rows_built_total</code> (counter), <code>lgbm_train_mae</code> (gauge MAE trên tập kiểm định).</li><li><strong>MUST</strong> xử lý đặc trưng thiếu (NULL) trong cửa sổ trailing: điền theo quy ước rõ ràng (ví dụ <code>trailing_min_*</code> lùi về giá sớm nhất có sẵn, <code>category_seasonality</code> về trung tính khi category thưa) thay vì để NaN rò vào mô hình một cách ngầm định.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao LightGBM thay vì Prophet ở mốc &gt;=180 ngày (DEC-DEAL-40)?</strong> Prophet mô hình hóa xu hướng cộng mùa vụ theo từng thành phần độc lập, nó không bắt được tương tác giữa các đặc trưng. Nhưng đáy giá thật sự sinh ra từ tương tác: một double-date rơi đúng cửa sổ payday cộng với volatility cao thường tạo đáy sâu hơn tổng riêng lẻ từng yếu tố. Gradient boosting học chính các tương tác bậc cao này. Khi SKU đã có &gt;=180 ngày dữ liệu thì mẫu đủ dày để LightGBM không overfit, và nó thắng Prophet ở đúng những SKU quan trọng nhất (lịch sử dài, lượt theo dõi cao).</p>\n<p><strong>Vì sao một feature store thực thể hóa (DEC-DEAL-42)?</strong> Lỗi nguy hiểm nhất của một mô hình triển khai là train/serve skew: đặc trưng tính lúc train (theo lô, bằng pandas) khác tinh vi so với lúc serve (theo từng SKU, online). Mô hình trông tuyệt vời offline rồi sai online, và lỗi âm thầm vì không có ngoại lệ nào ném ra. Một bảng <code>feature_store</code> là nguồn sự thật duy nhất: cùng một hàng <code>(product_id, as_of_date)</code> được cả train lẫn serve đọc, nên vector đặc trưng giống hệt nhau theo định nghĩa. Đây là cách rẻ nhất để loại bỏ cả một lớp lỗi.</p>\n<p><strong>Vì sao target là future_min_price_14d (DEC-DEAL-41)?</strong> Câu hỏi người dùng thực sự hỏi là \"14 ngày tới giá có xuống nữa không, hay giờ mua là được rồi?\". Mô hình hóa trực tiếp giá nhỏ nhất trong 14 ngày tới khớp đúng câu hỏi đó: nếu đáy dự đoán không thấp hơn giá hiện tại bao nhiêu thì giờ đã gần đáy, nên mua. Chọn min thay vì giá trung bình vì người dùng quan tâm điểm thấp nhất họ có thể chờ được, không phải mức giá kỳ vọng.</p>\n<p><strong>Vì sao forward-join chỉ lúc train (DEC-DEAL-43)?</strong> Nhãn <code>future_min_price_14d</code> theo định nghĩa nhìn về tương lai. Lúc train trên lịch sử đã hoàn tất, tương lai đó đã xảy ra nên join hợp lệ. Nhưng nếu cùng phép join đó lọt vào đường serve, mô hình sẽ \"thấy\" giá tương lai và học gian - một dạng rò nhãn (label leakage) làm điểm offline đẹp giả tạo rồi sụp khi chạy thật, nơi tương lai chưa tồn tại. Tách bạch: <code>labels.py</code> chỉ chạy trong pipeline train; <code>serve.py</code> không bao giờ gọi nó và chỉ đọc cột đặc trưng (nhãn để trống).</p>\n<p><strong>Vì sao giữ chung hợp đồng P(bottom within 14d) với FR-DEAL-004 (DEC-DEAL-44)?</strong> FR-DEAL-006 (cảnh báo) chỉ nên quan tâm \"xác suất giờ là đáy\", không nên quan tâm mô hình nào tính ra nó. Nếu LightGBM và Prophet phát hai hình dạng tín hiệu khác nhau thì tầng cảnh báo phải rẽ nhánh theo mô hình, rồi vỡ mỗi lần ta đổi mô hình hay thêm mô hình thứ ba. Giữ chung đúng một hợp đồng (<code>p_bottom_14d</code>, <code>expected_min_14d</code>, <code>horizon_days</code>, chỉ khác <code>model_kind</code>) làm tầng trên bất khả tri theo mô hình - ta thay engine bên dưới mà không động tới FR-DEAL-006.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/ml/migrations/0002_feature_store.sql\n-- 1 dòng/(product_id, as_of_date): đủ 11 đặc trưng + cột nhãn nullable.\n-- Đặc trưng chỉ nhìn quá khứ (&lt;= as_of_date); nhãn nhìn 14 ngày tới, CHỈ điền lúc train.\nCREATE TABLE feature_store (\n  product_id               BIGINT  NOT NULL REFERENCES tracked_product(id),\n  as_of_date               DATE    NOT NULL,\n  day_of_month             SMALLINT NOT NULL,                 -- 1..31\n  is_double_date           BOOLEAN NOT NULL,                  -- d == m\n  days_to_next_double_date SMALLINT NOT NULL,\n  is_payday_window         BOOLEAN NOT NULL,\n  trailing_min_30          BIGINT  NOT NULL,                  -- VND\n  trailing_min_60          BIGINT  NOT NULL,\n  trailing_min_90          BIGINT  NOT NULL,\n  price_vs_median90        REAL    NOT NULL,                  -- close_p / median90\n  volatility_30d           REAL    NOT NULL,                  -- std log-return 30d\n  category_seasonality     REAL    NOT NULL,                  -- chỉ số mùa vụ gộp theo category\n  flash_sale_flag          BOOLEAN NOT NULL,\n  platform_id              SMALLINT NOT NULL,\n  future_min_price_14d     BIGINT,                            -- NHÃN: nullable, chỉ điền lúc train\n  PRIMARY KEY (product_id, as_of_date)\n);\n\nCREATE INDEX idx_fs_asof ON feature_store (as_of_date);</code></pre>\n<h3>Feature store (Python)</h3>\n<pre><code class=\"language-python\"># services/ml/bottom/feature_store.py\nimport pandas as pd\n\nFEATURE_COLS = [\n    \"day_of_month\", \"is_double_date\", \"days_to_next_double_date\",\n    \"is_payday_window\", \"trailing_min_30\", \"trailing_min_60\", \"trailing_min_90\",\n    \"price_vs_median90\", \"volatility_30d\", \"category_seasonality\",\n    \"flash_sale_flag\", \"platform_id\",\n]\n\ndef build_features(daily: pd.DataFrame, as_of: pd.Timestamp,\n                   category_id: int, platform_id: int) -&gt; dict:\n    \"\"\"Dựng vector đặc trưng tại as_of - CHỈ dùng dữ liệu &lt;= as_of (no future).\"\"\"\n    hist = daily[daily[\"day\"] &lt;= as_of].sort_values(\"day\")\n    if hist.empty:\n        raise ValueError(\"no history at as_of\")\n    close_p = int(hist[\"close_p\"].iloc[-1])\n    median90 = float(hist[\"close_p\"].tail(90).median())\n    feats = {\n        \"day_of_month\": as_of.day,\n        \"is_double_date\": int(as_of.day == as_of.month),\n        \"days_to_next_double_date\": _days_to_next_double_date(as_of),\n        \"is_payday_window\": int(_in_payday_window(as_of)),\n        \"trailing_min_30\": int(hist[\"min_p\"].tail(30).min()),\n        \"trailing_min_60\": int(hist[\"min_p\"].tail(60).min()),\n        \"trailing_min_90\": int(hist[\"min_p\"].tail(90).min()),\n        \"price_vs_median90\": close_p / median90 if median90 else 1.0,\n        \"volatility_30d\": _log_return_std(hist[\"close_p\"].tail(30)),\n        \"category_seasonality\": _category_seasonality(category_id, as_of),\n        \"flash_sale_flag\": int(bool(hist[\"flash_sale\"].iloc[-1])),\n        \"platform_id\": platform_id,\n    }\n    return feats  # cùng hàm này feed cả train lẫn serve -&gt; không skew</code></pre>\n<h3>Labels (Python) - CHỈ lúc train</h3>\n<pre><code class=\"language-python\"># services/ml/bottom/labels.py\nimport pandas as pd\n\nHORIZON_DAYS = 14\n\ndef future_min_price_14d(daily: pd.DataFrame, as_of: pd.Timestamp) -&gt; int | None:\n    \"\"\"Forward-join: min giá trong (as_of, as_of + 14d]. CHỈ gọi lúc train.\n    Trả None nếu cửa sổ tương lai chưa đủ 14 ngày dữ liệu (tránh nhãn cụt).\"\"\"\n    end = as_of + pd.Timedelta(days=HORIZON_DAYS)\n    fwd = daily[(daily[\"day\"] &gt; as_of) &amp; (daily[\"day\"] &lt;= end)]\n    if fwd.empty or fwd[\"day\"].max() &lt; end:\n        return None\n    return int(fwd[\"min_p\"].min())</code></pre>\n<h3>LightGBM (Python)</h3>\n<pre><code class=\"language-python\"># services/ml/bottom/lgbm_model.py\nimport lightgbm as lgb\nimport numpy as np\nfrom .feature_store import FEATURE_COLS\n\nSEED = 42\nDEFAULT_MARGIN = 0.03  # biên P(bottom): đáy dự đoán &gt;= current*(1-margin) -&gt; coi như gần đáy\n\ndef train(df) -&gt; lgb.LGBMRegressor:\n    \"\"\"Train hồi quy future_min_price_14d trên đúng FEATURE_COLS, seed cố định.\"\"\"\n    model = lgb.LGBMRegressor(random_state=SEED, n_estimators=400, learning_rate=0.05)\n    model.fit(df[FEATURE_COLS], df[\"future_min_price_14d\"])\n    return model\n\ndef predict(model, feats: dict) -&gt; int:\n    x = np.array([[feats[c] for c in FEATURE_COLS]])\n    return int(model.predict(x)[0])  # expected_min_14d\n\ndef p_bottom(expected_min_14d: int, current_price: int, margin: float = DEFAULT_MARGIN) -&gt; float:\n    \"\"\"P(bottom within 14d): cao khi đáy dự đoán không thấp hơn current quá margin.\"\"\"\n    if current_price &lt;= 0:\n        return 0.0\n    ratio = expected_min_14d / current_price          # ~1.0 =&gt; đang gần đáy\n    score = (ratio - (1.0 - margin)) / margin         # ánh xạ [1-margin,1] -&gt; [0,1]\n    return float(min(1.0, max(0.0, score)))</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch -&gt; <code>feature_store</code> tồn tại với PK <code>(product_id, as_of_date)</code> và đủ 11 cột đặc trưng + cột nhãn <code>future_min_price_14d</code> nullable.</li><li><code>build_features</code> chỉ dùng dữ liệu <code>&lt;= as_of</code> (không có điểm nào sau <code>as_of</code> lọt vào vector).</li><li>Mỗi đặc trưng tính đúng định nghĩa ở §1 #3 (double-date khi <code>d == m</code>, <code>price_vs_median90 = close_p/median90</code>, volatility là std log-return 30 ngày, v.v.).</li><li><code>future_min_price_14d(daily, as_of)</code> trả <code>min(min_p)</code> trong cửa sổ <code>(as_of, as_of+14d]</code>; trả <code>None</code> khi tương lai chưa đủ 14 ngày.</li><li>Đường serve không bao giờ gọi <code>future_min_price_14d</code> và không đọc cột nhãn (no leakage).</li><li>Train và serve đọc đặc trưng từ cùng <code>feature_store</code>/<code>build_features</code> - vector đặc trưng cho cùng <code>(product_id, as_of_date)</code> giống hệt nhau.</li><li>SKU <code>&lt;180</code> ngày -&gt; fallback Prophet (FR-DEAL-004), KHÔNG chạy LightGBM; SKU <code>&gt;=180</code> ngày -&gt; chạy LightGBM.</li><li><code>train</code> trả <code>LGBMRegressor</code> đã fit trên đúng <code>FEATURE_COLS</code> với mục tiêu <code>future_min_price_14d</code>; <code>random_state</code> cố định.</li><li><code>p_bottom(expected_min_14d, current_price)</code> trả giá trị trong <code>[0,1]</code>, tăng khi đáy dự đoán tiến gần <code>current_price</code>.</li><li>Tín hiệu phát ra cùng schema FR-DEAL-004: <code>p_bottom_14d</code>, <code>expected_min_14d</code>, <code>horizon_days=14</code>, chỉ khác <code>model_kind='lgbm'</code>.</li><li>Dự đoán ghi vào <code>price_forecast</code> với <code>model_kind='lgbm'</code> và <code>p_bottom_14d</code>.</li><li>Cùng <code>feature_store</code> + cùng seed -&gt; cùng mô hình + cùng dự đoán (tái lập).</li><li>Đặc trưng thiếu được điền theo quy ước (không NaN ngầm); metric <code>lgbm_fallback_prophet_total</code> tăng khi SKU <code>&lt;180</code> ngày.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-python\"># services/ml/tests/test_feature_store.py\nfrom services.ml.bottom.feature_store import build_features, FEATURE_COLS\n\ndef test_feature_vector_complete(daily_120d):\n    feats = build_features(daily_120d, as_of=AS_OF, category_id=4221, platform_id=1)\n    for col in FEATURE_COLS:                      # đủ 11 đặc trưng, không thiếu cột\n        assert col in feats\n    assert feats[\"is_double_date\"] in (0, 1)\n\ndef test_features_use_no_future(daily_120d):\n    # thêm 1 điểm giá rẻ bất thường SAU as_of: không được lọt vào vector\n    poisoned = inject_cheap_point(daily_120d, after=AS_OF)\n    feats = build_features(poisoned, as_of=AS_OF, category_id=4221, platform_id=1)\n    assert feats[\"trailing_min_30\"] == build_features(\n        daily_120d, AS_OF, 4221, 1)[\"trailing_min_30\"]</code></pre>\n<pre><code class=\"language-python\"># services/ml/tests/test_labels.py\nfrom services.ml.bottom.labels import future_min_price_14d\n\ndef test_label_future_min_14d(daily_series):\n    # đáy 14 ngày tới của as_of phải bằng min(min_p) trong (as_of, as_of+14d]\n    label = future_min_price_14d(daily_series, as_of=AS_OF)\n    assert label == expected_forward_min(daily_series, AS_OF, 14)\n\ndef test_label_no_leak_at_serve(daily_series):\n    # cửa sổ tương lai chưa đủ 14 ngày (as_of gần mép phải) -&gt; None, không bịa nhãn\n    near_edge = daily_series[\"day\"].max() - pd.Timedelta(days=5)\n    assert future_min_price_14d(daily_series, as_of=near_edge) is None</code></pre>\n<pre><code class=\"language-python\"># services/ml/tests/test_lgbm_model.py\nfrom services.ml.bottom import lgbm_model as m\nfrom services.ml.bottom.serve import route_model\n\ndef test_eligibility_180d_gate():\n    assert route_model(days_history=179) == \"prophet\"   # fallback FR-DEAL-004\n    assert route_model(days_history=180) == \"lgbm\"\n\ndef test_lgbm_train_predict_shape(train_df, one_feat_row):\n    model = m.train(train_df)\n    pred = m.predict(model, one_feat_row)\n    assert isinstance(pred, int)                          # expected_min_14d\n\ndef test_p_bottom_contract_matches_prophet(one_feat_row):\n    model = m.train_fixture()\n    exp = m.predict(model, one_feat_row)\n    p = m.p_bottom(exp, current_price=one_feat_row_current())\n    assert 0.0 &lt;= p &lt;= 1.0                                # cùng miền tín hiệu như FR-DEAL-004</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration <code>0002_feature_store.sql</code> (bảng + index) -&gt; <code>feature_store.py</code> (<code>build_features</code> + <code>FEATURE_COLS</code>) -&gt; <code>labels.py</code> (<code>future_min_price_14d</code>, chỉ pipeline train) -&gt; <code>lgbm_model.py</code> (<code>train</code>/<code>predict</code>/<code>p_bottom</code>) -&gt; sửa <code>serve.py</code> (cổng <code>route_model</code> theo số ngày: <code>&gt;=180</code> LightGBM, dưới ngưỡng gọi Prophet FR-DEAL-004) -&gt; tests. Pipeline train chạy theo lô: dựng <code>feature_store</code> cho dải <code>as_of_date</code> lịch sử, điền nhãn bằng forward-join, fit <code>LGBMRegressor</code>, lưu model artifact. Đường serve chỉ đọc một hàng đặc trưng tại <code>as_of = today</code> (cột nhãn trống), gọi <code>predict</code> + <code>p_bottom</code>, ghi <code>price_forecast</code>. Một hàm <code>build_features</code> duy nhất dùng cho cả hai để chặn skew.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>FR-DEAL-004</strong> - Prophet baseline; FR-DEAL-005 fallback về nó cho SKU <code>&lt;180</code> ngày và dùng chung bảng <code>price_forecast</code> (FR-DEAL-004 sở hữu DDL) + hợp đồng tín hiệu. Ánh xạ cột khi ghi: <code>expected_min_14d</code> của FR này ghi vào cột <code>yhat</code>; <code>as_of_date</code> ghi vào cột <code>run_date</code>; <code>horizon_days = 14</code> ứng với cột <code>horizon_day</code> (1..14); <code>model_kind = 'lgbm'</code> đã nằm trong CHECK của bảng. Không tạo bảng <code>price_forecast</code> thứ hai - chỉ INSERT vào bảng do FR-DEAL-004 định nghĩa.</li><li><strong>FR-DEAL-002</strong> - category prior là fallback cuối khi cả Prophet chưa đủ; <code>category_seasonality</code> mượn ý tưởng gộp theo category.</li><li><strong>FR-PRICE-002</strong> - <code>price_daily</code> (continuous aggregate) và <code>price_snapshot</code> là nguồn cho mọi đặc trưng và nhãn.</li><li><strong>FR-DEAL-006 (downstream)</strong> - tiêu thụ tín hiệu <code>p_bottom_14d</code> chia sẻ, bất khả tri theo <code>model_kind</code>.</li><li>Lib: <code>lightgbm</code>, <code>pandas</code>, <code>numpy</code>, <code>scikit-learn</code>; driver Postgres cho <code>feature_store</code>/<code>price_forecast</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Một hàng feature_store (kèm nhãn lúc train)</h3>\n<pre><code class=\"language-json\">{\n  \"product_id\": 90112,\n  \"as_of_date\": \"2026-06-06\",\n  \"day_of_month\": 6,\n  \"is_double_date\": true,\n  \"days_to_next_double_date\": 0,\n  \"is_payday_window\": false,\n  \"trailing_min_30\": 89000,\n  \"trailing_min_60\": 85000,\n  \"trailing_min_90\": 82000,\n  \"price_vs_median90\": 0.94,\n  \"volatility_30d\": 0.071,\n  \"category_seasonality\": 1.12,\n  \"flash_sale_flag\": true,\n  \"platform_id\": 1,\n  \"future_min_price_14d\": 81000\n}</code></pre>\n<h3>Một dòng dự đoán ghi vào price_forecast (model_kind = lgbm)</h3>\n<pre><code class=\"language-json\">{\n  \"product_id\": 90112,\n  \"as_of_date\": \"2026-06-27\",\n  \"model_kind\": \"lgbm\",\n  \"expected_min_14d\": 84000,\n  \"p_bottom_14d\": 0.78,\n  \"horizon_days\": 14\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li><code>margin</code> của <code>p_bottom</code> cố định 0,03 hay co giãn theo <code>volatility_30d</code> của SKU - tinh chỉnh khi có nhãn click thực.</li><li>Ngưỡng <code>&gt;=180</code> ngày có nên khác theo category (thời trang đổi giá nhanh hơn điện máy) - cấu hình per-category ở slice sau.</li><li>Hiệu chỉnh xác suất (calibration) cho <code>p_bottom_14d</code> để con số đọc đúng là xác suất - tách sang FR riêng khi có dữ liệu kết quả.</li><li>Train tăng dần (incremental) thay vì train lại toàn bộ theo lô - tối ưu chi phí giai đoạn sau.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Train/serve skew (đặc trưng tính khác nhau)</td><td>so vector serve vs feature_store</td><td>mô hình sai online dù tốt offline</td><td>Cùng một <code>build_features</code> + đọc chung <code>feature_store</code> (§1 #6)</td></tr><tr><td>Rò nhãn (forward-join lọt vào serve)</td><td>test no_leak + review đường serve</td><td>điểm offline giả tạo, sụp khi chạy thật</td><td><code>labels.py</code> chỉ trong pipeline train; serve không gọi (§1 #5)</td></tr><tr><td>SKU <code>&lt;180</code> ngày bị route nhầm vào LightGBM</td><td>test cổng 180d</td><td>LightGBM thua Prophet trên dữ liệu mỏng</td><td><code>route_model</code> chặn <code>&lt;180</code> -&gt; Prophet (§1 #7)</td></tr><tr><td>Đặc trưng NULL trong cửa sổ trailing</td><td>check NULL khi build</td><td>NaN rò vào mô hình, dự đoán rác</td><td>Điền theo quy ước rõ ràng (§1 #14)</td></tr><tr><td><code>category_seasonality</code> không ổn định (category thưa)</td><td>giám sát phương sai theo category</td><td>đặc trưng nhiễu kéo dự đoán lệch</td><td>Về trung tính khi <code>sample_count</code> category dưới sàn</td></tr><tr><td>Nhãn cụt ở mép phải lịch sử (chưa đủ 14 ngày)</td><td><code>future_min_price_14d</code> trả None</td><td>hàng train có nhãn sai/thiếu</td><td>Bỏ hàng nhãn None khỏi tập train (§1 #4)</td></tr><tr><td>Mô hình không tái lập (seed trôi)</td><td>chạy lại so dự đoán</td><td>khó debug, kết quả khác mỗi lần</td><td><code>random_state</code> cố định + đọc ổn định thứ tự (§1 #12)</td></tr><tr><td>Hợp đồng tín hiệu lệch FR-DEAL-004</td><td>test p_bottom_contract</td><td>FR-DEAL-006 phải rẽ nhánh theo mô hình</td><td>Cùng <code>p_bottom_14d</code>/<code>expected_min_14d</code>/<code>horizon_days</code> (§1 #10)</td></tr><tr><td>Lệch phân phối (price_daily đổi đơn vị/null)</td><td>giám sát MAE kiểm định</td><td>dự đoán trôi âm thầm</td><td>Theo dõi <code>lgbm_train_mae</code>; cảnh báo khi vọt</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>LightGBM là nâng cấp theo SKU, không thay thế toàn cục: nó chỉ nhận các SKU <code>&gt;=180</code> ngày, phần còn lại vẫn do Prophet (FR-DEAL-004) và category prior (FR-DEAL-002) lo - một cầu thang chất lượng theo độ dày dữ liệu.</li><li>Feature store là quyết định kiến trúc quan trọng nhất ở đây: nó biến train/serve skew từ một lớp lỗi âm thầm thành một bất biến theo định nghĩa (cùng hàng, cùng vector).</li><li>Tách <code>labels.py</code> khỏi đường serve là ranh giới chống rò nhãn: forward-join là hợp lệ lúc train, là gian lận lúc serve - mã phải làm ranh giới đó hiển nhiên.</li><li>Giữ chung hợp đồng <code>p_bottom_14d</code> với FR-DEAL-004 là điều khiến FR-DEAL-006 không phải biết mô hình nào - đổi engine bên dưới không động tới tầng cảnh báo.</li><li><code>future_min_price_14d</code> khớp đúng câu hỏi người dùng \"giờ mua được chưa hay còn xuống\" - chọn min (không phải trung bình) vì người dùng quan tâm điểm thấp nhất họ chờ được.</li><li>Seed cố định và đọc ổn định thứ tự là điều kiện để audit lại được một dự đoán cũ - cùng đầu vào phải cho cùng đầu ra.</li></ul>\n<hr>\n<p><em>Hết FR-DEAL-005. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
