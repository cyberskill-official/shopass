window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-NOTIF-004"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service NOTIF <strong>MUST</strong> cung cấp một midnight-spike scheduler san phẳng đỉnh 00:00 (flatten the curve) trước khi giao cho fan-out. Scheduler nhận một lô alert tới hạn, đặt <code>notification.scheduled_at</code> cho từng dòng bằng jitter trong <code>[-90s, +180s]</code>, gom theo bucket phút, và spread phần dư khi một bucket vượt trần FCM, sao cho KHÔNG bucket phút nào vượt 600.000 tin/phút/project. Scheduler chỉ đặt thời điểm; việc phát đúng giờ thuộc fan-out (FR-NOTIF-003). Hợp đồng:</p>\n<ol><li><strong>MUST</strong> với mỗi alert, tính <code>dispatch_at = event_time + jitter</code>, trong đó <code>jitter</code> là số ngẫu nhiên trong khoảng <code>[-90s, +180s]</code> (bất đối xứng: từ 90 giây trước tới 180 giây sau <code>event_time</code>) theo §3.5(5) và DEC-NOTIF-31. <code>jitter</code> <strong>MUST</strong> nằm trong cận này, không bao giờ ngoài.</li><li><strong>MUST</strong> gom các alert vào bucket theo phút bằng khóa <code>floor(dispatch_at / 60s)</code> (DEC-NOTIF-32). Mỗi bucket đại diện đúng một phút lịch.</li><li><strong>MUST</strong> giữ hằng <code>FCM_RATE_LIMIT_PER_MIN = 600_000</code> (§3.6, DEC-NOTIF-33). Đây là trần cứng cho số tin trong một phút của một project FCM.</li><li><strong>MUST</strong> khi <code>size(bucket) &gt; FCM_RATE_LIMIT_PER_MIN</code> thì gọi <code>spreadAcrossNextMinutes(bucket)</code>: giữ lại tối đa <code>FCM_RATE_LIMIT_PER_MIN</code> tin trong phút hiện tại, đẩy phần dư sang phút kế tiếp, lặp cho tới khi mọi bucket <code>&lt;= FCM_RATE_LIMIT_PER_MIN</code> (DEC-NOTIF-33).</li><li><strong>MUST</strong> đảm bảo bất biến sau khi spread: với mọi bucket phút, <code>size(bucket) &lt;= FCM_RATE_LIMIT_PER_MIN</code>. Đây là bất biến cốt lõi - vi phạm nó là ăn 429.</li><li><strong>MUST</strong> tránh dồn cụm vào các mốc tròn <code>:00</code>, <code>:15</code>, <code>:30</code>, <code>:45</code> (DEC-NOTIF-34): jitter bất đối xứng làm khối alert lệch khỏi đúng giây sự kiện, và bước spread <strong>MUST KHÔNG</strong> đẩy phần dư về đúng giây <code>:00</code> của một phút mốc tròn theo cách tạo cụm mới ở đó. Phân bố sau lập lịch không được có đỉnh nhọn tại các mốc tròn.</li><li><strong>MUST</strong> cung cấp <code>ScheduleAlerts(now time.Time, alerts []Alert, rng RNG) map[int64]time.Time</code>: nhận lô alert, trả ánh xạ <code>notification_id -&gt; scheduled_at</code> đã jitter + bucket + spread. Hàm <strong>MUST</strong> thuần (deterministic) khi <code>rng</code> cố định.</li><li><strong>MUST</strong> nhận <code>rng</code> qua tham số (dependency injection) để jitter tất định khi test (DEC-NOTIF-35); code production tiêm một <code>RNG</code> thật, test tiêm <code>RNG</code> seeded để tái lập đúng phân bố. <code>ScheduleAlerts</code> <strong>MUST KHÔNG</strong> dùng <code>rand</code> toàn cục hay <code>time.Now()</code> ẩn bên trong. Sau khi tính, <strong>MUST</strong> ghi <code>scheduled_at</code> cho từng dòng <code>notification</code> qua <code>repo.BatchSetScheduledAt(ctx, map[int64]time.Time)</code> bằng một câu UPDATE hàng loạt (không N+1).</li><li><strong>MUST</strong> giữ ranh giới với tầng gửi (DEC-NOTIF-35): scheduler <strong>MUST KHÔNG</strong> gọi FCM/APNs/SES/SMS. Nó chỉ đặt <code>scheduled_at</code>; fan-out (FR-NOTIF-003) đọc các dòng tới hạn theo <code>scheduled_at</code> rồi mới phát.</li><li><strong>MUST</strong> giữ ổn định thứ tự khi spread: trong một bucket vượt trần, phần được giữ lại và phần bị đẩy sang phút sau <strong>MUST</strong> theo thứ tự tất định (vd theo <code>notification_id</code>) để kết quả tái lập được, không phụ thuộc thứ tự map.</li><li><strong>SHOULD</strong> ưu tiên giữ lại alert high-value (OTP, deal giá trị cao) trong phút sớm khi phải spread, để cảnh báo quan trọng không bị đẩy lùi; alert thường nhường chỗ trước.</li><li><strong>SHOULD</strong> phát OTel: <code>notif_scheduler_batch_size</code> (histogram - số alert mỗi lô), <code>notif_scheduler_buckets_total</code> (counter), <code>notif_scheduler_overflow_spread_total</code> (counter - số tin phải spread vì vượt trần), <code>notif_scheduler_max_bucket_size</code> (gauge - bucket lớn nhất sau spread, phải <code>&lt;= 600_000</code>).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao phải flatten the curve cho đỉnh 00:00?</strong> §3.6 đo được traffic thông báo tăng hơn 2x trong 30 giây tới 2 phút đầu mỗi giờ, và nửa đêm là đỉnh của đỉnh: flash sale các sàn cùng mở lúc 00:00, hàng loạt rule giá cùng tới ngưỡng, batch dự đoán đáy giá (FR-DEAL-006) cùng bắn. Nếu để tất cả đập vào FCM trong một phút, một bucket phút dễ vượt trần 600.000 và FCM trả 429. Một khi 429 xảy ra, phần dư retry dồn sang phút sau làm phút đó cũng vỡ - hiệu ứng cuộn tuyết. San phẳng đường cong là cách FCM khuyến nghị để chính ta không tự bắn vào chân mình lúc cao điểm.</p>\n<p><strong>Vì sao jitter bất đối xứng <code>[-90s, +180s]</code>?</strong> Ý tưởng là rải đều các alert quanh thời điểm sự kiện thay vì để chúng chụm đúng một giây. Bất đối xứng (90 giây trước, 180 giây sau) cố ý nghiêng về phía sau: với phần lớn cảnh báo, gửi sớm tối đa 90 giây vẫn kịp giá trị, còn cho phép trễ tới 180 giây mở rộng cửa sổ để hạ mật độ tin/phút mà không làm người dùng cảm thấy chậm. Cửa sổ rộng hơn về sau cũng là nơi có chỗ trống để hứng phần spread khi phút sự kiện quá tải. Đây là cách rẻ nhất để biến một cột nhọn thành một dải phẳng trước cả khi cần tới bước spread.</p>\n<p><strong>Vì sao gom bucket theo phút (<code>floor(dispatch_at / 60s)</code>)?</strong> Trần của FCM tính theo phút (600.000 tin/phút/project), nên đơn vị kiểm soát tự nhiên là phút. Quy mỗi <code>dispatch_at</code> về phút chứa nó cho ta đúng đại lượng cần khống chế: số tin trong mỗi phút lịch. Bucket phút biến bài toán \"đừng vượt rate limit\" thành một phép đếm đơn giản trên từng nhóm, dễ kiểm và dễ chứng minh bất biến.</p>\n<p><strong>Vì sao spread phần dư sang phút kế khi vượt trần?</strong> Jitter một mình không bảo đảm tuyệt đối: nếu lô quá lớn, ngay cả sau khi rải, một bucket vẫn có thể vượt 600.000. spread là chốt chặn cuối: giữ lại đúng trần trong phút hiện tại, đẩy phần thừa sang phút sau, lặp cho tới khi mọi phút đều dưới trần. Nhờ vậy bất biến <code>size(bucket) &lt;= 600_000</code> luôn đúng bất kể lô lớn cỡ nào - ta đánh đổi một chút độ trễ phát của phần dư để đổi lấy việc không bao giờ ăn 429.</p>\n<p><strong>Vì sao tránh dồn vào mốc tròn <code>:00/:15/:30/:45</code>?</strong> FCM khuyến nghị tránh các mốc tròn vì rất nhiều hệ thống lập lịch \"ngây thơ\" đều bắn đúng các giây này, làm hạ tầng FCM thấy spike đồng pha và siết lại. Nếu jitter và spread của ta lại vô tình kéo khối tin về đúng giây <code>:00</code> của mốc tròn, ta tự đặt mình vào nhóm bị siết. Vì thế jitter cố ý lệch, và spread cố ý không nhắm phần dư về đúng đầu phút mốc tròn - mục tiêu là một phân bố trơn, không có gai tại các mốc đó.</p>\n<p><strong>Vì sao rng tiêm được và scheduler chỉ đặt scheduled_at, không tự gửi?</strong> Lập lịch dựa trên ngẫu nhiên mà không kiểm soát được nguồn ngẫu nhiên thì không test được: ta không thể khẳng định \"bucket không vượt trần\" nếu mỗi lần chạy ra một phân bố khác. Tiêm <code>RNG</code> cho phép test seeded tái lập đúng phân bố và kiểm bất biến tất định. Và vì gửi ở quy mô (rate-limit, dead-letter, retry) là việc của fan-out FR-NOTIF-003, scheduler dừng đúng ở \"đặt thời điểm\": nó ghi <code>scheduled_at</code> rồi trao lại, giữ một ranh giới trách nhiệm sạch và tránh trộn logic lập lịch với gọi nhà cung cấp.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<p>Không có DDL mới. Bảng <code>notification</code> và cột <code>scheduled_at</code> đã do FR-NOTIF-001 định nghĩa; FR này chỉ ghi vào cột đó qua một UPDATE hàng loạt.</p>\n<h3>Hằng số và kiểu (Go)</h3>\n<pre><code class=\"language-go\">// services/notif/internal/notif/scheduler.go\n\nconst (\n    // FCM_RATE_LIMIT_PER_MIN: trần cứng 600.000 tin/phút/project (§3.6).\n    // Không bucket phút nào được vượt giá trị này, nếu không sẽ ăn 429.\n    FCM_RATE_LIMIT_PER_MIN = 600_000\n\n    // Jitter bất đối xứng [-90s, +180s] (§3.5(5), DEC-NOTIF-31).\n    JitterMin = -90 * time.Second  // 90 giây TRƯỚC event_time\n    JitterMax = 180 * time.Second  // 180 giây SAU event_time\n)\n\n// RNG tiêm được để jitter tất định khi test (DEC-NOTIF-35).\n// Production tiêm rng thật; test tiêm rng seeded.\ntype RNG interface {\n    // Int63n trả số trong [0, n); chuẩn như math/rand.\n    Int63n(n int64) int64\n}\n\ntype Alert struct {\n    NotificationID int64\n    EventTime      time.Time\n    HighValue      bool // OTP / deal giá trị cao: ưu tiên giữ phút sớm khi spread (§1 #11)\n}\n\n// minuteKey quy một thời điểm về phút chứa nó: floor(t / 60s) (DEC-NOTIF-32).\nfunc minuteKey(t time.Time) int64 { return t.Unix() / 60 }\n\n// roundMarkSecond cho biết giây-trong-giờ của t có rơi đúng mốc tròn\n// :00/:15/:30/:45 không (DEC-NOTIF-34).\nfunc isRoundMark(t time.Time) bool {\n    m := t.Minute()\n    return t.Second() == 0 &amp;&amp; (m == 0 || m == 15 || m == 30 || m == 45)\n}</code></pre>\n<h3>applyJitter + bucketByMinute (§1 #1, #2)</h3>\n<pre><code class=\"language-go\">// applyJitter đặt dispatch_at = event_time + jitter, jitter trong [-90s,+180s].\n// Span = JitterMax - JitterMin (= 270s); offset = rng trong [0, span], rồi + JitterMin.\nfunc applyJitter(a Alert, rng RNG) time.Time {\n    span := int64((JitterMax - JitterMin) / time.Second) // 270\n    off := time.Duration(rng.Int63n(span+1)) * time.Second\n    return a.EventTime.Add(JitterMin + off) // [event-90s, event+180s]\n}\n\n// bucketByMinute gom alert (đã có dispatch_at) vào bucket theo phút.\nfunc bucketByMinute(items []scheduled) map[int64][]scheduled {\n    buckets := make(map[int64][]scheduled)\n    for _, it := range items {\n        k := minuteKey(it.At)\n        buckets[k] = append(buckets[k], it)\n    }\n    return buckets\n}\n\ntype scheduled struct {\n    ID        int64\n    At        time.Time\n    HighValue bool\n}</code></pre>\n<h3>spreadAcrossNextMinutes (§1 #4, #5, #10, #11)</h3>\n<pre><code class=\"language-go\">// spreadAcrossNextMinutes bảo đảm mọi bucket phút &lt;= FCM_RATE_LIMIT_PER_MIN.\n// Bucket vượt trần thì giữ lại đúng trần (ưu tiên high-value + thứ tự id),\n// đẩy phần dư sang phút kế; lặp cho tới khi không còn bucket nào vượt.\nfunc spreadAcrossNextMinutes(buckets map[int64][]scheduled) map[int64][]scheduled {\n    keys := sortedKeys(buckets) // tăng dần, lặp tất định\n    for i := 0; i &lt; len(keys); i++ {\n        k := keys[i]\n        b := buckets[k]\n        if len(b) &lt;= FCM_RATE_LIMIT_PER_MIN {\n            continue\n        }\n        sortStable(b) // high-value trước, rồi theo ID (§1 #10, #11)\n        keep, overflow := b[:FCM_RATE_LIMIT_PER_MIN], b[FCM_RATE_LIMIT_PER_MIN:]\n        buckets[k] = keep\n\n        next := k + 1\n        // Đẩy overflow xuống phút kế; gán At = đầu phút kế + 1s để KHÔNG\n        // rơi đúng giây :00 mốc tròn (DEC-NOTIF-34).\n        nextStart := time.Unix(next*60, 0).Add(1 * time.Second)\n        for j := range overflow {\n            overflow[j].At = nextStart\n        }\n        buckets[next] = append(buckets[next], overflow...)\n        if !containsKey(keys, next) {\n            keys = append(keys, next) // phút mới sinh ra cũng phải duyệt\n        }\n    }\n    return buckets\n}</code></pre>\n<h3>ScheduleAlerts (§1 #7, #8, #9)</h3>\n<pre><code class=\"language-go\">// ScheduleAlerts: jitter -&gt; bucket theo phút -&gt; spread overflow.\n// Trả map notification_id -&gt; scheduled_at. Thuần khi rng cố định.\n// KHÔNG gọi nhà cung cấp (DEC-NOTIF-35); chỉ đặt thời điểm cho fan-out.\nfunc ScheduleAlerts(now time.Time, alerts []Alert, rng RNG) map[int64]time.Time {\n    items := make([]scheduled, 0, len(alerts))\n    for _, a := range alerts {\n        items = append(items, scheduled{\n            ID: a.NotificationID, At: applyJitter(a, rng), HighValue: a.HighValue,\n        })\n    }\n    buckets := spreadAcrossNextMinutes(bucketByMinute(items))\n\n    out := make(map[int64]time.Time, len(alerts))\n    for _, b := range buckets {\n        for _, it := range b {\n            out[it.ID] = it.At\n        }\n    }\n    return out\n}\n\n// Caller (orchestrator) ghi xuống DB:\n//   sched := ScheduleAlerts(time.Now(), alerts, prodRNG)\n//   err := repo.BatchSetScheduledAt(ctx, sched) // 1 UPDATE hàng loạt\n// Fan-out (FR-NOTIF-003) sau đó nhặt dòng theo scheduled_at và phát.</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Với mọi alert, <code>scheduled_at - event_time</code> nằm trong <code>[-90s, +180s]</code> (không bao giờ ngoài cận).</li><li>jitter bất đối xứng đúng hướng: cận dưới là <code>-90s</code>, cận trên là <code>+180s</code> (không phải <code>+-90s</code> hay <code>+-180s</code>).</li><li><code>minuteKey(dispatch_at)</code> = <code>floor(dispatch_at / 60s)</code>; hai alert cùng phút lịch rơi cùng bucket.</li><li>Sau <code>ScheduleAlerts</code>, KHÔNG bucket phút nào có <code>size &gt; 600_000</code> (bất biến §1 #5).</li><li>Khi một phút có <code>&gt; 600_000</code> alert, đúng <code>600_000</code> được giữ lại, phần dư xuất hiện ở phút kế (hoặc các phút kế tiếp nếu vẫn tràn).</li><li>Spread lặp tới hội tụ: lô lớn bằng <code>N x 600_000 + r</code> được rải qua đủ số phút để mọi bucket dưới trần.</li><li>Phân bố sau lập lịch KHÔNG có đỉnh nhọn tại các giây <code>:00</code> của mốc tròn <code>:00/:15/:30/:45</code>; phần dư bị spread không bị gán đúng giây <code>:00</code> mốc tròn.</li><li><code>ScheduleAlerts</code> tất định: cùng <code>alerts</code> + cùng <code>rng</code> seeded -&gt; cùng map kết quả qua nhiều lần chạy.</li><li>Thứ tự keep/overflow tất định (theo high-value rồi ID), không phụ thuộc thứ tự duyệt map.</li><li>Alert <code>HighValue=true</code> được ưu tiên giữ ở phút sớm khi bucket phải spread.</li><li><code>ScheduleAlerts</code> KHÔNG gọi FCM/APNs/SES/SMS (kiểm qua review + không import client gửi trong scheduler).</li><li><code>repo.BatchSetScheduledAt</code> (§1 #8) ghi <code>scheduled_at</code> cho đúng tập <code>notification_id</code> bằng một UPDATE hàng loạt (không N+1); metric <code>notif_scheduler_max_bucket_size &lt;= 600_000</code>.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/notif/internal/notif/scheduler_test.go\n\n// seededRNG: RNG tất định cho test (DEC-NOTIF-35).\ntype seededRNG struct{ r *rand.Rand }\nfunc newSeededRNG(seed int64) RNG { return &amp;seededRNG{rand.New(rand.NewSource(seed))} }\nfunc (s *seededRNG) Int63n(n int64) int64 { return s.r.Int63n(n) }\n\nfunc TestJitter_WithinAsymmetricBounds(t *testing.T) {\n    rng := newSeededRNG(1)\n    base := time.Date(2026, 6, 28, 0, 0, 0, 0, time.UTC) // 00:00\n    for i := 0; i &lt; 10_000; i++ {\n        at := applyJitter(Alert{NotificationID: int64(i), EventTime: base}, rng)\n        d := at.Sub(base)\n        require.GreaterOrEqual(t, d, JitterMin) // &gt;= -90s\n        require.LessOrEqual(t, d, JitterMax)    // &lt;= +180s\n    }\n}\n\nfunc TestBucket_NeverExceedsLimit(t *testing.T) {\n    rng := newSeededRNG(7)\n    base := time.Date(2026, 6, 28, 0, 0, 0, 0, time.UTC)\n    alerts := makeAlerts(t, base, FCM_RATE_LIMIT_PER_MIN*3+123) // 3 phút + dư\n    sched := ScheduleAlerts(base, alerts, rng)\n\n    counts := map[int64]int{}\n    for _, at := range sched {\n        counts[minuteKey(at)]++\n    }\n    for k, c := range counts {\n        require.LessOrEqualf(t, c, FCM_RATE_LIMIT_PER_MIN,\n            \"bucket phút %d có %d tin &gt; trần\", k, c) // bất biến §1 #5\n    }\n}\n\nfunc TestOverflow_SpreadsToNextMinute(t *testing.T) {\n    rng := newSeededRNG(3)\n    base := time.Date(2026, 6, 28, 0, 0, 0, 0, time.UTC)\n    // Ép mọi alert vào cùng một phút bằng event_time trùng + rng cố định,\n    // rồi kiểm phần dư chảy sang phút kế.\n    alerts := makeAlerts(t, base, FCM_RATE_LIMIT_PER_MIN+5_000)\n    sched := ScheduleAlerts(base, alerts, rng)\n\n    counts := map[int64]int{}\n    for _, at := range sched {\n        counts[minuteKey(at)]++\n    }\n    require.GreaterOrEqual(t, len(counts), 2)          // tràn sang ít nhất 1 phút nữa\n    for _, c := range counts {\n        require.LessOrEqual(t, c, FCM_RATE_LIMIT_PER_MIN)\n    }\n}\n\nfunc TestSpread_AvoidsRoundMarks(t *testing.T) {\n    rng := newSeededRNG(9)\n    base := time.Date(2026, 6, 28, 0, 0, 0, 0, time.UTC)\n    alerts := makeAlerts(t, base, FCM_RATE_LIMIT_PER_MIN*2+10)\n    sched := ScheduleAlerts(base, alerts, rng)\n    for _, at := range sched {\n        // Không tin nào bị spread rơi đúng giây :00 của mốc tròn (DEC-NOTIF-34).\n        require.Falsef(t, isRoundMark(at),\n            \"tin bị gán đúng mốc tròn %s\", at.Format(\"15:04:05\"))\n    }\n}\n\nfunc TestLargeBatch_Flattened(t *testing.T) {\n    rng := newSeededRNG(42)\n    base := time.Date(2026, 6, 28, 0, 0, 0, 0, time.UTC)\n    alerts := makeAlerts(t, base, FCM_RATE_LIMIT_PER_MIN*5) // 5x trần trong 1 sự kiện\n    sched := ScheduleAlerts(base, alerts, rng)\n    require.Len(t, sched, FCM_RATE_LIMIT_PER_MIN*5) // không mất alert nào\n    counts := map[int64]int{}\n    for _, at := range sched {\n        counts[minuteKey(at)]++\n    }\n    require.GreaterOrEqual(t, len(counts), 5) // rải qua &gt;= 5 phút\n    for _, c := range counts {\n        require.LessOrEqual(t, c, FCM_RATE_LIMIT_PER_MIN)\n    }\n}\n\nfunc TestScheduleAlerts_Deterministic(t *testing.T) {\n    base := time.Date(2026, 6, 28, 0, 0, 0, 0, time.UTC)\n    alerts := makeAlerts(t, base, 50_000)\n    a := ScheduleAlerts(base, alerts, newSeededRNG(100))\n    b := ScheduleAlerts(base, alerts, newSeededRNG(100))\n    require.Equal(t, a, b) // cùng seed -&gt; cùng kết quả (DEC-NOTIF-35)\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>scheduler.go</code> (hằng <code>FCM_RATE_LIMIT_PER_MIN</code>, <code>JitterMin</code>, <code>JitterMax</code>, interface <code>RNG</code>, các helper <code>minuteKey</code>/<code>isRoundMark</code>) -&gt; <code>applyJitter</code> + <code>bucketByMinute</code> -&gt; <code>spreadAcrossNextMinutes</code> -&gt; <code>ScheduleAlerts</code> -&gt; <code>repo.BatchSetScheduledAt</code> -&gt; tests. Orchestrator (engine alert FR-TRACK-004 / batch đáy giá FR-DEAL-006) sau khi đã có các dòng <code>notification</code> pending sẽ gọi <code>ScheduleAlerts(time.Now(), alerts, prodRNG)</code> rồi <code>BatchSetScheduledAt</code>; fan-out (FR-NOTIF-003) nhặt theo <code>scheduled_at</code>. Không có lời gọi nhà cung cấp nào trong package scheduler. Production tiêm một <code>RNG</code> mỏng bọc <code>math/rand</code> (hoặc <code>crypto/rand</code> rút gọn); test tiêm <code>seededRNG</code>.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>FR-NOTIF-001 (upstream)</strong> - định nghĩa bảng <code>notification</code> và cột <code>scheduled_at</code> mà scheduler ghi vào; cũng là nơi routing đã quyết kênh + tạo dòng pending trước khi lập lịch.</li><li><strong>FR-NOTIF-003 (downstream, depends_on)</strong> - fan-out đọc các dòng <code>notification</code> theo <code>scheduled_at</code> (qua <code>idx_notif_dispatch</code>) và phát đúng giờ; scheduler đặt thời điểm, fan-out thực thi rate-limit khi gửi. Scheduler là van san phẳng đặt TRƯỚC fan-out.</li><li><strong>FR-TRACK-004 / FR-DEAL-006 (producer)</strong> - sinh lô alert tới hạn (đặc biệt cụm 00:00); gọi <code>ScheduleAlerts</code> rồi <code>BatchSetScheduledAt</code>.</li><li>Lib: <code>math/rand</code> (bọc trong <code>RNG</code> tiêm được), <code>time</code>, <code>pgx</code> (cho <code>BatchSetScheduledAt</code>). Không import client FCM/APNs trong package này.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Trước lập lịch: 1.500.000 alert cùng tới hạn lúc 00:00:00</h3>\n<pre><code class=\"language-text\">event_time = 2026-06-28T00:00:00Z cho cả 1.500.000 alert\n(flash sale Shopee + rule giá + batch đáy giá cùng bắn)\nNếu enqueue thô: bucket phút 00:00 = 1.500.000 tin &gt; 600.000 -&gt; 429.</code></pre>\n<h3>Sau ScheduleAlerts: phân bố dispatch_at theo phút</h3>\n<pre><code class=\"language-text\">phút 00:00  -&gt; 600.000 tin  (jitter rải trong [-90s,+180s], giữ tối đa trần)\nphút 00:01  -&gt; 600.000 tin  (phần dư spread xuống, gán 00:01:01 né mốc tròn)\nphút 00:02  -&gt; 300.000 tin  (phần dư còn lại)\n-----------------------------------------------\ntổng        = 1.500.000 tin, KHÔNG phút nào &gt; 600.000  (bất biến §1 #5)\nkhông tin nào rơi đúng giây :00 của mốc tròn :00/:15/:30/:45 (DEC-NOTIF-34)</code></pre>\n<h3>Ghi xuống DB (một UPDATE hàng loạt)</h3>\n<pre><code class=\"language-sql\">-- repo.BatchSetScheduledAt dựng UPDATE ... FROM (VALUES ...) theo id\nUPDATE notification AS n\nSET    scheduled_at = v.sched\nFROM   (VALUES (55012, TIMESTAMPTZ '2026-06-28 00:00:37Z'),\n               (55013, TIMESTAMPTZ '2026-06-28 00:01:01Z'))\n       AS v(id, sched)\nWHERE  n.id = v.id;</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Lập lịch theo từng project FCM nếu sau này tách nhiều project (trần 600k là per-project; hiện một project nên một van là đủ).</li><li>Tham số hóa cận jitter theo loại alert (deal cực nóng cho jitter hẹp hơn để gửi sớm) - tinh chỉnh sau khi có dữ liệu thực về đỉnh 00:00.</li><li>Backpressure liên giờ: khi cụm 00:00 spread lấn sang 00:03+, có nên hợp nhất với cụm 01:00 của giờ kế - giai đoạn sau, hiện cửa sổ spread đủ ngắn.</li><li>Quiet-hours phối hợp với preference center (không push ban đêm trừ khi user đồng ý) - gắn với FR-NOTIF-001 mục đã hoãn.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Một bucket phút vượt 600.000 mà không spread</td><td>test bất biến + metric max_bucket_size</td><td>FCM trả 429, phần dư bị từ chối</td><td>spreadAcrossNextMinutes lặp tới khi mọi bucket &lt;= trần (§1 #4,#5)</td></tr><tr><td>jitter ra ngoài [-90s,+180s]</td><td>TestJitter_WithinAsymmetricBounds</td><td>rải sai, cụm lệch</td><td>applyJitter dùng span=270s + offset trong [0,span], + JitterMin</td></tr><tr><td>jitter đối xứng nhầm (+-90s)</td><td>review + test cận trên</td><td>cửa sổ sau hẹp, dễ tràn</td><td>Cận đúng: dưới -90s, trên +180s (DEC-NOTIF-31)</td></tr><tr><td>Phần dư spread bị gán đúng giây :00 mốc tròn</td><td>TestSpread_AvoidsRoundMarks</td><td>FCM siết vì spike đồng pha</td><td>Gán nextStart = đầu phút + 1s, né mốc tròn (DEC-NOTIF-34)</td></tr><tr><td>Spread không hội tụ (lô khổng lồ)</td><td>TestLargeBatch_Flattened</td><td>treo/tràn vô hạn</td><td>Vòng lặp duyệt cả phút mới sinh ra, mỗi vòng đẩy đúng phần dư</td></tr><tr><td>Kết quả không tất định</td><td>TestScheduleAlerts_Deterministic</td><td>test rung, không kiểm được</td><td>RNG tiêm được + sortStable theo id (DEC-NOTIF-35, §1 #10)</td></tr><tr><td>Mất alert khi spread (rơi rớt)</td><td>TestLargeBatch_Flattened đếm len</td><td>cảnh báo biến mất</td><td>keep + overflow hợp lại đủ, không drop</td></tr><tr><td>Scheduler tự gọi FCM</td><td>code review (không import client)</td><td>vượt rate-limit, lấn việc fan-out</td><td>Chỉ đặt scheduled_at (DEC-NOTIF-35); gửi là FR-NOTIF-003</td></tr><tr><td>Ghi scheduled_at N+1</td><td>review SQL + metric</td><td>DB nghẽn lúc đỉnh</td><td>BatchSetScheduledAt một UPDATE hàng loạt (§1 #8)</td></tr><tr><td>Alert high-value bị đẩy lùi khi spread</td><td>test ưu tiên</td><td>OTP/deal nóng tới trễ</td><td>sortStable ưu tiên HighValue giữ phút sớm (§1 #11)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Scheduler là cái van san phẳng đặt TRƯỚC fan-out: nó chỉ đặt <code>scheduled_at</code>, còn rate-limit lúc gửi thật là việc của FR-NOTIF-003 - hai lớp phòng thủ nối tiếp, không trùng vai.</li><li>Bất biến sống còn: sau <code>ScheduleAlerts</code>, mọi bucket phút <code>&lt;= 600.000</code>. Mọi thứ khác (jitter, né mốc tròn, ưu tiên high-value) là để đường cong đẹp; bất biến này là để không ăn 429.</li><li>jitter bất đối xứng <code>[-90s, +180s]</code> nghiêng về sau có chủ đích: cửa sổ sau rộng hơn vừa hạ mật độ tin/phút vừa chừa chỗ hứng phần spread, mà vẫn kịp giá trị cảnh báo.</li><li>Né mốc tròn <code>:00/:15/:30/:45</code> không phải tiểu tiết: rất nhiều scheduler ngây thơ bắn đúng các giây này nên FCM cảnh giác với chúng; lệch ra là tự tách khỏi nhóm bị siết.</li><li>RNG tiêm được là điều kiện để FR này verify được: không kiểm soát ngẫu nhiên thì không chứng minh được bất biến bucket. Production bọc <code>math/rand</code>; test seeded.</li><li>Tiền/giá trong payload vẫn là int64 VND theo DEC-PRICE-05; scheduler không đụng tới nội dung, chỉ đụng thời điểm.</li></ul>\n<hr>\n<p><em>Hết FR-NOTIF-004. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
