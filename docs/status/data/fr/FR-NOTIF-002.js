window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-NOTIF-002"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Dispatcher FCM <strong>MUST</strong> gửi push tới Web và Android qua FCM HTTP v1 API, là consumer downstream của fan-out (FR-NOTIF-003): nó nhặt các dòng <code>notification</code> có <code>channel='push'</code> đang <code>status='queued'</code>, gửi, rồi cập nhật <code>status</code> -&gt; <code>sent</code>/<code>failed</code> + <code>sent_at</code>. Dispatcher KHÔNG routing kênh và KHÔNG render lại nội dung (việc của FR-NOTIF-001). Hợp đồng:</p>\n<ol><li><strong>MUST</strong> gửi qua FCM HTTP v1 endpoint <code>POST https://fcm.googleapis.com/v1/projects/{project_id}/messages:send</code> với Bearer OAuth2 access-token lấy từ service-account (DEC-NOTIF-10). <strong>MUST NOT</strong> dùng legacy server-key API (<code>/fcm/send</code>) đã deprecated.</li><li><strong>MUST</strong> lấy địa chỉ đích (FCM registration token) từ <code>user_channel_token</code> của FR-NOTIF-001: bản ghi <code>channel='push'</code>, <code>verified=true</code>, và <code>platform IN ('android','web')</code> (FCM chỉ phụ trách Android + Web; token <code>platform='ios'</code> thuộc FR-NOTIF-005/APNs và <strong>MUST NOT</strong> bị FCM nhặt). Token chưa verified hoặc đã bị vô hiệu <strong>MUST NOT</strong> được gửi tới.</li><li><strong>MUST</strong> dựng JSON message HTTP v1 đúng cấu trúc: <code>message.token</code>, <code>message.notification.{title,body}</code> (đã render bởi FR-NOTIF-001), tùy chọn <code>message.data</code> (key-value string) cho deep-link, và override theo nền tảng (<code>webpush</code>, <code>android</code>) khi cần.</li><li><strong>MUST</strong> áp một token bucket nội bộ ~<code>600.000</code> message/phút/project (refill mỗi phút) trước khi gọi FCM (DEC-NOTIF-11). Quota mặc định của FCM là 600.000 message/phút/project và phủ trên 99% nhà phát triển FCM; van nội bộ giữ ta dưới ngưỡng để hạn chế 429 từ phía server.</li><li><strong>MUST</strong> xử lý HTTP <code>429</code> <code>RESOURCE_EXHAUSTED</code> bằng exponential backoff có jitter và <strong>MUST</strong> tôn trọng header <code>Retry-After</code> khi FCM trả về (DEC-NOTIF-12). Message bị 429 <strong>MUST NOT</strong> bị drop; nó được trả lại hàng đợi (giữ <code>status='queued'</code>) để thử lại sau, không mất thông báo.</li><li><strong>MUST</strong> phân loại phản hồi FCM thành ba nhóm và hành xử đúng:</li></ol>\n<ul><li>Thành công (HTTP 200, có <code>name</code>) -&gt; <code>MarkSent</code>: <code>status='sent'</code>, <code>sent_at=now()</code>.</li><li>Lỗi tạm thời (429 <code>RESOURCE_EXHAUSTED</code>, 500/503 <code>UNAVAILABLE</code>, timeout) -&gt; retry theo backoff; vượt số lần thử tối đa thì <code>MarkFailed</code> để dead-letter (FR-NOTIF-003).</li><li>Lỗi token vĩnh viễn (404 <code>UNREGISTERED</code>, 400 <code>INVALID_ARGUMENT</code> cho token sai định dạng) -&gt; token chết.</li></ul>\n<ol><li><strong>MUST</strong> xử lý <code>UNREGISTERED</code> / token không hợp lệ bằng cách set <code>user_channel_token.verified=false</code> cho <code>(user_id, 'push')</code> tương ứng (DEC-NOTIF-13) và <code>MarkFailed</code> dòng <code>notification</code>. Lần sau routing (FR-NOTIF-001) sẽ không coi token đó là khả dụng nữa - ngừng gửi vào token chết.</li><li><strong>MUST</strong> nhặt việc an toàn khi chạy nhiều worker song song: <code>ClaimPushBatch</code> dùng <code>SELECT ... FOR UPDATE SKIP LOCKED</code> trên <code>notification</code> (<code>channel='push'</code>, <code>status='queued'</code>), cập nhật sang trạng thái đang xử lý, để hai worker không gửi trùng một dòng (DEC-NOTIF-14).</li><li><strong>MUST</strong> idempotent ở mức hợp lý: một dòng <code>notification</code> đã <code>status='sent'</code> <strong>MUST NOT</strong> bị gửi lại; <code>ClaimPushBatch</code> chỉ lấy <code>status='queued'</code>, và <code>MarkSent</code>/<code>MarkFailed</code> là cập nhật có điều kiện trên trạng thái hiện tại.</li><li><strong>MUST</strong> trả error (không panic) khi OAuth2 token hết hạn/làm mới thất bại hoặc FCM trả lỗi không phân loại được, để dispatcher loop áp retry/backoff và không kẹt worker.</li><li><strong>SHOULD</strong> phát OTel metric: <code>fcm_send_total{result}</code> (counter: sent|retry|failed|token_dead), <code>fcm_retry_total{reason}</code> (counter), <code>fcm_token_invalidated_total</code> (counter), <code>fcm_quota_throttled_total</code> (counter - đếm khi token bucket chặn), <code>fcm_send_duration_ms</code> (histogram).</li><li><strong>MUST</strong> giữ ranh giới trách nhiệm: dispatcher chỉ gửi push cho dòng đã được FR-NOTIF-001 chọn kênh và render. Nó <strong>MUST NOT</strong> tự chọn kênh, <strong>MUST NOT</strong> đổi nội dung, và <strong>MUST NOT</strong> gọi email/SMS provider.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao FCM HTTP v1 chứ không legacy server-key (DEC-NOTIF-10)?</strong> Google đã deprecate legacy FCM API (<code>/fcm/send</code> với server key tĩnh) và tắt dần. HTTP v1 dùng OAuth2 service-account ngắn hạn, an toàn hơn (không phơi key tĩnh dài hạn), và là đường duy nhất còn được hỗ trợ lâu dài cùng các tính năng override theo nền tảng. Xây thẳng trên v1 để khỏi phải làm lại khi legacy ngừng hẳn.</p>\n<p><strong>Vì sao token bucket 600k/phút nội bộ (DEC-NOTIF-11)?</strong> Quota mặc định FCM là 600.000 message/phút/project và \"phủ trên 99% nhà phát triển FCM\". Ta đặt một cái van nội bộ ngay trước lời gọi FCM để chủ động ở dưới ngưỡng. Tự throttle rẻ hơn nhiều so với để FCM trả 429 rồi backoff: van nội bộ làm mượt đỉnh trước khi nó thành lỗi server, còn backoff là lưới an toàn cho phần tràn. Hai lớp bổ trợ nhau.</p>\n<p><strong>Vì sao 429 phải backoff + tôn trọng Retry-After, không drop (DEC-NOTIF-12)?</strong> Đỉnh tải của SănDeal là 00:00 - flash sale mở, hàng loạt alert giá nổ cùng lúc (§3.8). Đúng lúc đó là lúc dễ vượt quota nhất. 429 <code>RESOURCE_EXHAUSTED</code> là tín hiệu \"chậm lại\", không phải \"bỏ đi\". Drop thẳng nghĩa là người dùng mất đúng cảnh báo họ chờ cả ngày. Backoff có jitter tránh cả đàn worker thử lại đồng pha (thundering herd); tôn trọng <code>Retry-After</code> là làm theo đúng nhịp FCM yêu cầu thay vì đoán.</p>\n<p><strong>Vì sao UNREGISTERED set verified=false thay vì xóa (DEC-NOTIF-13)?</strong> Token trả <code>UNREGISTERED</code> là máy đã gỡ app hoặc token đã xoay vòng. Gửi tiếp vào đó là đốt quota và rate-limit vào hư không, lại càng dễ chạm trần lúc cao điểm. Đặt <code>verified=false</code> (thay vì xóa hàng) giữ ranh giới sạch với FR-NOTIF-001: routing chỉ coi bản ghi <code>verified=true</code> là khả dụng, nên token chết tự rớt khỏi vòng chọn kênh mà vẫn còn vết để chẩn đoán. Khi thiết bị đăng ký lại, token mới ghi đè và <code>verified</code> bật lại.</p>\n<p><strong>Vì sao consumer của fan-out chứ không tự lấy thẳng từ DB (DEC-NOTIF-14)?</strong> Fan-out (FR-NOTIF-003) là nơi áp flatten-the-curve và rải tải theo thời gian; dispatcher chỉ là cái miệng cuối nhặt dòng <code>status='queued'</code> đã được fan-out chuẩn bị. Tách như vậy cho phép nhiều dispatcher push chạy song song mà không phải tự lo việc rải đỉnh, và giữ logic gửi (rate-limit, backoff, token) gọn trong một chỗ. <code>FOR UPDATE SKIP LOCKED</code> làm việc nhặt an toàn khi scale ngang worker.</p>\n<p><strong>Vì sao ranh giới cứng với FR-NOTIF-001 (§1 #12)?</strong> FR-NOTIF-001 đã quyết kênh (push là rẻ nhất khả dụng) và render nội dung an toàn rồi. Nếu dispatcher lại đụng vào chọn kênh hay nội dung thì hai chỗ cùng sửa một thứ, khó test và dễ lệch. Dispatcher dừng đúng ở \"gửi token này nội dung này qua FCM, ghi kết quả\". Trách nhiệm đơn, test gọn.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Repo: nhặt việc + cập nhật trạng thái (Go)</h3>\n<p>Không cần migration mới - tái dùng <code>notification</code> và <code>user_channel_token</code> của FR-NOTIF-001. Chỉ bổ sung hàm repo.</p>\n<pre><code class=\"language-go\">// services/notif/internal/notif/repo.go (bổ sung)\n\n// ClaimPushBatch nhặt tối đa n dòng push đang queued, khóa hàng để worker khác bỏ qua.\n// Dùng FOR UPDATE SKIP LOCKED để nhiều dispatcher chạy song song không gửi trùng.\nfunc (r *Repo) ClaimPushBatch(ctx context.Context, n int) ([]PushJob, error) {\n    rows, err := r.pool.Query(ctx, `\n        SELECT n.id, n.user_id, t.address AS token, n.payload\n        FROM notification n\n        JOIN user_channel_token t\n          ON t.user_id = n.user_id AND t.channel = 'push' AND t.verified = true\n        WHERE n.channel = 'push' AND n.status = 'queued'\n        ORDER BY n.scheduled_at NULLS FIRST, n.id\n        FOR UPDATE OF n SKIP LOCKED\n        LIMIT $1`, n)\n    if err != nil {\n        return nil, err\n    }\n    defer rows.Close()\n    var jobs []PushJob\n    for rows.Next() {\n        var j PushJob\n        if err := rows.Scan(&amp;j.NotifID, &amp;j.UserID, &amp;j.Token, &amp;j.Payload); err != nil {\n            return nil, err\n        }\n        jobs = append(jobs, j)\n    }\n    return jobs, rows.Err()\n}\n\n// MarkSent: queued -&gt; sent + sent_at (chỉ khi còn queued, idempotent).\nfunc (r *Repo) MarkSent(ctx context.Context, id int64) error {\n    _, err := r.pool.Exec(ctx,\n        `UPDATE notification SET status='sent', sent_at=now()\n         WHERE id=$1 AND status='queued'`, id)\n    return err\n}\n\n// MarkFailed: queued -&gt; failed (vượt retry hoặc token chết); fan-out lo dead-letter.\nfunc (r *Repo) MarkFailed(ctx context.Context, id int64) error {\n    _, err := r.pool.Exec(ctx,\n        `UPDATE notification SET status='failed'\n         WHERE id=$1 AND status='queued'`, id)\n    return err\n}\n\n// InvalidateToken: tắt token chết (UNREGISTERED) -&gt; routing FR-NOTIF-001 thôi chọn push.\nfunc (r *Repo) InvalidateToken(ctx context.Context, userID int64) error {\n    _, err := r.pool.Exec(ctx,\n        `UPDATE user_channel_token SET verified=false, updated_at=now()\n         WHERE user_id=$1 AND channel='push'`, userID)\n    return err\n}</code></pre>\n<h3>FCM HTTP v1 client (Go)</h3>\n<pre><code class=\"language-go\">// services/notif/internal/fcm/client.go\n\n// SendResult phân loại phản hồi FCM để dispatcher hành xử (§1 #6).\ntype SendResult int\n\nconst (\n    ResultSent      SendResult = iota // 200 OK, có name\n    ResultRetry                       // 429/500/503/timeout -&gt; backoff\n    ResultTokenDead                   // UNREGISTERED / INVALID_ARGUMENT token\n    ResultFailed                      // lỗi vĩnh viễn khác\n)\n\ntype SendOutcome struct {\n    Result     SendResult\n    RetryAfter time.Duration // &gt; 0 khi FCM trả header Retry-After (§1 #5)\n}\n\n// Send gửi một message qua FCM HTTP v1 (DEC-NOTIF-10).\nfunc (c *Client) Send(ctx context.Context, msg Message) (SendOutcome, error) {\n    body, _ := json.Marshal(map[string]any{\"message\": msg})\n    tok, err := c.oauth.Token(ctx) // OAuth2 access-token từ service-account\n    if err != nil {\n        return SendOutcome{Result: ResultRetry}, err\n    }\n    url := fmt.Sprintf(\"https://fcm.googleapis.com/v1/projects/%s/messages:send\", c.projectID)\n    req, _ := http.NewRequestWithContext(ctx, http.MethodPost, url, bytes.NewReader(body))\n    req.Header.Set(\"Authorization\", \"Bearer \"+tok)\n    req.Header.Set(\"Content-Type\", \"application/json\")\n\n    resp, err := c.http.Do(req)\n    if err != nil {\n        return SendOutcome{Result: ResultRetry}, err // timeout/mạng -&gt; retry\n    }\n    defer resp.Body.Close()\n    return classify(resp), nil\n}\n\n// classify ánh xạ HTTP status + error code FCM sang SendOutcome.\nfunc classify(resp *http.Response) SendOutcome {\n    switch resp.StatusCode {\n    case 200:\n        return SendOutcome{Result: ResultSent}\n    case 429:\n        return SendOutcome{Result: ResultRetry, RetryAfter: parseRetryAfter(resp.Header)}\n    case 500, 503:\n        return SendOutcome{Result: ResultRetry, RetryAfter: parseRetryAfter(resp.Header)}\n    case 404:\n        return SendOutcome{Result: ResultTokenDead} // UNREGISTERED\n    case 400:\n        // INVALID_ARGUMENT: token sai định dạng -&gt; coi như token chết\n        return SendOutcome{Result: ResultTokenDead}\n    default:\n        return SendOutcome{Result: ResultFailed}\n    }\n}</code></pre>\n<h3>Backoff + token bucket (Go)</h3>\n<pre><code class=\"language-go\">// services/notif/internal/fcm/backoff.go\n\n// nextDelay: exponential có jitter; ưu tiên Retry-After của FCM khi có (§1 #5).\nfunc nextDelay(attempt int, retryAfter time.Duration) time.Duration {\n    if retryAfter &gt; 0 {\n        return retryAfter // tôn trọng FCM (DEC-NOTIF-12)\n    }\n    base := time.Duration(1&lt;&lt;attempt) * time.Second // 1s,2s,4s,8s...\n    if base &gt; 5*time.Minute {\n        base = 5 * time.Minute\n    }\n    jitter := time.Duration(rand.Int63n(int64(base / 2)))\n    return base/2 + jitter // ±50% tránh thundering herd lúc 00:00\n}\n\n// services/notif/internal/fcm/quota.go\n\n// Bucket: token bucket ~600k/phút/project (DEC-NOTIF-11), refill mỗi phút.\ntype Bucket struct {\n    mu       sync.Mutex\n    tokens   int\n    capacity int\n    last     time.Time\n}\n\nfunc NewBucket() *Bucket { return &amp;Bucket{tokens: 600_000, capacity: 600_000, last: time.Now()} }\n\n// Allow trả false khi đã chạm trần phút này -&gt; dispatcher chờ/throttle (fcm_quota_throttled_total).\nfunc (b *Bucket) Allow() bool {\n    b.mu.Lock()\n    defer b.mu.Unlock()\n    if time.Since(b.last) &gt;= time.Minute {\n        b.tokens, b.last = b.capacity, time.Now() // refill theo phút\n    }\n    if b.tokens &lt;= 0 {\n        return false\n    }\n    b.tokens--\n    return true\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>Client.Send</code> gọi đúng endpoint HTTP v1 <code>.../v1/projects/{project_id}/messages:send</code> với header <code>Authorization: Bearer &lt;oauth-token&gt;</code>; KHÔNG gọi legacy <code>/fcm/send</code>.</li><li>Phản hồi 200 (có <code>name</code>) -&gt; <code>MarkSent</code>: dòng <code>notification</code> chuyển <code>status='sent'</code> và <code>sent_at</code> được set.</li><li>Phản hồi 429 <code>RESOURCE_EXHAUSTED</code> -&gt; <code>SendOutcome.Result=ResultRetry</code>; dòng giữ <code>status='queued'</code> (không bị drop), worker lập lịch thử lại theo backoff.</li><li>Khi 429 kèm header <code>Retry-After: 30</code> -&gt; <code>nextDelay</code> trả đúng 30s (tôn trọng FCM thay vì công thức exponential).</li><li>Không có <code>Retry-After</code> -&gt; <code>nextDelay(attempt)</code> tăng theo cấp số nhân có jitter, có trần (&lt;= 5 phút).</li><li>Phản hồi 404 <code>UNREGISTERED</code> -&gt; <code>ResultTokenDead</code>; <code>InvalidateToken</code> set <code>user_channel_token.verified=false</code>, dòng <code>notification</code> -&gt; <code>status='failed'</code>.</li><li>Phản hồi 400 <code>INVALID_ARGUMENT</code> (token sai định dạng) -&gt; <code>ResultTokenDead</code>, xử lý như token chết.</li><li><code>ClaimPushBatch</code> chỉ trả dòng <code>channel='push'</code>, <code>status='queued'</code>, và chỉ ghép với <code>user_channel_token</code> <code>verified=true</code>; token chưa verified KHÔNG được nhặt.</li><li>Hai worker <code>ClaimPushBatch</code> đồng thời (FOR UPDATE SKIP LOCKED) KHÔNG nhặt trùng cùng một dòng.</li><li>Token bucket: gọi thứ 600.001 trong cùng một phút -&gt; <code>Allow()</code> trả false (<code>fcm_quota_throttled_total</code> tăng); sau khi sang phút mới -&gt; refill, <code>Allow()</code> trả true.</li><li>Dòng đã <code>status='sent'</code> -&gt; <code>MarkSent</code>/<code>MarkFailed</code> lần nữa không đổi gì (cập nhật có điều kiện <code>status='queued'</code>); không gửi lại.</li><li>Metric <code>fcm_send_total{result}</code> tăng đúng nhánh (sent/retry/failed/token_dead); <code>fcm_token_invalidated_total</code> tăng khi gặp UNREGISTERED.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/notif/internal/fcm/client_test.go\nfunc TestSend_Success_MarksSent(t *testing.T) {\n    srv := stubFCM(t, 200, `{\"name\":\"projects/p/messages/123\"}`, nil)\n    c := newClient(t, srv.URL)\n    out, err := c.Send(ctx, Message{Token: \"tok-ok\"})\n    require.NoError(t, err)\n    require.Equal(t, ResultSent, out.Result)\n}\n\nfunc TestSend_UsesHTTPv1Endpoint_NotLegacy(t *testing.T) {\n    var gotPath string\n    srv := captureRequest(t, &amp;gotPath, 200, `{\"name\":\"n\"}`)\n    c := newClient(t, srv.URL)\n    c.Send(ctx, Message{Token: \"tok\"})\n    require.Contains(t, gotPath, \"/v1/projects/\")\n    require.Contains(t, gotPath, \"messages:send\")\n    require.NotContains(t, gotPath, \"/fcm/send\") // legacy không được dùng (DEC-NOTIF-10)\n}\n\nfunc TestSend_429_TriggersRetry_NotDropped(t *testing.T) {\n    srv := stubFCM(t, 429, `{\"error\":{\"status\":\"RESOURCE_EXHAUSTED\"}}`, nil)\n    c := newClient(t, srv.URL)\n    out, _ := c.Send(ctx, Message{Token: \"tok\"})\n    require.Equal(t, ResultRetry, out.Result) // không drop, sẽ thử lại (DEC-NOTIF-12)\n}\n\nfunc TestSend_429_RespectsRetryAfter(t *testing.T) {\n    srv := stubFCM(t, 429, `{}`, http.Header{\"Retry-After\": []string{\"30\"}})\n    c := newClient(t, srv.URL)\n    out, _ := c.Send(ctx, Message{Token: \"tok\"})\n    require.Equal(t, 30*time.Second, nextDelay(0, out.RetryAfter)) // tôn trọng Retry-After\n}\n\nfunc TestSend_Unregistered_MarksTokenDead(t *testing.T) {\n    srv := stubFCM(t, 404, `{\"error\":{\"status\":\"UNREGISTERED\"}}`, nil)\n    c := newClient(t, srv.URL)\n    out, _ := c.Send(ctx, Message{Token: \"tok-dead\"})\n    require.Equal(t, ResultTokenDead, out.Result) // -&gt; InvalidateToken + status=failed (DEC-NOTIF-13)\n}\n\n// services/notif/internal/fcm/dispatcher_test.go\nfunc TestDispatch_Unregistered_InvalidatesTokenAndFails(t *testing.T) {\n    d, repo, uid, nid := setupQueuedPush(t, stubFCM(t, 404, `{\"error\":{\"status\":\"UNREGISTERED\"}}`, nil))\n    d.RunOnce(ctx)\n    require.False(t, repo.tokenVerified(t, uid))           // verified=false\n    require.Equal(t, \"failed\", repo.status(t, nid))        // notification.status=failed\n}\n\nfunc TestDispatch_Success_SetsSentAt(t *testing.T) {\n    d, repo, _, nid := setupQueuedPush(t, stubFCM(t, 200, `{\"name\":\"n\"}`, nil))\n    d.RunOnce(ctx)\n    require.Equal(t, \"sent\", repo.status(t, nid))\n    require.NotNil(t, repo.sentAt(t, nid))\n}\n\n// services/notif/internal/fcm/backoff_test.go\nfunc TestQuota_BlocksOverLimitThenRefills(t *testing.T) {\n    b := NewBucket()\n    for i := 0; i &lt; 600_000; i++ { require.True(t, b.Allow()) }\n    require.False(t, b.Allow())     // vượt 600k/phút -&gt; chặn (DEC-NOTIF-11)\n    b.last = time.Now().Add(-2 * time.Minute)\n    require.True(t, b.Allow())      // sang phút mới -&gt; refill\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>repo.go</code> (ClaimPushBatch + MarkSent/MarkFailed + InvalidateToken trên schema sẵn của FR-NOTIF-001) -&gt; <code>message.go</code> (dựng JSON HTTP v1) -&gt; <code>client.go</code> (Send + classify) -&gt; <code>backoff.go</code> + <code>quota.go</code> (retry + van quota) -&gt; <code>dispatcher.go</code> (vòng RunOnce: claim -&gt; Allow -&gt; Send -&gt; mark) -&gt; tests. Dispatcher chạy thành pool worker, mỗi worker <code>ClaimPushBatch</code> rồi gửi; <code>FOR UPDATE SKIP LOCKED</code> giữ an toàn khi scale ngang. Fan-out (FR-NOTIF-003) là thứ đẩy dòng vào <code>status='queued'</code> và lo rải đỉnh; dispatcher chỉ nhặt và gửi.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>FR-NOTIF-001</strong> - bảng <code>notification</code> (đọc <code>channel='push'</code>, cập nhật <code>status</code>/<code>sent_at</code>) và <code>user_channel_token</code> (đọc token <code>verified=true</code>, set <code>verified=false</code> khi token chết). Dispatcher KHÔNG routing/render - hai việc đó thuộc FR-NOTIF-001.</li><li><strong>FR-NOTIF-003 (upstream)</strong> - fan-out chuyển dòng <code>notification</code> push sang <code>status='queued'</code> và rải đỉnh (flatten-the-curve); dispatcher là consumer downstream nhặt các dòng đó. Dead-letter các dòng <code>status='failed'</code> cũng do fan-out lo.</li><li><strong>FR-INFRA-002</strong> - <code>app_user</code> cho FK <code>user_id</code> (gián tiếp qua schema FR-NOTIF-001).</li><li>Lib: <code>golang.org/x/oauth2/google</code> (OAuth2 service-account cho HTTP v1), <code>net/http</code>, <code>encoding/json</code>, <code>pgx</code> (FOR UPDATE SKIP LOCKED).</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Request gửi FCM HTTP v1 (dispatcher dựng từ dòng notification push)</h3>\n<pre><code class=\"language-json\">{\n  \"message\": {\n    \"token\": \"fcm-registration-token-cua-thiet-bi\",\n    \"notification\": {\n      \"title\": \"Giá đã giảm về mức bạn chờ\",\n      \"body\": \"Sản phẩm bạn theo dõi còn 79.000 VND.\"\n    },\n    \"data\": { \"product_id\": \"90112\", \"deeplink\": \"sandeal://product/90112\" },\n    \"webpush\": { \"fcm_options\": { \"link\": \"https://sandeal.vn/p/90112\" } },\n    \"android\": { \"priority\": \"high\" }\n  }\n}</code></pre>\n<h3>Dòng notification: queued -&gt; sent sau khi gửi thành công</h3>\n<pre><code class=\"language-sql\">-- Trước (fan-out đã đẩy vào hàng đợi):\n-- 55012 | push | price_below | queued | sent_at = NULL\nUPDATE notification SET status='sent', sent_at=now() WHERE id=55012 AND status='queued';\n-- Sau: 55012 | push | price_below | sent | sent_at = 2026-06-28 00:00:03+07</code></pre>\n<h3>Token chết (UNREGISTERED): tắt verified, dòng -&gt; failed</h3>\n<pre><code class=\"language-sql\">UPDATE user_channel_token SET verified=false, updated_at=now() WHERE user_id=4021 AND channel='push';\nUPDATE notification        SET status='failed'                  WHERE id=55013 AND status='queued';</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>APNs (iOS) là dispatcher riêng (FR-NOTIF-005) - cùng vòng đời <code>notification</code> nhưng provider khác; không gộp vào FR này.</li><li>Multicast / <code>messages:send</code> theo batch nhiều token một lần - tối ưu throughput giai đoạn sau nếu volume push đủ lớn để cần.</li><li>Xin tăng quota tạm thời trước sự kiện đỉnh (vd siêu sale 11.11 vượt 600k/phút) - quy trình vận hành ở §11, chưa cần code.</li><li>Đo deliverability thật (push tới mà người dùng có thấy không) - cần tín hiệu client-side, để FR analytics sau.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Dùng legacy server-key API</td><td>test <code>UsesHTTPv1Endpoint</code></td><td>gửi lỗi cứng (đã deprecated)</td><td>Chỉ gọi HTTP v1 messages:send (DEC-NOTIF-10)</td></tr><tr><td>429 RESOURCE_EXHAUSTED lúc đỉnh 00:00</td><td><code>fcm_send_total{retry}</code></td><td>nguy cơ mất push nếu drop</td><td>Backoff + Retry-After, giữ queued, thử lại (DEC-NOTIF-12)</td></tr><tr><td>Bỏ qua Retry-After, thử lại quá sớm</td><td>test <code>RespectsRetryAfter</code></td><td>bị siết tiếp, vòng lặp 429</td><td><code>nextDelay</code> ưu tiên Retry-After của FCM</td></tr><tr><td>Token UNREGISTERED (app đã gỡ)</td><td>HTTP 404 + classify</td><td>đốt quota vào token chết</td><td><code>InvalidateToken</code> verified=false + status=failed (DEC-NOTIF-13)</td></tr><tr><td>Token sai định dạng</td><td>HTTP 400 INVALID_ARGUMENT</td><td>gửi hỏng lặp lại</td><td>Coi như token chết, ngừng gửi</td></tr><tr><td>Vượt 600k/phút từ phía ta</td><td>token bucket <code>Allow()=false</code></td><td>FCM sẽ trả 429</td><td>Van nội bộ chặn trước (DEC-NOTIF-11), <code>fcm_quota_throttled_total</code></td></tr><tr><td>Hai worker gửi trùng một dòng</td><td>test concurrency</td><td>push đúp tới user</td><td>FOR UPDATE SKIP LOCKED (§1 #8)</td></tr><tr><td>OAuth2 token hết hạn/làm mới lỗi</td><td>Send trả ResultRetry+err</td><td>không gửi được</td><td>Retry; cache + refresh token service-account</td></tr><tr><td>Gửi lại dòng đã sent</td><td>cập nhật có điều kiện</td><td>spam người dùng</td><td>MarkSent/Claim chỉ chạm status='queued' (§1 #9)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Dispatcher FCM là cái miệng cuối của kênh push - kênh ưu tiên số một trong cost model §3.6 (push gần miễn phí); nó nhặt dòng <code>queued</code>, gửi, ghi <code>sent</code>/<code>failed</code>, không hơn.</li><li>Quota mặc định FCM là 600.000 message/phút/project và phủ trên 99% nhà phát triển; token bucket nội bộ giữ ta dưới ngưỡng, backoff là lưới an toàn cho phần tràn.</li><li>Xin tăng quota phải đặt trước: gửi yêu cầu trước &gt;= 15 ngày cho mức thường; nếu cần &gt; 18.000.000 message/phút thì trước &gt;= 30 ngày; mỗi project chỉ được tối đa 2 sự kiện temp-quota mỗi năm. Lên lịch trước các đợt siêu sale.</li><li>Ranh giới cứng với FR-NOTIF-001: dispatcher không routing, không render lại nội dung; chỉ gửi push cho dòng đã định kênh. Email/SMS có dispatcher riêng (FR-NOTIF-006/007).</li><li>UNREGISTERED set <code>verified=false</code> thay vì xóa hàng: routing tự thôi chọn push, vẫn còn vết chẩn đoán; thiết bị đăng ký lại sẽ ghi đè token và bật lại verified.</li><li>Backoff có jitter cố ý +/-50% để cả đàn worker không thử lại đồng pha sau một đợt 429 hàng loạt lúc 00:00 (chống thundering herd).</li></ul>\n<hr>\n<p><em>Hết FR-NOTIF-002. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
