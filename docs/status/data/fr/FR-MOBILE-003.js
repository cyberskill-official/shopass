window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-MOBILE-003"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>App mobile <strong>MUST</strong> cho người dùng chia sẻ deal đang sale qua deep-link mang <code>referral_code</code> (lấy từ FR-BILL-004), mở app từ universal/app link tới đúng màn sản phẩm, và gắn attribution lúc người nhận đăng ký. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> dựng share link dạng universal link (iOS) / app link (Android) trỏ tới URL <code>https://sandeal/...</code> mang <code>product_id</code> + <code>ref</code> (referral_code của người chia sẻ) (DEC-MOBILE-20); mở app nếu đã cài, fallback web nếu chưa.</li><li><strong>MUST</strong> lấy <code>referral_code</code> từ FR-BILL-004 (mỗi user một mã) (DEC-MOBILE-21); mobile <strong>MUST NOT</strong> tự sinh mã.</li><li><strong>MUST</strong> chỉ tạo + chia sẻ link khi người dùng chủ động bấm \"Chia sẻ\" trên một deal (user-initiated) (DEC-MOBILE-22); <strong>MUST NOT</strong> tự động đăng/gửi link nền.</li><li><strong>MUST</strong> khi mở app từ deep-link có <code>ref</code>: route tới đúng màn sản phẩm (<code>product_id</code>) và LƯU <code>ref</code> tạm (pending) (DEC-MOBILE-23).</li><li><strong>MUST</strong> gắn attribution chỉ tại lúc người nhận đăng ký, qua <code>app_user.referral_code_id</code> của FR-BILL-004 (DEC-MOBILE-23); attribution bất biến sau khi gắn; nếu người nhận đã có referrer thì <strong>MUST NOT</strong> ghi đè.</li><li><strong>MUST</strong> chặn self-referral phía client như lớp phòng vệ đầu (DEC-MOBILE-24): người dùng đang đăng nhập không được dùng <code>ref</code> là mã của chính mình. Backend (FR-BILL-004) vẫn là người gác cổng cuối.</li><li><strong>MUST</strong> chỉ đưa <code>product_id</code> + <code>ref</code> (mã ngắn) vào deep-link công khai (DEC-MOBILE-25); <strong>MUST NOT</strong> nhúng token/PII của người chia sẻ.</li><li><strong>MUST</strong> để việc trả thưởng referral cho backend (FR-BILL-004 phát sự kiện cho FR-TRUST-004 anti-fraud + delay reward); mobile <strong>MUST NOT</strong> tự trả thưởng hay tự tăng <code>uses</code>.</li><li><strong>MUST</strong> xử lý deep-link không hợp lệ/thiếu trường nhã nhặn: link thiếu <code>product_id</code> -&gt; mở màn chủ; <code>ref</code> sai định dạng -&gt; bỏ qua ref, vẫn mở sản phẩm.</li><li><strong>SHOULD</strong> đo telemetry tối thiểu (số lần bấm Chia sẻ, số lần mở từ deep-link) ở mức ẩn danh; <strong>MUST NOT</strong> ghi cặp (người chia sẻ -&gt; người nhận) ngoài attribution chính thức ở backend.</li><li><strong>MUST</strong> dùng <code>httpClient</code> của FR-MOBILE-001 cho lời gọi lấy mã + đăng ký (kế thừa auth + gateway).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p>Vì sao universal/app link, không scheme riêng (DEC-MOBILE-20, §1 #1)? Scheme riêng (<code>sandeal://</code>) không mở được web fallback và dễ bị app khác chiếm. Universal link (iOS) / app link (Android) là URL https thật: nếu có app thì mở app, không có thì mở web - một link phục vụ cả người đã cài lẫn người mới (đúng mục tiêu viral: người nhận có thể chưa cài app).</p>\n<p>Vì sao mã từ backend, không tự sinh (DEC-MOBILE-21, §1 #2)? <code>referral_code</code> là tài sản gắn anti-fraud (FR-BILL-004/TRUST-004). Nếu mobile tự sinh thì có thể trùng, giả, hoặc bỏ qua kiểm tra. Một nguồn phát mã duy nhất ở backend đảm bảo mã unique + truy vết được + gắn đúng người.</p>\n<p>Vì sao user-initiated, không tự đăng nền (DEC-MOBILE-22, §1 #3)? §5.7 nói rõ seeding cộng đồng KHÔNG được spam link affiliate nền. Tự động đăng/gửi link là spam - phá niềm tin cộng đồng và lặp đúng tiếng xấu Honey. Người dùng chủ động bấm \"Chia sẻ\" mới tạo link; SănDeal không bao giờ tự gửi thay họ.</p>\n<p>Vì sao attribution chỉ gắn lúc đăng ký + bất biến (DEC-MOBILE-23, §1 #5)? Gắn referrer tại đăng ký là thời điểm tự nhiên + một-lần, khớp mô hình FR-BILL-004 (referral_code_id bất biến). Lưu ref pending từ lúc click tới lúc đăng ký giữ được attribution qua khoảng người dùng cài app rồi mới tạo tài khoản. Bất biến tránh tranh chấp \"ai giới thiệu\" về sau.</p>\n<p>Vì sao chặn self-referral phía client nhưng backend vẫn gác cổng (DEC-MOBILE-24, §1 #6)? Chặn phía client là trải nghiệm tốt (báo ngay \"không thể tự giới thiệu\") + giảm tải. Nhưng client không đáng tin tuyệt đối (có thể bị sửa). FR-BILL-004 đã cấm self-referral ở backend (DEC-BILL-18) - đó là hàng rào thật. Mobile là lớp phòng vệ đầu, không phải lớp duy nhất.</p>\n<p>Vì sao không nhúng token/PII vào link (DEC-MOBILE-25, §1 #7)? Share link đi ra ngoài (gửi bạn bè, đăng nhóm) - là dữ liệu công khai. Nhúng token nghĩa là rò phiên; nhúng PII (email/tên) nghĩa là rò danh tính người chia sẻ. Chỉ <code>product_id</code> + mã ref ngắn (vốn không phải bí mật) là an toàn để công khai.</p>\n<p>Vì sao trả thưởng do backend + delay (DEC-MOBILE-24, §1 #8)? §5.3 cảnh báo referral abuse + farming tài khoản ảo. Trả thưởng ngay phía client là mời gọi gian lận. FR-BILL-004 phát sự kiện cho FR-TRUST-004 (anti-fraud) và delay reward tới khi vượt kiểm tra. Mobile không chạm vào tiền thưởng - chỉ tạo link + gắn attribution.</p>\n<hr>\n<h2>§3 - Hợp đồng API / mã</h2>\n<h3>Dựng share link - mã từ backend, không token (TypeScript)</h3>\n<pre><code class=\"language-ts\">// apps/mobile/src/share/shareLink.ts\nimport { http } from '../api/httpClient';\n\nconst BASE = 'https://sandeal.app/p';\n\n// lấy referral_code của user hiện tại (FR-BILL-004) - KHÔNG tự sinh.\nasync function myReferralCode(): Promise&lt;string&gt; {\n  const res = await http.get('/v1/referral/code'); // trả mã của user đăng nhập\n  const { code } = await res.json();\n  return code;\n}\n\n// link công khai chỉ mang product_id + ref (mã ngắn). KHÔNG token/PII.\nexport async function buildShareLink(productId: number): Promise&lt;string&gt; {\n  const ref = await myReferralCode();\n  return `${BASE}/${productId}?ref=${encodeURIComponent(ref)}`;\n}</code></pre>\n<h3>Xử lý deep-link + lưu ref pending (TypeScript)</h3>\n<pre><code class=\"language-ts\">// apps/mobile/src/deeplink/linkHandler.ts\nimport { savePendingReferral } from './pendingReferral';\n\ntype ParsedLink = { productId?: number; ref?: string };\n\nexport function parseDeepLink(url: string): ParsedLink {\n  const u = new URL(url);\n  const m = u.pathname.match(/\\/p\\/(\\d+)/);\n  return { productId: m ? Number(m[1]) : undefined, ref: u.searchParams.get('ref') ?? undefined };\n}\n\n// mở app từ link: route tới sản phẩm; lưu ref pending (gắn sau, lúc đăng ký).\nexport async function handleDeepLink(url: string, nav: Navigator): Promise&lt;void&gt; {\n  const { productId, ref } = parseDeepLink(url);\n  if (ref) {\n    await savePendingReferral(ref); // pending tới lúc đăng ký\n  }\n  if (productId) {\n    nav.navigate('Product', { productId });\n  } else {\n    nav.navigate('Home'); // link thiếu product -&gt; màn chủ\n  }\n}</code></pre>\n<h3>Chống self-referral phía client + attribution lúc đăng ký (TypeScript)</h3>\n<pre><code class=\"language-ts\">// apps/mobile/src/deeplink/pendingReferral.ts\nimport { getMyCode } from '../share/shareLink';\n\n// lớp phòng vệ đầu: người đăng nhập không tự dùng mã của chính mình.\nexport async function applyPendingReferralAtSignup(): Promise&lt;string | null&gt; {\n  const ref = await readPendingReferral();\n  if (!ref) return null;\n  const own = await currentUserCodeOrNull(); // null nếu chưa đăng nhập\n  if (own &amp;&amp; own === ref) {\n    await clearPendingReferral();\n    return null; // self-referral - backend cũng sẽ từ chối (FR-BILL-004)\n  }\n  // attribution thật do FR-BILL-004 ghi vào app_user.referral_code_id lúc đăng ký\n  return ref;\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Bấm \"Chia sẻ\" trên một deal -&gt; sinh universal/app link <code>https://sandeal.app/p/{productId}?ref={code}</code>.</li><li><code>ref</code> trong link là <code>referral_code</code> lấy từ <code>GET /v1/referral/code</code> (FR-BILL-004); mobile KHÔNG tự sinh mã.</li><li>Link KHÔNG chứa token hay PII (email/tên) của người chia sẻ (kiểm bằng test).</li><li>Share chỉ xảy ra khi người dùng bấm; KHÔNG có đường tự động đăng/gửi link nền (review + test).</li><li>Mở app từ link có <code>ref</code> -&gt; route tới đúng màn sản phẩm + lưu <code>ref</code> pending.</li><li>Người nhận đăng ký -&gt; attribution gắn qua luồng FR-BILL-004; nếu đã có referrer -&gt; KHÔNG ghi đè.</li><li>Người dùng đang đăng nhập dùng <code>ref</code> là mã của chính mình -&gt; bị chặn phía client (và backend từ chối).</li><li>Link thiếu <code>product_id</code> -&gt; mở màn chủ; <code>ref</code> sai định dạng -&gt; bỏ qua ref, vẫn mở sản phẩm.</li><li>Mobile KHÔNG tự trả thưởng hay tự tăng <code>uses</code>; việc đó do backend (FR-BILL-004 -&gt; FR-TRUST-004).</li><li>Mọi lời gọi đi qua <code>httpClient</code> của FR-MOBILE-001.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-ts\">// apps/mobile/src/share/shareLink.test.ts\ntest('share link mang product_id + ref, không token/PII', async () =&gt; {\n  jest.spyOn(http, 'get').mockResolvedValue(jsonRes({ code: 'CHI2026' }));\n  const link = await buildShareLink(90112);\n  expect(link).toBe('https://sandeal.app/p/90112?ref=CHI2026');\n  expect(link).not.toMatch(/token|bearer|@/i); // không token, không email\n});\n\ntest('mobile không tự sinh mã - luôn gọi backend', async () =&gt; {\n  const getSpy = jest.spyOn(http, 'get').mockResolvedValue(jsonRes({ code: 'CHI2026' }));\n  await buildShareLink(90112);\n  expect(getSpy).toHaveBeenCalledWith('/v1/referral/code');\n});\n\n// apps/mobile/src/deeplink/linkHandler.test.ts\ntest('parse link lấy product_id + ref', () =&gt; {\n  const p = parseDeepLink('https://sandeal.app/p/90112?ref=CHI2026');\n  expect(p.productId).toBe(90112);\n  expect(p.ref).toBe('CHI2026');\n});\n\ntest('mở link có ref -&gt; lưu pending + route sản phẩm', async () =&gt; {\n  const nav = { navigate: jest.fn() };\n  await handleDeepLink('https://sandeal.app/p/90112?ref=CHI2026', nav as any);\n  expect(await readPendingReferral()).toBe('CHI2026');\n  expect(nav.navigate).toHaveBeenCalledWith('Product', { productId: 90112 });\n});\n\ntest('link thiếu product -&gt; màn chủ', async () =&gt; {\n  const nav = { navigate: jest.fn() };\n  await handleDeepLink('https://sandeal.app/p/?ref=CHI2026', nav as any);\n  expect(nav.navigate).toHaveBeenCalledWith('Home');\n});\n\n// apps/mobile/src/deeplink/pendingReferral.test.ts\ntest('self-referral bị chặn phía client', async () =&gt; {\n  await savePendingReferral('CHI2026');\n  jest.spyOn(authClient, 'currentUserCodeOrNull').mockResolvedValue('CHI2026'); // chính mình\n  const ref = await applyPendingReferralAtSignup();\n  expect(ref).toBeNull();\n  expect(await readPendingReferral()).toBeNull();\n});\n\ntest('ref người khác -&gt; giữ để gắn attribution', async () =&gt; {\n  await savePendingReferral('HUY2026');\n  jest.spyOn(authClient, 'currentUserCodeOrNull').mockResolvedValue('CHI2026');\n  const ref = await applyPendingReferralAtSignup();\n  expect(ref).toBe('HUY2026'); // backend (FR-BILL-004) ghi attribution\n});</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: shareLink.ts (lấy mã + dựng link) -&gt; ShareSheet.tsx (user-initiated share) -&gt; linkHandler.ts (parse + route) -&gt; pendingReferral.ts (lưu pending + chống self-referral) -&gt; authClient.ts (gắn ref lúc đăng ký) -&gt; cấu hình apple-app-site-association + assetlinks.json -&gt; tests. Universal/app link cần file association host trên domain <code>sandeal.app</code>. Backend cần <code>GET /v1/referral/code</code> (mặt tiền của FR-BILL-004) trả mã của user đăng nhập, và luồng đăng ký nhận <code>ref</code> để ghi <code>app_user.referral_code_id</code>. Test qua Jest với mock http + storage.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li>FR-MOBILE-001 - scaffold + <code>httpClient</code> + deep-link foundation (React Navigation linking).</li><li>FR-BILL-004 - <code>referral_code</code> (mỗi user một mã) + attribution <code>app_user.referral_code_id</code> + cấm self-referral + phát sự kiện anti-fraud.</li><li>FR-MOBILE-002 (liên quan) - màn deal là nơi đặt nút \"Chia sẻ\".</li><li>FR-TRUST-004 (downstream của BILL) - anti-fraud nhận sự kiện referral để chống farming.</li><li>Lib: <code>react-native-share</code>, React Navigation linking.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Lấy mã của user (GET /v1/referral/code)</h3>\n<pre><code class=\"language-json\">{ \"code\": \"CHI2026\" }</code></pre>\n<h3>Share link công khai (chỉ product_id + ref)</h3>\n<pre><code>https://sandeal.app/p/90112?ref=CHI2026</code></pre>\n<h3>Mở app từ link -&gt; ref pending (lưu cục bộ tạm)</h3>\n<pre><code class=\"language-json\">{ \"pending_referral\": \"CHI2026\", \"saved_at\": \"2026-06-28T09:00:00Z\" }</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Thẻ xem trước (preview card) đẹp khi share lên mạng xã hội (Open Graph cho web fallback) - tinh chỉnh sau.</li><li>Theo dõi hiệu quả từng kênh chia sẻ (Zalo/Messenger/...) ở mức ẩn danh - thêm khi cần đo viral coefficient.</li><li>Thưởng theo cấp (multi-tier referral) - giữ một cấp ở slice này, mở rộng nếu chiến lược tăng trưởng cần, vẫn qua FR-BILL-004 + FR-TRUST-004.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Tự động đăng/gửi link nền</td><td>review + test user-initiated</td><td>spam, tẩy chay cộng đồng</td><td>Chỉ tạo link khi bấm Chia sẻ (DEC-MOBILE-22)</td></tr><tr><td>Mobile tự sinh referral_code</td><td>test luôn gọi backend</td><td>mã trùng/giả, bỏ anti-fraud</td><td>Lấy mã từ FR-BILL-004 (DEC-MOBILE-21)</td></tr><tr><td>Token/PII trong link công khai</td><td>test link không token</td><td>rò phiên/danh tính</td><td>Chỉ product_id + ref ngắn (DEC-MOBILE-25)</td></tr><tr><td>Self-referral</td><td>TestSelf-referral chặn</td><td>farming thưởng</td><td>Chặn client + backend gác cổng (DEC-MOBILE-24)</td></tr><tr><td>Mobile tự trả thưởng</td><td>review + DEC-MOBILE-24</td><td>gian lận bỏ qua anti-fraud</td><td>Thưởng do backend + delay (FR-TRUST-004)</td></tr><tr><td>Ghi đè referrer đã có</td><td>AC #6</td><td>tranh chấp attribution</td><td>Bất biến sau khi gắn (FR-BILL-004)</td></tr><tr><td>Deep-link thiếu/sai trường</td><td>TestLink thiếu product</td><td>app crash/đứng</td><td>Mở màn chủ; bỏ qua ref sai</td></tr><tr><td>Mất attribution giữa click và đăng ký</td><td>TestPending lưu/đọc</td><td>mất công viral</td><td>Lưu ref pending tới lúc đăng ký</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Virality phải user-initiated: người dùng chủ động bấm Chia sẻ; SănDeal không bao giờ tự gửi link (khác spam Honey-style, §5.7).</li><li>Mã referral từ backend (FR-BILL-004) là nguồn duy nhất - mobile không tự sinh, không tự trả thưởng.</li><li>Universal/app link phục vụ cả người đã cài (mở app) lẫn người mới (web fallback) - đúng mục tiêu viral chạm người chưa có app.</li><li>Attribution gắn một-lần lúc đăng ký, bất biến; ref pending giữ được attribution qua khoảng cài app -&gt; tạo tài khoản.</li><li>Self-referral chặn hai lớp: client (trải nghiệm + giảm tải) và backend (hàng rào thật).</li><li>Link công khai chỉ mang product_id + mã ref ngắn - không bao giờ token/PII của người chia sẻ.</li><li>Chống farming/abuse là việc của FR-TRUST-004 qua sự kiện từ FR-BILL-004; mobile chỉ tạo link + gắn attribution.</li></ul>\n<hr>\n<p><em>Hết FR-MOBILE-003. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
