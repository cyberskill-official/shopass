window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-EXT-007"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Content script TikTok Shop <strong>MUST</strong> đọc giỏ hàng qua DOM đã render trong webview/SPA của tab đăng nhập, tránh tuyệt đối API ký (msToken/_signature/X-Bogus), và tái dùng khung reader/normalize/health của FR-EXT-002. Hợp đồng:</p>\n<ol><li><code>manifest.json</code> <strong>MUST</strong> khai <code>content_scripts</code> với <code>matches</code> cho domain TikTok Shop (ví dụ <code>https://*.tiktok.com/*</code> giới hạn trang shop), <code>run_at: \"document_idle\"</code>, và thêm <code>host_permissions</code> tương ứng (per-domain, không <code>&lt;all_urls&gt;</code>, theo FR-EXT-001).</li><li>Content script <strong>MUST</strong> đọc dữ liệu giỏ bằng cách parse DOM đã render (DEC-EXT-35) - TikTok không có đường internal JSON tiện như Shopee vì cart nằm trong webview/SPA và API ký mạnh.</li><li>Content script <strong>MUST NOT</strong> tự sinh, ký, hay gửi <code>msToken</code>/<code>_signature</code>/<code>X-Bogus</code> hay gọi internal API ký của TikTok (DEC-EXT-36). Ngược-kỹ-nghệ chữ ký bị cấm: vừa giòn (đổi liên tục) vừa rủi ro ToS/pháp lý.</li><li>Vì TikTok là SPA (điều hướng client-side, route đổi không tải lại trang), <code>spa-observer.ts</code> <strong>MUST</strong> dùng <code>MutationObserver</code> và/hoặc theo dõi route change để biết khi giỏ render xong, rồi mới đọc (DEC-EXT-38) - không đọc một lần lúc load rồi bỏ.</li><li><code>dom-selectors.ts</code> <strong>MUST</strong> cung cấp nhiều selector dự phòng cho mỗi trường (item/giá/qty) vì DOM TikTok đổi theo A/B test + cấu trúc SPA (DEC-EXT-38). Selector chính trượt -&gt; thử dự phòng trước khi coi là hỏng.</li><li>Khi mọi selector hỏng, content script <strong>MUST</strong> phát health signal về service worker để FR-SCRAPE-006 (DOM-change monitoring) ghi nhận - KHÔNG nuốt lỗi im lặng (tái dùng cơ chế FR-EXT-002).</li><li>Content script <strong>MUST</strong> tái dùng khung reader + <code>normalize.ts</code> + health signal của FR-EXT-002 (DEC-EXT-37); chỉ thay lớp selector/đường đọc đặc thù TikTok. <strong>MUST NOT</strong> phân nhánh một pipeline tối thiểu hóa riêng.</li><li>Content script <strong>MUST</strong> giữ nguyên cam kết niềm tin FR-EXT-002 (DEC-EXT-39): KHÔNG đọc <code>document.cookie</code>/token, KHÔNG thu mật khẩu, token/cookie KHÔNG rời client; chỉ trích <code>productId</code>/giá/qty đã render.</li><li>Content script <strong>MUST</strong> chỉ đọc: KHÔNG tự sửa giỏ, KHÔNG đặt hàng, KHÔNG áp voucher tự động (DEC-EXT-39).</li><li>Kết quả <strong>MUST</strong> gửi cho service worker dạng <code>CartReadMessage</code> typed với <code>platform: \"tiktok\"</code>, chứa CHỈ <code>CartItem[]</code> + <code>VoucherItem[]</code> tối thiểu - đi tiếp qua pipeline FR-EXT-003.</li><li>Content script <strong>MUST</strong> chỉ chạy khi consent \"read_cart\" (FR-EXT-006) bật; gọi <code>ensureConsent</code> trước khi đọc.</li><li>Content script <strong>MUST</strong> xử lý chưa-đăng-nhập / giỏ rỗng lịch sự (không lỗi, báo \"chưa có dữ liệu giỏ\"); và xử lý SPA chưa render xong (chờ observer) thay vì đọc DOM trống rồi báo hỏng nhầm.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao đọc DOM, không gọi API ký (DEC-EXT-35/36)?</strong> TikTok ký request nội bộ bằng msToken/_signature/X-Bogus cùng app attestation - một hệ chống bot tinh vi. Ngược-kỹ-nghệ chữ ký để gọi API là cái bẫy kép: chữ ký đổi mỗi lần TikTok cập nhật (code vỡ liên tục, chi phí bảo trì khổng lồ), VÀ ranh giới ToS/pháp lý nguy hiểm (§5.5). Đọc DOM đã render trong tab đăng nhập của chính người dùng là đường an toàn và bền hơn - đúng tinh thần session piggyback, chỉ khác là không có đường internal JSON.</p>\n<p><strong>Vì sao MutationObserver cho SPA (DEC-EXT-38)?</strong> TikTok là single-page app: bấm vào giỏ đổi route nhưng trang không tải lại, DOM được vẽ lại bằng JS. Reader đọc một lần lúc <code>document_idle</code> sẽ bắt nhầm trạng thái cũ hoặc DOM trống. <code>MutationObserver</code> + theo dõi route change cho reader biết \"giỏ đã render xong, đọc bây giờ\" - đây là khác biệt cốt lõi so với content script Shopee (trang truyền thống hơn).</p>\n<p><strong>Vì sao tái dùng khung FR-EXT-002 (DEC-EXT-37)?</strong> FR-EXT-002 đã định nghĩa khung reader + normalize (tối thiểu hóa) + health signal và đã được test cho cam kết \"không rò cookie\". Phân nhánh một pipeline riêng cho TikTok nghĩa là nhân đôi bug và mở một bề mặt mới có thể lọt credential. Tái dùng giữ một điểm kiểm soát tối thiểu hóa duy nhất; chỉ lớp selector/đường đọc là per-sàn.</p>\n<p><strong>Vì sao giữ nguyên cam kết niềm tin (DEC-EXT-39)?</strong> Ranh giới \"không cookie, không mật khẩu, chỉ đọc\" không phụ thuộc sàn - nó là định vị hậu-Honey của toàn extension (§5.4). TikTok không phải ngoại lệ. Test no-secret-leak tương tự FR-EXT-002 khẳng định ranh giới này cho cả TikTok, không chỉ tin quy ước.</p>\n<p><strong>Vì sao chờ render thay vì báo hỏng nhầm (§1 #12)?</strong> Trong SPA, DOM trống ngay sau khi đổi route là bình thường (chưa vẽ xong), không phải lỗi parser. Nếu reader báo health \"hỏng\" mỗi lần gặp DOM trống tạm thời, FR-SCRAPE-006 ngập tín hiệu nhiễu. Chờ observer báo render xong rồi mới đọc/đánh giá hỏng phân biệt đúng \"chưa xong\" với \"thật sự hỏng\".</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>manifest.json (bổ sung content script TikTok)</h3>\n<pre><code class=\"language-jsonc\">// extension/manifest.json (thêm)\n{\n  \"host_permissions\": [\"https://shopee.vn/*\", \"https://*.tiktok.com/*\"],\n  \"content_scripts\": [\n    {\n      \"matches\": [\"https://*.tiktok.com/*\"],\n      \"js\": [\"content/tiktok/index.js\"],\n      \"run_at\": \"document_idle\"\n    }\n  ]\n}</code></pre>\n<h3>spa-observer.ts (chờ giỏ render xong trong SPA)</h3>\n<pre><code class=\"language-ts\">// extension/src/content/tiktok/spa-observer.ts\nexport function onCartRendered(cb: () =&gt; void): () =&gt; void {\n  const obs = new MutationObserver(() =&gt; {\n    if (document.querySelector(CART_ROOT_SELECTORS.find(s =&gt; document.querySelector(s)) ?? \"x\")) {\n      cb();   // giỏ đã render → đọc bây giờ\n    }\n  });\n  obs.observe(document.body, { childList: true, subtree: true });\n  return () =&gt; obs.disconnect();   // dọn observer\n}</code></pre>\n<h3>cart-reader.ts (đọc DOM, KHÔNG API ký; tái dùng normalize + health FR-EXT-002)</h3>\n<pre><code class=\"language-ts\">// extension/src/content/tiktok/cart-reader.ts\nimport { normalizeCart } from \"../shared/normalize\";     // tái dùng FR-EXT-002\nimport { reportHealth } from \"../shared/health\";         // tái dùng FR-EXT-002\n\nexport async function readTiktokCart(): Promise&lt;CartReadMessage&gt; {\n  if (!(await ensureConsent(\"read_cart\"))) {             // FR-EXT-006 gate\n    return { type: \"CART_READ\", platform: \"tiktok\", items: [], vouchers: [] };\n  }\n  let raw = readCartFromDom();                            // CHỈ DOM; KHÔNG msToken/_signature/X-Bogus\n  if (raw === null) {\n    reportHealth({ platform: \"tiktok\", broke: \"cart\", source: \"dom\" }); // FR-SCRAPE-006\n    raw = [];\n  }\n  const items = normalizeCart(raw);                      // tối thiểu hóa chung\n  return { type: \"CART_READ\", platform: \"tiktok\", items, vouchers: readVouchersFromDom() };\n  // LƯU Ý: KHÔNG document.cookie, KHÔNG gọi API ký ở đây.\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>manifest.json</code> có <code>content_scripts</code> match domain TikTok Shop, <code>run_at: document_idle</code>; <code>host_permissions</code> thêm <code>https://*.tiktok.com/*</code>, KHÔNG <code>&lt;all_urls&gt;</code>.</li><li>Grep <code>src/content/tiktok/**</code>: KHÔNG có <code>msToken</code>, <code>_signature</code>, <code>X-Bogus</code>, KHÔNG gọi internal API ký TikTok.</li><li>Grep <code>src/content/tiktok/**</code>: KHÔNG có <code>document.cookie</code>, KHÔNG đọc <code>input[type=password]</code>.</li><li>Reader đọc giỏ từ DOM mẫu TikTok -&gt; trả đúng item tối thiểu (productId/price/qty).</li><li>Selector chính trượt -&gt; dùng selector dự phòng; vẫn parse được item từ DOM \"biến thể\".</li><li>SPA route đổi (giỏ render lại) -&gt; observer kích hoạt đọc lại; reader không kẹt ở trạng thái cũ.</li><li>Mọi selector hỏng -&gt; phát health signal (broke: \"cart\") VÀ trả <code>items: []</code>, không ném lỗi.</li><li>Reader tái dùng <code>normalize.ts</code> + health của FR-EXT-002 (grep import từ <code>content/shared/</code>), không phân nhánh riêng.</li><li><code>CartReadMessage</code> có <code>platform: \"tiktok\"</code>, chỉ <code>items</code> + <code>vouchers</code>; không cookie/token (introspection payload).</li><li>Reader chỉ chạy khi consent \"read_cart\" bật (gọi <code>ensureConsent</code>); chưa bật -&gt; trả rỗng.</li><li><code>npm test</code> xanh; <code>tsc --noEmit</code> sạch.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-ts\">// extension/test/tiktok-no-api-sign.test.ts\ntest(\"KHÔNG ký/gửi msToken/_signature/X-Bogus, KHÔNG đọc cookie\", async () =&gt; {\n  for (const f of [\"cart-reader\", \"dom-selectors\", \"spa-observer\", \"index\"]) {\n    const src = await readFile(`src/content/tiktok/${f}.ts`, \"utf8\");\n    expect(src).not.toMatch(/msToken|_signature|X-Bogus/i);   // không API ký\n    expect(src).not.toMatch(/document\\.cookie/);              // không cookie\n    expect(src).not.toMatch(/input\\[type=[\"']?password/i);    // không mật khẩu\n  }\n});\n\ntest(\"payload TikTok KHÔNG chứa cookie/token\", async () =&gt; {\n  setConsent([\"read_cart\"]);\n  document.body.innerHTML = tiktokCartFixtureMain;\n  const msg = await readTiktokCart();\n  const flat = JSON.stringify(msg).toLowerCase();\n  for (const b of [\"cookie\", \"token\", \"mstoken\", \"signature\", \"x-bogus\", \"password\"]) {\n    expect(flat).not.toContain(b);\n  }\n});</code></pre>\n<pre><code class=\"language-ts\">// extension/test/tiktok-cart-reader.test.ts\ntest(\"đọc giỏ từ DOM TikTok đã render\", async () =&gt; {\n  setConsent([\"read_cart\"]);\n  document.body.innerHTML = tiktokCartFixtureMain;          // 2 item\n  const msg = await readTiktokCart();\n  expect(msg.platform).toBe(\"tiktok\");\n  expect(msg.items.length).toBe(2);\n});\n\ntest(\"selector chính trượt → dự phòng (biến thể)\", () =&gt; {\n  document.body.innerHTML = tiktokCartFixtureVariant;       // class đổi\n  const items = readCartFromDom();\n  expect(items &amp;&amp; items.length).toBeGreaterThan(0);\n});\n\ntest(\"reader tái dùng normalize/health FR-EXT-002, không phân nhánh\", async () =&gt; {\n  const src = await readFile(\"src/content/tiktok/cart-reader.ts\", \"utf8\");\n  expect(src).toMatch(/from [\"']\\.\\.\\/shared\\/normalize[\"']/);\n  expect(src).toMatch(/from [\"']\\.\\.\\/shared\\/health[\"']/);\n});</code></pre>\n<pre><code class=\"language-ts\">// extension/test/tiktok-spa-observer.test.ts\ntest(\"SPA route đổi → observer kích hoạt đọc lại\", async () =&gt; {\n  const spy = jest.fn();\n  const stop = onCartRendered(spy);\n  document.body.innerHTML = tiktokCartFixtureMain;          // mô phỏng render\n  await flushMutations();\n  expect(spy).toHaveBeenCalled();\n  stop();\n});\n\ntest(\"parse hỏng hẳn → health + items rỗng\", async () =&gt; {\n  setConsent([\"read_cart\"]);\n  document.body.innerHTML = \"&lt;div&gt;không khớp&lt;/div&gt;\";\n  const sp = jest.fn(); setHealthReporter(sp);\n  const msg = await readTiktokCart();\n  expect(sp).toHaveBeenCalledWith(expect.objectContaining({ broke: \"cart\", platform: \"tiktok\" }));\n  expect(msg.items).toEqual([]);\n});</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: cập nhật <code>manifest.json</code> (content_scripts tiktok + host_permissions) -&gt; <code>dom-selectors.ts</code> (bảng selector dự phòng TikTok) -&gt; <code>spa-observer.ts</code> (MutationObserver/route change báo render xong) -&gt; <code>cart-reader.ts</code> (đọc DOM, tái dùng normalize + health FR-EXT-002, gate consent) -&gt; <code>index.ts</code> (entry, gửi CartReadMessage platform tiktok) -&gt; tests. KHÔNG chạm msToken/_signature/X-Bogus. Khung reader/normalize/health dùng chung với Shopee (FR-EXT-002) và Lazada (FR-EXT-008); chỉ lớp selector + observer SPA là đặc thù TikTok. Kết quả qua pipeline FR-EXT-003.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>FR-EXT-002</strong> - cung cấp khung reader + normalize + health signal tái dùng; ranh giới niềm tin chung.</li><li><strong>FR-EXT-003</strong> - pipeline tối thiểu hóa nhận <code>CartReadMessage(platform:tiktok)</code>, lọc lần hai.</li><li><strong>FR-EXT-006</strong> - consent \"read_cart\" gate trước khi đọc.</li><li><strong>FR-SCRAPE-006</strong> - DOM-change monitoring nhận health signal khi parser TikTok hỏng.</li><li><strong>FR-SCRAPE-007 (song song, P2)</strong> - adapter TikTok backend (ưu tiên DOM-render, né API ký); reader này là phía client.</li><li><strong>FR-TRUST-002</strong> - local-first + tối thiểu hóa; reader giữ cam kết.</li><li><strong>NFR-EXT-001</strong> - content script gửi message cho SW ephemeral; không giữ state global.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Message content TikTok -&gt; service worker (tối thiểu, KHÔNG credential)</h3>\n<pre><code class=\"language-json\">{\n  \"type\": \"CART_READ\",\n  \"platform\": \"tiktok\",\n  \"items\": [\n    { \"productId\": \"T-883201\", \"price\": 159000, \"qty\": 1 },\n    { \"productId\": \"T-771045\", \"price\": 49000, \"qty\": 3 }\n  ],\n  \"vouchers\": [\n    { \"code\": \"TIKTOKFREESHIP\", \"minSpend\": 0, \"discountText\": \"Freeship 20k\" }\n  ]\n}</code></pre>\n<h3>Health signal khi parser TikTok hỏng (-&gt; FR-SCRAPE-006)</h3>\n<pre><code class=\"language-json\">{ \"type\": \"PARSER_HEALTH\", \"platform\": \"tiktok\", \"broke\": \"cart\", \"source\": \"dom\", \"ts\": 1790000000000 }</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Đọc trang sản phẩm TikTok (PDP) ngoài giỏ - slice sau; P2 slice 1 nhắm giỏ + voucher.</li><li>Xử lý TikTok in-app webview (ngoài trình duyệt desktop) - hoãn tới mobile (FR-MOBILE-002); FR này nhắm web desktop.</li><li>Ngưỡng debounce observer cho SPA route đổi nhanh liên tiếp - tinh chỉnh khi đo thấy đọc lại quá dày.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Ngược-kỹ-nghệ ký msToken/_signature/X-Bogus</td><td>tiktok-no-api-sign test</td><td>giòn + rủi ro ToS/pháp lý</td><td>Chỉ đọc DOM render (DEC-EXT-36)</td></tr><tr><td>Đọc document.cookie/token</td><td>no-secret-leak test</td><td>rò credential, §5.4</td><td>Cấm về mã (DEC-EXT-39)</td></tr><tr><td>Thu thập mật khẩu</td><td>grep password</td><td>phá cam kết lõi</td><td>Không hook form login (DEC-EXT-39)</td></tr><tr><td>Đọc DOM trống lúc SPA chưa render</td><td>spa-observer test</td><td>parse hỏng nhầm</td><td>Chờ observer báo render xong (DEC-EXT-38)</td></tr><tr><td>Selector chính trượt</td><td>health + variant test</td><td>mất dữ liệu giỏ</td><td>Selector dự phòng (DEC-EXT-38)</td></tr><tr><td>Parse hỏng nuốt im lặng</td><td>thiếu health signal</td><td>mất tính năng âm thầm</td><td>Bắt buộc health -&gt; FR-SCRAPE-006 (§1 #6)</td></tr><tr><td>Phân nhánh pipeline riêng</td><td>grep import shared</td><td>nhân đôi bug + bề mặt rò</td><td>Tái dùng normalize/health FR-EXT-002 (DEC-EXT-37)</td></tr><tr><td>Tự sửa giỏ/đặt hàng</td><td>grep mutate</td><td>đặt nhầm + nghi lạm dụng</td><td>Chỉ đọc (DEC-EXT-39)</td></tr><tr><td>Đọc khi chưa consent</td><td>consent gate test</td><td>xử lý không cơ sở pháp lý</td><td>ensureConsent(\"read_cart\") trước (§1 #11)</td></tr><tr><td>host_permissions &lt;all_urls&gt;</td><td>manifest test</td><td>Web Store reject</td><td>Per-domain tiktok.com (§1 #1)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>TikTok Shop (41,31% GMV §6.1) là sàn bắt buộc cho moat đa sàn, nhưng khó nhất: cart trong webview/SPA + API ký mạnh.</li><li>Ranh giới cứng: KHÔNG ngược-kỹ-nghệ msToken/_signature/X-Bogus - giòn + rủi ro ToS/pháp lý (§5.5); chỉ đọc DOM render trong tab đăng nhập.</li><li>SPA cần MutationObserver/route tracking - khác biệt cốt lõi so với content script Shopee; đọc một lần lúc load là sai.</li><li>Tái dùng khung reader/normalize/health của FR-EXT-002 giữ một điểm kiểm soát tối thiểu hóa duy nhất; chỉ selector + observer là per-sàn.</li><li>Cam kết niềm tin (không cookie/mật khẩu, chỉ đọc) không phụ thuộc sàn - test no-secret-leak khẳng định cho cả TikTok.</li></ul>\n<hr>\n<p><em>Hết FR-EXT-007. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
