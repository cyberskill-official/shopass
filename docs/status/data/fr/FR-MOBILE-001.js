window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-MOBILE-001"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>App mobile <strong>MUST</strong> dựng trên React Native (TypeScript), xác thực bằng JWT của FR-AUTH-002 với refresh token trong secure storage của HĐH, và đăng ký push qua FCM (Android + iOS) với device token đăng về backend. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> xác thực bằng JWT access + refresh của FR-AUTH-002 (DEC-MOBILE-02); <strong>MUST NOT</strong> phát hành cơ chế auth riêng cho mobile.</li><li><strong>MUST</strong> lưu refresh token trong secure storage của HĐH (Keychain iOS / Keystore Android qua <code>react-native-keychain</code>); access token chỉ giữ trong bộ nhớ tiến trình. <strong>MUST NOT</strong> lưu refresh token hay mật khẩu trong <code>AsyncStorage</code> thường hoặc bất kỳ kho cleartext nào.</li><li><strong>MUST NOT</strong> bao giờ lưu mật khẩu người dùng trên thiết bị; chỉ giữ token (DEC-MOBILE-06).</li><li><strong>MUST</strong> gọi backend qua API Gateway/BFF (FR-INFRA-001) bằng HTTPS với access token ở header <code>Authorization: Bearer ...</code> (DEC-MOBILE-05).</li><li><strong>MUST</strong> tự refresh khi nhận <code>401</code> do access token hết hạn: dùng refresh token lấy access mới, thử lại request một lần. Nếu refresh thất bại -&gt; đăng xuất (xóa token, về màn Login).</li><li><strong>MUST</strong> đăng ký push qua FCM cho cả Android và iOS (FCM trừu tượng hóa APNs) (DEC-MOBILE-03); device token đăng về backend qua <code>POST /v1/devices</code> để gắn <code>user_id</code> vào <code>user_channel_token</code> (nguồn của FR-NOTIF-002/005).</li><li><strong>MUST</strong> xin quyền nhận push của HĐH trước khi đăng token; nếu người dùng từ chối -&gt; không đăng token, app vẫn dùng được (push là kênh phụ trợ, không chặn).</li><li><strong>MUST</strong> làm tươi device token mỗi lần khởi động và khi HĐH xoay token (<code>onTokenRefresh</code>); token mới đăng về backend, thay token cũ.</li><li><strong>MUST</strong> gỡ liên kết device token (unregister) khi đăng xuất (DEC-MOBILE-04): xóa token khỏi secure storage + bộ nhớ, gọi backend gỡ <code>user_channel_token</code> để ngừng push tới thiết bị đã thoát.</li><li><strong>MUST</strong> cung cấp điều hướng auth-gated: chưa đăng nhập -&gt; màn Login; đã đăng nhập -&gt; app shell (khung cho FR-MOBILE-002/003).</li><li><strong>SHOULD</strong> đo client telemetry tối thiểu (login thành công/thất bại, đăng token thành công/thất bại) ở mức ẩn danh, tôn trọng consent.</li><li><strong>MUST</strong> đảm bảo đăng xuất xóa sạch mọi token khỏi secure storage và bộ nhớ (không còn dấu vết phiên).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p>Vì sao React Native, không Flutter ở slice 1 (DEC-MOBILE-01)? Web app SănDeal là Next.js/TypeScript (FR-WEB). React Native dùng cùng ngôn ngữ + nhiều khái niệm UI, nên đội ngũ chuyển ngữ cảnh ít hơn và chia sẻ được logic (kiểu dữ liệu, client API). Flutter mạnh nhưng kéo theo Dart + một hệ sinh thái thứ hai. Slice 1 chọn React Native để ship nhanh; FR ghi rõ Flutter là phương án đã cân nhắc nếu sau này đổi.</p>\n<p>Vì sao JWT chung, không auth riêng cho mobile (DEC-MOBILE-02, §1 #1)? Mỗi cơ chế xác thực là một bề mặt phải bảo mật + kiểm toán. Dùng lại JWT access/refresh của FR-AUTH-002 nghĩa là mobile thừa hưởng nguyên vòng đời token đã được thiết kế (hết hạn, refresh, thu hồi) thay vì phát minh lại. Một nguồn sự thật cho xác thực trên mọi client.</p>\n<p>Vì sao refresh token trong secure storage, access chỉ trong bộ nhớ (DEC-MOBILE-02, §1 #2)? Đây là ranh giới bảo mật cốt lõi trên mobile. Refresh token sống lâu nên nếu lộ thì kẻ tấn công duy trì phiên - phải cất trong Keychain/Keystore (vùng được HĐH bảo vệ, gắn với khóa thiết bị). Access token sống ngắn nên giữ trong bộ nhớ tiến trình là đủ; không ghi xuống đĩa giảm bề mặt lộ. AsyncStorage thường là plaintext, đọc được khi thiết bị bị root - tuyệt đối không để token ở đó.</p>\n<p>Vì sao FCM cho cả hai nền tảng (DEC-MOBILE-03, §1 #6)? Backend đã có FCM dispatcher (FR-NOTIF-002). FCM gửi được tới iOS qua APNs ngầm, nên một đường gửi phục vụ cả Android lẫn iOS - bớt một dispatcher phải vận hành ở giai đoạn đầu. FR-NOTIF-005 (APNs trực tiếp) dành cho khi cần kiểm soát sâu hơn ở iOS.</p>\n<p>Vì sao unregister token khi đăng xuất (DEC-MOBILE-04, §1 #9)? Nếu người dùng A đăng xuất rồi người dùng B đăng nhập trên cùng thiết bị (hoặc A bán máy), mà token vẫn liên kết A, thì push của A gửi tới thiết bị giờ thuộc về người khác - rò thông báo cá nhân (giá sản phẩm A theo dõi, đơn hàng A). Gỡ liên kết khi đăng xuất cắt đứt đường rò đó.</p>\n<p>Vì sao tự refresh khi 401 (DEC-MOBILE-05, §1 #5)? Access token hết hạn là việc thường xuyên (vòng đời ngắn). Bắt người dùng đăng nhập lại mỗi lần hết hạn là trải nghiệm tệ. Tự refresh trong nền + thử lại một lần làm phiên liền mạch; chỉ khi refresh cũng hỏng (refresh token hết hạn/thu hồi) mới buộc đăng nhập lại.</p>\n<hr>\n<h2>§3 - Hợp đồng API / mã</h2>\n<h3>Lưu token an toàn (TypeScript)</h3>\n<pre><code class=\"language-ts\">// apps/mobile/src/auth/tokenStore.ts\nimport * as Keychain from 'react-native-keychain';\n\nconst SERVICE = 'sandeal.refresh';\n\n// refresh token CHỈ vào secure storage của HĐH (Keychain/Keystore).\nexport async function saveRefreshToken(token: string): Promise&lt;void&gt; {\n  await Keychain.setGenericPassword('refresh', token, {\n    service: SERVICE,\n    accessible: Keychain.ACCESSIBLE.WHEN_UNLOCKED_THIS_DEVICE_ONLY,\n  });\n}\n\nexport async function getRefreshToken(): Promise&lt;string | null&gt; {\n  const creds = await Keychain.getGenericPassword({ service: SERVICE });\n  return creds ? creds.password : null;\n}\n\nexport async function clearTokens(): Promise&lt;void&gt; {\n  await Keychain.resetGenericPassword({ service: SERVICE });\n  // access token chỉ ở bộ nhớ - bị xóa khi reset state, không chạm đĩa\n}</code></pre>\n<h3>Đăng ký device push (TypeScript)</h3>\n<pre><code class=\"language-ts\">// apps/mobile/src/push/registerDevice.ts\nimport messaging from '@react-native-firebase/messaging';\nimport { http } from '../api/httpClient';\n\n// xin quyền -&gt; lấy FCM token -&gt; đăng về backend (gắn user_channel_token).\nexport async function registerDevice(): Promise&lt;void&gt; {\n  const status = await messaging().requestPermission();\n  const granted =\n    status === messaging.AuthorizationStatus.AUTHORIZED ||\n    status === messaging.AuthorizationStatus.PROVISIONAL;\n  if (!granted) return; // người dùng từ chối - không đăng, app vẫn chạy\n\n  const token = await messaging().getToken();\n  await http.post('/v1/devices', { fcm_token: token, platform: platformName() });\n\n  // HĐH xoay token -&gt; đăng token mới thay token cũ\n  messaging().onTokenRefresh(async (next) =&gt; {\n    await http.post('/v1/devices', { fcm_token: next, platform: platformName() });\n  });\n}\n\nexport async function unregisterDevice(): Promise&lt;void&gt; {\n  const token = await messaging().getToken();\n  await http.delete('/v1/devices', { fcm_token: token }); // gỡ user_channel_token\n  await messaging().deleteToken();\n}</code></pre>\n<h3>Auto-refresh khi 401 (TypeScript)</h3>\n<pre><code class=\"language-ts\">// apps/mobile/src/api/httpClient.ts (rút gọn)\nasync function request(path: string, init: RequestInit, retry = true): Promise&lt;Response&gt; {\n  const res = await fetch(GATEWAY + path, withAuth(init, accessToken));\n  if (res.status === 401 &amp;&amp; retry) {\n    const ok = await refreshAccessToken(); // dùng refresh token trong Keychain\n    if (!ok) {\n      await logout();           // refresh hỏng -&gt; về Login\n      return res;\n    }\n    return request(path, init, false); // thử lại một lần với access mới\n  }\n  return res;\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>App build chạy trên cả Android và iOS (scaffold RN + TypeScript hợp lệ).</li><li>Login thành công lưu refresh token vào Keychain/Keystore; KHÔNG có refresh token nào trong AsyncStorage thường (kiểm bằng test + review).</li><li>Access token chỉ tồn tại trong bộ nhớ; không ghi xuống đĩa.</li><li>Gọi API kèm <code>Authorization: Bearer &lt;access&gt;</code>; nhận <code>401</code> -&gt; tự refresh -&gt; thử lại một lần -&gt; thành công.</li><li>Refresh thất bại (refresh token hết hạn) -&gt; đăng xuất, về màn Login.</li><li>Đăng ký push: xin quyền -&gt; lấy FCM token -&gt; <code>POST /v1/devices</code> đăng token + platform.</li><li>Người dùng từ chối quyền push -&gt; không đăng token, app vẫn dùng được bình thường.</li><li>HĐH xoay token (<code>onTokenRefresh</code>) -&gt; token mới đăng về backend.</li><li>Đăng xuất -&gt; <code>clearTokens()</code> xóa refresh token khỏi Keychain + <code>unregisterDevice()</code> gỡ liên kết FCM.</li><li>Sau đăng xuất, không còn token nào trong secure storage hay bộ nhớ (kiểm bằng test).</li><li>Không có mật khẩu người dùng nào được ghi xuống thiết bị (review + test).</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-ts\">// apps/mobile/src/auth/authClient.test.ts\ntest('login lưu refresh token vào Keychain, không vào AsyncStorage', async () =&gt; {\n  await authClient.login('user@example.com', 'pw');\n  expect(Keychain.setGenericPassword).toHaveBeenCalled();\n  expect(AsyncStorage.setItem).not.toHaveBeenCalledWith(\n    expect.stringContaining('refresh'), expect.anything());\n});\n\ntest('401 -&gt; refresh -&gt; retry một lần thành công', async () =&gt; {\n  fetchMock.mockResponseOnce('', { status: 401 });\n  fetchMock.mockResponseOnce(JSON.stringify({ ok: true }), { status: 200 });\n  jest.spyOn(authClient, 'refreshAccessToken').mockResolvedValue(true);\n  const res = await http.get('/v1/me');\n  expect(res.status).toBe(200);\n  expect(authClient.refreshAccessToken).toHaveBeenCalledTimes(1);\n});\n\ntest('refresh hỏng -&gt; logout, về Login', async () =&gt; {\n  fetchMock.mockResponse('', { status: 401 });\n  jest.spyOn(authClient, 'refreshAccessToken').mockResolvedValue(false);\n  const spy = jest.spyOn(authClient, 'logout');\n  await http.get('/v1/me');\n  expect(spy).toHaveBeenCalled();\n});\n\ntest('logout xóa sạch token', async () =&gt; {\n  await authClient.login('user@example.com', 'pw');\n  await authClient.logout();\n  expect(Keychain.resetGenericPassword).toHaveBeenCalled();\n  expect(await getRefreshToken()).toBeNull();\n});\n\n// apps/mobile/src/push/registerDevice.test.ts\ntest('từ chối quyền push -&gt; không đăng token', async () =&gt; {\n  messaging().requestPermission = jest.fn().mockResolvedValue(\n    messaging.AuthorizationStatus.DENIED);\n  const postSpy = jest.spyOn(http, 'post');\n  await registerDevice();\n  expect(postSpy).not.toHaveBeenCalledWith('/v1/devices', expect.anything());\n});\n\ntest('cấp quyền -&gt; đăng FCM token về backend', async () =&gt; {\n  messaging().requestPermission = jest.fn().mockResolvedValue(\n    messaging.AuthorizationStatus.AUTHORIZED);\n  messaging().getToken = jest.fn().mockResolvedValue('fcm_abc');\n  const postSpy = jest.spyOn(http, 'post');\n  await registerDevice();\n  expect(postSpy).toHaveBeenCalledWith('/v1/devices',\n    expect.objectContaining({ fcm_token: 'fcm_abc' }));\n});\n\ntest('logout gỡ liên kết device token', async () =&gt; {\n  const delSpy = jest.spyOn(http, 'delete');\n  await unregisterDevice();\n  expect(delSpy).toHaveBeenCalledWith('/v1/devices', expect.objectContaining({ fcm_token: expect.any(String) }));\n});</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: scaffold RN -&gt; tokenStore.ts (Keychain) -&gt; authClient.ts (login/refresh/logout) -&gt; httpClient.ts (auto-refresh) -&gt; registerDevice.ts (FCM) -&gt; RootNavigator.tsx -&gt; tests. Backend cần endpoint <code>POST /v1/devices</code> + <code>DELETE /v1/devices</code> để gắn/gỡ <code>user_channel_token</code> (mở rộng nhẹ từ FR-NOTIF-002, vốn đã có bảng token). iOS cần cấu hình APNs auth key trong Firebase project để FCM gửi được tới iOS. Test chạy qua Jest với mock của Keychain/messaging/fetch.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li>FR-AUTH-002 - JWT access + refresh + endpoint login/refresh; mobile dùng lại nguyên cơ chế.</li><li>FR-NOTIF-002 - FCM dispatcher + bảng <code>user_channel_token</code>; mobile đăng device token để dispatcher gửi push.</li><li>FR-NOTIF-005 (liên quan) - APNs dispatcher trực tiếp; mobile chuẩn bị nền iOS nếu sau này cần.</li><li>FR-INFRA-001 (gateway) - mọi lời gọi backend qua gateway HTTPS + JWT.</li><li>Lib: <code>react-native-keychain</code>, <code>@react-native-firebase/messaging</code>, React Navigation.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Đăng device token (POST /v1/devices)</h3>\n<pre><code class=\"language-json\">{ \"fcm_token\": \"fcm_abc123...\", \"platform\": \"ios\" }</code></pre>\n<h3>Gỡ device token khi đăng xuất (DELETE /v1/devices)</h3>\n<pre><code class=\"language-json\">{ \"fcm_token\": \"fcm_abc123...\" }</code></pre>\n<h3>Header request có auth</h3>\n<pre><code>GET /v1/me\nAuthorization: Bearer &lt;access_jwt_ngắn_hạn&gt;</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Đăng nhập sinh trắc học (FaceID/vân tay) mở khóa refresh token - lớp tiện ích thêm sau, vẫn trên nền Keychain.</li><li>Hỗ trợ đăng nhập mạng xã hội trên mobile (Google/Zalo) - tái dùng FR-AUTH-004 qua deep-link OAuth ở slice sau.</li><li>Đánh giá lại lựa chọn React Native vs Flutter sau slice 1 nếu nhu cầu hiệu năng đồ họa cao.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Refresh token trong AsyncStorage thường</td><td>test + review</td><td>token lộ khi thiết bị bị root</td><td>Bắt buộc Keychain/Keystore (DEC-MOBILE-02)</td></tr><tr><td>Lưu mật khẩu trên thiết bị</td><td>review + test</td><td>rò mật khẩu</td><td>Chỉ giữ token, không bao giờ mật khẩu (DEC-MOBILE-06)</td></tr><tr><td>401 vòng lặp refresh vô hạn</td><td>retry=false sau lần đầu</td><td>treo app</td><td>Chỉ refresh + retry một lần; hỏng -&gt; logout</td></tr><tr><td>Token không gỡ khi đăng xuất</td><td>logout test</td><td>push rò sang phiên khác</td><td>unregisterDevice() khi logout (DEC-MOBILE-04)</td></tr><tr><td>Token cũ sau khi HĐH xoay</td><td>onTokenRefresh test</td><td>push gửi tới token chết</td><td>Đăng token mới khi onTokenRefresh</td></tr><tr><td>Người dùng từ chối quyền push</td><td>registerDevice test</td><td>không có push</td><td>App vẫn chạy; push là kênh phụ trợ</td></tr><tr><td>Auth riêng cho mobile</td><td>review</td><td>bề mặt xác thực thứ hai</td><td>Dùng lại JWT FR-AUTH-002 (DEC-MOBILE-02)</td></tr><tr><td>iOS không nhận push</td><td>cấu hình APNs key Firebase</td><td>mất kênh iOS</td><td>Cấu hình APNs auth key trong Firebase project</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Ranh giới bảo mật cốt lõi mobile: refresh token trong secure storage HĐH, access token chỉ trong bộ nhớ, không bao giờ mật khẩu trên đĩa.</li><li>Một nguồn sự thật cho xác thực: JWT của FR-AUTH-002 dùng chung web + mobile, không phát minh lại.</li><li>Một đường push (FCM) phục vụ cả Android + iOS ở giai đoạn đầu; FR-NOTIF-005 (APNs) cho khi cần kiểm soát sâu.</li><li>Gỡ device token khi đăng xuất là biện pháp chống rò thông báo cá nhân sang phiên/người dùng khác trên cùng thiết bị.</li><li>Tự refresh khi 401 giữ phiên liền mạch; chỉ buộc đăng nhập lại khi refresh token cũng hỏng.</li><li>FR này là khung; theo dõi giá + checkout (FR-MOBILE-002) và deep-link virality (FR-MOBILE-003) dựng lên trên.</li></ul>\n<hr>\n<p><em>Hết FR-MOBILE-001. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
