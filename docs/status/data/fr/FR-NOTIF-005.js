window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-NOTIF-005"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Dispatcher APNs <strong>MUST</strong> gửi push tới iPhone/iPad qua APNs HTTP/2 provider API (<code>api.push.apple.com</code>), là consumer downstream của fan-out (FR-NOTIF-003): nó nhặt các dòng <code>notification</code> có <code>channel='push'</code> đang <code>status='queued'</code> cho thiết bị iOS, gửi qua nhiều kết nối HTTP/2 song song, rồi cập nhật <code>status</code> -&gt; <code>sent</code>/<code>failed</code> + <code>sent_at</code>. Dispatcher KHÔNG routing kênh và KHÔNG render lại nội dung (việc của FR-NOTIF-001). Đây là anh em iOS của FR-NOTIF-002 (FCM cho Android/Web); cùng vòng đời <code>notification</code>, khác nhà cung cấp. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> gửi qua APNs HTTP/2 endpoint <code>POST https://api.push.apple.com/3/device/{device_token}</code> với token-based auth (DEC-NOTIF-50). <strong>MUST NOT</strong> dùng certificate-based / binary legacy API.</li><li><strong>MUST</strong> xác thực bằng JWT provider token (DEC-NOTIF-50): JWT ký thuật toán <code>ES256</code> bằng khóa <code>.p8</code> (APNs Auth Key), header <code>alg=ES256</code> + <code>kid=&lt;key_id&gt;</code>, claims <code>iss=&lt;team_id&gt;</code> + <code>iat=&lt;now&gt;</code>; đặt vào header <code>authorization: bearer &lt;jwt&gt;</code>. Provider token <strong>MUST</strong> được cache và ký lại trước khi quá 60 phút (APNs từ chối token cũ hơn 1 giờ với 403 <code>ExpiredProviderToken</code>).</li><li><strong>MUST</strong> đặt các header APNs bắt buộc cho mỗi request: <code>apns-topic</code> (bundle id của app iOS), <code>apns-push-type</code> (<code>alert</code> cho cảnh báo giá), <code>apns-priority</code> (10 cho alert hiển thị ngay), và <code>apns-id</code> (UUID idempotency, log để truy vết). Header <code>apns-expiration</code> <strong>SHOULD</strong> đặt để APNs không giữ message quá lâu nếu thiết bị offline.</li><li><strong>MUST</strong> lấy <code>device_token</code> đích từ <code>user_channel_token</code> của FR-NOTIF-001: bản ghi <code>channel='push'</code>, <code>verified=true</code>, và thuộc thiết bị iOS. Token chưa verified hoặc đã bị vô hiệu <strong>MUST NOT</strong> được gửi tới.</li><li><strong>MUST</strong> mở <strong>nhiều kết nối HTTP/2 song song</strong> tới <code>api.push.apple.com</code> và <strong>multiplex</strong> nhiều request trên mỗi kết nối (DEC-NOTIF-51). Một kết nối HTTP/2 đơn không đủ throughput cho đỉnh 00:00; pool nhiều kết nối + multiplexing là cách APNs khuyến nghị để đạt thông lượng cao.</li><li><strong>MUST</strong> dựng aps payload JSON đúng cấu trúc: <code>aps.alert.{title,body}</code> (đã render bởi FR-NOTIF-001), tùy chọn <code>aps.sound</code>, <code>aps.badge</code>, và custom key ngoài <code>aps</code> (vd <code>product_id</code>, <code>deeplink</code>) cho điều hướng. Payload <strong>MUST</strong> giữ dưới giới hạn 4KB của APNs cho alert push.</li><li><strong>MUST</strong> phân loại phản hồi APNs theo HTTP status + <code>reason</code> và hành xử đúng:</li></ol>\n<ul><li>Thành công (HTTP <code>200</code>) -&gt; <code>MarkSent</code>: <code>status='sent'</code>, <code>sent_at=now()</code>.</li><li>Token chết (HTTP <code>410</code> <code>Unregistered</code>, hoặc <code>400</code> <code>BadDeviceToken</code>) -&gt; token không còn active.</li><li>Lỗi server tạm thời (HTTP <code>500</code> <code>InternalServerError</code>, <code>503</code> <code>ServiceUnavailable</code>, timeout) -&gt; retry theo backoff.</li><li>Bị siết (HTTP <code>429</code> <code>TooManyRequests</code>) -&gt; backoff, tôn trọng <code>Retry-After</code>.</li></ul>\n<ol><li><strong>MUST</strong> xử lý HTTP <code>410</code> (<code>Unregistered</code> - apns-id/device token không còn active) bằng cách set <code>user_channel_token.verified=false</code> cho <code>(user_id, 'push')</code> tương ứng (DEC-NOTIF-52) và <code>MarkFailed</code> dòng <code>notification</code>. Lần sau routing (FR-NOTIF-001) sẽ không coi token đó là khả dụng nữa - ngừng gửi vào token chết. HTTP <code>400 BadDeviceToken</code> xử lý cùng nhánh token chết.</li><li><strong>MUST</strong> xử lý HTTP <code>500</code>/<code>503</code> (lỗi server APNs) bằng exponential backoff có jitter và <strong>MUST</strong> tôn trọng header <code>Retry-After</code> khi APNs trả về (DEC-NOTIF-53). Message bị 500/503 <strong>MUST NOT</strong> bị drop; nó được trả lại hàng đợi (giữ <code>status='queued'</code>) để thử lại sau, không mất thông báo. Vượt số lần thử tối đa thì <code>MarkFailed</code> để dead-letter (FR-NOTIF-003).</li><li><strong>MUST</strong> nhặt việc an toàn khi chạy nhiều worker song song: <code>ClaimIOSPushBatch</code> dùng <code>SELECT ... FOR UPDATE SKIP LOCKED</code> trên <code>notification</code> (<code>channel='push'</code>, <code>status='queued'</code>, thiết bị iOS), để hai worker không gửi trùng một dòng (DEC-NOTIF-54). Một dòng <code>notification</code> đã <code>status='sent'</code> <strong>MUST NOT</strong> bị gửi lại; <code>MarkSent</code>/<code>MarkFailed</code> là cập nhật có điều kiện trên trạng thái hiện tại.</li><li><strong>MUST</strong> trả error (không panic) khi ký JWT provider token thất bại (<code>.p8</code> lỗi), kết nối HTTP/2 đứt, hoặc APNs trả lỗi không phân loại được, để dispatcher loop áp retry/backoff và không kẹt worker. Header <code>apns-id</code> trả về <strong>SHOULD</strong> được log để đối soát với APNs khi cần.</li><li><strong>MUST</strong> giữ ranh giới trách nhiệm: dispatcher chỉ gửi push cho dòng đã được FR-NOTIF-001 chọn kênh và render. Nó <strong>MUST NOT</strong> tự chọn kênh, <strong>MUST NOT</strong> đổi nội dung, và <strong>MUST NOT</strong> gọi FCM/email/SMS provider. <strong>SHOULD</strong> phát OTel metric: <code>apns_send_total{result}</code> (sent|retry|failed|token_dead), <code>apns_retry_total{reason}</code>, <code>apns_token_invalidated_total</code>, <code>apns_connections_active</code> (gauge), <code>apns_send_duration_ms</code> (histogram).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao APNs HTTP/2 token-based chứ không certificate-based legacy (DEC-NOTIF-50)?</strong> APNs hiện đại là HTTP/2 với hai cách xác thực: certificate (.p12 gắn từng app, hết hạn hàng năm, xoay vòng thủ công nặng) và token-based (JWT provider token ký bằng một khóa .p8 dùng cho nhiều app/topic, không hết hạn cứng). Token-based nhẹ vận hành hơn hẳn: một khóa .p8, ký JWT ngắn hạn theo nhu cầu, không phải canh ngày hết hạn chứng chỉ. Phần còn lại của hệ thống (FCM cũng dùng OAuth2 ngắn hạn) đã chuẩn hóa theo hướng token ngắn hạn; làm APNs theo cùng kiểu giữ vận hành đồng nhất. Provider token phải ký lại trước 60 phút vì APNs từ chối token cũ hơn 1 giờ.</p>\n<p><strong>Vì sao nhiều kết nối HTTP/2 song song + multiplexing (DEC-NOTIF-51)?</strong> Đỉnh tải SănDeal là 00:00 - flash sale mở, hàng loạt alert giá nổ cùng lúc (§3.8), và một nửa số đó là người dùng iOS. HTTP/2 cho phép multiplex nhiều request trên một kết nối, nhưng một kết nối đơn vẫn có trần (số stream đồng thời, độ trễ round-trip). APNs khuyến nghị mở nhiều kết nối song song và rải request qua chúng để đạt throughput cao. Pool nhiều kết nối + multiplexing trên mỗi kết nối là hai lớp song song bổ trợ: nhiều kết nối phá trần stream của một kết nối, multiplexing tận dụng tối đa mỗi kết nối. Gửi tuần tự trên một kết nối là tự bóp cổ chai đúng lúc cần nhanh nhất.</p>\n<p><strong>Vì sao 410 set verified=false thay vì xóa (DEC-NOTIF-52)?</strong> APNs trả <code>410 Unregistered</code> khi device token không còn active - máy đã gỡ app, hoặc iOS xoay vòng token. Gửi tiếp vào đó là đốt kết nối và công sức vào hư không, lại càng dễ nghẽn lúc cao điểm. Đặt <code>verified=false</code> (thay vì xóa hàng) giữ ranh giới sạch với FR-NOTIF-001 giống hệt cách FR-NOTIF-002 xử lý <code>UNREGISTERED</code> của FCM: routing chỉ coi bản ghi <code>verified=true</code> là khả dụng, nên token chết tự rớt khỏi vòng chọn kênh mà vẫn còn vết để chẩn đoán. Khi thiết bị đăng ký lại, token mới ghi đè và <code>verified</code> bật lại. Một cơ chế, hai nhà cung cấp - dễ suy luận và đối xứng với anh em FCM.</p>\n<p><strong>Vì sao 500/503 phải backoff + tôn trọng Retry-After, không drop (DEC-NOTIF-53)?</strong> <code>500 InternalServerError</code> và <code>503 ServiceUnavailable</code> là lỗi phía server APNs, thường thoáng qua. Đúng lúc đỉnh 00:00 là lúc mọi hệ thống chịu tải nặng nhất, kể cả APNs. Đây là tín hiệu \"thử lại sau\", không phải \"bỏ đi\". Drop thẳng nghĩa là người dùng iOS mất đúng cảnh báo họ chờ cả ngày chỉ vì một trục trặc server tạm thời. Backoff có jitter tránh cả đàn worker thử lại đồng pha (thundering herd) sau một đợt lỗi hàng loạt; tôn trọng <code>Retry-After</code> là làm theo đúng nhịp APNs yêu cầu thay vì đoán. Cùng triết lý retry như FR-NOTIF-002 áp cho 429/500/503 của FCM.</p>\n<p><strong>Vì sao consumer của fan-out chứ không tự lấy thẳng từ DB (DEC-NOTIF-54)?</strong> Fan-out (FR-NOTIF-003) là nơi áp flatten-the-curve và rải tải theo thời gian; dispatcher APNs chỉ là cái miệng cuối nhặt dòng <code>status='queued'</code> đã được fan-out chuẩn bị, lọc đúng thiết bị iOS. Tách như vậy cho phép nhiều dispatcher APNs chạy song song mà không phải tự lo việc rải đỉnh, và giữ logic gửi (pool kết nối, backoff, token JWT) gọn trong một chỗ. <code>FOR UPDATE SKIP LOCKED</code> làm việc nhặt an toàn khi scale ngang worker. FR-NOTIF-003 route các dòng <code>channel='push'</code> của thiết bị iOS sang dispatcher này, còn dòng push của Android/Web sang FR-NOTIF-002.</p>\n<p><strong>Vì sao ranh giới cứng với FR-NOTIF-001 (§1 #12)?</strong> FR-NOTIF-001 đã quyết kênh (push là rẻ nhất khả dụng) và render nội dung an toàn rồi. Nếu dispatcher lại đụng vào chọn kênh hay nội dung thì hai chỗ cùng sửa một thứ, khó test và dễ lệch. Dispatcher APNs dừng đúng ở \"gửi token iOS này nội dung này qua APNs HTTP/2, ghi kết quả\". Trách nhiệm đơn, test gọn, và đối xứng hoàn toàn với FR-NOTIF-002 ở phía FCM.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Repo: nhặt việc iOS (Go)</h3>\n<p>Không cần migration mới - tái dùng <code>notification</code> và <code>user_channel_token</code> của FR-NOTIF-001, và tái dùng <code>MarkSent</code>/<code>MarkFailed</code>/<code>InvalidateToken</code> đã thêm ở FR-NOTIF-002. Chỉ bổ sung hàm nhặt việc lọc thiết bị iOS.</p>\n<pre><code class=\"language-go\">// services/notif/internal/notif/repo.go (bổ sung)\n\n// ClaimIOSPushBatch nhặt tối đa n dòng push iOS đang queued, khóa hàng để worker khác bỏ qua.\n// Lọc platform='ios' qua user_channel_token; FOR UPDATE SKIP LOCKED để nhiều dispatcher APNs\n// chạy song song không gửi trùng. MarkSent/MarkFailed/InvalidateToken tái dùng từ FR-NOTIF-002.\nfunc (r *Repo) ClaimIOSPushBatch(ctx context.Context, n int) ([]PushJob, error) {\n    rows, err := r.pool.Query(ctx, `\n        SELECT n.id, n.user_id, t.address AS token, n.payload\n        FROM notification n\n        JOIN user_channel_token t\n          ON t.user_id = n.user_id AND t.channel = 'push' AND t.verified = true\n        WHERE n.channel = 'push' AND n.status = 'queued' AND t.platform = 'ios'\n        ORDER BY n.scheduled_at NULLS FIRST, n.id\n        FOR UPDATE OF n SKIP LOCKED\n        LIMIT $1`, n)\n    if err != nil {\n        return nil, err\n    }\n    defer rows.Close()\n    var jobs []PushJob\n    for rows.Next() {\n        var j PushJob\n        if err := rows.Scan(&amp;j.NotifID, &amp;j.UserID, &amp;j.Token, &amp;j.Payload); err != nil {\n            return nil, err\n        }\n        jobs = append(jobs, j)\n    }\n    return jobs, rows.Err()\n}</code></pre>\n<h3>JWT provider token ES256 từ .p8 (Go)</h3>\n<pre><code class=\"language-go\">// services/notif/internal/apns/jwt.go\n\n// ProviderToken ký JWT ES256 bằng .p8 (DEC-NOTIF-50), cache &lt;60 phút rồi ký lại.\ntype ProviderToken struct {\n    mu      sync.Mutex\n    keyID   string            // kid (header)\n    teamID  string            // iss (claim)\n    key     *ecdsa.PrivateKey // nạp từ .p8\n    cached  string\n    issued  time.Time\n}\n\n// Bearer trả JWT còn hạn; ký lại nếu cũ hơn ~55 phút (APNs từ chối token &gt; 1 giờ).\nfunc (p *ProviderToken) Bearer() (string, error) {\n    p.mu.Lock()\n    defer p.mu.Unlock()\n    if p.cached != \"\" &amp;&amp; time.Since(p.issued) &lt; 55*time.Minute {\n        return p.cached, nil\n    }\n    now := time.Now()\n    header := map[string]string{\"alg\": \"ES256\", \"kid\": p.keyID}\n    claims := map[string]any{\"iss\": p.teamID, \"iat\": now.Unix()}\n    signed, err := signES256(header, claims, p.key) // base64url(header).base64url(claims).sig\n    if err != nil {\n        return \"\", err\n    }\n    p.cached, p.issued = signed, now\n    return signed, nil\n}</code></pre>\n<h3>APNs HTTP/2 client (Go)</h3>\n<pre><code class=\"language-go\">// services/notif/internal/apns/client.go\n\n// SendResult phân loại phản hồi APNs để dispatcher hành xử (§1 #7).\ntype SendResult int\n\nconst (\n    ResultSent      SendResult = iota // 200 OK\n    ResultRetry                       // 429/500/503/timeout -&gt; backoff\n    ResultTokenDead                   // 410 Unregistered / 400 BadDeviceToken\n    ResultFailed                      // lỗi vĩnh viễn khác\n)\n\ntype SendOutcome struct {\n    Result     SendResult\n    RetryAfter time.Duration // &gt; 0 khi APNs trả header Retry-After (§1 #9)\n    APNsID     string        // header apns-id để đối soát/log (§1 #11)\n}\n\n// Send gửi một message qua APNs HTTP/2 (DEC-NOTIF-50). conn là một kết nối từ pool (§1 #5).\nfunc (c *Client) Send(ctx context.Context, conn *http2Conn, token string, p Payload) (SendOutcome, error) {\n    body, _ := json.Marshal(p)\n    bearer, err := c.provider.Bearer() // JWT provider token, cache &lt;60 phút\n    if err != nil {\n        return SendOutcome{Result: ResultRetry}, err\n    }\n    url := fmt.Sprintf(\"https://api.push.apple.com/3/device/%s\", token)\n    req, _ := http.NewRequestWithContext(ctx, http.MethodPost, url, bytes.NewReader(body))\n    req.Header.Set(\"authorization\", \"bearer \"+bearer)\n    req.Header.Set(\"apns-topic\", c.bundleID)        // bundle id app iOS\n    req.Header.Set(\"apns-push-type\", \"alert\")\n    req.Header.Set(\"apns-priority\", \"10\")           // alert hiển thị ngay\n    req.Header.Set(\"apns-id\", newUUID())            // idempotency + truy vết\n\n    resp, err := conn.Do(req) // multiplex trên kết nối HTTP/2 sẵn (§1 #5)\n    if err != nil {\n        return SendOutcome{Result: ResultRetry}, err // timeout/mạng -&gt; retry\n    }\n    defer resp.Body.Close()\n    return classify(resp), nil\n}\n\n// classify ánh xạ HTTP status + reason APNs sang SendOutcome.\nfunc classify(resp *http.Response) SendOutcome {\n    out := SendOutcome{APNsID: resp.Header.Get(\"apns-id\")}\n    switch resp.StatusCode {\n    case 200:\n        out.Result = ResultSent\n    case 410:\n        out.Result = ResultTokenDead // Unregistered: token không còn active (DEC-NOTIF-52)\n    case 400:\n        out.Result = ResultTokenDead // BadDeviceToken -&gt; coi như token chết\n    case 429:\n        out.Result, out.RetryAfter = ResultRetry, parseRetryAfter(resp.Header) // TooManyRequests\n    case 500, 503:\n        out.Result, out.RetryAfter = ResultRetry, parseRetryAfter(resp.Header) // lỗi server (DEC-NOTIF-53)\n    default:\n        out.Result = ResultFailed\n    }\n    return out\n}</code></pre>\n<h3>Connection pool song song + backoff (Go)</h3>\n<pre><code class=\"language-go\">// services/notif/internal/apns/pool.go\n\n// Pool giữ nhiều kết nối HTTP/2 song song tới api.push.apple.com (DEC-NOTIF-51).\n// Multiplex nhiều request trên mỗi kết nối; chọn kết nối theo round-robin.\ntype Pool struct {\n    conns []*http2Conn\n    next  uint64 // round-robin counter (atomic)\n}\n\nfunc NewPool(size int, dial func() (*http2Conn, error)) (*Pool, error) {\n    p := &amp;Pool{conns: make([]*http2Conn, 0, size)}\n    for i := 0; i &lt; size; i++ {\n        c, err := dial() // mỗi kết nối là một HTTP/2 tới api.push.apple.com\n        if err != nil {\n            return nil, err\n        }\n        p.conns = append(p.conns, c)\n    }\n    return p, nil\n}\n\n// Pick chọn kết nối kế tiếp (round-robin) để rải request qua nhiều kết nối song song.\nfunc (p *Pool) Pick() *http2Conn {\n    i := atomic.AddUint64(&amp;p.next, 1)\n    return p.conns[i%uint64(len(p.conns))]\n}\n\n// services/notif/internal/apns/backoff.go\n\n// nextDelay: exponential có jitter; ưu tiên Retry-After của APNs khi có (§1 #9).\nfunc nextDelay(attempt int, retryAfter time.Duration) time.Duration {\n    if retryAfter &gt; 0 {\n        return retryAfter // tôn trọng APNs (DEC-NOTIF-53)\n    }\n    base := time.Duration(1&lt;&lt;attempt) * time.Second // 1s,2s,4s,8s...\n    if base &gt; 5*time.Minute {\n        base = 5 * time.Minute\n    }\n    jitter := time.Duration(rand.Int63n(int64(base / 2)))\n    return base/2 + jitter // ±50% tránh thundering herd lúc 00:00\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>Client.Send</code> gọi đúng endpoint HTTP/2 <code>https://api.push.apple.com/3/device/{token}</code> với header <code>authorization: bearer &lt;jwt&gt;</code>, <code>apns-topic</code>, <code>apns-push-type</code>, <code>apns-priority</code>; KHÔNG dùng certificate/binary legacy.</li><li>JWT provider token ký <code>ES256</code> với <code>kid</code>/<code>iss</code> đúng; cache được tái dùng trong &lt; 60 phút và ký lại sau ngưỡng (~55 phút), không ký mới mỗi request.</li><li>Phản hồi <code>200</code> -&gt; <code>MarkSent</code>: dòng <code>notification</code> chuyển <code>status='sent'</code> và <code>sent_at</code> được set.</li><li>Phản hồi <code>410</code> <code>Unregistered</code> -&gt; <code>ResultTokenDead</code>; <code>InvalidateToken</code> set <code>user_channel_token.verified=false</code>, dòng <code>notification</code> -&gt; <code>status='failed'</code>.</li><li>Phản hồi <code>400</code> <code>BadDeviceToken</code> -&gt; <code>ResultTokenDead</code>, xử lý như token chết (cùng nhánh với 410).</li><li>Phản hồi <code>500</code>/<code>503</code> -&gt; <code>SendOutcome.Result=ResultRetry</code>; dòng giữ <code>status='queued'</code> (không bị drop), worker lập lịch thử lại theo backoff.</li><li>Khi <code>500</code>/<code>503</code>/<code>429</code> kèm header <code>Retry-After: 30</code> -&gt; <code>nextDelay</code> trả đúng 30s (tôn trọng APNs thay vì công thức exponential).</li><li>Không có <code>Retry-After</code> -&gt; <code>nextDelay(attempt)</code> tăng theo cấp số nhân có jitter, có trần (&lt;= 5 phút).</li><li>Pool mở đúng <code>size</code> kết nối HTTP/2 tới <code>api.push.apple.com</code>; <code>Pick()</code> rải request qua các kết nối theo round-robin (nhiều kết nối song song, §1 #5).</li><li><code>ClaimIOSPushBatch</code> chỉ trả dòng <code>channel='push'</code>, <code>status='queued'</code>, <code>platform='ios'</code>, và chỉ ghép với <code>user_channel_token</code> <code>verified=true</code>; token chưa verified hoặc thiết bị non-iOS KHÔNG được nhặt.</li><li>Hai worker <code>ClaimIOSPushBatch</code> đồng thời (FOR UPDATE SKIP LOCKED) KHÔNG nhặt trùng cùng một dòng; dòng đã <code>status='sent'</code> -&gt; <code>MarkSent</code>/<code>MarkFailed</code> lần nữa không đổi gì.</li><li>Metric <code>apns_send_total{result}</code> tăng đúng nhánh (sent/retry/failed/token_dead); <code>apns_token_invalidated_total</code> tăng khi gặp 410; <code>apns_connections_active</code> phản ánh số kết nối pool.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/notif/internal/apns/client_test.go\nfunc TestSend_Success_MarksSent(t *testing.T) {\n    srv := stubAPNS(t, 200, \"\", nil) // 200 OK, body rỗng\n    c := newClient(t, srv.URL)\n    out, err := c.Send(ctx, dial(t, srv.URL), \"tok-ok\", Payload{})\n    require.NoError(t, err)\n    require.Equal(t, ResultSent, out.Result)\n}\n\nfunc TestSend_UsesHTTP2DeviceEndpoint_TokenAuth(t *testing.T) {\n    var gotPath, gotAuth, gotTopic string\n    srv := captureRequest(t, &amp;gotPath, &amp;gotAuth, &amp;gotTopic, 200, \"\")\n    c := newClient(t, srv.URL)\n    c.Send(ctx, dial(t, srv.URL), \"tok\", Payload{})\n    require.Contains(t, gotPath, \"/3/device/\")            // endpoint HTTP/2\n    require.True(t, strings.HasPrefix(gotAuth, \"bearer \")) // token-based JWT (DEC-NOTIF-50)\n    require.NotEmpty(t, gotTopic)                          // apns-topic bắt buộc\n}\n\nfunc TestSend_410_MarksTokenDead(t *testing.T) {\n    srv := stubAPNS(t, 410, `{\"reason\":\"Unregistered\"}`, nil)\n    c := newClient(t, srv.URL)\n    out, _ := c.Send(ctx, dial(t, srv.URL), \"tok-dead\", Payload{})\n    require.Equal(t, ResultTokenDead, out.Result) // -&gt; InvalidateToken + status=failed (DEC-NOTIF-52)\n}\n\nfunc TestSend_500_TriggersRetry_NotDropped(t *testing.T) {\n    srv := stubAPNS(t, 500, `{\"reason\":\"InternalServerError\"}`, nil)\n    c := newClient(t, srv.URL)\n    out, _ := c.Send(ctx, dial(t, srv.URL), \"tok\", Payload{})\n    require.Equal(t, ResultRetry, out.Result) // không drop, sẽ thử lại (DEC-NOTIF-53)\n}\n\nfunc TestSend_503_RespectsRetryAfter(t *testing.T) {\n    srv := stubAPNS(t, 503, `{\"reason\":\"ServiceUnavailable\"}`,\n        http.Header{\"Retry-After\": []string{\"30\"}})\n    c := newClient(t, srv.URL)\n    out, _ := c.Send(ctx, dial(t, srv.URL), \"tok\", Payload{})\n    require.Equal(t, 30*time.Second, nextDelay(0, out.RetryAfter)) // tôn trọng Retry-After\n}\n\n// services/notif/internal/apns/pool_test.go\nfunc TestPool_OpensParallelConnsAndRoundRobins(t *testing.T) {\n    var dialed int32\n    p, err := NewPool(4, func() (*http2Conn, error) {\n        atomic.AddInt32(&amp;dialed, 1)\n        return fakeConn(), nil\n    })\n    require.NoError(t, err)\n    require.Equal(t, int32(4), dialed)              // 4 kết nối song song (DEC-NOTIF-51)\n    a, b := p.Pick(), p.Pick()\n    require.NotSame(t, a, b)                         // rải qua các kết nối khác nhau\n}\n\n// services/notif/internal/apns/dispatcher_test.go\nfunc TestDispatch_410_InvalidatesTokenAndFails(t *testing.T) {\n    d, repo, uid, nid := setupQueuedIOSPush(t, stubAPNS(t, 410, `{\"reason\":\"Unregistered\"}`, nil))\n    d.RunOnce(ctx)\n    require.False(t, repo.tokenVerified(t, uid))    // verified=false\n    require.Equal(t, \"failed\", repo.status(t, nid)) // notification.status=failed\n}\n\nfunc TestDispatch_Success_SetsSentAt(t *testing.T) {\n    d, repo, _, nid := setupQueuedIOSPush(t, stubAPNS(t, 200, \"\", nil))\n    d.RunOnce(ctx)\n    require.Equal(t, \"sent\", repo.status(t, nid))\n    require.NotNil(t, repo.sentAt(t, nid))\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>repo.go</code> (ClaimIOSPushBatch trên schema sẵn của FR-NOTIF-001, tái dùng MarkSent/MarkFailed/InvalidateToken của FR-NOTIF-002) -&gt; <code>jwt.go</code> (ký JWT provider token ES256 từ .p8, cache &lt;60 phút) -&gt; <code>payload.go</code> (dựng aps JSON dưới 4KB) -&gt; <code>client.go</code> (Send + classify) -&gt; <code>pool.go</code> (nhiều kết nối HTTP/2 song song + round-robin) + <code>backoff.go</code> (retry 500/503) -&gt; <code>dispatcher.go</code> (vòng RunOnce: claim -&gt; Pick kết nối -&gt; Send song song -&gt; mark) -&gt; tests. Dispatcher chạy thành pool worker, mỗi worker <code>ClaimIOSPushBatch</code> rồi gửi qua <code>Pool.Pick()</code>; <code>FOR UPDATE SKIP LOCKED</code> giữ an toàn khi scale ngang. Fan-out (FR-NOTIF-003) đẩy dòng push iOS vào <code>status='queued'</code> và lo rải đỉnh; dispatcher chỉ nhặt và gửi. Khóa <code>.p8</code>, <code>team_id</code>, <code>key_id</code>, <code>bundle_id</code> nạp từ cấu hình/secret store, không hard-code.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>FR-NOTIF-001</strong> - bảng <code>notification</code> (đọc <code>channel='push'</code>, cập nhật <code>status</code>/<code>sent_at</code>) và <code>user_channel_token</code> (đọc <code>device_token</code> <code>verified=true</code> của thiết bị iOS, set <code>verified=false</code> khi token chết). Dispatcher KHÔNG routing/render - hai việc đó thuộc FR-NOTIF-001.</li><li><strong>FR-NOTIF-003 (upstream)</strong> - fan-out chuyển dòng <code>notification</code> push sang <code>status='queued'</code>, rải đỉnh (flatten-the-curve), và route dòng push của thiết bị iOS sang dispatcher này; dispatcher là consumer downstream. Dead-letter các dòng <code>status='failed'</code> cũng do fan-out lo.</li><li><strong>FR-NOTIF-002 (anh em)</strong> - dispatcher FCM cho Android/Web là anh em của FR này: cùng vòng đời <code>notification</code>, cùng pattern claim/mark, cùng triết lý backoff và đánh dấu token chết (<code>UNREGISTERED</code> của FCM tương ứng <code>Unregistered</code> 410 của APNs); khác nhà cung cấp và cách auth (OAuth2 service-account vs JWT provider token .p8). <code>MarkSent</code>/<code>MarkFailed</code>/<code>InvalidateToken</code> được FR-NOTIF-002 thêm vào <code>repo.go</code> và FR này tái dùng.</li><li><strong>FR-INFRA-002</strong> - <code>app_user</code> cho FK <code>user_id</code> (gián tiếp qua schema FR-NOTIF-001).</li><li>Lib: <code>golang.org/x/net/http2</code> (kết nối HTTP/2 tới APNs), <code>crypto/ecdsa</code> + <code>crypto/x509</code> (nạp .p8, ký ES256), <code>encoding/json</code>, <code>pgx</code> (FOR UPDATE SKIP LOCKED).</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Request gửi APNs HTTP/2 (dispatcher dựng từ dòng notification push iOS)</h3>\n<pre><code>POST /3/device/0a1b2c3d-device-token-cua-iphone HTTP/2\nHost: api.push.apple.com\nauthorization: bearer eyJhbGciOiJFUzI1NiIsImtpZCI6IkFCQzEyM30...\napns-topic: vn.sandeal.app\napns-push-type: alert\napns-priority: 10\napns-id: 9d9b1c4e-0f2a-4d3b-8a7e-1c2d3e4f5a6b\napns-expiration: 0\n\n{\n  \"aps\": {\n    \"alert\": {\n      \"title\": \"Giá đã giảm về mức bạn chờ\",\n      \"body\": \"Sản phẩm bạn theo dõi còn 79.000 VND.\"\n    },\n    \"sound\": \"default\",\n    \"badge\": 1\n  },\n  \"product_id\": \"90112\",\n  \"deeplink\": \"sandeal://product/90112\"\n}</code></pre>\n<h3>Dòng notification: queued -&gt; sent sau khi gửi thành công</h3>\n<pre><code class=\"language-sql\">-- Trước (fan-out đã đẩy vào hàng đợi, thiết bị iOS):\n-- 55021 | push | price_below | queued | sent_at = NULL\nUPDATE notification SET status='sent', sent_at=now() WHERE id=55021 AND status='queued';\n-- Sau: 55021 | push | price_below | sent | sent_at = 2026-06-28 00:00:03+07</code></pre>\n<h3>Token chết (410 Unregistered): tắt verified, dòng -&gt; failed</h3>\n<pre><code class=\"language-sql\">UPDATE user_channel_token SET verified=false, updated_at=now() WHERE user_id=4022 AND channel='push';\nUPDATE notification        SET status='failed'                  WHERE id=55022 AND status='queued';</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Đọc phản hồi feedback hàng loạt / kênh thu hồi token APNs ngoài 410 trên đường gửi - hiện đủ dùng 410 inline; thêm cơ chế quét định kỳ nếu cần dọn token chết chủ động.</li><li>Tinh chỉnh số kết nối pool theo tải thực (autoscale số kết nối HTTP/2 theo độ sâu hàng đợi) - bắt đầu với hằng số cấu hình, tối ưu sau khi có số đo lúc đỉnh.</li><li>Live Activities / push tới Apple Watch / <code>apns-push-type</code> khác (<code>background</code>, <code>voip</code>) - ngoài phạm vi cảnh báo giá, mở khi có tính năng cần.</li><li>Đa app/topic (nhiều bundle id) ký bằng cùng một .p8 - khóa .p8 dùng được cho nhiều topic; mở rộng map <code>apns-topic</code> khi có app thứ hai.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Dùng certificate/binary legacy API</td><td>test <code>UsesHTTP2DeviceEndpoint_TokenAuth</code></td><td>vận hành nặng, lệch chuẩn</td><td>Chỉ gọi HTTP/2 <code>/3/device/{token}</code> token-based (DEC-NOTIF-50)</td></tr><tr><td>JWT provider token &gt; 1 giờ</td><td>HTTP 403 ExpiredProviderToken</td><td>gửi bị từ chối</td><td>Cache &lt;60 phút, ký lại trước ~55 phút (§1 #2)</td></tr><tr><td>410 Unregistered (app đã gỡ/token xoay)</td><td>HTTP 410 + classify</td><td>đốt kết nối vào token chết</td><td><code>InvalidateToken</code> verified=false + status=failed (DEC-NOTIF-52)</td></tr><tr><td>400 BadDeviceToken</td><td>HTTP 400 + reason</td><td>gửi hỏng lặp lại</td><td>Coi như token chết, ngừng gửi (cùng nhánh 410)</td></tr><tr><td>500/503 lỗi server APNs lúc đỉnh 00:00</td><td><code>apns_send_total{retry}</code></td><td>nguy cơ mất push nếu drop</td><td>Backoff + Retry-After, giữ queued, thử lại (DEC-NOTIF-53)</td></tr><tr><td>Bỏ qua Retry-After, thử lại quá sớm</td><td>test <code>RespectsRetryAfter</code></td><td>bị siết tiếp, vòng lặp lỗi</td><td><code>nextDelay</code> ưu tiên Retry-After của APNs</td></tr><tr><td>Một kết nối HTTP/2 nghẽn lúc đỉnh</td><td><code>apns_connections_active</code> thấp</td><td>throughput iOS nghẽn cổ chai</td><td>Pool nhiều kết nối song song + multiplex (DEC-NOTIF-51)</td></tr><tr><td>Hai worker gửi trùng một dòng</td><td>test concurrency</td><td>push đúp tới user iOS</td><td>FOR UPDATE SKIP LOCKED (§1 #10)</td></tr><tr><td>Payload vượt 4KB</td><td>APNs từ chối PayloadTooLarge</td><td>không gửi được</td><td>Giữ aps payload gọn dưới 4KB (§1 #6)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Dispatcher APNs là cái miệng cuối của kênh push cho iOS - anh em của FR-NOTIF-002 (FCM Android/Web); cùng vòng đời <code>notification</code>, cùng pattern claim/mark, khác nhà cung cấp và cách auth.</li><li>Token-based auth (JWT provider token ký bằng .p8) nhẹ vận hành hơn certificate-based: một khóa .p8 cho nhiều topic, ký JWT ngắn hạn theo nhu cầu, không phải canh ngày hết hạn chứng chỉ; phải ký lại trước 60 phút.</li><li>Nhiều kết nối HTTP/2 song song + multiplexing là cách APNs khuyến nghị đạt throughput cao; một kết nối đơn tự thành cổ chai lúc đỉnh 00:00.</li><li>410 set <code>verified=false</code> thay vì xóa hàng: routing FR-NOTIF-001 tự thôi chọn push, vẫn còn vết chẩn đoán; thiết bị đăng ký lại sẽ ghi đè token và bật lại verified - đối xứng hoàn toàn với cách FR-NOTIF-002 xử lý <code>UNREGISTERED</code> của FCM.</li><li>500/503 backoff + tôn trọng Retry-After, không drop: lỗi server APNs thường thoáng qua, drop thẳng làm người dùng iOS mất cảnh báo họ chờ cả ngày; jitter +/-50% chống thundering herd sau đợt lỗi hàng loạt.</li><li>Ranh giới cứng với FR-NOTIF-001: dispatcher không routing, không render lại nội dung; chỉ gửi push iOS cho dòng đã định kênh. FCM (Android/Web), email, SMS có dispatcher riêng.</li></ul>\n<hr>\n<p><em>Hết FR-NOTIF-005. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
