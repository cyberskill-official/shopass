window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-INFRA-003"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service INFRA <strong>MUST</strong> cung cấp một lớp quản lý secret tập trung: mọi bí mật sống trong Vault/AWS Secrets Manager, service lấy runtime qua một provider trừu tượng, hỗ trợ version/rotation, và không bao giờ log thô. Hợp đồng:</p>\n<ol><li>Mọi secret <strong>MUST</strong> sống trong Vault hoặc AWS Secrets Manager (DEC-INFRA-11): DB credentials, JWT signing key (cho FR-AUTH-002), proxy credentials theo nhà cung cấp (cho FR-SCRAPE-004), gateway tokens, payment gateway keys (cho FR-BILL-002). KHÔNG đặt trong file <code>.env</code> commit vào repo hay hằng số trong code.</li><li>Service <strong>MUST</strong> lấy secret qua interface <code>SecretProvider</code> (DEC-INFRA-12). Backend cụ thể (Vault KV v2 hoặc AWS SM) hoán đổi được sau interface mà không sửa code gọi.</li><li>Kiểu trả về <code>Secret</code> <strong>MUST</strong> mang <code>Value</code> và <code>Version</code>; provider <strong>MUST</strong> đọc đúng version hiện hành (Vault: metadata version; AWS SM: <code>AWSCURRENT</code> stage).</li><li>Provider <strong>MUST</strong> hỗ trợ rotation (DEC-INFRA-13): khi secret được xoay ở backend, service đọc lần kế tiếp (sau TTL cache) nhận version mới mà không cần restart và không pin cứng một version.</li><li>Lớp cache <strong>MUST</strong> giảm tải backend: cache giá trị per-path với TTL ngắn cấu hình được (mặc định ví dụ 60s); trong TTL không gọi lại backend; hết TTL refresh.</li><li>Kiểu <code>Secret</code> <strong>MUST</strong> mask khi in/serialize (DEC-INFRA-14): <code>String()</code> và <code>MarshalJSON()</code> KHÔNG lộ giá trị thô (chỉ hiện fingerprint hoặc 4 ký tự cuối). Cấm log giá trị secret ở mọi nơi.</li><li>Bố cục path <strong>MUST</strong> có scope rõ (DEC-INFRA-15): ví dụ <code>db/main</code>, <code>auth/jwt-signing</code>, <code>scrape/proxy/brightdata</code>, <code>scrape/proxy/oxylabs</code>, <code>gateway/tokens</code>, <code>bill/momo</code>. Policy đọc <strong>MUST</strong> least-privilege (mỗi service chỉ đọc path nó cần).</li><li>Khi backend không truy cập được, provider <strong>MUST</strong> trả lỗi rõ ràng (KHÔNG fallback sang giá trị mặc định/cleartext); service quyết định fail-closed cho path nhạy cảm.</li><li>Provider <strong>MUST</strong> an toàn dùng đồng thời (nhiều goroutine đọc cùng path) - cache có khóa, không race.</li><li>Lớp secrets <strong>SHOULD</strong> phát OTel metric: <code>secret_fetch_total{path, backend, result}</code>, <code>secret_cache_hit_total</code>, <code>secret_rotation_detected_total{path}</code> - không bao giờ kèm giá trị secret trong nhãn.</li><li>Tài liệu path layout + least-privilege policy <strong>MUST</strong> tồn tại như nguồn sự thật để FR-AUTH-002, FR-SCRAPE-004, FR-BILL-002 biết đọc path nào.</li><li>Lớp secrets <strong>MUST</strong> là điểm tham chiếu duy nhất cho audit no-cleartext của FR-COMPLY-005 (chứng minh không có credential cleartext rời lớp này).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p>Vì sao secret không nằm trong env/code (DEC-INFRA-11)? Đây là nguyên nhân rò rỉ phổ biến nhất: secret lọt vào git history, container image layer, log, hay ảnh chụp màn hình. Một khi lọt vào repo, gần như không xóa sạch được khỏi lịch sử. Tập trung vào Vault/SM cho một nơi kiểm soát truy cập, audit, và xoay.</p>\n<p>Vì sao một interface <code>SecretProvider</code> (DEC-INFRA-12)? SănDeal có thể chạy trên hạ tầng tự quản (Vault) hoặc AWS (Secrets Manager) tùy giai đoạn. Trừu tượng hóa sau một interface cho phép đổi backend mà không sửa mọi service gọi - chỉ thay implementation và policy.</p>\n<p>Vì sao version + rotation (DEC-INFRA-13)? Bảo mật tốt đòi xoay secret định kỳ và xoay khẩn khi nghi lộ. Nếu service pin cứng một version, xoay sẽ làm hỏng (đọc version cũ đã thu hồi). Cache TTL ngắn + đọc version hiện hành cho xoay mượt: sau TTL, lần đọc kế nhận giá trị mới.</p>\n<p>Vì sao cache TTL ngắn (§1 #5)? Gọi Vault/SM mỗi lần cần secret là chậm và tốn (rate-limit của backend). Cache giảm tải; TTL ngắn (vài chục giây) cân bằng giữa hiệu năng và độ trễ nhận giá trị xoay. Đủ ngắn để rotation lan nhanh, đủ dài để không đập backend.</p>\n<p>Vì sao mask khi serialize (DEC-INFRA-14)? Lỗi vô tình <code>log.Printf(\"%+v\", cfg)</code> hay trả secret trong JSON response là cách rò rỉ âm thầm. Làm <code>Secret</code> tự mask ở <code>String()</code>/<code>MarshalJSON()</code> biến an toàn thành mặc định: dù lập trình viên lỡ in ra, giá trị thô vẫn không lộ.</p>\n<p>Vì sao path có scope + least-privilege (DEC-INFRA-15)? Nếu mọi service đọc được mọi secret, một service bị xâm nhập là lộ tất cả. Phân path theo phạm vi (proxy creds tách theo vendor, JWT key riêng) + policy chỉ-đọc-path-cần giới hạn bán kính nổ khi một service bị chiếm.</p>\n<p>Vì sao là điểm tham chiếu cho audit PDPL (§1 #12)? FR-COMPLY-005 phải chứng minh no-cleartext credential. Nếu mọi secret đi qua lớp này, audit chỉ cần xác nhận không có credential nào rời lớp ra env/code/log - một điểm kiểm thay vì rà toàn codebase.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Provider interface (Go)</h3>\n<pre><code class=\"language-go\">// secrets/provider.go\ntype Secret struct {\n    value   string // không export; truy cập qua Reveal() có kiểm soát\n    Version string\n}\n\n// Reveal trả giá trị thô — chỉ gọi ở điểm dùng (mở DB, ký JWT), không log.\nfunc (s Secret) Reveal() string { return s.value }\n\n// String/MarshalJSON luôn mask (DEC-INFRA-14).\nfunc (s Secret) String() string          { return mask(s.value) }\nfunc (s Secret) MarshalJSON() ([]byte, error) { return json.Marshal(mask(s.value)) }\n\ntype SecretProvider interface {\n    // Get trả secret tại path (ví dụ \"auth/jwt-signing\"); đọc version hiện hành.\n    Get(ctx context.Context, path string) (Secret, error)\n}</code></pre>\n<h3>Cache có TTL + refresh (§1 #4, #5, #9)</h3>\n<pre><code class=\"language-go\">// secrets/cache.go\ntype cachedProvider struct {\n    inner SecretProvider\n    ttl   time.Duration\n    mu    sync.Mutex\n    items map[string]cacheItem // path -&gt; {secret, fetchedAt}\n}\n\nfunc (c *cachedProvider) Get(ctx context.Context, path string) (Secret, error) {\n    c.mu.Lock(); defer c.mu.Unlock()\n    if it, ok := c.items[path]; ok &amp;&amp; time.Since(it.at) &lt; c.ttl {\n        metrics.CacheHit(path)\n        return it.sec, nil\n    }\n    sec, err := c.inner.Get(ctx, path) // refresh: nhận version mới nếu đã xoay\n    if err != nil {\n        return Secret{}, err // KHÔNG fallback cleartext (§1 #8)\n    }\n    if old, ok := c.items[path]; ok &amp;&amp; old.sec.Version != sec.Version {\n        metrics.RotationDetected(path)\n    }\n    c.items[path] = cacheItem{sec: sec, at: time.Now()}\n    return sec, nil\n}</code></pre>\n<h3>Path layout (tài liệu, least-privilege)</h3>\n<pre><code class=\"language-text\">db/main                  -&gt; DB credentials (đọc bởi mọi service cần DB)\nauth/jwt-signing         -&gt; RS256 signing key (đọc bởi AUTH; JWKS public expose riêng)\nscrape/proxy/brightdata  -&gt; proxy creds Bright Data  (đọc bởi SCRAPE)\nscrape/proxy/oxylabs     -&gt; proxy creds Oxylabs      (đọc bởi SCRAPE)\ngateway/tokens           -&gt; token nội bộ gateway     (đọc bởi gateway)\nbill/momo, bill/zalopay  -&gt; payment keys             (đọc bởi BILL)</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>Get(\"auth/jwt-signing\")</code> từ backend giả -&gt; trả <code>Secret</code> có <code>Value</code> và <code>Version</code> đúng.</li><li>In <code>Secret</code> qua <code>fmt.Sprintf(\"%v\")</code> -&gt; ra chuỗi mask, KHÔNG lộ giá trị thô.</li><li><code>json.Marshal(secret)</code> -&gt; JSON mask, KHÔNG chứa giá trị thô.</li><li>Đọc cùng path hai lần trong TTL -&gt; backend chỉ bị gọi một lần (cache hit lần hai).</li><li>Đọc lại sau khi TTL hết -&gt; gọi backend lần nữa (refresh).</li><li>Backend xoay secret (đổi version) -&gt; sau TTL, <code>Get</code> trả version mới; metric <code>secret_rotation_detected_total</code> tăng.</li><li>Backend lỗi/không truy cập được -&gt; <code>Get</code> trả lỗi, KHÔNG trả giá trị mặc định/cleartext.</li><li>Đọc đồng thời cùng path từ nhiều goroutine -&gt; không race (chạy với <code>-race</code>).</li><li><code>Reveal()</code> trả giá trị thô đúng tại điểm dùng (mở DB / ký JWT).</li><li>Không có file <code>.env</code> chứa secret trong repo (kiểm bằng grep CI); cấu hình chỉ chứa path, không chứa giá trị.</li><li>Tài liệu path layout liệt kê đủ db/auth/scrape-proxy/gateway/bill với chú thích service nào đọc.</li><li>Provider Vault và AWS SM cùng pass bộ test hợp đồng (hoán đổi backend không đổi hành vi).</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// secrets/mask_test.go\nfunc TestSecret_NeverLeaksRaw(t *testing.T) {\n    s := Secret{value: \"super-secret-token-123\", Version: \"v3\"}\n    require.NotContains(t, fmt.Sprintf(\"%v\", s), \"super-secret-token-123\")\n    require.NotContains(t, fmt.Sprintf(\"%+v\", s), \"super-secret-token-123\")\n    b, _ := json.Marshal(s)\n    require.NotContains(t, string(b), \"super-secret-token-123\")\n    require.Equal(t, \"super-secret-token-123\", s.Reveal()) // chỉ Reveal() lộ\n}\n\n// secrets/cache_test.go\nfunc TestCache_HitWithinTTL(t *testing.T) {\n    backend := &amp;countingProvider{val: \"x\", ver: \"v1\"}\n    p := newCache(backend, 60*time.Second)\n    _, _ = p.Get(ctx, \"db/main\")\n    _, _ = p.Get(ctx, \"db/main\")\n    require.Equal(t, 1, backend.calls) // lần hai từ cache\n}\n\nfunc TestCache_RefreshAfterTTL(t *testing.T) {\n    backend := &amp;countingProvider{val: \"x\", ver: \"v1\"}\n    p := newCache(backend, 10*time.Millisecond)\n    _, _ = p.Get(ctx, \"db/main\")\n    time.Sleep(20 * time.Millisecond)\n    _, _ = p.Get(ctx, \"db/main\")\n    require.Equal(t, 2, backend.calls) // refresh sau TTL\n}\n\nfunc TestCache_RotationDetected(t *testing.T) {\n    backend := &amp;countingProvider{val: \"old\", ver: \"v1\"}\n    p := newCache(backend, 10*time.Millisecond)\n    _, _ = p.Get(ctx, \"auth/jwt-signing\")\n    backend.val, backend.ver = \"new\", \"v2\" // xoay\n    time.Sleep(20 * time.Millisecond)\n    s, _ := p.Get(ctx, \"auth/jwt-signing\")\n    require.Equal(t, \"v2\", s.Version)\n}\n\nfunc TestProvider_BackendError_NoFallback(t *testing.T) {\n    p := newCache(&amp;errProvider{}, time.Minute)\n    _, err := p.Get(ctx, \"db/main\")\n    require.Error(t, err) // KHÔNG trả cleartext mặc định\n}\n\nfunc TestCache_ConcurrentNoRace(t *testing.T) {\n    p := newCache(&amp;countingProvider{val: \"x\", ver: \"v1\"}, time.Minute)\n    var wg sync.WaitGroup\n    for i := 0; i &lt; 50; i++ {\n        wg.Add(1)\n        go func() { defer wg.Done(); _, _ = p.Get(ctx, \"db/main\") }()\n    }\n    wg.Wait() // chạy với -race\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Implementation Vault dùng KV v2 (đọc <code>data</code> + <code>metadata.version</code>); AWS SM dùng <code>GetSecretValue</code> với <code>VersionStage=AWSCURRENT</code>. Cả hai bọc trong <code>cachedProvider</code> để mọi service hưởng cache + phát hiện rotation đồng nhất. <code>mask()</code> lộ tối đa 4 ký tự cuối hoặc một fingerprint hash. CI có một bước grep chặn pattern secret trong repo (key=value dạng token dài, không phải path). Least-privilege policy khai báo ở phía Vault/SM, ngoài code, nhưng tài liệu path layout sống cùng package.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li>Không có FR phụ thuộc trước (nền móng P0).</li><li>FR-INFRA-001 (downstream) - gateway lấy JWKS/khoá qua provider, không nhúng env.</li><li>FR-AUTH-002 (downstream) - đọc <code>auth/jwt-signing</code> để ký JWT.</li><li>FR-SCRAPE-004 (downstream) - đọc <code>scrape/proxy/&lt;vendor&gt;</code> cho proxy rotation.</li><li>FR-BILL-002 (downstream) - đọc payment keys.</li><li>FR-COMPLY-005 (downstream) - dùng lớp này làm điểm chứng minh no-cleartext.</li><li>Hạ tầng: HashiCorp Vault hoặc AWS Secrets Manager.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Đọc secret tại điểm dùng (nội bộ)</h3>\n<pre><code class=\"language-go\">sec, err := provider.Get(ctx, \"db/main\")\nif err != nil { return fmt.Errorf(\"đọc db creds: %w\", err) }\npool, err := pgxpool.New(ctx, sec.Reveal()) // Reveal() chỉ ở đây, không log</code></pre>\n<h3>Bố cục path trong Vault (KV v2)</h3>\n<pre><code class=\"language-text\">secret/data/sandeal/db/main\nsecret/data/sandeal/auth/jwt-signing\nsecret/data/sandeal/scrape/proxy/brightdata</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Dynamic DB credentials (Vault tạo creds ngắn hạn per-service) - siết hơn static; thêm khi vận hành ổn định.</li><li>Auto-rotation lịch trình cho JWT signing key (xoay định kỳ tự động) - phối với JWKS đa-<code>kid</code> ở FR-AUTH-002.</li><li>Envelope encryption / KMS cho dữ liệu tại tầng ứng dụng (ngoài secret hạ tầng) - phạm vi PDPL nâng cao.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Secret lọt vào repo/env</td><td>grep CI</td><td>rò rỉ vĩnh viễn</td><td>Cấm (DEC-INFRA-11); xoay ngay nếu lộ</td></tr><tr><td>Log in secret thô</td><td>mask_test + review</td><td>rò rỉ qua log</td><td><code>Secret.String()</code> mask mặc định (§1 #6)</td></tr><tr><td>Backend Vault/SM down</td><td>lỗi Get + metric</td><td>service không khởi động được</td><td>Fail-closed path nhạy cảm; backend HA; cache còn hạn</td></tr><tr><td>Pin cứng version cũ</td><td>rotation test</td><td>đọc creds đã thu hồi</td><td>Cache TTL + đọc version hiện hành (§1 #4)</td></tr><tr><td>Một service đọc mọi secret</td><td>review policy</td><td>bán kính nổ lớn khi bị chiếm</td><td>Least-privilege path scope (§1 #7)</td></tr><tr><td>Race khi đọc đồng thời</td><td>go test -race</td><td>giá trị/cache hỏng</td><td>Cache có mutex (§1 #9)</td></tr><tr><td>Fallback cleartext khi lỗi</td><td>test NoFallback</td><td>che lỗi + rủi ro</td><td>Cấm fallback; trả lỗi rõ (§1 #8)</td></tr><tr><td>Proxy creds lộ</td><td>giám sát hạn mức</td><td>đốt chi phí proxy đắt</td><td>Path riêng per-vendor + xoay</td></tr><tr><td>Metric kèm giá trị secret</td><td>review nhãn</td><td>rò rỉ qua observability</td><td>Nhãn chỉ path/result, không value (§1 #10)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Secret rời env/code là một trong các nguồn rò rỉ lớn nhất; tập trung vào Vault/SM cho kiểm soát, audit và xoay tại một chỗ.</li><li>Interface <code>SecretProvider</code> cho phép đổi Vault &lt;-&gt; AWS SM theo giai đoạn hạ tầng mà không sửa code gọi.</li><li>Cache TTL ngắn + đọc version hiện hành là chìa khóa để rotation mượt: xoay ở backend lan tới service sau một TTL, không cần restart.</li><li>Mask mặc định ở <code>String()</code>/<code>MarshalJSON()</code> biến \"không lộ secret\" thành hành vi mặc định, chịu được lỗi in ấn vô tình.</li><li>Path có scope + least-privilege giới hạn bán kính nổ: một service bị chiếm không lộ toàn bộ kho secret.</li><li>Đây là điểm chứng minh no-cleartext cho audit PDPL (FR-COMPLY-005): một điểm kiểm thay vì rà toàn codebase.</li></ul>\n<hr>\n<p><em>Hết FR-INFRA-003. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
