window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-TRUST-006"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service TRUST <strong>MUST</strong> thu thập device fingerprint dưới dạng hash bảo toàn riêng tư (không lưu thuộc tính thô, không PII), ghi cạnh <code>device</code> vào đồ thị quan hệ FR-TRUST-004, và gắn cờ cụm nhiều tài khoản chung thiết bị như TÍN HIỆU để điều tra - KHÔNG auto-khóa. Hợp đồng:</p>\n<ol><li>Client (<code>collect.ts</code> + <code>hash.ts</code>) <strong>MUST</strong> tính <code>device_hash</code> bằng one-way hash từ tập thuộc tính ổn định-vừa-phải; <strong>MUST NOT</strong> gửi thuộc tính thô (user-agent đầy đủ, danh sách font, canvas raw) về server - chỉ gửi hash (DEC-TRUST-26).</li><li><code>device_hash</code> <strong>MUST</strong> gắn salt phía server và xoay được (DEC-TRUST-30); thuộc tính thô không rời máy nguyên dạng (nhất quán local-first FR-TRUST-002).</li><li>Fingerprint <strong>MUST</strong> chỉ phục vụ MỤC ĐÍCH chống gian lận, có cơ sở pháp lý PDPL (FR-COMPLY-001); <strong>MUST NOT</strong> dùng cho theo dõi hành vi, quảng cáo, hay định danh cá nhân ngoài chống gian lận (DEC-TRUST-27).</li><li><code>device.go</code> <strong>MUST</strong> lưu <code>device_fingerprint (device_hash, user_id, first_seen, last_seen)</code>; khi một <code>device_hash</code> gắn với NHIỀU <code>user_id</code>, <strong>MUST</strong> ghi cạnh <code>link_type='device'</code> vào <code>account_link_edge</code> (FR-TRUST-004) giữa các user đó (DEC-TRUST-27).</li><li>Engine anti-fraud (FR-TRUST-004) <strong>MUST</strong> tiêu thụ cạnh <code>device</code> như một loại liên kết trong đồ thị quan hệ; nhiều account chung thiết bị làm dày cụm -&gt; tăng tín hiệu graph.</li><li>Nhiều tài khoản chung một <code>device_hash</code> <strong>MUST</strong> được coi là TÍN HIỆU (không phải bằng chứng) (DEC-TRUST-28); quyết định cuối do điều tra (nhất quán DEC-TRUST-16, không auto-ban/auto-khóa thiết bị).</li><li>Ngưỡng cảnh báo (số account/device kích hoạt tín hiệu) <strong>MUST</strong> cấu hình được; <strong>MUST</strong> chấp nhận chia sẻ thiết bị hợp lý (gia đình, máy net chung) - ngưỡng + điều tra thay vì khóa máy mù (DEC-TRUST-29).</li><li>Fingerprint <strong>MUST</strong> được minh bạch trong chính sách dữ liệu (FR-TRUST-002): user được biết fingerprint dùng cho mục đích chống gian lận, thu cái gì (ở mức hash), và vì sao (DEC-TRUST-30).</li><li><code>device_fingerprint</code> <strong>MUST NOT</strong> chứa PII sàn thật hay định danh cá nhân ngoài <code>device_hash</code> + FK <code>user_id</code> nội bộ.</li><li>Hệ thống <strong>MUST</strong> idempotent: cùng <code>(device_hash, user_id)</code> không nhân bản; cập nhật <code>last_seen</code>.</li><li>Một user dùng một thiết bị (một <code>device_hash</code> cho một <code>user_id</code>) <strong>MUST</strong> KHÔNG sinh cạnh device hay tín hiệu (tránh quấy nhiễu user thật).</li><li><strong>MUST</strong> phát metric: <code>device_shared_accounts_total</code> (phân phối số account/device), <code>device_edge_written_total</code> - để cân ngưỡng và quan sát chia sẻ thiết bị hợp lý vs farm.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao hash một chiều, không thuộc tính thô (DEC-TRUST-26)?</strong> Fingerprint thiết bị thô (user-agent đầy đủ + font + canvas) là công cụ theo dõi mạnh, xâm phạm riêng tư - đúng cái SănDeal cam kết KHÔNG làm (§5.4). Hash một chiều giữ được khả năng \"hai phiên có cùng thiết bị không\" (so hash) mà bỏ khả năng \"thiết bị này là ai/trông thế nào\" (không đảo ngược). Đủ để liên kết tài khoản, không đủ để theo dõi cá nhân.</p>\n<p><strong>Vì sao chỉ ghi cạnh device vào đồ thị có sẵn (DEC-TRUST-27)?</strong> FR-TRUST-004 đã có đồ thị quan hệ từ referral/payment. Fingerprint không dựng hệ thống mới mà thêm một loại cạnh (<code>device</code>) vào đồ thị đó. Multi-account lộ ra khi nhiều tài khoản chia sẻ thiết bị - một cạnh mạnh bổ sung cho referral/payment. Tái dùng đồ thị giữ engine một chỗ và nhất quán cách chấm điểm.</p>\n<p><strong>Vì sao chỉ là tín hiệu, không auto-khóa (DEC-TRUST-28)?</strong> Chia sẻ thiết bị hợp lý tồn tại thật: gia đình dùng chung máy, quán net, điện thoại mượn. Auto-khóa tài khoản/thiết bị khi thấy nhiều account chung hash sẽ khóa nhầm người dùng thật - tổn hại niềm tin. Coi nó là tín hiệu (làm dày cụm, tăng điểm) để điều tra cùng các tín hiệu khác, thay vì kết luận một mình.</p>\n<p><strong>Vì sao mục đích duy nhất chống gian lận + cơ sở PDPL (DEC-TRUST-27, §1 #3)?</strong> PDPL (Luật 91/2025) yêu cầu xử lý đúng mục đích, đơn mục đích. Fingerprint thu thập có thể bị lạm dụng cho theo dõi/quảng cáo - phải khóa chặt mục đích vào chống gian lận, có cơ sở pháp lý, và minh bạch. Đây là ranh giới giữ fingerprint là công cụ phòng vệ chứ không thành công cụ giám sát.</p>\n<p><strong>Vì sao minh bạch trong chính sách (DEC-TRUST-30)?</strong> SănDeal bán niềm tin (§5.4). Thu thập fingerprint mà giấu là mâu thuẫn với định vị. Nói rõ trong chính sách (FR-TRUST-002): có dùng fingerprint, ở mức hash, cho mục đích chống gian lận - để người dùng biết và để nhất quán với cam kết minh bạch. Salt xoay được giảm rủi ro hash bị dùng liên miền.</p>\n<p><strong>Vì sao SHOULD chứ không MUST?</strong> FR-TRUST-004 đã bắt multi-account phần nào qua đồ thị referral/payment. Fingerprint làm dày cạnh device - hữu ích nhưng không phải điều kiện tối thiểu để hệ chống gian lận vận hành. Đặt SHOULD phản ánh đúng: nên có để mạnh hơn, không chặn release nếu hoãn.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>hash.ts (one-way + salt server, không gửi thô)</h3>\n<pre><code class=\"language-ts\">// extension/src/fingerprint/hash.ts\n// collect() trả tập thuộc tính ổn định-vừa-phải (KHÔNG gửi thô đi đâu).\n// hashDevice gửi server CHỈ chuỗi hash; server thêm salt rồi lưu.\nimport { collect } from \"./collect\";\n\nexport async function deviceHash(serverSalt: string): Promise&lt;string&gt; {\n  const attrs = collect();                       // {tz, lang, platform coarse, screenBucket...}\n  const stable = JSON.stringify(attrs);          // KHÔNG bao giờ rời máy nguyên dạng\n  const data = new TextEncoder().encode(serverSalt + \"|\" + stable);\n  const digest = await crypto.subtle.digest(\"SHA-256\", data);\n  return [...new Uint8Array(digest)].map(b =&gt; b.toString(16).padStart(2, \"0\")).join(\"\");\n}\n// Gửi server: { device_hash } DUY NHẤT. Không attrs thô.</code></pre>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/trust/migrations/0004_device_fingerprint.sql\nCREATE TABLE device_fingerprint (\n  device_hash TEXT        NOT NULL,              -- one-way hash (salt server), KHÔNG thuộc tính thô\n  user_id     BIGINT      NOT NULL REFERENCES app_user(id),\n  first_seen  TIMESTAMPTZ NOT NULL DEFAULT now(),\n  last_seen   TIMESTAMPTZ NOT NULL DEFAULT now(),\n  PRIMARY KEY (device_hash, user_id)             -- idempotent: cập nhật last_seen\n);\nCREATE INDEX idx_fp_hash ON device_fingerprint (device_hash);\n-- Mục đích DUY NHẤT: chống gian lận (PDPL). KHÔNG PII, KHÔNG theo dõi/quảng cáo.</code></pre>\n<h3>device.go (hash chung nhiều user -&gt; cạnh device)</h3>\n<pre><code class=\"language-go\">// services/trust/internal/fraud/device.go\n// Record lưu fingerprint; nếu device_hash đã gắn user khác -&gt; ghi cạnh 'device'.\nfunc (d *Device) Record(ctx context.Context, hash string, userID int64) error {\n    if err := d.upsert(ctx, hash, userID); err != nil { return err }\n    others, _ := d.usersForHash(ctx, hash, userID) // user khác cùng hash\n    for _, other := range others {\n        if err := d.graph.AddEdge(ctx, userID, other, \"device\", 1.0); err != nil { // FR-TRUST-004\n            return err\n        }\n    }\n    if len(others)+1 &gt;= d.cfg.SharedAccountThreshold {\n        metrics.DeviceSharedAccounts(len(others) + 1)   // TÍN HIỆU, không auto-khóa\n    }\n    return nil\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Payload gửi server chỉ chứa <code>device_hash</code>; KHÔNG có user-agent đầy đủ/font list/canvas raw (test introspection).</li><li><code>device_hash</code> là one-way; không có đường lưu/khôi phục thuộc tính thô từ hash.</li><li><code>device_fingerprint</code> không chứa PII sàn; chỉ <code>device_hash</code> + FK <code>user_id</code> nội bộ.</li><li>Một <code>device_hash</code> gắn nhiều <code>user_id</code> -&gt; ghi cạnh <code>link_type='device'</code> vào <code>account_link_edge</code> (FR-TRUST-004).</li><li>Engine FR-TRUST-004 tiêu thụ cạnh <code>device</code>: cụm nhiều account chung thiết bị làm dày cụm -&gt; tín hiệu graph tăng.</li><li>Nhiều account chung hash là TÍN HIỆU, KHÔNG auto-khóa tài khoản/thiết bị (kiểm: không có code set banned theo device).</li><li>Ngưỡng <code>SharedAccountThreshold</code> cấu hình; đổi config -&gt; tín hiệu kích hoạt đổi, không sửa code.</li><li>Chia sẻ thiết bị dưới ngưỡng (ví dụ 2 tài khoản gia đình) -&gt; không kích hoạt cảnh báo gay gắt; điều tra theo ngưỡng.</li><li>Một user một thiết bị (một hash một user) -&gt; KHÔNG cạnh device, KHÔNG tín hiệu (không quấy nhiễu user thật).</li><li>Fingerprint được mô tả trong chính sách dữ liệu (FR-TRUST-002): mục đích chống gian lận, mức hash, lý do.</li><li>Idempotent: cùng <code>(device_hash, user_id)</code> không nhân bản; cập nhật <code>last_seen</code>.</li><li>Metric <code>device_shared_accounts_total</code>/<code>device_edge_written_total</code> hoạt động; <code>npm test</code> + <code>go test</code> xanh.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-ts\">// extension/test/fingerprint-no-pii.test.ts\nimport { deviceHash } from \"../src/fingerprint/hash\";\n\ntest(\"chỉ gửi hash, KHÔNG thuộc tính thô/PII\", async () =&gt; {\n  const h = await deviceHash(\"server-salt-xyz\");\n  expect(h).toMatch(/^[0-9a-f]{64}$/);          // SHA-256 hex\n  // payload gửi server (giả lập) chỉ có device_hash\n  const payload = { device_hash: h };\n  const flat = JSON.stringify(payload).toLowerCase();\n  expect(flat).not.toMatch(/mozilla|font|canvas|webgl|user-agent/); // không thuộc tính thô\n  expect(Object.keys(payload)).toEqual([\"device_hash\"]);\n});\n\ntest(\"hash ổn định trên cùng thiết bị, khác salt -&gt; khác hash (xoay được)\", async () =&gt; {\n  const a = await deviceHash(\"salt-1\");\n  const b = await deviceHash(\"salt-2\");\n  expect(a).not.toEqual(b);                      // salt xoay đổi hash\n});</code></pre>\n<pre><code class=\"language-go\">// services/trust/internal/fraud/device_test.go\nfunc TestDevice_SharedHash_WritesEdge(t *testing.T) {\n    d := setupDevice(t)\n    d.Record(ctx, \"h1\", 100)\n    d.Record(ctx, \"h1\", 101)     // cùng device, user khác\n    require.True(t, edgeExists(t, d, 100, 101, \"device\")) // cạnh vào FR-TRUST-004\n}\n\nfunc TestDevice_SoloUser_NoEdge(t *testing.T) {\n    d := setupDevice(t)\n    d.Record(ctx, \"h2\", 200)\n    require.Equal(t, 0, edgeCountFor(t, d, 200)) // một user một thiết bị -&gt; không cạnh\n}\n\nfunc TestDevice_SharedIsSignal_NotBan(t *testing.T) {\n    d := setupDeviceWithThreshold(t, 3)\n    for _, u := range []int64{300, 301, 302} { d.Record(ctx, \"h3\", u) }\n    require.GreaterOrEqual(t, sharedCount(t, d, \"h3\"), 3)  // tín hiệu\n    require.False(t, anyAccountBanned(t, d, []int64{300,301,302})) // KHÔNG auto-khóa\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>collect.ts</code> (thu thuộc tính ổn định-vừa-phải, không PII) -&gt; <code>hash.ts</code> (one-way + salt server, chỉ gửi hash) -&gt; migration 0004 (<code>device_fingerprint</code>) -&gt; <code>device.go</code> (lưu hash + khi nhiều user/hash ghi cạnh <code>device</code> vào <code>account_link_edge</code>) -&gt; nối <code>graph.go</code> tiêu thụ cạnh device -&gt; tests. Engine FR-TRUST-004 không cần đổi logic chấm điểm - chỉ thêm một loại cạnh. Fingerprint phải được thêm vào chính sách dữ liệu (FR-TRUST-002) cùng lúc ship. Salt server lưu trong Vault (FR-INFRA-003) và xoay được.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>FR-TRUST-004</strong> - đồ thị quan hệ <code>account_link_edge</code> + engine chấm điểm; fingerprint ghi cạnh <code>device</code> và engine tiêu thụ.</li><li><strong>FR-TRUST-005</strong> - self-referral/multi-account check dùng cụm liên kết; cạnh device làm cụm chính xác hơn.</li><li><strong>FR-TRUST-002</strong> - chính sách dữ liệu phải mô tả fingerprint (mục đích chống gian lận, mức hash); local-first.</li><li><strong>FR-COMPLY-001</strong> - cơ sở pháp lý PDPL cho mục đích chống gian lận; đơn mục đích.</li><li><strong>FR-INFRA-003</strong> - Vault lưu salt server (xoay được); thuộc tính thô không rời máy.</li><li>NFR-TRUST-001 - tối thiểu hóa + không PII; fingerprint hash tuân ràng buộc này.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Client gửi server (chỉ hash)</h3>\n<pre><code class=\"language-json\">{ \"device_hash\": \"9f2c8a1b...e71a\" }\n// KHÔNG user-agent thô, KHÔNG font list, KHÔNG canvas. Thuộc tính thô không rời máy.</code></pre>\n<h3>Hệ quả: nhiều account chung thiết bị -&gt; cạnh + tín hiệu</h3>\n<pre><code>device_fingerprint: h3 gắn user 300, 301, 302 (3 account, 1 thiết bị)\n-&gt; account_link_edge: (300,301,device), (300,302,device), (301,302,device)\n-&gt; FR-TRUST-004 cụm dày hơn -&gt; risk_score tăng -&gt; điều tra\n   KHÔNG auto-khóa: có thể là máy chung; ngưỡng + điều tra quyết.</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Chọn tập thuộc tính fingerprint cụ thể (cân ổn định vs riêng tư) - tinh theo tỷ lệ trùng/độ phân biệt thật; FR đặt cơ chế hash + chỉ-gửi-hash.</li><li>Tần suất xoay salt server - căn theo chính sách bảo mật; xoay phá liên kết lịch sử (đánh đổi: chống lạm dụng vs giữ lịch sử gian lận).</li><li>Fingerprint mobile (FR-MOBILE-001) - cùng nguyên tắc hash + chỉ-gửi-hash, thêm khi mobile ship.</li><li>Chống giả mạo fingerprint (kẻ gian đổi thuộc tính né hash) - lớp đối kháng sâu hơn; engine FR-TRUST-004 vẫn bắt qua referral/payment khi device bị né.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Lưu thuộc tính thô nhận dạng được</td><td>fingerprint-no-pii test</td><td>công cụ theo dõi, vi phạm §5.4</td><td>One-way hash, chỉ gửi hash (DEC-TRUST-26)</td></tr><tr><td>Dùng fingerprint cho theo dõi/quảng cáo</td><td>review mục đích + PDPL</td><td>vi phạm đơn-mục-đích PDPL</td><td>Khóa mục đích chống gian lận + cơ sở pháp lý (DEC-TRUST-27)</td></tr><tr><td>Auto-khóa khi nhiều account chung device</td><td>device_test (signal not ban)</td><td>khóa nhầm máy chung/gia đình</td><td>Tín hiệu + điều tra, không auto-khóa (DEC-TRUST-28)</td></tr><tr><td>Ngưỡng cứng không chỉnh được</td><td>review config</td><td>không cân chia sẻ hợp lý</td><td>Ngưỡng cấu hình (DEC-TRUST-29)</td></tr><tr><td>Giấu fingerprint với user</td><td>review chính sách</td><td>mâu thuẫn cam kết minh bạch</td><td>Minh bạch trong chính sách dữ liệu (DEC-TRUST-30)</td></tr><tr><td>Quấy nhiễu user một-thiết-bị</td><td>solo-user test</td><td>tín hiệu nhiễu</td><td>Một hash một user -&gt; không cạnh (§1 #11)</td></tr><tr><td>PII sàn lọt vào bảng fingerprint</td><td>review schema</td><td>vi phạm tối thiểu hóa</td><td>Chỉ hash + FK nội bộ (§1 #9)</td></tr><tr><td>Nhân bản fingerprint khi ghi lại</td><td>PK + idempotent test</td><td>rác dữ liệu</td><td>PK(device_hash,user_id) + upsert (§1 #10)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Fingerprint của SănDeal khác fingerprint quảng cáo ở bản chất: hash một chiều (không đảo về thuộc tính thô), mục đích duy nhất chống gian lận, có cơ sở PDPL, và minh bạch - công cụ phòng vệ, không phải giám sát.</li><li>Nó không dựng hệ thống mới mà thêm một loại cạnh (<code>device</code>) vào đồ thị quan hệ FR-TRUST-004 - multi-account lộ khi nhiều tài khoản chia sẻ thiết bị.</li><li>Chỉ là tín hiệu, không kết luận: chia sẻ thiết bị hợp lý (gia đình, máy net) tồn tại thật, nên ngưỡng + điều tra thay vì auto-khóa - nhất quán nguyên tắc không-auto-ban của cả module TRUST.</li><li>Salt server xoay được + chỉ-gửi-hash giữ thuộc tính thô không rời máy (local-first) và giảm rủi ro liên kết liên miền.</li><li>SHOULD vì FR-TRUST-004 đã bắt multi-account phần nào qua referral/payment; fingerprint làm dày cạnh device, hữu ích nhưng không phải điều kiện tối thiểu.</li><li>Minh bạch fingerprint trong chính sách (FR-TRUST-002) giữ nhất quán với định vị niềm tin (§5.4): có dùng, ở mức hash, cho chống gian lận - không giấu.</li></ul>\n<hr>\n<p><em>Hết FR-TRUST-006. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
