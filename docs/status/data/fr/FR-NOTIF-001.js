window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-NOTIF-001"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service NOTIF <strong>MUST</strong> định nghĩa bảng <code>notification</code>, một template engine render an toàn, và một bộ routing chọn kênh theo cost model (push &gt; email &gt; sms). FR này là điểm vào của fan-out; nó KHÔNG tự gọi nhà cung cấp. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> định nghĩa bảng <code>notification (id BIGSERIAL PK, user_id BIGINT REFERENCES app_user(id), channel TEXT NOT NULL, template TEXT NOT NULL, payload JSONB, scheduled_at TIMESTAMPTZ, sent_at TIMESTAMPTZ, status TEXT NOT NULL DEFAULT 'pending', created_at TIMESTAMPTZ DEFAULT now())</code>.</li><li><strong>MUST</strong> ràng buộc <code>channel</code> - <code>{push, email, sms}</code> và <code>status</code> - <code>{pending, queued, sent, failed}</code> qua CHECK (DEC-NOTIF-05). Một dòng <code>notification</code> đại diện đúng một (user, kênh) đã được routing chọn.</li><li><strong>MUST</strong> định nghĩa bảng <code>user_channel_token (user_id BIGINT, channel TEXT, platform TEXT, address TEXT, verified BOOLEAN DEFAULT false, updated_at TIMESTAMPTZ, PRIMARY KEY (user_id, channel, platform))</code> lưu khả năng nhận của user: token FCM/APNs cho <code>push</code>, email cho <code>email</code>, số điện thoại cho <code>sms</code>. Cột <code>platform</code> thuộc <code>{ios, android, web, email, sms}</code> tách kênh <code>push</code> thành hai nhánh gửi: FR-NOTIF-002 (FCM) nhặt <code>platform IN ('android','web')</code>, FR-NOTIF-005 (APNs) nhặt <code>platform='ios'</code>; nhờ <code>platform</code> trong khóa chính, một user có thể có đồng thời token iOS + Android + Web và nhận push trên từng thiết bị. Chỉ bản ghi <code>verified=true</code> (push token còn hạn, email/phone đã xác minh) mới được routing coi là khả dụng.</li><li><strong>MUST</strong> cung cấp template engine với registry các template chốt: <code>price_below</code>, <code>drop_pct</code>, <code>real_sale</code>, <code>bottom_predicted</code> (khớp <code>rule_type</code> của FR-TRACK-003). Mỗi template có <code>title</code> + <code>body</code> theo từng kênh.</li><li><strong>MUST</strong> render template từ <code>payload</code> an toàn (DEC-NOTIF-02): mọi giá trị từ <code>payload</code> (giá, tên sản phẩm) <strong>MUST</strong> được escape phù hợp kênh trước khi chèn (HTML-escape cho email body, không cho ký tự điều khiển phá push). Payload KHÔNG được chèn markup/script vào nội dung.</li><li><strong>MUST</strong> trả lỗi khi template thiếu biến bắt buộc (DEC-NOTIF-02): nếu <code>payload</code> thiếu khóa template cần (vd <code>price</code>), render trả lỗi và dòng <code>notification</code> KHÔNG chuyển sang <code>queued</code> - không gửi nội dung nửa vời như \"Giá còn {price}\".</li><li><strong>MUST</strong> cung cấp <code>ResolveChannel(desired []string, caps UserChannels) (string, bool)</code> áp cost model (DEC-NOTIF-03): từ giao của <code>desired</code> (channel[] producer yêu cầu) và khả năng nhận khả dụng của user, chọn kênh <strong>rẻ nhất</strong> theo thứ tự ưu tiên <code>push &gt; email &gt; sms</code>. Trả <code>ok=false</code> nếu không kênh nào khả dụng.</li><li><strong>MUST</strong> chỉ chọn <code>sms</code> khi (a) <code>sms</code> nằm trong <code>desired</code>, (b) push và email đều KHÔNG khả dụng hoặc producer đánh dấu thông báo high-value/OTP, và (c) user có số điện thoại verified (DEC-NOTIF-03). SMS KHÔNG được chọn cho thông báo thường khi push/email khả dụng.</li><li><strong>MUST</strong> expose <code>InsertNotification(ctx, n Notification) (int64, error)</code> ghi một dòng <code>status='pending'</code> sau khi routing đã quyết kênh và render đã thành công; trả <code>id</code>. Đồng thời giữ ranh giới với tầng gửi (DEC-NOTIF-05): FR này KHÔNG gọi FCM/APNs/SES/SendGrid/SMS. Sau khi có dòng <code>notification</code> pending, fan-out (FR-NOTIF-003) và các dispatcher (FR-NOTIF-002/005/006/007) lo việc gửi và cập nhật <code>status</code> -&gt; <code>sent</code>/<code>failed</code> + <code>sent_at</code>.</li><li><strong>MUST</strong> tạo index hỗ trợ fan-out lấy việc: <code>idx_notif_dispatch ON notification (status, scheduled_at) WHERE status IN ('pending','queued')</code> (partial, nhẹ).</li><li><strong>MUST</strong> lưu mọi giá trong <code>payload</code>/render dạng <code>BIGINT</code> VND khi là tiền (đồng nhất DEC-PRICE-05); format hiển thị (vd \"79.000 VND\") làm ở bước render, dữ liệu gốc giữ int64.</li><li><strong>SHOULD</strong> phát OTel: <code>notification_created_total{channel, template}</code> (counter), <code>notification_routing_total{chosen_channel, downgraded}</code> (counter - đếm khi phải hạ kênh do không khả dụng), <code>template_render_error_total{template}</code> (counter).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao routing theo cost model push &gt; email &gt; sms (DEC-NOTIF-03)?</strong> §3.6 nêu rõ mô hình chi phí: push (FCM/APNs) gần như miễn phí, email rất rẻ (~$0,10/1.000 với SES), SMS đắt (200-500 VND/tin nội địa, ~$0,1552 qua Twilio - gấp hàng nghìn lần push). Với mô hình free-tier tài trợ bằng affiliate, kênh gửi là biến phí trực tiếp. Routing luôn chọn kênh rẻ nhất mà user thực sự nhận được giữ chi phí thông báo gần sàn, bảo vệ unit economics. SMS để dành cho việc thực sự đáng (OTP, alert giá trị cao).</p>\n<p><strong>Vì sao tách user_channel_token với verified (DEC-NOTIF-03, §1 #3)?</strong> Routing cần biết user \"có thể nhận qua kênh nào\". Một user cài app có token push; một user khác chỉ có email; một số có cả. <code>verified</code> quan trọng: gửi push tới token hết hạn lãng phí và ăn lỗi; gửi SMS tới số chưa xác minh có thể sai người. Chỉ coi bản ghi verified là khả dụng làm routing quyết đúng và tránh gửi vào hư không.</p>\n<p><strong>Vì sao template engine escape an toàn và lỗi khi thiếu biến (DEC-NOTIF-02)?</strong> Nội dung thông báo nhúng dữ liệu từ scraper (tên sản phẩm, giá) - dữ liệu ngoài, không tin được. Nếu chèn thẳng, một tên sản phẩm chứa ký tự đặc biệt làm vỡ email HTML hoặc trông như injection. Escape theo kênh chặn việc đó. Lỗi-khi-thiếu-biến tránh gửi \"Giá còn {price}\" (placeholder lộ) - thà không gửi còn hơn gửi nội dung hỏng làm mất niềm tin.</p>\n<p><strong>Vì sao một dòng notification cho mỗi (user, kênh) sau routing (DEC-NOTIF-01)?</strong> Producer gửi một \"ý định cảnh báo\" với <code>channel[]</code> mong muốn. Routing quy về đúng một kênh thực (kênh rẻ nhất khả dụng). Ghi một dòng <code>notification</code> cho kết quả đó cho ta đơn vị theo dõi rõ: trạng thái gửi, thời điểm, kênh thật. Fan-out lấy các dòng pending này làm việc.</p>\n<p><strong>Vì sao FR này không tự gửi (DEC-NOTIF-05, §1 #9)?</strong> Gửi ở quy mô là module riêng (FR-NOTIF-002..007) với fan-out, rate-limit FCM, flatten-the-curve, dead-letter. Trộn routing/render với gọi nhà cung cấp làm hai thứ khó test chung và dễ vượt rate-limit. FR này dừng ở \"quyết kênh + dựng nội dung + ghi pending\"; phần gửi là việc của các FR sau, tách trách nhiệm sạch.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/notif/migrations/0001_notification.sql\nCREATE TABLE notification (\n  id           BIGSERIAL   PRIMARY KEY,\n  user_id      BIGINT      NOT NULL REFERENCES app_user(id),\n  channel      TEXT        NOT NULL CHECK (channel IN ('push','email','sms')),\n  template     TEXT        NOT NULL,\n  payload      JSONB,\n  scheduled_at TIMESTAMPTZ,\n  sent_at      TIMESTAMPTZ,\n  status       TEXT        NOT NULL DEFAULT 'pending'\n                 CHECK (status IN ('pending','queued','sent','failed')),\n  created_at   TIMESTAMPTZ NOT NULL DEFAULT now()\n);\n\n-- Fan-out (FR-NOTIF-003) lấy việc theo trạng thái + lịch -&gt; partial index\nCREATE INDEX idx_notif_dispatch ON notification (status, scheduled_at)\n  WHERE status IN ('pending','queued');\n\n-- services/notif/migrations/0002_user_channel_token.sql\nCREATE TABLE user_channel_token (\n  user_id    BIGINT      NOT NULL REFERENCES app_user(id),\n  channel    TEXT        NOT NULL CHECK (channel IN ('push','email','sms')),\n  platform   TEXT        NOT NULL CHECK (platform IN ('ios','android','web','email','sms')),\n  address    TEXT        NOT NULL,                 -- FCM/APNs token | email | phone\n  verified   BOOLEAN     NOT NULL DEFAULT false,\n  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  PRIMARY KEY (user_id, channel, platform)         -- mot user co the co token ios + android + web\n);\n-- platform tach push thanh FCM (android/web) vs APNs (ios): FR-NOTIF-002 nhat platform IN ('android','web'),\n-- FR-NOTIF-005 nhat platform='ios'. Email/sms dat platform='email'/'sms' cho dong nhat khoa.</code></pre>\n<h3>Routing theo cost model (Go)</h3>\n<pre><code class=\"language-go\">// services/notif/internal/notif/routing.go\n\n// channelRank: số nhỏ = rẻ hơn = ưu tiên cao hơn (push &gt; email &gt; sms, §3.6).\nvar channelRank = map[string]int{\"push\": 0, \"email\": 1, \"sms\": 2}\n\ntype UserChannels struct {\n    Push  bool // có token push verified\n    Email bool // có email verified\n    SMS   bool // có phone verified\n}\n\n// ResolveChannel chọn kênh rẻ nhất trong giao(desired, khả dụng) (DEC-NOTIF-03).\n// highValue cho phép SMS ngay cả khi push/email khả dụng (OTP/alert giá trị cao).\nfunc ResolveChannel(desired []string, caps UserChannels, highValue bool) (string, bool) {\n    avail := func(c string) bool {\n        switch c {\n        case \"push\":\n            return caps.Push\n        case \"email\":\n            return caps.Email\n        case \"sms\":\n            return caps.SMS &amp;&amp; (highValue || (!caps.Push &amp;&amp; !caps.Email)) // §1 #8: SMS chỉ khi đáng\n        }\n        return false\n    }\n    best, found := \"\", false\n    for _, c := range desired {\n        if !avail(c) {\n            continue\n        }\n        if !found || channelRank[c] &lt; channelRank[best] {\n            best, found = c, true\n        }\n    }\n    return best, found\n}</code></pre>\n<h3>Template engine an toàn (Go)</h3>\n<pre><code class=\"language-go\">// services/notif/internal/notif/template.go\n\ntype Rendered struct{ Title, Body string }\n\n// templates: registry chốt khớp rule_type của FR-TRACK-003.\nvar templates = map[string]func(map[string]any) (Rendered, error){\n    \"price_below\": func(d map[string]any) (Rendered, error) {\n        price, ok := intField(d, \"price\")\n        if !ok {\n            return Rendered{}, fmt.Errorf(\"template price_below thiếu price\")\n        }\n        return Rendered{\n            Title: \"Giá đã giảm về mức bạn chờ\",\n            Body:  fmt.Sprintf(\"Sản phẩm bạn theo dõi còn %s.\", formatVND(price)),\n        }, nil\n    },\n    // drop_pct, real_sale, bottom_predicted tương tự, mỗi cái kiểm đủ biến.\n}\n\n// Render dựng nội dung; thiếu biến -&gt; lỗi, KHÔNG trả nội dung nửa vời (DEC-NOTIF-02).\nfunc Render(template string, data map[string]any) (Rendered, error) {\n    fn, ok := templates[template]\n    if !ok {\n        return Rendered{}, fmt.Errorf(\"template không tồn tại: %s\", template)\n    }\n    return fn(data) // mỗi fn tự escape giá trị ngoài trước khi chèn\n}\n\n// formatVND format int64 -&gt; \"79.000 VND\"; dữ liệu gốc luôn là int64 (DEC-PRICE-05).\nfunc formatVND(v int64) string { /* nhóm hàng nghìn bằng dấu chấm */ return \"\" }</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch -&gt; bảng <code>notification</code> + <code>user_channel_token</code> tồn tại; CHECK <code>channel</code>/<code>status</code> hoạt động.</li><li><code>INSERT notification</code> với <code>channel='telegram'</code> hoặc <code>status='done'</code> -&gt; lỗi CHECK.</li><li><code>Render(\"price_below\", {price:79000})</code> trả <code>Body</code> chứa \"79.000 VND\"; số gốc là int64.</li><li><code>Render(\"price_below\", {})</code> (thiếu <code>price</code>) -&gt; lỗi; KHÔNG trả nội dung có placeholder.</li><li>Render với tên sản phẩm chứa ký tự đặc biệt (vd <code>&lt;b&gt;</code>) -&gt; được escape, không chèn markup thô vào email body.</li><li><code>ResolveChannel([\"push\",\"email\"], {Push:true, Email:true}, false)</code> -&gt; <code>\"push\"</code> (rẻ nhất khả dụng).</li><li><code>ResolveChannel([\"push\",\"email\"], {Push:false, Email:true}, false)</code> -&gt; <code>\"email\"</code> (push không khả dụng, hạ kênh).</li><li><code>ResolveChannel([\"push\",\"sms\"], {Push:false, SMS:true}, false)</code> -&gt; <code>\"sms\"</code> (push không khả dụng, sms là lựa chọn còn lại).</li><li><code>ResolveChannel([\"push\",\"sms\"], {Push:true, SMS:true}, false)</code> -&gt; <code>\"push\"</code>; SMS KHÔNG được chọn cho thông báo thường khi push khả dụng.</li><li><code>ResolveChannel([\"sms\"], {SMS:true}, true)</code> (high-value) -&gt; <code>\"sms\"</code> được phép.</li><li><code>ResolveChannel([\"push\"], {Push:false}, false)</code> -&gt; <code>ok=false</code> (không kênh khả dụng); producer xử lý.</li><li><code>idx_notif_dispatch</code> tồn tại dạng partial; metric <code>notification_routing_total{downgraded}</code> tăng khi phải hạ kênh.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/notif/internal/notif/routing_test.go\nfunc TestRouting_PicksCheapestAvailable(t *testing.T) {\n    c, ok := ResolveChannel([]string{\"push\", \"email\"},\n        UserChannels{Push: true, Email: true}, false)\n    require.True(t, ok)\n    require.Equal(t, \"push\", c) // rẻ nhất (§3.6)\n}\n\nfunc TestRouting_DowngradesWhenPushUnavailable(t *testing.T) {\n    c, _ := ResolveChannel([]string{\"push\", \"email\"},\n        UserChannels{Push: false, Email: true}, false)\n    require.Equal(t, \"email\", c)\n}\n\nfunc TestRouting_SMSNotChosenWhenPushAvailable(t *testing.T) {\n    c, _ := ResolveChannel([]string{\"push\", \"sms\"},\n        UserChannels{Push: true, SMS: true}, false)\n    require.Equal(t, \"push\", c) // không đốt SMS khi push khả dụng (DEC-NOTIF-03)\n}\n\nfunc TestRouting_SMSAllowedForHighValue(t *testing.T) {\n    c, ok := ResolveChannel([]string{\"sms\"}, UserChannels{SMS: true}, true)\n    require.True(t, ok)\n    require.Equal(t, \"sms\", c)\n}\n\nfunc TestRouting_NoChannelAvailable(t *testing.T) {\n    _, ok := ResolveChannel([]string{\"push\"}, UserChannels{Push: false}, false)\n    require.False(t, ok)\n}\n\n// services/notif/internal/notif/template_test.go\nfunc TestRender_PriceBelow(t *testing.T) {\n    r, err := Render(\"price_below\", map[string]any{\"price\": int64(79_000)})\n    require.NoError(t, err)\n    require.Contains(t, r.Body, \"79.000 VND\")\n}\n\nfunc TestRender_MissingVar_Errors(t *testing.T) {\n    _, err := Render(\"price_below\", map[string]any{})\n    require.Error(t, err) // không gửi nội dung nửa vời (DEC-NOTIF-02)\n}\n\nfunc TestRender_EscapesPayload(t *testing.T) {\n    r, _ := Render(\"price_below\", map[string]any{\n        \"price\": int64(79_000), \"product_name\": \"&lt;script&gt;x&lt;/script&gt;\",\n    })\n    require.NotContains(t, r.Body, \"&lt;script&gt;\") // escape, không inject\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration <code>0001_notification.sql</code> + <code>0002_user_channel_token.sql</code> -&gt; <code>template.go</code> (registry + render escape) -&gt; <code>routing.go</code> (ResolveChannel cost model) -&gt; <code>repo.go</code> (InsertNotification + GetUserChannels) -&gt; tests. Producer (FR-TRACK-004 / FR-DEAL-006) gọi <code>GetUserChannels</code> -&gt; <code>ResolveChannel</code> -&gt; <code>Render</code> -&gt; <code>InsertNotification</code> (pending). Không có lời gọi nhà cung cấp nào trong package <code>notif</code> ở FR này. Format VND nhóm hàng nghìn bằng dấu chấm ở bước render; dữ liệu gốc giữ int64.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>FR-INFRA-002</strong> - bảng <code>app_user</code> cho FK <code>user_id</code>.</li><li><strong>FR-TRACK-004 (upstream)</strong> - engine alert enqueue request (user_id + channel[] + template + data); FR này routing + render + ghi pending.</li><li><strong>FR-DEAL-006 (upstream)</strong> - batch đáy giá cũng đẩy request qua điểm vào này.</li><li><strong>FR-NOTIF-003 (downstream)</strong> - fan-out lấy dòng <code>notification</code> pending (qua <code>idx_notif_dispatch</code>) để gửi.</li><li><strong>FR-NOTIF-002/005/006/007 (downstream)</strong> - dispatcher từng kênh cập nhật <code>status</code> + <code>sent_at</code>.</li><li>Lib: <code>pgx</code> (JSONB), <code>encoding/json</code>, <code>html</code> (escape), <code>text/template</code> hoặc render thủ công.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Producer đẩy request (nội bộ, từ FR-TRACK-004)</h3>\n<pre><code class=\"language-json\">{ \"user_id\": 4021, \"channel\": [\"push\", \"email\"], \"template\": \"price_below\",\n  \"data\": { \"price\": 79000, \"threshold\": 89000, \"product_id\": 90112 } }</code></pre>\n<h3>Dòng notification sau routing (push được chọn)</h3>\n<pre><code class=\"language-sql\">SELECT id, channel, template, status FROM notification WHERE user_id = 4021 ORDER BY id DESC LIMIT 1;\n-- 55012 | push | price_below | pending   (fan-out sẽ nhặt và gửi)</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Đa ngôn ngữ template (vi-VN trước; thêm locale khi mở SEA) - bám <code>app_user.locale</code>; mở rộng registry sau.</li><li>Tùy chọn người dùng tắt từng kênh (preference center) - thêm bảng <code>user_channel_pref</code>; gắn vào routing sau.</li><li>Gộp nhiều alert thành digest theo user trong khoảng ngắn - tối ưu trải nghiệm + chi phí giai đoạn sau.</li><li>Quiet-hours (không push ban đêm trừ khi user đồng ý) - phối hợp với FR-NOTIF-004 scheduler.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>channel/status ngoài enum</td><td>CHECK</td><td>từ chối ghi</td><td>Hai giá trị chốt (DEC-NOTIF-02/05)</td></tr><tr><td>Chọn SMS khi push khả dụng</td><td>routing test</td><td>đốt chi phí</td><td>Cost model push&gt;email&gt;sms (DEC-NOTIF-03)</td></tr><tr><td>Template thiếu biến</td><td>Render lỗi</td><td>không gửi nửa vời</td><td>Lỗi-khi-thiếu (DEC-NOTIF-02)</td></tr><tr><td>Payload chèn markup</td><td>escape</td><td>nội dung an toàn</td><td>Escape theo kênh (§1 #5)</td></tr><tr><td>Push tới token hết hạn</td><td>verified=false</td><td>không chọn push</td><td>Chỉ coi bản ghi verified khả dụng</td></tr><tr><td>Không kênh khả dụng</td><td>ResolveChannel ok=false</td><td>producer biết</td><td>Trả false, không ghi rác</td></tr><tr><td>FR này tự gọi nhà cung cấp</td><td>code review</td><td>vượt rate-limit</td><td>Chỉ ghi pending (DEC-NOTIF-05)</td></tr><tr><td>Giá format sai (float)</td><td>kiểu int64 + test</td><td>sai số/hiển thị xấu</td><td>int64 gốc, format ở render</td></tr><tr><td>Fan-out không tìm được việc</td><td>idx_notif_dispatch</td><td>trễ gửi</td><td>Partial index theo status</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>FR-NOTIF-001 là nền module thông báo: bảng <code>notification</code>, template engine, routing - mọi dispatcher và fan-out neo vào.</li><li>Routing cost model push &gt; email &gt; sms là đòn bẩy chi phí trực tiếp: SMS đắt gấp hàng nghìn lần push nên chỉ dùng khi thực sự đáng (OTP/high-value).</li><li><code>user_channel_token.verified</code> đảm bảo routing chỉ chọn kênh user thực sự nhận được, tránh gửi vào hư không.</li><li>Template engine escape + lỗi-khi-thiếu-biến giữ nội dung an toàn với dữ liệu scraper ngoài và không gửi placeholder lộ.</li><li>Ranh giới cứng: FR này dừng ở \"quyết kênh + dựng nội dung + ghi pending\"; gửi là việc của FR-NOTIF-002..007 qua fan-out.</li><li>Tiền luôn int64 VND trong payload; format hiển thị làm ở render, không có bước float.</li></ul>\n<hr>\n<p><em>Hết FR-NOTIF-001. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
