window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-TRUST-005"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service TRUST <strong>MUST</strong> giữ payout affiliate/cashback qua một cửa sổ delay để điều tra, phát hiện gaming attribution (last-click thao túng, self-referral, dấu hiệu cookie-stuffing), và chỉ giải ngân khi đủ điều kiện - KHÔNG trả ngay, cũng KHÔNG tự từ chối mù. Hợp đồng:</p>\n<ol><li>Khi <code>affiliate_conversion</code> chuyển <code>confirmed</code> (FR-AFFIL-001), payout <strong>MUST NOT</strong> được trả ngay; phải tạo <code>payout_hold</code> với <code>eligible_at = confirmed_at + delay_window</code> (DEC-TRUST-21).</li><li><code>attribution_guard.go</code> <strong>MUST</strong> phát hiện dấu hiệu gaming và gắn <code>hold_reason</code> (DEC-TRUST-22): (a) last-click bị thao túng - click affiliate xảy ra ngay trước order trong khi sản phẩm đã ở giỏ từ lâu (click \"chèn cuối\" để cướp attribution); (b) self-referral - user mua qua link affiliate của chính mình (hoặc cụm liên kết FR-TRUST-004); (c) dấu hiệu cookie-stuffing - click không gắn với user-action hợp lệ (vi phạm mô hình user-initiated, NFR-AFFIL-001).</li><li><code>delay.go</code> <strong>MUST</strong> tính <code>eligible_at</code>; nếu chủ thể có <code>risk_score</code> cao (từ FR-TRUST-004), delay <strong>MUST</strong> được kéo dài hoặc chuyển trạng thái giữ-chờ-điều tra - KHÔNG tự từ chối payout (DEC-TRUST-23).</li><li><code>release.go</code> (job giải ngân) <strong>MUST</strong> chỉ trả payout khi ĐỦ BA điều kiện (DEC-TRUST-24): (a) đã qua <code>eligible_at</code>, (b) không còn <code>hold_reason</code> điều tra mở, (c) network đã confirm (postback FR-AFFIL-003).</li><li>Mọi quyết định delay/hold <strong>MUST</strong> ghi <code>hold_reason</code> cụ thể (audit trail); <strong>MUST NOT</strong> giữ tiền user khi không có tín hiệu cụ thể (DEC-TRUST-25). Giữ tiền vô cớ là tổn hại.</li><li><code>payout_hold</code> <strong>MUST</strong> có trạng thái: <code>holding</code> (trong delay) -&gt; <code>released</code> (đã trả) | <code>under_investigation</code> (giữ tiếp) -&gt; <code>released</code> | <code>denied</code> (chỉ sau điều tra kết luận <code>confirmed_fraud</code>, không tự động).</li><li>Self-referral check <strong>MUST</strong> dùng đồ thị quan hệ FR-TRUST-004: nếu người mua và chủ link thuộc cùng cụm liên kết -&gt; gắn <code>hold_reason='self_referral'</code>.</li><li>Engine <strong>MUST</strong> idempotent: một conversion chỉ sinh một <code>payout_hold</code> (UNIQUE <code>conversion_id</code>); chấm lại không nhân bản.</li><li>Tiền (<code>payout_amount</code>) <strong>MUST</strong> lưu BIGINT VND nhất quán DEC-AFFIL-04/DEC-PRICE-05; không float.</li><li>Khi điều tra kết luận <code>cleared</code> (FR-TRUST-004), <code>payout_hold</code> <strong>MUST</strong> được giải phóng ở chu kỳ release kế tiếp (tiền thật không bị quỵt).</li><li>Job release <strong>MUST</strong> an toàn với chạy lặp (idempotent): trả một <code>payout_hold</code> đúng một lần (không double-pay), kể cả khi job chạy lại.</li><li><strong>MUST</strong> phát metric: <code>payout_hold_total{reason}</code>, <code>payout_released_total</code>, <code>payout_under_investigation_total</code>, và phân phối thời gian giữ (để cân delay vs trải nghiệm).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao delay payout (DEC-TRUST-21)?</strong> Affiliate fraud có đặc tính: tiền đi rồi khó đòi lại. Trả ngay khi \"confirmed\" nghĩa là kẻ gian rút xong trước khi ai kịp nhìn. Cửa sổ delay (hold period) cho hệ thống và điều tra viên thời gian phát hiện gaming trước khi tiền rời tay - đây là best practice ngành (§5.3) và là tuyến phòng thủ rẻ nhất.</p>\n<p><strong>Vì sao bắt last-click thao túng (DEC-TRUST-22a)?</strong> Mô hình affiliate là last-click: click cuối trước order ăn hoa hồng. Kẻ gian lợi dụng bằng cách chèn một click affiliate ngay trước khi order một món đã nằm sẵn trong giỏ từ lâu - \"cướp\" attribution mà không tạo giá trị thật. Phát hiện mẫu \"click chèn cuối lên sản phẩm đã ở giỏ lâu\" chặn trò này.</p>\n<p><strong>Vì sao self-referral check qua đồ thị (DEC-TRUST-22b, §1 #7)?</strong> Tự mua qua link của chính mình (hoặc tài khoản cùng cụm) là cách rút hoa hồng phổ biến. Một mình khó thấy; nhìn ở đồ thị quan hệ (FR-TRUST-004), nếu người mua và chủ link cùng cụm liên kết thì lộ. Tái dùng đồ thị có sẵn thay vì dựng lại.</p>\n<p><strong>Vì sao chỉ delay/giữ, KHÔNG tự từ chối (DEC-TRUST-23)?</strong> Tự từ chối payout khi nghi ngờ là quỵt tiền user thật nếu nghi sai - tổn hại niềm tin nặng, ngược định vị SănDeal. Risk cao thì kéo dài delay hoặc giữ chờ điều tra, nhưng quyết \"từ chối\" chỉ đến sau khi điều tra kết luận <code>confirmed_fraud</code>. Mặc định nghiêng về trả tiền thật, chỉ giữ khi có lý do.</p>\n<p><strong>Vì sao ba điều kiện để giải ngân (DEC-TRUST-24)?</strong> Trả tiền đúng cần cả ba: hết delay (đã có thời gian điều tra), không hold mở (không đang nghi), và network đã confirm (tiền thật sự về từ sàn - không trả tiền chưa nhận). Thiếu bất kỳ điều nào, giữ lại. Đây là cổng đúng-đắn cho dòng tiền ra.</p>\n<p><strong>Vì sao ghi hold_reason cụ thể (DEC-TRUST-25)?</strong> Giữ tiền user mà không nói lý do là tùy tiện và phá niềm tin. Mỗi delay/hold phải gắn tín hiệu cụ thể (last-click thao túng / self-referral / risk cao) để điều tra kiểm tra được và để giải thích với user nếu cần. Không có tín hiệu thì không giữ.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/trust/migrations/0003_payout_hold.sql\nCREATE TABLE payout_hold (\n  conversion_id  BIGINT      PRIMARY KEY REFERENCES affiliate_conversion(id),\n  beneficiary    BIGINT      NOT NULL REFERENCES app_user(id),\n  payout_amount  BIGINT      NOT NULL CHECK (payout_amount &gt;= 0),   -- VND, không thập phân\n  confirmed_at   TIMESTAMPTZ NOT NULL,\n  eligible_at    TIMESTAMPTZ NOT NULL,                              -- confirmed_at + delay_window\n  hold_reason    TEXT,                                             -- NULL nếu không có tín hiệu giữ\n  status         TEXT        NOT NULL DEFAULT 'holding'\n                 CHECK (status IN ('holding','under_investigation','released','denied')),\n  released_at    TIMESTAMPTZ,\n  CHECK (status &lt;&gt; 'released' OR released_at IS NOT NULL)\n);\nCREATE INDEX idx_hold_due ON payout_hold (eligible_at)\n  WHERE status = 'holding';</code></pre>\n<h3>attribution_guard.go (phát hiện gaming)</h3>\n<pre><code class=\"language-go\">// services/trust/internal/payout/attribution_guard.go\n// Inspect trả hold_reason (rỗng nếu sạch). KHÔNG quyết từ chối - chỉ gắn cờ.\nfunc (g *Guard) Inspect(ctx context.Context, conv Conversion) string {\n    // (a) last-click thao túng: click ngay trước order, sản phẩm đã ở giỏ lâu\n    if click, ok := g.lastClick(ctx, conv); ok {\n        if conv.OrderedAt.Sub(click.At) &lt; g.cfg.SuspiciousClickGap &amp;&amp;\n           g.itemInCartSince(ctx, conv).Before(click.At.Add(-g.cfg.OldCartWindow)) {\n            return \"last_click_manipulation\"\n        }\n    }\n    // (b) self-referral: người mua &amp; chủ link cùng cụm (FR-TRUST-004)\n    if g.fraud.SameCluster(ctx, conv.BuyerID, conv.ReferrerID) {\n        return \"self_referral\"\n    }\n    // (c) cookie-stuffing: click không gắn user-action hợp lệ (NFR-AFFIL-001)\n    if !conv.Click.UserInitiated {\n        return \"cookie_stuffing_signal\"\n    }\n    return \"\"\n}</code></pre>\n<h3>release.go (giải ngân khi đủ ba điều kiện)</h3>\n<pre><code class=\"language-go\">// services/trust/internal/payout/release.go\nconst dueSQL = `\nSELECT conversion_id, beneficiary, payout_amount\nFROM payout_hold h\nWHERE h.status = 'holding'\n  AND h.eligible_at &lt;= now()                       -- (a) hết delay\n  AND h.hold_reason IS NULL                         -- (b) không tín hiệu giữ\n  AND EXISTS (SELECT 1 FROM affiliate_conversion c  -- (c) network confirmed\n              WHERE c.id = h.conversion_id AND c.status = 'confirmed')\nFOR UPDATE SKIP LOCKED;`\n\n// ReleaseDue trả các hold đủ điều kiện đúng một lần (idempotent qua status flip + lock).\nfunc (r *Releaser) ReleaseDue(ctx context.Context) (int, error) { /* select dueSQL -&gt; pay -&gt; set released */ }</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Conversion <code>confirmed</code> -&gt; tạo <code>payout_hold</code> với <code>eligible_at = confirmed_at + delay_window</code>; KHÔNG trả ngay.</li><li>Trước <code>eligible_at</code> -&gt; <code>ReleaseDue</code> KHÔNG trả hold đó (còn trong delay).</li><li>Sau <code>eligible_at</code>, không <code>hold_reason</code>, network confirmed -&gt; <code>ReleaseDue</code> trả đúng một lần, status <code>released</code>.</li><li>Last-click thao túng (click sát order + sản phẩm đã ở giỏ lâu) -&gt; <code>hold_reason='last_click_manipulation'</code>, không giải ngân.</li><li>Self-referral (người mua + chủ link cùng cụm FR-TRUST-004) -&gt; <code>hold_reason='self_referral'</code>.</li><li>Click không user-initiated -&gt; <code>hold_reason='cookie_stuffing_signal'</code> (nối NFR-AFFIL-001).</li><li><code>risk_score</code> cao (FR-TRUST-004) -&gt; delay kéo dài / chuyển <code>under_investigation</code>; KHÔNG tự <code>denied</code>.</li><li>Điều tra kết luận <code>cleared</code> -&gt; hold được giải phóng ở chu kỳ release kế tiếp (tiền thật không bị quỵt).</li><li><code>denied</code> chỉ xảy ra sau điều tra <code>confirmed_fraud</code>; không có nhánh tự động <code>denied</code>.</li><li>Giải ngân cần ĐỦ ba điều kiện; thiếu bất kỳ (chưa hết delay / có hold_reason / chưa network-confirm) -&gt; giữ.</li><li>Idempotent: một conversion một <code>payout_hold</code>; <code>ReleaseDue</code> chạy lặp không double-pay (lock + status).</li><li><code>payout_amount</code> BIGINT VND; metric <code>payout_hold_total{reason}</code>/<code>payout_released_total</code> hoạt động; giữ tiền luôn kèm <code>hold_reason</code>.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/trust/internal/payout/delay_test.go\nfunc TestDelay_NotReleasedBeforeWindow(t *testing.T) {\n    r := setup(t)\n    h := seedHold(t, r, confirmedAt(time.Now()), delay(72*time.Hour))\n    n, _ := r.ReleaseDue(ctx)            // ngay bây giờ\n    require.Equal(t, 0, n)               // còn trong delay\n    require.Equal(t, \"holding\", statusOf(t, r, h))\n}\n\nfunc TestDelay_ReleasedAfterWindow_AllConditions(t *testing.T) {\n    r := setup(t)\n    h := seedHold(t, r, confirmedAt(time.Now().Add(-100*time.Hour)), delay(72*time.Hour))\n    networkConfirmed(t, r, h)            // (c)\n    n, _ := r.ReleaseDue(ctx)\n    require.Equal(t, 1, n)\n    require.Equal(t, \"released\", statusOf(t, r, h))\n}</code></pre>\n<pre><code class=\"language-go\">// services/trust/internal/payout/attribution_guard_test.go\nfunc TestGuard_LastClickManipulation(t *testing.T) {\n    g := setupGuard(t)\n    conv := convWithCart(t, cartSince(-48*time.Hour), clickAt(-1*time.Minute), orderAt(0))\n    require.Equal(t, \"last_click_manipulation\", g.Inspect(ctx, conv))\n}\n\nfunc TestGuard_SelfReferral_SameCluster(t *testing.T) {\n    g := setupGuard(t)\n    conv := convBuyerAndReferrerLinked(t)   // cùng cụm FR-TRUST-004\n    require.Equal(t, \"self_referral\", g.Inspect(ctx, conv))\n}\n\nfunc TestGuard_CleanConversion_NoHold(t *testing.T) {\n    g := setupGuard(t)\n    require.Equal(t, \"\", g.Inspect(ctx, cleanUserInitiatedConv(t)))\n}</code></pre>\n<pre><code class=\"language-go\">// services/trust/internal/payout/release_test.go\nfunc TestRelease_HighRisk_HoldsNotDenies(t *testing.T) {\n    r := setup(t)\n    h := seedHold(t, r, confirmedAt(time.Now().Add(-100*time.Hour)), delay(72*time.Hour))\n    setRiskHigh(t, r, h)                 // FR-TRUST-004\n    r.ReleaseDue(ctx)\n    require.Equal(t, \"under_investigation\", statusOf(t, r, h)) // KHÔNG denied tự động\n}\n\nfunc TestRelease_Idempotent_NoDoublePay(t *testing.T) {\n    r := setup(t); h := seedReleasable(t, r)\n    r.ReleaseDue(ctx); r.ReleaseDue(ctx)\n    require.Equal(t, 1, payCount(t, r, h)) // trả đúng một lần\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration 0003 (<code>payout_hold</code>) -&gt; nối affil repo (conversion <code>confirmed</code> -&gt; tạo <code>payout_hold</code>, không trả ngay) -&gt; <code>attribution_guard.go</code> (gắn <code>hold_reason</code> cho last-click/self-referral/cookie-stuffing) -&gt; <code>delay.go</code> (tính <code>eligible_at</code>, kéo dài khi risk cao) -&gt; <code>release.go</code> (job giải ngân khi đủ ba điều kiện, idempotent qua lock + status flip). Self-referral check tái dùng <code>SameCluster</code> của FR-TRUST-004. Job release chạy định kỳ; <code>denied</code> chỉ đến từ kết luận điều tra <code>confirmed_fraud</code>, không tự động. Cashback layering (FR-AFFIL-005) trả qua cùng cơ chế hold-then-release.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>FR-AFFIL-001</strong> - <code>affiliate_conversion</code> (status confirmed) là nguồn tạo <code>payout_hold</code>; <code>affiliate_click</code> (user_initiated) cho cookie-stuffing check.</li><li><strong>FR-TRUST-004</strong> - <code>risk_score</code> + <code>SameCluster</code> (đồ thị quan hệ) cho kéo dài delay + self-referral check.</li><li><strong>FR-AFFIL-005 (downstream)</strong> - cashback layering trả qua cơ chế hold-then-release của FR này.</li><li><strong>FR-AFFIL-003</strong> - postback network confirm là điều kiện (c) để giải ngân.</li><li><strong>FR-BILL-002</strong> - cổng thanh toán thực hiện chi trả khi <code>release.go</code> quyết payout.</li><li>NFR-AFFIL-001 - cookie-stuffing/user-initiated; FR này thực thi tuyến phòng thủ payout cho cam kết đó.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>payout_hold tạo khi conversion confirmed (giữ, không trả ngay)</h3>\n<pre><code class=\"language-json\">{\n  \"conversion_id\": 77120, \"beneficiary\": 4412, \"payout_amount\": 31000,\n  \"confirmed_at\": \"2026-09-01T10:00:00Z\",\n  \"eligible_at\": \"2026-09-04T10:00:00Z\",   // +72h delay\n  \"hold_reason\": null, \"status\": \"holding\"\n}</code></pre>\n<h3>Conversion bị gắn cờ gaming (giữ tiếp, có lý do)</h3>\n<pre><code class=\"language-json\">{\n  \"conversion_id\": 77130, \"hold_reason\": \"last_click_manipulation\",\n  \"status\": \"holding\",\n  \"note\": \"click affiliate 40s trước order; sản phẩm đã ở giỏ 2 ngày -&gt; nghi cướp attribution\"\n}\n// KHÔNG tự denied; chờ điều tra. Nếu cleared -&gt; released chu kỳ kế.</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Độ dài <code>delay_window</code> tối ưu theo sàn/network - hiệu chỉnh theo dữ liệu thật (cân chống gian lận vs trải nghiệm chờ); FR đặt cơ chế cấu hình.</li><li>Chargeback/đối soát ngược khi network rút lại commission sau khi đã release - cần luồng claw-back, xét khi gặp ở production.</li><li>Tự động clear ca rủi ro thấp rõ ràng (rút ngắn delay) - chỉ sau khi đo false positive đủ thấp.</li><li>Thông báo cho user vì sao payout bị giữ (minh bạch hold_reason ở mức phù hợp) - thiết kế UX ở FR web/mobile, FR này cung cấp dữ liệu.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Trả payout ngay khi confirmed</td><td>delay_test</td><td>tiền gian lận đi trước khi phát hiện</td><td>Cửa sổ delay bắt buộc (DEC-TRUST-21)</td></tr><tr><td>Last-click bị chèn cướp attribution</td><td>guard test</td><td>hoa hồng oan</td><td>Phát hiện click sát order + giỏ cũ (DEC-TRUST-22a)</td></tr><tr><td>Self-referral rút hoa hồng</td><td>guard test (same cluster)</td><td>tự cày tiền</td><td>Đồ thị quan hệ FR-TRUST-004 (§1 #7)</td></tr><tr><td>Cookie-stuffing (click không user-action)</td><td>guard test</td><td>vi phạm né-Honey</td><td>Cờ cookie_stuffing_signal (NFR-AFFIL-001)</td></tr><tr><td>Tự từ chối payout khi nghi</td><td>release_test (hold not deny)</td><td>quỵt user thật</td><td>Chỉ delay/giữ; denied chỉ sau điều tra (DEC-TRUST-23)</td></tr><tr><td>Giữ tiền vô cớ</td><td>review hold_reason</td><td>tùy tiện, mất niềm tin</td><td>Giữ luôn kèm hold_reason cụ thể (DEC-TRUST-25)</td></tr><tr><td>Trả tiền chưa network-confirm</td><td>release SQL điều kiện (c)</td><td>trả tiền chưa nhận</td><td>Đủ ba điều kiện mới release (DEC-TRUST-24)</td></tr><tr><td>Double-pay khi job chạy lại</td><td>idempotent test</td><td>chi trùng</td><td>Lock + status flip một lần (§1 #11)</td></tr><tr><td>Tiền thật bị quỵt khi cleared</td><td>release sau cleared test</td><td>hại user thật bị nghi oan</td><td>Giải phóng hold ở chu kỳ kế (§1 #10)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Delay payout là tuyến phòng thủ rẻ nhất cho dòng tiền affiliate: cho thời gian phát hiện gaming trước khi tiền rời tay (best practice §5.3).</li><li>Nguyên tắc đối xứng với FR-TRUST-004: chỉ delay/giữ + điều tra, KHÔNG tự từ chối - mặc định nghiêng về trả tiền thật, chỉ giữ khi có hold_reason cụ thể.</li><li>Ba dấu hiệu gaming (last-click thao túng, self-referral, cookie-stuffing) cùng nhắm đúng các trò rút hoa hồng phổ biến; self-referral tái dùng đồ thị FR-TRUST-004.</li><li>Ba điều kiện giải ngân (hết delay + không hold + network-confirm) là cổng đúng-đắn cho tiền ra: không sớm, không khi đang nghi, không khi chưa nhận tiền thật.</li><li>Job release idempotent (lock + status) đảm bảo mỗi hold trả đúng một lần kể cả khi chạy lặp - an toàn cho tiền.</li><li>Cookie-stuffing check nối thẳng cam kết né-Honey (§4.2): đây đúng là trò Honey bị phơi bày, và SănDeal chặn nó ở cả tầng click (NFR-AFFIL-001) lẫn tầng payout (FR này).</li></ul>\n<hr>\n<p><em>Hết FR-TRUST-005. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
