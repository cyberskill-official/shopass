window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SCRAPE-004"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Module proxy <strong>MUST</strong> cấp session residential xoay vòng theo tier nhà cung cấp và nước, khớp fingerprint, dưới một cost-guard ngân sách. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> mặc định dùng residential proxy cho mọi target có WAF/anti-bot (DEC-SCRAPE-15). Datacenter chỉ được dùng cho target xác nhận KHÔNG có Cloudflare/Akamai; với Shopee/TikTok/Lazada luôn residential.</li><li><strong>MUST</strong> phân tầng nhà cung cấp (DEC-SCRAPE-16) qua enum <code>tier - {enterprise, mid, budget}</code>:</li></ol>\n<ul><li><code>enterprise</code>: Bright Data, Oxylabs (~8,5-12 USD/GB) - độ tin cậy cao nhất, tỷ lệ success ~99,95%.</li><li><code>mid</code>: Decodo, SOAX, NetNut (~3-6 USD/GB).</li><li><code>budget</code>: IPRoyal (~1,75 USD/GB) - rẻ nhất.</li></ul>\n<ol><li><strong>MUST</strong> chọn tier theo độ khó target (DEC-SCRAPE-17) qua hàm thuần <code>SelectTier(d TargetDifficulty) Tier</code>:</li></ol>\n<ul><li>Akamai (Lazada), ByteDance attestation (TikTok) -&gt; <code>enterprise</code>.</li><li>Shopee qua internal JSON (dễ hơn) -&gt; <code>budget</code> hoặc <code>mid</code>.</li><li>Mặc định khi không rõ -&gt; <code>mid</code> (cân bằng).</li></ul>\n<ol><li><strong>MUST</strong> cấp session proxy theo cặp <code>(tier, country)</code> và xoay vòng IP: <code>Acquire(ctx, tier, country) (ProxySession, error)</code>; mỗi session có <code>url/user/pass</code>, và pool <strong>MUST</strong> tránh tái dùng cùng một IP liên tục cho cùng một sàn trong cửa sổ ngắn.</li><li><strong>MUST</strong> đánh dấu IP bị ban/challenge: khi adapter/farm báo IP bị chặn, pool <strong>MUST</strong> ghi nhận và không cấp lại IP đó trong khoảng cooldown.</li><li><strong>MUST</strong> gắn proxy session với fingerprint profile cùng nước (DEC-SCRAPE-19): proxy VN chỉ ghép profile VN của FR-SCRAPE-003; ghép sai nước bị từ chối.</li><li><strong>MUST</strong> cost-guard (DEC-SCRAPE-18): theo dõi GB tiêu thụ và chi phí ước tính per provider per ngày vào <code>proxy_usage</code>; khi chi phí ngày vượt ngân sách cấu hình, cost-guard <strong>MUST</strong>:</li></ol>\n<ul><li>hạ tier (enterprise -&gt; mid -&gt; budget) cho request không tới hạn, và/hoặc</li><li>tạm dừng quét tier <code>cold</code> (FR-SCRAPE-001), giữ tier <code>hot</code> (flash sale) chạy.</li></ul>\n<ol><li><strong>MUST</strong> expose <code>CanProceed(tier, country) (bool, reason)</code> để orchestrator hỏi trước khi cấp session khi gần trần ngân sách.</li><li><strong>MUST</strong> quy đổi chi phí bằng số nguyên (USD micro hoặc cent), KHÔNG dùng float tích lũy cho tiền (đồng bộ nguyên tắc tiền tệ của hệ thống).</li><li><strong>SHOULD</strong> phát OTel metric: <code>proxy_gb_used_total{provider, tier}</code> (counter), <code>proxy_cost_usd_total{provider}</code> (counter), <code>proxy_ip_banned_total{provider}</code> (counter), <code>proxy_acquire_duration_ms{tier}</code> (histogram).</li><li><strong>MUST</strong> đọc credentials proxy từ Vault (FR-INFRA-003), KHÔNG hardcode/cleartext.</li><li><strong>MUST</strong> ghi <code>proxy_usage</code> per ngày để báo cáo unit economics đối chiếu ngưỡng §4.1 (~0,1-0,2 USD/user/tháng).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao bắt buộc residential (DEC-SCRAPE-15)?</strong> Tài liệu nói thẳng: datacenter proxy gần như vô dụng với Cloudflare/Akamai (§3.3). IP datacenter nằm trong dải ASN dễ nhận diện, bị chặn theo lô. Lazada (Akamai) và TikTok (ByteDance) sẽ chặn ngay. Residential mang IP của hộ gia đình thật, khó phân biệt với người dùng thường - đó là lý do nó đắt và là lý do nó cần thiết.</p>\n<p><strong>Vì sao phân tầng nhà cung cấp (DEC-SCRAPE-16/17)?</strong> Chênh lệch giá 5-7 lần giữa IPRoyal (1,75) và Bright Data (12). Dùng enterprise cho mọi thứ là đốt tiền; dùng budget cho Akamai là bị chặn. Chọn tier theo độ khó target: trả tiền cao chỉ ở nơi cần độ tin cậy cao (Akamai/ByteDance), dùng budget/mid ở nơi dễ (Shopee JSON). Đây là cách giữ proxy trong ngân sách §4.1.</p>\n<p><strong>Vì sao cost-guard chủ động hạ tier/dừng cold (DEC-SCRAPE-18)?</strong> Proxy là biến phí lớn nhất phía băng thông. Một bug retry-loop hay một đợt sàn siết có thể đốt cả ngân sách ngày trong vài giờ. Cost-guard đặt trần cứng: khi gần trần, ưu tiên giữ tier <code>hot</code> (flash sale - giá trị cao nhất) và hy sinh tier <code>cold</code> (SKU ít quan tâm). Tiền được tiêu vào nơi tạo giá trị.</p>\n<p><strong>Vì sao gắn proxy với fingerprint cùng nước (DEC-SCRAPE-19)?</strong> Như đã nêu ở FR-SCRAPE-003: IP mang geolocation. Profile VN (timezone, ngôn ngữ VN) qua IP nước khác là mâu thuẫn. Buộc cặp (proxy &lt;-&gt; profile cùng nước) ngay tại tầng cấp session giữ câu chuyện nhất quán, không để adapter lỡ tay ghép sai.</p>\n<p><strong>Vì sao xoay IP + cooldown ban (§1 #4, #5)?</strong> Tái dùng một IP dày đặc cho cùng một sàn làm IP đó \"nóng\" và bị ban, kéo theo các request sau cũng hỏng. Xoay vòng phân tán dấu vết; đánh dấu IP đã bị chặn và cho nghỉ tránh cấp lại IP đang bị sàn theo dõi.</p>\n<p><strong>Vì sao chi phí số nguyên (§1 #9)?</strong> Cộng dồn GB và tiền bằng float tích lũy sai số qua hàng triệu request. Dùng cent/micro-USD nguyên giữ sổ chi phí chính xác để báo cáo unit economics đáng tin.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/scrape/migrations/0002_proxy_usage.sql\nCREATE TABLE proxy_usage (\n  day          DATE        NOT NULL,\n  provider     TEXT        NOT NULL,           -- 'brightdata','oxylabs','decodo','iproyal',...\n  tier         TEXT        NOT NULL,           -- 'enterprise'|'mid'|'budget'\n  country      TEXT        NOT NULL,\n  bytes_used   BIGINT      NOT NULL DEFAULT 0,\n  cost_micro_usd BIGINT    NOT NULL DEFAULT 0, -- chi phí ước tính, micro-USD (số nguyên)\n  PRIMARY KEY (day, provider, tier, country)\n);</code></pre>\n<h3>Tier selection (Go)</h3>\n<pre><code class=\"language-go\">// services/scrape/internal/proxy/tier.go\ntype Tier string\nconst (\n    TierEnterprise Tier = \"enterprise\"  // Bright Data, Oxylabs (~8,5-12 USD/GB)\n    TierMid        Tier = \"mid\"         // Decodo, SOAX, NetNut (~3-6 USD/GB)\n    TierBudget     Tier = \"budget\"      // IPRoyal (~1,75 USD/GB)\n)\n\ntype TargetDifficulty int\nconst (\n    DiffShopeeJSON TargetDifficulty = iota // dễ: internal JSON\n    DiffAkamai                              // Lazada\n    DiffByteDance                           // TikTok attestation\n    DiffUnknown\n)\n\n// SelectTier chọn tier proxy theo độ khó target (DEC-SCRAPE-17).\nfunc SelectTier(d TargetDifficulty) Tier {\n    switch d {\n    case DiffAkamai, DiffByteDance:\n        return TierEnterprise   // độ tin cậy cao nhất cho WAF mạnh\n    case DiffShopeeJSON:\n        return TierBudget       // JSON dễ -&gt; tiết kiệm\n    default:\n        return TierMid\n    }\n}</code></pre>\n<h3>Cost-guard (Go)</h3>\n<pre><code class=\"language-go\">// services/scrape/internal/proxy/costguard.go\ntype Decision int\nconst (\n    Proceed     Decision = iota // dưới ngân sách\n    DowngradeTier                // hạ tier cho request không tới hạn\n    BlockCold                    // dừng tier cold, giữ hot\n)\n\n// Evaluate quyết định dựa trên chi phí ngày so ngân sách (micro-USD số nguyên).\nfunc (g *CostGuard) Evaluate(ctx context.Context, day time.Time) (Decision, error) {\n    spent, err := g.repo.SpentMicroUSD(ctx, day)\n    if err != nil {\n        return Proceed, err\n    }\n    switch {\n    case spent &gt;= g.dailyBudgetMicro:\n        return BlockCold, nil          // chạm trần -&gt; chỉ giữ hot\n    case spent &gt;= g.dailyBudgetMicro*8/10:\n        return DowngradeTier, nil      // 80% -&gt; hạ tier\n    default:\n        return Proceed, nil\n    }\n}</code></pre>\n<h3>Pool (Go)</h3>\n<pre><code class=\"language-go\">// services/scrape/internal/proxy/pool.go\nfunc (p *Pool) Acquire(ctx context.Context, tier Tier, country string) (ProxySession, error) {\n    if ok, reason := p.guard.CanProceed(tier, country); !ok {\n        return ProxySession{}, fmt.Errorf(\"cost-guard: %s\", reason)\n    }\n    ip := p.rotate(tier, country)         // tránh IP nóng + IP đang cooldown ban\n    creds := p.vault.ProxyCreds(tier)     // FR-INFRA-003, không cleartext\n    return ProxySession{URL: ip.URL, User: creds.User, Pass: creds.Pass, Country: country}, nil\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch -&gt; <code>proxy_usage</code> tồn tại với khóa <code>(day, provider, tier, country)</code> và <code>cost_micro_usd</code> BIGINT.</li><li><code>SelectTier(DiffAkamai)</code> = <code>enterprise</code>; <code>SelectTier(DiffByteDance)</code> = <code>enterprise</code>.</li><li><code>SelectTier(DiffShopeeJSON)</code> = <code>budget</code>; <code>SelectTier(DiffUnknown)</code> = <code>mid</code>.</li><li><code>Acquire(tier, \"VN\")</code> trả session có <code>Country=\"VN\"</code> và creds lấy từ Vault (không cleartext trong code).</li><li>Pool không cấp lại cùng một IP cho cùng sàn trong cửa sổ ngắn (xoay vòng).</li><li>IP bị báo ban -&gt; không được cấp lại trong cooldown (kiểm qua <code>MarkBanned</code> + <code>Acquire</code>).</li><li>Proxy session VN chỉ ghép profile VN; yêu cầu ghép proxy VN với profile ID -&gt; bị từ chối.</li><li><code>CostGuard.Evaluate</code> dưới 80% ngân sách -&gt; <code>Proceed</code>; &gt;=80% -&gt; <code>DowngradeTier</code>; chạm trần -&gt; <code>BlockCold</code>.</li><li>Khi <code>BlockCold</code>, orchestrator vẫn cấp được proxy cho job tier <code>hot</code>, nhưng job <code>cold</code> bị chặn.</li><li>Chi phí cộng dồn dùng số nguyên (micro-USD); với 1,75 USD/GB x 2GB -&gt; <code>cost_micro_usd</code> đúng, không sai số float.</li><li><code>proxy_usage</code> ghi đúng <code>bytes_used</code> và <code>cost_micro_usd</code> per ngày/provider để báo cáo §4.1.</li><li>Metric <code>proxy_gb_used_total</code>, <code>proxy_cost_usd_total</code>, <code>proxy_ip_banned_total</code> thay đổi đúng.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/scrape/internal/proxy/tier_test.go\nfunc TestSelectTier_ByDifficulty(t *testing.T) {\n    require.Equal(t, TierEnterprise, SelectTier(DiffAkamai))    // Lazada\n    require.Equal(t, TierEnterprise, SelectTier(DiffByteDance)) // TikTok\n    require.Equal(t, TierBudget, SelectTier(DiffShopeeJSON))    // Shopee JSON\n    require.Equal(t, TierMid, SelectTier(DiffUnknown))\n}</code></pre>\n<pre><code class=\"language-go\">// services/scrape/internal/proxy/costguard_test.go\nfunc TestCostGuard_DowngradeThenBlock(t *testing.T) {\n    g := newGuard(t, /*dailyBudgetMicro=*/10_000_000) // 10 USD/ngày\n    setSpent(t, g, 5_000_000)\n    d, _ := g.Evaluate(ctx, today)\n    require.Equal(t, Proceed, d)\n\n    setSpent(t, g, 8_500_000) // 85%\n    d, _ = g.Evaluate(ctx, today)\n    require.Equal(t, DowngradeTier, d)\n\n    setSpent(t, g, 10_000_000) // chạm trần\n    d, _ = g.Evaluate(ctx, today)\n    require.Equal(t, BlockCold, d)\n}\n\nfunc TestCost_IntegerMicroUSD(t *testing.T) {\n    // 1,75 USD/GB x 2GB = 3,50 USD = 3_500_000 micro-USD, chính xác\n    got := costMicroUSD(/*usdPerGBMicro=*/1_750_000, /*bytes=*/2&lt;&lt;30)\n    require.Equal(t, int64(3_500_000), got)\n}</code></pre>\n<pre><code class=\"language-go\">// services/scrape/internal/proxy/pool_test.go\nfunc TestAcquire_GeoMatchesProfile(t *testing.T) {\n    p := newPool(t)\n    _, err := p.BindProfile(proxyVN(), profileID(\"VN\"))\n    require.NoError(t, err)\n    _, err = p.BindProfile(proxyVN(), profileID(\"US\"))\n    require.Error(t, err) // proxy VN + profile US bị từ chối\n}\n\nfunc TestAcquire_BannedIPCooldown(t *testing.T) {\n    p := newPool(t)\n    s, _ := p.Acquire(ctx, TierBudget, \"VN\")\n    p.MarkBanned(s.IP)\n    for i := 0; i &lt; 20; i++ {\n        s2, _ := p.Acquire(ctx, TierBudget, \"VN\")\n        require.NotEqual(t, s.IP, s2.IP) // IP bị ban không cấp lại\n    }\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration 0002 (proxy_usage) -&gt; tier.go (SelectTier thuần, test trước) -&gt; costguard.go (Evaluate + repo SpentMicroUSD) -&gt; pool.go (Acquire + rotate + MarkBanned) -&gt; session.go (bind profile cùng nước) -&gt; tests. Provider tiers và ngân sách/ngày ở config.go (scrape-svc, Go); creds đọc từ Vault (FR-INFRA-003). Cost-guard chạy đồng bộ trong đường <code>Acquire</code> để chặn trước khi tiêu, không phải cảnh báo sau.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>FR-SCRAPE-003</strong> - fingerprint profile ghép proxy cùng nước; farm dùng proxy khi render.</li><li><strong>FR-SCRAPE-001</strong> - orchestrator hỏi <code>CanProceed</code> trước khi cấp session; cost-guard chặn tier cold.</li><li><strong>FR-SCRAPE-002 / 007 / 008</strong> - adapter lấy proxy session để gọi endpoint / render.</li><li><strong>FR-INFRA-003</strong> - credentials proxy từ Vault.</li><li>§4.1 - <code>proxy_usage</code> cung cấp số liệu báo cáo unit economics.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Cấp session cho job Lazada (Akamai -&gt; enterprise)</h3>\n<pre><code class=\"language-go\">tier := proxy.SelectTier(proxy.DiffAkamai)        // enterprise\nsess, err := proxyPool.Acquire(ctx, tier, \"VN\")\n// sess.URL trỏ residential VN tier enterprise; ghép profile VN của farm</code></pre>\n<h3>Cấu hình provider tiers + ngân sách (config)</h3>\n<pre><code class=\"language-yaml\">proxy:\n  daily_budget_usd: 40\n  providers:\n    enterprise: [brightdata, oxylabs]   # ~8,5-12 USD/GB\n    mid:        [decodo, soax, netnut]  # ~3-6 USD/GB\n    budget:     [iproyal]               # ~1,75 USD/GB\n  ban_cooldown_min: 30</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Giá GB chính xác từng provider (thay đổi theo hợp đồng/khối lượng) - đọc từ config, cập nhật khi ký hợp đồng thật.</li><li>Tự động chuyển provider khi một nhà cung cấp tụt success-rate - bắt đầu chọn tier tĩnh, thêm chọn động theo metric sau.</li><li>Mobile proxy (4G) cho target khó nhất - cân nhắc nếu enterprise residential vẫn bị chặn.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Datacenter cho Akamai/Cloudflare</td><td>tỷ lệ block cao</td><td>ban chắc</td><td>Bắt buộc residential (§1 #1)</td></tr><tr><td>Enterprise cho mọi request</td><td><code>proxy_cost_usd_total</code></td><td>đốt tiền 5-7x</td><td>SelectTier theo độ khó (§1 #3)</td></tr><tr><td>Retry-loop đốt ngân sách</td><td>cost-guard trần</td><td>vỡ §4.1</td><td>BlockCold + DowngradeTier (§1 #7)</td></tr><tr><td>Proxy nước khác + fingerprint</td><td>guard BindProfile</td><td>mâu thuẫn geo</td><td>Cặp (proxy &lt;-&gt; profile cùng nước) (§1 #6)</td></tr><tr><td>Tái dùng IP nóng</td><td><code>proxy_ip_banned_total</code></td><td>ban chùm</td><td>Xoay vòng + cooldown (§1 #4,#5)</td></tr><tr><td>Float tích lũy chi phí</td><td>cost test</td><td>sổ sai</td><td>micro-USD số nguyên (§1 #9)</td></tr><tr><td>Creds proxy cleartext</td><td>audit secrets</td><td>lộ bí mật</td><td>Đọc từ Vault (§1 #11)</td></tr><tr><td>Chạm trần làm tắt cả hot</td><td>test BlockCold</td><td>mất flash sale</td><td>BlockCold chỉ chặn cold, giữ hot (§1 #7)</td></tr><tr><td>proxy_usage không ghi</td><td>báo cáo trống</td><td>không đối chiếu §4.1 được</td><td>Ghi per ngày/provider (§1 #12)</td></tr><tr><td>Một provider sập</td><td>success-rate sụt</td><td>quét hụt</td><td>Nhiều provider/tier; chuyển tay (hoãn auto)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Proxy là biến phí lớn nhất phía băng thông; tiering nhà cung cấp + cost-guard là hai đòn bẩy giữ §4.1 (~0,1-0,2 USD/user).</li><li>Residential bắt buộc vì datacenter vô dụng với Cloudflare/Akamai - không phải lựa chọn tiết kiệm mà là điều kiện qua được Lazada/TikTok.</li><li>Cost-guard ưu tiên giá trị: chạm trần thì hy sinh <code>cold</code> (SKU ít quan tâm), giữ <code>hot</code> (flash sale) - tiền vào nơi tạo giá trị nhất.</li><li>Cặp (proxy &lt;-&gt; fingerprint cùng nước) là một nửa của câu chuyện nhất quán geo; nửa kia là profile của FR-SCRAPE-003.</li><li>Sổ chi phí số nguyên (micro-USD) cho phép báo cáo unit economics đáng tin để đối chiếu ngưỡng §4.1 hằng tháng.</li></ul>\n<hr>\n<p><em>Hết FR-SCRAPE-004. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
