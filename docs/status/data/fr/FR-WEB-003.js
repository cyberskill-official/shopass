window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-WEB-003"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>UI biểu đồ lịch sử giá <strong>MUST</strong> là một màn hình trong khu vực đăng nhập, tiêu thụ DUY NHẤT feed của FR-DEAL-003, vẽ thân giá theo ngày kèm các chú giải tín hiệu deal, đạt render p95 &lt;500ms, và tôn trọng độ chín dữ liệu để không kết luận ẩu. Hợp đồng:</p>\n<ol><li>UI <strong>MUST</strong> lấy dữ liệu DUY NHẤT từ feed <code>GET /v1/products/{id}/chart?range=...</code> của FR-DEAL-003 qua <code>lib/api.ts</code> (FR-WEB-001); KHÔNG tự gọi raw <code>price_snapshot</code>, KHÔNG tự dựng chuỗi giá từ dữ liệu thô (DEC-WEB-11).</li><li>Biểu đồ <strong>MUST</strong> vẽ phần thân từ mảng <code>daily</code> của feed (<code>{day, min_p, max_p, close_p}</code>), dùng <code>close_p</code> làm đường giá hiển thị, sắp tăng theo <code>day</code>.</li><li>Biểu đồ <strong>MUST</strong> vẽ các đường tham chiếu từ <code>annotations</code>: <code>median90</code> (đường trung vị 90 ngày) và <code>trailing_min</code> (đường đáy giá trong khoảng) - lấy thẳng từ feed, KHÔNG tự tính (DEC-WEB-11).</li><li>Biểu đồ <strong>MUST</strong> đánh dấu các mốc <code>annotations.double_dates</code> (ngày đôi 1.1...12.12 trong khoảng) trên trục thời gian, giúp người dùng đọc đúng bối cảnh lịch sale VN.</li><li>UI <strong>MUST</strong> hiển thị nhãn verdict lấy thẳng từ <code>annotations.verdict</code> (<code>SALE_AO</code>, <code>SALE_XIN</code>, <code>TAM_DUOC</code>, hoặc <code>UNKNOWN</code>) - KHÔNG suy diễn lại ở client (DEC-WEB-13). Nhãn này phải đồng nhất với nhãn trên thẻ sản phẩm (cùng nguồn FR-DEAL-001 qua feed).</li><li>UI <strong>MUST</strong> tôn trọng <code>maturity</code> của feed (DEC-WEB-14): khi <code>maturity == \"NEW\"</code> (&lt;14 ngày) hiển thị thông báo \"đang thu thập dữ liệu, chưa đủ để kết luận\" và KHÔNG hiển thị badge verdict kết luận (verdict là <code>UNKNOWN</code>); khi <code>maturity == \"WARMING\"</code> (hoặc <code>annotations.accumulating == true</code>) hiển thị ghi chú \"đang tích lũy dữ liệu\" kèm biểu đồ.</li><li>Bộ chọn khoảng (range selector) <strong>MUST</strong> chỉ phơi các nút trong allowlist của feed <code>{7d, 30d, 90d, 180d, 1y}</code> (DEC-WEB-15); đổi range gọi lại feed với range mới. KHÔNG gửi range ngoài allowlist.</li><li>Render <strong>MUST</strong> đạt p95 &lt;500ms tính từ khi nhận dữ liệu feed tới khi vẽ xong (DEC-WEB-12): feed đã downsample theo ngày nên payload nhỏ; không tải toàn chuỗi raw, không tính toán nặng ở client.</li><li>UI <strong>MUST</strong> xử lý trạng thái rỗng lịch sự: SKU có thật nhưng <code>daily</code> rỗng (chưa có snapshot) hiển thị \"đang thu thập dữ liệu giá\", không vỡ; lỗi mạng/<code>404</code> hiển thị thông báo lỗi rõ ràng.</li><li>Màn hình <strong>MUST</strong> nằm trong route group <code>(app)</code> (sau guard auth của FR-WEB-001); request feed mang JWT do <code>lib/api.ts</code> gắn.</li><li>Mọi giá hiển thị <strong>MUST</strong> được hiểu là số nguyên VND (int64 từ feed) - định dạng tiền tệ vi-VN khi render, KHÔNG dùng phép tính dấu phẩy động làm sai số.</li><li>Toàn bộ <strong>MUST</strong> vượt <code>npx tsc --noEmit</code> sạch và <code>npm test</code> xanh.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao chỉ tiêu thụ feed FR-DEAL-003 (DEC-WEB-11)?</strong> Feed đã làm sẵn ba việc nặng ở server: downsample về bucket ngày (payload nhỏ), tính median90/trailing_min, và gắn verdict từ một nguồn (FR-DEAL-001). Client tự gọi raw rồi tính lại vừa chậm (p95 vỡ), vừa lệch (verdict khác thẻ sản phẩm). Client chỉ nên vẽ. Đây là ranh giới phân tách rõ giữa tính toán (server) và hiển thị (client).</p>\n<p><strong>Vì sao p95 &lt;500ms (DEC-WEB-12)?</strong> §3.8 đặt ngưỡng biểu đồ &lt;500ms. Đây là màn hình người dùng mở để quyết \"mua hay đợi\" - chậm là mất khoảnh khắc quyết định. Vì feed đã downsample, payload nhỏ và client chỉ vẽ, ngưỡng này đạt được mà không cần tối ưu phức tạp. Nếu lỡ tải raw thì payload phình và render giật.</p>\n<p><strong>Vì sao verdict không suy diễn ở client (DEC-WEB-13)?</strong> Nhãn sale ảo phải đồng nhất giữa thẻ sản phẩm và biểu đồ. Nếu thẻ ghi SALE_XIN mà biểu đồ tự tính ra SALE_AO thì người dùng mất tin ngay - và niềm tin là moat của SănDeal hậu-Honey. Lấy thẳng <code>annotations.verdict</code> đảm bảo một nhãn từ một nguồn.</p>\n<p><strong>Vì sao tôn trọng maturity (DEC-WEB-14)?</strong> Vẽ kết luận trên SKU mới &lt;14 ngày là dán nhãn dựa trên vài điểm dữ liệu - đúng kiểu kết luận ẩu mà SănDeal hứa loại bỏ (§5.1 cold-start). NEW thì nói thẳng \"chưa đủ dữ liệu\"; WARMING thì vẽ nhưng gắn cờ đang tích lũy. Trung thực về độ chín là một phần của lời hứa niềm tin.</p>\n<p><strong>Vì sao range selector chỉ allowlist (DEC-WEB-15)?</strong> Feed chỉ nhận <code>{7d,30d,90d,180d,1y}</code>; range lạ trả 400. Phơi đúng các nút này ở UI tránh gửi range không hợp lệ và tránh lỗi hiển thị. UI và feed thống nhất một tập range.</p>\n<p><strong>Vì sao tách verdict-badge, range-selector, maturity-notice riêng (§3)?</strong> Mỗi mảnh là một đơn vị hiển thị độc lập, dễ test riêng (badge khớp verdict, selector phát range đúng, notice hiện đúng theo maturity). Ghép lại trong <code>price-chart</code> giữ component chính gọn.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Kiểu dữ liệu mirror feed (lib/chart/types.ts)</h3>\n<pre><code class=\"language-ts\">// web/lib/chart/types.ts - mirror DTO của FR-DEAL-003\nexport interface DailyPoint { day: string; min_p: number; max_p: number; close_p: number } // VND int64\nexport type Verdict = \"SALE_AO\" | \"SALE_XIN\" | \"TAM_DUOC\" | \"UNKNOWN\";\nexport type Maturity = \"MATURE\" | \"WARMING\" | \"NEW\";\n\nexport interface Annotations {\n  median90: number; trailing_min: number;       // VND\n  verdict: Verdict; accumulating: boolean; double_dates: string[];\n}\nexport interface ChartResponse {\n  product_id: number; range: string; maturity: Maturity;\n  daily: DailyPoint[]; annotations: Annotations;\n}\n\nexport const RANGE_ALLOWLIST = [\"7d\", \"30d\", \"90d\", \"180d\", \"1y\"] as const; // khớp feed (DEC-WEB-15)\nexport type Range = typeof RANGE_ALLOWLIST[number];</code></pre>\n<h3>Lấy dữ liệu (lib/chart/fetch-chart.ts)</h3>\n<pre><code class=\"language-ts\">// web/lib/chart/fetch-chart.ts\nimport { apiFetch } from \"@/lib/api\";\nimport { RANGE_ALLOWLIST, type ChartResponse, type Range } from \"./types\";\n\nexport async function fetchChart(productId: number, range: Range): Promise&lt;ChartResponse&gt; {\n  if (!RANGE_ALLOWLIST.includes(range)) throw new Error(\"range ngoài allowlist\"); // DEC-WEB-15\n  const res = await apiFetch(`/v1/products/${productId}/chart?range=${range}`); // feed FR-DEAL-003\n  if (res.status === 404) throw new NotFoundError();\n  if (!res.ok) throw new Error(\"không tải được biểu đồ\");\n  return (await res.json()) as ChartResponse; // client KHÔNG tính lại verdict/median (DEC-WEB-11/13)\n}</code></pre>\n<h3>Badge verdict (verdict-badge.tsx) - hiển thị thẳng, không suy diễn</h3>\n<pre><code class=\"language-tsx\">// web/components/price-chart/verdict-badge.tsx\nimport type { Verdict, Maturity } from \"@/lib/chart/types\";\n\nconst LABEL: Record&lt;Verdict, string&gt; = {\n  SALE_AO: \"Sale ảo\", SALE_XIN: \"Sale xịn\", TAM_DUOC: \"Tạm được\", UNKNOWN: \"Chưa đủ dữ liệu\",\n};\n\nexport function VerdictBadge({ verdict, maturity }: { verdict: Verdict; maturity: Maturity }) {\n  if (maturity === \"NEW\") return null;             // &lt;14 ngày: KHÔNG badge kết luận (DEC-WEB-14)\n  return &lt;span data-verdict={verdict}&gt;{LABEL[verdict]}&lt;/span&gt;; // lấy thẳng từ feed (DEC-WEB-13)\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>fetchChart</code> gọi <code>GET /v1/products/{id}/chart?range=</code> qua <code>apiFetch</code>; grep <code>web/components/price-chart/**</code> + <code>lib/chart/**</code>: KHÔNG có gọi raw <code>price_snapshot</code>/<code>price-history</code>.</li><li>Biểu đồ vẽ thân từ <code>daily</code> (close_p) sắp tăng theo <code>day</code>.</li><li>Biểu đồ vẽ đường <code>median90</code> và <code>trailing_min</code> lấy từ <code>annotations</code> (không tự tính trong client).</li><li>Mốc <code>annotations.double_dates</code> được đánh dấu trên trục thời gian.</li><li><code>VerdictBadge</code> hiển thị nhãn từ <code>annotations.verdict</code>; với cùng verdict, nhãn khớp nhãn thẻ sản phẩm (cùng nguồn); grep: client không có hàm tính verdict.</li><li><code>maturity == \"NEW\"</code> -&gt; ẩn badge verdict + hiện \"đang thu thập dữ liệu, chưa kết luận\"; <code>WARMING</code>/<code>accumulating</code> -&gt; hiện ghi chú đang tích lũy kèm biểu đồ.</li><li>Range selector chỉ render nút trong <code>{7d,30d,90d,180d,1y}</code>; <code>fetchChart</code> ném lỗi nếu nhận range ngoài allowlist (không gọi mạng).</li><li>SKU có thật, <code>daily</code> rỗng -&gt; hiện \"đang thu thập dữ liệu giá\", không vỡ; <code>404</code>/lỗi mạng -&gt; thông báo lỗi rõ.</li><li>Màn hình nằm trong group <code>(app)</code> (sau guard auth); request feed mang JWT qua <code>apiFetch</code>.</li><li>Giá hiển thị định dạng tiền VND (vi-VN) từ số nguyên int64; không phép dấu phẩy động làm sai số.</li><li><code>npx tsc --noEmit</code> sạch; <code>npm test</code> xanh.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-tsx\">// web/test/price-chart.test.tsx\nimport { render, screen } from \"@testing-library/react\";\nimport { PriceChart } from \"../components/price-chart/price-chart\";\n\nconst feed = {\n  product_id: 90112, range: \"90d\", maturity: \"MATURE\",\n  daily: [\n    { day: \"2026-04-04\", min_p: 119000, max_p: 149000, close_p: 119000 },\n    { day: \"2026-05-05\", min_p: 109000, max_p: 139000, close_p: 109000 },\n  ],\n  annotations: { median90: 129000, trailing_min: 99000, verdict: \"TAM_DUOC\",\n                 accumulating: false, double_dates: [\"2026-04-04\", \"2026-05-05\"] },\n};\n\ntest(\"vẽ đường tham chiếu median90 + trailing_min từ feed (không tự tính)\", () =&gt; {\n  render(&lt;PriceChart data={feed as any} /&gt;);\n  expect(screen.getByTestId(\"ref-median90\")).toHaveAttribute(\"data-value\", \"129000\");\n  expect(screen.getByTestId(\"ref-trailing-min\")).toHaveAttribute(\"data-value\", \"99000\");\n});\n\ntest(\"đánh dấu mốc ngày đôi từ feed\", () =&gt; {\n  render(&lt;PriceChart data={feed as any} /&gt;);\n  expect(screen.getAllByTestId(\"double-date-marker\").length).toBe(2);\n});</code></pre>\n<pre><code class=\"language-tsx\">// web/test/verdict-badge.test.tsx\nimport { render, screen } from \"@testing-library/react\";\nimport { VerdictBadge } from \"../components/price-chart/verdict-badge\";\n\ntest(\"badge lấy thẳng verdict từ feed\", () =&gt; {\n  render(&lt;VerdictBadge verdict=\"SALE_AO\" maturity=\"MATURE\" /&gt;);\n  expect(screen.getByText(\"Sale ảo\")).toHaveAttribute(\"data-verdict\", \"SALE_AO\");\n});\n\ntest(\"SKU NEW (&lt;14 ngày) KHÔNG hiện badge kết luận\", () =&gt; {\n  const { container } = render(&lt;VerdictBadge verdict=\"UNKNOWN\" maturity=\"NEW\" /&gt;);\n  expect(container).toBeEmptyDOMElement();\n});</code></pre>\n<pre><code class=\"language-tsx\">// web/test/range-selector.test.tsx\nimport { fetchChart } from \"../lib/chart/fetch-chart\";\nimport { RANGE_ALLOWLIST } from \"../lib/chart/types\";\n\ntest(\"chỉ allowlist range; range lạ ném lỗi, không gọi mạng\", async () =&gt; {\n  const spy = mockFetchOk();\n  await expect(fetchChart(1, \"5d\" as any)).rejects.toThrow(/allowlist/);\n  expect(spy).not.toHaveBeenCalled();\n  expect(RANGE_ALLOWLIST).toEqual([\"7d\", \"30d\", \"90d\", \"180d\", \"1y\"]);\n});</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>lib/chart/types.ts</code> (mirror DTO feed, allowlist range) -&gt; <code>lib/chart/fetch-chart.ts</code> (gọi feed qua apiFetch, validate range) -&gt; <code>verdict-badge.tsx</code> + <code>range-selector.tsx</code> + <code>maturity-notice.tsx</code> (mảnh hiển thị nhỏ, test riêng) -&gt; <code>price-chart.tsx</code> (vẽ thân daily + đường tham chiếu + mốc ngày đôi, ghép các mảnh) -&gt; <code>products/[id]/chart/page.tsx</code> (client component trong shell (app)) -&gt; tests. Chart là client component (<code>\"use client\"</code>) vì cần tương tác range; trang gọi <code>fetchChart</code> phía client sau khi shell (app) qua guard auth (FR-WEB-001). Dùng thư viện chart nhẹ (Recharts/visx); không tự viết engine vẽ.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>FR-DEAL-003</strong> - feed <code>GET /v1/products/{id}/chart</code> là nguồn dữ liệu DUY NHẤT (daily + annotations + maturity). Hình dạng JSON của nó là hợp đồng với UI này (depends_on cứng).</li><li><strong>FR-WEB-001</strong> - scaffold + <code>lib/api.ts</code> (gắn JWT) + shell <code>(app)</code> + guard auth; màn hình chart nằm trong khu vực bảo vệ (depends_on cứng).</li><li><strong>FR-DEAL-001 (qua feed)</strong> - nguồn verdict sale ảo; UI hiển thị <code>annotations.verdict</code>, không tính lại - đảm bảo nhãn khớp thẻ sản phẩm.</li><li><strong>FR-PRICE-002 (gián tiếp)</strong> - <code>price_daily</code> nuôi feed; UI không chạm trực tiếp.</li><li><strong>FR-WEB-002 (upstream funnel)</strong> - landing SEO dẫn CTA tới màn hình này; đây là đầu ra trải nghiệm của funnel.</li><li>Lib: <code>next</code> 14, thư viện chart (Recharts/visx), <code>@testing-library/react</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Feed nhận từ FR-DEAL-003 (rút gọn)</h3>\n<pre><code class=\"language-json\">{\n  \"product_id\": 90112, \"range\": \"90d\", \"maturity\": \"MATURE\",\n  \"daily\": [\n    { \"day\": \"2026-04-04\", \"min_p\": 119000, \"max_p\": 149000, \"close_p\": 119000 },\n    { \"day\": \"2026-05-05\", \"min_p\": 109000, \"max_p\": 139000, \"close_p\": 109000 },\n    { \"day\": \"2026-06-26\", \"min_p\": 99000,  \"max_p\": 119000, \"close_p\": 99000 }\n  ],\n  \"annotations\": {\n    \"median90\": 129000, \"trailing_min\": 99000, \"verdict\": \"TAM_DUOC\",\n    \"accumulating\": false, \"double_dates\": [\"2026-04-04\", \"2026-05-05\", \"2026-06-06\"]\n  }\n}</code></pre>\n<h3>Trạng thái SKU mới (NEW) - UI không kết luận</h3>\n<pre><code class=\"language-json\">{ \"product_id\": 5501, \"range\": \"90d\", \"maturity\": \"NEW\",\n  \"daily\": [ { \"day\": \"2026-06-24\", \"min_p\": 50000, \"max_p\": 60000, \"close_p\": 55000 } ],\n  \"annotations\": { \"median90\": 0, \"trailing_min\": 50000, \"verdict\": \"UNKNOWN\",\n                   \"accumulating\": false, \"double_dates\": [] } }</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>So sánh giá chéo 3 sàn trên cùng biểu đồ (overlay nhiều sàn) - chờ FR-PRICE-004; slice này một SKU một sàn.</li><li>Tô màu vùng theo verdict per đoạn (band per khoảng) khi feed trả dải verdict - bám DEC-DEAL của FR-DEAL-003.</li><li>Tương tác zoom/hover chi tiết theo điểm - thêm khi UX cần, không đổi nguồn dữ liệu.</li><li>Hiển thị giá theo currency khác khi mở SEA - bám tracked_product, giữ int64 minor unit.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Gọi raw price_snapshot tự dựng</td><td>grep + p95 metric</td><td>vỡ &lt;500ms, biểu đồ giật</td><td>Chỉ tiêu thụ feed FR-DEAL-003 (DEC-WEB-11)</td></tr><tr><td>Client tự tính verdict</td><td>grep hàm verdict</td><td>nhãn lệch thẻ sản phẩm</td><td>Lấy thẳng annotations.verdict (DEC-WEB-13)</td></tr><tr><td>Vẽ kết luận SKU NEW</td><td>verdict-badge test</td><td>kết luận ẩu ít dữ liệu</td><td>Ẩn badge khi NEW (DEC-WEB-14)</td></tr><tr><td>Gửi range ngoài allowlist</td><td>range-selector test</td><td>backend 400</td><td>Allowlist + validate fetchChart (DEC-WEB-15)</td></tr><tr><td>daily rỗng làm vỡ render</td><td>empty-state test</td><td>trang lỗi</td><td>\"đang thu thập dữ liệu giá\" lịch sự (§1 #9)</td></tr><tr><td>404 không xử lý</td><td>fetch test</td><td>trang treo</td><td>NotFoundError + thông báo rõ</td></tr><tr><td>Sai số tiền do float</td><td>review format</td><td>giá hiển thị lệch</td><td>Định dạng từ int64 VND (§1 #11)</td></tr><tr><td>Màn hình lộ khi chưa đăng nhập</td><td>guard (app)</td><td>rò rỉ</td><td>Nằm trong (app), JWT qua apiFetch (§1 #10)</td></tr><tr><td>Payload phình do không downsample</td><td>review feed</td><td>render chậm</td><td>Feed đã bucket ngày (DEC-WEB-12)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Đây là màn hình trả lời câu hỏi lõi \"sale thật hay ảo\" - minh chứng trực quan cho lời hứa sản phẩm, và là đầu ra của funnel SEO (FR-WEB-002).</li><li>Client chỉ vẽ; mọi tính toán (downsample, median90, trailing_min, verdict) nằm ở feed FR-DEAL-003 để giữ p95 &lt;500ms và một nguồn verdict.</li><li>Nhãn verdict đồng nhất giữa thẻ và biểu đồ là điều kiện niềm tin - lấy thẳng từ feed, không suy diễn ở client.</li><li>Tôn trọng maturity (NEW chưa kết luận, WARMING đang tích lũy) là phần của lời hứa không kết luận ẩu (§5.1).</li><li>Range selector và feed thống nhất một tập allowlist để tránh range lạ trả 400.</li><li>Giá giữ int64 VND suốt từ feed tới hiển thị, chỉ định dạng khi render - không có bước float nào.</li></ul>\n<hr>\n<p><em>Hết FR-WEB-003. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
