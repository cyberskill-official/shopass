window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-WEB-005"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>GraphQL BFF <strong>MUST</strong> là một service đọc đứng sau API Gateway (FR-INFRA-001), phục vụ endpoint <code>/graphql</code> cho web app truy vấn linh hoạt wishlist và biểu đồ, với resolver ủy quyền cho service REST, DataLoader chống N+1, và trần độ sâu/độ phức tạp. Hợp đồng:</p>\n<ol><li>BFF <strong>MUST</strong> đứng sau gateway: gateway (FR-INFRA-001) định tuyến <code>/graphql</code> tới BFF và đã verify JWT; BFF nhận thông tin user (sub/claims) qua context do gateway truyền (header forwarded), KHÔNG tự verify hay parse lại token (DEC-WEB-21).</li><li>Resolver <strong>MUST NOT</strong> truy cập DB trực tiếp: mọi dữ liệu lấy qua service REST hiện có - wishlist/alert từ track-svc (FR-TRACK-002/003), dữ liệu biểu đồ từ deal-svc (FR-DEAL-003) (DEC-WEB-22). Kiểm chủ sở hữu nằm ở service REST, BFF không nhân bản logic đó.</li><li>BFF <strong>MUST</strong> dùng DataLoader để gom và cache trong phạm vi một request (DEC-WEB-23): khi một query nạp biểu đồ cho nhiều <code>product_id</code> (vd mọi item trong wishlist), DataLoader gộp thành một lô thay vì N lời gọi REST tuần tự (chống N+1).</li><li>BFF <strong>MUST</strong> đặt trần độ sâu query (max depth) và trần độ phức tạp (cost/complexity) (DEC-WEB-24); query vượt trần bị từ chối với lỗi GraphQL rõ ràng trước khi thực thi resolver - chặn truy vấn lồng sâu ác ý làm cạn tài nguyên.</li><li>Ở slice này BFF <strong>MUST</strong> chỉ phục vụ <code>Query</code> (đọc); KHÔNG định nghĩa <code>Mutation</code> ghi wishlist/alert (DEC-WEB-25). Mọi thao tác ghi vẫn qua REST của FR-TRACK-002/003 để tránh hai đường ghi lệch nhau.</li><li>Schema <strong>MUST</strong> phơi tối thiểu: <code>me</code> (thông tin user hiện tại), <code>wishlists</code> (danh sách wishlist của caller kèm item), và <code>productChart(productId, range)</code> (dữ liệu biểu đồ từ feed FR-DEAL-003) - đủ để dashboard nạp wishlist kèm biểu đồ trong một round-trip.</li><li>Mọi resolver <strong>MUST</strong> yêu cầu user context hợp lệ (từ gateway); request thiếu context user (không qua gateway hoặc gateway không gắn claims) bị từ chối - resolver không trả dữ liệu cá nhân cho ngữ cảnh ẩn danh.</li><li>BFF <strong>MUST</strong> truyền <code>request-id</code> (do gateway gắn, FR-INFRA-001) xuống các lời gọi REST để giữ vết xuyên service (đồng nhất observability FR-INFRA-004).</li><li><code>productChart</code> <strong>MUST</strong> trả đúng hình dạng feed FR-DEAL-003 (daily + annotations + maturity) - BFF không tự tính verdict/median, chỉ chuyển tiếp + gộp; client hiển thị giống FR-WEB-003.</li><li>BFF <strong>MUST</strong> xử lý lỗi từ service REST một cách có cấu trúc: <code>403</code>/<code>404</code> từ REST ánh xạ thành lỗi GraphQL trung lập (không lộ tài nguyên user khác); lỗi mạng ánh xạ thành lỗi server rõ ràng, không nuốt im lặng.</li><li>Toàn bộ <strong>MUST</strong> vượt <code>npx tsc --noEmit</code> sạch và <code>npm test</code> xanh.</li><li>BFF <strong>SHOULD</strong> phát OTel <code>graphql_resolver_duration_ms{field}</code> và <code>graphql_rejected_total{reason}</code> (đếm query bị chặn vì depth/cost) để theo dõi truy vấn nặng/ác ý.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao BFF đứng sau gateway, không tự verify token (DEC-WEB-21)?</strong> Gateway (FR-INFRA-001) đã là điểm thực thi JWT duy nhất cho toàn hệ thống. BFF tự verify lại là nhân đôi logic - hai chỗ kiểm token dễ lệch (một chỗ cập nhật JWKS, chỗ kia quên). BFF tin gateway và nhận claims qua context. Đây là mẫu BFF chuẩn: edge xác thực, dịch vụ phía sau nhận danh tính đã xác thực.</p>\n<p><strong>Vì sao resolver ủy quyền REST, không chạm DB (DEC-WEB-22)?</strong> Logic kiểm chủ sở hữu (chống IDOR) nằm ở track-svc (FR-TRACK-002/003). Nếu BFF chạm DB trực tiếp thì phải sao chép logic đó - một chỗ quên kiểm là lỗ IDOR rò rỉ wishlist/alert người khác. Ủy quyền REST giữ một nguồn logic và một chỗ kiểm quyền; BFF chỉ là lớp định hình + gộp dữ liệu cho client.</p>\n<p><strong>Vì sao DataLoader chống N+1 (DEC-WEB-23)?</strong> GraphQL dễ rơi vào N+1: query \"wishlist kèm biểu đồ mỗi item\" ngây thơ bắn một lời gọi chart cho mỗi item. Với wishlist 20 item là 20 round-trip REST tuần tự - chậm. DataLoader gom các id trong một tick thành một lô, gọi một lần, cache trong request. Đây là khác biệt giữa BFF nhanh và BFF tệ hơn cả REST.</p>\n<p><strong>Vì sao trần độ sâu/độ phức tạp (DEC-WEB-24)?</strong> GraphQL cho client tự đặt hình dạng query - bao gồm query lồng sâu ác ý làm cạn tài nguyên (lớp tấn công kinh điển của GraphQL). Trần max depth + cost từ chối query vượt ngưỡng trước khi chạy resolver. Không có trần là mở cửa DoS.</p>\n<p><strong>Vì sao chỉ đọc, ghi vẫn qua REST (DEC-WEB-25)?</strong> Hai đường ghi (REST + GraphQL mutation) dễ lệch validate (một đường cập nhật luật, đường kia quên) và lệch trạng thái. Slice này BFF chỉ tối ưu đọc (nơi GraphQL tỏa sáng: gộp nhiều nguồn trong một round-trip); ghi giữ một đường REST của FR-TRACK-002/003 để một nguồn validate.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Schema (schema.graphql) - chỉ Query</h3>\n<pre><code class=\"language-graphql\"># services/bff/src/schema.graphql\ntype Query {\n  me: User!\n  wishlists: [Wishlist!]!                       # của caller; track-svc kiểm chủ sở hữu\n  productChart(productId: ID!, range: String = \"90d\"): ChartData!\n}\n\ntype User { id: ID!; displayName: String }\n\ntype Wishlist {\n  id: ID!; name: String!\n  items: [WishlistItem!]!\n}\ntype WishlistItem {\n  productId: ID!\n  targetPrice: Int                              # VND int64, nullable\n  chart(range: String = \"90d\"): ChartData       # nạp qua DataLoader (chống N+1)\n}\n\ntype ChartData {                                # mirror feed FR-DEAL-003\n  maturity: String!\n  daily: [DailyPoint!]!\n  annotations: Annotations!\n}\ntype DailyPoint { day: String!; minP: Int!; maxP: Int!; closeP: Int! }\ntype Annotations { median90: Int!; trailingMin: Int!; verdict: String!; accumulating: Boolean!; doubleDates: [String!]! }\n\n# KHÔNG có type Mutation ở slice này (DEC-WEB-25)</code></pre>\n<h3>Context (context.ts) - nhận claims từ gateway, không verify</h3>\n<pre><code class=\"language-ts\">// services/bff/src/context.ts\nexport interface GqlContext {\n  userId: string | null;   // sub do gateway gắn (DEC-WEB-21); BFF KHÔNG verify token\n  requestId: string;       // do gateway gắn (FR-INFRA-001)\n  rest: RestClient;\n}\n\nexport function buildContext(req: IncomingRequest): GqlContext {\n  return {\n    userId: req.headers[\"x-user-id\"] ?? null,        // gateway forward sub (đã verify)\n    requestId: req.headers[\"x-request-id\"] ?? crypto.randomUUID(),\n    rest: new RestClient(req.headers[\"x-user-id\"], req.headers[\"x-request-id\"]),\n  };\n}</code></pre>\n<h3>DataLoader chart (loaders/chart-loader.ts) - chống N+1</h3>\n<pre><code class=\"language-ts\">// services/bff/src/loaders/chart-loader.ts\nimport DataLoader from \"dataloader\";\n\n// Gom nhiều (productId, range) trong một request thành các lời gọi feed FR-DEAL-003,\n// cache theo khóa trong phạm vi request (DEC-WEB-23).\nexport function makeChartLoader(rest: RestClient) {\n  return new DataLoader&lt;{ productId: string; range: string }, ChartData&gt;(\n    async (keys) =&gt; {\n      // gọi REST cho từng key NHƯNG được gom trong một tick; cache tránh trùng\n      return Promise.all(keys.map((k) =&gt; rest.getChart(k.productId, k.range))); // feed FR-DEAL-003\n    },\n    { cacheKeyFn: (k) =&gt; `${k.productId}:${k.range}` }\n  );\n}</code></pre>\n<h3>Resolver wishlist (ủy quyền REST, không chạm DB)</h3>\n<pre><code class=\"language-ts\">// services/bff/src/resolvers/wishlist.ts\nexport const wishlistResolvers = {\n  Query: {\n    wishlists: (_: unknown, __: unknown, ctx: GqlContext) =&gt; {\n      if (!ctx.userId) throw new GraphQLError(\"UNAUTHENTICATED\"); // §1 #7\n      return ctx.rest.listWishlists();                            // track-svc kiểm chủ sở hữu (DEC-WEB-22)\n    },\n  },\n  WishlistItem: {\n    chart: (item: { productId: string }, args: { range: string }, ctx: GqlContext) =&gt;\n      ctx.loaders.chart.load({ productId: item.productId, range: args.range ?? \"90d\" }), // DataLoader\n  },\n};</code></pre>\n<h3>Trần độ sâu/độ phức tạp (security/limits.ts)</h3>\n<pre><code class=\"language-ts\">// services/bff/src/security/limits.ts\nexport const MAX_DEPTH = 8;          // chặn query lồng quá sâu (DEC-WEB-24)\nexport const MAX_COST = 1000;        // trần độ phức tạp\n\n// validate trước khi thực thi; vượt trần -&gt; GraphQLError, đếm graphql_rejected_total\nexport function enforceLimits(document: DocumentNode): void {\n  const depth = queryDepth(document);\n  if (depth &gt; MAX_DEPTH) throw new GraphQLError(`query quá sâu (&gt;${MAX_DEPTH})`);\n  const cost = queryCost(document);\n  if (cost &gt; MAX_COST) throw new GraphQLError(`query quá phức tạp (&gt;${MAX_COST})`);\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>BFF nhận user qua context do gateway gắn (header forwarded); grep <code>services/bff/**</code>: KHÔNG có verify/parse JWT (DEC-WEB-21).</li><li>Resolver lấy dữ liệu qua <code>RestClient</code> (track-svc/deal-svc); grep: KHÔNG có truy vấn DB/SQL trực tiếp trong BFF (DEC-WEB-22).</li><li>Query nạp biểu đồ cho N product trong một wishlist gọi feed qua DataLoader gộp một lô - KHÔNG N lời gọi tuần tự rời rạc (test đếm batch).</li><li>Query vượt <code>MAX_DEPTH</code> hoặc <code>MAX_COST</code> bị từ chối với <code>GraphQLError</code> trước khi chạy resolver (DEC-WEB-24).</li><li>Schema chỉ có <code>Query</code>, KHÔNG có <code>Mutation</code> ghi wishlist/alert (DEC-WEB-25).</li><li>Schema phơi <code>me</code>, <code>wishlists</code> (kèm items), <code>productChart(productId, range)</code>; <code>ChartData</code> mirror hình dạng feed FR-DEAL-003.</li><li>Resolver thiếu <code>userId</code> context (ngữ cảnh ẩn danh) ném <code>UNAUTHENTICATED</code>; không trả dữ liệu cá nhân.</li><li>BFF truyền <code>request-id</code> (header) xuống lời gọi REST.</li><li><code>productChart</code>/<code>chart</code> trả <code>verdict</code>/<code>median90</code> từ feed (không tự tính trong BFF).</li><li>Lỗi <code>403</code>/<code>404</code> từ REST ánh xạ thành lỗi GraphQL trung lập; lỗi mạng thành lỗi server, không nuốt im lặng.</li><li><code>npx tsc --noEmit</code> sạch; <code>npm test</code> xanh.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-ts\">// services/bff/test/resolver-auth.test.ts\nimport { wishlistResolvers } from \"../src/resolvers/wishlist\";\n\ntest(\"resolver thiếu userId → UNAUTHENTICATED, không gọi REST\", () =&gt; {\n  const ctx = { userId: null, rest: { listWishlists: jest.fn() } } as any;\n  expect(() =&gt; wishlistResolvers.Query.wishlists(null, null, ctx)).toThrow(/UNAUTHENTICATED/);\n  expect(ctx.rest.listWishlists).not.toHaveBeenCalled();\n});\n\ntest(\"BFF không verify token (nhận sub từ context gateway)\", async () =&gt; {\n  const src = await readFile(\"src/context.ts\", \"utf8\");\n  expect(src).not.toMatch(/jwt\\.verify|verifyToken|jwks/i); // DEC-WEB-21\n});</code></pre>\n<pre><code class=\"language-ts\">// services/bff/test/dataloader-n1.test.ts\nimport { makeChartLoader } from \"../src/loaders/chart-loader\";\n\ntest(\"nạp chart cho 5 product trong một request gộp một batch (chống N+1)\", async () =&gt; {\n  let batchCalls = 0;\n  const rest = { getChart: jest.fn(async () =&gt; { return {} as any; }) };\n  const spyBatch = jest.fn(() =&gt; { batchCalls++; });\n  const loader = makeChartLoader(rest as any, spyBatch);\n  await Promise.all([1, 2, 3, 4, 5].map((id) =&gt; loader.load({ productId: String(id), range: \"90d\" })));\n  expect(batchCalls).toBe(1);                 // một lô, KHÔNG 5 round-trip rời rạc\n  expect(rest.getChart).toHaveBeenCalledTimes(5); // 5 key nhưng trong một batch\n});</code></pre>\n<pre><code class=\"language-ts\">// services/bff/test/query-depth.test.ts\nimport { enforceLimits, MAX_DEPTH } from \"../src/security/limits\";\nimport { parse } from \"graphql\";\n\ntest(\"query quá sâu bị chặn trước khi chạy resolver\", () =&gt; {\n  const deep = parse(`{ wishlists { items { chart { annotations { doubleDates } } } } }`.repeat(1));\n  // dựng query vượt MAX_DEPTH\n  const tooDeep = parse(buildNestedQuery(MAX_DEPTH + 2));\n  expect(() =&gt; enforceLimits(tooDeep)).toThrow(/quá sâu/);\n});\n\ntest(\"query trong trần được chấp nhận\", () =&gt; {\n  const ok = parse(`{ wishlists { id name } }`);\n  expect(() =&gt; enforceLimits(ok)).not.toThrow();\n});</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>schema.graphql</code> (chỉ Query: me/wishlists/productChart) -&gt; <code>context.ts</code> (nhận sub + request-id từ gateway, không verify) -&gt; <code>rest-client.ts</code> (gọi track-svc + deal-svc, truyền request-id) -&gt; <code>resolvers/wishlist.ts</code> + <code>resolvers/chart.ts</code> (ủy quyền REST) -&gt; <code>loaders/chart-loader.ts</code> (DataLoader gộp) -&gt; <code>security/limits.ts</code> (max depth + cost) -&gt; <code>server.ts</code> (wire tất cả, đặt sau gateway) -&gt; tests. BFF có thể chạy như route handler trong web app Next (<code>/api/graphql</code>) hoặc một service Node riêng - dù cách nào cũng nằm SAU gateway (FR-INFRA-001 định tuyến <code>/graphql</code>). Vì là SHOULD, có thể hoãn tới sau khi REST path (FR-WEB-001 lib/api.ts) đã phục vụ MVP; khi làm phải giữ đúng bốn ranh giới (không verify token, không chạm DB, có trần query, không mutation ghi).</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>FR-INFRA-001</strong> - gateway định tuyến <code>/graphql</code> tới BFF và verify JWT trước; BFF nhận claims qua context, không verify lại (depends_on cứng).</li><li><strong>FR-WEB-001</strong> - web app tiêu thụ BFF; <code>lib/api.ts</code> mở rộng để gọi GraphQL endpoint cạnh REST (depends_on cứng).</li><li><strong>FR-TRACK-002 / FR-TRACK-003</strong> - nguồn wishlist/alert; resolver ủy quyền REST của chúng (giữ kiểm chủ sở hữu một chỗ).</li><li><strong>FR-DEAL-003</strong> - feed biểu đồ; <code>productChart</code>/<code>chart</code> resolver gọi feed này qua DataLoader; <code>ChartData</code> mirror hình dạng của nó.</li><li><strong>FR-WEB-003 / FR-WEB-004 (đồng cấp)</strong> - cùng tiêu thụ dữ liệu wishlist + biểu đồ; BFF là đường thay thế REST cho dashboard cần gộp nhiều nguồn.</li><li>Lib: <code>graphql</code>, một runtime (GraphQL Yoga/Apollo Server), <code>dataloader</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Query dashboard: wishlist kèm biểu đồ mỗi item (một round-trip)</h3>\n<pre><code class=\"language-graphql\">query Dashboard {\n  wishlists {\n    id\n    name\n    items {\n      productId\n      targetPrice\n      chart(range: \"90d\") {            # nạp qua DataLoader, gộp một batch\n        maturity\n        annotations { median90 trailingMin verdict doubleDates }\n      }\n    }\n  }\n}</code></pre>\n<h3>Lỗi query vượt trần độ sâu</h3>\n<pre><code class=\"language-json\">{ \"errors\": [ { \"message\": \"query quá sâu (&gt;8)\" } ] }</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Persisted queries / allowlist query phía server để siết chặt hơn nữa truy vấn cho phép - cân nhắc khi mở public API.</li><li>Mutation qua GraphQL (nếu sau này hợp nhất đường ghi) - chốt mô hình một-đường-ghi trước khi cân nhắc.</li><li>Subscription (WSS) cho cập nhật giá realtime trên dashboard - bám đường WSS của gateway (FR-INFRA-001) khi cần.</li><li>Field-level cost cụ thể theo độ nặng từng resolver - tinh chỉnh trần cost sau khi đo tải thật.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>BFF tự verify token</td><td>grep jwt.verify/jwks</td><td>nhân đôi sai lệch auth</td><td>Nhận claims từ gateway (DEC-WEB-21)</td></tr><tr><td>Resolver chạm DB trực tiếp</td><td>grep SQL/DB trong bff</td><td>bỏ kiểm chủ sở hữu -&gt; IDOR</td><td>Ủy quyền REST (DEC-WEB-22)</td></tr><tr><td>N+1 nạp chart mỗi item</td><td>dataloader-n1 test</td><td>chậm hơn cả REST</td><td>DataLoader gộp một batch (DEC-WEB-23)</td></tr><tr><td>Query lồng sâu ác ý</td><td>query-depth test</td><td>cạn tài nguyên (DoS)</td><td>Trần max depth + cost (DEC-WEB-24)</td></tr><tr><td>Thêm mutation ghi ở BFF</td><td>review schema</td><td>hai đường ghi lệch</td><td>Chỉ Query; ghi qua REST (DEC-WEB-25)</td></tr><tr><td>Resolver trả dữ liệu cho ẩn danh</td><td>resolver-auth test</td><td>rò rỉ dữ liệu cá nhân</td><td>Yêu cầu userId context (§1 #7)</td></tr><tr><td>403/404 REST lộ tài nguyên khác</td><td>review ánh xạ lỗi</td><td>rò rỉ quyền riêng tư</td><td>Lỗi GraphQL trung lập (§1 #10)</td></tr><tr><td>BFF tự tính verdict/median</td><td>grep tính toán</td><td>lệch feed/thẻ</td><td>Chuyển tiếp feed FR-DEAL-003 (§1 #9)</td></tr><tr><td>Mất request-id xuyên service</td><td>trace gãy</td><td>khó điều tra</td><td>Truyền x-request-id xuống REST (§1 #8)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>BFF GraphQL là tiện ích hiệu năng (SHOULD): nạp wishlist kèm biểu đồ mỗi item trong một round-trip thay vì nhiều lời gọi REST tuần tự.</li><li>Bốn ranh giới phải giữ khi làm: không verify token (gateway đã làm), không chạm DB (ủy quyền REST giữ kiểm chủ sở hữu một chỗ), có trần query (chống DoS), chỉ đọc (một đường ghi REST).</li><li>Resolver ủy quyền REST giữ một nguồn logic + một chỗ kiểm IDOR - BFF chỉ định hình + gộp dữ liệu.</li><li>DataLoader là khác biệt giữa BFF nhanh và BFF tệ hơn REST; gộp N id thành một lô trong một request.</li><li>Trần độ sâu/độ phức tạp chặn lớp tấn công DoS kinh điển của GraphQL.</li><li>Vì SHOULD, có thể hoãn sau MVP (REST path đã đủ); khi làm phải đúng các ranh giới trên để không tạo lỗ hổng mới.</li></ul>\n<hr>\n<p><em>Hết FR-WEB-005. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
