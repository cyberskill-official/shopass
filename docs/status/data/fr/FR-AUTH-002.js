window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AUTH-002"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service AUTH <strong>MUST</strong> phát hành cặp access/refresh token, expose JWKS để gateway verify, và quản refresh có rotation/thu hồi. Hợp đồng:</p>\n<ol><li>Đăng nhập thành công (mật khẩu verify qua FR-AUTH-001) <strong>MUST</strong> trả một <code>TokenPair</code>: access JWT ngắn hạn + refresh token dài hạn (DEC-AUTH-06).</li><li>Access token <strong>MUST</strong> là JWT ký RS256 (DEC-AUTH-07). Khóa ký private lấy qua FR-INFRA-003 (<code>auth/jwt-signing</code>), KHÔNG nhúng trong code/env, KHÔNG chia sẻ với service khác.</li><li>Access JWT <strong>MUST</strong> mang claims: <code>user_id</code>, <code>locale</code>, <code>tier</code>, cùng claim chuẩn <code>exp</code>, <code>iat</code>, <code>iss</code>, <code>aud</code> (DEC-AUTH-08). <code>aud</code> đặt đúng để gateway (FR-INFRA-001 #2) verify khớp.</li><li>Access token <strong>MUST</strong> có TTL ngắn (ví dụ 15 phút, cấu hình được) (DEC-AUTH-06) để giới hạn cửa sổ rủi ro khi token lộ.</li><li>AUTH <strong>MUST</strong> expose JWKS tại <code>/.well-known/jwks.json</code> chứa public key tương ứng (DEC-AUTH-10). Mỗi khóa có <code>kid</code>; JWKS hỗ trợ nhiều <code>kid</code> đồng thời để xoay khóa ký không downtime.</li><li>Refresh token <strong>MUST</strong> dài hạn hơn (ví dụ 30 ngày) và <strong>MUST</strong> lưu dạng hash trong DB (DEC-AUTH-09), KHÔNG cleartext. Bảng <code>refresh_token</code> lưu <code>user_id</code>, <code>token_hash</code>, <code>expires_at</code>, <code>revoked_at</code>.</li><li>Refresh <strong>MUST</strong> rotation một-lần (DEC-AUTH-09): mỗi refresh token dùng được đúng một lần; khi đổi lấy cặp mới, token cũ bị đánh dấu đã dùng/thu hồi. Dùng lại token đã xoay -&gt; từ chối.</li><li>AUTH <strong>MUST</strong> hỗ trợ thu hồi: token bị <code>revoked_at</code> không cấp được cặp mới (dùng cho đăng xuất và FR-AUTH-005 suspend/delete).</li><li>AUTH <strong>MUST</strong> cung cấp <code>IssueTokenPair(ctx, userID) (TokenPair, error)</code> và <code>Refresh(ctx, refreshToken) (TokenPair, error)</code>; gateway chỉ verify access JWT (không gọi AUTH mỗi request).</li><li>Phát hiện tái sử dụng refresh token đã xoay <strong>SHOULD</strong> kích hoạt thu hồi cả họ token của phiên đó (token theft detection) - coi như dấu hiệu đánh cắp.</li><li>Claims <code>tier</code> <strong>MUST</strong> phản ánh gói người dùng (<code>free</code>/<code>premium</code>...) để gateway/route áp feature gating mà không truy DB mỗi request.</li><li>AUTH <strong>MUST</strong> ký bằng <code>kid</code> hiện hành và đưa <code>kid</code> vào header JWT, để gateway chọn đúng public key trong JWKS khi xoay khóa.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p>Vì sao RS256 + JWKS, không HS256 (DEC-AUTH-07)? HS256 dùng một secret đối xứng: ai verify được cũng ký được. Nếu gateway giữ secret đó để verify, một lần lộ là giả mạo mọi phiên. RS256 tách private (chỉ AUTH ký) khỏi public (gateway verify qua JWKS). Gateway không bao giờ chạm khóa ký, giữ ranh giới phát-hành vs verify (FR-INFRA-001) sạch.</p>\n<p>Vì sao access ngắn + refresh dài (DEC-AUTH-06)? Access token tự chứng thực (gateway verify không cần gọi AUTH), nên không thu hồi tức thời được - cách giới hạn rủi ro là cho nó sống ngắn. Refresh token sống dài để người dùng không phải đăng nhập lại liên tục, nhưng nó đi qua AUTH mỗi lần dùng nên thu hồi được. Cặp này cân bằng tiện lợi và kiểm soát.</p>\n<p>Vì sao lưu hash refresh, không cleartext (DEC-AUTH-09)? Refresh token là credential dài hạn. Nếu DB lộ và token lưu cleartext, kẻ tấn công chiếm phiên dài hạn hàng loạt. Lưu hash (như mật khẩu) làm DB lộ không trực tiếp cho token dùng được.</p>\n<p>Vì sao rotation một-lần (DEC-AUTH-09, §1 #7)? Refresh token tĩnh dùng mãi là rủi ro: bị đánh cắp một lần là dùng vô hạn. Rotation một-lần (mỗi refresh cấp một refresh mới và vô hiệu cái cũ) thu hẹp cửa sổ: token đánh cắp chỉ dùng được tới lần refresh hợp lệ kế tiếp của chủ thật.</p>\n<p>Vì sao phát hiện tái sử dụng kích hoạt thu hồi họ (§1 #10)? Nếu một refresh token đã xoay bị dùng lại, hoặc chủ thật vừa dùng (và kẻ cắp đến sau) hoặc ngược lại - dù sao cũng là dấu hiệu hai bên cùng giữ token. Thu hồi cả họ token buộc đăng nhập lại, cắt kẻ cắp ra.</p>\n<p>Vì sao nhiều kid trong JWKS (DEC-AUTH-10)? Xoay khóa ký định kỳ là thực hành tốt. Nếu JWKS chỉ có một khóa, lúc xoay sẽ có khoảng token cũ (ký khóa cũ) và token mới (ký khóa mới) cùng tồn tại. Giữ nhiều <code>kid</code> cho cả hai verify được trong giai đoạn chuyển, xoay không downtime - khớp với FR-INFRA-001 #12.</p>\n<p>Vì sao tier trong claims (§1 #11)? Feature gating (free vs premium) cần biết gói ở mỗi request. Nếu phải truy DB mỗi lần thì chậm và tốn. Đưa <code>tier</code> vào claims cho gateway/route quyết định ngay từ token; access TTL ngắn giữ tier không quá cũ khi người dùng nâng cấp.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration refresh_token</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0005_refresh_token.up.sql\nCREATE TABLE refresh_token (\n  id          BIGSERIAL   PRIMARY KEY,\n  user_id     BIGINT      NOT NULL REFERENCES app_user(id),\n  token_hash  TEXT        NOT NULL,            -- hash của refresh token, KHÔNG cleartext\n  family_id   UUID        NOT NULL,            -- nhóm token cùng một chuỗi rotation\n  expires_at  TIMESTAMPTZ NOT NULL,\n  revoked_at  TIMESTAMPTZ,                     -- NULL = còn hiệu lực\n  used_at     TIMESTAMPTZ,                     -- đánh dấu đã xoay (dùng một lần)\n  created_at  TIMESTAMPTZ DEFAULT now()\n);\nCREATE INDEX idx_rt_user ON refresh_token (user_id);\nCREATE UNIQUE INDEX idx_rt_hash ON refresh_token (token_hash);</code></pre>\n<h3>Claims + phát hành access JWT (§1 #2, #3, #12)</h3>\n<pre><code class=\"language-go\">// services/auth/internal/auth/token.go\ntype Claims struct {\n    UserID int64  `json:\"user_id\"`\n    Locale string `json:\"locale\"`\n    Tier   string `json:\"tier\"`\n    jwt.RegisteredClaims\n}\n\nfunc (s *Service) issueAccess(u AppUser) (string, error) {\n    now := time.Now()\n    c := Claims{\n        UserID: u.ID, Locale: u.Locale, Tier: u.Tier,\n        RegisteredClaims: jwt.RegisteredClaims{\n            Issuer: s.iss, Audience: jwt.ClaimStrings{s.aud},\n            IssuedAt: jwt.NewNumericDate(now),\n            ExpiresAt: jwt.NewNumericDate(now.Add(s.accessTTL)), // §1 #4 (ngắn)\n        },\n    }\n    tok := jwt.NewWithClaims(jwt.SigningMethodRS256, c)\n    tok.Header[\"kid\"] = s.currentKID // §1 #12\n    return tok.SignedString(s.signingKey) // signingKey lấy qua FR-INFRA-003\n}</code></pre>\n<h3>Refresh rotation một-lần + theft detection (§1 #7, #8, #10)</h3>\n<pre><code class=\"language-go\">// services/auth/internal/auth/refresh.go\nfunc (s *Service) Refresh(ctx context.Context, raw string) (TokenPair, error) {\n    rt, err := s.repo.FindRefreshByHash(ctx, hash(raw))\n    if err != nil { return TokenPair{}, ErrInvalidRefresh }\n    if rt.RevokedAt != nil || time.Now().After(rt.ExpiresAt) {\n        return TokenPair{}, ErrInvalidRefresh\n    }\n    if rt.UsedAt != nil { // đã xoay rồi mà dùng lại = nghi đánh cắp (§1 #10)\n        s.repo.RevokeFamily(ctx, rt.FamilyID)\n        return TokenPair{}, ErrRefreshReuseDetected\n    }\n    s.repo.MarkUsed(ctx, rt.ID)                 // dùng một lần (§1 #7)\n    return s.issuePairInFamily(ctx, rt.UserID, rt.FamilyID) // cấp cặp mới cùng family\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Đăng nhập đúng mật khẩu -&gt; trả <code>TokenPair</code> (access + refresh khác rỗng).</li><li>Access JWT verify được bằng public key từ <code>/.well-known/jwks.json</code> (chữ ký RS256 hợp lệ).</li><li>Access JWT chứa <code>user_id</code>, <code>locale</code>, <code>tier</code>, <code>exp</code>, <code>iss</code>, <code>aud</code> đúng giá trị.</li><li>Access JWT hết hạn (sau TTL) -&gt; verify thất bại.</li><li>Access JWT ký bằng <code>kid</code> không có trong JWKS -&gt; verify thất bại.</li><li>JWKS trả &gt;=1 khóa; sau khi thêm khóa thứ hai (xoay), JWKS có 2 <code>kid</code>, token ký khóa mới verify được.</li><li>Refresh token lưu trong DB là hash, KHÔNG khớp giá trị thô (kiểm <code>token_hash</code> != raw).</li><li><code>Refresh(validToken)</code> -&gt; trả cặp mới; token cũ bị <code>used_at</code> set.</li><li><code>Refresh</code> lại cùng token đã dùng -&gt; <code>ErrRefreshReuseDetected</code>, và cả family bị thu hồi.</li><li><code>Refresh(revokedToken)</code> -&gt; <code>ErrInvalidRefresh</code>.</li><li><code>Refresh(expiredToken)</code> -&gt; <code>ErrInvalidRefresh</code>.</li><li>Sau thu hồi family (theft detection), refresh token khác cùng family cũng bị từ chối.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/auth/internal/auth/token_test.go\nfunc TestAccess_VerifiableViaJWKS(t *testing.T) {\n    s := newServiceWithKeys(t)\n    pair, _ := s.IssueTokenPair(ctx, 90112)\n    jwks := s.JWKS()\n    claims, err := verifyWithJWKS(pair.Access, jwks)\n    require.NoError(t, err)\n    require.Equal(t, int64(90112), claims.UserID)\n}\n\nfunc TestAccess_Expired_Rejected(t *testing.T) {\n    s := newServiceWithKeys(t); s.accessTTL = -time.Minute // đã hết hạn\n    pair, _ := s.IssueTokenPair(ctx, 1)\n    _, err := verifyWithJWKS(pair.Access, s.JWKS())\n    require.Error(t, err)\n}\n\nfunc TestAccess_UnknownKID_Rejected(t *testing.T) {\n    s := newServiceWithKeys(t)\n    pair, _ := s.IssueTokenPair(ctx, 1)\n    _, err := verifyWithJWKS(pair.Access, emptyJWKS()) // không có kid\n    require.Error(t, err)\n}\n\nfunc TestJWKS_MultipleKID_AfterRotation(t *testing.T) {\n    s := newServiceWithKeys(t)\n    s.AddSigningKey(genKey(t), \"key-2\") // xoay\n    require.Len(t, s.JWKS().Keys, 2)\n    pair, _ := s.IssueTokenPair(ctx, 1) // ký bằng kid hiện hành\n    _, err := verifyWithJWKS(pair.Access, s.JWKS())\n    require.NoError(t, err)\n}\n\n// services/auth/internal/auth/refresh_test.go\nfunc TestRefresh_OneTimeUse(t *testing.T) {\n    s := newServiceWithKeys(t)\n    pair, _ := s.IssueTokenPair(ctx, 1)\n    p2, err := s.Refresh(ctx, pair.Refresh)\n    require.NoError(t, err)\n    require.NotEqual(t, pair.Refresh, p2.Refresh) // rotation cấp mới\n    _, err2 := s.Refresh(ctx, pair.Refresh)       // dùng lại token cũ\n    require.ErrorIs(t, err2, ErrRefreshReuseDetected)\n}\n\nfunc TestRefresh_StoredAsHash(t *testing.T) {\n    s := newServiceWithKeys(t)\n    pair, _ := s.IssueTokenPair(ctx, 1)\n    row := s.repo.rawRow(t, 1)\n    require.NotEqual(t, pair.Refresh, row.TokenHash) // lưu hash, không cleartext\n}\n\nfunc TestRefresh_ReuseRevokesFamily(t *testing.T) {\n    s := newServiceWithKeys(t)\n    pair, _ := s.IssueTokenPair(ctx, 1)\n    p2, _ := s.Refresh(ctx, pair.Refresh)\n    _, _ = s.Refresh(ctx, pair.Refresh)     // tái sử dụng → thu hồi family\n    _, err := s.Refresh(ctx, p2.Refresh)     // token mới cùng family cũng bị chặn\n    require.ErrorIs(t, err, ErrInvalidRefresh)\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. <code>token.go</code> ký RS256 với private key lấy qua FR-INFRA-003; header JWT mang <code>kid</code> hiện hành. <code>jwks.go</code> build JWKS từ public key(s); giữ nhiều <code>kid</code> để xoay không downtime, khớp gateway refresh JWKS (FR-INFRA-001 #12). <code>refresh.go</code> lưu hash refresh + <code>family_id</code> cho rotation và theft detection. <code>login.go</code> nối: verify mật khẩu (FR-AUTH-001) -&gt; <code>IssueTokenPair</code>. Gateway chỉ verify access JWT bằng JWKS, không gọi AUTH mỗi request - đó là lý do access TTL phải ngắn.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li>FR-AUTH-001 - <code>Verify</code> mật khẩu trước khi cấp token; <code>app_user</code> cung cấp <code>locale</code>/<code>tier</code>.</li><li>FR-INFRA-001 - gateway verify access JWT bằng JWKS này; <code>aud</code> phải khớp.</li><li>FR-INFRA-003 - khóa ký RS256 lấy qua provider secret (<code>auth/jwt-signing</code>).</li><li>FR-AUTH-005 (downstream) - suspend/delete tài khoản dùng thu hồi refresh token.</li><li>FR-WEB-001 / FR-EXT-005 / FR-MOBILE-001 (downstream) - client dùng cặp token để gọi API.</li><li>Thư viện: golang-jwt + JWKS.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Phản hồi đăng nhập</h3>\n<pre><code class=\"language-json\">{\n  \"access_token\": \"eyJhbGciOiJSUzI1NiIsImtpZCI6ImtleS0yMDI2LTA2In0...\",\n  \"refresh_token\": \"rt_9f2c...e004\",\n  \"token_type\": \"Bearer\",\n  \"expires_in\": 900\n}</code></pre>\n<h3>JWKS</h3>\n<pre><code class=\"language-json\">{\n  \"keys\": [\n    {\"kty\":\"RSA\",\"kid\":\"key-2026-06\",\"use\":\"sig\",\"alg\":\"RS256\",\"n\":\"...\",\"e\":\"AQAB\"},\n    {\"kty\":\"RSA\",\"kid\":\"key-2026-09\",\"use\":\"sig\",\"alg\":\"RS256\",\"n\":\"...\",\"e\":\"AQAB\"}\n  ]\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Danh sách thu hồi access token (introspection/blacklist) cho thu hồi tức thời trước khi access hết hạn - chỉ cần nếu TTL ngắn vẫn chưa đủ.</li><li>Binding token với thiết bị (DPoP/mTLS) chống đánh cắp - tăng phòng thủ giai đoạn sau.</li><li>Đăng nhập bằng số điện thoại + OTP (ngoài email + mật khẩu) - gắn FR-NOTIF (SMS) khi mở.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>HS256 secret chia sẻ</td><td>review</td><td>lộ một service = giả mạo mọi phiên</td><td>RS256 + JWKS (§1 #2)</td></tr><tr><td>Access TTL dài</td><td>review</td><td>token lộ dùng tới khi hết hạn</td><td>TTL ngắn (§1 #4)</td></tr><tr><td>Refresh lưu cleartext</td><td>refresh test</td><td>DB lộ = chiếm phiên hàng loạt</td><td>Lưu hash (§1 #6)</td></tr><tr><td>Refresh dùng lại mãi</td><td>one-time test</td><td>token cắp dùng vô hạn</td><td>Rotation một-lần (§1 #7)</td></tr><tr><td>Đánh cắp refresh token</td><td>reuse test</td><td>chiếm phiên</td><td>Theft detection thu hồi family (§1 #10)</td></tr><tr><td>Xoay khóa gây downtime verify</td><td>JWKS test</td><td>token mới bị từ chối</td><td>Nhiều kid trong JWKS (§1 #5)</td></tr><tr><td>Gateway gọi AUTH mỗi request</td><td>thiết kế</td><td>chậm + AUTH thành nút cổ chai</td><td>Access tự chứng thực, TTL ngắn (§6)</td></tr><tr><td>tier cũ sau khi nâng cấp</td><td>TTL access</td><td>gating sai tạm thời</td><td>TTL ngắn làm tier mới hiệu lực nhanh</td></tr><tr><td>aud sai gateway từ chối</td><td>token test</td><td>đăng nhập được nhưng API 401</td><td><code>aud</code> khớp gateway (§1 #3)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>RS256 + JWKS tách phát-hành (AUTH ký bằng private) khỏi verify (gateway dùng public), giữ ranh giới FR-INFRA-001 sạch và không khóa ký nào rời AUTH.</li><li>Access ngắn + refresh dài cân bằng: access tự chứng thực nên phải ngắn; refresh đi qua AUTH nên thu hồi được.</li><li>Lưu hash refresh + rotation một-lần + theft detection là ba lớp bảo vệ credential dài hạn.</li><li>Nhiều <code>kid</code> trong JWKS cho xoay khóa ký không downtime, khớp gateway refresh JWKS khi gặp <code>kid</code> lạ.</li><li><code>tier</code> trong claims cho feature gating không truy DB mỗi request; access TTL ngắn giữ tier không quá cũ.</li><li>Gateway verify cục bộ bằng JWKS, không gọi AUTH mỗi request - đó là lý do access TTL ngắn là bắt buộc, không phải tùy chọn.</li></ul>\n<hr>\n<p><em>Hết FR-AUTH-002. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
