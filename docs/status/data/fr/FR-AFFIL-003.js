window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AFFIL-003"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service AFFIL <strong>MUST</strong> tích hợp affiliate network (Involve Asia, Accesstrade): lưu cấu hình network (template build link + tham chiếu secret), cung cấp template cho deep link (FR-AFFIL-002), và nhận webhook postback để ghi/đối soát conversion theo last-click, có xác thực chữ ký và idempotent. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> định nghĩa bảng <code>affiliate_network (id, code, base_url, target_param, sub_id_param, platform_id, postback_secret_ref, active)</code>: <code>code</code> - {<code>'involve_asia'</code>,<code>'accesstrade'</code>} (CHECK), <code>platform_id</code> REFERENCES <code>platform(id)</code> (network nào phục vụ sàn nào).</li><li><code>postback_secret_ref</code> <strong>MUST</strong> là một tham chiếu tới secret trong Vault (FR-INFRA-003), KHÔNG phải secret cleartext (DEC-AFFIL-11). Cột này chứa key path/handle; secret thật đọc từ Vault lúc verify.</li><li><strong>MUST</strong> expose <code>TemplateFor(platformID) (network string, tmpl NetworkTemplate, ok bool)</code> (dùng bởi FR-AFFIL-002): trả <code>base_url</code>, <code>target_param</code>, <code>sub_id_param</code> của network <code>active</code> phục vụ sàn đó. Không có network active -&gt; <code>ok=false</code> (FR-AFFIL-002 trả 503).</li><li><strong>MUST</strong> phục vụ webhook <code>POST /v1/affiliate/postback/{network}</code> nhận payload network mang tối thiểu <code>{sub_id, order_value, commission, status}</code> (<code>status</code> là trạng thái phía network, ví dụ <code>approved</code>/<code>pending</code>/<code>rejected</code>).</li><li><strong>MUST</strong> xác thực mọi postback trước khi ghi (DEC-AFFIL-13): kiểm chữ ký HMAC (hoặc secret token) bằng <code>postback_secret</code> đọc từ Vault cho <code>{network}</code>. Chữ ký sai/thiếu -&gt; <code>401</code>, KHÔNG ghi conversion, KHÔNG đổi sổ cái.</li><li><strong>MUST</strong> ghi raw payload + header chữ ký + <code>received_at</code> + cờ <code>verified</code> vào <code>affiliate_postback_log</code> cho mọi postback (kể cả bị từ chối) (DEC-AFFIL-15) - bằng chứng truy vết tranh chấp.</li><li>Sau khi xác thực, <strong>MUST</strong> ánh xạ <code>sub_id</code> -&gt; <code>click_id</code> (FR-AFFIL-001) và ghi conversion qua <code>RecordConversion</code>. <code>sub_id</code> không khớp click nào -&gt; <code>404</code>/<code>ErrUnknownSubID</code>, không tạo conversion mồ côi (đồng nhất FR-AFFIL-001 #8).</li><li><strong>MUST</strong> map trạng thái network sang vòng đời conversion (DEC-AFFIL-14): network <code>approved</code> -&gt; <code>ConfirmConversion</code>; <code>rejected</code> -&gt; <code>RejectConversion</code>; <code>pending</code> -&gt; giữ conversion ở <code>pending</code>. KHÔNG đặt <code>confirmed</code> khi network chưa <code>approved</code> (DEC-AFFIL-05).</li><li><strong>MUST</strong> idempotent với postback lặp (network retry): cùng <code>sub_id</code> postback nhiều lần <strong>MUST NOT</strong> tạo conversion trùng (dựa UNIQUE click_id ở FR-AFFIL-001); postback <code>approved</code> lặp trên conversion đã <code>confirmed</code> là no-op.</li><li><strong>MUST</strong> thực thi last-click attribution (DEC-AFFIL-12): conversion quy về đúng click đã sinh <code>sub_id</code> trong postback - không đoán, không gán theo thời gian.</li><li><strong>MUST</strong> đọc <code>postback_secret</code> từ Vault ở thời điểm verify, KHÔNG cache cleartext lâu trong tiến trình ngoài vòng đời request (giảm bề mặt lộ secret).</li><li><strong>SHOULD</strong> phát OTel: <code>affiliate_postback_total{network, result}</code> (<code>result - {confirmed, pending, rejected, unauthorized, unknown_subid}</code>), <code>affiliate_conversion_confirmed_value_vnd</code> (histogram commission đã confirm).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao secret tham chiếu Vault, không cleartext (DEC-AFFIL-11, §1 #2)?</strong> <code>postback_secret</code> là khóa để xác thực postback. Ai có nó giả mạo được conversion và rút cashback. Lưu cleartext trong DB nghĩa là một lần lộ DB là lộ khả năng giả mạo toàn bộ tiền affiliate, đồng thời vi phạm no-cleartext của PDPL (§5.5). Lưu một tham chiếu và đọc secret từ Vault lúc cần thu hẹp bề mặt lộ về đúng Vault.</p>\n<p><strong>Vì sao xác thực chữ ký trước khi ghi (DEC-AFFIL-13)?</strong> Webhook postback là endpoint công khai: bất kỳ ai biết URL đều POST được. Nếu ghi conversion từ payload chưa xác thực, kẻ xấu bịa <code>{sub_id, commission}</code> để bơm conversion giả rồi rút cashback. HMAC bằng secret chung với network đảm bảo chỉ network thật mới ghi được - đây là rào chống gian lận tài chính cốt lõi.</p>\n<p><strong>Vì sao ghi raw postback dù bị từ chối (DEC-AFFIL-15)?</strong> Đối soát affiliate hay có tranh chấp số liệu (network báo X conversion, ta thấy Y). Có raw payload + thời điểm + kết quả verify cho mỗi postback là bằng chứng để hòa giải. Ghi cả postback bị từ chối còn giúp phát hiện tấn công giả mạo (nhiều postback chữ ký sai = ai đó đang dò).</p>\n<p><strong>Vì sao map trạng thái network sang vòng đời, không confirm sớm (DEC-AFFIL-14, §1 #8)?</strong> Network có vòng đời riêng: đơn mới là <code>pending</code>, được duyệt sau khi qua cửa sổ trả hàng thành <code>approved</code>, hoặc bị <code>rejected</code>. SănDeal phải phản chiếu đúng: chỉ <code>approved</code> mới <code>ConfirmConversion</code> để mở khóa cashback. Confirm sớm khi network còn <code>pending</code> là trả tiền cho đơn có thể đảo - lỗ.</p>\n<p><strong>Vì sao idempotent postback (§1 #9)?</strong> Network gửi lại postback khi không nhận được 200 (mạng chập chờn). Nếu mỗi lần tạo một conversion, sổ cái phình và hoa hồng đếm trùng. Dựa UNIQUE click_id (FR-AFFIL-001) làm postback idempotent: lần đầu tạo, lần sau cập nhật trạng thái hoặc no-op.</p>\n<p><strong>Vì sao đọc secret từ Vault mỗi lần verify (§1 #11)?</strong> Cache secret cleartext lâu trong tiến trình là một bản sao secret nằm trong RAM dài hạn - bề mặt lộ thêm (core dump, debugger). Đọc đúng lúc verify, dùng xong bỏ, giữ secret ở Vault là nguồn sự thật. Có thể cache ngắn với TTL nếu cần hiệu năng, nhưng mặc định là đọc theo nhu cầu.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migrations</h3>\n<pre><code class=\"language-sql\">-- services/affil/migrations/0003_affiliate_network.sql\nCREATE TABLE affiliate_network (\n  id                  SMALLSERIAL PRIMARY KEY,\n  code                TEXT     NOT NULL UNIQUE\n                        CHECK (code IN ('involve_asia','accesstrade')),\n  platform_id         SMALLINT NOT NULL REFERENCES platform(id),\n  base_url            TEXT     NOT NULL,   -- gốc deep link network\n  target_param        TEXT     NOT NULL,   -- tên tham số bọc URL đích\n  sub_id_param        TEXT     NOT NULL,   -- tên tham số mang sub_id (last-click)\n  postback_secret_ref TEXT     NOT NULL,   -- Vault key path; KHÔNG cleartext (§1 #2)\n  active              BOOLEAN  NOT NULL DEFAULT true\n);\n\n-- services/affil/migrations/0004_affiliate_postback_log.sql\nCREATE TABLE affiliate_postback_log (\n  id          BIGSERIAL   PRIMARY KEY,\n  network     TEXT        NOT NULL,\n  raw_payload JSONB       NOT NULL,        -- bằng chứng tranh chấp (§1 #6)\n  signature   TEXT,\n  verified    BOOLEAN     NOT NULL,\n  received_at TIMESTAMPTZ NOT NULL DEFAULT now()\n);\nCREATE INDEX idx_postback_received ON affiliate_postback_log (received_at DESC);</code></pre>\n<h3>Postback verify (Go)</h3>\n<pre><code class=\"language-go\">// services/affil/internal/affil/postback_verify.go\n\n// VerifyPostback kiểm HMAC-SHA256 của body bằng secret đọc từ Vault cho network.\n// Trả false nếu thiếu/sai chữ ký -&gt; handler trả 401, KHÔNG ghi conversion (§1 #5).\nfunc VerifyPostback(ctx context.Context, secrets SecretReader, network string, body []byte, gotSig string) (bool, error) {\n    secret, err := secrets.Get(ctx, secretRefFor(network)) // đọc từ Vault (FR-INFRA-003)\n    if err != nil {\n        return false, err\n    }\n    mac := hmac.New(sha256.New, []byte(secret))\n    mac.Write(body)\n    want := hex.EncodeToString(mac.Sum(nil))\n    return hmac.Equal([]byte(want), []byte(gotSig)), nil // so sánh hằng thời gian\n}</code></pre>\n<h3>Postback handler (Go)</h3>\n<pre><code class=\"language-go\">// services/affil/internal/api/postback.go\n\nfunc (h *Handler) HandlePostback(w http.ResponseWriter, req *http.Request) {\n    network := req.PathValue(\"network\")\n    body, _ := io.ReadAll(req.Body)\n    sig := req.Header.Get(\"X-Signature\")\n\n    ok, err := affil.VerifyPostback(req.Context(), h.secrets, network, body, sig)\n    h.repo.LogPostback(req.Context(), network, body, sig, ok) // luôn ghi raw (§1 #6)\n    if err != nil { writeErr(w, 500, \"verify error\"); return }\n    if !ok {\n        metrics.Postback(network, \"unauthorized\")\n        writeErr(w, 401, \"invalid signature\"); return // KHÔNG ghi conversion (§1 #5)\n    }\n\n    var p PostbackPayload\n    if err := json.Unmarshal(body, &amp;p); err != nil { writeErr(w, 400, \"bad payload\"); return }\n\n    cid, err := h.repo.RecordConversion(req.Context(), p.SubID, p.OrderValue, p.Commission, network)\n    switch {\n    case errors.Is(err, affil.ErrUnknownSubID):\n        metrics.Postback(network, \"unknown_subid\")\n        writeErr(w, 404, \"unknown sub_id\"); return       // không conversion mồ côi (§1 #7)\n    case errors.Is(err, affil.ErrConversionExists):\n        cid = h.repo.ConversionIDBySubID(req.Context(), p.SubID) // idempotent (§1 #9)\n    case err != nil:\n        writeErr(w, 500, \"internal error\"); return\n    }\n\n    switch p.Status { // map trạng thái network -&gt; vòng đời (§1 #8)\n    case \"approved\":\n        _ = h.repo.ConfirmConversion(req.Context(), cid); metrics.Postback(network, \"confirmed\")\n    case \"rejected\":\n        _ = h.repo.RejectConversion(req.Context(), cid, \"network rejected\"); metrics.Postback(network, \"rejected\")\n    default:\n        metrics.Postback(network, \"pending\") // giữ pending\n    }\n    w.WriteHeader(200)\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch -&gt; <code>affiliate_network</code> và <code>affiliate_postback_log</code> tồn tại; <code>affiliate_network</code> có CHECK <code>code</code>.</li><li>Seed Involve Asia + Accesstrade -&gt; <code>TemplateFor(platform_id)</code> trả <code>base_url/target_param/sub_id_param</code> đúng cho network active.</li><li><code>TemplateFor</code> cho sàn không có network active -&gt; <code>ok=false</code> (FR-AFFIL-002 sẽ trả 503).</li><li><code>postback_secret_ref</code> lưu là tham chiếu Vault (không phải secret thô); review xác nhận không có cột secret cleartext.</li><li>Postback chữ ký sai -&gt; <code>401</code>, KHÔNG tạo conversion, nhưng VẪN có dòng <code>affiliate_postback_log</code> với <code>verified=false</code>.</li><li>Postback hợp lệ với <code>sub_id</code> đã có click + <code>status='pending'</code> -&gt; conversion <code>pending</code>, một dòng.</li><li>Postback hợp lệ với <code>status='approved'</code> -&gt; conversion <code>confirmed</code>, <code>confirmed_at</code> được set.</li><li>Postback hợp lệ với <code>status='rejected'</code> -&gt; conversion <code>rejected</code>.</li><li>Postback với <code>sub_id</code> không khớp click -&gt; <code>404</code>, KHÔNG tạo conversion mồ côi.</li><li>Postback lặp (cùng <code>sub_id</code>, network retry) -&gt; không tạo conversion thứ hai; postback <code>approved</code> lặp trên conversion đã <code>confirmed</code> là no-op (vẫn <code>200</code>).</li><li>Mọi postback (kể cả bị từ chối) đều có dòng raw trong <code>affiliate_postback_log</code>.</li><li>Metric <code>affiliate_postback_total{network,result}</code> tăng đúng nhãn theo từng nhánh.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/affil/internal/api/postback_test.go\nfunc TestPostback_BadSignature_401_NoConversion(t *testing.T) {\n    h, repo := setupPostback(t)\n    rec := doSignedPOST(t, h, \"involve_asia\", `{\"sub_id\":\"sd_x\",\"order_value\":250000,\"commission\":12000,\"status\":\"approved\"}`, \"WRONGSIG\")\n    require.Equal(t, 401, rec.Code)\n    require.Equal(t, 0, repo.ConversionCount())   // không ghi conversion\n    require.Equal(t, 1, repo.PostbackLogCount())  // vẫn log raw (verified=false)\n}\n\nfunc TestPostback_Valid_Pending(t *testing.T) {\n    h, repo := setupPostbackWithClick(t, \"sd_x\")\n    body := `{\"sub_id\":\"sd_x\",\"order_value\":250000,\"commission\":12000,\"status\":\"pending\"}`\n    rec := doSignedPOST(t, h, \"involve_asia\", body, sign(body))\n    require.Equal(t, 200, rec.Code)\n    require.Equal(t, \"pending\", repo.StatusBySubID(\"sd_x\"))\n}\n\nfunc TestPostback_Approved_Confirms(t *testing.T) {\n    h, repo := setupPostbackWithClick(t, \"sd_y\")\n    body := `{\"sub_id\":\"sd_y\",\"order_value\":250000,\"commission\":12000,\"status\":\"approved\"}`\n    doSignedPOST(t, h, \"involve_asia\", body, sign(body))\n    require.Equal(t, \"confirmed\", repo.StatusBySubID(\"sd_y\"))\n}\n\nfunc TestPostback_UnknownSubID_404_NoOrphan(t *testing.T) {\n    h, repo := setupPostback(t)\n    body := `{\"sub_id\":\"sd_none\",\"order_value\":1,\"commission\":0,\"status\":\"approved\"}`\n    rec := doSignedPOST(t, h, \"involve_asia\", body, sign(body))\n    require.Equal(t, 404, rec.Code)\n    require.Equal(t, 0, repo.ConversionCount())\n}\n\nfunc TestPostback_RetryIdempotent(t *testing.T) {\n    h, repo := setupPostbackWithClick(t, \"sd_z\")\n    body := `{\"sub_id\":\"sd_z\",\"order_value\":250000,\"commission\":12000,\"status\":\"approved\"}`\n    doSignedPOST(t, h, \"involve_asia\", body, sign(body))\n    rec := doSignedPOST(t, h, \"involve_asia\", body, sign(body)) // network retry\n    require.Equal(t, 200, rec.Code)\n    require.Equal(t, 1, repo.ConversionCount()) // không trùng\n    require.Equal(t, \"confirmed\", repo.StatusBySubID(\"sd_z\"))\n}</code></pre>\n<pre><code class=\"language-go\">// services/affil/internal/affil/postback_verify_test.go\nfunc TestVerify_GoodSignature(t *testing.T) {\n    secrets := fakeVault{\"affil/involve_asia\": \"shh-secret\"}\n    body := []byte(`{\"sub_id\":\"sd_x\"}`)\n    sig := hmacHex(\"shh-secret\", body)\n    ok, err := VerifyPostback(ctx, secrets, \"involve_asia\", body, sig)\n    require.NoError(t, err); require.True(t, ok)\n}\n\nfunc TestVerify_TamperedBody(t *testing.T) {\n    secrets := fakeVault{\"affil/involve_asia\": \"shh-secret\"}\n    sig := hmacHex(\"shh-secret\", []byte(`{\"commission\":1000}`))\n    ok, _ := VerifyPostback(ctx, secrets, \"involve_asia\", []byte(`{\"commission\":9999999}`), sig)\n    require.False(t, ok) // body đổi -&gt; chữ ký không khớp\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration <code>0003_affiliate_network.sql</code> -&gt; <code>0004_affiliate_postback_log.sql</code> -&gt; <code>network.go</code> (<code>TemplateFor</code> + seed Involve Asia/Accesstrade với <code>postback_secret_ref</code> trỏ Vault) -&gt; <code>postback_verify.go</code> (HMAC-SHA256, <code>hmac.Equal</code> so sánh hằng thời gian, secret từ FR-INFRA-003) -&gt; <code>postback.go</code> (verify -&gt; log raw -&gt; RecordConversion -&gt; map status -&gt; Confirm/Reject, idempotent) -&gt; đăng ký route <code>POST /v1/affiliate/postback/{network}</code> trong <code>router.go</code> (<code>http.ServeMux</code> Go 1.22 với <code>PathValue</code>) -&gt; tests. Webhook KHÔNG đi qua JWT user (network không có JWT); thay vào đó xác thực bằng chữ ký HMAC. Secret đọc qua client Vault của FR-INFRA-003.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>FR-AFFIL-002</strong> - tạo deep link + sinh <code>sub_id</code> mà postback dùng để đối soát; <code>TemplateFor</code> của FR này phục vụ FR-AFFIL-002.</li><li><strong>FR-AFFIL-001</strong> - cung cấp <code>RecordConversion</code>/<code>ConfirmConversion</code>/<code>RejectConversion</code> + bảng <code>affiliate_click</code>/<code>affiliate_conversion</code>.</li><li><strong>FR-INFRA-003 (Vault)</strong> - đọc <code>postback_secret</code> để xác thực postback; secret không nằm trong DB.</li><li><strong>FR-INFRA-001 (gateway)</strong> - định tuyến webhook; route postback bỏ qua JWT user, dùng chữ ký HMAC.</li><li><strong>FR-AFFIL-005 (downstream, P3)</strong> - cashback đọc conversion <code>confirmed</code> (chỉ chi sau khi network approve).</li><li><strong>FR-TRUST-005 (downstream)</strong> - phát hiện gaming attribution dùng log postback + tỷ lệ confirm/reject.</li><li><strong>NFR-AFFIL-001</strong> - compliance: postback đối soát đúng last-click do click user-initiated sinh ra.</li><li>Lib: <code>crypto/hmac</code>, <code>crypto/sha256</code>, <code>encoding/hex</code>, <code>net/http</code>, <code>encoding/json</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Webhook postback từ network (Involve Asia)</h3>\n<pre><code class=\"language-http\">POST /v1/affiliate/postback/involve_asia HTTP/1.1\nContent-Type: application/json\nX-Signature: 9f86d081884c7d659a2feaa0c55ad015a3bf4f1b2b0b822cd15d6c15b0f00a08\n\n{\n  \"sub_id\": \"sd_ab12cd34ef56\",\n  \"order_value\": 250000,\n  \"commission\": 12000,\n  \"status\": \"approved\"\n}</code></pre>\n<h3>Cấu hình network (seed)</h3>\n<pre><code class=\"language-sql\">INSERT INTO affiliate_network (code, platform_id, base_url, target_param, sub_id_param, postback_secret_ref)\nVALUES\n  ('involve_asia', 1, 'https://go.involve.asia/aff', 'url', 'sub_id', 'affil/involve_asia/postback_secret'),\n  ('accesstrade',  1, 'https://go.isclix.com/deep_link', 'url_enc', 'utm_content', 'affil/accesstrade/postback_secret');</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Cookie window per-network (7 ngày vs 30/7 - cần xác minh §10 tài liệu nguồn với Involve Asia/Accesstrade) - thêm cột <code>cookie_window_days</code> vào <code>affiliate_network</code> khi xác minh xong.</li><li>Chọn network trả hoa hồng cao nhất khi một sàn có nhiều network active - thêm logic chọn ở <code>TemplateFor</code>; hiện dùng network active đầu tiên.</li><li>Đối soát định kỳ kéo report từ API network (ngoài postback) để bắt conversion bị bỏ lỡ - thêm job nền sau.</li><li>Reject conversion khi đơn bị trả hàng sau khi đã confirm (chargeback) - cần postback <code>reversed</code>; map thêm khi network hỗ trợ.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Ghi conversion từ postback chưa xác thực</td><td>bad-signature test</td><td>giả mạo conversion, rút cashback</td><td>Verify HMAC trước khi ghi (§1 #5)</td></tr><tr><td>Secret cleartext trong DB</td><td>review (§4 #4)</td><td>lộ DB = giả mạo toàn bộ postback</td><td>Tham chiếu Vault (DEC-AFFIL-11)</td></tr><tr><td>Confirm sớm khi network pending</td><td>status-map test</td><td>trả cashback đơn có thể đảo</td><td>Chỉ approved -&gt; confirm (§1 #8)</td></tr><tr><td>Postback lặp tạo conversion trùng</td><td>retry test</td><td>đếm hoa hồng trùng</td><td>Idempotent qua UNIQUE click_id (§1 #9)</td></tr><tr><td>sub_id không khớp click</td><td>404 test</td><td>conversion mồ côi</td><td>Từ chối ghi (§1 #7)</td></tr><tr><td>Không ghi raw postback</td><td>log-count test</td><td>mất bằng chứng tranh chấp</td><td>Luôn LogPostback (§1 #6)</td></tr><tr><td>Body bị sửa giữa đường</td><td>tampered-body test</td><td>số tiền giả</td><td>HMAC trên body, hmac.Equal (§3)</td></tr><tr><td>Thiếu network template</td><td>TemplateFor ok=false</td><td>FR-AFFIL-002 trả 503</td><td>Seed network; lộ lỗi cấu hình (§1 #3)</td></tr><tr><td>Secret cache cleartext lâu</td><td>review</td><td>bề mặt lộ thêm</td><td>Đọc Vault lúc verify (§1 #11)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>FR-AFFIL-003 đóng vòng tiền: click user-initiated (FR-AFFIL-002) -&gt; conversion (postback) -&gt; confirm khi network approve -&gt; cashback (FR-AFFIL-005).</li><li>Xác thực chữ ký HMAC trước khi ghi là rào chống giả mạo conversion - webhook là endpoint công khai, không xác thực là mời gian lận tài chính.</li><li>Secret postback tham chiếu Vault, không cleartext: thu hẹp bề mặt lộ và tuân no-cleartext PDPL (§5.5).</li><li>Map trạng thái network sang vòng đời conversion giữ SănDeal chỉ trả cashback cho đơn đã approve, không đơn còn pending (có thể đảo).</li><li>Idempotent postback (UNIQUE click_id) chịu được network retry mà không đếm trùng hoa hồng.</li><li>Ghi raw mọi postback (cả bị từ chối) là bằng chứng đối soát và tín hiệu phát hiện tấn công giả mạo.</li><li>Last-click thực thi qua sub_id: conversion quy về đúng click do user bấm sinh ra, không đoán.</li></ul>\n<hr>\n<p><em>Hết FR-AFFIL-003. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
