window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-TRACK-001"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service TRACK <strong>MUST</strong> expose endpoint REST <code>POST /v1/track</code> nhận <code>{platform, item_url}</code>, parse <code>item_url</code> theo từng sàn ra định danh SKU, upsert vào <code>tracked_product</code> (qua FR-PRICE-001), gắn người dùng vào SKU đó, rồi enqueue một scrape job priming để có snapshot giá đầu tiên. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> phục vụ route <code>POST /v1/track</code> với thân JSON <code>{platform: string, item_url: string}</code>. Thiếu trường bắt buộc trả <code>400</code>.</li><li><strong>MUST</strong> validate <code>platform</code> thuộc allowlist code sàn <code>{shopee, tiktok, lazada}</code> (DEC-TRACK-05); ngoài allowlist trả <code>400</code> với thân <code>{\"error\":\"unsupported platform\"}</code>. KHÔNG enqueue gì khi validate thất bại.</li><li><strong>MUST</strong> parse <code>item_url</code> theo từng sàn ra <code>(platform_item_id, shop_id)</code> (DEC-TRACK-01): handler tự bóc id từ url, KHÔNG nhận <code>platform_item_id</code> do client gửi sẵn. URL không khớp mẫu của <code>platform</code> trả <code>400</code> với thân <code>{\"error\":\"invalid item_url\"}</code>.</li><li><strong>MUST</strong> map <code>platform</code> code sang <code>platform_id</code> (SMALLINT) qua bảng <code>platform</code> (FR-INFRA-002) trước khi upsert; code hợp lệ nhưng chưa seed trong <code>platform</code> trả <code>400</code>.</li><li><strong>MUST</strong> tạo hoặc lấy <code>tracked_product</code> qua <code>Upsert</code> idempotent của FR-PRICE-001 (DEC-TRACK-02): cùng <code>(platform_id, platform_item_id)</code> chỉ một dòng dù nhiều user track. Handler <strong>MUST NOT</strong> ghi <code>tracked_product</code> bằng INSERT trần.</li><li><strong>MUST</strong> gắn người dùng vào SKU qua bảng nối <code>user_tracked_product (user_id, product_id, tracked_at)</code> với <code>UNIQUE (user_id, product_id)</code> (DEC-TRACK-04); link idempotent qua <code>ON CONFLICT (user_id, product_id) DO NOTHING</code>. Một user track cùng SKU nhiều lần vẫn một dòng nối.</li><li><strong>MUST</strong> lấy <code>user_id</code> từ JWT do API Gateway (FR-INFRA-001) gắn; handler KHÔNG tự parse token. Request thiếu auth bị gateway chặn trước.</li><li><strong>MUST</strong> enqueue một scrape job priming sau khi có <code>tracked_product.id</code> (DEC-TRACK-03): job ưu tiên để scraper (FR-SCRAPE-002) lấy snapshot giá đầu tiên ngay, không chờ vòng quét định kỳ. Enqueue đặt sau khi link user thành công.</li><li><strong>MUST</strong> trả <code>201 Created</code> với thân <code>{product_id, platform, already_tracked}</code>; <code>already_tracked=true</code> khi user đã track SKU này từ trước (link là no-op), <code>false</code> khi vừa tạo link mới.</li><li><strong>MUST</strong> đảm bảo thứ tự bền vững: nếu enqueue scrape job lỗi sau khi đã upsert + link, handler <strong>MUST</strong> vẫn trả <code>201</code> (sản phẩm đã được theo dõi) và ghi log lỗi enqueue để retry nền - không lăn ngược việc link user.</li><li><strong>MUST</strong> đặt <code>Content-Type: application/json; charset=utf-8</code>; giá (nếu trả kèm) là <code>BIGINT</code> VND (int64), đồng nhất với DEC-PRICE-05.</li><li><strong>SHOULD</strong> phát OTel: <code>track_requests_total{platform, status}</code> (counter), <code>track_new_product_total{platform}</code> (counter đếm SKU lần đầu vào hệ thống), <code>track_scrape_enqueue_total{result}</code> (counter).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao parse item_url phía server thay vì nhận id sẵn (DEC-TRACK-01)?</strong> Người dùng dán một đường link sản phẩm họ copy từ app sàn - đó là input tự nhiên. Nếu để client tự bóc <code>platform_item_id</code> rồi gửi, ta tin một giá trị client kiểm soát: client lỗi gửi id sai, hoặc kẻ xấu gửi id trỏ tới SKU bất kỳ để bơm dữ liệu rác. Parse url phía server cho ta một nguồn sự thật, đồng thời chuẩn hóa được các biến thể url (có/không query tracking, short link) về cùng một id.</p>\n<p><strong>Vì sao upsert qua FR-PRICE-001 thay vì INSERT (DEC-TRACK-02)?</strong> Nhiều user có thể cùng theo dõi một SKU hot. SKU là thực thể dùng chung, không thuộc riêng user nào. <code>Upsert</code> idempotent của FR-PRICE-001 đảm bảo đúng một dòng <code>tracked_product</code> cho mỗi SKU; quan hệ \"ai theo dõi\" nằm ở bảng nối riêng. Tách vậy giữ registry sản phẩm sạch và cho phép chia sẻ chi phí scraping (một SKU quét một lần phục vụ mọi người theo dõi).</p>\n<p><strong>Vì sao enqueue scrape job priming (DEC-TRACK-03)?</strong> Cold-start là rủi ro đã nêu (§5.1): SKU mới chưa có lịch sử giá. Nếu chỉ chờ vòng quét định kỳ, người dùng vừa track xong vào xem ngay sẽ thấy biểu đồ rỗng - đúng lúc họ quan tâm nhất. Một job priming ưu tiên lấy snapshot đầu tiên trong vài giây, cho biểu đồ ít nhất một điểm và khởi động chuỗi giá.</p>\n<p><strong>Vì sao bảng nối user_tracked_product với UNIQUE (DEC-TRACK-04)?</strong> Quan hệ user-SKU là nhiều-nhiều: một user theo dõi nhiều SKU, một SKU được nhiều user theo dõi. Bảng nối là cách chuẩn. <code>UNIQUE (user_id, product_id)</code> làm việc track idempotent - bấm track hai lần không tạo hai link, và là khóa <code>ON CONFLICT</code> tự nhiên.</p>\n<p><strong>Vì sao vẫn trả 201 khi enqueue lỗi (§1 #10)?</strong> Hành động người dùng yêu cầu là \"theo dõi sản phẩm này\", và điều đó đã hoàn thành khi upsert + link xong. Snapshot priming là tối ưu trải nghiệm, không phải điều kiện thành công. Lăn ngược cả việc track chỉ vì hàng đợi scrape tạm trục trặc là phản trực giác; thay vào đó ghi log để retry nền và để vòng quét định kỳ bù.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/track/migrations/0001_user_tracked_product.sql\nCREATE TABLE user_tracked_product (\n  user_id    BIGINT      NOT NULL REFERENCES app_user(id),\n  product_id BIGINT      NOT NULL REFERENCES tracked_product(id),\n  tracked_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  PRIMARY KEY (user_id, product_id)\n);\n\n-- Liệt kê nhanh mọi SKU một user đang theo dõi (cho wishlist/alert/dashboard)\nCREATE INDEX idx_utp_user ON user_tracked_product (user_id);\n-- Đếm nhanh số người theo dõi một SKU (chia sẻ chi phí scraping, ưu tiên tần suất quét)\nCREATE INDEX idx_utp_product ON user_tracked_product (product_id);</code></pre>\n<h3>Parser url theo sàn (Go)</h3>\n<pre><code class=\"language-go\">// services/track/internal/track/url_parser.go\n\n// ParsedItem là kết quả bóc từ item_url.\ntype ParsedItem struct {\n    PlatformItemID string\n    ShopID         string // có thể rỗng nếu sàn không nhúng shop trong url\n}\n\n// shopeeItemRe khớp .../&lt;shop_id&gt;.&lt;item_id&gt; hoặc ...-i.&lt;shop_id&gt;.&lt;item_id&gt;\nvar shopeeItemRe = regexp.MustCompile(`i\\.(\\d+)\\.(\\d+)`)\nvar lazadaItemRe = regexp.MustCompile(`/products/.*-i(\\d+)`)\nvar tiktokItemRe = regexp.MustCompile(`/product/(\\d+)`)\n\n// ParseItemURL bóc (platform_item_id, shop_id) theo từng sàn (DEC-TRACK-01).\n// Trả ok=false nếu url không khớp mẫu của platform -&gt; handler trả 400.\nfunc ParseItemURL(platform, rawURL string) (ParsedItem, bool) {\n    u, err := url.Parse(rawURL)\n    if err != nil || u.Host == \"\" {\n        return ParsedItem{}, false\n    }\n    switch platform {\n    case \"shopee\":\n        m := shopeeItemRe.FindStringSubmatch(u.Path)\n        if m == nil {\n            return ParsedItem{}, false\n        }\n        return ParsedItem{ShopID: m[1], PlatformItemID: m[2]}, true\n    case \"lazada\":\n        m := lazadaItemRe.FindStringSubmatch(u.Path)\n        if m == nil {\n            return ParsedItem{}, false\n        }\n        return ParsedItem{PlatformItemID: m[1]}, true\n    case \"tiktok\":\n        m := tiktokItemRe.FindStringSubmatch(u.Path)\n        if m == nil {\n            return ParsedItem{}, false\n        }\n        return ParsedItem{PlatformItemID: m[1]}, true\n    default:\n        return ParsedItem{}, false\n    }\n}</code></pre>\n<h3>Handler (Go)</h3>\n<pre><code class=\"language-go\">// services/track/internal/api/track.go\n\ntype TrackRequest struct {\n    Platform string `json:\"platform\"`\n    ItemURL  string `json:\"item_url\"`\n}\ntype TrackResponse struct {\n    ProductID      int64  `json:\"product_id\"`\n    Platform       string `json:\"platform\"`\n    AlreadyTracked bool   `json:\"already_tracked\"`\n}\n\nfunc (h *Handler) HandleTrack(w http.ResponseWriter, req *http.Request) {\n    userID := auth.UserID(req.Context()) // do gateway gắn (FR-INFRA-001)\n    var body TrackRequest\n    if err := json.NewDecoder(req.Body).Decode(&amp;body); err != nil {\n        writeErr(w, http.StatusBadRequest, \"invalid body\")\n        return\n    }\n    platformID, ok := h.platforms.IDByCode(body.Platform) // map code -&gt; platform_id\n    if !ok {\n        writeErr(w, http.StatusBadRequest, \"unsupported platform\")\n        return\n    }\n    item, ok := track.ParseItemURL(body.Platform, body.ItemURL)\n    if !ok {\n        writeErr(w, http.StatusBadRequest, \"invalid item_url\")\n        return\n    }\n    tp, err := h.price.Upsert(req.Context(), price.TrackedProduct{ // FR-PRICE-001\n        PlatformID:     platformID,\n        PlatformItemID: item.PlatformItemID,\n        ShopID:         nilIfEmpty(item.ShopID),\n    })\n    if err != nil {\n        writeErr(w, http.StatusInternalServerError, \"internal error\")\n        return\n    }\n    linkedNew, err := h.repo.LinkUserProduct(req.Context(), userID, tp.ID)\n    if err != nil {\n        writeErr(w, http.StatusInternalServerError, \"internal error\")\n        return\n    }\n    if err := h.scrapeQueue.EnqueuePriming(req.Context(), tp.ID); err != nil {\n        log.Warn(\"priming enqueue failed\", \"product_id\", tp.ID, \"err\", err) // §1 #10: không lăn ngược\n    }\n    w.Header().Set(\"Content-Type\", \"application/json; charset=utf-8\")\n    w.WriteHeader(http.StatusCreated)\n    _ = json.NewEncoder(w).Encode(TrackResponse{\n        ProductID: tp.ID, Platform: body.Platform, AlreadyTracked: !linkedNew,\n    })\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>POST /v1/track {platform:\"shopee\", item_url:\"https://shopee.vn/...-i.88123.20114455667\"}</code> trả <code>201</code> + <code>product_id &gt; 0</code>.</li><li>URL Shopee parse đúng <code>shop_id=88123</code>, <code>platform_item_id=20114455667</code>; Lazada <code>/products/...-i7788</code> -&gt; <code>7788</code>; TikTok <code>/product/990011</code> -&gt; <code>990011</code>.</li><li><code>platform</code> ngoài allowlist (vd <code>\"amazon\"</code>) trả <code>400</code> + <code>{\"error\":\"unsupported platform\"}</code>, KHÔNG enqueue.</li><li><code>item_url</code> không khớp mẫu của <code>platform</code> trả <code>400</code> + <code>{\"error\":\"invalid item_url\"}</code>.</li><li>Track cùng SKU lần hai bởi cùng user trả <code>201</code> + <code>already_tracked=true</code>; số dòng <code>user_tracked_product</code> vẫn 1.</li><li>Hai user khác nhau track cùng SKU -&gt; một dòng <code>tracked_product</code>, hai dòng <code>user_tracked_product</code>.</li><li><code>tracked_product</code> được tạo qua <code>Upsert</code> (cùng <code>(platform_id, platform_item_id)</code> không nhân bản); xác nhận chỉ một dòng.</li><li>Sau track thành công, một scrape job priming được enqueue đúng một lần với <code>product_id</code> vừa có.</li><li>Enqueue scrape lỗi (mock queue fail) -&gt; handler vẫn trả <code>201</code>, link user vẫn tồn tại, log cảnh báo được ghi.</li><li>Request thiếu JWT bị gateway chặn (handler không bao giờ thấy); handler không tự xác thực.</li><li><code>platform</code> hợp lệ nhưng chưa seed trong bảng <code>platform</code> -&gt; <code>400</code>.</li><li>Metric <code>track_new_product_total</code> tăng khi SKU lần đầu vào hệ thống; <code>track_requests_total{status}</code> tăng theo mỗi phản hồi.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/track/internal/track/url_parser_test.go\nfunc TestParse_Shopee(t *testing.T) {\n    it, ok := ParseItemURL(\"shopee\", \"https://shopee.vn/Tai-nghe-i.88123.20114455667?sp_atk=x\")\n    require.True(t, ok)\n    require.Equal(t, \"88123\", it.ShopID)\n    require.Equal(t, \"20114455667\", it.PlatformItemID)\n}\n\nfunc TestParse_Lazada(t *testing.T) {\n    it, ok := ParseItemURL(\"lazada\", \"https://www.lazada.vn/products/abc-pro-i7788.html\")\n    require.True(t, ok)\n    require.Equal(t, \"7788\", it.PlatformItemID)\n}\n\nfunc TestParse_TikTok(t *testing.T) {\n    it, ok := ParseItemURL(\"tiktok\", \"https://www.tiktok.com/view/product/990011\")\n    require.True(t, ok)\n    require.Equal(t, \"990011\", it.PlatformItemID)\n}\n\nfunc TestParse_BadURL(t *testing.T) {\n    _, ok := ParseItemURL(\"shopee\", \"https://shopee.vn/no-item-id-here\")\n    require.False(t, ok) // -&gt; handler trả 400\n}\n\n// services/track/internal/api/track_test.go\nfunc TestTrack_NewProduct_201(t *testing.T) {\n    h, q := setupHandler(t)\n    rec := doPOST(t, h, \"/v1/track\",\n        `{\"platform\":\"shopee\",\"item_url\":\"https://shopee.vn/x-i.88123.20114455667\"}`)\n    require.Equal(t, 201, rec.Code)\n    require.Equal(t, 1, q.PrimingCount()) // đúng một job priming\n}\n\nfunc TestTrack_UnsupportedPlatform_400(t *testing.T) {\n    h, q := setupHandler(t)\n    rec := doPOST(t, h, \"/v1/track\", `{\"platform\":\"amazon\",\"item_url\":\"https://x\"}`)\n    require.Equal(t, 400, rec.Code)\n    require.Equal(t, 0, q.PrimingCount()) // không enqueue khi validate fail\n}\n\nfunc TestTrack_Idempotent_SameUser(t *testing.T) {\n    h, _ := setupHandler(t)\n    body := `{\"platform\":\"shopee\",\"item_url\":\"https://shopee.vn/x-i.88123.20114455667\"}`\n    doPOST(t, h, \"/v1/track\", body)\n    rec := doPOST(t, h, \"/v1/track\", body)\n    var resp TrackResponse\n    decode(t, rec, &amp;resp)\n    require.True(t, resp.AlreadyTracked)\n    require.Equal(t, 1, countLinks(t, h)) // không nhân bản link\n}\n\nfunc TestTrack_EnqueueFails_Still201(t *testing.T) {\n    h, q := setupHandler(t)\n    q.FailNext() // queue từ chối job kế\n    rec := doPOST(t, h, \"/v1/track\",\n        `{\"platform\":\"shopee\",\"item_url\":\"https://shopee.vn/x-i.88123.20114455667\"}`)\n    require.Equal(t, 201, rec.Code)       // §1 #10: sản phẩm vẫn được theo dõi\n    require.Equal(t, 1, countLinks(t, h)) // link user không bị lăn ngược\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>url_parser.go</code> (parse 3 sàn) -&gt; migration <code>0001_user_tracked_product.sql</code> -&gt; <code>repo.go</code> (<code>LinkUserProduct</code> ON CONFLICT DO NOTHING) -&gt; <code>track.go</code> (handler ghép parse + Upsert price-svc + link + enqueue) -&gt; đăng ký route trong <code>router.go</code> sau JWT middleware (FR-INFRA-001) -&gt; tests. Handler dùng <code>http.ServeMux</code> Go 1.22. <code>EnqueuePriming</code> đẩy vào cùng hàng đợi scrape của FR-SCRAPE-001/002 với cờ ưu tiên. <code>Upsert</code> gọi sang package <code>price</code> (cùng monorepo) hoặc qua client nội bộ - không tự ghi <code>tracked_product</code>.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>FR-PRICE-001</strong> - cung cấp <code>Upsert(tracked_product)</code> idempotent và bảng <code>tracked_product</code>; là điều kiện cứng cho việc tạo/lấy SKU.</li><li><strong>FR-SCRAPE-002</strong> - tiêu thụ scrape job priming, lấy snapshot giá đầu tiên của SKU mới track.</li><li><strong>FR-INFRA-001 (gateway)</strong> - gắn JWT auth và <code>user_id</code> vào context trước handler.</li><li><strong>FR-INFRA-002</strong> - bảng <code>platform</code> để map code sàn -&gt; <code>platform_id</code>; bảng <code>app_user</code> cho FK <code>user_id</code>.</li><li><strong>FR-TRACK-002 / FR-TRACK-003 (downstream)</strong> - wishlist và alert_rule gắn vào <code>tracked_product</code> đã track ở đây.</li><li>Lib: <code>pgx</code>, <code>encoding/json</code>, <code>net/http</code>, <code>regexp</code>, <code>net/url</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Request</h3>\n<pre><code>curl -s -X POST -H \"Authorization: Bearer $JWT\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"platform\":\"shopee\",\"item_url\":\"https://shopee.vn/Tai-nghe-ABC-i.88123.20114455667\"}' \\\n  \"https://api.sandeal.vn/v1/track\"</code></pre>\n<h3>Response (201)</h3>\n<pre><code class=\"language-json\">{\n  \"product_id\": 90112,\n  \"platform\": \"shopee\",\n  \"already_tracked\": false\n}</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Resolve short link (vd <code>s.shopee.vn/...</code>) bằng một HEAD request theo redirect trước khi parse - thêm khi gặp nhiều short link trong thực tế; không cản FR này.</li><li>Giới hạn số SKU theo dõi theo tier (free vs Premium) - gắn vào FR-BILL khi có gating.</li><li>Track theo <code>canonical_key</code> để tự theo dõi cùng món trên cả 3 sàn một lần - chờ FR-PRICE-005 so khớp; mở rộng sau.</li><li>Bỏ theo dõi (<code>DELETE /v1/track/{product_id}</code>) - thêm khi làm UI quản lý wishlist (FR-WEB-004).</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>platform ngoài allowlist</td><td>validate code</td><td>400 unsupported platform</td><td>UI chỉ cho chọn 3 sàn đã hỗ trợ</td></tr><tr><td>item_url không khớp mẫu</td><td>parser ok=false</td><td>400 invalid item_url</td><td>Hướng dẫn người dùng dán đúng link sản phẩm</td></tr><tr><td>platform_item_id client gửi giả</td><td>parse server-side</td><td>bỏ qua, chỉ tin url</td><td>Không nhận id từ client (DEC-TRACK-01)</td></tr><tr><td>Trùng track cùng user</td><td>ON CONFLICT DO NOTHING</td><td>already_tracked=true</td><td>Theo thiết kế (idempotent)</td></tr><tr><td>Nhiều user cùng SKU</td><td>Upsert + bảng nối</td><td>một SKU, nhiều link</td><td>Theo thiết kế (chia sẻ SKU)</td></tr><tr><td>Enqueue scrape lỗi</td><td>log warn</td><td>vẫn 201, không có snapshot ngay</td><td>Retry nền + vòng quét định kỳ bù</td></tr><tr><td>platform chưa seed</td><td>IDByCode false</td><td>400</td><td>Seed platform (FR-INFRA-002) chạy trước</td></tr><tr><td>Short link chưa resolve</td><td>parser ok=false</td><td>400</td><td>Hoãn: resolve redirect (xem §9)</td></tr><tr><td>Race hai request track cùng SKU+user</td><td>ON CONFLICT</td><td>một link, không lỗi</td><td>Theo thiết kế (idempotent)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li><code>POST /v1/track</code> là cửa vào duy nhất của vòng theo dõi giá: parse url -&gt; SKU dùng chung -&gt; link user -&gt; priming snapshot.</li><li>Parse url phía server là rào an toàn: client chỉ đưa link, server quyết định id, tránh id rác và giả mạo.</li><li>Tách <code>tracked_product</code> (dùng chung) khỏi <code>user_tracked_product</code> (riêng user) cho phép chia sẻ chi phí scraping: một SKU hot quét một lần phục vụ mọi người theo dõi.</li><li>Scrape job priming là liều thuốc cold-start ở mức từng SKU: người dùng vừa track có ngay điểm giá đầu tiên thay vì biểu đồ rỗng.</li><li>Việc track không bị lăn ngược chỉ vì enqueue trục trặc - hành động người dùng (theo dõi) và tối ưu trải nghiệm (priming) tách rời nhau.</li><li>Khi mở SEA, thêm mẫu parse url cho sàn nước khác vào <code>ParseItemURL</code>; phần còn lại của handler không đổi.</li></ul>\n<hr>\n<p><em>Hết FR-TRACK-001. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
