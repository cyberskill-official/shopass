window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-EXT-002"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Content script Shopee <strong>MUST</strong> chạy trong tab đã đăng nhập của chính người dùng, đọc dữ liệu giỏ hàng/voucher đã render theo nguyên tắc session piggyback, và TUYỆT ĐỐI không thu thập mật khẩu hay để token/cookie phiên rời máy client. Hợp đồng:</p>\n<ol><li><code>manifest.json</code> <strong>MUST</strong> khai báo <code>content_scripts</code> với <code>matches: [\"https://shopee.vn/*\"]</code>, <code>run_at: \"document_idle\"</code> - chạy đúng trên domain Shopee đã khai trong <code>host_permissions</code> (FR-EXT-001 §1 #6).</li><li>Content script <strong>MUST</strong> đọc dữ liệu giỏ hàng theo một trong hai đường, ưu tiên (a): (a) gọi internal endpoint <code>/api/v4/cart/get</code> bằng <code>fetch(..., { credentials: \"include\" })</code> để mượn cookie phiên first-party của tab; (b) parse DOM giỏ hàng đã render khi endpoint không truy cập được (DEC-EXT-07).</li><li>Content script <strong>MUST NOT</strong> đọc, sao chép, hay ghi log giá trị cookie/session token: cấm <code>document.cookie</code>, cấm đọc header <code>Set-Cookie</code>/<code>Authorization</code>, cấm đưa bất kỳ giá trị credential nào vào biến hoặc payload (DEC-EXT-08, DEC-EXT-09). <code>credentials: \"include\"</code> chỉ để trình duyệt tự đính cookie vào request - extension không bao giờ thấy giá trị cookie.</li><li>Content script <strong>MUST NOT</strong> thu thập mật khẩu hay bất kỳ input credential nào (không đọc <code>input[type=password]</code>, không hook form đăng nhập) (DEC-EXT-08).</li><li>Dữ liệu trích xuất <strong>MUST</strong> giới hạn ở giỏ hàng/voucher đã hiển thị: với mỗi item lấy <code>productId</code>, <code>price</code>, <code>qty</code>; với voucher lấy mã + điều kiện hiển thị. Mọi trường khác <strong>MUST</strong> bị loại trước khi giao cho service worker (DEC-EXT-10). FR-EXT-003 lọc lần hai (defense in depth).</li><li><code>dom-selectors.ts</code> <strong>MUST</strong> cung cấp nhiều selector dự phòng cho mỗi trường (item, giá, qty, voucher) vì DOM Shopee đổi theo A/B test (DEC-EXT-11). Khi selector chính trượt, parser <strong>MUST</strong> thử selector dự phòng trước khi coi là hỏng.</li><li>Khi cả internal JSON lẫn mọi selector DOM đều hỏng, content script <strong>MUST</strong> phát một tín hiệu sức khỏe (health signal) về service worker để FR-SCRAPE-006 (DOM-change monitoring) ghi nhận - KHÔNG nuốt lỗi im lặng (DEC-EXT-11).</li><li>Content script <strong>MUST</strong> chỉ đọc: KHÔNG tự sửa giỏ, KHÔNG đặt hàng, KHÔNG tự áp voucher (DEC-EXT-12). Áp voucher tự động (nếu có) thuộc FR-CART-005 với ràng buộc user-initiated riêng - không nằm ở đây.</li><li>Kết quả đọc <strong>MUST</strong> gửi cho service worker qua <code>chrome.runtime.sendMessage</code> dạng <code>CartReadMessage</code> typed (định nghĩa ở <code>types.ts</code>) chứa CHỈ <code>CartItem[]</code> + <code>VoucherItem[]</code> tối thiểu - KHÔNG cookie/header/token.</li><li><code>api-client.ts</code> <strong>MUST</strong> đặt timeout cho <code>fetch</code> &lt; 30 giây (ràng buộc MV3: fetch treo &gt;30s làm SW liên đới bị kill; cũng tránh treo UI tab).</li><li>Content script <strong>MUST</strong> xử lý trường hợp người dùng CHƯA đăng nhập (endpoint trả lỗi auth / DOM giỏ rỗng) một cách lịch sự: không lỗi, báo trạng thái \"chưa có dữ liệu giỏ\", không thử đọc credential để \"tự đăng nhập\".</li><li>Toàn bộ đường dữ liệu <strong>MUST</strong> tuân local-first (FR-TRUST-002): trích xuất và chuẩn hóa diễn ra trên máy client; chỉ dữ liệu tối thiểu hóa đi tiếp (FR-EXT-003 quyết định gửi gì về backend).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao session piggyback, không lưu token (DEC-EXT-07/08)?</strong> Định vị lõi của SănDeal hậu-Honey là \"extension không phải malware\": nó đọc giỏ của chính bạn, trong tab của chính bạn, mà không bao giờ chạm credential. <code>credentials: \"include\"</code> để trình duyệt tự gắn cookie first-party vào request <code>/api/v4/cart/get</code> - extension nhận được JSON giỏ hàng nhưng KHÔNG thấy giá trị cookie. Đây là khác biệt tinh tế nhưng sống còn: mượn phiên (qua trình duyệt) khác hoàn toàn với đánh cắp phiên (đọc cookie ra). Test phải khẳng định ranh giới này, vì một lần lỡ là thảm họa PDPL + niềm tin.</p>\n<p><strong>Vì sao cấm document.cookie tuyệt đối (§1 #3)?</strong> Chỉ cần một dòng <code>document.cookie</code> lọt vào là extension \"có khả năng\" lấy token - đủ để một audit độc lập (FR-TRUST-003) đánh trượt và đủ để người dùng nghi ngờ. Cấm về mã (grep test), không chỉ về quy ước, để cam kết là kiểm chứng được.</p>\n<p><strong>Vì sao nhiều selector dự phòng (DEC-EXT-11)?</strong> Shopee chạy A/B test liên tục; DOM giỏ hàng đổi class/cấu trúc mà không báo. Parser một-selector vỡ thầm lặng và người dùng mất tính năng mà ta không biết. Bảng selector dự phòng + ưu tiên internal JSON (ổn định hơn DOM) + phát health signal khi hỏng cho phép FR-SCRAPE-006 phát hiện và vá nhanh.</p>\n<p><strong>Vì sao ưu tiên internal JSON hơn DOM (§1 #2)?</strong> Endpoint <code>/api/v4/cart/get</code> trả cấu trúc ổn định hơn DOM render và rẻ hơn để parse. DOM là đường lui khi endpoint đổi/khóa. Hai đường cho cùng kết quả tối thiểu hóa.</p>\n<p><strong>Vì sao chỉ đọc, không sửa giỏ (DEC-EXT-12)?</strong> Tự sửa giỏ/đặt hàng là hành vi rủi ro cao (sai một bước là đặt nhầm đơn của người dùng) và dễ bị nghi tự động hóa lạm dụng - đúng loại hành vi Honey bị phạt. SănDeal chỉ đọc; mọi hành động (áp voucher) phải user-initiated và tách sang FR-CART-005 với ràng buộc riêng.</p>\n<p><strong>Vì sao xử lý chưa-đăng-nhập lịch sự (§1 #11)?</strong> Người dùng có thể bật extension khi chưa đăng nhập Shopee. Đúng đắn là báo \"chưa có dữ liệu giỏ\" - tuyệt đối không được thử đọc credential để \"giúp đăng nhập\". Hành vi này vừa hợp triết lý, vừa tránh lỗi.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Kiểu dữ liệu (types.ts bổ sung)</h3>\n<pre><code class=\"language-ts\">// extension/src/shared/types.ts (thêm)\nexport interface CartItem {\n  productId: string;   // chỉ ID hiển thị, KHÔNG kèm thông tin định danh người dùng\n  price: number;       // VND, số nguyên\n  qty: number;\n}\n\nexport interface VoucherItem {\n  code: string;        // mã voucher hiển thị\n  minSpend?: number;   // điều kiện hiển thị (nếu có)\n  discountText?: string;\n}\n\nexport interface CartReadMessage {\n  type: \"CART_READ\";\n  platform: \"shopee\";\n  items: CartItem[];\n  vouchers: VoucherItem[];\n  // KHÔNG có trường cookie/token/header\n}</code></pre>\n<h3>api-client.ts (mượn cookie, KHÔNG đọc cookie)</h3>\n<pre><code class=\"language-ts\">// extension/src/content/shopee/api-client.ts\nconst CART_ENDPOINT = \"https://shopee.vn/api/v4/cart/get\";\n\nexport async function fetchCartViaApi(): Promise&lt;CartItem[] | null&gt; {\n  const ctrl = new AbortController();\n  const t = setTimeout(() =&gt; ctrl.abort(), 25_000); // &lt;30s (MV3 ràng buộc)\n  try {\n    const res = await fetch(CART_ENDPOINT, {\n      method: \"GET\",\n      credentials: \"include\",   // trình duyệt tự gắn cookie first-party\n      signal: ctrl.signal\n    });\n    if (!res.ok) return null;   // chưa đăng nhập / endpoint đổi → fallback DOM\n    const json = await res.json();\n    return mapCart(json);       // chỉ rút productId/price/qty\n  } catch {\n    return null;                // lỗi → fallback DOM, không nuốt im lặng ở caller\n  } finally {\n    clearTimeout(t);\n  }\n  // LƯU Ý: KHÔNG có document.cookie, KHÔNG đọc Set-Cookie/Authorization ở đây.\n}</code></pre>\n<h3>cart-reader.ts (orchestrate: JSON trước, DOM sau, health signal)</h3>\n<pre><code class=\"language-ts\">// extension/src/content/shopee/cart-reader.ts\nexport async function readCart(): Promise&lt;CartReadMessage&gt; {\n  let items = await fetchCartViaApi();          // (a) ưu tiên internal JSON\n  let source: \"api\" | \"dom\" = \"api\";\n  if (items === null) {\n    items = readCartFromDom();                  // (b) fallback DOM nhiều selector\n    source = \"dom\";\n  }\n  if (items === null) {\n    reportHealth({ platform: \"shopee\", broke: \"cart\", source }); // FR-SCRAPE-006\n    items = [];\n  }\n  const vouchers = readVouchersFromDom();\n  return { type: \"CART_READ\", platform: \"shopee\", items, vouchers };\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>manifest.json</code> có <code>content_scripts</code> match <code>https://shopee.vn/*</code>, <code>run_at: document_idle</code>.</li><li>Grep toàn bộ <code>src/content/shopee/**</code> + <code>normalize.ts</code>: KHÔNG có <code>document.cookie</code>, KHÔNG đọc header <code>Set-Cookie</code>/<code>Authorization</code>, KHÔNG đọc <code>input[type=password]</code>.</li><li><code>CartReadMessage</code> chỉ chứa <code>items: CartItem[]</code> + <code>vouchers: VoucherItem[]</code>; KHÔNG có khóa nào tên cookie/token/session/auth (test introspection payload).</li><li><code>fetchCartViaApi</code> dùng <code>credentials: \"include\"</code> và có timeout &lt; 30s (AbortController).</li><li>Khi internal JSON trả 200 hợp lệ -&gt; đọc đúng item tối thiểu (productId/price/qty) từ JSON mẫu.</li><li>Khi internal JSON lỗi (non-200) -&gt; fallback đọc DOM; parse DOM mẫu trả đúng item.</li><li>Khi selector DOM chính trượt -&gt; dùng selector dự phòng; vẫn parse được item từ DOM \"biến thể A/B\".</li><li>Khi cả JSON lẫn mọi selector hỏng -&gt; phát health signal (broke: \"cart\") VÀ trả <code>items: []</code> (không ném lỗi vỡ tab).</li><li>Khi người dùng chưa đăng nhập (JSON auth-fail + DOM giỏ rỗng) -&gt; trả message rỗng lịch sự, không lỗi, không thử đọc credential.</li><li>Content script KHÔNG gọi API sửa giỏ/đặt hàng/áp voucher (grep: không có endpoint mutate).</li><li><code>npm test</code> xanh; <code>tsc --noEmit</code> sạch.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-ts\">// extension/test/shopee-no-secret-leak.test.ts\nimport { readCart } from \"../src/content/shopee/cart-reader\";\nimport { readFile } from \"fs/promises\";\n\ntest(\"payload KHÔNG chứa cookie/token\", async () =&gt; {\n  const msg = await readCart();\n  const flat = JSON.stringify(msg).toLowerCase();\n  for (const banned of [\"cookie\", \"token\", \"session\", \"authorization\", \"password\"]) {\n    expect(flat).not.toContain(banned);\n  }\n});\n\ntest(\"mã nguồn KHÔNG đọc document.cookie / password input\", async () =&gt; {\n  for (const f of [\"cart-reader\", \"voucher-reader\", \"api-client\", \"dom-selectors\", \"index\"]) {\n    const src = await readFile(`src/content/shopee/${f}.ts`, \"utf8\");\n    expect(src).not.toMatch(/document\\.cookie/);\n    expect(src).not.toMatch(/Set-Cookie|Authorization/i);\n    expect(src).not.toMatch(/input\\[type=[\"']?password/i);\n  }\n});</code></pre>\n<pre><code class=\"language-ts\">// extension/test/shopee-cart-reader.test.ts\ntest(\"đọc giỏ từ internal JSON khi 200\", async () =&gt; {\n  mockFetchOk(cartJsonFixture);                 // 3 item\n  const msg = await readCart();\n  expect(msg.items).toHaveLength(3);\n  expect(msg.items[0]).toMatchObject({ productId: \"90112\", price: 89000, qty: 1 });\n});\n\ntest(\"fallback DOM khi JSON non-200\", async () =&gt; {\n  mockFetch(403);\n  document.body.innerHTML = shopeeCartDomFixtureMain;\n  const msg = await readCart();\n  expect(msg.items.length).toBeGreaterThan(0);\n});</code></pre>\n<pre><code class=\"language-ts\">// extension/test/shopee-dom-fallback.test.ts\ntest(\"selector chính trượt → dùng selector dự phòng (A/B variant)\", () =&gt; {\n  document.body.innerHTML = shopeeCartDomFixtureVariantB; // class đổi\n  const items = readCartFromDom();\n  expect(items &amp;&amp; items.length).toBeGreaterThan(0);\n});\n\ntest(\"parse hỏng hẳn → health signal + items rỗng, không ném lỗi\", async () =&gt; {\n  mockFetch(500);\n  document.body.innerHTML = \"&lt;div&gt;không khớp gì&lt;/div&gt;\";\n  const spy = jest.fn();\n  setHealthReporter(spy);\n  const msg = await readCart();\n  expect(spy).toHaveBeenCalledWith(expect.objectContaining({ broke: \"cart\" }));\n  expect(msg.items).toEqual([]);\n});</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: cập nhật <code>manifest.json</code> (content_scripts shopee) + <code>types.ts</code> (CartItem/VoucherItem/CartReadMessage) -&gt; <code>dom-selectors.ts</code> (bảng selector dự phòng) -&gt; <code>api-client.ts</code> (fetch credentials include, no cookie read) -&gt; <code>cart-reader.ts</code> + <code>voucher-reader.ts</code> (orchestrate + health signal) -&gt; <code>normalize.ts</code> (tối thiểu hóa) -&gt; <code>index.ts</code> (entry content script) -&gt; tests. Content script gửi <code>CartReadMessage</code> cho service worker (FR-EXT-001 <code>onMessage</code>); service worker chuyển cho pipeline tối thiểu hóa (FR-EXT-003). Health signal nối vào FR-SCRAPE-006.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>FR-EXT-001</strong> - scaffold MV3 + messaging + storage phải có trước (content script gửi message cho SW).</li><li><strong>FR-EXT-003 (downstream)</strong> - pipeline tối thiểu hóa nhận <code>CartReadMessage</code>, lọc lần hai trước khi gửi backend.</li><li><strong>FR-EXT-007 / FR-EXT-008 (downstream, P2)</strong> - content script TikTok Shop / Lazada tái dùng khung reader + normalize + health signal này.</li><li><strong>FR-CART-002 / FR-CART-005 / FR-CART-006 (downstream, P2)</strong> - cart_snapshot, auto-test mã, checklist xu dựa dữ liệu giỏ đọc ở đây.</li><li><strong>FR-SCRAPE-006</strong> - DOM-change monitoring nhận health signal khi parser hỏng.</li><li><strong>FR-SCRAPE-002</strong> - adapter Shopee backend (<code>/api/v4/...</code>, <code>is_login:false</code>) song song; reader này là phía client.</li><li><strong>FR-TRUST-002 / FR-TRUST-003</strong> - local-first + audit độc lập chứng minh không gửi cookie/mật khẩu.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Message content -&gt; service worker (tối thiểu, KHÔNG credential)</h3>\n<pre><code class=\"language-json\">{\n  \"type\": \"CART_READ\",\n  \"platform\": \"shopee\",\n  \"items\": [\n    { \"productId\": \"90112\", \"price\": 89000, \"qty\": 1 },\n    { \"productId\": \"77310\", \"price\": 245000, \"qty\": 2 }\n  ],\n  \"vouchers\": [\n    { \"code\": \"FREESHIPXTRA\", \"minSpend\": 0, \"discountText\": \"Freeship đến 15k\" }\n  ]\n}</code></pre>\n<h3>Health signal khi parser hỏng (-&gt; FR-SCRAPE-006)</h3>\n<pre><code class=\"language-json\">{ \"type\": \"PARSER_HEALTH\", \"platform\": \"shopee\", \"broke\": \"cart\", \"source\": \"dom\", \"ts\": 1782000000000 }</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Có đọc thêm trang sản phẩm (PDP) ngoài giỏ hàng hay không - slice sau; slice 1 nhắm giỏ + voucher.</li><li>Ngưỡng nhịp đọc tự động (debounce theo thao tác giỏ) - tinh chỉnh ở FR-EXT-005 khi gắn đồng bộ realtime.</li><li>Chuẩn hóa voucher điều kiện phức (bậc thang) - hoãn tới FR-CART-001 (voucher_catalog) ở P2.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Đọc document.cookie / token</td><td>test no-secret-leak (grep + payload)</td><td>rò rỉ credential -&gt; thảm họa PDPL/niềm tin</td><td>Cấm về mã; chỉ credentials:'include' (DEC-EXT-08/09)</td></tr><tr><td>Thu thập mật khẩu</td><td>grep input[type=password]</td><td>vi phạm cam kết lõi</td><td>Không hook form login (DEC-EXT-08)</td></tr><tr><td>DOM A/B đổi, selector chính trượt</td><td>health signal + dom-fallback test</td><td>mất dữ liệu giỏ</td><td>Selector dự phòng + ưu tiên JSON (DEC-EXT-11)</td></tr><tr><td>Parse hỏng hẳn nuốt im lặng</td><td>thiếu health signal</td><td>mất tính năng âm thầm</td><td>Bắt buộc phát health -&gt; FR-SCRAPE-006 (§1 #7)</td></tr><tr><td>fetch internal endpoint treo &gt;30s</td><td>timeout AbortController</td><td>SW liên đới bị kill</td><td>Timeout 25s (§1 #10)</td></tr><tr><td>Người dùng chưa đăng nhập</td><td>giỏ rỗng/auth-fail</td><td>nhầm là lỗi</td><td>Báo \"chưa có dữ liệu giỏ\" lịch sự (§1 #11)</td></tr><tr><td>Tự sửa giỏ/đặt hàng</td><td>grep endpoint mutate</td><td>đặt nhầm đơn + nghi lạm dụng</td><td>Chỉ đọc (DEC-EXT-12); mutate tách FR-CART-005</td></tr><tr><td>Gửi trường thừa về backend</td><td>introspection payload</td><td>lộ dữ liệu ngoài tối thiểu</td><td>normalize.ts cắt + FR-EXT-003 lọc lần hai</td></tr><tr><td>Shopee đổi đường dẫn /api/v4</td><td>api trả non-200</td><td>mất đường JSON</td><td>Fallback DOM + cập nhật endpoint qua FR-SCRAPE-006</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Đây là bề mặt khả dụng tối thiểu của extension và là FR rủi ro niềm tin cao nhất: session piggyback đúng nghĩa = mượn cookie qua trình duyệt (<code>credentials:'include'</code>), KHÔNG đọc cookie ra.</li><li>Cấm <code>document.cookie</code> về mã (grep test) biến cam kết \"không chạm credential\" thành kiểm chứng được - tiền đề cho audit độc lập FR-TRUST-003.</li><li>Ưu tiên internal JSON hơn DOM cho ổn định; DOM là đường lui; health signal nối FR-SCRAPE-006 để vá nhanh khi A/B đổi.</li><li>Chỉ-đọc là ranh giới cứng; mọi hành động (áp voucher) phải user-initiated, tách sang FR-CART-005.</li><li>Khung reader + normalize + health signal ở đây được TikTok Shop (FR-EXT-007) và Lazada (FR-EXT-008) tái dùng - chỉ thay lớp selector/đường đọc per-sàn.</li></ul>\n<hr>\n<p><em>Hết FR-EXT-002. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
