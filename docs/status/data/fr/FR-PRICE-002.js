window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PRICE-002"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service PRICE <strong>MUST</strong> lưu chuỗi thời gian giá sản phẩm vào TimescaleDB hypertable <code>price_snapshot</code>, ghi theo nguyên tắc delta-only, nén sau 30 ngày, và phục vụ biểu đồ qua continuous aggregate. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> định nghĩa bảng <code>price_snapshot (product_id, ts, price, list_price, stock, sold, flash_sale)</code> với <code>PRIMARY KEY (product_id, ts)</code>, <code>product_id</code> REFERENCES <code>tracked_product(id)</code>.</li><li><strong>MUST</strong> biến <code>price_snapshot</code> thành hypertable: <code>create_hypertable('price_snapshot','ts', chunk_time_interval =&gt; INTERVAL '7 days')</code> (DEC-PRICE-01).</li><li><strong>MUST</strong> lưu <code>price</code> và <code>list_price</code> dạng <code>BIGINT</code> (đơn vị VND, không thập phân) - KHÔNG dùng float/numeric (DEC-PRICE-05). Sai số float trên tiền tệ là không chấp nhận được.</li><li><strong>MUST</strong> ghi delta-only (DEC-PRICE-04): trước khi INSERT, đọc snapshot gần nhất của <code>product_id</code>; chỉ ghi dòng mới khi ÍT NHẤT một trong <code>(price, list_price, stock, flash_sale)</code> thay đổi. Nếu mọi trường bằng snapshot gần nhất -&gt; bỏ qua, tăng metric <code>delta_skipped_total</code>.</li><li><strong>MUST</strong> bật nén (DEC-PRICE-02): <code>ALTER TABLE price_snapshot SET (timescaledb.compress, timescaledb.compress_segmentby='product_id')</code> + <code>add_compression_policy('price_snapshot', INTERVAL '30 days')</code>. Chunk cũ hơn 30 ngày được nén tự động.</li><li><strong>MUST</strong> tạo continuous aggregate <code>price_daily</code> (DEC-PRICE-03): bucket 1 ngày, cột <code>min_p = min(price)</code>, <code>max_p = max(price)</code>, <code>close_p = last(price, ts)</code>, GROUP BY <code>product_id, day</code>. Đăng ký refresh policy (refresh cửa sổ gần nhất mỗi giờ).</li><li><strong>MUST</strong> đặt retention policy: giữ raw <code>price_snapshot</code> 18 tháng (<code>add_retention_policy('price_snapshot', INTERVAL '18 months')</code>); continuous aggregate <code>price_daily</code> giữ vô thời hạn (lịch sử dài phục vụ dự đoán đáy giá FR-DEAL-004).</li><li><strong>MUST</strong> expose hàm repo:</li></ol>\n<ul><li><code>InsertSnapshot(ctx, snap PriceSnapshot) (written bool, err error)</code> - áp dụng delta-only; <code>written=false</code> khi bị bỏ qua.</li><li><code>QueryRange(ctx, productID int64, from, to time.Time) ([]PriceSnapshot, error)</code> - đọc raw trong khoảng.</li><li><code>QueryDaily(ctx, productID int64, from, to time.Time) ([]DailyBucket, error)</code> - đọc từ <code>price_daily</code>.</li></ul>\n<ol><li><strong>MUST</strong> đánh index hỗ trợ truy vấn theo <code>product_id</code> + thời gian (hypertable đã index <code>ts</code>; thêm index phụ nếu cần cho <code>flash_sale</code> filter).</li><li><strong>SHOULD</strong> phát OTel metric: <code>price_snapshot_written_total{platform_id}</code> (counter), <code>price_snapshot_delta_skipped_total{platform_id}</code> (counter), <code>price_query_duration_ms</code> (histogram).</li><li><strong>MUST</strong> xử lý ghi đồng thời cùng <code>(product_id, ts)</code>: dùng <code>INSERT ... ON CONFLICT (product_id, ts) DO NOTHING</code> để idempotent với retry của scraper.</li><li><strong>MUST</strong> đảm bảo <code>price &gt; 0</code> và <code>list_price IS NULL OR list_price &gt;= price</code> qua CHECK constraint (giá bán không cao hơn giá niêm yết).</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao hypertable chunk 7 ngày (DEC-PRICE-01)?</strong> <code>price_snapshot</code> là bảng lớn nhất hệ thống - hàng triệu SKU x nhiều snapshot/ngày = hàng tỷ dòng. Hypertable tự chia chunk theo thời gian: query \"90 ngày gần nhất\" chỉ chạm ~13 chunk thay vì quét toàn bảng. Chunk 7 ngày cân bằng giữa số chunk (không quá nhiều) và độ mịn của nén/retention.</p>\n<p><strong>Vì sao delta-only (DEC-PRICE-04)?</strong> Scraper quét SKU theo tần suất (flash sale: phút; SKU thường: vài giờ). Phần lớn lần quét giá KHÔNG đổi. Ghi mỗi lần quét đốt storage 10-100x vô ích. Delta-only chỉ ghi khi có thay đổi thực - vừa tiết kiệm storage (giữ unit economics §4.1 ~0,1-0,2 USD/user), vừa làm chuỗi giá \"sạch\" cho thuật toán sale ảo.</p>\n<p><strong>Vì sao price là BIGINT VND (DEC-PRICE-05)?</strong> Giá VN luôn là số nguyên đồng (không có \"0,5 đồng\"). Lưu float gây sai số tích lũy khi so sánh <code>current_price &gt;= median90 * 0.97</code> trong sale ảo. BIGINT là chính xác tuyệt đối và đủ lớn (giá tới hàng nghìn tỷ vẫn vừa int64).</p>\n<p><strong>Vì sao nén sau 30 ngày (DEC-PRICE-02)?</strong> Dữ liệu cũ hơn 30 ngày hiếm khi ghi mới, chủ yếu đọc. Nén columnar segmentby <code>product_id</code> đạt tỷ lệ nén cao (cùng product_id, giá tương quan) -&gt; giảm storage 10-20x cho phần \"đuôi\" lịch sử dài. 30 ngày để chunk \"nóng\" (đang ghi) không bị nén sớm.</p>\n<p><strong>Vì sao continuous aggregate (DEC-PRICE-03)?</strong> Biểu đồ giá (FR-WEB-003, NFR &lt;500ms) và sale ảo (FR-DEAL-001) không cần độ phân giải giây - cần min/max/close theo ngày. Tính realtime từ raw mỗi lần render là chậm. Continuous aggregate tính sẵn, refresh tăng dần -&gt; query biểu đồ chỉ đọc bảng nhỏ đã tổng hợp.</p>\n<p><strong>Vì sao retention raw 18 tháng nhưng aggregate vô hạn (§1 #7)?</strong> Raw giây/phút chỉ hữu ích gần đây (sale ảo cần 90 ngày). Aggregate ngày nhẹ và là input cho dự đoán đáy giá LightGBM (FR-DEAL-005, cần &gt;=180 ngày). Giữ aggregate dài, vứt raw cũ - cân bằng chi phí vs giá trị.</p>\n<p><strong>Vì sao ON CONFLICT DO NOTHING (§1 #11)?</strong> Scraper có retry; cùng <code>(product_id, ts)</code> có thể được gửi 2 lần. PK + DO NOTHING làm INSERT idempotent, không vỡ khi trùng.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migrations</h3>\n<pre><code class=\"language-sql\">-- services/price/migrations/0002_price_snapshot.sql\nCREATE TABLE price_snapshot (\n  product_id   BIGINT      NOT NULL REFERENCES tracked_product(id),\n  ts           TIMESTAMPTZ NOT NULL,\n  price        BIGINT      NOT NULL CHECK (price &gt; 0),          -- VND, không thập phân\n  list_price   BIGINT      CHECK (list_price IS NULL OR list_price &gt;= price),\n  stock        INTEGER,\n  sold         INTEGER,\n  flash_sale   BOOLEAN     NOT NULL DEFAULT false,\n  PRIMARY KEY (product_id, ts)\n);\n\nSELECT create_hypertable('price_snapshot', 'ts',\n  chunk_time_interval =&gt; INTERVAL '7 days');\n\nCREATE INDEX idx_ps_flash ON price_snapshot (product_id, ts DESC)\n  WHERE flash_sale = true;\n\n-- services/price/migrations/0003_price_daily_cagg.sql\nCREATE MATERIALIZED VIEW price_daily\n  WITH (timescaledb.continuous) AS\n  SELECT product_id,\n         time_bucket('1 day', ts) AS day,\n         min(price)      AS min_p,\n         max(price)      AS max_p,\n         last(price, ts) AS close_p\n  FROM price_snapshot\n  GROUP BY product_id, day\n  WITH NO DATA;\n\nSELECT add_continuous_aggregate_policy('price_daily',\n  start_offset =&gt; INTERVAL '3 days',\n  end_offset   =&gt; INTERVAL '1 hour',\n  schedule_interval =&gt; INTERVAL '1 hour');\n\n-- services/price/migrations/0004_compression_policy.sql\nALTER TABLE price_snapshot SET (\n  timescaledb.compress,\n  timescaledb.compress_segmentby = 'product_id'\n);\nSELECT add_compression_policy('price_snapshot', INTERVAL '30 days');\nSELECT add_retention_policy('price_snapshot',  INTERVAL '18 months');</code></pre>\n<h3>Types (Go)</h3>\n<pre><code class=\"language-go\">// services/price/internal/price/types.go\ntype PriceSnapshot struct {\n    ProductID  int64     `db:\"product_id\"`\n    TS         time.Time `db:\"ts\"`\n    Price      int64     `db:\"price\"`       // VND\n    ListPrice  *int64    `db:\"list_price\"`\n    Stock      *int32    `db:\"stock\"`\n    Sold       *int32    `db:\"sold\"`\n    FlashSale  bool      `db:\"flash_sale\"`\n}\n\ntype DailyBucket struct {\n    ProductID int64     `db:\"product_id\"`\n    Day       time.Time `db:\"day\"`\n    MinP      int64     `db:\"min_p\"`\n    MaxP      int64     `db:\"max_p\"`\n    CloseP    int64     `db:\"close_p\"`\n}</code></pre>\n<h3>Delta-only (§1 #4)</h3>\n<pre><code class=\"language-go\">// services/price/internal/price/delta.go\n// InsertSnapshot áp dụng delta-only: chỉ ghi khi khác snapshot gần nhất.\nfunc (r *Repo) InsertSnapshot(ctx context.Context, s PriceSnapshot) (bool, error) {\n    last, err := r.latest(ctx, s.ProductID)\n    if err != nil &amp;&amp; !errors.Is(err, pgx.ErrNoRows) {\n        return false, err\n    }\n    if err == nil &amp;&amp; !changed(last, s) {\n        metrics.DeltaSkipped(s.ProductID)\n        return false, nil // không đổi → bỏ qua\n    }\n    _, err = r.pool.Exec(ctx,\n        `INSERT INTO price_snapshot (product_id, ts, price, list_price, stock, sold, flash_sale)\n         VALUES ($1,$2,$3,$4,$5,$6,$7)\n         ON CONFLICT (product_id, ts) DO NOTHING`,\n        s.ProductID, s.TS, s.Price, s.ListPrice, s.Stock, s.Sold, s.FlashSale)\n    if err != nil {\n        return false, err\n    }\n    metrics.SnapshotWritten(s.ProductID)\n    return true, nil\n}\n\nfunc changed(a, b PriceSnapshot) bool {\n    return a.Price != b.Price ||\n        !eqPtr(a.ListPrice, b.ListPrice) ||\n        a.FlashSale != b.FlashSale ||\n        !eqPtr32(a.Stock, b.Stock)\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Migration chạy sạch -&gt; <code>price_snapshot</code> tồn tại VÀ là hypertable (<code>SELECT * FROM timescaledb_information.hypertables WHERE hypertable_name='price_snapshot'</code> trả 1 dòng).</li><li><code>chunk_time_interval</code> = 7 ngày (kiểm qua <code>timescaledb_information.dimensions</code>).</li><li>INSERT snapshot mới (product chưa có dữ liệu) -&gt; <code>written=true</code>, 1 dòng trong bảng.</li><li>INSERT snapshot trùng giá/stock/flash với dòng gần nhất -&gt; <code>written=false</code>, KHÔNG thêm dòng.</li><li>INSERT khi <code>price</code> đổi -&gt; <code>written=true</code>, thêm 1 dòng.</li><li>INSERT khi <code>flash_sale</code> flip (false-&gt;true) cùng giá -&gt; <code>written=true</code> (flash là tín hiệu có ý nghĩa).</li><li>INSERT <code>price &lt;= 0</code> -&gt; lỗi CHECK constraint.</li><li>INSERT <code>list_price &lt; price</code> -&gt; lỗi CHECK constraint.</li><li>INSERT trùng <code>(product_id, ts)</code> 2 lần -&gt; DO NOTHING, không lỗi, 1 dòng.</li><li><code>QueryRange(productID, now-90d, now)</code> trả đúng các snapshot trong khoảng, sắp theo <code>ts</code>.</li><li>Continuous aggregate <code>price_daily</code> tồn tại; sau refresh, <code>QueryDaily</code> trả min/max/close đúng theo ngày.</li><li>Sau khi nén thủ công 1 chunk cũ (<code>compress_chunk</code>), query vẫn trả đúng dữ liệu (nén trong suốt với đọc).</li><li>Metric <code>price_snapshot_written_total</code> tăng khi <code>written=true</code>; <code>delta_skipped_total</code> tăng khi bỏ qua.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/price/internal/price/delta_test.go\nfunc TestDelta_NoChange_Skips(t *testing.T) {\n    r, pid := setupWithProduct(t)\n    s := PriceSnapshot{ProductID: pid, TS: t0, Price: 100_000}\n    w1, _ := r.InsertSnapshot(ctx, s)\n    require.True(t, w1)\n\n    s2 := PriceSnapshot{ProductID: pid, TS: t0.Add(time.Hour), Price: 100_000}\n    w2, _ := r.InsertSnapshot(ctx, s2)\n    require.False(t, w2) // giá không đổi → bỏ qua\n\n    n := countRows(t, r, pid)\n    require.Equal(t, 1, n)\n}\n\nfunc TestDelta_PriceChange_Writes(t *testing.T) {\n    r, pid := setupWithProduct(t)\n    r.InsertSnapshot(ctx, PriceSnapshot{ProductID: pid, TS: t0, Price: 100_000})\n    w, _ := r.InsertSnapshot(ctx, PriceSnapshot{ProductID: pid, TS: t0.Add(time.Hour), Price: 89_000})\n    require.True(t, w)\n    require.Equal(t, 2, countRows(t, r, pid))\n}\n\nfunc TestDelta_FlashFlip_Writes(t *testing.T) {\n    r, pid := setupWithProduct(t)\n    r.InsertSnapshot(ctx, PriceSnapshot{ProductID: pid, TS: t0, Price: 100_000, FlashSale: false})\n    w, _ := r.InsertSnapshot(ctx, PriceSnapshot{ProductID: pid, TS: t0.Add(time.Minute), Price: 100_000, FlashSale: true})\n    require.True(t, w) // flash flip là tín hiệu, dù giá bằng\n}\n\nfunc TestHypertable_Exists(t *testing.T) {\n    r, _ := setupWithProduct(t)\n    var n int\n    r.pool.QueryRow(ctx,\n        `SELECT count(*) FROM timescaledb_information.hypertables\n         WHERE hypertable_name='price_snapshot'`).Scan(&amp;n)\n    require.Equal(t, 1, n)\n}\n\nfunc TestQueryRange_90d(t *testing.T) {\n    r, pid := setupWithProduct(t)\n    seedDailyPrices(t, r, pid, 120) // 120 ngày\n    rows, _ := r.QueryRange(ctx, pid, time.Now().AddDate(0,0,-90), time.Now())\n    require.LessOrEqual(t, len(rows), 90)\n    require.True(t, sortedByTS(rows))\n}\n\nfunc TestCheck_PriceNonPositive(t *testing.T) {\n    r, pid := setupWithProduct(t)\n    _, err := r.InsertSnapshot(ctx, PriceSnapshot{ProductID: pid, TS: t0, Price: 0})\n    require.Error(t, err) // CHECK price &gt; 0\n}\n\nfunc TestConflict_Idempotent(t *testing.T) {\n    r, pid := setupWithProduct(t)\n    s := PriceSnapshot{ProductID: pid, TS: t0, Price: 100_000}\n    r.InsertSnapshot(ctx, s)\n    r.InsertSnapshot(ctx, s) // trùng PK → DO NOTHING\n    require.Equal(t, 1, countRows(t, r, pid))\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration 0002 (bảng + hypertable) -&gt; 0003 (cagg) -&gt; 0004 (compress + retention) -&gt; repo.go + delta.go -&gt; tests. Migration chạy qua <code>golang-migrate</code> hoặc <code>tern</code>. Continuous aggregate phải tạo <code>WITH NO DATA</code> rồi để policy backfill (tránh khóa dài lúc deploy trên bảng lớn).</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>FR-PRICE-001</strong> - <code>tracked_product</code> phải tồn tại trước (FK <code>product_id</code>).</li><li><strong>FR-SCRAPE-005 (downstream)</strong> - scraper gọi <code>InsertSnapshot</code> với delta-only.</li><li><strong>FR-DEAL-001 (downstream)</strong> - sale ảo đọc <code>QueryRange</code> 90 ngày.</li><li><strong>FR-WEB-003 / FR-DEAL-003 (downstream)</strong> - biểu đồ đọc <code>price_daily</code>.</li><li>Extension/lib: TimescaleDB 2.x (compression, continuous aggregate, retention); driver <code>pgx</code>.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Scraper ghi snapshot (nội bộ)</h3>\n<pre><code class=\"language-go\">written, err := priceRepo.InsertSnapshot(ctx, price.PriceSnapshot{\n    ProductID: 90112,\n    TS:        time.Now(),\n    Price:     89_000,        // VND\n    ListPrice: ptr(int64(149_000)),\n    Stock:     ptr(int32(37)),\n    Sold:      ptr(int32(1240)),\n    FlashSale: true,\n})\n// written=true nếu giá đổi so với lần quét trước; false nếu bỏ qua (delta-only)</code></pre>\n<h3>Đọc lịch sử 90 ngày (cho biểu đồ / sale ảo)</h3>\n<pre><code class=\"language-sql\">SELECT day, min_p, max_p, close_p\nFROM price_daily\nWHERE product_id = 90112\n  AND day &gt;= now() - INTERVAL '90 days'\nORDER BY day;</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Phân vùng theo <code>platform_id</code> ngoài <code>product_id</code> cho nén - slice sau nếu skew.</li><li>Downsampling raw (giữ phút trong 7 ngày, giờ trong 30 ngày) - tối ưu storage giai đoạn sau.</li><li>Multi-currency (THB/IDR khi mở SEA) - gắn <code>currency</code> vào <code>tracked_product</code>, giữ <code>price</code> BIGINT theo minor unit của nước.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>FK product_id không tồn tại</td><td>lỗi pgx</td><td>400/skip</td><td>Scraper tạo tracked_product trước (FR-PRICE-001)</td></tr><tr><td>price &lt;= 0</td><td>DB CHECK</td><td>từ chối ghi</td><td>Scraper validate trước khi gửi</td></tr><tr><td>list_price &lt; price</td><td>DB CHECK</td><td>từ chối ghi</td><td>Sửa nguồn parse (có thể đảo cột)</td></tr><tr><td>Trùng (product_id, ts)</td><td>ON CONFLICT</td><td>DO NOTHING</td><td>Theo thiết kế (idempotent retry)</td></tr><tr><td>Cagg refresh chậm/đọng</td><td>TS policy log</td><td>biểu đồ trễ vài giờ</td><td>Refresh thủ công cửa sổ; chỉnh schedule_interval</td></tr><tr><td>Nén làm chậm ghi vào chunk cũ</td><td>hiếm (chunk cũ ít ghi)</td><td>INSERT vào chunk nén -&gt; giải nén ngầm</td><td>Chấp nhận; chunk nóng không bị nén (30d)</td></tr><tr><td>Storage time-series phình</td><td>dashboard Grafana</td><td>chi phí tăng</td><td>Kiểm tra retention policy chạy; delta-only hoạt động</td></tr><tr><td>Delta-only bỏ sót thay đổi (so sánh thiếu trường)</td><td>property test</td><td>mất tín hiệu giá</td><td><code>changed()</code> so đủ price/list_price/stock/flash</td></tr><tr><td>Đọc range lớn (&gt;1 năm raw)</td><td>p95 query metric</td><td>chậm</td><td>Chuyển sang price_daily cho khoảng dài</td></tr><tr><td>Migration cagg khóa bảng lớn</td><td>deploy treo</td><td>downtime</td><td>Tạo WITH NO DATA + backfill qua policy</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li><code>price_snapshot</code> là bảng nền tảng - mọi tính năng lõi (sale ảo, biểu đồ, dự đoán đáy, so sánh chéo, B2B data) đọc từ đây hoặc từ <code>price_daily</code>.</li><li>Delta-only là đòn bẩy unit economics lớn nhất phía storage: phần lớn lần quét giá không đổi nên không ghi.</li><li>BIGINT VND tránh hoàn toàn sai số float trên phép so sánh phần trăm của thuật toán sale ảo.</li><li>Continuous aggregate tách \"độ phân giải ghi\" (giây/phút) khỏi \"độ phân giải đọc biểu đồ\" (ngày) - đọc nhanh mà vẫn giữ raw chi tiết.</li><li>Retention 18 tháng raw + aggregate vô hạn cân bằng chi phí với nhu cầu lịch sử dài của ML dự đoán đáy.</li><li>Khi mở SEA, gắn currency vào tracked_product; price vẫn BIGINT theo minor unit từng nước.</li></ul>\n<hr>\n<p><em>Hết FR-PRICE-002. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
