window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DEAL-001"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service DEAL <strong>MUST</strong> cung cấp một hàm thuần phân loại trạng thái giảm giá của một sản phẩm dựa trên 90 ngày lịch sử giá đọc từ <code>price_snapshot</code> (FR-PRICE-002). Hàm hiện thực đúng pseudo-code §3.5(1), không thêm bớt nhánh. Hợp đồng:</p>\n<ol><li><strong>MUST</strong> nạp lịch sử giá <code>hist</code> của <code>product_id</code> trong cửa sổ 90 ngày từ <code>price_snapshot</code> qua price repo (<code>QueryRange</code>), lấy cột <code>price</code> thành chuỗi <code>[]int64</code> (DEC-DEAL-05).</li><li><strong>MUST</strong> nếu <code>len(hist) &lt; 14</code> thì trả <code>UNKNOWN</code> ngay (cold-start) và KHÔNG tính tiếp - đây là điểm bàn giao cho FR-DEAL-002 (DEC-DEAL-02).</li><li><strong>MUST</strong> tính <code>median90</code> = percentile thứ 50 của <code>hist.price</code> theo phương pháp nearest-rank.</li><li><strong>MUST</strong> tính <code>p10</code> = percentile thứ 10 của <code>hist.price</code> theo cùng phương pháp.</li><li><strong>MUST</strong> tính <code>trailing_min</code> = giá trị nhỏ nhất trong <code>hist.price</code>.</li><li><strong>MUST</strong> tính <code>inflated</code> = (<code>list_price &gt; median90 * 1.15</code>): giá niêm yết bị thổi quá 15 phần trăm trên trung vị 90 ngày (DEC-DEAL-01).</li><li><strong>MUST</strong> tính <code>not_real_discount</code> = (<code>current_price &gt;= median90 * 0.97</code>): giá bán hiện tại vẫn nằm trong 3 phần trăm quanh trung vị, tức không giảm thật (DEC-DEAL-01).</li><li><strong>MUST</strong> trả <code>SALE_AO</code> khi <code>inflated AND not_real_discount</code> (giá gốc bị thổi và giá bán không giảm thật).</li><li><strong>MUST</strong> trả <code>SALE_XIN</code> khi <code>current_price &lt;= p10 AND current_price &lt;= trailing_min * 1.02</code> (chạm vùng 10 phần trăm thấp nhất và sát đáy lịch sử trong biên 2 phần trăm).</li><li><strong>MUST</strong> trả <code>TAM_DUOC</code> cho mọi trường hợp còn lại (không phải sale ảo, cũng chưa phải sale xịn).</li><li><strong>MUST</strong> thực hiện mọi phép so ngưỡng bằng integer-safe math trên <code>int64</code> (ví dụ <code>current*100 &gt;= median90*97</code>, <code>list*100 &gt; median90*115</code>, <code>current*100 &lt;= trailingMin*102</code>), KHÔNG dùng float (DEC-DEAL-03), nhất quán với BIGINT VND của FR-PRICE-002.</li><li><strong>MUST</strong> là hàm thuần và tất định: cùng một chuỗi <code>hist</code> cùng <code>current</code>, <code>list</code> luôn cho cùng verdict, không phụ thuộc thời gian gọi hay trạng thái ngoài.</li><li><strong>MUST</strong> trả kiểu <code>Verdict</code> là enum đóng gồm đúng bốn giá trị <code>SALE_AO</code>, <code>SALE_XIN</code>, <code>TAM_DUOC</code>, <code>UNKNOWN</code> (DEC-DEAL-04). Verdict này là đầu vào cho luật cảnh báo sale thật của FR-TRACK-003.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao đúng ba ngưỡng 1.15 / 0.97 / 1.02 (DEC-DEAL-01)?</strong> Chiêu sale ảo kinh điển trên sàn VN là thổi <code>list_price</code> rồi gắn nhãn \"-50%\", trong khi giá bán thực gần như đứng yên so với 90 ngày. Ngưỡng <code>list_price &gt; median90 * 1.15</code> bắt đúng phần \"giá gốc bị thổi\", còn <code>current_price &gt;= median90 * 0.97</code> bắt phần \"giảm giả\". Hai điều kiện phải xảy ra cùng lúc mới kết luận sale ảo, tránh báo nhầm khi sàn chỉ niêm yết giá gốc cao theo thói quen. Ngưỡng <code>trailing_min * 1.02</code> cho sale xịn nói rằng giá hiện tại phải sát đáy lịch sử, không chỉ thấp tương đối. Ba con số này chốt từ §3.5 và là hằng load-bearing: đổi chúng là đổi định nghĩa sản phẩm, phải sửa kèm đặc tả và audit.</p>\n<p><strong>Vì sao dưới 14 ngày trả UNKNOWN thay vì đoán (DEC-DEAL-02)?</strong> Với ít hơn 14 điểm, trung vị và percentile không đại diện cho mặt bằng giá thật, dễ cho kết luận sai và làm người dùng mất tin. Trả <code>UNKNOWN</code> là một câu trả lời trung thực, đồng thời là tín hiệu bàn giao rõ ràng để FR-DEAL-002 xử lý cold-start bằng nguồn khác (giá danh mục, sản phẩm tương tự). Đoán bừa lúc thiếu dữ liệu là rủi ro lớn hơn nhiều so với việc thừa nhận chưa đủ cơ sở.</p>\n<p><strong>Vì sao integer-safe math trên tiền tệ (DEC-DEAL-03)?</strong> Giá VN luôn là số nguyên đồng và FR-PRICE-002 (DEC-PRICE-05) lưu <code>price</code> dạng BIGINT. Nếu nhân với <code>0.97</code> kiểu float, sai số dấu phẩy động xuất hiện đúng tại các biên so sánh phần trăm, làm cùng một chuỗi giá có thể cho verdict khác nhau giữa các kiến trúc hoặc bản dựng. Nhân chéo bằng số nguyên (<code>current*100 &gt;= median90*97</code>) cho kết quả chính xác tuyệt đối và tất định, giữ đúng cam kết §1 #12.</p>\n<p><strong>Vì sao dùng trung vị (median90) chứ không phải trung bình?</strong> Trung vị ít nhạy với ngoại lai: một vài lần giá nhảy bất thường (lỗi parse, flash sale chớp nhoáng) không kéo lệch mặt bằng. Trung bình bị các giá trị cực đoan kéo đi, làm ngưỡng <code>median90 * 1.15</code> và <code>median90 * 0.97</code> mất ý nghĩa. Với chuỗi giá có nhiễu, trung vị là mốc tham chiếu ổn định hơn cho cả hai vế của phép phân loại.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Types (Go)</h3>\n<pre><code class=\"language-go\">// services/deal/internal/fakesale/types.go\npackage fakesale\n\n// Verdict là enum đóng cho kết quả phân loại sale ảo (DEC-DEAL-04).\ntype Verdict string\n\nconst (\n    SaleAo   Verdict = \"SALE_AO\"   // giá gốc bị thổi, giảm giả\n    SaleXin  Verdict = \"SALE_XIN\"  // giảm thật, sát đáy lịch sử\n    TamDuoc  Verdict = \"TAM_DUOC\"  // không ảo, cũng chưa phải sale xịn\n    Unknown  Verdict = \"UNKNOWN\"   // dưới 14 ngày dữ liệu (cold-start)\n)\n\nconst minHistoryPoints = 14</code></pre>\n<h3>Helper thống kê (Go)</h3>\n<pre><code class=\"language-go\">// services/deal/internal/fakesale/stats.go\npackage fakesale\n\nimport \"sort\"\n\n// percentile trả phân vị thứ p (0..100) theo nearest-rank trên chuỗi giá int64.\n// Sao chép trước khi sort để giữ hàm thuần, không sửa slice đầu vào.\nfunc percentile(hist []int64, p int) int64 {\n    n := len(hist)\n    s := make([]int64, n)\n    copy(s, hist)\n    sort.Slice(s, func(i, j int) bool { return s[i] &lt; s[j] })\n    // rank nearest-rank: ceil(p/100 * n), kẹp trong [1, n].\n    rank := (p*n + 99) / 100\n    if rank &lt; 1 {\n        rank = 1\n    }\n    if rank &gt; n {\n        rank = n\n    }\n    return s[rank-1]\n}\n\nfunc minInt64(hist []int64) int64 {\n    m := hist[0]\n    for _, v := range hist[1:] {\n        if v &lt; m {\n            m = v\n        }\n    }\n    return m\n}</code></pre>\n<h3>Hàm phát hiện (Go)</h3>\n<pre><code class=\"language-go\">// services/deal/internal/fakesale/detect.go\npackage fakesale\n\n// DetectFakeSale phân loại trạng thái giảm giá theo §3.5(1).\n// Hàm thuần, tất định: cùng (hist, current, list) luôn cho cùng Verdict.\n// Mọi so ngưỡng dùng integer-safe math trên int64 (DEC-DEAL-03).\nfunc DetectFakeSale(hist []int64, current, list int64) Verdict {\n    if len(hist) &lt; minHistoryPoints {\n        return Unknown // cold-start, bàn giao FR-DEAL-002\n    }\n    median90 := percentile(hist, 50)\n    p10 := percentile(hist, 10)\n    trailingMin := minInt64(hist)\n\n    // inflated: list_price &gt; median90 * 1.15  -&gt;  list*100 &gt; median90*115\n    inflated := list*100 &gt; median90*115\n    // not_real_discount: current_price &gt;= median90 * 0.97  -&gt;  current*100 &gt;= median90*97\n    notRealDiscount := current*100 &gt;= median90*97\n\n    if inflated &amp;&amp; notRealDiscount {\n        return SaleAo\n    }\n    // SALE_XIN: current &lt;= p10 AND current &lt;= trailing_min * 1.02\n    if current &lt;= p10 &amp;&amp; current*100 &lt;= trailingMin*102 {\n        return SaleXin\n    }\n    return TamDuoc\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>DetectFakeSale</code> nạp được chuỗi <code>price</code> 90 ngày từ <code>QueryRange</code> của price repo (kiểm qua mock repo trong service test).</li><li><code>len(hist) &lt; 14</code> -&gt; trả <code>UNKNOWN</code>, không tính median/p10/min.</li><li><code>median90</code> bằng đúng percentile thứ 50 nearest-rank của chuỗi đầu vào.</li><li><code>p10</code> bằng đúng percentile thứ 10 nearest-rank của chuỗi đầu vào.</li><li><code>trailing_min</code> bằng đúng <code>min</code> của chuỗi đầu vào.</li><li><code>inflated</code> đúng true khi và chỉ khi <code>list*100 &gt; median90*115</code>.</li><li><code>not_real_discount</code> đúng true khi và chỉ khi <code>current*100 &gt;= median90*97</code>.</li><li><code>inflated AND not_real_discount</code> -&gt; trả <code>SALE_AO</code>.</li><li><code>current &lt;= p10 AND current*100 &lt;= trailingMin*102</code> -&gt; trả <code>SALE_XIN</code>.</li><li>Mọi trường hợp còn lại -&gt; trả <code>TAM_DUOC</code>.</li><li>Không có float trong đường tính ngưỡng (kiểm qua đọc mã: chỉ phép <code>int64</code>); cùng chuỗi cho verdict ổn định qua nhiều lần chạy.</li><li>Hàm tất định: gọi hai lần cùng đầu vào cho cùng kết quả; không sửa slice <code>hist</code> đầu vào.</li><li>Kiểu trả về là <code>Verdict</code> và chỉ nhận một trong bốn hằng đã định nghĩa.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/deal/internal/fakesale/detect_test.go\npackage fakesale\n\nimport \"testing\"\n\n// flat tạo chuỗi n điểm cùng giá v (mặt bằng giá ổn định).\nfunc flat(v int64, n int) []int64 {\n    h := make([]int64, n)\n    for i := range h {\n        h[i] = v\n    }\n    return h\n}\n\n// mixed tạo chuỗi 81 điểm ở giá hi + 9 điểm ở giá lo: median90 = hi\n// nhưng p10 = trailing_min = lo (đáy lịch sử nằm thấp hơn mặt bằng).\nfunc mixed(hi, lo int64) []int64 {\n    return append(flat(hi, 81), flat(lo, 9)...)\n}\n\nfunc TestDetect_ColdStart_Unknown(t *testing.T) {\n    if got := DetectFakeSale(flat(100_000, 13), 50_000, 200_000); got != Unknown {\n        t.Fatalf(\"13 điểm phải UNKNOWN, được %s\", got)\n    }\n}\n\nfunc TestDetect_InflatedListPrice_SaleAo(t *testing.T) {\n    hist := flat(100_000, 90) // median90 = 100_000\n    // list 149_000 &gt; 115_000 (thổi) ; current 120_000 &gt;= 97_000 (giảm giả)\n    if got := DetectFakeSale(hist, 120_000, 149_000); got != SaleAo {\n        t.Fatalf(\"phải SALE_AO, được %s\", got)\n    }\n}\n\nfunc TestDetect_GenuineLow_SaleXin(t *testing.T) {\n    hist := append(flat(100_000, 89), 80_000) // trailing_min = 80_000, p10 thấp\n    // current 80_000 &lt;= p10 và 80_000*100 &lt;= 80_000*102 -&gt; sát đáy\n    if got := DetectFakeSale(hist, 80_000, 100_000); got != SaleXin {\n        t.Fatalf(\"phải SALE_XIN, được %s\", got)\n    }\n}\n\nfunc TestDetect_Middle_TamDuoc(t *testing.T) {\n    // median90 = 100_000, đáy lịch sử = 60_000. current 90_000 dưới biên 0.97\n    // (không ảo) nhưng vẫn cao hơn p10/đáy -&gt; chưa phải sale xịn -&gt; TAM_DUOC.\n    hist := mixed(100_000, 60_000)\n    if got := DetectFakeSale(hist, 90_000, 100_000); got != TamDuoc {\n        t.Fatalf(\"phải TAM_DUOC, được %s\", got)\n    }\n}\n\nfunc TestDetect_BoundaryThresholds(t *testing.T) {\n    // median90 = 100_000, p10 = trailing_min = 60_000 (đáy thấp hơn mặt bằng).\n    hist := mixed(100_000, 60_000)\n    cases := []struct {\n        name          string\n        current, list int64\n        want          Verdict\n    }{\n        // list*100 == median90*115 (bằng, không lớn hơn) -&gt; không inflated -&gt; không SALE_AO;\n        // current cao hơn p10 -&gt; không sale xịn -&gt; TAM_DUOC.\n        {\"list_bang_115\", 100_000, 115_000, TamDuoc},\n        // list*100 &gt; median90*115 và current == median90*0.97 đúng biên -&gt; SALE_AO.\n        {\"current_bang_97\", 97_000, 116_000, SaleAo},\n        // current ngay dưới biên 0.97 -&gt; không not_real_discount, lại cao hơn p10 -&gt; TAM_DUOC.\n        {\"current_duoi_97\", 96_999, 116_000, TamDuoc},\n    }\n    for _, c := range cases {\n        t.Run(c.name, func(t *testing.T) {\n            if got := DetectFakeSale(hist, c.current, c.list); got != c.want {\n                t.Fatalf(\"%s: muốn %s, được %s\", c.name, c.want, got)\n            }\n        })\n    }\n}\n\nfunc TestDetect_SaleXin_BoundaryAtFloor(t *testing.T) {\n    // Chuỗi 90 điểm: một đáy duy nhất 50_000, mười hai điểm 78_000, còn lại 100_000.\n    // -&gt; trailing_min = 50_000, p10 = 78_000 (đáy thấp hơn p10 để biên 1.02 thành ràng buộc).\n    hist := append(append([]int64{50_000}, flat(78_000, 12)...), flat(100_000, 77)...)\n    // current == trailing_min*1.02 = 51_000 đúng biên, và 51_000 &lt;= p10 (78_000) -&gt; SALE_XIN.\n    if got := DetectFakeSale(hist, 51_000, 100_000); got != SaleXin {\n        t.Fatalf(\"biên 1.02 phải SALE_XIN, được %s\", got)\n    }\n    // Ngay trên biên (51_001) -&gt; vượt trailing_min*1.02 -&gt; không còn SALE_XIN.\n    if got := DetectFakeSale(hist, 51_001, 100_000); got == SaleXin {\n        t.Fatal(\"vượt biên 1.02 không được là SALE_XIN\")\n    }\n}\n\nfunc TestPercentile_NearestRank(t *testing.T) {\n    h := []int64{10, 20, 30, 40, 50}\n    cases := []struct {\n        p    int\n        want int64\n    }{\n        {0, 10}, {10, 10}, {50, 30}, {100, 50},\n    }\n    for _, c := range cases {\n        if got := percentile(h, c.p); got != c.want {\n            t.Fatalf(\"p%d: muốn %d, được %d\", c.p, c.want, got)\n        }\n    }\n    // percentile không được sửa slice đầu vào\n    if h[0] != 10 || h[4] != 50 {\n        t.Fatal(\"percentile đã sửa slice đầu vào\")\n    }\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: <code>types.go</code> (enum Verdict) -&gt; <code>stats.go</code> (percentile + minInt64) -&gt; <code>detect.go</code> (DetectFakeSale) -&gt; <code>detect_test.go</code>. Sau khi gói <code>fakesale</code> xanh, nối vào <code>service.go</code>: nạp <code>QueryRange(productID, now-90d, now)</code> từ price repo (FR-PRICE-002), rút cột <code>price</code> thành <code>[]int64</code>, gọi <code>DetectFakeSale</code>, phát OTel counter <code>fake_sale_verdict_total{verdict}</code>. Gói <code>fakesale</code> không phụ thuộc DB - chỉ là hàm thuần trên chuỗi - nên test chạy nhanh, không cần TimescaleDB.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>FR-PRICE-002</strong> - nguồn 90 ngày lịch sử giá (<code>price_snapshot.price</code>, BIGINT VND) qua <code>QueryRange</code>. Hằng integer-safe math đồng bộ DEC-PRICE-05.</li><li><strong>FR-DEAL-002 (downstream, blocks)</strong> - mở rộng nhánh cold-start: khi <code>DetectFakeSale</code> trả <code>UNKNOWN</code>, FR-DEAL-002 dùng nguồn thay thế.</li><li><strong>FR-TRACK-003 (consumer)</strong> - luật cảnh báo <code>real_sale</code> tiêu thụ verdict (<code>SALE_XIN</code> kích cảnh báo sale thật).</li><li><strong>FR-DEAL-003 (downstream)</strong> - hiển thị nhãn verdict kèm biểu đồ giá.</li><li>Thư viện: chỉ <code>sort</code> chuẩn của Go; không phụ thuộc ngoài.</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>Ví dụ SALE_AO (giá gốc bị thổi)</h3>\n<p>Lịch sử 90 điểm với mặt bằng quanh <code>100000</code> đồng nên <code>median90 ~ 100000</code>. Sản phẩm gắn nhãn giảm sâu:</p>\n<ul><li><code>list_price = 149000</code> -&gt; <code>149000 * 100 = 14900000 &gt; 100000 * 115 = 11500000</code> =&gt; <code>inflated = true</code> (giá gốc thổi hơn 15 phần trăm).</li><li><code>current_price = 120000</code> -&gt; <code>120000 * 100 = 12000000 &gt;= 100000 * 97 = 9700000</code> =&gt; <code>not_real_discount = true</code> (giá bán vẫn trên mặt bằng).</li><li><code>inflated AND not_real_discount</code> =&gt; verdict = <code>SALE_AO</code>.</li></ul>\n<h3>Ví dụ SALE_XIN (giảm thật, sát đáy)</h3>\n<p>Lịch sử 90 điểm chủ yếu quanh <code>100000</code> nhưng có một đợt chạm <code>80000</code>, nên <code>trailing_min = 80000</code> và <code>p10 = 80000</code>:</p>\n<ul><li><code>current_price = 80000</code> -&gt; <code>80000 &lt;= p10 (80000)</code> đúng, và <code>80000 * 100 = 8000000 &lt;= 80000 * 102 = 8160000</code> đúng (sát đáy trong biên 2 phần trăm).</li><li>=&gt; verdict = <code>SALE_XIN</code>. FR-TRACK-003 bắn cảnh báo sale thật.</li></ul>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Ngưỡng theo từng ngành hàng (thời trang biến động khác điện máy) - hoãn sang FR-DEAL-004/005 (ML), giai đoạn này dùng ba hằng cố định toàn cục.</li><li>Trọng số theo thời gian (giá gần đây quan trọng hơn) - cân nhắc weighted percentile ở slice sau.</li><li>Phát hiện chuỗi giá có khoảng trống dài (sản phẩm ngừng bán rồi bán lại) - xử lý ở tầng nạp dữ liệu trước khi gọi hàm.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Lịch sử có khoảng trống (gap nhiều ngày)</td><td>đếm điểm khi nạp</td><td>percentile lệch theo mật độ điểm</td><td>Chuẩn hóa/lấy mẫu đều ở tầng nạp trước khi gọi hàm</td></tr><tr><td><code>len(hist) &lt; 14</code></td><td>kiểm tra đầu hàm</td><td>thiếu cơ sở thống kê</td><td>Trả UNKNOWN, bàn giao FR-DEAL-002 (theo thiết kế)</td></tr><tr><td><code>list_price</code> null hoặc 0</td><td>tầng service trước khi gọi</td><td><code>inflated</code> tính sai (so với 0)</td><td>Bỏ qua nhánh inflated hoặc đặt list = current ở tầng gọi; ghi rõ ở service.go</td></tr><tr><td>Dùng float làm tròn sai ở biên</td><td>đọc mã + test biên</td><td>verdict không ổn định</td><td>Bắt buộc integer-safe math (DEC-DEAL-03), test <code>current_bang_97</code></td></tr><tr><td>Tràn số khi nhân <code>*115</code> / <code>*102</code></td><td>giá tối đa &lt;&lt; int64</td><td>tràn lý thuyết</td><td>Giá VND thực tế &lt;&lt; 9,2e18 nên an toàn; ghi chú giới hạn</td></tr><tr><td>Ngưỡng cần tinh chỉnh theo ngành hàng</td><td>phản hồi người dùng</td><td>phân loại lệch một số ngành</td><td>Hoãn sang ML FR-DEAL-004/005, giữ ba hằng cố định ở v1</td></tr><tr><td>Chuỗi toàn giá bằng nhau (phẳng)</td><td>test <code>flat</code></td><td>p10 = median90 = min, dễ ra SALE_XIN giả</td><td>Logic đúng theo §3.5; tài liệu hóa hành vi chuỗi phẳng</td></tr><tr><td>Slice <code>hist</code> bị sửa ngoài ý muốn</td><td>test percentile</td><td>nhiễm bẩn caller</td><td><code>percentile</code> copy trước khi sort (hàm thuần)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li><code>DetectFakeSale</code> là tính năng đinh của SănDeal - phân biệt với một công cụ theo dõi giá thuần (so §5.6 BeeCost).</li><li>Ba hằng <code>1.15 / 0.97 / 1.02</code> là load-bearing: đổi chúng là đổi định nghĩa sản phẩm, phải sửa kèm §3.5 và audit.</li><li>Integer-safe math trên BIGINT VND đảm bảo verdict tất định, đồng bộ DEC-PRICE-05 của FR-PRICE-002.</li><li>Trung vị ít nhạy với ngoại lai hơn trung bình, nên median90 là mốc tham chiếu ổn định cho chuỗi giá có nhiễu.</li><li>UNKNOWN không phải lỗi mà là câu trả lời trung thực khi thiếu dữ liệu, đồng thời là điểm bàn giao sạch cho FR-DEAL-002.</li><li>Gói <code>fakesale</code> cố ý không chạm DB: hàm thuần trên <code>[]int64</code>, dễ test và tái dùng cho mô phỏng/backtest sau này.</li></ul>\n<hr>\n<p><em>Hết FR-DEAL-001. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
