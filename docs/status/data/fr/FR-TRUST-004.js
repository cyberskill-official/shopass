window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-TRUST-004"]="<h2>§1 - Mô tả (BCP-14 normative)</h2>\n<p>Service TRUST <strong>MUST</strong> cung cấp một engine chống gian lận chấm điểm rủi ro từ ba lớp tín hiệu (velocity, đồ thị quan hệ, luật ngưỡng), gắn cờ giải thích được để điều tra, và HOLD reward khi rủi ro cao - KHÔNG tự khóa/tịch thu mù. Hợp đồng:</p>\n<ol><li>Engine <strong>MUST</strong> sản sinh <code>risk_score</code> (0-100) cho mỗi chủ thể (user/referral event/conversion) và <strong>MUST</strong> lưu <code>fraud_signal</code> kèm <code>reasons[]</code> giải thích vì sao điểm đó (DEC-TRUST-19). Không quyết định hộp đen.</li><li>Engine <strong>MUST NOT</strong> tự khóa hay tịch thu tài khoản/điểm khi <code>risk_score</code> cao; nó CHỈ gắn cờ + HOLD reward để con người hoặc luồng điều tra (FR-TRUST-005) quyết (DEC-TRUST-16). False positive khóa nhầm user thật là tổn hại nghiêm trọng.</li><li><strong>MUST</strong> có lớp velocity (<code>velocity.go</code>): đếm sự kiện trong cửa sổ thời gian trượt (số signup từ cùng nguồn referral, số redeem reward, số conversion) và so ngưỡng cấu hình; burst bất thường -&gt; tín hiệu (DEC-TRUST-17a).</li><li><strong>MUST</strong> có lớp đồ thị quan hệ (<code>graph.go</code>): dựng cạnh <code>account_link_edge</code> giữa tài khoản liên kết qua referral chung / thanh toán chung / (sau này) thiết bị chung (FR-TRUST-006); cụm dày/lớn bất thường -&gt; tín hiệu (DEC-TRUST-17b).</li><li><strong>MUST</strong> có lớp luật ngưỡng (<code>rules.go</code>) CẤU HÌNH được - không hard-code; ngưỡng và trọng số tín hiệu nằm ở cấu hình để tinh chỉnh theo dữ liệu thật (DEC-TRUST-17c).</li><li><code>score.go</code> <strong>MUST</strong> tổ hợp ba lớp thành <code>risk_score</code> minh bạch (mỗi tín hiệu đóng góp ghi trong <code>reasons[]</code>); khi <code>risk_score</code> vượt ngưỡng HOLD, reward liên quan (referral reward FR-BILL-004) <strong>MUST</strong> chuyển trạng thái HOLD (nối FR-TRUST-005), KHÔNG tự tịch thu (DEC-TRUST-20).</li><li>Engine <strong>MUST</strong> làm việc trên dữ liệu đã có: <code>referral_code</code>/attribution (FR-BILL-004), <code>affiliate_click</code>/<code>affiliate_conversion</code> (FR-AFFIL-001); device fingerprint là FR-TRUST-006 (tách riêng) và engine tiêu thụ khi có, không tự thu thập thô (DEC-TRUST-18).</li><li><code>fraud_signal</code> <strong>MUST</strong> có vòng đời trạng thái: <code>open</code> (mới gắn cờ) -&gt; <code>investigating</code> -&gt; <code>confirmed_fraud</code> | <code>cleared</code>; quyết định cuối do điều tra, không do engine tự chuyển sang <code>confirmed_fraud</code>.</li><li>Engine <strong>MUST</strong> idempotent khi chấm lại cùng chủ thể với cùng dữ liệu (không nhân bản <code>fraud_signal</code>); cập nhật điểm khi có tín hiệu mới.</li><li><strong>MUST</strong> đảm bảo công bằng cơ bản: một user lẻ hành vi bình thường (không cụm, không burst) <strong>MUST</strong> có <code>risk_score</code> thấp và KHÔNG bị cờ - tránh quấy nhiễu user thật.</li><li>Mọi tín hiệu/điểm <strong>MUST</strong> không nhúng PII sàn thật; chủ thể tham chiếu qua FK nội bộ (<code>app_user.id</code>) nhất quán chính sách tối thiểu hóa (FR-TRUST-002).</li><li>Engine <strong>MUST</strong> phát metric: <code>fraud_signal_open_total{kind}</code>, <code>reward_hold_total</code>, <code>risk_score</code> histogram - để giám sát tỷ lệ gắn cờ và hiệu chỉnh ngưỡng.</li></ol>\n<hr>\n<h2>§2 - Vì sao thiết kế này (rationale cho người đọc)</h2>\n<p><strong>Vì sao chấm điểm + gắn cờ thay vì auto-ban (DEC-TRUST-16)?</strong> Gian lận và hành vi thật đôi khi giống nhau bề mặt (nhiều người nhà cùng dùng một referral, một quán net nhiều máy). Auto-ban theo điểm sẽ khóa nhầm user thật - tổn hại niềm tin, ngược hẳn định vị của SănDeal. Chấm điểm + gắn cờ để điều tra giữ con người trong vòng quyết định ở các ranh giới mờ, và chỉ HOLD (không tịch thu) cho tới khi rõ.</p>\n<p><strong>Vì sao ba lớp tín hiệu (DEC-TRUST-17)?</strong> Một lớp đơn dễ bị lách. Velocity bắt burst (cày nhanh), nhưng kẻ gian có thể rải chậm. Đồ thị quan hệ bắt cụm tài khoản liên kết dù rải chậm. Luật ngưỡng cho phép thêm heuristic cụ thể. Ba lớp bổ sung nhau: cùng nhau khó lách hơn nhiều so với từng cái.</p>\n<p><strong>Vì sao đồ thị quan hệ (DEC-TRUST-17b)?</strong> Farm tài khoản ảo không lẻ - chúng liên kết qua referral chung, thanh toán chung, (sau này) thiết bị chung. Một tài khoản đơn trông vô hại; nhìn ở mức cụm, mật độ liên kết bất thường lộ ra. Dựng đồ thị cạnh rồi đo kích thước/mật độ cụm là cách thấy cái mà nhìn từng tài khoản không thấy.</p>\n<p><strong>Vì sao ngưỡng cấu hình được (DEC-TRUST-17c)?</strong> Ngưỡng đúng phụ thuộc dữ liệu thật và đổi theo thời gian (mùa sale, chiến dịch referral). Hard-code làm không tinh chỉnh được mà không deploy. Đặt ngưỡng/trọng số ở cấu hình để chỉnh nhanh theo tỷ lệ false positive/negative quan sát qua metric.</p>\n<p><strong>Vì sao reasons[] giải thích được (DEC-TRUST-19)?</strong> Điều tra viên cần biết VÌ SAO một chủ thể bị cờ để quyết đúng. Hộp đen (\"điểm 87, tin máy đi\") không điều tra được và dễ sai. Lưu đóng góp từng tín hiệu (velocity X vượt ngưỡng, thuộc cụm Y kích thước Z) làm quyết định kiểm tra được và công bằng.</p>\n<p><strong>Vì sao tách device fingerprint sang FR-TRUST-006 (DEC-TRUST-18)?</strong> Fingerprint là vấn đề riêng (thu thập + chuẩn hóa + quyền riêng tư) đủ lớn để tách FR. Engine này tiêu thụ tín hiệu fingerprint như một loại cạnh đồ thị khi FR-TRUST-006 sẵn sàng, nhưng không tự làm fingerprint thô - giữ ranh giới rõ và thứ tự phụ thuộc đúng.</p>\n<hr>\n<h2>§3 - Hợp đồng API / DDL</h2>\n<h3>Migrations</h3>\n<pre><code class=\"language-sql\">-- services/trust/migrations/0001_fraud_signal.sql\nCREATE TABLE fraud_signal (\n  id              BIGSERIAL PRIMARY KEY,\n  subject_user_id BIGINT      NOT NULL REFERENCES app_user(id),\n  kind            TEXT        NOT NULL,        -- 'velocity' | 'graph' | 'rule'\n  risk_score      SMALLINT    NOT NULL CHECK (risk_score BETWEEN 0 AND 100),\n  reasons         JSONB       NOT NULL,        -- [{signal, detail, contribution}] - giải thích\n  status          TEXT        NOT NULL DEFAULT 'open'\n                  CHECK (status IN ('open','investigating','confirmed_fraud','cleared')),\n  created_at      TIMESTAMPTZ NOT NULL DEFAULT now(),\n  updated_at      TIMESTAMPTZ NOT NULL DEFAULT now(),\n  UNIQUE (subject_user_id, kind)               -- idempotent: cập nhật, không nhân bản\n);\nCREATE INDEX idx_fraud_open ON fraud_signal (status, risk_score DESC) WHERE status = 'open';\n\n-- services/trust/migrations/0002_account_link_edge.sql\nCREATE TABLE account_link_edge (\n  a_user    BIGINT NOT NULL REFERENCES app_user(id),\n  b_user    BIGINT NOT NULL REFERENCES app_user(id),\n  link_type TEXT   NOT NULL,                   -- 'referral' | 'payment' | 'device'(FR-TRUST-006)\n  weight    REAL   NOT NULL DEFAULT 1.0,\n  PRIMARY KEY (a_user, b_user, link_type),\n  CHECK (a_user &lt; b_user)                      -- cạnh vô hướng chuẩn hóa (tránh trùng đảo chiều)\n);\nCREATE INDEX idx_edge_b ON account_link_edge (b_user, link_type);</code></pre>\n<h3>score.go (tổ hợp tín hiệu -&gt; risk_score + reasons)</h3>\n<pre><code class=\"language-go\">// services/trust/internal/fraud/score.go\ntype Reason struct {\n    Signal       string `json:\"signal\"`\n    Detail       string `json:\"detail\"`\n    Contribution int    `json:\"contribution\"` // điểm cộng vào risk_score\n}\n\ntype Assessment struct {\n    UserID    int64\n    RiskScore int       // 0-100\n    Reasons   []Reason\n}\n\n// Assess tổ hợp ba lớp; KHÔNG khóa, chỉ chấm điểm + giải thích.\nfunc (e *Engine) Assess(ctx context.Context, userID int64) (Assessment, error) {\n    var reasons []Reason\n    score := 0\n    if v := e.velocity(ctx, userID); v.Triggered { score += v.Weight; reasons = append(reasons, v.Reason) }\n    if g := e.graphCluster(ctx, userID); g.Triggered { score += g.Weight; reasons = append(reasons, g.Reason) }\n    if r := e.rules(ctx, userID); r.Triggered { score += r.Weight; reasons = append(reasons, r.Reason) }\n    if score &gt; 100 { score = 100 }\n\n    a := Assessment{UserID: userID, RiskScore: score, Reasons: reasons}\n    if score &gt;= e.cfg.HoldThreshold {\n        e.holdRewards(ctx, userID)   // HOLD (FR-TRUST-005), KHÔNG tịch thu\n    }\n    return a, e.upsertSignal(ctx, a) // idempotent upsert\n}</code></pre>\n<h3>graph.go (cụm tài khoản liên kết)</h3>\n<pre><code class=\"language-go\">// services/trust/internal/fraud/graph.go\n// clusterSize đếm tài khoản trong cùng cụm liên kết (BFS qua account_link_edge).\nconst clusterSQL = `\nWITH RECURSIVE reach(uid) AS (\n  SELECT $1\n  UNION\n  SELECT CASE WHEN e.a_user = r.uid THEN e.b_user ELSE e.a_user END\n  FROM account_link_edge e JOIN reach r ON r.uid IN (e.a_user, e.b_user)\n)\nSELECT count(*) FROM reach;`</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>Assess(userID)</code> trả <code>risk_score</code> 0-100 + <code>reasons[]</code> không rỗng khi có tín hiệu; lưu <code>fraud_signal</code>.</li><li><code>risk_score</code> cao <strong>KHÔNG</strong> tự khóa/tịch thu; chỉ gắn cờ + HOLD reward (kiểm: không có code đặt user status='banned' trong engine).</li><li>Velocity: burst signup từ cùng nguồn referral vượt ngưỡng trong cửa sổ -&gt; tín hiệu velocity, điểm tăng.</li><li>Velocity: nhịp signup/redeem bình thường -&gt; không tín hiệu, điểm thấp.</li><li>Đồ thị: cụm dày tài khoản liên kết (referral + payment chung) -&gt; tín hiệu graph; kích thước cụm trong <code>reasons[]</code>.</li><li>Đồ thị: user lẻ không liên kết -&gt; cluster size 1 -&gt; không tín hiệu.</li><li>Ngưỡng cấu hình: đổi <code>HoldThreshold</code> trong config -&gt; hành vi HOLD đổi tương ứng, không sửa code.</li><li><code>risk_score &gt;= HoldThreshold</code> -&gt; reward referral (FR-BILL-004) chuyển HOLD; KHÔNG có nhánh tịch thu.</li><li><code>reasons[]</code> giải thích được: mỗi tín hiệu có <code>signal</code> + <code>detail</code> + <code>contribution</code>.</li><li>Idempotent: <code>Assess</code> lại cùng user cùng dữ liệu -&gt; không nhân bản <code>fraud_signal</code> (UNIQUE), cập nhật điểm.</li><li>User thật (không cụm, không burst) -&gt; <code>risk_score</code> thấp, không cờ (công bằng cơ bản).</li><li>Không PII sàn trong <code>fraud_signal</code>/<code>account_link_edge</code>; metric <code>fraud_signal_open_total</code>/<code>reward_hold_total</code> hoạt động.</li></ol>\n<hr>\n<h2>§5 - Kiểm thử (verification)</h2>\n<pre><code class=\"language-go\">// services/trust/internal/fraud/velocity_test.go\nfunc TestVelocity_BurstSignup_Flags(t *testing.T) {\n    e, src := setupEngine(t)\n    for i := 0; i &lt; 50; i++ { seedSignup(t, e, fromReferral(src)) } // 50 signup/giờ cùng nguồn\n    a, _ := e.Assess(ctx, anyUserIn(src))\n    require.Greater(t, a.RiskScore, 0)\n    require.Contains(t, reasonSignals(a), \"velocity\")\n}\n\nfunc TestVelocity_NormalPace_Clean(t *testing.T) {\n    e, _ := setupEngine(t)\n    u := seedNormalUser(t, e) // vài hành vi rải đều\n    a, _ := e.Assess(ctx, u)\n    require.Equal(t, 0, a.RiskScore)\n}</code></pre>\n<pre><code class=\"language-go\">// services/trust/internal/fraud/graph_test.go\nfunc TestGraph_DenseCluster_Flags(t *testing.T) {\n    e := setupEngine(t)\n    cluster := seedLinkedAccounts(t, e, 20) // 20 tài khoản liên kết referral+payment\n    a, _ := e.Assess(ctx, cluster[0])\n    require.Contains(t, reasonSignals(a), \"graph\")\n    require.GreaterOrEqual(t, clusterSizeInReasons(a), 20)\n}\n\nfunc TestGraph_SoloUser_NoFlag(t *testing.T) {\n    e := setupEngine(t)\n    u := seedSoloUser(t, e)\n    a, _ := e.Assess(ctx, u)\n    require.NotContains(t, reasonSignals(a), \"graph\")\n}</code></pre>\n<pre><code class=\"language-go\">// services/trust/internal/fraud/score_test.go\nfunc TestScore_HighRisk_HoldsNotSeizes(t *testing.T) {\n    e := setupEngineWithThreshold(t, 60)\n    u := seedFraudLikeUser(t, e) // burst + cụm dày\n    a, _ := e.Assess(ctx, u)\n    require.GreaterOrEqual(t, a.RiskScore, 60)\n    require.True(t, rewardOnHold(t, e, u))      // HOLD\n    require.False(t, accountBanned(t, e, u))    // KHÔNG tự khóa\n}</code></pre>\n<hr>\n<h2>§6 - Khung triển khai</h2>\n<p>Xem §3. Thứ tự: migration 0001 (<code>fraud_signal</code>) + 0002 (<code>account_link_edge</code>) -&gt; <code>velocity.go</code> (đếm cửa sổ trượt) -&gt; <code>graph.go</code> (cụm liên kết qua recursive CTE) -&gt; <code>rules.go</code> (luật ngưỡng cấu hình) -&gt; <code>score.go</code> (tổ hợp ba lớp + reasons[] + HOLD reward) -&gt; tests. Engine đọc dữ liệu từ FR-BILL-004 (referral) + FR-AFFIL-001 (affiliate); cạnh <code>device</code> được FR-TRUST-006 ghi vào <code>account_link_edge</code> khi sẵn sàng. HOLD reward gọi sang FR-TRUST-005 (delay payout). Quyết định cuối (<code>confirmed_fraud</code>/<code>cleared</code>) do luồng điều tra, không do engine.</p>\n<hr>\n<h2>§7 - Phụ thuộc</h2>\n<ul><li><strong>FR-BILL-004</strong> - <code>referral_code</code> + attribution là nguồn tín hiệu referral abuse + cạnh <code>referral</code> của đồ thị.</li><li><strong>FR-AFFIL-001</strong> - <code>affiliate_click</code>/<code>affiliate_conversion</code> là nguồn tín hiệu velocity conversion + nối FR-TRUST-005.</li><li><strong>FR-TRUST-005 (downstream)</strong> - delay payout affiliate; HOLD reward khi <code>risk_score</code> cao gọi sang luồng này.</li><li><strong>FR-TRUST-006 (downstream)</strong> - device fingerprint ghi cạnh <code>device</code> vào <code>account_link_edge</code>; engine tiêu thụ.</li><li><strong>FR-TRUST-002</strong> - chính sách tối thiểu hóa; tín hiệu/đồ thị không nhúng PII sàn, dùng FK nội bộ.</li><li>Hạ tầng: PostgreSQL recursive CTE cho đồ thị; nếu cụm lớn, cân nhắc bảng/đồ thị chuyên dụng sau (xem §9).</li></ul>\n<hr>\n<h2>§8 - Payload ví dụ</h2>\n<h3>fraud_signal được tạo (giải thích được)</h3>\n<pre><code class=\"language-json\">{\n  \"subject_user_id\": 88231, \"kind\": \"graph\", \"risk_score\": 74, \"status\": \"open\",\n  \"reasons\": [\n    { \"signal\": \"velocity\", \"detail\": \"37 redeem reward trong 1 giờ (ngưỡng 10)\", \"contribution\": 34 },\n    { \"signal\": \"graph\", \"detail\": \"thuộc cụm 22 tài khoản liên kết referral+payment\", \"contribution\": 40 }\n  ]\n}</code></pre>\n<h3>Hệ quả: reward referral chuyển HOLD (không tịch thu)</h3>\n<pre><code>referral_reward(user=88231) status: pending -&gt; HOLD\n  lý do: fraud_signal#5012 risk_score=74 &gt;= HoldThreshold(60)\n  chờ điều tra (FR-TRUST-005). KHÔNG tịch thu, KHÔNG khóa tài khoản.</code></pre>\n<hr>\n<h2>§9 - Câu hỏi mở</h2>\n<p>Đã chốt. Hoãn:</p>\n<ul><li>Đồ thị quy mô lớn (hàng triệu cạnh) bằng store đồ thị chuyên dụng thay recursive CTE - chuyển khi mật độ vượt khả năng Postgres; FR này dùng CTE cho quy mô khởi đầu.</li><li>Mô hình ML chấm điểm gian lận (thay luật ngưỡng) - khi đủ nhãn <code>confirmed_fraud</code>/<code>cleared</code> từ điều tra; FR này đặt nền tín hiệu + nhãn.</li><li>Tự động hóa một phần quyết định cho ca rõ ràng (điểm cực cao + cụm rất dày) - chỉ sau khi đo false positive đủ thấp; mặc định vẫn con người quyết.</li><li>Ngưỡng/ trọng số tối ưu cụ thể - hiệu chỉnh theo metric thật sau khi chạy; FR đặt cơ chế cấu hình.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Lỗi</th><th>Phát hiện</th><th>Hệ quả</th><th>Khắc phục</th></tr></thead><tbody><tr><td>Auto-ban theo điểm số</td><td>review + score_test</td><td>khóa nhầm user thật, mất niềm tin</td><td>Chỉ gắn cờ + HOLD, con người quyết (DEC-TRUST-16)</td></tr><tr><td>Quyết định hộp đen</td><td>review reasons[]</td><td>không điều tra được</td><td>Lưu reasons[] giải thích từng tín hiệu (DEC-TRUST-19)</td></tr><tr><td>Lách bằng rải chậm (né velocity)</td><td>graph test</td><td>farm qua mặt velocity</td><td>Đồ thị quan hệ bắt cụm dù rải chậm (DEC-TRUST-17b)</td></tr><tr><td>Ngưỡng hard-code</td><td>review config</td><td>không tinh chỉnh được</td><td>Ngưỡng/trọng số ở cấu hình (DEC-TRUST-17c)</td></tr><tr><td>False positive cao quấy nhiễu user thật</td><td>metric + AC #11</td><td>mất user thật</td><td>Điều tra trước khi hành động + chỉnh ngưỡng</td></tr><tr><td>Nhân bản fraud_signal khi chấm lại</td><td>UNIQUE + test idempotent</td><td>rác dữ liệu</td><td>UNIQUE(subject,kind) + upsert (§1 #9)</td></tr><tr><td>PII sàn lọt vào tín hiệu/đồ thị</td><td>review schema</td><td>vi phạm tối thiểu hóa</td><td>FK nội bộ, không PII sàn (FR-TRUST-002)</td></tr><tr><td>Tịch thu reward tự động</td><td>score_test (HOLD not seize)</td><td>hại user thật bị nghi oan</td><td>HOLD, chuyển FR-TRUST-005 (DEC-TRUST-20)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Ghi chú</h2>\n<ul><li>Nguyên tắc trung tâm: chấm điểm + gắn cờ để điều tra, KHÔNG auto-ban. Ranh giới gian lận/thật mờ, nên giữ con người trong vòng quyết định và chỉ HOLD (không tịch thu) tới khi rõ.</li><li>Ba lớp tín hiệu (velocity + đồ thị + luật) bổ sung nhau: velocity bắt burst, đồ thị bắt cụm rải chậm, luật thêm heuristic - cùng nhau khó lách.</li><li>Đồ thị quan hệ thấy cái mà nhìn từng tài khoản không thấy: một tài khoản vô hại, một cụm 22 tài khoản liên kết thì không.</li><li>reasons[] giải thích được làm mỗi quyết định kiểm tra được và công bằng - ngược với hộp đen \"tin máy đi\".</li><li>Engine tiêu thụ dữ liệu đã có (referral FR-BILL-004, affiliate FR-AFFIL-001) và cạnh device từ FR-TRUST-006 khi sẵn sàng; nó không tự thu thập fingerprint thô.</li><li>HOLD reward nối sang FR-TRUST-005 (delay payout) là cách chặn rút tiền gian lận mà không tịch thu oan - đúng best practice ngành (§5.3).</li></ul>\n<hr>\n<p><em>Hết FR-TRUST-004. Status: ready_to_implement (mục tiêu audit 10/10).</em></p>";
