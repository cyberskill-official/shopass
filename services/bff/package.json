{
  "name": "shopass-bff",
  "version": "1.0.0",
  "description": "GraphQL BFF cho web (TASK-WEB-005) - đọc-only, đứng sau gateway, ủy quyền REST",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc && cp src/schema.graphql dist/schema.graphql",
    "test": "tsc && cp src/schema.graphql dist/schema.graphql && node --test dist/__tests__/*.test.js",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@graphql-tools/schema": "^10.0.38",
    "dataloader": "^2.2.0",
    "graphql": "^16.8.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=24 <25"
  }
}
